# Архив: subscription.md

Снято 2026-09-20. Норма живёт в `.claude/rules/subscription.md`.
Здесь: классы C (процесс вокруг кода) и D (замеры, доводы, отменённое, пересказы),
КРОМЕ записей, несущих названный гейт, — те остались нормами в корпусе.

## Снятые записи (опись 2026-09-19)

- **sub-header** (класс D, вердикт «в архив») — вступление модуля, пересказ:
  «Модуль о **единой форме подписки** на изменения ресурсов — той, что заведена
  эпиком `#1016` (закрыт 2026-08-30).»
- **sub-owner-measure** (класс D, вердикт «в архив») — замер числа владельцев на
  дату, не норма: «Замер 2026-09-16: платформа **5** (`compute nlb registry
  storage vpc`, `kacho@45d1719fe8`) · служба доступа **1** (`kaname@83ddd08a`).»
- **sub-wiring-sign** (класс D, вердикт «слить с другой») — признак несработавшей
  провязки, поглощён красным полем `sub-stream-kills-timer`: «Признак того, что
  провязка НЕ сработала (и его надо уметь отличать от нормы): на покоящейся
  странице запросы идут периодически.»
- **sub-nine-steps-note** (класс D, вердикт «в архив») — итоговый пересказ
  порядка из девяти шагов: «Шагов девять, и **семь из них — провязка готового**.»
- **sub-wave-consolidation** (класс C, держатель `multi-agent-flow-orchestration.md
  §14`, вердикт «в архив») — довод про сборку, не про код сервиса: «И второе, про
  сборку: **пять столкновений из пяти были одного класса** — гейт, судящий
  правку, живёт в **другом пакете**, поэтому выборочный прогон полосы его не
  зовёт и каждая ветка по отдельности зелена.»

## Снято 2026-09-26 (ws#780): сжатие корпуса под потолок check-06 на сведении волны 0 с 771

Сведённое дерево `778` × `771` дало 221 997 знаков при потолке 200 000 (решение владельца
2026-09-19); потолок не поднимался. Ниже — ПРЕЖНИЕ редакции строк, сжатых этим изменением,
дословно: доводы, замеры и пересказы канона уходят сюда, норма (id · императив · держатель ·
red) осталась в корпусе под тем же id.

**sub-e2e-observable** — прежняя редакция:

sub-e2e-observable · утверждай наблюдаемое: в консоли изменение другим клиентом видно без перезагрузки и без опроса, строка без метки уходит из суженного списка — playwright; поток края — отказы до открытия (401, 403, 405, 429, 501 «владелец не объявлен»), кадры, возобновление по `Last-Event-ID` без потери и повтора — newman (testing-newman.md#edge-is-newman) · ui-future/e2e/specs/subscription-*; newman — ЗАВЕСТИ new-rpc-newman-case · red: проба утверждает вызов, а не наблюдаемое; поток края без newman-кейса

(второй проход того же сжатия)

**sub-new-service-order** — прежняя редакция:

sub-new-service-order · 1 миграция `<svc>_outbox`; 2 запись в writer-TX мутации; 3 `internal/subscriptionjournal/journal.go`; 4 поднять сервер фундамента на внутреннем слушателе; 5 объявить владельца краю; 6 дописать `subjects.ts`; 7 ребро в `polyrepo.md`; 8 дополнить страницу арендатора; 9 пробы: интеграционная на транзакцию, сквозная консоли на наблюдаемое, newman на поток края · internal/repohygiene/subscription*.go, journal*.go · red: пропущенный шаг
