# Архив оснастки

Снято решением владельца 2026-09-20: «в `.claude` только те данные, которые используются при
написании кода, тестировании и доставке в k8s».

Здесь лежат 13 файлов правил, чей предмет — процесс вокруг кода (трекер, ветки, PR, волна,
записки, реестр), и классы C/D базовых файлов: исторические замеры, доводы «почему так решили»,
описания отменённых устройств, пересказы соседних правил.

**Это не нормы.** Норма живёт в `.claude/rules/<имя>.md` строкой
`<id> · <императив> · <держатель> · red: <признак>`.

Архив не грузится автоматически (`.claude/settings.json` → `claudeMdExcludes`) и не судится
`docfresh` (`TRUTH_EXCLUDE`): его текст называет координаты, верные на момент снятия.

**49 норм класса A из этих файлов НЕ ПЕРЕСЕЛЕНЫ, вопреки тому, что здесь стояло до
2026-09-21.** Прежняя редакция утверждала, что они уехали в `.claude/rules/00-kacho-core.md`
§«Доставка и общее дерево». Перемер 2026-09-21: такого раздела в файле НЕТ
(`grep -c 'Доставка' .claude/rules/00-kacho-core.md` → 0; разделов в файле три —
«Что это за продукт», «Naming convention», «Non-negotiables»). Строки только ЗАГОТОВЛЕНЫ:
`tmp/rules-compression/relocated-to-core.txt`, 49 строк, ни одна из них в корпусе не стоит.
Значит у этих 49 норм сегодня нет ни держателя, ни дома, и утверждение обратного было
ложью корпуса о себе самом — ровно тем классом, ради которого архив и объявляется.

53 нормы A, держащиеся названными гейтами, здесь: механизм остался в CI, уехал только текст.

**Следствие, которое обязано быть названо.** Тела агентов продолжают ссылаться на снятые
файлы и путём `.claude/rules/<имя>.md`, и вызовом `Skill rule-<имя>`: 315 вхождений по
22 именам (замер 2026-09-21). Ссылка читается как рабочая и исполниться не может. Остаток
объявлен числом и сторожится — `scripts/rules-gate/rule-address-baseline.txt`,
`check-11-rule-address-exists-as-written.sh`: он может только сокращаться. Чем сокращать —
решение владельца: вернуть норму в корпус поимённо либо снять триггер вместе с ней.

Возврат нормы в корпус: поимённо, под потолком корпуса 200 000 символов. Запас на
2026-09-22 (сведённая голова волны `02b3e6f2` плюс полоса правдивости) — **29 584** символа;
предикат — `python3 scripts/rules-gate/corpus_ceiling.py`, строка «объём: N из 200000».
Число стареет при каждой правке корпуса и здесь не сторожится: сторожит его потолок
`scripts/rules-gate/check-06-corpus-ceiling.sh`, а не эта строка.

## hooks

- `rag-freshness.sh` — снят 2026-09-20: в `.claude/settings.json` не провязан ни в одном из 10 вызовов.
  Возврат: `git mv .claude/backup/hooks/rag-freshness.sh .claude/hooks/`.
