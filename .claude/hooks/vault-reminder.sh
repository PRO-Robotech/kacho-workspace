#!/usr/bin/env bash
# UserPromptSubmit hook — сигнал маршрутизации про vault.
#
# Вывод уходит в окно ГЛАВНОГО ПОТОКА, то есть диспетчеру, а у него нет ни Read, ни
# Bash, ни Edit. Поэтому здесь нет ни одного императива: «прочитай записку» и «обнови
# trail» он не исполнит by construction — он может только поставить полосу. Названы
# агенты, которые это закрывают; текст обязан читаться как повод завести полосу, а не
# как поручение читающему.
#
# КАНОН — `obsidian/kacho/CLAUDE.md` (решение владельца 2026-09-21). Он
# самодостаточен, и им же судит `scripts/vault-gate`. Прежняя координата нормы,
# лежавшая в корпусе, здесь СНЯТА, а не подставлена другим файлом: правило ушло
# в `.claude/backup/vault.md` 2026-09-20 вместе с двенадцатью процессными
# соседями, а по прежнему пути корпуса файла нет, `Skill` его
# не откроет, и ссылка на путь корпуса была неисполнима. Это ТРЕТИЙ раз, когда
# хук называет несуществующую координату: он печатал потолок «1-3KB», снятый
# владельцем 2026-08-05, и ссылался на раздел «Obsidian vault — обязательно»
# корневого CLAUDE.md, которого в нём нет. Класс держится теперь не вниманием, а
# `scripts/rules-gate/check-11-rule-address-exists-as-written.sh`.
#
# ХУК НИЧЕГО НЕ ОСМАТРИВАЕТ, И ЭТО СКАЗАНО ВСЛУХ В САМОМ ВЫВОДЕ. Он печатает
# один и тот же текст на каждом обращении: ни записок, ни трейлов, ни дерева он
# не читает. Без этой строки баннер читался бы как «vault проверен, находок
# нет» — то есть как утверждение о дереве, которого никто не делал.
cat <<'EOF'

📔 KACHO VAULT DISCIPLINE — сигнал, а не поручение главному потоку
─────────────────────────────────────────────────────────────────────────────
Печатается на КАЖДОМ обращении одним и тем же текстом: хук НИЧЕГО не
осматривает — ни записок, ни трейлов, ни дерева. Отсутствие находок здесь не
означает, что их нет: их не искали. Это фон, сам по себе полосы не требует.
Vault закрывают двое, и адресат зависит от момента:

  ДО кода — контекст по предмету                            → scout
    1-2 узкие записки из obsidian/kacho/{resources,rpc,packages,edges}/;
    INDEX.md — когда адрес неизвестен; KAC/issue-<N>.md — по задаче в работе.
    Нужно больше трёх записок — это уже не разведка, а пересмотр объёма.

  ПОСЛЕ работы — записка и trail                            → vault-scribe
    единственный, кто пишет в obsidian/kacho/**: resources · rpc · packages ·
    edges по затронутому и KAC/issue-<N>.md («Затронутые сущности vault»,
    PR-URL, status).

Перечень затронутого даёт не этот хук, а строка «затронуто в vault» возврата
полосы: она и есть задание для vault-scribe.

Чего не закрывает ничьё внимание: trail, не обновлённый после вливания; секрет
в записке (vault git-committed и ПУБЛИЧЕН); один предмет, изложенный дважды —
и в README, и в узкой записке.

Канон и оболочка записки: obsidian/kacho/CLAUDE.md · судит scripts/vault-gate
─────────────────────────────────────────────────────────────────────────────
EOF
