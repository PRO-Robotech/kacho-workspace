"""Полнота package tasks mapping — ОДИН предикат на все вызывающие семейства.

Приёмка §9: acceptance ID set обязан exact-set совпасть с design, tasks и
evidence plan, а «missing» даёт RED (`CG_TRACE_ID_MISSING`). Приёмка §11: «у
каждого реального caller есть one-fact injection: valid fixture проходит, тот же
input с одним invalid Change Graph fact блокируется». Отсюда предмет модуля:
вызывающий подаёт гейту tasks mapping своего package'а, и гейт обязан заметить
потерянный в нём acceptance ID — иначе caller пропустит push либо job на
изменении, чей граф уже негоден.

> [!important] Почему предикат ОБЩИЙ, а не по одному на семейство
> Диагностику `CG_TRACE_ID_MISSING` предъявляют пять кейсов, и миры у них
> структурно разные: у SDD-1-TRACE-02 эталонный набор идентификаторов приёмки
> лежит В МИРЕ (`acceptance_ids`), и сравнение множеств прямое; у четырёх
> вызывающих (WSCI, WSPP, PPRE, PCI) эталона в мире нет НИ ОДНОЙ координатой,
> поэтому «удалён существующий кейс приёмки» по самому миру неразрешим, и
> каждая полоса вынуждена ввести СВОЙ инвариант под одну и ту же диагностику.
> Два из четырёх вызывающих семейств ведёт эта полоса — значит расхождение
> между ними может быть сделано невозможным by construction, а не оставлено на
> усмотрение сведения волны, которое его не найдёт: кейсы зелёные у всех.
> Поэтому предикат живёт здесь, а `cg.wsci` и `cg.wspp` зовут его оба.
> Формулировка взята у полосы `cg.pci` — «перечень задач обязан покрыть обе
> половины инверсии рождения: базовый кейс серии и хотя бы один производный».
> Дословную идентичность реализаций установит сведение волны: чужого модуля в
> этой рабочей копии нет, и утверждать о нём я могу только формулировку.
>
> **Волна сведена, идентичность УСТАНОВЛЕНА и она не подтвердилась.** Абзац
> выше писался, когда чужих модулей не было видно; теперь они в дереве, и
> предикатов одного предмета в нём ТРИ, дающих на одном входе три разных
> ответа. Замер (перечень `["SDD-1-POST-01", "SDD-1-POST-NA-01"]` — две базы
> разных серий, то есть неполнота): здесь — «полно» (закрыто задачей #495, см.
> ниже), `cg.ppre` — «потеряно», `cg.pci` — «потеряно», но по чужой причине:
> его собственная регулярка не разбирает суффикс серии вовсе, поэтому ЗАКОННУЮ
> пару `["SDD-1-POST-NA-01", "SDD-1-POST-NA-02"]` он объявляет потерей. Три
> реализации, три разных ответа, и найти это можно было только сравнением: у
> всех троих кейсы зелёные.
>
> Сведение двух оставшихся сюда в этой правке НЕ делается, и это решение, а не
> недосмотр: у `cg.ppre` и `cg.pci` неразбираемая запись — ВЕРДИКТ («строка,
> не трассирующая ничего»), а здесь — собственный отказ («не знаю» не выдаётся
> за «нет»). Оба поведения объявлены и обоснованы своими шапками; свести их
> значит выбрать одно из двух, а это отдельный предмет со своей приёмкой.

**Инвариант.** Идентификаторы группируются по СЕРИИ, и каждая группа обязана
нести ОБЕ половины инверсии рождения:

* **базовый кейс серии** — порядковый 1. Это положительный вход, на котором
  проверка обязана молчать;
* **хотя бы один производный** — one-fact injection к нему (§11, §12), то есть
  вход, на котором она обязана падать.

Группа, потерявшая любую из половин, доказательства не несёт: держатель,
показавший один исход, неотличим от держателя, который другого показать не
может. Это ровно тот класс, ради которого приёмка требует birth inversion.

**Группа — СЕРИЯ, а не семейство.** Различие не педантское, и стоило оно
ложноотрицательного исхода (задача #495). У семейства `post` серий ДВЕ:
`SDD-1-POST-NN` и `SDD-1-POST-NA-NN`. Правила у них общие — модуль семейства
один, — а инверсия рождения СВОЯ у каждой: у серии с суффиксом свой базовый
кейс и свои производные. Сложенные в одну группу, два базовых кейса разных
серий выглядят парой «база + производный», и потеря производного не находится
ничем. Разбор, отличающий серию от семейства, живёт в `registry` (`series_of`),
и второго его места нет.

**Замер, на котором инвариант стоит** (`tests/testdata`, 196 кейсов, 33
семейства, **34 серии** — единственная серия, не совпадающая со своим
семейством, `post-na`): серий без кейса с порядковым 1 — **ноль**; серий с
единственным кейсом (для которых пара была бы невыполнима) — **ноль**; кейсов с
порядковым 1, объявляющих positive twin, — **один** (`SDD-1-DRIVER-01`, чей
twin принадлежит другому семейству, и на членстве в группе это не сказывается).
Числа названы, чтобы предикат не читался как догадка о структуре приёмки.
Предикат переписи назван в `selftest/prove.py` пробами G-WSCI-6/G-WSCI-7.

**Пустой mapping — потеря, а не чистота.** Приёмка §7 прямо запрещает vacuous
GREEN на нулевой переписи: «Active package с 0 acceptance IDs → RED». Package,
подавший ноль ID, не подал ни одной половины.

**Дубликат половиной не является.** Один и тот же ID, названный дважды, второго
члена пары не даёт, поэтому члены группы считаются по идентификатору.

**Сироту здесь не ищут.** Обратная сторона — ID, которого в приёмке нет вовсе, —
это `CG_TRACE_ID_ORPHAN`, и она принадлежит семейству трассы (SDD-1-TRACE-03).
Заводить её здесь значило бы объявить диагностику, которую не предъявит ни один
кейс вызывающих семейств, — то есть код без пробы.

**Грамматика идентификатора — у `registry`, и второго её места нет.** Разбор
ведут `registry.series_of` и `registry.ordinal_of` — те же три поля одного
образца, которым harness выводит семейство из ID. Здесь стояла СВОЯ регулярка
на порядковый номер («читается одно поле уже проверенного идентификатора») —
то есть ровно то второе место, от которого этот абзац предостерегал. Оно снято:
поля берутся у `registry`, и разойтись им негде.

**Неразбираемый идентификатор поднимает СОБСТВЕННЫЙ ОТКАЗ, а не вердикт.**
Строка, не являющаяся идентификатором кейса, означает, что испытуемый не
понимает поданного, а «не знаю» никогда не выдаётся за «нет».
"""

from . import outcome
from . import registry

# Порядковый номер базового кейса серии — первой половины инверсии рождения.
SERIES_BASE_ORDINAL = 1


def lost_acceptance_id(mapping):
    """Потеряна ли в поданном package tasks mapping половина инверсии рождения.

    `mapping` — то, что вернул `world.read_all(...)`: последовательность
    идентификаторов кейсов.
    """
    if not isinstance(mapping, (list, tuple)):
        raise outcome.SelfFailure(
            outcome.SELF_WORLD_MALFORMED,
            "package tasks mapping обязан быть последовательностью "
            "идентификаторов кейсов, получено %s" % type(mapping).__name__,
        )
    if not mapping:
        return True
    groups = {}
    for case_id in mapping:
        series = registry.series_of(case_id)
        groups.setdefault(series, {})[str(case_id)] = registry.ordinal_of(case_id)
    for members in groups.values():
        base_present = SERIES_BASE_ORDINAL in members.values()
        derived_present = len(members) > 1
        if not (base_present and derived_present):
            return True
    return False
