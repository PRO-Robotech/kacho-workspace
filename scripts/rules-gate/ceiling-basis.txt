# ОСНОВАНИЕ КАЖДОГО ДЕЙСТВУЮЩЕГО ПОТОЛКА. Заведено 2026-09-21 вместе с check-10.
#
# ЗАЧЕМ. Потолок базы и протокола — единственное, что удерживает текст, который
# читается при КАЖДОМ старте, от накопления «дешевле дописать сюда, чем завести
# правило». Само число живёт в одном месте — `BUDGET` в check-02. Но число без
# основания поднимается одной строкой, и подъём неотличим от опечатки.
#
# ИЗМЕРЕНО 2026-09-21: за всю историю потолок объявлялся ДВАЖДЫ и поднимался ОДИН
# раз. Установка (b3f75bfc, 2026-09-18) оставила запас 7 762 Б у базы (8,1 %) и
# 1 848 Б у протокола (15,4 %). Единственный подъём (abc5b828, 2026-09-20)
# оставил 131 Б (0,13 %) и 604 Б (4,8 %) — в шестьдесят раз теснее установки, и
# его собственный текст это признаёт: «Запас до потолка остался ТОТ ЖЕ, 131 Б».
# Подъём, отмеренный по размеру написанного, — не решение, а оформление факта:
# следующая норма потребует следующего подъёма в тот же день.
#
# ФОРМА СТРОКИ — шесть полей через `|`:
#   <файл> | <потолок> | <дата> | <установка|подъём> | <предмет> | <что невозможно без него>
#
# ТРИ ТРЕБОВАНИЯ К ПОДЪЁМУ (их исполняет check-10):
#   1. потолок здесь обязан СОВПАДАТЬ с `BUDGET` в check-02. Разошлись — значит
#      число подняли, а основание осталось от прежнего: основание, пережившее
#      свой подъём, — находка;
#   2. запас после подъёма обязан быть НЕ МЕНЬШЕ медианного абзаца этого файла
#      (правило «запас на абзац, но не на раздел», медиана берётся обходом, а не
#      назначается). Подъём ровно по размеру написанного этого не выполняет;
#   3. подъём и правка, ради которой он сделан, — ОДНО изменение. Подъём «про
#      запас» отдельным изменением не принимается: предмет называется здесь, и
#      без предмета строка не проходит поле 5.
CLAUDE.md | 12000 | 2026-09-18 | установка | общий протокол, читаемый ОБОИМИ концами — диспетчером и каждым исполнителем | без потолка протокол накапливает нормы продукта, место которым в правилах, и растёт у всех сразу
.claude/agents/dispatcher.md | 96000 | 2026-09-18 | установка | база маршрутизации, заменяющая системный промпт главного потока | без потолка база принимает абзац, который дешевле дописать сюда, чем завести правило, и читается он при каждом старте
