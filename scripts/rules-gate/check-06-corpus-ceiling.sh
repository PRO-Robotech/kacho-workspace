#!/usr/bin/env bash
# check-06 — ОБЪЁМ (НАБЛЮДЕНИЕМ) И ФОРМА КОРПУСА (НАХОДКОЙ).
#
# Объём печатается и НЕ блокирует с 2026-09-22: предел судил СУММУ корпуса, а её
# не грузит никто — замер дал максимум по агенту 85 559 при сумме 197 784, и
# предел оказался анти-коррелирован с вредом. Решает теперь величина с адресатом,
# `check-13-agent-window-load.sh`. Прежний предел 200 000 остаётся ориентиром в
# печати, и то, что он был ВЫБРАН, а не выведен, сказано вслух в шапке разборщика.
# Блокируют здесь ФОРМА строки-нормы и ОТКАЗ по непрочитанному дереву.
#
# Вердикт — в КОДЕ ВЫХОДА: 0 молчит, 1 находка формы, 2 отказ по беспредметному обходу.
#
# Разборщик берётся РЯДОМ С СОБОЙ, а дерево — из корня: инъекция гоняет проверку
# на КОПИИ дерева, куда `scripts/` не копируется.
set -euo pipefail
SELF_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$(git rev-parse --show-toplevel)"
exec python3 "$SELF_DIR/corpus_ceiling.py"
