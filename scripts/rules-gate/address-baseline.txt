# БАЗА ИЗВЕСТНЫХ ВИСЯЧИХ АДРЕСОВ — снята 2026-09-20 при заведении check-07.
#
# Это НЕ исключения по вкусу, а названный числом долг ЧУЖОГО репозитория. Все 12
# адреса ниже стоят в `PRO-Robotech/kacho` (`internal/repohygiene/**`): гейты
# продукта цитируют разделы правил по координатам, оставшимся от прошлых разрезов
# корпуса — `security.md` -> `security-hardening.md`/`security-disclosure.md`,
# `testing.md` -> `testing-verdict.md`/`testing-newman.md`, `multi-agent-flow.md`
# -> `multi-agent-flow-shared-tree.md`/`-orchestration.md`. Правятся PR в продукт,
# не здесь: этот репозиторий их файлов не содержит.
#
# Ни один не создан выносом легаси: до сужения предмета и нормализации разбора их
# насчитывалось 247, из них 228 оказались ошибками САМОГО гейта (перенос строки в
# Go-комментарии, «§» внутри заголовка, типографские кавычки, тире, многоточие
# сокращения, цель-утверждение вместо заголовка). Каждая снята предикатом, а не
# исключением; остаток — настоящий.
#
# ПРАВИЛО: база только СОКРАЩАЕТСЯ. check-07 краснеет в двух случаях —
#   (1) висячий адрес, которого в базе нет: новая поломка;
#   (2) адрес из базы, который ТЕПЕРЬ резолвится: зажил, обязан быть убран отсюда.
# Второе и есть самоистечение: база не может пережить свой предмет.
multi-agent-flow.md §13
security.md §6
security.md §Контроль, действующий на ВЫДАЧЕ…
security.md §Контроль, у которого нет МЕХАНИЗМА исполниться
security.md §Механического детектора сборки НЕТ
security.md §Публичные артефакты
testing.md §Fixture-seed обязан проверять `op.error`
testing.md §Fixture-seed обязан проверять `op.error` перед извлечением resource-id из `metadata`
testing.md §Fixture-seed обязан проверять `op.error`…
testing.md §Диагноз ставится по ТЕКСТУ отказа
testing.md §Проба не имеет права падать на достижении своей цели
testing.md §Фикстура пробы, привязанная к снимаемому предмету
