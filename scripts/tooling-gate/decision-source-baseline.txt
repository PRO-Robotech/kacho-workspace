# БАЗА ИЗВЕСТНЫХ СТРОК §12 БЕЗ ИСТОЧНИКА — снята 2026-09-21 при заведении check-13.
#
# §12 базы маршрутизации разрешает противоречия «по более позднему решению,
# С ИСТОЧНИКОМ» — это её собственная преамбула. Источником считается ДАТА либо
# координата правила (`файл.md`). Ссылка на соседний раздел той же базы (§7, §8,
# §11) источником НЕ считается: §12 судит, какое из двух чтений ПОЗДНЕЕ, а
# сосед по документу о старшинстве не говорит ничего — он сам такой же текст.
#
# Строки ниже стояли без источника на момент заведения проверки. Они НЕ
# исключения по вкусу: у каждой решение принято, но не записано, кем и когда.
# Найти основание задним числом может только тот, кто его принимал.
#
# ПРАВИЛО: база только СОКРАЩАЕТСЯ. check-13 краснеет в двух случаях —
#   (1) строка без источника, которой в базе нет: новая находка;
#   (2) строка из базы, которая ТЕПЕРЬ источник называет: зажила, обязана быть
#       убрана отсюда.
# Второе и есть самоистечение: база не может пережить свой предмет.
#
# ЧИСЛА СТРОК ЗДЕСЬ НЕТ НАМЕРЕННО: ведомость только сокращается, и любое
# выписанное число станет ложным при ПЕРВОМ же законном сокращении, а ловить его
# нечем. Действующее число печатает ПЕРЕПИСЬ check-13 на каждом прогоне.
#
# Строка «Ролей автора замысла… НЕ заводим» снята с базы в тот же
# день: решение принял диспетчер 2026-09-21, и оно записано с датой, автором и
# основанием. Она и была поводом завести проверку.
# КЛЮЧ СТРОКИ — ровно то, что производит сам предикат: первые 60 знаков части
# ДО стрелки. Сверка ТОЧНАЯ, поэтому ключ нельзя дописывать и нельзя укорачивать:
# и то и другое сделало бы запись негодной, а не более общей.
Разбор классов до приёмки против отпечатка ОДОБРЕННОЙ приёмк
«Когда» локального прогона сводилось к MR в ствол
Багфикс допускал `rpc-implementer`, чьё условие требует APPR
«Честный красный» против миграции
35–40 минут против медианы 14 минут
