#!/usr/bin/env bash
# ПРОБА ИНЪЕКЦИИ для PRO-Robotech/kacho#514 — файл ВРЕМЕННЫЙ и снимается
# следующим коммитом этой же ветки.
#
# Предмет пробы: доказать, что обязательная проверка, ставшая красной, ОТМЕНЯЕТ
# предложение слить. Ниже — намеренная ошибка shellcheck (неэкранированная
# подстановка), от которой краснеет джоба `bats-and-shellcheck`.
x=$1
if [ $x = 1 ]; then echo "инъекция"; fi
