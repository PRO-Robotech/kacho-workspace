#!/usr/bin/env bash
# check-03 — регенерация детерминирована.
#
# ЧТО УТВЕРЖДАЕТ. Два прогона генератора на неизменных входах дают побайтово
# одинаковый результат.
#
# ПОЧЕМУ ЭТО ОТДЕЛЬНАЯ ПРОВЕРКА, А НЕ СЛЕДСТВИЕ check-02. Недетерминизм (порядок
# обхода каталога, зависимость от локали, отметка времени, случайный ключ) даёт
# расхождение ЧЕРЕЗ РАЗ. Тогда check-02 краснеет на исправном дереве и зеленеет
# на испорченном — то есть вердикт становится функцией удачи, а не дерева, и
# первый же ложный срабат отключает набор целиком.
#
# Коды выхода: 0 — осмотрено N, находок 0; 1 — находка; 2 — сверять не с чем.
set -uo pipefail
# shellcheck source=_lib.sh
. "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_lib.sh"
adapter_gate_run "check-03-regeneration-deterministic" "determinism" "два прогона генератора сверены между собой"
