#!/usr/bin/env bash
# check-04 — в порождённом нет ЗАГЛАВНОЙ формы имени каталога второй среды.
#
# ЧТО УТВЕРЖДАЕТ. Ни один объявленный выход не содержит имени второй среды,
# записанного с заглавной буквы.
#
# ПОЧЕМУ ЭТО ПРЕДИКАТ, А НЕ ВЫЧИТКА. Заглавная форма не является опечаткой: она
# рождается слепой подстановкой по имени и приходит сразу сотнями. Замер по
# дереву до заведения генератора: 141 вхождение в 25 файлах. Такое количество
# вычиткой не ловится ни при какой внимательности.
#
# Коды выхода: 0 — осмотрено N, находок 0; 1 — находка; 2 — сверять не с чем.
set -uo pipefail
# shellcheck source=_lib.sh
. "$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/_lib.sh"
adapter_gate_run "check-04-canonical-case" "canonical-case" "порождённые прочитаны на заглавную форму имени второй среды"
