# WATCH-1 — единая форма подписки: план реализации (фаза Ф1)

> **Для исполнителя-агента:** обязательный суб-скил — `superpowers:subagent-driven-development`
> либо `superpowers:executing-plans`. Шаги размечены чекбоксами (`- [ ]`).
> Перед первой строкой кода — `class-exposure-analyst` (см. §0).

**Цель.** Объявить **одну** форму подписки на изменения ресурсов, переиспользуемую всеми
модулями, и поставить гейты, которые не дадут завести вторую. **Поведения не меняется нигде:**
Ф1 не производит ни сервера, ни перевода доменов, ни снятия прежней формы.

**Приёмка — единственный нормативный вход:**
`docs/specs/sub-phase-WATCH-1-unified-subscription-contract-acceptance.md` (APPROVED,
30 сценариев, 8 расхождений с постановкой). **План не заменяет приёмку и не переопределяет
её. Расхождение плана с приёмкой — дефект плана.**

**Стек.** Go 1.2x, монорепо `project/kacho` (`github.com/PRO-Robotech/kacho`);
`proto/kacho/cloud/subscription/` (новый пакет контракта) · `internal/repohygiene`
(репо-широкие гейты, разбор AST и дескрипторов, обычный `go test`) · `buf` (lint/breaking) ·
плюс **отдельный коммит в другом репозитории** — `kacho-workspace`.

**Эпик/задача:** продукт [#1016](https://github.com/PRO-Robotech/kacho/issues/1016) (Ф1) /
[#1017](https://github.com/PRO-Robotech/kacho/issues/1017). Смежные предметы, заведённые
приёмкой: [#1043](https://github.com/PRO-Robotech/kacho/issues/1043),
[#1046](https://github.com/PRO-Robotech/kacho/issues/1046).

---

## §0 Что сделать ДО первого шага

- [ ] **APPROVED от `acceptance-reviewer`** на текущей редакции приёмки — включая §10.
      Ban #1 и DoD §8 п.10. Прежняя редакция этого раздела шага не содержала вовсе, и
      анализ классов справедливо назвал это находкой (B10).
- [x] **`class-exposure-analyst`** по этому плану и приёмке — **исполнен 2026-08-23**,
      вернул десять блокирующих; вердикт по каждой — приёмка §10. Четыре меняют форму,
      поэтому план ниже правится **вместе** с приёмкой, а не после неё. Он читает замысел и отвечает двумя
      списками: какие классы дефектов замысел может задеть и что должно быть верно в коде,
      чтобы не задел. Его находки вносятся **в приёмку** (если сценарий оказался
      неконструируемым) либо **в этот план** (если меняется порядок), а не обходятся в коде.
- [ ] Завести ветку `issue-1017` **от свежей релизной**, не от `main` (`git-issues.md`
      §«Накопительная релизная ветка»). Релизная линия — `release:watch`.
- [ ] Записка trail `obsidian/kacho/KAC/issue-1017.md` со состоянием `in-progress`.

---

## §1 Global constraints — действуют на каждом шаге

| # | Ограничение | Откуда |
|---|---|---|
| 1 | **Ф1 не производит `service`/`rpc`** — только типы сообщений | приёмка §2.1 |
| 2 | Ни `buf breaking` не ломается, ни ведомость разрывов не пополняется | приёмка WATCH-1-18 |
| 3 | Прежняя форма compute **остаётся живой и обслуживаемой** до Ф3 | приёмка WATCH-1-17 |
| 4 | Каждый гейт **доказан инъекцией в обе стороны** и печатает объём осмотренного | `testing.md` §«Гейт на класс» |
| 5 | Каждое послабление **истекает само**: запись, которой нечего исключать, — находка | там же, п. 5 |
| 6 | Числа **перемеряются на текущей ревизии**, а не переносятся из приёмки | §2 ниже |
| 7 | Отсрочек нет: маркер отложенной работы роняет прогон | ban #11 |
| 8 | Имена в коде — только латиницей; комментарии и тексты — по-русски | ban #17 |

---

## §2 Как читать дерево на каждом шаге — перемер, а не перенос числа

Приёмка сверена с `origin/main` = `958e8fe0d`. **К моменту исполнения это число устареет** —
за время её написания общая копия продукта ушла на 99 коммитов. Поэтому первым делом каждого
шага:

```sh
git fetch origin main -q && git rev-parse --short origin/main    # база ЭТОГО шага
```

и все предикаты гоняются с явной ревизией (`git grep … origin/main -- …`), **не** по рабочей
копии. Числа приёмки — ориентир; расхождение перемера с ней — находка, которую надо назвать,
а не подогнать.

**Предикаты, которые понадобятся (все с областью — без неё они мерят другое):**

```sh
# форм потоковой подписки в контрактах
git grep -h 'returns (stream' origin/main -- proto | wc -l

# объявлений запроса подписки (несущий предикат Ф1)
git grep -c 'message WatchRequest\|message SubscribeRequest\|message .*SubscriptionRequest' \
  origin/main -- proto

# кто ИСПОЛНЯЕТ LISTEN (а не упоминает его)
git grep -lE '\.Exec\([^)]*"(UN)?LISTEN' origin/main \
  -- 'services/*/**/*.go' ':!*_test.go' | wc -l

# импортёры общего пакета — образец, по которому меряется «взяли ли форму»
git grep -l 'kacho/cloud/reference/reference.proto' origin/main -- proto | wc -l
```

---

## §3 Раскладка шагов — и почему порядок именно такой

```
Ш1  надгробие ─────────────┐  независим от контракта, закрывает ЖИВУЮ дыру → первым
Ш2  инъекционный стенд ────┤
Ш3+Ш4 ОДНИМ коммитом ─────┤  гейт и контракт вносятся вместе (B9):
      G1 + три типа        │  порознь окно красное и блокирует отправку
Ш5  G2 форма контракта ────┤
Ш6  G3 не мёртвая + G4 окно┤
Ш7  конвенция (др. репо) ──┤
Ш8  постановка + trail ────┘
```

**Почему надгробие первым, а не последним.** Р4 — не подготовка к работе, а **действующая
дыра**: имя снятой формы не зарезервировано, и вернуть его может любой коммит между сегодня и
Ф3. Оно не зависит ни от контракта, ни от гейтов, поэтому платить за него задержкой нечем.

**Почему гейт раньше контракта — и почему Ш3 с Ш4 идут ОДНИМ коммитом (правка по B9).**
Гейт, поставленный после, проверяет написанное и потому зелен по построению. Но прежняя
редакция называла «RED → GREEN» переход, где красное лечится **записью послабления**, — а это
доказывает работоспособность пути исключения, а не способность гейта обнаружить нарушение.
Хуже того: если ожидание выводится как «ведомость + общая форма», то между Ш3 и Ш4 общей формы
нет, гейт красный, и **хук отправки блокирует каждый коммит окна**.

Поэтому: **Ш3 и Ш4 — один коммит**, а настоящее доказательство способности падать даёт
**только стенд Ш2**, где дефект вносится по-настоящему.

**Почему конвенция предпоследней.** Она в **другом репозитории**, и её коммит не должен
уехать раньше, чем контракт, который она описывает, окажется в стволе продукта, — иначе
правило будет утверждать о дереве то, чего в нём ещё нет.

---

# Ш1 — надгробие: закрыть Р4

> **Красное до, зелёное после** — легко предъявить: проба надгробия существует и её вход
> перечислим.

### Задача 1.1: имя снятой формы внесено

- [ ] Перемерить: `git show origin/main:internal/repohygiene/retiredrpcsurface_test.go |
      grep 'FQN:' | grep -ci loadbalancer` → ожидается `0`. **Не `0` — Р4 закрыт кем-то
      другим, шаг отпадает, и это надо сказать, а не тихо пропустить.**
- [ ] Добавить запись `kacho.cloud.loadbalancer.v1.InternalResourceLifecycleService/Subscribe`
      в `retiredRPCSurface` с причиной: снята #814, потребителей не было ни одного; имя не
      должно вернуться под замысел «нам нужна подписка» — живой формой станет
      `kacho.cloud.subscription`.
- [ ] Прогнать пробы надгробия. Перепись обязана вырасти на единицу (`grep -c 'FQN:'`:
      `33 → 34`; перемерить, не переносить — ствол уже добавил запись про снятую форму
      compute, см. приёмку §1.2).

### Задача 1.2: ложная причина соседней записи исправлена

- [ ] `internal/repohygiene/retiredrpcsurface_test.go`, причина записи
      `kacho.cloud.vpc.v1.InternalResourceLifecycleService/Subscribe` содержит фразу «Живым
      остаётся `loadbalancer.v1.…`» — форма снята, утверждение ложно.
- [ ] Переписать причину: назвать форму снятой и указать, чем подписка выражается теперь.
      **Саму запись не трогать** — надгробие не истекает.
- [ ] Предикат: `git grep -c 'Живым остаётся' -- internal/repohygiene` → `0`.

**Выход Ш1:** дыра Р4 закрыта; два места об одном предмете сведены к одному.

> [!warning] Мёртвое не снимать, пока из него не вынута техника
> Задача **#1043** (читатель nlb) заведена этой же приёмкой и **помечена `blocked`**: в этом
> файле написан единственный в дереве ответ на класс потери строки при возобновлении
> (`watermark`; приёмка §10.1). Снять его до переноса значило бы уничтожить ответ, который
> Ф2 обязана перенести. Предикат снятия — **«техника перенесена»** (`watermark` в дереве не
> ноль вне `services/nlb`), а не «`LifecycleFeed` в дереве нет».
>
> Урок общий и записан в приёмке: «мёртвый код» и «код без вызывающих» — разные вещи.
> Второе есть замер, первое — суждение о ценности, и оно требует спросить, **чего в дереве
> не станет вместе с ним**.

---

# Ш2 — инъекционный стенд

> Без него все последующие гейты недоказуемы: «зелёный» будет неотличим от «не умеет падать».

### Задача 2.1: стенд, порождающий синтетическое дерево контрактов

- [ ] Завести харнесс, который строит **временный** каталог `proto` с управляемым содержимым
      и натравливает на него анализатор. **Изоляция обязательна** (`multi-agent-flow.md` §13):
      своё окружение (`GIT_DIR`/`GIT_WORK_TREE`/`GIT_INDEX_FILE`) либо явный `-C` — никакой
      записи в индекс репозитория, из которого идёт прогон.
- [ ] Стенд обязан уметь порождать **обе** стороны каждой оси, иначе он доказывает половину:

| ось | дефект (ждём красное) | законный близнец (ждём молчание) |
|---|---|---|
| единственность | второй доменный запрос подписки вне ведомости | тот же домен, но **импортирующий** общий тип |
| послабление | запись ведомости, чьего объявления в дереве нет | запись, чьё объявление есть |
| мутабельная ось | поле `name` / `labels` в общей форме | поле `ids` (иммутабельная ось) |
| позиция | скалярная позиция без различения трёх состояний | позиция, различающая три |
| мёртвая форма | тип с нулём импортёров при закрытой #1018 | тот же тип при открытой #1018 |

**Выход Ш2:** стенд предъявлен, и по каждой оси показаны обе стороны — до того, как написан
хоть один гейт.

---

# Ш3+Ш4 — ОДИН КОММИТ: гейт единственности и контракт

> **Почему один шаг, а не два.** Задачи 3.x и 4.x разделены по предмету, но **вносятся одним
> коммитом**. Разнеси их — и между коммитами существует состояние, где гейт ждёт общую форму,
> а её ещё нет: гейт красный, и хук отправки блокирует каждый коммит окна. Выводи ожидание
> только из ведомости — гейт перестанет замечать отсутствие общей формы вовсе, то есть
> получит ровно тот дефект, из-за которого забракован предикат эпика (Р1).
>
> **Доказательство способности гейта падать даёт стенд Ш2, а не это окно.** Красное,
> лечимое **записью послабления**, доказывает работоспособность пути исключения, а не
> способность обнаружить нарушение.

### Задача 3.1: ведомость послаблений с самоистечением

- [ ] Завести ведомость (структура рядом с гейтом, по образцу `retiredRPCSurface` и
      `knownInertCatalogRows`): поля `symbol`, `reason`, `expires_when`.
- [ ] Единственная запись Ф1 — `kacho.cloud.compute.v1.WatchRequest`, причина: домен
      переводится в Ф3 (#1019), снятие — после перевода.
- [ ] **Самоистечение:** запись, чьего объявления в дереве нет, — **находка**. Это отдельная
      проба, а не строчка в комментарии.

### Задача 3.2: гейт с ожидаемым значением на границе фаз

- [ ] Анализатор считает объявления запроса подписки в `proto` и сверяет с суммой
      «ведомость + общая форма».
- [ ] Ожидаемое значение — **из приёмки §4.3**, а не из головы: `2` после Ф1 и Ф3, `1` после
      снятия. Значение **выводится** из ведомости, а не выписывается константой: снял запись —
      ожидание уменьшилось само.
- [ ] Гейт печатает объём осмотренного (файлов `.proto` прочитано, объявлений найдено) и
      **падает на пустом обходе**: ноль прочитанных файлов не может дать «ноль находок».
- [ ] **Инъекция, три стороны** (стенд Ш2): доменное объявление вне ведомости → красное с
      именем файла · импорт общего типа → молчание · запись ведомости без объявления → красное.

**Выход Ш3:** гейт **красный на сегодняшнем дереве** до внесения записи о `compute` и зелёный
после. Предъявить оба состояния — это и есть RED → GREEN.

---

## Ш3+Ш4, вторая половина — контракт: типы сообщений

### Задача 4.1: пакет и файл

- [ ] `proto/kacho/cloud/subscription/subscription.proto`, пакет `kacho.cloud.subscription`,
      `go_package … /pkg/api/kacho/cloud/subscription;subscriptionv1` — раскладка **дословно**
      по образцу `kacho.cloud.reference` (ни одного `service` в файле).

### Задача 4.2: что объявляется — и что каждое поле обязано нести в комментарии

| элемент | требование приёмки |
|---|---|
| три оси фильтра — виды · проект · идентификаторы | все **необязательны**, конъюнкция; незаданная не сужает (WATCH-1-01/02) |
| **отсутствие** полей имени и меток | причина стоит **в контракте**: имя мутабельно (ban #15), метки фильтрует клиент (WATCH-1-06) |
| позиция начала | **непрозрачный токен** (кодек — `pkg/pagetoken`, назван поимённо); три различимых состояния, и **исход незаданного назван** (WATCH-1-10); обязательство «не пропускает закоммиченное после выдачи» стоит дословно (WATCH-1-31) |
| **служебное первое сообщение — отдельный тип** | несёт позицию · признак догона · **перечень честно отобранных осей** (WATCH-1-12, -13, -32) |
| **оболочка события** | носитель нагрузки **назван** типом (WATCH-1-33); **авторизуемый якорь — поле оболочки** (WATCH-1-35); пустая нагрузка не отдаётся подписчику (WATCH-1-34) |
| исход «позиция утрачена» | выразим, и «горизонт не удерживается» — законное состояние владельца (WATCH-1-11) |
| первое сообщение потока — позиция | выразимо формой, не соглашением (WATCH-1-12) |
| словарь видов | принадлежит **владельцу**, не заперт на «ресурсы домена» (WATCH-1-22) |
| ни одно поле не обязывает сообщать причину остановки нагрузкой | WATCH-1-30 |

- [ ] `buf lint` и `buf breaking` зелёные.
- [ ] Генерация стабов — **плагинами из модуля** (`go run <пакет>`), не из `PATH`
      (`git-issues.md` §обязательные проверки).
- [ ] G1 зелёный на новом состоянии: `2` при одной записи ведомости.

**Выход Ш3+Ш4 (один коммит):** контракт в дереве — три типа (запрос подписки · служебное
первое сообщение · оболочка события), гейт единственности зелёный, прежняя форма compute не
тронута, ведомость несёт одну запись и умеет истечь.

---

# Ш5 — G2: форма контракта

> Один анализатор, несколько утверждений. Дробить его на семь гейтов не нужно: предмет один —
> **форма общей подписки**, и читается она одним разбором дескрипторов.

### Задача 5.1: закрытый перечень осей и их семантика

- [ ] Перечень имён осей — рядом с гейтом, с пометкой семантики (иммутабельная / мутабельная).
- [ ] Ось **вне закрытого перечня** → красное с именем поля (WATCH-1-07).
- [ ] Ось, добавленная **вместе с записью в перечень**, → молчание.
- [ ] Запись перечня, которой в форме не отвечает ни одно поле, → **находка** (самоистечение).
- [ ] У каждой записи перечня проставлен **признак категории** — «якорная» либо «удобства»
      (WATCH-1-32, вторая отказная сторона). Гейт требует, чтобы признак **стоял**, и не
      судит, верно ли он проставлен: это смысл, а не синтаксис.

> **Правка по анализу классов.** Прежняя редакция велела гейту отличать «мутабельную
> семантику» от иммутабельной — разбором это не решается, и B3 забраковал такую проверку.
> Гейт судит **членство в перечне** и **наличие признака**, то есть делает решение видимым.

### Задача 5.2: структурные утверждения

- [ ] Все три оси **необязательны** — это несущее: на нём держится вывод §6.1 приёмки о том,
      что неотбираемая ось остаётся незаданной и ломающего изменения не возникает.
- [ ] **Исход незаданной позиции назван** контрактом (WATCH-1-10). Проверять «ветвей три»
      **нельзя**: на проводе состояний четыре, и такое утверждение зелено на форме с
      молчаливым умолчанием — B4 забраковал именно его.
- [ ] Исход «позиция утрачена» присутствует.
- [ ] Обязательного поля причины остановки в событии **нет**.
- [ ] Обязательного идентификатора ресурса нет — иначе предмет «отзыв доступа субъекта» в
      форму не помещается (WATCH-1-22).
- [ ] Перепись: полей прочитано, осей распознано, утверждений проверено.

**Выход Ш5:** форма проверяется разбором, а не вниманием ревьюера.

---

# Ш6 — ведомость транспортных сообщений и G4 «окна без подписки нет»

> Заголовок правлен: прежний называл гейт **G3**, снятый находкой B2. Заголовок, переживший
> свой предмет, — тот же класс, который этот план ловит в дереве.

### Задача 6.1: ~~G3~~ — снят; его работу делает существующая ведомость (B1, B2)

> **Правка по анализу классов.** G3 писать не нужно, и хуже — он закрывал не тот класс.
>
> **B1:** артефакт Ф1 роняет **действующий** гейт `internal/repohygiene/transportmessagereach.go`
> — его предмет дословно есть имя на `Request`/`Response`/`Metadata`, которого не называет ни
> один глагол. Ф1 объявляет ровно это, а гейт бежит под `-short`
> (`grep -c 'testing.Short()'` → **0**), то есть блокирует **отправку ветки**, не только
> конвейер.
>
> **B2:** предикат «ноль импортёров» удовлетворяется сервером, который пишет сам этот эпик,
> — то есть после Ф2 гейт вечно-зелён by construction. А похороненное имя снято за состояние
> «сервер есть, клиента нет», которого он не видит.
>
> **Исход:** запись в `transportMessageAllow` с причиной и ссылкой на задачу. Механизм уже
> несёт **обе** оси самоистечения (дерево и состояние задачи), и переиспользовать его — ровно
> то, чем занят весь эпик: один механизм вместо трёх.
>
> **Срок записи истекает на #1018, и это механика, а не выбор.** Ведомость сторожит
> «сообщение, которого не называет ни один глагол»; глагол появляется в Ф2 — значит запись
> истекает там. Прежняя редакция привязывала срок к #1021 (снятие поллинга): истечение от
> этого не изменилось бы, но **причина была бы названа неверно**, а запись с неверной
> причиной снимают не тогда и не так.
>
> Вопрос «у формы появился потребитель» — отдельный и живёт в приёмке (§7 п. 1б), а не в
> этой ведомости.

- [ ] Внести запись в `transportMessageAllow`: символы Ф1, причина, ссылка на задачу.
- [ ] **Приземлить G3 РЯДОМ, а не вместо.** Ведомость судит по **суффиксу имени**
      (`Request`/`Response`/`Metadata`) и потому видит **один** тип Ф1 из трёх; служебное
      сообщение и оболочка события ей невидимы by construction. G3 судит по **пакету** и
      покрывает все три. DoD приёмки требует его поимённо (§8 п.5) — снятие «в пользу
      ведомости» делало DoD неисполнимым.
- [ ] Убедиться, что `go test ./... -short` зелёный **до** отправки — иначе хук не пропустит.

### ~~Задача 6.1 (прежняя редакция)~~: G3 — тип с нулём импортёров

- [ ] Гейт: тип из `kacho.cloud.subscription` с **нулём** импортёров — находка, **если** #1018
      закрыта; пока открыта — молчит и **называет её номером**.
- [ ] Состояние задачи — измерение сетевое; по образцу уже работающего гейта проб консоли
      оно идёт **по явной ручке окружения**, а его состояние перепись называет на **каждом**
      прогоне, чтобы «сверено 0» никогда не выглядело как «сверено».
- [ ] Инъекция: тип без импортёров при закрытой задаче → красное; он же при открытой →
      молчание; тип с импортёром → молчание в обоих случаях.

> **Зачем гейт, если и так очевидно.** Приёмка §2.1 отвечает надгробию: похороненное имя
> отличалось от этой формы отсутствием **срока** и **предмета**. Срок и предмет, не
> подкреплённые проверкой, — обещание; этот гейт и есть то, что делает их фактом.

### ~~Задача 6.2~~: G4 — СНЯТ вместе с предметом

> Ствол снял последнюю форму подписки (`d9e976460`, kacho#813): смонтированных потоковых
> методов **ноль**. Гейт «методов подписки ≥ 1» краснел бы с первого прогона, бежит под
> `-short` и блокировал бы **отправку каждой ветки** — ровно класс B9, к которому этот план
> применил починку в Ш3+Ш4. Свойство, которое он стерёг, ствол потерял до начала фазы.

### ~~Задача 6.2 (прежняя редакция)~~: G4 — окна без подписки не существует

- [ ] Гейт: методов подписки, обслуживаемых хотя бы одним листенером, — **не менее одного**,
      на каждой ревизии ствола.
- [ ] Инъекция: снять монтирование прежней формы, не введя новой → красное.
- [ ] Гейт живёт **до конца эпика** и снимается вместе с последним переводом — у него есть
      предмет всё это время.

**Выход Ш6:** порядок фаз держится проверкой, а не памятью исполнителя.

---

# Ш7 — конвенция: ДРУГОЙ репозиторий, отдельный коммит

> **Предикат, объясняющий шаг:** `git ls-tree -r origin/main --name-only | grep -c
> api-conventions.md` в монорепо → **0**. Файл живёт только в `kacho-workspace`.

- [ ] В `kacho-workspace`: `.claude/rules/api-conventions.md`, §«Форма ресурса» — утверждение
      «**Watch RPC не существует**» привести к дереву.
- [ ] Правка идёт **вместе** с оговоркой (WATCH-1-26), иначе она читается как разрешение
      «подписывайся вместо поллинга везде»:
      - мутация по-прежнему возвращает `Operation`, исход узнаётся поллом;
      - подписки на операции не заводится;
      - **одного потока ресурсов недостаточно**: событие пишется в той же транзакции, что и
        ресурсная строка, поэтому при отказе операции события нет вовсе — наблюдатель не
        отличит «ещё делается» от «упало».
- [ ] Отдельный коммит в отдельном репозитории (`polyrepo.md` §порядок работ). Без этой строки
      шаг тихо не делается — он единственный вне монорепо.

**Держится вниманием, и это сказано в приёмке §5:** проверка свежести документации сверяет
координаты, а это утверждение — проза без координаты. Хук на нём молчит при любом состоянии
дерева.

---

# Ш8 — постановка, trail, закрытие

### Задача 8.1: расхождения внесены в задачи

- [ ] Внести восемь расхождений §1 приёмки в [#1017](https://github.com/PRO-Robotech/kacho/issues/1017)
      и [#1016](https://github.com/PRO-Robotech/kacho/issues/1016) — **комментарием**, не
      правкой тела: тело свидетельствует о том, что думали при заведении.
- [ ] **Условие DoD п.9 выполнено** (Р5 исправлен по существу) — иначе внесение подменило бы
      устаревший замер неверным.
- [ ] Отдельно поправить DoD эпика: п.3 (предикат не различает состояний) и п.4 (обе половины
      мерят не то, что называют).

### Задача 8.2: trail и закрытие

- [ ] `obsidian/kacho/KAC/issue-1017.md` — состояние, PR-URL, затронутые сущности vault.
- [ ] Новая записка `obsidian/kacho/edges/` не заводится: рёбер Ф1 не создаёт (метода нет).
- [ ] Закрыть #1017 **с артефактом** — PR-URL и перечень приземлённых гейтов.

---

## §4 Сценарий → шаг: где каждое требование приёмки исполняется

| сценарии | шаг |
|---|---|
| WATCH-1-19, -20 | Ш1 |
| WATCH-1-14, -15, -16 | Ш3 |
| WATCH-1-06, -10, -11, -22, -30 | Ш4 (объявление) + Ш5 (проверка) |
| WATCH-1-17 | Ш6, задача 6.2 |
| WATCH-1-25, -26 | Ш7 |
| WATCH-1-18 | ничего не делается — ведомость обязана остаться пустой |
| WATCH-1-27 | Ш4 (в Ф1 — отсутствие `service`); остальное — приёмка #1018 |
| WATCH-1-01…05, -08, -09, -12, -13, -28, -29 | **не в Ф1** — сервер Ф2 (#1018); в Ф1 закрыта их конструируемость (приёмка §6) |
| §7 п. 1б | Ш6, задача 6.1 |

---

## §5 Выход Ф1 — сверяется по DoD приёмки §8, не по этому списку

Кратко, чтобы было чем проверить готовность шага, но **нормативен DoD приёмки**:

- [ ] контракт объявлен, `buf` зелёный, `service` в Ф1 отсутствует;
- [ ] приземлены **G1, G2, G3** и запись в ведомость транспортных сообщений — каждый
      доказан инъекцией и печатает перепись. **G4 не приземляется**: снят вместе с предметом
      (приёмка §1.2 — смонтированных методов подписки на стволе ноль);
- [ ] ведомость послаблений несёт одну запись и умеет истечь;
- [ ] надгробие пополнено, ложная причина исправлена;
- [ ] конвенция правлена отдельным коммитом в воркспейсе;
- [ ] ведомость разрывов на стволе пуста;
- [ ] расхождения внесены в постановку; trail обновлён; #1017 закрыта с артефактом.

---

## §6 Чего этот план НЕ делает

Сервер (#1018) · перевод compute (#1019) · край (#1020) · консоль (#1021) · отзыв прав
(#1022) · остальные модули (#1023) · вынос iam (#1024) · серверный фильтр по меткам (#1025) ·
снятие прежней формы (после Ф3) · уборка мёртвого: читатель nlb (#1043) и таблица курсоров
(#1046).

**Два последних Ф1 не трогает, но приёмка их называет** — чтобы Ф2 не подобрала мёртвое:
таблица курсоров опаснее, она предлагает хранить позицию на сервере и тем отменяет
переподключение к другой реплике.
