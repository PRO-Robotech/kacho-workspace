# CI-RS routine: two-stage producer review serialization

Status: root + architect readback selected exact architect proposal SHA256 5871e593aa6c92d04e08f883d5fb9731b839f1cbbf11cb5723433f33b15f7bc1. This separately freezes that selection without editing routine-vocabulary-v2.md or accepted scenario/schema subjects. The tester proposal b3edd1dd and architect later eight-key alternative are not selected.

ProducerReviewSubject has exactly seven required keys: repository, commit, tree, executable_closure, tool_versions, machine_proofs, regression_proofs. Types and nested closed schemas remain v2. R = SHA256 of canonical UTF-8 JSON of exactly this subject, copied from producer_execution without root, review or authorization. No normalization or omitted proof bindings.

ProducerReviewRecord has exactly seven required keys: schema_version=1, kind=producer-review, subject_sha256=R, outcome=GREEN, reviewer (nonempty string), independence_record (FileRef), authority_coordinate (CommentCoordinate). There is **no readback field**. Exact live/captured coordinate, actor, body SHA and applicability readback remains mandatory through the test HTTP boundary or production actual readback. This event cannot contain a digest of its own later record. No record confers authority solely by claiming GREEN or an actor name.

After prior independent review/event readback, serialize ProducerReviewRecord and bind its bytes with producer_execution.review FileRef. ProducerExecutionSubject then has exactly eight keys: all seven ProducerReviewSubject keys plus review. E = SHA256 of canonical UTF-8 JSON of that object, removing only root and authorization from producer_execution. The separate kind=producer-execution AuthorityRecord and root event must bind E. Component Proof ReviewRecord is unchanged; component-review or producer-review never substitutes for execution authorization.

Canonical JSON is the agreed sorted-key, compact UTF-8 representation, no newline, no optional ASCII/HTML/JavaScript escaping; set arrays keep their validated order and ordered contexts remain ordered. Changing inner source/closure/tool/test/proof/output changes R and E; changing review bytes changes its FileRef and E. A local root locator alone leaves R/E unchanged but actual source identity/closure must still be verified. The authorization locator is excluded from these subjects; its actual bytes/authority still require validation and enter the final plan binding.

The sibling vectors contain exact canonical files and hashes for lawful two-stage construction, changed inner mode with old review retained, changed review with old execution authorization retained, root locator relocation and authorization locator relocation. They are synthetic serialization controls, not valid external records, source permission, actual T5 or live D8 approval. Required executable negative cases additionally reject unknown/missing/duplicate fields, kind confusion, stale subject digests, absent/unreadable records, coordinate/actor/body mismatch, and any missing separate execution authority before effects. Actual D8 authority remains pending future T5 GREEN and a separate root event.
