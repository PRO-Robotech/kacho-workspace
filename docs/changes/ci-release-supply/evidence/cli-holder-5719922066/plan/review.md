# Independent CLI/P9 caller-holder plan

Verdict: the worker proposal is a sound bounded direction, but v1 alone is not a ready frozen caller oracle. The P5 revision binding and actual executable artifact identity needed explicit routine resolution. Root has accepted their intent by task message; the exact forms and finite parser matrix here still need final readback before test-only work. No source/test/canonical file was edited and no SUT verdict is claimed.

The inspected owned copy is Kacho `28a80b7a5ff0f14221e1077b77a4dbf60a6922de`, clean, at `tmp/area-ci-release-cli-plan`. It predates ongoing checksum/deleted-head work; this review binds only the unchanged caller paths and accepted subjects listed in inputs.json. Worker proposal SHA256 is58fb821ceb6c5b8658ad0908058636b079559de47dc99dbe078cbb56d07434e1. Actual tools/releasepreflight/main.go, internal/release/cli.go, the two new shell adapters, consumers.json, internal-modules.json and canonical reference program are absent. The existing engines are present. File absence is readiness data, not an executed RED.

## Findings and resolved routine intent

**R1: pass the exact P5 SHA.** Interface lines22–27 and D3 bind P9 to the already observed candidate revision. publish-version.sh captures HEAD_SHA at P5. The v1 private route with only version and consumers could reread a newer HEAD. The proposed closed private route adds the existing --revision name carrying that captured SHA, while historical public flags remain unchanged. A holder will capture S, mutate actual HEAD once at the child-invocation boundary, and prove that result.candidate_sha and archive bytes remain S (or a truthful refusal), never silent validation of the replacement HEAD. A separate untracked-only package twin proves working tree cannot repair S.

**R2: preserve the actual executable identity.** D8 names the compiled executable if used. Current executable_closure is a tracked commit-relative SourceFile set and producerIdentity checks it against Git and disk. A temporary freshly rebuilt executable is not a member of that set. Existing reviewed machine/regression ProofSubject.runs.outputs already accepts readable regular-file raw-byte FileRefs, verifies their SHA256, and participates in R/E/plan. Binding the frozen binary there, with actual build/test evidence at F, followed by per-invocation root verification and direct execution, satisfies the existing representation without a new field. This is not a claim that runtime source closure proves os.Executable identity; root's explicit verification is necessary. A shell rebuild remains an ordinary tested entrypoint and cannot inherit the frozen binary's T6 authorization.

**R3: historical fixture is not lawful for P9 yet.** Its default example.com/synthetic module and local origin fail current exact github.com/repository identity, and it has no declared tracked consumer program. It currently proves P1–P8 script behavior, not P9. Add canonical module/origin, real tracked importable packages/program and a valid explicit declaration, plus the already permitted owned Git transport mapping. Do not fake P9 GREEN or add a skip. Keep all18 historical assertions and the precise P6 inversion (only G fails). Copy a defect script into an owned complete producer script/tool layout so dirname-based tool/default resolution still works; do not run the defect from an arbitrary directory with missing companions. Run both the ordinary and defective copies against independent tiny repositories, preserving no-write snapshots.

**R4: route selection must not broaden shell contracts.** A generic CLI can route --mode to preflight, but publish-module-tree.sh must still reject that flag and the private P9 positional route. Likewise the pins wrapper cannot accept a second mode or a revision that substitutes one SHA for multiple products. Direct actual-binary and actual-shell invalid cases must preserve schema rc2 rather than the go run launcher's rc1. The historical human-readable shell must not be converted into a one-JSON endpoint; only its captured child/new adapters have that result contract.

## Minimal stages and exact proposed paths

Stage A runtime composition: new internal/release/cli.go, tools/releasepreflight/main.go, scripts/release/publish-module-tree.sh and scripts/release/assert-internal-pins-reachable.sh. New cli.go uses only the existing dependency type and engines. These paths do not authorize changes to consumer, pins, publisher protocol algorithms or their frozen holders. Wrapper bootstrap should remain ordinary bounded build/exec/cleanup code; no product environment executable override. There is no need to add another CLI flag or a new common algorithm package.

Stage B legacy P9: new scripts/release/consumers.json and internal/release/testdata/reference-consumer/main.go; narrow publish-version.sh parse/invoke/accounting change. Independently owned mechanical changes in consumable_test.go read the same canonical tracked program instead of the current string; preserve its reference import, Referrer witness and every assertion. Independently owned pack_test.go change may reuse the existing ordinary archive builder; if production extraction is actually necessary, authorize only a separately reviewed small archive.go boundary, not blanket rewrite. P8 must read the canonical program from its archived revision, not an uncommitted replacement. No canonical architecture files change.

Proposed independent test-only paths: new internal/release/supply_cli_test.go (compiled main/new-shell routing, status and sanitation), new internal/release/supply_p9_caller_test.go (actual legacy caller and revision/declaration/archive obligations), new scripts/release/release-callers-inject.sh as the bounded driver. Existing scripts/release/publish-version-inject.sh receives only the lawful fixture/materialization/transport prerequisites needed by mandatory P9; original assert_all block and inversion are bound separately in legacy-oracle-binding.json. Existing frozen consumer/candidate/pins/publisher/checksum helpers stay unchanged and can be reused normally. These additive coordinates require root readback, not implicit expansion of T1 paths.

Stage C pins real CI caller remains separately scoped. Accepted T4 requires an actual named pre-merge caller, and final-main is required for final delivered pins. Existing ci.yaml has actual pull_request/main and push/main routes; no current pins caller exists. Its new step must receive an exact Kacho checkout commit and independently fetched exact Kaname revision, preserve full tracked go.mod census, strict explicit module-owner mappings, origin and final-main outcomes, and nonzero propagation. Pin-only full manifest already has exact schema_version/product_trees/internal_modules/budgets. A bare two-entry internal-modules.json is not that manifest. The proposed next decision must name how Kaname SHA is selected, how both exact trees are materialized, the concrete callable manifest builder (without a new silent mode/flag), and where strict duplicate/unknown validation occurs. Do not silently choose current Kaname main or a module-cache directory as a Git tree. Do not treat a YAML substring as proof of invocation. .github/workflows/ci.yaml and scripts/release/internal-modules.json are deferred Stage C source paths; Makefile and scripts/ci-local.sh need no change for A/B. Existing Makefile release-preflight already invokes the legacy script and will naturally include P9. No early Kacho PR is permitted.

## Finite independent holder plan

1. Before capability: resolve/capture actual Git and Go, independently construct tiny committed canonical-origin target and consumer Git trees, produce the actual Go ZIP, build every declared import/context from that ZIP with a separate empty target cache, and demonstrate one missing-package refusal plus restored lawful twin. Capture nonempty package/import/consumer census, source/ZIP hashes, process exits and cleanup. A broken prerequisite is HARNESS_NOT_EXECUTED, not source RED.
2. Compile and execute the actual tools/releasepreflight binary. Admission and exact0/1/2/3 behavior use that binary and actual shell scripts, never go run. Simple lawful consumers calls can use default dependencies with canonical identity and local immutable revision; they require no live forge. Full candidate/pins/publisher routing also runs the same ordinary adapter in the existing explicitly injected SupplyDependencies bridge using already proven fixtures. Record the two scopes separately: a test bridge is not evidence that production main accepts fixture endpoints, and a parsing-only real binary case is not a full lawful publisher proof. No fabricated tool outputs.
3. Execute every exact parser-matrix row, assert one JSON EOF, exact schema/phase/reason/rc, no credential leakage, no remote writes and no extra stdout. Additional lawful routing rows cover all three modes and publisher default plan using the preexisting finite fixtures; forwarding must preserve every argument and existing budget. Add relative input paths from a non-root cwd and hostile inherited GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE/GIT_CONFIG_COUNT tests against a separate decoy repo; prove no decoy changes and correct actual candidate identity. Missing build/runtime tools must yield the finalized NOT_EXECUTED bootstrap shape. Every created process joins and owned cache/output directory cleans up.
4. P9 data matrix: default canonical declaration; explicit equivalent declaration; missing/unread file; empty file; malformed JSON; duplicate top-level or nested key; unknown key; wrong type; empty consumers; zero target imports; two imports with missing second archived package and restored twin; tracked program missing at S; package only untracked beside S; captured-S versus later-HEAD mutation; --skip-pack still executes P9 and records P8 unasked. All negative rows have computed single-fact deltas and lawful twins. Both declaration types remain covered by existing consumer holders; the caller adds explicit invocation witnesses and does not claim an invented Kacho downstream. Assert all five consumers predicates, nonempty actual census and exact candidate_sha for successful P9, not just rc0.
5. Run the unchanged18 P1–P8 oracle bodies against the fixture-only successor and its narrow P6 mutation: ordinary18 pass, defective exactlyG fails. Assert legacy RED outranks P8/P9 unasked; no owner push command on any failed/unasked predicate; local tags and remote refs unchanged in every case. Add dedicated --skip-pack/P9 cases instead of weakening the existing skip expectation.
6. Canonical P8 helper successor: preserve exact program semantics and existing assertions, then run TestExternalConsumerCanBuildTheModule and the existing four consumability inversion tests against the shared ordinary archive builder. Bind byte/token preservation outside the approved extraction/read location. This is separate from tiny named-P8 shell dispatch proof.
7. Once Stage C input builder is explicitly selected, parse the actual workflow, materialize its exact named step for both pull_request and push event contexts, execute it against real owned Git histories and the unchanged pins engine, and capture reachability/main refusal plus restored twins. Add missing/unread mapping, duplicate key, omitted tracked nested go.mod, missing Kaname tree and failing child cases. The step cannot turn any of these into successful CI. A parsed workflow plus synthetic event execution remains distinct from later live T7 Actions proof.

## Readiness and boundaries

A/B holder implementation can start only after root fixes the exact parser/declaration/bootstrap table and holder coordinates. Source remains closed until independent executable baseline and root review. API absence after genuine prerequisites is CAPABILITY_ABSENT with zero SUT calls; malformed compiled linkage is harness NOT_EXECUTED. No tests or source were written in this planning task. No live D8 authorization, producer GREEN, pins CI completion, T5/T6/T7 delivery or issue closure is asserted. The original publisher/checksum artifacts remain immutable, and #2672 is still outside this active task.
