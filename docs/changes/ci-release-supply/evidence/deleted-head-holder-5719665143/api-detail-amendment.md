# Deleted-head resume: authoritative PR detail readback amendment

This immutable amendment extends proposal `deleted-head-resume-proposal.md` SHA256 `96a1d775a76872bec5fa75585cebd7c5da7438ae689ce2a1b414f85784c2c69d`, without changing its bytes, the accepted subjects, or product/test source. It is proposed routine implementation detail for the same D4 and CI-RS-11/19/22 boundary; root readback and independent holder remain required.

Actual read-only GitHub API evidence: `merged-pr-api-shape-readonly.json` SHA256 `e72cddba11e8ab07d00882e5849c4e6e09ca8112b285759b0323856f3d3c18b3`. A closed pull-list request returned eight corelib PRs. Selected merged PR #18 has `merged_at` and `merge_commit_sha`, but **no `merged` or `mergeable` key** in its list item. The exact `/repos/PRO-Robotech/corelib/pulls/18` detail has `merged: true`, the same original head, base and merge SHA, and `mergeable: null`. Only selected identity/state fields were retained; no credentials or unrelated response fields were saved. This proves an API-shape mismatch independently of the existing synthetic fixture. It does not execute the publisher or establish release readiness.

Current `findPR()` decodes the list directly into `supplyPR.Merged bool`, then applies complete PR validation. The absent list key becomes false, so the real merged/closed PR is rejected even while its original head still exists. Removing the live-head requirement alone is insufficient.

Proposed finite mechanism: keep existing exact query, pagination, complete unique-match detection and conflict semantics. Treat a unique list item only as discovery of a positive PR number plus its repository/head/base/marker identity. Read that exact numbered detail through the existing bounded read-only HTTP seam before deciding open/merged state or creating/reusing effects. Validate the detail's number equals the discovered/requested number, canonical head/base repositories, derived head name, expected head SHA, main base, exact plan marker, and the authoritative Boolean `merged`. Require the ordinary existing merged-SHA rule only when merged is true. A missing, wrongly typed, unreadable or malformed detail cannot authorize an operation; existing SOURCE_UNAVAILABLE/NOT_EXECUTED applies to absent evidence, while an observed conflicting identity keeps existing conflict reasons. Do not infer merged=true from `merged_at`, list `merge_commit_sha`, state=closed, or branch absence. Do not treat null `mergeable` on an already merged PR as a refusal; this is the actual detail shape, and mergeability is an open-PR concern.

List/detail identity disagreement, changed detail head, duplicate discovery, unmerged detail and missing/unavailable detail all remain refusing controls. Successful discovery performs one authoritative detail read (with existing bounded read retry), not a write. Subsequent fresh checks retain their existing exact numbered detail reads. No arbitrary endpoint, extra CLI flag, new effect state, fixture bypass or production transport change is introduced. The minimal source correction stays in the approved publisher files: `publisher_forge.go` for list→detail readback and identity, `publisher_effects.go`/`publisher_release.go` for deleted-head resume; exact final pathset awaits root authorization.

Independent additive holder should make list responses match actual GitHub omission of `merged` and `mergeable`, retain full detail responses, and prove the lawful merged resume with and without the head ref. Its negative twins must distinguish list discovery from unavailable/wrong-number/wrong-identity/unmerged detail. Old 52 expectations remain unchanged; the fixture must not fabricate detail-only fields in a list to conceal the production mismatch.
