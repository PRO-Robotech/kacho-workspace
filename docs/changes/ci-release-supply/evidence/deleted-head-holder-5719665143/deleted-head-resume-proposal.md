# Deleted receiving head after accepted merge — bounded proposal

Fresh read-only GitHub repository readback confirms PRO-Robotech/corelib main, allow_squash_merge=true and delete_branch_on_merge=true; receiving-auto-delete-readonly.json SHA25629839985698a2e4449e829b09b955893891672647602edb45b929071b5743f10. Current publisher release() requires the derived live branch first, so normal automatic deletion after a completed protected merge prevents release/probe resume. A later deliver resume has the same problem: it reaches new-branch/base checks before discovering the already merged PR. Current fixture keeps the branch, therefore existing52 scenarios do not establish this operational path.

Proposed clarification inside existing D4/effect semantics, without new schema keys/states/scenario ordinals or historical contract edits: an absent derived branch is not evidence that a verified merge never happened. Reconstruct a completed operation from the unique exact receiving PR: repository, exact derived head name, expected original head SHA, base main, plan marker, authoritative merged flag and exact merge SHA must all match. Obtain that exact native merge object, verify exact accepted candidate tree, current main ancestry and required checks through the existing engines. Any duplicate PR, different head SHA/plan/repository, missing/unmerged PR, missing object or failed main/content/check predicate still refuses. A different currently present derived branch remains CONFLICT; it is never ignored in favor of old PR evidence.

For an absent branch with a fully verified merged PR, emit no branch effect record and do not assert branch PRESENT or ABSENT. No branch write was attempted in this invocation and no current branch identity was observed; ABSENT must not claim the earlier creation never applied. The PR and merge can independently be recorded PRESENT with their observed exact identities, and the existing stage can reach MERGED_VERIFIED. Probe still has effects=[] as required. Stage/effect reconstruction never recreates/deletes a branch or changes repository auto-delete policy. Existing runs that actually observed a present branch keep that truthful branch record.

For a fresh invocation with no branch and no matching PR, the original exact base/input/CAS path remains. If a matching PR exists but is unmerged while its head is missing, refuse rather than resurrecting the head. For a merged PR, continue only through the unchanged merge verification and explicit repo/plan key; release still needs exact landed-sha and performs ordinary tag/note creation only after current-main reachability, and probe still performs all mandatory archive/public-checksum/consumer checks before ARCHIVE_VERIFIED.

Independent bounded controls proposed before source correction: actual deletion of the fixture head after actual merge, then lawful no-write deliver resume; release with exact landed SHA and no branch creation; read-only probe; paired wrong PR head identity, unmerged/missing branch, and target no longer ancestor of current main. Preserve original52 expectations and raw historical run captures. These are existing CI-RS11/19/22 recovery/readback obligations, not new acceptance ordinals. This file is a proposal only: source/test correction and any precise effect interpretation require root readback and independent holder transition. Current 0fb source and running evidence remain unchanged.
