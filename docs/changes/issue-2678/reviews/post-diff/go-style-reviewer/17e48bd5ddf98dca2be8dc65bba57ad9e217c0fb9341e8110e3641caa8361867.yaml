# Пост-дифф ревью Go-кода продукта — роль go-style-reviewer.
#
# Имя файла — и есть content_digest: запись самоидентифицируется отпечатком
# отсуженного содержимого. Прежние записи не правятся; сменился дайджест —
# новый запуск и новый файл (change-graph §2 «вердикт привязан к ОТПЕЧАТКУ»).
#
# Предыдущая запись этой роли — ba4d8f89…yaml на отпечатке @3f68255d58f, вердикт
# ⛔. Она обесценена правкой и НЕ правится: этот файл судит новый отпечаток целиком.
schema_version: 1
change_id: issue-2678
holder: post-diff/go-style-reviewer
owner: go-style-reviewer
supersedes_fingerprint: 3f68255d58f57bf3fa9e30dce4ed6a13f7b02584
subject: >-
  kacho #2678 + #2594 — круг по замечаниям: имена ручек, доезжающие до разбора;
  стражи предпосылки в пробах носителя; разбор порта числом; один довод о причине
  самопротиворечивого вердикта; сборщик СВОИХ остатков; суженное обоснование
  быстрого пути

# ── отпечаток ──────────────────────────────────────────────────────────────
fingerprint:
  repo: PRO-Robotech/kacho
  branch: issue-2678
  worktree: /home/dk/workspace/github/PRO-Robotech/cloud-demo/kacho-workspace/tmp/issue-2678
  base: f445aaaa55472d1c676595d4a33daee14882ad2d
  head: f9dc93b3b79c74ca66631f8f1577e5affa479bc9
  commits:
    - e4edf76af68  # #2678 — пробы носителя nlb и storage берут эфемерный порт
    - 3f68255d58f  # #2594 — отправка судится отправляемой ревизией
    - f9dc93b3b79  # круг по замечаниям — предмет ЭТОЙ записи
content_digest: sha256:17e48bd5ddf98dca2be8dc65bba57ad9e217c0fb9341e8110e3641caa8361867
names_digest: sha256:c8f2338c002df9784d8c082fa55d55460334ecd531aa0586773678742b971a95
digest_definition: >-
  content_digest — sha256 сырого вывода `git diff origin/main...HEAD`;
  names_digest — sha256 вывода `git diff origin/main...HEAD --name-only | sort`.
  Перемерено своей командой в worktree; `git status --porcelain` пуст,
  `git merge-base origin/main HEAD` == f445aaaa554 == заявленный base.

# ── вердикт ────────────────────────────────────────────────────────────────
verdict: accepted            # ✅ принято
blocks_landing: false
return_to: '—'
timestamp: '2026-09-21T00:00:00Z'
review_scope: >-
  полный закоммиченный дифф: 14 файлов, +1018 / -55, три коммита.
  Go-часть — 12 файлов (+412 / -41), прочитана целиком; продуктового (не-пробного)
  Go в диффе ровно один файл — services/nlb/internal/apps/kacho/config/config.go,
  и правка в нём шапочная. Оболочечная часть (scripts/hooks/pre-push +258/-13,
  prepush-range-inject.sh +348/-1) прочитана целиком по СЕМАНТИКЕ пп. 5 и 6
  задания; как скрипт её судит приёмщик опытом — здесь о ней вердикта нет.
  Исход линта и проб не объявляется: сырой вывод читает landing-reviewer,
  независимый перемер — check-verifier.

files_reviewed:
  - services/nlb/cmd/kacho-loadbalancer/carrier_start_test.go    # +14 / -3
  - services/nlb/cmd/kacho-loadbalancer/describe_test.go         # +100 / -13
  - services/nlb/cmd/kacho-loadbalancer/dialpeers_mtls_test.go   # +1 / -1
  - services/nlb/cmd/kacho-loadbalancer/ephemeralport_test.go    # +43 (новый)
  - services/nlb/internal/apps/kacho/config/config.go            # +7 / -3
  - services/storage/cmd/storage/carrier_start_test.go           # +11 / -3
  - services/storage/cmd/storage/describe_test.go                # +42
  - services/storage/cmd/storage/ephemeralport_test.go           # +35 (новый)
  - tools/declaredbreak/declaredbreak.go                         # +55 / -4
  - tools/declaredbreak/cmd/adjudicate-declared-breaks/main.go   # +5 / -10
  - tools/declaredbreak/incoherent_test.go                       # +9 / -4
  - tools/declaredbreak/incoherentreport_test.go                 # +90 (новый)
  - scripts/hooks/pre-push                                       # +258 / -13 (вне Go-предмета)
  - scripts/hooks/prepush-range-inject.sh                        # +348 / -1  (вне Go-предмета)

# ── шесть возвращённых пунктов: закрыт / не закрыт ─────────────────────────
returned_items:
  - id: 1
    title: класс «имя ручки, которого разбор не читает» — четыре места
    status: закрыт
    evidence: >-
      Перемерено обходом по ревизиям, а не прочитано в заявлении.
      `git grep` живых ПОДСТАНОВОК дефектных имён в services/{nlb,storage}/cmd:
      на 3f68255d58f — ровно 4 (describe_test.go:66, :196, :243 и
      dialpeers_mtls_test.go:62, все `KACHO_NLB_EXTAPI__*__INTERNAL_ADDR`);
      на HEAD — 0. Число сошлось с заявленным.
      Перепись сверху: все 38 различных имён `KACHO_NLB_*` в services/nlb/ +
      gateway/ + deploy/ — каждое либо выводится из ключа defaults.go заменой
      ТОЛЬКО точки на `__` (load.go:67 — `strings.NewReplacer(".", "__")`),
      либо стоит явным BindEnv (их ровно четыре: extapi.geo.addr,
      authz.trust-any-forwarder, authz.trust-domain, fga.require-iam).
      Storage: `KACHO_STORAGE_INTERNAL_PORT` совпадает с тегом envconfig в
      services/storage/internal/config/config.go:43 — имя проверено по
      описателю, а не по соседней пробе.
      Оговорка: ноль установлен для мест, которые ПОДСТАВЛЯЮТ величину.
      Имя той же дефектной формы живо как ТЕКСТ, печатаемый оператору, — см.
      находку important/1; число по всему дереву идёт отдельной задачей и в
      этот вердикт не входит.
  - id: 2
    title: страж предпосылки вместо заглушенного красного
    status: закрыт
    evidence: >-
      Страж СУДИТ предпосылку и не маскирует. describe_test.go:236-241 —
      `t.Fatalf` ДО вызова `describe`, при несозданной предпосылке проба
      ОТКАЗЫВАЕТ, а не молчит; текст отказа называет обе величины
      (internal-addr, addr) и говорит, что о конструкторе это не говорит ничего.
      requireEphemeralListeners стоит до вызова носителя в обеих службах:
      nlb carrier_start_test.go:60 и describe_test.go:331, storage
      carrier_start_test.go:58. Направление отказа fail-closed: не доехавшая
      подстановка оставляет умолчание (nlb `tcp://0.0.0.0:9090`, storage `9090`),
      и обе величины страж отвергает.
  - id: 3
    title: сравнение порта строкой → разбор числа
    status: закрыт
    evidence: >-
      ПЕРЕМЕРЕНО МНОЙ прямым вызовом net.Listen на 10 написаниях, а не
      прочитано в комментарии: «0», «00», «0000», «+0», «-0» поднимают
      ЭФЕМЕРНЫЙ слушатель (ядро выдало 34697/39967/46765/44513/46609);
      « 0», «0x0», «10» — отказ; «9090» — фиксированный порт.
      `strconv.Atoi(port) == 0` совпадает с net.Listen на всех ВОСЬМИ
      написаниях, которые комментарий называет. ParseUint отверг бы «+0»/«-0»,
      которые продукт принимает, — выбор Atoi подтверждён.
      Утверждения двусторонние (законные молчат, фиксированный находится),
      таблицы по 11-12 входов в обеих службах.
      Nlb-распознаватель переиспользует ПРОДУКТОВЫЙ `hostPort`
      (services/nlb/cmd/kacho-loadbalancer/describe.go:285), второго разборщика
      схемы не заводит.
      Точность, не находка: пустой порт («») net.Listen ПРИНИМАЕТ (измерено:
      127.0.0.1:42163), распознаватель даёт false. Расхождение объявлено в
      таблице («отсутствие не считается за разрешение») и направлено в
      безопасную сторону — страж краснее продукта; формулировка «область приёма
      совпадает» в комментарии оговорена восемью перечисленными написаниями и
      для них верна.
  - id: 4
    title: снятая догадка о причине в соседней шапке
    status: закрыт
    evidence: >-
      Мест об этом предмете четыре, и все говорят ОДНО: declaredbreak.go:221-233
      (шапка Result.Incoherent — перечень двух наблюдавшихся причин),
      declaredbreak.go:391-406 (шапка IncoherentReport — правило «причину не
      называем», ссылка на перечень у Incoherent), incoherent_test.go:16-24
      («в ТОТ раз» + вторая причина + ссылка на IncoherentReport),
      incoherentreport_test.go:6-25 (предмет, измеренный 2026-09-11).
      ТРЕТЬЕГО утверждения нет: ни одно из четырёх не называет причину ЭТОГО
      отказа, все отсылают к одному перечню.
      Текст отказа сведён в один кусок — Result.IncoherentReport; вторая копия
      снята из cmd/adjudicate-declared-breaks/main.go (-10 строк), вызывающий
      печатает то, что вернул предикат.
      Проба двусторонняя: положительный контроль утверждает обе половины
      числами И по образцу с каждой стороны (без образцов отказ стал бы беднее
      прежнего) плюс отсутствие трёх запрещённых оборотов; отрицательный —
      TestIncoherentReportIsEmptyWhenTheVerdictIsCoherent, и он с собственным
      контролем «положительный близнец сам не самопротиворечив».
  - id: 5
    title: сборщик осиротевшей выкладки — не трогает ли чужого
    status: закрыт
    evidence: >-
      Собирает ТОЛЬКО своё — установлено по четырём независимым признакам,
      а не по заявлению.
      (1) Обход ограничен ВЫДЕЛЕННЫМ каталогом `$git_common/kacho-prepush-subject/`
      (pre-push:283), который заводит только этот хук; общий `worktrees/` он не
      обходит ни одной командой.
      (2) Маска `kacho-prepush-*` плюс требование ЧИСЛОВОГО суффикса
      (pre-push:288-290) — каталог без номера пропускается.
      (3) `git worktree prune` в хуке отсутствует: перемерено grep'ом, все
      упоминания слова — комментарии на строках 256 и 277 («его здесь нет
      намеренно») и `docker builder prune` в подсказке на 437.
      (4) Снос адресный: `git worktree remove --force "$dir"` и
      `rm -rf "$dir" "$git_common/worktrees/kacho-prepush-$pid"` (pre-push:295-296)
      — обе координаты несут свою приставку и свой номер.
      Переиспользование номеров закрыто вторым признаком: живой pid И возраст
      меньше суток → пропуск (pre-push:291-293).
      Остаётся непокрытым ПРОБОЙ — см. находку important/2.
  - id: 6
    title: обоснование быстрого пути «судим копию на месте» шире предмета
    status: закрыт
    evidence: >-
      pre-push:338-351 — прежний довод («неотслеживаемые не считаются: проверки
      читают состав дерева через `git ls-files`») снят как неверный для
      большинства проверок, и названа причина: сборка, статический анализ и
      пробы читают ФАЙЛОВУЮ СИСТЕМУ. Расхождением теперь считается всё, что
      печатает `git status --porcelain` без ключей.
      Исключение для игнорируемого объявлено ВЫБОРОМ, и цена названа в том же
      комментарии: объявив артефакты расхождением, обычная отправка ушла бы
      временным путём, где их нет, и проверки консоли целиком уходили бы в
      «НЕ выполнено» — узкое ложное утверждение обменяли бы на широкое
      отсутствие вердикта.
      Обе ветки судятся фикстурой РАЗДЕЛЬНО:
      prepush-range-inject.sh:532-536 (лишний `.go` рядом → `STRAY:no`) и
      :541-545 (игнорируемый артефакт → «вердикт — о рабочей копии»).
      Сама фикстура перестала жить вольготнее продукта: :315 объявляет
      `scripts/` игнорируемыми тем же словом, каким это делает дерево.

# ── находки ────────────────────────────────────────────────────────────────
findings:
  critical: []                 # блокирующих нет

  important:
    - class: DOC-vs-CODE
      coord: services/nlb/internal/apps/kacho/config/config.go:366
      in_diff_file: true
      finding: >-
        Файл из диффа противоречит сам себе. Новая шапка того же файла
        (строки 12-16, добавлены ЭТИМ изменением) утверждает: «форма с
        подчёркиванием на месте дефиса не читается ничем — ни отказа, ни
        предупреждения, величина просто остаётся умолчанием». Строка 366 того
        же файла называет оператору ровно такую форму:
        `KACHO_NLB_AUTHZ__TRUSTED_FORWARDER_SANS`.
        Ключ — `authz.trusted-forwarder-sans`; явного BindEnv у него НЕТ
        (defaults.go:133 — только SetDefault), значит читаемая форма —
        `KACHO_NLB_AUTHZ__TRUSTED-FORWARDER-SANS`, и именно она стоит в пробе
        (describe_test.go:70). Это тот же класс, который закрывали пунктом 1.
      requirement: >-
        Привести строку 366 к читаемой форме тем же изменением — либо вынести
        координату в отдельную задачу о дереве и сослаться на неё здесь.
      why_not_blocking: >-
        Строка 366 предшествует диффу и не тронута им; число дефектных имён по
        дереву заданием выведено из предмета этой роли («идёт отдельной
        задачей»). Утечки наружу нет: страж круга fail-closed — оператор,
        выставивший названную форму, получит отказ, а не тихо открытый круг.
      radius_outside_diff: >-
        Для ТОЙ задачи, не для этого вердикта. Радиус искался по ИМЕНИ
        механизма, а не по диффу. Та же форма как ТЕКСТ, который процесс
        печатает оператору: validate.go:122 (SANsKnob в отказе стража круга) и
        cmd/kacho-loadbalancer/describe.go:124 (ForwarderKnobs.SANs в
        дескрипторе) — это не комментарии. Комментарием — defaults.go:132.
        Соседний класс того же вида: observability.go:168 печатает
        `KACHO_NLB_METRICS_ADDR` при ключе `metrics.address` (читаемая форма —
        `KACHO_NLB_METRICS__ADDRESS`); defaults.go:111,
        deploy/configmap-sample.yaml:118 и tests/newman/cases/list-filter.py:29
        называют `KACHO_NLB_AUTHZ__LIST_FILTER__ENABLED` при ключе
        `authz.list-filter.enabled`.

    - class: ONE-SIDED-PROBE
      coord: scripts/hooks/prepush-range-inject.sh:547-566
      finding: >-
        У сборщика остатков утверждается только ПОЛОЖИТЕЛЬНАЯ сторона:
        осиротевшая выкладка с заведомо мёртвым номером собрана, и сборка
        названа числом. Отрицательной — «чужая рабочая копия рядом НЕ
        тронута» — в фикстуре нет.
        Сегодня код узок (обоснование — в returned_items/5), но расширение
        обхода — возвращённый `git worktree prune`, расширенная маска,
        поднятый на уровень каталог — прошло бы ВСЕ утверждения молча. Цена
        такого расширения в этом клоне названа самим комментарием хука: два
        десятка чужих рабочих копий.
        Дифф держит двусторонность везде, где заводит свойство
        (incoherentreport_test.go, ephemeralport_test.go, три инъекции дефекта
        у хука) — эта половина выпала из того же правила.
      requirement: >-
        Завести близнеца: рядом с собираемой выкладкой поставить запись БЕЗ
        приставки `kacho-prepush-` (или с приставкой, но живым номером) и
        утвердить, что после прогона она жива.
      why_not_blocking: >-
        Недостающая половина — регрессионный замок на свойстве, которое сейчас
        верно и проверено чтением; предмет круга — существование сборщика, и
        оно закрыто. Конструкция самой оболочечной пробы заданием отнесена к
        приёмщику.

    - class: PRECISION
      coord: services/nlb/cmd/kacho-loadbalancer/describe_test.go:232
      finding: >-
        «Отрицание проходило потому, что второго и не было: умолчания у ключа
        нет» — названный факт ложен. defaults.go:87 регистрирует
        `extapi.<peer>.internal-addr` с умолчанием, и умолчание это — ПУСТАЯ
        СТРОКА. Вывод комментария (адрес оказался пуст) от этого не меняется,
        но рядом стоит страж, который именно на это умолчание и опирается:
        читатель, поверивший «умолчания нет», не поймёт, почему страж молчит
        на незаданной ручке.
      requirement: >-
        «умолчание у ключа — пустая строка» вместо «умолчания у ключа нет».

  style:
    - coord: >-
        services/nlb/cmd/kacho-loadbalancer/describe_test.go:147 и
        services/storage/cmd/storage/describe_test.go:134
      finding: >-
        `portIsKernelAssigned` живёт двумя копиями вместе с почти дословно
        повторённой шапкой. Горизонталью двух ПРОДУКТОВ это не является
        (одно дерево, пробный код, `package main` двух служб), поэтому в
        фундамент не идёт и кандидатом туда не объявляется.
        Прозу авторы уже свели: services/storage/cmd/storage/ephemeralport_test.go:7-10
        называет nlb-файл местом разбора и не переписывает его. Для кода того
        же смягчения нет.

  foundation_candidates: []    # горизонтали двух продуктов в диффе нет

# ── канон, перемеренный по ДОБАВЛЕННЫМ строкам диффа ───────────────────────
approved:
  panics_in_working_path: 0       # panic(, log.Fatal, MustParse — ноль
  context_background_or_todo: 0
  raw_loggers: 0                  # fmt.Print*, log.Printf — ноль
  unwrapped_error_returns: 0      # `return err` без обёртки — ноль
  errorf_with_percent_v_in_wrap: 0
  # %v встречается дважды — t.Errorf о БУЛЕВОМ значении, не обёртка ошибки:
  # nlb ephemeralport_test.go:41, storage ephemeralport_test.go:33
  fresh_deferral_markers: 0       # запрет #11
  tests_in_same_pr: true          # запрет #12
  orm_or_global_singletons: 0     # запрет #3, arch-ban-global-singleton
  business_logic_in_transport: 0
  compile_fact: >-
    `go vet ./services/nlb/cmd/kacho-loadbalancer/... ./services/storage/cmd/storage/...
    ./tools/declaredbreak/...` вывода не дал. Это факт о сборке диффа, НЕ вердикт
    прогона: исход линта и проб объявляет landing-reviewer, перемеряет check-verifier.
  fixture_not_more_lenient_than_product: >-
    Проверена ось «корень прогона»: заглушка фикстуры выводит корень из
    BASH_SOURCE (prepush-range-inject.sh:345) — ровно так же, как настоящий
    scripts/ci-local.sh:29; хук зовёт `bash "$subject_root/scripts/ci-local.sh"`
    (pre-push:386), и настоящий прогонщик делает `cd "$ROOT"` от своего пути
    (ci-local.sh:168). Набор групп берётся от ДИАПАЗОНА КОММИТОВ
    (prepush-groups.sh:58-63), а не от рабочего дерева.
  tests_count_in_diff: >-
    9 из 14 файлов — пробные; три НОВЫХ файла проб; у каждого нового свойства
    утверждения двусторонние; у хука три раздельные инъекции дефекта
    (make_broken_draft, make_broken_reap, make_broken_subject), каждая с
    законным близнецом, и у каждой объявлена норма «≥1 провал».

# ── заказ следующему ───────────────────────────────────────────────────────
next:
  agent: landing-reviewer
  task: >-
    сбор пост-дифф записей на отпечатке 17e48bd5…; Go-вердикт ✅, блокирующих
    нет. Три неблокирующие находки (координаты выше) — решение о том, чинить их
    этим заходом или отдельной задачей о дереве, за посадкой.
