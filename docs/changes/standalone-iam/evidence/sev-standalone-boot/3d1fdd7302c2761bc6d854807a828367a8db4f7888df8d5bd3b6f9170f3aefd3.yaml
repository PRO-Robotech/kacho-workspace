# План свидетельства держателя sev-standalone-boot — изменение standalone-iam.
#
# Написан ДО прогона. Поля снятого исхода пусты НАМЕРЕННО: записать в них
# ожидание значило бы выдать намерение за наблюдение. Их заполняет прогон.
schema_version: 1
change_id: standalone-iam
holder: sev-standalone-boot
kind: machine
owner: integration-tester
subject: пакет standalone-iam на состоянии ISSUE_READY
subject_sha256: 3d1fdd7302c2761bc6d854807a828367a8db4f7888df8d5bd3b6f9170f3aefd3
executable: 'ЗАВОДИТСЯ ЭТИМ ПЕРЕХОДОМ: установка чарта отдельного клона в боевой посадке + ожидание готовности'
predicate: 'отдельный клон поднимается без монорепо и без внешнего поставщика и отвечает на публичный RPC'

# ── снятое: заполняет прогон, не перо ──────────────────────────────────────
captured_category: null
input_sha256: null
output_sha256: null
stdout_digest: null
stderr_digest: null

# Исходов ровно три: GREEN · RED · NOT_EXECUTED. Отсутствие вывода — это
# NOT_EXECUTED, а не «наверное прошло».
note: >-
  производителя нет: держатель заводится этим переходом. До его появления
  исход — «не выполнилось», и он НЕ вычитается из вердикта и НЕ зачитывается
  в успех
