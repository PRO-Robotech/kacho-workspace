# План свидетельства держателя ws-tooling-gate — изменение standalone-iam.
#
# Написан ДО прогона. Поля снятого исхода пусты НАМЕРЕННО: записать в них
# ожидание значило бы выдать намерение за наблюдение. Их заполняет прогон.
schema_version: 1
change_id: standalone-iam
holder: ws-tooling-gate
kind: machine
owner: integration-tester
subject: пакет standalone-iam на состоянии ISSUE_READY
subject_sha256: 3d1fdd7302c2761bc6d854807a828367a8db4f7888df8d5bd3b6f9170f3aefd3
executable: './scripts/tooling-gate/run-all.sh'
predicate: 'пути в скриптах резолвятся, карта агентов сходится с деревом, триггеры сужены по ветке'

# ── снятое: заполняет прогон, не перо ──────────────────────────────────────
captured_category: null
input_sha256: null
output_sha256: null
stdout_digest: null
stderr_digest: null

# Исходов ровно три: GREEN · RED · NOT_EXECUTED. Отсутствие вывода — это
# NOT_EXECUTED, а не «наверное прошло».
note: >-
  прогон не запускался: пакет в состоянии ISSUE_READY, а прогон держателя
  относится к содержимому изменения, которого ещё нет
