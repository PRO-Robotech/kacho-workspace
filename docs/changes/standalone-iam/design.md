# standalone-iam — замысел перехода

> **Статус:** DRAFT · на одобрение **не подан**.
> **Дата subject:** 2026-09-06 — редакция 3. Редакция 2: 2026-09-06. Первая: 2026-09-04.
> **Предмет:** переход `services/iam` в отдельный продукт.
> **Приёмка:** `acceptance.md` этого пакета.
>
> **Решение владельца 2026-09-06 ОТМЕНИЛО решение 2026-09-04** и задало целевую
> раскладку из трёх модулей. Действующее решение, отменённое и судьба каждого его
> следствия — §0; открытый остаток и почему `DESIGN_APPROVED` всё ещё недостижим — §7.
>
> **Редакция 3 — запись четырёх решений владельца того же дня.** Раскладку из трёх
> модулей несёт §0 и она редакцией 3 не тронута; три остальных заведены как **Р10**
> (провод переименовывается весь и сейчас), **Р11** (прежняя схема Postgres — только
> чистая установка) и **Р12** (пакет опций авторизации уезжает в `corelib`). Разборы —
> §7.1–§7.3; отпечаток `design.md` сменился, `acceptance.md` редакцией 3 **не тронута**.
>
> **Прежняя редакция не удалена.** По решению 2026-09-04 велась работа, и её следы
> в дереве — заведённые репозитории, начальный импорт, закреплённая версия — иначе
> необъяснимы. Записи §0.1–§0.7 сохранены дословно и снабжены вердиктом каждая.

---

## 0. Решение владельца 2026-09-06 — три модуля; решение 2026-09-04 им ОТМЕНЕНО

### ДЕЙСТВУЮЩЕЕ решение владельца 2026-09-06

**Целевая раскладка — три модуля, зависимости однонаправленны:**

```
corelib   общий фундамент, не привязанный ни к одному продукту
   ↑              ↑
kaname    контракт доступа + служба + её клиентские пакеты
   ↑
kacho     платформа: сервисы · край · консоль · провайдер
```

Консоль остаётся в Kachō.

**Норма разделения: Kaname наследует от Kachō КОД, но не ИМЯ.** Общее — фундамент
(библиотеки, общие механизмы, готовая функциональность). Своё — идентичность: всё,
чем продукт себя называет. Проверочный вопрос при каждом сомнении: *это имя, которым
продукт себя называет, или код, который он исполняет?* Первое — своё, второе берётся
у Kachō как есть.

**Направление стрелок несёт запрет, а не только порядок.** `kacho` зависит от
`kaname`; значит обратной зависимости `kaname → kacho` в целевой раскладке **не
существует** — она была бы циклом. Сегодня она есть, и это измерено ниже (П4).

**Порядок, названный владельцем и жёсткий:** сперва публикация версии платформы из
ствола, потом решение о публичности фундамента — служба под AGPL не вправе ссылаться
на фундамент, недоступный публично.

#### Предикат готовности перехода — семь пунктов, и его состояние ЗАМЕРЕНО

Замер 2026-09-06; предикат назван у каждой строки, чтобы его можно было повторить, а
не поверить. «Не измерено» — отдельное состояние: оно не зачитывается ни в успех, ни
в отказ.

| # | пункт | состояние на 2026-09-06 | предикат |
|---|---|---|---|
| **П1** | `PRO-Robotech/kaname` и `PRO-Robotech/corelib` резолвятся | **выполнен по букве**: резолвятся оба. Оба **приватны**; `corelib` **пуст** (`This repository is empty`), заведён 2026-09-05 | `gh repo view PRO-Robotech/kaname`; `gh repo view PRO-Robotech/corelib` |
| **П2** | путь модуля службы объявлен ЕЁ СОБСТВЕННЫМ именем | **не выполнен**: объявлен `module github.com/PRO-Robotech/kacho-iam` | `gh api repos/PRO-Robotech/kaname/contents/go.mod -q .content \| base64 -d \| head -1` |
| **П3** | в контракте, схеме, таблицах, ручках, клеймах токена и витрине оператора чужого бренда — ноль | **не выполнен, и производителя у переписи нет** — держатель `sev-name-residue` объявлен без исполнителя (`holders.yaml`) | производитель заводится этим переходом; до него — «не выполнилось» |
| **П4** | фундамент вынесен в свой модуль; клиентские пакеты службы уехали с ней; цикла между модулями нет | **не выполнен**: `corelib` пуст, а `kaname` требует `github.com/PRO-Robotech/kacho v0.0.0-20260904231955-a30d906b8edf` — то самое ребро, которое в целевой раскладке даёт цикл | `gh api repos/PRO-Robotech/kaname/contents/go.mod -q .content \| base64 -d \| grep PRO-Robotech` |
| **П5** | служба собирается и поднимается вне дерева монорепо | **половина**: сборка свежего клона записана исполненной (§0.7 п. 4); подъём производителя не имеет — `IAM-SEV-BOOT-01` помечен «ждёт производителя» | приёмка §2, семейство `BOOT` |
| **П6** | открытых задач `area:iam` — ноль | **не выполнен**: в `kacho` — **37**, в воркспейсе — **0** | `gh issue list -R PRO-Robotech/kacho --state open --label area:iam --limit 200 --json number -q '.[].number' \| wc -l` |
| **П7** | локальный прогон и конвейер зелёные целиком | **не измерено**: дерева продукта в рабочей копии этого пакета нет | требует клона продукта; см. §1 о том, почему пакет живёт отдельно от дерева |

**Итог замера, названный точно, а не округлённо:** выполнен по букве **один** — П1,
и то с оговорками, которые он сам не запрещает (оба репозитория приватны, `corelib`
пуст). Не выполнены **пять** — П2…П6. Не измерялся вовсе **один** — П7, и он не
зачитывается ни в успех, ни в отказ. «Почти готово» здесь было бы заявлением шире
сделанного.

---

### ОТМЕНЁННОЕ решение владельца 2026-09-04 — дословная запись

**Было решено, дословно по существу:**

1. `services/iam` выносится в отдельный репозиторий вместе со всем, что к нему
   относится;
2. **общий фундамент `pkg/` и контракты `proto/` ОСТАЮТСЯ в репозитории kacho.**
   Все прочие потребители ссылаются на них внутри дерева; **вынесенный iam
   ссылается как на внешнюю зависимость** — versioned Go-модуль;
3. вынос фундамента и модулей отдельными репозиториями — **перспектива**, и
   форма зависимости обязана быть к ней готова;
4. консоль (`ui-future/`) остаётся в kacho;
5. iam обязан быть **production-ready для установки в чужом облаке**;
6. на выходе — **закрытые задачи по iam**, включая заведённые параллельно.

**Судьба каждого пункта:**

| пункт 2026-09-04 | что с ним стало |
|---|---|
| 1. выносится вместе со всем, что к нему относится | **устояло** и уточнено: с ним уезжают контракт доступа и его клиентские пакеты |
| 2. фундамент и контракты остаются в kacho, iam ссылается на них извне | **отменено в части МЕСТА**: фундамент уезжает в `corelib`, контракт доступа — в `kaname`. **Форма** зависимости (versioned-модуль, ноль директив замены) устояла и не пересматривается |
| 3. вынос фундамента отдельным модулем — перспектива | **отменено**: это предмет ЭТОГО перехода (П4) |
| 4. консоль остаётся в kacho | **устояло** — подтверждено дословно |
| 5. production-ready для чужого облака | **устояло** |
| 6. на выходе — закрытые задачи по iam | **устояло** и получило предикат: П6 |

**Судьба каждого следствия — по одному на подраздел ниже:**

| следствие | что с ним стало |
|---|---|
| §0.1 дебрендинг отменён by construction | **ОТМЕНЕНО** |
| §0.2 фундамент пермиссивен, оставшись в kacho | **ЧАСТИЧНО УСТОЯЛО** — пермиссивность да, место нет |
| §0.3 kacho обязан публиковать версии | **УСТОЯЛО и усилено** |
| §0.4 готовность к будущему выносу фундамента | **ИЗМЕНИЛО СМЫСЛ** — из перспективы в предмет |
| §0.5 модель — на установку | **НЕ ЗАДЕТО** |
| §0.6 Р2 рычаг и Р6 имя репозитория | **УСТОЯЛО**; исход Р6 стал конкретным |
| §0.7 разрез исполнен | **сделанное — факт**, перечень остатка ПЕРЕПИСАН |

**Почему записи не удалены.** Работа по отменённому решению исполнена и наблюдаема:
репозитории заведены, начальный импорт сделан, версия закреплена. Удалив запись, мы
оставили бы дерево, объяснить которое нечем, — и следующий читатель принял бы следы
за ошибку. Отменённое решение остаётся фактом истории; ложным становится только его
предписывающая сила, и она снята вердиктами ниже.

### 0.1. ОТМЕНЕНО — «дебрендинг отменён by construction»

> **Вердикт 2026-09-06: ОТМЕНЕНО целиком.** Довод этого следствия держался на
> **необходимости** импорта `github.com/PRO-Robotech/kacho/pkg/...`. Решение
> 2026-09-06 эту необходимость снимает: фундамент уезжает в `corelib`, и путь
> модуля, который iam обязан импортировать, чужого бренда больше не несёт.
> Довод истёк вместе со своим предметом — это не ошибка прежней записи, а смена
> предмета под ней.
>
> **Взамен действует П3:** в контракте, схеме, таблицах, ручках, клеймах токена и
> витрине оператора чужого бренда — **ноль**. Шесть поверхностей названы владельцем
> поимённо, и дебрендинг возвращается вехой.
>
> *О шестой поверхности.* В директиве владельца она записана как «трине оператора»;
> читается как **витрина оператора** — ровно та поверхность, которую `kacho#2076`
> Р3 перечисляет поимённо (имя чарта, объекты Kubernetes, метки, имя образа, пути
> монтирования, переменные окружения, клиентская документация). Прочтение названо
> здесь вслух, чтобы оно было опровержимо, а не подразумевалось.
>
> **Возвращается вопрос об имени схемы** (Р5, §7): §0.1 снимал его вместе с
> дебрендингом; П3 называет схему и таблицы прямо. Ban #5 при этом не смягчается —
> применённая миграция не правится, значит переименование идёт новой миграцией и
> переводом всех читателей одним изменением.
>
> **Расхождение, которое надо назвать вслух, а не обойти.** Эпик `kacho#2076`
> (решения оркестратора 2026-09-05) держит **Р1** «контракт остаётся
> `kacho.cloud.iam.v1`» и **Р2** «схема `kacho_iam` остаётся». П3 владельца требует
> обратного по обеим. Два места об одном предмете; действует **позднее и высшее** —
> слово владельца. Приведение `kacho#2076` к факту — работа, у которой сегодня нет
> задачи; она возвращена оркестратору текстом этим же заходом и здесь координатой
> не называется: задачи ещё нет, и ссылка на неё была бы мёртвым адресом.

**Ниже — запись прежнего следствия, сохранённая дословно.**

Вынесенный iam **импортирует** `github.com/PRO-Robotech/kacho/pkg/...`. Значит имя
продукта остаётся в дереве iam **необходимым образом**, и условие «ноль упоминаний
Kachō вне посева» перестаёт быть целью — не потому, что его трудно достичь, а
потому, что оно противоречит принятой форме зависимости.

**Что это снимает:** веху дебрендинга целиком, вопрос о пяти пространствах имён,
и половину вопроса об имени схемы БД — переименовывать схему больше незачем.

**Что остаётся:** тексты, обращённые к арендатору чужого облака (страница
документации, сообщение отказа, имя чарта), где чужой бренд читается как ошибка,
а не как имя зависимости. Это **другой** предмет и другой объём.

### 0.2. ЧАСТИЧНО УСТОЯЛО — фундамент обязан стать пермиссивным

> **Вердикт 2026-09-06: половина устояла, половина отменена.**
>
> **Устояло — пермиссивность, и она усилена.** Довод §10 AGPL не тронут ничем:
> служба под AGPL линкует фундамент, значит фундамент обязан быть пермиссивным.
> После выноса он становится **отдельным модулем**, поэтому требование адресуется
> не подкаталогу чужого дерева, а самостоятельному продукту — и проверяется на нём.
>
> **Отменено — «оставив их в kacho».** Фундамент уезжает в `PRO-Robotech/corelib`.
> Контракты `proto/` при этом **не едут целиком**: с `kaname` уезжает контракт
> доступа, остальные остаются платформе.
>
> **Добавлено решением 2026-09-06 — ПУБЛИЧНОСТЬ фундамента.** Пермиссивная лицензия
> и публичная доступность — разные вещи, и вторая названа владельцем отдельно:
> служба под AGPL не вправе ссылаться на фундамент, недоступный публично. Сегодня
> `corelib` **приватен и пуст** (замер П1). Решение о его публичности владелец
> принимает **после** публикации версии платформы из ствола — это открытое решение,
> и в §7 оно заведено как **Р9**.

**Ниже — запись прежнего следствия, сохранённая дословно.**

AGPL-код **не вправе** линковать фундамент под лицензией, налагающей
дополнительные ограничения (§10 AGPL). Нынешняя лицензия дерева именно такова —
она не является открытой по собственному тексту. Значит форма зависимости,
выбранная владельцем, **требует** перелицензировать `pkg/` и `proto/` в
пермиссивную (Apache-2.0), оставив их в kacho.

Это не расширение объёма, а условие исполнимости: без него зависимость
невыразима юридически, при том что технически она собирается.

### 0.3. УСТОЯЛО и усилено — публикация версий

> **Вердикт 2026-09-06: устояло, и предмет вырос.** Публикация версий остаётся
> несущей: без неё ни один потребитель не соберётся по объявленной версии, а
> собранное «рядом лежащим деревом» о публикации не говорит ничего.
>
> **Что изменилось.** Публикаций теперь **две** — платформы и фундамента, — и
> порядок между ними задан владельцем: сперва версия платформы из ствола, потом
> решение о публичности фундамента. Направление зависимости при этом
> **перевёрнуто** против прежней записи: не служба пинит платформу, а платформа
> пинит службу; служба пинит фундамент.
>
> **Что при этом остаётся верным дословно:** `PRO-Robotech/kacho#1766` (монорепо не
> публикует версий) по-прежнему открыт и по-прежнему несущий. Замер 2026-09-06:
> зависимость `kaname` на платформу закреплена **псевдоверсией**
> `v0.0.0-20260904231955-a30d906b8edf`, то есть публикации семантической версии не
> было ни одной.

**Ниже — запись прежнего следствия, сохранённая дословно.**

Внешней зависимости нужен адрес и версия. Монорепо сегодня версий не публикует —
это открытая задача `PRO-Robotech/kacho#1766`, и она **из смежной становится
несущей**: пока публикации нет, вынесенный iam собирается только рядом лежащим
деревом, то есть не является отдельным продуктом.

### 0.4. ИЗМЕНИЛО СМЫСЛ — вынос фундамента стал ПРЕДМЕТОМ, а не перспективой

> **Вердикт 2026-09-06: следствие живо, но описывает другую работу.** Прежняя
> запись требовала **готовности** к переезду, который когда-нибудь случится, и
> выводила из этого форму объявления зависимости. Переезд назначен на **этот**
> переход (П4), поэтому требование перестаёт быть про подготовку и становится про
> исполнение.
>
> **Что из прежней записи устояло дословно:** поверхность фундамента обязана быть
> **объявленной**, а не выведенной из того, что кто-то однажды импортировал. Это
> требование теперь не про будущее, а про содержимое `corelib`: в него едет
> объявленная поверхность, а не то, что оказалось нужным при первой сборке.
>
> **Что перестало быть верным:** формулировка «переезд не меняет **ни одного**
> импорта». Импорты меняются **ровно один раз** — сейчас, с `kacho/pkg/...` на
> `corelib/...`, — и после этого второго переезда в замысле нет. Требование
> «импорты до и после совпадают» было наблюдаемым свойством прежней раскладки; в
> новой у него нет предмета, и приёмка правится вместе с этим (`IAM-SEV-DEP-04`).

**Ниже — запись прежнего следствия, сохранённая дословно.**

Владелец назвал перспективу прямо. Значит зависимость объявляется так, чтобы её
последующий переезд в свой репозиторий не менял **ни одного импорта** у
потребителей: путь модуля остаётся, меняется только то, где он живёт. Форма, при
которой iam импортирует внутренние подпакеты kacho вразнобой, этому противоречит —
поверхность фундамента обязана быть **объявленной**, а не выведенной из того, что
кто-то однажды импортировал.

### 0.5. НЕ ЗАДЕТО решением 2026-09-06 — модель НА УСТАНОВКУ

> **Вердикт 2026-09-06: не задето, и это сказано вслух, а не пропущено.** Решение
> 2026-09-06 говорит о **раскладке модулей и об имени**; предмет §0.5 — форма
> подачи модели прав, — и они не пересекаются ни одной осью. Четыре замка, их
> доводы и порядок снятия остаются в силе дословно.
>
> Сказано отдельной строкой потому, что «смотрел и не задето» отличимо от «не
> смотрел» только тогда, когда это написано.

Формулировка «iam принимает на вход ямлики с моделью клиентских модулей,
ресурсов и глаголов, дальше сам делает интеграцию на запись кортежей и проверки
прав» имела **два прочтения**, и владелец выбрал первое:

**(а) модель на УСТАНОВКУ** — YAML подаёт оператор чужого облака при
развёртывании; все арендаторы этой инсталляции делят один набор типов.

Отвергнуто: **(б) модель на арендатора**, где каждый объявляет свои типы.

**Что это снимает.** Пространство имён типа, разбор модели на каждого
арендатора вместо одного, правило столкновения имён между арендаторами и ответ
на записанный в коде довод — два разбора дали бы форму, где входной контроль
отвергает запрос, который вердикт разрешает. Всё это работа, которой теперь нет.

**Что это НЕ снимает** — четыре замка, каждый заперт осознанно и с записанным
доводом:

| # | замок | почему заперт |
|---|---|---|
| 1 | набор модулей закрыт образом | членство спрашивают у строк, но литерал служит левой стороной паритета старта |
| 2 | вход принимает имя модуля, не тело | тело сделало бы применение «кирпичом»: следующий старт применяет доставленное, а страж паритета отказывает на различии |
| 3 | модель вшита в образ | тип вне модели даёт **отказ, а не ошибку** — клиентский тип получал бы вечный отказ, неотличимый от «прав не выдали» |
| 4 | каталог прав порождается из аннотаций контракта | у чужого модуля аннотаций нет вовсе; край сопоставляет маршрут по полному имени метода |

**Порядок снятия задан деревом, а не выбором:** опора паритета переводится с
литерала сборки на применённые манифесты первой — остальные три упрутся в неё.

**Цена, которую надо назвать вслух.** Установка модели после старта сегодня
отвергается, и довод записан. При варианте (а) это приемлемо — оператор подаёт
модель при развёртывании, — но обязано стать **объявленным контрактом**, а не
умолчанием: «объявление нового модуля требует перезапуска» либо снимается, либо
пишется в документации оператора.

### 0.6. УСТОЯЛО — решения Р2 и Р6, принятые оркестратором 2026-09-05

> **Вердикт 2026-09-06: устояло; исход Р6 стал конкретным.**
>
> **Р2 (AGPL + соглашение о вкладе до открытия) не тронут ничем.** Его довод — про
> необратимость окна, а не про раскладку модулей. Решение 2026-09-06 добавляет к
> нему **вторую** лицензионную ось: у фундамента своя лицензия и своя публичность
> (§0.2, Р9), и AGPL службы связывает их порядком, а не текстом.
>
> **Р6 (архивировать занятое имя, завести новый репозиторий) исполнен, и имя
> названо владельцем.** Замер 2026-09-06: `PRO-Robotech/kacho-iam-polyrepo-archive`
> — публичный и **заархивирован**; `PRO-Robotech/kacho-iam` **резолвится
> переадресацией на `PRO-Robotech/kaname`**, то есть заведённый по §0.7 репозиторий
> уже переименован. Имя продукта — **Kaname**, решение владельца 2026-09-05, эпик
> `PRO-Robotech/kacho#2076`; здесь оно не пересказывается.
>
> **Довод про начальный импорт устоял** и решением 2026-09-06 не тронут: он про
> адъюдикацию исторических путей, а не про место фундамента.

Владелец делегировал решения прямо: «Все решения принимаешь ты. С моей стороны только
требования». Оба вопроса держали разрез и потому решены здесь, по пяти вопросам ban #18.
Владелец вправе пересмотреть; до пересмотра действуют эти.

**Р2 — коммерческий рычаг: AGPL-3.0-or-later ПЛЮС соглашение о вкладе, принятое ДО
открытия репозитория.**

Отвергнуто «AGPL без соглашения» по оси **перспективности**: ошибка необратима.
Открытие без соглашения закрывает двойное лицензирование **навсегда** — вернуться можно
только собрав подписи каждого стороннего автора поимённо, а число их растёт с первого
дня. Сегодня цена нулевая: почти вся история домена принадлежит одному автору.

Отвергнуто «оставить нынешнюю лицензию» по оси **благо продукта**: она не является
открытой по собственному тексту, и продукт, объявленный опенсорсным под ней, вводит
арендатора в заблуждение — это не выигрыш, а долг доверия.

**Р6 — имя репозитория: `kacho-iam` архивируется, продукт заводится под новым именем;
история едет начальным импортом одним коммитом.**

По имени. Три исхода названы в задаче #1763, и только один не оставляет **второго живого
претендента**: архивировать занятый и завести новый. Оживление даёт репозиторий без
защиты ствола, с пятнадцатью чужими ветками и описанием, пережившим свой предмет;
«просто новое имя» оставляет старый торчать публично и находиться по имени **первым** —
внешний потребитель склонирует состояние полуторамесячной давности и не узнает об этом.

По истории — начальный импорт, и довод не тот, что был в аудите. Прежний («перенос
закрепляет схему `kacho_iam` навсегда») **ослаб**: решение владельца отменило дебрендинг,
и имя схемы остаётся в любом случае. Действующий довод — §«Публичные артефакты»
`security.md`: **1827 исторических путей не поддаются доказуемой адъюдикации** на признак
восстановимости, а история едет в публичный репозиторий целиком и навсегда. Цена названа
честно: `git blame` по домену теряется, и это записывается в тело начального коммита
вместе с именем монорепо и точной ревизией.

**Что остаётся за владельцем и не решается здесь:** нажатие. Архивация чужого
репозитория и первая публикация — необратимые внешние действия; они делаются его рукой
либо его явным словом, а не выводом из этой записи.

### 0.7. Разрез исполнен 2026-09-05 — сделанное факт, перечень остатка ПЕРЕПИСАН

> **Вердикт 2026-09-06: перечень «сделано» остаётся фактом; перечень остатка
> заменён.** Четыре исполненных шага произошли и наблюдаемы — их правка сделала бы
> ложными утверждения, которые верны. Заменён порядок остатка: решение 2026-09-06
> вставляет в него вынос фундамента, переименование пути модуля и дебрендинг, а
> направление зависимости переворачивает.

**Сделано, и каждый шаг обратим:**

1. прежнее полирепо **осушено**: из 26 открытых задач 13 закрыты замером по дереву
   монорепо, 13 перенесены в него с сохранением признака и предиката; открытых PR 0;
2. занятое имя освобождено: репозиторий переименован в `kacho-iam-polyrepo-archive`
   и **заархивирован** (архивацию снимают, переименование откатывают);
3. заведён `PRO-Robotech/kacho-iam` — **приватный**, наполнен деревом службы
   (2188 файлов) начальным импортом с названной ревизией источника;
4. **свежий клон собирается**: ни соседних деревьев, ни `go.mod` выше по дереву —
   зависимость платформы тянется с публичного прокси по версии, директив замены ноль.

**Почему репозиторий ПРИВАТНЫЙ, а не публичный.** Публикация сегодня распространяла бы
службу под AGPL вместе с закреплённой версией платформы, чей фундамент в **опубликованной**
ревизии остаётся под прежней лицензией. §10 AGPL этого не допускает. Пермиссивный фундамент
живёт в ветке линии и приедет к службе только с публикацией версии **из неё**.

> **Что в этом абзаце ПЕРЕСТАЛО быть верным (2026-09-06).** «Пермиссивный фундамент
> приедет к службе с публикацией версии платформы» — так было при раскладке, где
> служба пинит платформу. В целевой раскладке служба платформу **не пинит вовсе**:
> фундамент приезжает к ней из `corelib` своей версией. Довод про §10 AGPL при этом
> устоял целиком и стал шире: он адресуется теперь `corelib`, а не ревизии платформы.

**Порядок остатка — ПЕРЕПИСАН решением 2026-09-06.** Первые два шага названы
владельцем и не переставляются; остальные выведены из направления зависимостей.

| # | шаг | чей | почему здесь |
|---|---|---|---|
| 1 | влить линию выноса в ствол монорепо | наш | без ствола публиковать нечего |
| 2 | опубликовать версию платформы из ствола (`scripts/release/publish-version.sh`) | **владельца** — необратимо | назван владельцем первым |
| 3 | решение о публичности фундамента | **владельца** | названо владельцем вторым; открыто как **Р9** (§7) |
| 4 | вынести фундамент в `corelib`, объявить его поверхность, опубликовать версию | наш | П4; `corelib` сегодня пуст |
| 5 | перевести службу с зависимости на платформу на зависимость на `corelib` | наш | снимает ребро, которое в целевой раскладке было бы циклом |
| 6 | объявить путь модуля службы её собственным именем | наш | П2 |
| 7 | дебрендинг **десяти** поверхностей: шесть П3 плюс четыре Р10 | наш | П3 и Р10 (§7.1); переименование схемы — новой миграцией, ban #5, и только чистая установка (Р11, §7.2) |
| 8 | закрыть открытые задачи `area:iam` | наш | П6; замер 2026-09-06 — **37** |
| 9 | сделать `kaname` публичным | **владельца** — необратимо | требует шага 3: публичного фундамента |

**Порядок 3 → 9 не переставляется by construction:** публикация службы под AGPL
раньше решения о публичности фундамента даёт ровно то состояние, которое §10 AGPL не
допускает, — и откатить публикацию нельзя.

**Остаток, названный честно:** защита ствола нового репозитория **не заведена** — она
недоступна приватному репозиторию на нынешнем плане. Появится вместе с публикацией
(шаг 9) и обязана быть заведена тем же заходом: прежний репозиторий пришлось
архивировать в том числе потому, что защиты у него не было никогда.

**Второй остаток, названный числом:** ни шаг 4, ни шаг 7 сегодня **не имеют задачи**.
Их текст с признаком, предметом и предикатом снятия возвращён оркестратору этим же
заходом; в перечень вех `change.yaml` они не вносятся, пока задачи нет — веха без
задачи есть координата без предмета.

---

## 1. Эпоха и обязательность контура — замер, а не чтение

Обязательность пакета выводится из ancestry базы изменения относительно
`cutover_commit` своего репозитория (§4, §8 приёмки SDD-1); эвристика имени ветки
запрещена. Замер на 2026-09-04:

| DAG | cutover | резолвится | предок ствола | отсутствует у соседа |
|---|---|---|---|---|
| `kacho-workspace` | `1202e3cf5f6c…` | да | да | да |
| `kacho` | `c2d573654cd7…` | да | да | да |

База ветки, на которой заводится изменение, **содержит** cutover воркспейса.
Отсюда три следствия, и каждое меняет форму работы:

1. **позиция — `package required`**, исключений у неё приёмка не объявляет ни
   одного. Bootstrap-исключение выдано поимённо паре «SDD-1 + `ws#480`» и к
   этому изменению неприменимо;
2. **маршрута у изменения нет вовсе.** `legacy` и `migrate` — значения поля
   `route` в **реестре наследия**, то есть свойство записи о чужом, уже
   существовавшем изменении. `ws#382` в реестре отсутствует, и дописать его
   задним числом нельзя: реестр — точное множество, лишняя запись даёт красное;
3. **баз две, и обе post-cutover** — требование пакета приходит с обеих сторон.

**Пакет живёт в воркспейсе.** Каталога `docs/changes/` в дереве продукта не
существует; продукт держит идентификатор изменения и пин ревизии воркспейса.

> **Что при этом НЕ верно.** Контур обязателен по норме и по вердикту приёмки,
> но **машинно ещё никого не блокирует**: производственного вызывающего, который
> подавал бы испытуемому живого кандидата, в дереве нет — его интерфейс принимает
> только мир кейса. Пакет пишется потому, что этого требует §8, а не потому, что
> иначе покраснеет отправка. Сказано прямо, чтобы «гейт проверит» не читалось как
> «проверено».

---

## 2. Решения о форме пакета — их приходится принимать, потому что их никто не задаёт

Главная находка подготовки: **имён ключей `change.yaml` и `holders.yaml` не
задаёт ни один источник**. Приёмка SDD-1 называет предметы владения (§2) и
обязательный состав фактов (§7: одиннадцать величин машинного держателя), но ни
одного ключа. Испытуемый называет координаты **мира кейса** — это его
собственный словарь, а не схема файла пакета. Схема есть только у фикстуры, и
она объявляет себя моделью.

Значит ключи — **решение автора**, и оно записывается здесь, а не подбирается
молча.

### 2.1. `change.yaml` — координаты, состояние, отпечатки, ссылки

| ключ | значение |
|---|---|
| `schema_version` | версия формы пакета; `1` |
| `change_id` | `standalone-iam` |
| `lifecycle_state` | одно из десяти состояний §5 приёмки SDD-1 |
| `coordinates` | задача, репозитории и их базы |
| `hashes` | отпечатки `acceptance.md` и `design.md` — те же, которыми ключуются записи ревью |
| `links` | связанные изменения; `superseded_by`/`supersedes` при боковых состояниях |

**Чего в манифесте не будет и почему:**

- **нормативной прозы.** Манифест несёт координаты; требование, внесённое в него
  абзацем, — нарушение с диагностикой `CG_HUMAN_MANIFEST_PROSE`;
- **поля `route`** — оно принадлежит чужому реестру (§1);
- **поля `epoch`** — она выводится, а не объявляется; объявленная эпоха стала бы
  вторым источником для того, у чего источник один.

### 2.2. `holders.yaml` — точное множество держателей

Форма взята у фикстуры дословно, потому что она единственный конкретный образец
в дереве, и различие в ней осмысленно: **пять величин живут в объявлении, шесть
остальных — снятые, и живут в свидетельстве**.

| ключ держателя | значение |
|---|---|
| `kind` | `machine` либо `human-external` |
| `owner` | роль из реестра полномочий `policy.yaml` |
| `executable` | точная команда; значение вида «истина» запрещено — держатель, который не может упасть, держателем не является |
| `predicate` | что именно утверждается — одной фразой |
| `evidence_coordinate` | путь записи свидетельства |

**Ноль держателей — красное, а не пустой зелёный.** Это то же требование, что
«ноль находок обязано быть отличимо от ноль прочитанного», применённое к
держателю.

### 2.3. `evidence/<holder-id>/<subject-sha256>.yaml` — план, написанный до прогона

План свидетельств **отдельным файлом не является**: он и есть запись
свидетельства, написанная заранее с **пустым** полем исхода. Ожидание в поле
исхода — выдача намерения за наблюдение; поле заполняет прогон, а не перо.

> [!warning] Два плана свидетельства ОСИРОТЕЛИ, и один из них — до редакции 2
> **Находка, а не мусор,** и она называется здесь, потому что молча переименовать
> файл значило бы стереть единственный след того, что ключ когда-то не сошёлся.
>
> Планы ключуются отпечатком предмета. Замер 2026-09-06:
>
> | ключ | планов | предмет по записи | сходится ли |
> |---|---:|---|---|
> | `3d1fdd7302c2…` | **21** | у двух — `acceptance.md`, у девятнадцати — «пакет на состоянии ISSUE_READY» | это отпечаток `acceptance.md` |
> | `f5fcc146d6b4…` | **2** (`human-design`, `human-class-exposure-revalidation`) | `design.md` у обоих | **не сходится ни с одной ревизией `design.md`** |
>
> Второй ключ осиротел **не редакцией 2**: обе ревизии `design.md` в истории дают
> `9bf78c51a934…` (предикат: `for c in $(git log --format=%H -- design.md); do git
> show $c:<путь> | sha256sum; done`), то есть `f5fcc146…` не был отпечатком этого
> документа **никогда**. Планы были осиротевшими в момент принятия пакета в git.
>
> Редакция 2 добавляет к этому **вторую** осиротевшую связку: правка `design.md`
> меняет его отпечаток, а правка `acceptance.md` — свой, и все **23** плана
> перестают сходиться с текущими предметами. Наблюдений это не уничтожает: у всех
> двадцати трёх поля исхода **пусты** — ни одного прогона не было.
>
> **Машинно это никем не ловится**, и сказать надо прямо. Правило семейства
> `CG_HOLDER_SUBJECT_HASH_MISMATCH` у испытуемого **есть** (предикат:
> `grep -ro 'CG_HOLDER_SUBJECT_HASH_MISMATCH' scripts/change-graph-gate/`), но
> производственного вызывающего, который подавал бы ему этот пакет, в дереве нет —
> ровно то, о чём предупреждает врезка §1. Перекеивание планов — работа, у которой
> нет задачи; её текст возвращён оркестратору этим же заходом.
>
> **Перемер редакции 3 (2026-09-06): осиротевших по-прежнему 23, и редакция 3 не
> добавила НИ ОДНОГО.** Число не совпадение, а следствие: осиротели уже все, и
> осиротить нечего.
>
> | ключ планов | планов | чем был | когда осиротел |
> |---|---:|---|---|
> | `3d1fdd7302c2…` | **21** | `acceptance.md` **редакции 1** | редакцией 2 — до этой работы |
> | `f5fcc146d6b4…` | **2** | **ничем**: ни одной ревизией ни одного артефакта | с момента принятия пакета в git |
>
> Предикат, дающий обе строки за один заход и **не** полагающийся на память:
> `for c in $(git log --format=%H -- <путь>); do git show $c:<путь> | sha256sum; done`
> по `design.md` даёт `9bf78c51…` (ревизия 1) и `a93cf2fc…` (ревизия 2), по
> `acceptance.md` — `3d1fdd73…` и `cf4648c2…`. Ключа `f5fcc146…` нет **ни в одном**
> из четырёх значений — то есть он не был отпечатком ни того, ни другого никогда.
>
> **Отсюда точный счёт правки редакции 3.** Она меняет отпечаток `design.md`
> (`a93cf2fc…` → новый) и **не трогает** `acceptance.md`. Планов, ключуемых
> `a93cf2fc…`, в пакете **ноль** — значит осиротить она может ноль, и осиротила
> ноль. Прежняя редакция этого абзаца писала «правка `design.md` меняет его
> отпечаток… и все 23 плана перестают сходиться»; для редакции 2 это было верно, для
> редакции 3 — уже нет, потому что сходиться было **нечему**. Сказано числом, а не
> впечатлением: молча переименовать файлы значило бы стереть единственный след того,
> что ключ когда-то не сошёлся.

---

## 3. Пять расхождений источников, известных до первой строки

Подготовка сводила два независимых описания формы — по приёмке и по коду
испытуемого. Расхождения перечислены, потому что каждое **молча** испортит пакет,
если не решить заранее.

| # | предмет | приёмка | код | решение этого изменения |
|---|---|---|---|---|
| 1 | таблица владения предметами истины | десять строк (§2) | шесть, со **строгим равенством** | следовать приёмке: она владеет предметом. Мир кейса, смоделировавший настоящую таблицу, получит красное — это дефект кода, заводится задачей |
| 2 | реестр предикатов освобождения | три, в `policy.yaml` | два, и у одного **другое имя поля** | следовать `policy.yaml`: §5 объявляет реестр версионированным и лежащим там. Копия в коде разошлась молча — задача |
| 3 | расширение записей разбора классов | `.yaml` в пакете, `.md` вне его | не судит | следовать §3 дословно; несогласованность внутри приёмки — задача, не вольность автора |
| 4 | имя роли в связке одобрения | ограничений нет | обязано читаться как `<артефакт>-reviewer`, иначе прогон обрывается | из десяти ролей политики этому отвечают две; для остальных связка **не используется** |
| 5 | реестры, зашитые под SDD-1 | — | перечень ролей до кода, идентификатор изменения, отображение задач | на этот пакет **не переносятся**; их перенос — работа контура, а не выноса |

**Ни одно из пяти не мешает писать пакет.** Каждое зафиксировано здесь, чтобы
следующий не принял их за форму.

---

## 4. Порядок работ и рёбра

Вехи взяты из эпика `ws#382`. Состояние **перемеряется**, а не помнится: предикат —
`gh issue view <N> -R PRO-Robotech/kacho --json state`, замер **2026-09-06**. Каждая
веха — самостоятельное изменение со своим пакетом; этот пакет описывает **переход** и
связывает их.

| веха | задачи | состояние 2026-09-06 | ребро |
|---|---|---|---|
| **M0** лицензионная гигиена | `#1093`, `#1094` | **закрыта** (обе) | ничего не ждёт |
| **M1** три решения записью | `#1103` | **закрыта** | держала M8–M11 — ребро снято |
| **M2** развилка рендеринга | `#1104` | **закрыта** | — |
| **M3** гейт каталога прав | `#1105` | **открыта** | **до M11**, иначе расхождение перестанет ловиться навсегда |
| **M4** снятие push к краю | `#1024` | **закрыта** | — |
| **M5** объектная авторизация | `#1106` | **закрыта** | держала M9 — ребро снято |
| **M6** каталог как данные | `#1027`, `#1087`, `#1092` | **закрыта** (все три) | — |
| **M7** отказ от внешнего поставщика | `#1107` | **закрыта** | ждала Р4 — ребро снято |
| **M8** отвязка от корня | `#1108` | **закрыта** | — |
| **M9** свой край | `#1109` | **открыта** | — (M5 закрыта) |
| **M10** генераторы | `#1110` | **открыта** | ждёт Р7 |
| **M11** разрез | `#1111` | **открыта** | ждёт M3, M9, M10 |

**Открытых вех четыре из двенадцати** — M3, M9, M10, M11. Прежняя редакция называла
восемь открытых: это не прогресс замера, а прогресс дерева, и перемерять надо было
прежде, чем на числе что-то строить.

### 4.0. Работа, которую ВОЗВРАЩАЕТ решение 2026-09-06

Три предмета, снятых или не заводившихся при решении 2026-09-04. **Задач у первых
двух нет ни одной**, поэтому вехами они здесь не нумеруются и в `change.yaml` не
вносятся: веха без задачи — координата без предмета.

| предмет | почему возвращается | предикат снятия |
|---|---|---|
| вынос фундамента в `corelib` и объявление его поверхности | П4; §0.4 из перспективы стал предметом | `corelib` непуст, `kaname` не требует `github.com/PRO-Robotech/kacho`, цикла между модулями нет |
| дебрендинг **десяти** поверхностей: шесть П3 плюс четыре Р10 — заголовки метаданных, домен доверия, якорь кластера, типы ресурсов провайдера | П3; §0.1 отменён. Четыре поверхности добавлены решением владельца 2026-09-06 (Р10, §7.1) | перепись остатка имени печатает объём осмотренного и даёт ноль по каждой из **десяти** поверхностей; якорь кластера идёт **своей** приёмкой и падающей пробой |
| путь модуля службы под её именем | П2 | `module github.com/PRO-Robotech/kaname` в первой строке `go.mod` службы |

Третий предмет задачу **имеет** — эпик `PRO-Robotech/kacho#2076`, где он записан
вместе с решениями оркестратора 2026-09-05; два из этих решений П3 отменяет (§0.1).

### 4.1. Ребро, которое дороже остальных — СНЯТО закрытием M5

**M5 → M9.** Привязок девяносто девять, регистраций ноль — монтаж края выглядит
часовой работой. Смонтировав его раньше объектной авторизации, мы откроем
большинство RPC без пообъектного вопроса. Поверхность, побывавшая публичной,
считается раскрытой, и обратной дороги у этого нет.

> **Состояние 2026-09-06:** задача M5 (`kacho#1106`) **закрыта**, значит ребро
> разряжено и порядок больше ничего не держит. Сам предикат («привязок девяносто
> девять, регистраций ноль») здесь **не перемерялся**: дерева продукта в рабочей
> копии этого пакета нет, и утверждать о нём по факту закрытия задачи значило бы
> читать вердикт по метке, а не по предмету.

### 4.2. Ребро, которое легко пропустить

**M3 → M11.** Гейт согласия двух копий каталога прав обёрнут условием
существования файла. Сегодня файл есть, и гейт исполняется. Разрез уносит копию —
условие перестаёт выполняться **навсегда**, расхождение перестаёт ловиться, а
даёт оно отказ в доступе в рантайме.

---

## 5. Инварианты, которые переход обязан сохранить

1. **ацикличность графа вызовов.** iam объявлен листом; после разреза он им и
   остаётся. Ребро, заведённое разрезом, идёт от потребителя к владельцу;
2. **применённая миграция не правится.** Отсюда — §4.2 приёмки: судьба имени
   схемы решается, а не обходится. Решением 2026-09-06 (П3) она **решена в
   сторону переименования**, поэтому инвариант становится требованием к способу:
   новая миграция и перевод всех читателей одним изменением, а не правка старой;
3. **скрытие существования побайтово.** Отказ по неавторизованному доступу
   остаётся неотличим от отказа по несуществующему;
4. **три категории исхода.** «Не выполнилось» не вычитается из вердикта и не
   зачитывается в успех — ни у одного держателя перехода;
5. **послабление истекает само.** Всякое временное послабление, введённое ради
   перехода, несёт предикат снятия и краснеет, когда снимать станет нечего;
6. **ацикличность графа МОДУЛЕЙ** (заведён решением 2026-09-06). Целевая раскладка
   однонаправленна: `corelib` ← `kaname` ← `kacho`. Обратное ребро — цикл, а не
   неудобство: сборка перестаёт разрешаться by construction. Сегодня такое ребро
   **есть** — `kaname` требует `github.com/PRO-Robotech/kacho` (замер П4), — и
   переход обязан его снять, а не унести с собой.

---

## 6. Что переход ломает в корпусе правил

Правила загружаются в каждую сессию — значит утверждение, ставшее ложным после
разреза, читается чаще любого другого. Работа названа здесь, потому что сегодня
она **ни за кем не закреплена**, и в эпике её нет.

| утверждение | координата | после разреза |
|---|---|---|
| «Топология — ОДНО монорепо (факт)» | `.claude/rules/polyrepo.md` §«Топология» | **ложь**: репозиториев продукта **три** — `kacho`, `kaname`, `corelib`, — и воркспейс четвёртым |
| «Build-граф — ОДИН Go-модуль (факт)» | `.claude/rules/polyrepo.md` §«Build-граф» | **ложь**: модулей три. Правило про запрет директив замены, помеченное «сегодня неприменимо», становится действующей нормой |
| «Обратный раскол — НАМЕРЕНИЕ, а не факт» | там же, врезка | намерение **сбылось**; врезка снимается вместе с раскладкой |
| раскладка каталогов монорепо | там же, таблица «Каталог ↔ прежний репозиторий» | две строки описывают переехавшее: `services/iam/` и `pkg/` |
| «Единственная кросс-репозиторная граница» | там же, §«Порядок работ» | границ **четыре пары**, а не одна |
| гейт запрета директив замены | там же, §«Правило зависимостей» | получает предмет **впервые**: до разреза он тривиально зелен |
| «Переиспользование через общий фундамент `pkg/`» | `.claude/rules/architecture.md` §«Переиспользование» | **перестаёт держаться**: `pkg/` уезжает в `corelib`, и «нужное 2+ сервисам кладём в `pkg/`» адресует чужой модуль |

**Что изменило решение 2026-09-06 в этом разделе.** Прежняя редакция называла две
строки условными («репозиториев **минимум** три», «перестаёт держаться, **как
только** фундамент уезжает») — потому что вынос фундамента был перспективой. Он стал
предметом перехода (П4), значит условности больше нет: обе строки становятся ложными
**обязательно**, а не при некотором исходе.

**Замер объёма 2026-09-06:** правил, называющих `pkg/` домом общего кода, —
**6** (`grep -rln 'pkg/' .claude/rules/ | wc -l`). Число названо как **верхняя
граница кандидатов**, а не как перечень нарушений: адъюдикация по каждому — работа
той вехи, которая правит корпус, и она здесь не сделана.

Правится **тем же изменением**, что и разрез: правило, пережившее свой предмет,
опаснее отсутствующего.

---

## 7. Открытые решения — почему DESIGN_APPROVED недостижим

| # | решение | состояние 2026-09-06 | что открывает | необратимо |
|---|---|---|---|---|
| **Р1** | имя продукта | **закрыто владельцем 2026-09-05**: продукт — **Kaname**, эпик `kacho#2076`. Прежняя запись «остаётся, дебрендинг отменён» отменена вместе с §0.1 | M8–M11 | — |
| **Р2** | коммерческий рычаг и условия вклада | **решено оркестратором** (§0.6): AGPL-3.0-or-later плюс соглашение о вкладе до открытия. Прежняя строка «открыто» расходилась с §0.6 в этом же файле — расхождение снято в пользу §0.6 | M1 (закрыта) | да; окно закрывается первым сторонним вкладом |
| **Р3** | лицензия фундамента | **лицензия решена** (§0.2): пермиссивная. **Место изменено** решением 2026-09-06: фундамент уезжает в `corelib`, а не остаётся в kacho | направление П4 | да |
| **Р4** | полоса поставщика личности | **открыто**, спор двух документов. Задача вехи M7 (`#1107`) закрыта по замеру 2026-09-06; сам спор здесь **не перемерялся** — он требует чтения обоих документов, а дерева продукта в этой копии нет | смысл `BOOT-01` | нет |
| **Р5** | судьба имени схемы | **ВОЗВРАЩЕНО** решением 2026-09-06 и им же решено по направлению: П3 называет схему и таблицы, значит чужого бренда в них ноль. Прежнее «снято §0.1: переименовывать незачем» — ложь с 2026-09-06 | способ переименования (работа, не решение) | да |
| **Р6** | имя репозитория и история | **решено оркестратором** (§0.6) и **исполнено**: занятое имя заархивировано, продукт заведён и переименован в `kaname`. Прежняя строка «открыто» расходилась с §0.6 | M11 | да |
| **Р7** | контракт разметки прав | **открыто** | M10 (`#1110`, открыта) | нет |
| **Р8** | сайт документации и провайдер инфраструктуры | **частично**: консоль остаётся в kacho — подтверждено владельцем дословно 2026-09-06 | — | нет |
| **Р9** | **публичность фундамента `corelib`** — заведено решением 2026-09-06 | **открыто**: владелец принимает его **после** публикации версии платформы из ствола. Сегодня `corelib` приватен и пуст (замер П1) | шаг 9 §0.7 — публикацию `kaname` | да |
| **Р10** | **переименование провода целиком** — заведено решением владельца 2026-09-06 | **закрыто владельцем**: провод переименовывается **весь и сейчас**. Четыре поверхности сверх шести, названных П3: заголовки метаданных, домен доверия, якорь кластера, типы ресурсов провайдера | шаг 7 §0.7 — дебрендинг | да |
| **Р11** | **судьба прежней схемы Postgres** — заведено решением владельца 2026-09-06 | **закрыто владельцем**: только **чистая установка**. Страж старта отказывает в пуске, обнаружив прежнюю схему, называет её и отсылает к инструкции. Автоматического переноса нет | Р5 — способ переименования схемы | да |
| **Р12** | **дом пакета опций авторизации** — заведено решением владельца 2026-09-06 | **закрыто владельцем**: пакет уезжает в `corelib` как нейтральный словарь аннотаций, а не остаётся в `kaname` | П4 — направление стрелок между модулями | нет; довод — §7.3 |

**Открытых три из двенадцати** — Р4, Р7, Р9, — плюс Р8 частично. Ни одно не держит
план целиком: держат они отдельные вехи. Против прежней редакции изменилось два числа
и одно качество: решений стало девять (заведено Р9), открытых — три вместо четырёх, а
Р5 **вернулось из снятых**, потому что снявшее его следствие отменено.

**Редакция 3 (2026-09-06) заводит ещё три — Р10, Р11, Р12, — и все три ЗАКРЫТЫ
владельцем в день заведения.** Число открытых от этого не меняется: решения принимались
вместе с их заведением, а не оставлялись на потом. Разборы — §7.1–§7.3 ниже; каждый
несёт довод владельца, **отвергнутые** варианты и оговорку, потому что решение без
записанной альтернативы читается как единственно возможное и переоткрывается следующим.

**Целевая раскладка из трёх модулей строкой этой таблицы НЕ является, и это не
пропуск.** Она — действующее решение владельца 2026-09-06 и живёт в §0 целым разделом
вместе с предикатом готовности из семи пунктов и вердиктом по каждому следствию
отменённого решения. Второе место о ней здесь завело бы расхождение с §0 на первой же
правке; сказано вслух, чтобы отсутствие строки не читалось как отсутствие решения.

**Р9 связано с Р2 порядком, а не текстом.** Р2 отвечает, под какой лицензией
открывается служба; Р9 — доступен ли публично фундамент, на который она ссылается.
Публикация службы раньше Р9 даёт состояние, которого §10 AGPL не допускает, и
откатить её нельзя.

**Р2 — единственное, у которого есть срок.** Условия принятия вклада надо принять
**до** открытия репозитория: после первого стороннего вклада двойное лицензирование
требует подписи каждого автора поимённо. Сегодня это ещё дёшево — почти все
коммиты домена принадлежат одному автору, файлов соглашения о вкладе в дереве нет.

**Р4 — спор двух документов, а не пробел.** Приёмка F4d объявляет решение
принятым «в сторону переходное», но в разделе решений оркестратора и с оговоркой
о праве владельца пересмотреть; эпик, обновлённый позже, продолжает числить
вопрос открытым. Действует позднее — до вашего слова.

**Р6 исполнен, и это перемерено.** Прежняя редакция писала «остаётся необратимым и
требует ответа перед M11: имя репозитория занято живым публичным». Замер 2026-09-06:
`PRO-Robotech/kacho-iam-polyrepo-archive` — публичный и заархивирован;
`PRO-Robotech/kacho-iam` резолвится переадресацией на `PRO-Robotech/kaname`. Занятого
имени больше нет, необратимая часть исполнена, и абзац оставлен переписанным, а не
удалённым: следующий читатель обязан понимать, почему в истории есть архив.


### 7.1. Р10 — провод переименовывается ВЕСЬ и СЕЙЧАС

**Решение владельца 2026-09-06.** Дебрендинг не ограничивается шестью поверхностями
П3. Переименовывается **весь провод**, и делается это **сейчас**, а не после разреза.

**Довод владельца, дословно по существу:** продукт ещё не развёрнут у арендаторов,
поэтому цена переименования сегодня минимальна — и она **растёт с каждой установкой**.
Довод не про эстетику: каждая поверхность ниже есть координата, которую арендатор
однажды запишет в свою конфигурацию, и с этого момента её смена становится его работой,
а не нашей.

**Четыре поверхности сверх шести П3.** Числа замерены на дереве продукта
`ba79fdc519dc` (2026-09-06); предикат назван у каждой строки, чтобы его можно было
повторить, а не поверить.

| # | поверхность | из → в | объём | предикат |
|---|---|---|---:|---|
| 1 | заголовки метаданных | `x-kacho-*` → `x-kaname-*` | **13** объявлений | `git grep -c '"x-kacho-' -- gateway/internal/principalmeta/keys.go` |
| 2 | домен доверия | `spiffe://kacho.cloud` → `spiffe://kaname.cloud` | **238** вхождений · 219 строк · 88 файлов | `git grep -o 'spiffe://kacho\.cloud' \| wc -l` |
| 3 | якорь кластера | `cluster_kacho_root` → `cluster_kaname_root` | **563** строки · 20+ файлов | `git grep -c cluster_kacho_root \| awk -F: '{s+=$NF} END{print s}'` |
| 4 | типы ресурсов провайдера | `kacho_iam_*` → `kaname_*` | **9** типов | `git grep -roh 'kacho_iam_[a-z_]*' -- terraform/ \| sort -u \| wc -l` |

**Два числа разошлись с директивой, и разрешать их надо уточнением ЕДИНИЦЫ, а не
выбором правдоподобного.**

- **13 заголовков** — сходится **точно**, и единица теперь названа: это объявления в
  каноническом файле ключей (десять полных имён плюс два префикса и пространство имён).
  Тем же предикатом по всему дереву различных полных имён **19**, из них одно —
  синтетическая фикстура пробы, а часть объявлена вторым местом в фундаменте. То есть
  13 — величина **объявлений**, а не **имён**, и переименование обязано пройти по
  обоим местам объявления, иначе разойдётся молча.
- **181 вхождение домена доверия** — **не воспроизведено ни одной моей единицей**:
  тремя единицами выходит 238 (вхождения), 219 (строки), 88 (файлы); без проб — 91
  вхождение в 30 файлах. Величина директивы лежит между «с пробами» и «без проб»,
  значит её единица мне неизвестна. Записано как есть: подгонять число под
  правдоподобное значило бы выдать догадку за замер. Работе это не мешает — предикат
  переписи назван, и он даст свою величину в момент исполнения.

**Оговорка владельца, которая сама по себе задаёт форму работы.** Якорь кластера —
не косметика: ошибка в нём означает потерю доступа **кластерного администратора**, то
есть того единственного, кто эту ошибку мог бы починить. Поэтому шаг идёт **своей
приёмкой и падающей пробой**, а не внутри общей вехи дебрендинга.

**Ban #5 не смягчается.** Якорь заведён **применённой** миграцией (`git grep -ln
cluster_kacho_root -- '*/migrations/*.sql'` → `services/iam/internal/migrations/0001_initial.sql`),
поэтому переименование идёт **новой** миграцией и переводом всех читателей одним
изменением. Правка применённой миграции исключена.

**Отвергнуто — «переименовать после разреза, когда служба заживёт своей жизнью».**
Ось **перспективности**: отзыв ошибки становится дороже с каждой установкой, а после
первой чужой — требует работы арендатора, то есть перестаёт быть нашим. Отложить —
значит принять эту цену, не назвав её.

**Отвергнуто — «переименовать только то, что видит арендатор».** Ось **благо
продукта**: единственный выигрыш — меньше работы сейчас. Провод, переименованный
наполовину, даёт два словаря об одном предмете, и расходятся они молча — ровно класс,
который корпус ловит в чужом коде.

**Что этой записью НЕ сделано.** Ни одна из четырёх поверхностей не переименована;
блоков `moved` в провайдере сегодня **ноль** (`git grep -rln moved -- terraform/` →
пусто). Это намерение с предикатом, а не факт: предикат снятия — перепись остатка
имени по каждой из четырёх поверхностей печатает объём осмотренного и даёт ноль.
Задачи у работы сегодня нет, вехой она поэтому не нумеруется (§4.0).

### 7.2. Р11 — прежняя схема Postgres: ТОЛЬКО чистая установка

**Решение владельца 2026-09-06.** Переноса нет. Страж старта **отказывает в пуске**,
обнаружив прежнюю схему `kacho_iam`, **называет её** в тексте отказа и отсылает к
инструкции установки.

**Довод владельца, дословно по существу:** продукт ещё не в проде — переносить нечего;
а миграция переименования в чужой инсталляции есть операция, за исход которой мы
отвечать не можем. Отказ виден **сразу** и не даёт потерять данные молча.

**Отвергнуто — новая миграция `ALTER SCHEMA`.** Ось **производственность**: у неё нет
предмета (переносить нечего) и есть режим, годный только для нашего стенда. Ось
**безопасность**: она расширяет поверхность в сторону, где отказ наступает **в чужой
базе**, а откат её — вторая миграция поверх первой.

**Отвергнуто как исход вообще — тихая работа поверх пустой новой схемы.** Служба
поднялась бы, увидела пустую `kaname`, начала бы с нуля, а прежние строки остались бы
рядом невидимыми. Арендатор потерял бы данные **молча** — то есть узнал бы об этом не
от продукта, а по последствиям. Молчание здесь дороже отказа на порядок, и потому это
не вариант размена, а исход, которого не существует.

**Форма отказа задана и не выхолащивается.** Текст обязан назвать **обнаруженную
схему** и **что делать дальше** — это одно из трёх мест, прямо выведенных из-под
запрета §«Публичные артефакты» (`security.md`): сообщение отказа при refuse-to-start
есть рантайм-диагностика оператору, и без имени ручки и причины стенд не поднять.
Отказ, не восстанавливающий следующий шаг клиента, — находка по ban #18, а не
строгость.

**Связь с Р5 — порядком, а не текстом.** Р5 отвечает, **переименовывается** ли схема
(да, П3); Р11 отвечает, **что делать с уже существующей прежней**. Второе без первого
беспредметно, первое без второго оставляет установку, которая молча не сойдётся.

**Дом стража назван, потому что иначе требование неисполнимо.** Проверка посадки
службы живёт в `services/iam/internal/apps/kaname/config/`; общий дескриптор посадки —
`pkg/servicecontract`. Собственная проверка службы — **дополнение** к общему, а не
замена, и заводится ровно потому, что судит ось, которой у общего нет: наличие чужой
схемы в её собственной базе.

**Объём, который переименование затрагивает** — **568** строк в **99** файлах
(`git grep -c kacho_iam | awk -F: '{s+=$NF} END{print s}'`; `git grep -l kacho_iam | wc -l`),
замер `ba79fdc519dc`. Число названо **верхней границей кандидатов**, а не перечнем:
адъюдикация по каждому — работа вехи, и здесь она не сделана.

**Что этой записью НЕ сделано.** Стража, отказывающего на прежней схеме, в дереве
**нет**; падающей пробы на него — тоже. Намерение с предикатом: страж отказывает в
пуске на базе с `kacho_iam` и проходит на чистой, и обе стороны доказаны инъекцией.

### 7.3. Р12 — пакет опций авторизации уезжает в `corelib`

**Решение владельца 2026-09-06.** `kacho/iam/authz/v1` уезжает в `corelib` как
**нейтральный словарь аннотаций**, а не остаётся в `kaname`.

**Довод владельца, дословно по существу:** аннотации авторизации не принадлежат ни
платформе, ни службе. В `corelib` обе стороны зависят от него **вниз**, и стрелка
`kacho → kaname` заводится только **настоящим** контрактом доступа.

**Замер, на котором довод держится** (дерево продукта `ba79fdc519dc`, 2026-09-06):

| величина | значение | предикат |
|---|---:|---|
| контрактов, импортирующих пакет | **69** | `git grep -l 'kacho/iam/authz/v1' -- 'proto/**/*.proto' \| wc -l` |
| из них сам контракт доступа | **22** | тот же вывод, отбор `kacho/cloud/iam` |
| из них сам пакет опций | **1** | тот же вывод, отбор `kacho/iam/authz` |
| **чужих контрактов** | **46** | 69 − 22 − 1 |
| доменов среди чужих | **10** по каталогу, **9** без `kacho/cloud/api` | `\| awk -F/ '{print $2"/"$3"/"$4}' \| sort -u` |
| чужих контрактов, импортирующих **контракт доступа** | **1** — `proto/kacho/cloud/quota/v1/quota.proto` | `git grep -n '^import.*kacho/cloud/iam/v1' -- 'proto/**/*.proto'` |

Величины директивы — 46 чужих контрактов, 22 у самого контракта доступа и **ровно один**
настоящий импорт — воспроизведены **точно**. Разошлись две мелочи, и обе разрешаются
единицей: доменов 10, если считать по каталогу, и 9, если `kacho/cloud/api` за домен не
считать (там лежат общие файлы опций, а не домен); строк 69, а не 67 — я считал файлы, а
каждый несёт ровно один импорт.

> [!warning] Последняя строка таблицы — та, где предикат по подстроке ЛЖЁТ
> Отбор `git grep -l 'kacho/cloud/iam/v1'` даёт **два** чужих файла, и второй —
> `proto/kacho/cloud/reference/reference.proto`, где совпадение сидит **в
> комментарии**. Настоящих операторов импорта **один**. Различает только якорь
> `^import`: предикат обязан судить исполняемое, а не текст (`testing.md`
> §«Гейт на класс», п. 4). Названо здесь, потому что на этом числе держится довод
> владельца, и ошибка в нём удвоила бы объявленное число стрелок между модулями.

**Отвергнуто — оставить пакет в `kaname`.** Ось **перспективность**: тогда **каждый**
домен платформы получает зависимость от `kaname` — сорок шесть контрактов из девяти
доменов, — и переустановить платформу без службы становится нельзя **by construction**.
Ось **благо продукта**: выигрыш только в том, что сегодня не надо трогать 69 строк
импорта; завтра их станет больше, а отзыв решения потребует того же самого.

**Отвергнуто — размножить пакет по потребителям.** Ось **производственность**: словарь
аннотаций в N копиях расходится молча, и расхождение обнаружится отказом в доступе в
рантайме, а не сборкой.

**Необратимость: НЕТ, и довод назван, потому что «важно» и «необратимо» — разные оси.**
Критерий один: отзыв ломает внешне-адресуемую координату либо требует миграции данных
арендатора. Ни того, ни другого:

- **внешне-адресуемой координаты у пакета нет.** Арендатор адресуется по `id` (ban #15)
  и зовёт методы по их полному имени; имя пакета аннотаций в этом не участвует. Каталог
  прав, который аннотации порождают, ключуется **полным именем метода** и значением
  разрешения — предикат: первые ключи каталога суть `fqn` вида
  `kacho.cloud.<домен>.v1.<Служба>/<Метод>`, имени пакета опций в ключе нет вовсе.
  Значит переезд каталог не перекеивает;
- **миграции данных арендатора нет**: аннотации потребляются генерацией, а не
  хранилищем. Ни одна строка ни одной таблицы имени этого пакета не несёт.

Отзыв стоит механической правки одной строки импорта в каждом из 69 контрактов и
повторной генерации. Дорого по объёму — но обратимо, а это разные вещи.

**Что необратимо РЯДОМ и почему записано не здесь.** Необратима **публикация**
`corelib` — она уже заведена как Р9 и связана с Р2 порядком. Пометить необратимым ещё
и Р12 значило бы завести второе место об одном предмете: следующий читатель принял бы
переезд пакета за точку невозврата и стал бы искать её защиту не там, где она есть.

**Оговорка владельца.** Имя пакета несёт чужой бренд и **подлежит переименованию при
переезде**. Необратимость этого шага несёт **Р10**, а не Р12: переименование — часть
дебрендинга провода, и его цена уже названа там. Разведено намеренно, чтобы одна
работа не считалась дважды.

**Что этой записью НЕ сделано.** `corelib` сегодня **пуст** (замер П1), пакет не
переехал, ни один из 69 импортов не правлен. Предикат снятия: `corelib` несёт пакет
опций, ни один контракт платформы не импортирует его из `kaname`, и настоящих стрелок
`kacho → kaname` ровно столько, сколько импортов контракта доступа — сегодня один.

---

## 8. Разбор классов риска — вход, которого ещё нет

Отображение «пункт разбора → решение замысла» здесь **отсутствует**, и это не
упущение: первичный разбор ведёт `class-exposure-analyst`, его запись — вход для
этой части, а роль чужая. Записи разбора нет ни одной.

**Редакция 2 этого не меняет и не может.** Пересверка разбора ключуется отпечатком
замысла, поэтому правка замысла обесценивает её **by construction** — это верно, а
не досадно (`change-graph.md` §2). Обесценивать, впрочем, было нечего: записи нет ни
одной, а её план осиротел раньше редакции 2 (§2.3).

Практическое следствие: переход к `DESIGN_APPROVED` требует **и** закрытия
решений §7, **и** отображения каждого пункта разбора, **и** пересверки на
отпечаток этого документа. Ни одно из трёх сегодня не выполнено.
