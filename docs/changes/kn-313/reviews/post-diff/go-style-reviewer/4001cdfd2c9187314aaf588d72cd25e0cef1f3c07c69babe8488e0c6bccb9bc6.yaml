# Пост-дифф ревью Go-кода продукта — роль go-style-reviewer. ЧЕТВЁРТЫЙ КРУГ.
#
# Имя файла — и есть content_digest. Прежние записи (2919da5a…, d1813588…,
# ca53febd…) НЕ правятся: вердикт привязан к отпечатку (change-graph §2).
# Дайджест сменился — новый запуск и новый файл.
schema_version: 1
change_id: kn-313
holder: post-diff/go-style-reviewer
owner: go-style-reviewer
run: 4
supersedes_record: ca53febde9108224d3ccbdcde8c8fb1112e56f6a2151524cf9a594db510dc65f
subject: >-
  kaname#313 — собственный исполнитель заведению/снятию интерактивного клиента и
  снятию сессии входа при принудительном выходе; отсечка предъявления получает
  писателя. ЧЕТВЁРТЫЙ КРУГ: перемер рода, объявленного снятым коммитом 229a0693.

# ── отпечаток ──────────────────────────────────────────────────────────────
fingerprint:
  repo: PRO-Robotech/kaname
  base: d4aadfa790660d804d3672ca9ecc42dedf71a93a
  head: 229a0693adcf8003998a931800512f852d334ccc
  worktree: /home/dk/workspace/github/PRO-Robotech/cloud-demo/kacho-workspace/tmp/rebuild-wave-r4/kaname
  commits_over_base: 23
  files: 39
  insertions: 4385
  deletions: 133

content_digest: sha256:4001cdfd2c9187314aaf588d72cd25e0cef1f3c07c69babe8488e0c6bccb9bc6
digest_command: >-
  git -C /home/dk/workspace/github/PRO-Robotech/cloud-demo/kacho-workspace/tmp/rebuild-wave-r4/kaname
  diff d4aadfa790660d804d3672ca9ecc42dedf71a93a..229a0693 | sha256sum
digest_verified: >-
  ПЕРЕМЕРЕН СВОЕЙ КОМАНДОЙ ПЕРВЫМ ДЕЙСТВИЕМ и совпал с названным в задании.
  Перемерен ВТОРИЧНО после снятия моих временных зондов: значение то же,
  `git status --porcelain` пуст. Отпечаток, на котором вынесен вердикт, есть
  ровно тот, что назван заданием.

two_shas_one_content: >-
  ВЕРДИКТ ПРИВЯЗАН К СОДЕРЖИМОМУ, И ЭТО ЗДЕСЬ НЕ ФОРМАЛЬНОСТЬ. Голова, названная
  заданием (229a0693), живёт в ветви волны wave/ci-20260921-kaname-r4
  (`git merge-base --is-ancestor 229a0693 9223d94c` — да). Ветвь полосы
  lane/kn-own-interactive-and-logout несёт ДРУГУЮ голову — 58a5a1d0, с тем же
  заголовком коммита, и линии расходятся далеко: `git merge-base 229a0693
  58a5a1d0` = ba94bcd6, то есть ветвь пересобрана. Содержимое при этом ТОЖДЕСТВЕННО:
  `git diff d4aadfa7..58a5a1d0 | sha256sum` =
  4001cdfd2c9187314aaf588d72cd25e0cef1f3c07c69babe8488e0c6bccb9bc6 — ровно
  судимый дайджест, и коммитов над базой в обеих линиях 23. Значит этот вердикт
  покрывает ОБЕ линии по содержимому, и повторять ревью на 58a5a1d0 не нужно.

# ── вердикт ────────────────────────────────────────────────────────────────
verdict: returned            # ⛔ возврат
blocks_landing: true
timestamp: '2026-09-21T00:00:00Z'
review_scope: >-
  полный дифф d4aadfa7..229a0693, 39 файлов; запрет отсрочки и требование проб
  судятся по ВСЕМУ диффу, а не по последнему коммиту
return_to: 'автор диффа полосы lane/kn-own-interactive-and-logout (из задания)'

# ── РОД БЛОКИРУЮЩЕЙ ПРОШЛОГО КРУГА — НАЗВАН ПРЕЖДЕ НОВОГО ВЕРДИКТА ──────────
previous_blocking_genus:
  id: EXECUTOR-DISCRIMINATOR-DOES-NOT-DISCRIMINATE
  record: >-
    docs/changes/kn-313/reviews/post-diff/go-style-reviewer/
    ca53febde9108224d3ccbdcde8c8fb1112e56f6a2151524cf9a594db510dc65f.yaml
    (отпечаток d4aadfa7..823cc1b7, verdict returned, blocks_landing true)
  claim_of_that_round: >-
    Признак `cutoffTxBeginner`, которым дверь отсечки отличала пул от транзакции
    вызывающего, НЕ отличал их: метод `Begin(ctx) (pgx.Tx, error)` есть и у
    `pgx.Tx` (псевдо-вложенная транзакция на SAVEPOINT, pgx v5.10.0). Ветвь
    «пула» бралась на ВСЕХ вызывающих.
  requirement_of_that_round_had_TWO_clauses: >-
    Дословно: «Пробу — на РАЗЛИЧАЮЩЕЕ свойство: (1) отказ второй записи на
    пуловом исполнителе не оставляет первой в базе; (2) на транзакции
    вызывающего дверь своей не открывает.»
  answer_claimed_by_author: 'коммит 229a0693 «исполнители различаются по типу, а не по форме интерфейса»'

# ── ГЛАВНЫЙ ВОПРОС КРУГА: ПО СУЩЕСТВУ ИЛИ ПО ФОРМЕ ─────────────────────────
main_question_of_this_run:
  question: 'чинит ли 229a0693 названный род ПО СУЩЕСТВУ, а не по форме'
  answer_on_the_code: >-
    ПО СУЩЕСТВУ — ДА, и это измерено, а не выведено. Признак-интерфейс
    `cutoffTxBeginner` снят целиком; различение идёт переключателем по типу
    (`user_token_revocations_repo.go:137-155`): `case *pgxpool.Pool` — конкретный
    тип, `case pgx.Tx` — транзакция, `default` — отказ `iamerr.ErrInternal`.
    `*pgxpool.Pool` интерфейса `pgx.Tx` НЕ удовлетворяет (нет Commit/Rollback/
    LargeObjects/Prepare/Conn), поэтому порядок ветвей значения не имеет и
    перехвата пула транзакционной ветвью нет.
  answer_on_the_closed_list: >-
    ПОСТОРОННИЙ В ПЕРЕЧЕНЬ НЕ ПРОХОДИТ — перемерено обходом дерева, а не
    рассуждением. Перепись типов, удовлетворяющих `pgx.Tx`: ни одного
    собственного. Все 18 мест, где `pgx.Tx` встречается в непробном дереве, —
    ИМЕНОВАННЫЕ ПОЛЯ типа `pgx.Tx` либо параметры, ни одного ВСТРАИВАНИЯ
    (`grep -rn -B2 -A2 '^\s*pgx\.Tx\s*$'` даёт пусто), то есть обёртки,
    объявляющей себя транзакцией и ею не являющейся, в дереве нет. Перепись
    вызывающих двери: 4 — `UpsertRevokeAll` (пул), `UpsertRevokeAllTx`
    (pgx.Tx), `tx.go:262` (поле tx pgx.Tx), `human_session_repo.go:502` (поле tx
    pgx.Tx). Все четыре попадают в объявленные ветви.
  answer_on_the_holder: >-
    ПО ДЕРЖАТЕЛЮ — НЕТ. Это и есть предмет новой критической ниже.

# ── ПРИЗНАК ПОДЛОГА, КОТОРЫЙ ИСКАЛСЯ ПРЯМО ─────────────────────────────────
forgery_marker_searched_and_found:
  which_of_the_named_markers: 'УТВЕРЖДЕНИЕ, ЗЕЛЁНОЕ И ДО ПОЧИНКИ'
  not_found: >-
    «Закрытый перечень, куда проходит посторонний» — НЕ найден (перепись выше).
    «Различение, объявленное по типу и не различающее» — НЕ найдено: переключатель
    различает.
  found: >-
    Проба, поданная коммитом как держатель рода, ЗЕЛЕНА НА ПРЕД-ФИКСНОМ КОММИТЕ.

# ── КРИТИЧЕСКИЕ (блокируют посадку) ────────────────────────────────────────
critical:
  - id: PROBE-OF-THE-FIX-IS-GREEN-BEFORE-THE-FIX
    tag: '[GATE THAT CANNOT FAIL] + [TDD: НЕТ КРАСНОГО ДО КОДА] (запрет #12, ban12-tdd)'
    coordinates:
      - internal/repo/kaname/pg/cutoff_writers_write_both_integration_test.go:319  # сама проба
      - internal/repo/kaname/pg/user_token_revocations_repo.go:137                 # предмет, объявленный ею сторожимым
    claim: >-
      Коммит 229a0693 подаёт `TestIntegration_PoolDoorRollsBackTheFirstRecordWhenTheSecondFails`
      как пробу на РАЗЛИЧАЮЩЕЕ свойство и пишет в сообщении: «Новая проба судит
      отказ… Различение доказано опытом». Проба эта судит ПУЛОВУЮ ветвь. Пуловая
      ветвь была транзакционной И ДО ПОЧИНКИ: `*pgxpool.Pool` удовлетворял снятому
      `cutoffTxBeginner`, ветвь бралась, `Begin`/`writeBothCutoffs`/`Commit`
      исполнялись в точности как сегодня. Следовательно проба ничего не говорит о
      роде, ради которого заведена.
    measured_by_experiment: >-
      ПЕРЕМЕРЕНО ПРОГОНОМ, а не выведено. Отдельная рабочая копия на ПРЕД-ФИКСНОМ
      коммите a82933f1 (родитель 229a0693); в неё положен файл пробы, взятый ровно
      из 229a0693 (`git show 229a0693:…cutoff_writers_write_both_integration_test.go`);
      прогон `go test ./internal/repo/kaname/pg/ -run
      'TestIntegration_PoolDoorRollsBackTheFirstRecordWhenTheSecondFails$' -count=1 -v`
      на Postgres в контейнере дал:
      `--- PASS: TestIntegration_PoolDoorRollsBackTheFirstRecordWhenTheSecondFails (3.11s)`,
      `ok github.com/PRO-Robotech/kaname/internal/repo/kaname/pg 3.667s`.
      Рабочая копия снята, дерево полосы чисто, дайджест перемерен и совпал.
    what_the_author_measured_instead: >-
      Сообщение коммита называет СВОЙ опыт: «со снятой транзакцией двери новая
      проба краснеет, а прежняя остаётся зелёной». Опыт верный и доказывает
      верное — что новая проба сильнее прежней. Но он ставит инъекцию «снять
      транзакцию ЦЕЛИКОМ», а род был другой: «ветвь выбирается не тем признаком».
      Между этими двумя инъекциями проба не различает, и разница измерена выше:
      против настоящего пред-фиксного состояния она зелёная.
    second_clause_not_delivered_at_all: >-
      Требование прошлого круга несло ДВА предложения. Второе — «на транзакции
      вызывающего дверь своей не открывает» — не исполнено ничем: перепись проб
      диффа (43 новых `func Test`) не содержит ни одной, судящей транзакционную
      ветвь; перепись дерева по признаку вложенной транзакции
      (`grep -rni 'savepoint|вложенн' --include=*.go`) не даёт ни одного
      утверждения о двери. Именно это предложение и различает исполнителей.
    behaviour_change_left_unheld: >-
      Починка ИЗМЕНИЛА поведение на отказе, и изменение это ровно то, которое
      прошлый круг назвал молчаливым (consequence п. 3). До: на транзакции
      вызывающего открывалась вложенная, и отказ внутри `writeBothCutoffs`
      гасился откатом к точке сохранения — внешняя транзакция оставалась
      пригодной. После: `writeBothCutoffs` идёт прямо на транзакции вызывающего,
      и отказ отравляет её целиком. Направление верное (отравление безопаснее),
      но свойство это сегодня держится дисциплиной трёх вызывающих, а не
      прибором: ни одна проба его не утверждает.
    requirement: >-
      Пробу НА ТРАНЗАКЦИОННУЮ ВЕТВЬ, способную упасть на пред-фиксном коде:
      открыть транзакцию вызывающего, позвать дверь, добиться отказа второй
      записи законным входом (та же причина длиннее 121 знака) и утверждать, что
      ВНЕШНЯЯ транзакция после этого НЕПРИГОДНА (следующий оператор на ней
      отвергается, `COMMIT` не проходит). На пред-фиксном коде такая проба
      краснеет — там отказ гасился точкой сохранения и внешняя транзакция жила.
      Это и есть одно-фактная пара на различение. Пробу пуловой ветви оставить:
      она верна, только держателем рода не является, и имя её этого не обещает —
      обещает сообщение коммита. Красноту новой пробы на пред-фиксном коде
      ПОКАЗАТЬ прогоном в сообщении, а не рассуждением.

# ── ВАЖНЫЕ ─────────────────────────────────────────────────────────────────
important:
  - id: LATENT-REMAINDERS-WITHOUT-A-SUBJECT
    tag: '[ОТСРОЧКА БЕЗ ЗАВЕДЁННОГО ПРЕДМЕТА] (запрет #11, ban11-three-outcomes)'
    coordinates:
      - cmd/kaname/wiring.go:1182-1186
      - internal/apps/kaname/api/internal_iam/force_logout.go:410-411
      - internal/apps/kaname/api/internal_iam/force_logout.go:434-435
      - internal/check/provider_road_posture_answer.go:50-51
      - internal/repo/kaname/pg/minted_token_revocation_repo.go:91-93
      - internal/repo/kaname/pg/human_session_repo.go:347-349
    claim: >-
      Коммит 229a0693 отвечает на эту находку ПРОЗОЙ: в шесть мест добавлено
      «НОМЕРА У ЗАДАЧИ ПОКА НЕТ… до заведения адресом остатка служит эта
      координата», а перечень всех семи адресуется путём
      `tmp/kn-313-own-executor-runs/remnants-as-issue-lines.txt`.
      ban11-three-outcomes даёт три законных исхода: сделать сейчас · снять
      вместе с подпираемым кодом · ЗАВЕСТИ ПРЕДМЕТ с причиной и предикатом
      снятия. Причина и предикат названы у каждого остатка — это половина
      третьего исхода; предмет не заведён ни один.
    measured: >-
      Путь, которым остатки адресуются, ИЗ РЕПОЗИТОРИЯ НЕ РАЗРЕШАЕТСЯ:
      `git ls-files | grep -c remnants-as-issue-lines` = 0;
      каталога `tmp/kn-313-own-executor-runs/` в дереве kaname нет
      (`ls` даёт «No such file or directory»). Файл существует ТОЛЬКО во
      временном каталоге воркспейса
      (`kacho-workspace/tmp/kn-313-own-executor-runs/remnants-as-issue-lines.txt`,
      6367 байт), который в продукт не входит и не публикуется. Репозиторий
      публичен (CLAUDE.md §«Топология»), то есть у семи остатков нет ни одного
      адреса, переживающего клонирование.
    recurrence: >-
      ЧЕТВЁРТЫЙ КРУГ ПОДРЯД. Эта же находка стоит в записях 2919da5a (круг 1),
      d1813588 (круг 2) и ca53febd (круг 3) — по одному вхождению в каждой
      (`grep -c` = 1,1,1). Блокирующей она не была ни разу и остаётся важной
      здесь. Разбор рецидива — в разделе `recurrence_analysis` ниже.
    requirement: >-
      ЛИБО завести семь предметов и проставить номера тем же заходом — это
      кросс-инструментальная работа (`git-operator`), а не правка в дереве; ЛИБО
      убрать из прод-комментариев путь, который из репозитория не разрешается, и
      оставить причину с предикатом снятия по месту. Сегодняшняя редакция —
      худший из трёх вариантов: она обещает читателю адрес и его не даёт.

  - id: OWN-POSTURE-DRAINER-PATH-HAS-NO-PROBE
    tag: '[НОВАЯ ВЕТВЬ БЕЗ ПРОБЫ] + [(nil, nil) КАК ЗАКОННЫЙ ИСХОД]'
    coordinates:
      - cmd/kaname/provider_compensation_wiring.go:88   # `return nil, nil`
      - cmd/kaname/serve.go:1792                        # единственная защита вызывающего
      - cmd/kaname/drainer_shutdown_test.go:121         # проба, которая эту ветвь НЕ берёт
    claim: >-
      `buildProviderCompensationDrainer` получил третий исход: нулевая задача при
      нулевой ошибке. Исход законный и назван в шапке, защита у единственного
      прод-вызывающего есть (`if compensationDrainerTask == nil`). Прибора нет:
      ни одна проба не берёт ветвь `own`.
    measured: >-
      Перепись вызывающих — 2: `serve.go:1785` (защищён) и
      `drainer_shutdown_test.go:122`. Второй зовёт сборщик с `config.Config{}` и
      отдаёт результат в `assertTaskStopsOnCancel`, который делает
      `go func() { done <- task(ctx) }()` — на нулевой задаче это разыменование
      нулевой функции. Паники сегодня НЕТ, и причина измерена:
      `providerAdminHopIsBuilt` читает `HasExternalIdentityProvider`, а тот
      возвращает `c.IdentityProvider != IdentityProviderOwn`
      (`internal/apps/kaname/config/identity_provider.go:68`), то есть нулевая
      настройка читается как ВНЕШНЯЯ посадка и задача приходит ненулевой.
      Проба гашения, стало быть, покрывает только внешнюю посадку, а ветвь
      `own` — и новая, и не взятая ничем. Перепись проб по признаку
      `IdentityProviderOwn` рядом с дренажом/компенсацией даёт 0.
    requirement: >-
      Пробу на ветвь `own`: сборщик с настройкой `IdentityProvider = own`
      возвращает нулевую задачу и нулевую ошибку; и вторую — что `runServe` на
      такой посадке поднимается, задачу в группу не ставит и не падает.
      Законный близнец — та же проба под `external`, где задача ненулевая.

  - id: DEFAULT-ARM-IS-UNREACHABLE-AND-UNHELD
    tag: '[ВЕТВЬ БЕЗ ПРОД-ДОСТИЖИМОСТИ] (arch-lean-no-vestigial)'
    coordinate: internal/repo/kaname/pg/user_token_revocations_repo.go:152-154
    claim: >-
      Шапка обещает: «Третьего исполнителя дверь не принимает НАМЕРЕННО… отказ
      виден сразу». Обещание верное, но ветвь `default` из дерева недостижима
      (перепись вызывающих — 4, все в объявленных ветвях), а прибора на неё нет.
      `cutoffExecutor` — интерфейс с одним `Exec`
      (`minted_token_revocation_repo.go:106-109`), поэтому компилятор третьего
      исполнителя пропускает: «отказ виден сразу» — про ВРЕМЯ ИСПОЛНЕНИЯ, и это
      в шапке не сказано.
    requirement: >-
      Пробу в пакете: исполнитель, реализующий только `Exec`, получает от двери
      `iamerr.ErrInternal` с именем своего типа; законный близнец — тот же вход
      на пуле проходит. Либо сузить шапку до «отказом времени исполнения».

# ── КАНДИДАТЫ В ФУНДАМЕНТ ──────────────────────────────────────────────────
foundation_candidates: []
foundation_candidates_note: >-
  Новых горизонтальных помощников (генерация id, отображение ошибок,
  конфигурация, перехватчик, транзактор, повтор, выдержка, валидация) дифф не
  заводит. `internal/check/*` — приборы дерева ЭТОГО продукта и горизонтальными
  не являются: их предмет — раскладка kaname. Копий файлов чужого репозитория
  в диффе нет (запрет #20).

# ── СТИЛЬ (не блокирует) ───────────────────────────────────────────────────
style:
  - coordinate: internal/repo/kaname/pg/minted_token_revocation_repo.go:144-160
    note: >-
      Семнадцать строк надгробия снятому типу («ЗДЕСЬ СТОЯЛ ПРИЗНАК…») стоят в
      прод-файле. Разбор ценен и месту соответствует, но у него нет предиката
      снятия: через две полосы читатель не установит, актуален ли он. Уместнее —
      в записку хранилища, а по месту оставить одну строку с причиной формы.
  - coordinate: cmd/kaname/wiring.go
    note: >-
      `mustProviderAdminClient` несёт `log.Fatalf` — это композиционный корень
      (`cmd/kaname`), и по чек-листу §3 допустимо. Отмечено, чтобы не читалось
      находкой при следующем обходе.

# ── ОДОБРЕНО (перемерено на этом отпечатке) ────────────────────────────────
approved:
  - 'Обёртка ошибок: `%w` везде; `fmt.Errorf(…%v)` в диффе один — `cmd/kaname/serve.go` на значении `recover()`, которое ошибкой не является. Верно.'
  - 'Sentinel: `errors.Is`/`errors.As`; сравнений `err.Error() == …` в диффе нет.'
  - 'context: `ctx` первым аргументом; `context.Background()` добавлен в 6 файлах — ВСЕ шесть суть `_test.go` (перепись по `git diff -U0` с именем файла). В рабочем коде ни одного. `context.TODO()` — ноль.'
  - 'Паники в рабочем пути нет: `panic(` в добавленных строках ноль; `recover()` в `serve.go` — страж задачи, не паника; `log.Fatal` только в `cmd/kaname`; `uuid.MustParse` на входе запроса нет.'
  - 'Журнал: только `log/slog`, структурные поля; `fmt.Println`/`log.Printf`/сторонних логгеров в добавленных строках ноль; персональных данных в аргументах журнала не найдено.'
  - 'Имена: латиница (`grep -P "^(func|type|var|const)\s+\w*[А-Яа-яЁё]"` даёт пусто, запрет #17); receiver короткие; идентификаторы домена собственными типами (`domain.UserID`, `domain.RevokeReason`).'
  - '`init()` с побочными эффектами не заведено; связывание — в `cmd/kaname`.'
  - 'Направление зависимостей: `internal/domain` инфраструктуры не импортирует; SQL — в `internal/repo/`; handler `internal_iam` зовёт use-case и отображает ошибку.'
  - 'Проверки входа обеих записей отсечки стоят ДО первого оператора (`user_token_revocations_repo.go:112`) — порядок «проверка → запись» соблюдён.'
  - 'Курсоры закрываются руками с НАЗВАННОЙ причиной в двух местах (`human_session_repo.go:361-368`, `oauth_ceremony_repo.go:463-470`) — находка MANUAL-ROWS-CLOSE-WITHOUT-A-SAID-REASON прошлого круга снята.'
  - 'Имя пуловой пробы сужено до её утверждений (`…RejectsBadInputBeforeTheFirstRecord`) — находка PROBE-NAME-WIDER-THAN-ITS-ASSERTIONS прошлого круга снята.'
  - 'Одиночный писатель второй записи (`MintedTokenRevocationRepo.Revoke`, :62) находкой не является по решению полосы, и решение это держится пробой `TestPairedWriteInjection_SecondOnlyIsNotAFinding`.'
  - 'Дифф собирается и проходит `go vet` на собственной голове: `go build ./...` и `go vet ./...` на 229a0693 — чисто.'
  - '`go.mod`/`go.sum` не тронуты (0 файлов) — ребро модулей не двигалось, rule-polyrepo не срабатывает.'

# ── СЧЁТ ПО ВСЕМУ ДИФФУ ────────────────────────────────────────────────────
fresh_deferral_markers: 0
fresh_deferral_markers_predicate: >-
  `git diff <base>..<head> -U0 | grep '^+' | grep -E 'TODO|FIXME|XXX|HACK'` —
  ноль. Единственное совпадение по слову «позже» — проза комментария о поведении
  отказа, маркером отсрочки не являющаяся.
declared_remnants_without_a_subject: 7
declared_remnants_note: >-
  Число названо САМИМ автором в сообщении 229a0693 и не является счётом по
  маркерам: это остатки, объявленные прозой, у которых предмет не заведён.
  Смотри important[LATENT-REMAINDERS-WITHOUT-A-SUBJECT]. Два числа держатся
  раздельно намеренно: сворачивать их в одно значило бы лгать обоими.
tests_in_same_pr: true
tests_in_same_pr_predicate: >-
  `git diff <base>..<head> -U0 -- '*.go' | grep -cE '^\+func Test'` = 43 новых
  проб по полному диффу полосы, включая инъекционные пары к трём новым приборам
  дерева. Запрет #12 по наличию проб соблюдён; чем он НЕ соблюдён — предметом
  критической: проба поданного рода не краснеет до починки.
t_skip_added: 12
t_skip_note: >-
  Все 12 — стражи предусловия интеграционных проб (`testing.Short()` →
  «нужен Postgres в контейнере»). Область зелёного и исход прогона объявляю не я:
  сырой вывод читает `landing-reviewer`, независимый перемер — `check-verifier`.
go_mod_touched: false
security_comments_contradicting_code: []
security_comments_note: >-
  Перемерено по трём шапкам, которые прошлый круг назвал лгущими
  (`user_token_revocations_repo.go:115-135`, `minted_token_revocation_repo.go:144`,
  `:91`). Сегодня все три описывают то, что код делает: ветвление по типу,
  закрытый перечень, отказ на третьем. Расхождений не найдено.

# ── РЕЦИДИВ: ОТВЕТ НА ПРЯМОЙ ВОПРОС ЗАДАНИЯ ────────────────────────────────
recurrence_analysis:
  question_from_the_task: >-
    «Если по ОДНОМУ роду отказ идёт третий круг подряд — скажи это прямо: по моей
    базе это признак того, что править надо задание, а не документ, и решение
    тогда моё.»
  answer: 'ДА — по одному механизму, и это говорится прямо.'
  lineage_of_the_mechanism: >-
    Механизм один: ДВЕРЬ ОТСЕЧКИ И ЕЁ ОБЕЩАНИЕ «ОБЕ ЗАПИСИ ОДНОЙ ТРАНЗАКЦИЕЙ».
    · круг 1 (2919da5a): CUTOFF-HALF-WRITTEN-ON-SELF-SERVICE-PATHS — критическая, ⛔
    · круг 2 (d1813588): DOOR-IS-ONE-DOOR-BUT-NOT-ALWAYS-ONE-TRANSACTION — важная, ⛔ (по другим)
    · круг 3 (ca53febd): EXECUTOR-DISCRIMINATOR-DOES-NOT-DISCRIMINATE — критическая, ⛔
    · круг 4 (этот):     PROBE-OF-THE-FIX-IS-GREEN-BEFORE-THE-FIX — критическая, ⛔
    Кругов подряд, где этот механизм есть возвращающийся предмет: ТРИ (2,3,4).
    Блокирующих подряд по нему: ДВА (3,4).
  what_moved_and_what_did_not: >-
    Двигался КОД, и двигался в верную сторону каждый круг: половинная запись
    закрыта дверью, дверь сведена к одному оператору, различитель заменён на
    различающий. Не двигалось одно — ДЕРЖАТЕЛЬ. Четыре круга подряд предмет
    закрывается кодом и не закрывается прибором, способным упасть.
  second_lineage: >-
    LATENT-REMAINDERS-WITHOUT-A-SUBJECT — ЧЕТВЁРТЫЙ круг подряд, ни разу не
    блокировал, каждый круг отвечается прозой. Ответ прозой здесь неизбежен по
    устройству задания: завести предмет автор диффа не может — заведение задач
    не его инструмент.
  what_this_says_about_the_task_not_the_document: >-
    Оба рецидива указывают в одну сторону, и вывод этот мой, а решение — владельца.
    Задание полосе просит АВТОРА ДИФФА чинить то, что он чинить не оснащён:
    (1) предметы остатков заводит `git-operator`, не автор; (2) проба на
    транзакционную ветвь есть интеграционная работа на Postgres, и полоса
    интеграции ведётся отдельной ролью (`integration-tester`). Четыре круга
    автор отвечает на оба требования единственным доступным ему средством —
    прозой и пробой той ветви, которую умеет поднять. Пятый круг того же
    задания даст, по моей оценке, тот же исход.
  proposal_for_the_owner: >-
    Не круг пятый, а РАЗДЕЛЕНИЕ задания на три заказа с разными исполнителями:
    (а) `integration-tester` — проба транзакционной ветви, красная на a82933f1;
    (б) `git-operator` — семь предметов по перечню остатков и простановка номеров;
    (в) автор диффа — снятие из прод-комментариев пути, не разрешающегося из
    репозитория. Код полосы при этом править нечего: по существу род закрыт.
    Решение — владельца; я его не принимаю и вердикт свой не смягчаю.

# ── ЧЕГО Я НЕ УТВЕРЖДАЮ ────────────────────────────────────────────────────
not_claimed:
  - 'Исход линтера и прогона проб я не объявляю: сырой вывод читает `landing-reviewer`, независимый перемер — `check-verifier`. Числа автора (128 пакетов, 0 issues) я не подтверждал и не опровергал.'
  - 'Я не утверждаю, что код двери неверен. Перемерено обратное: различитель различает, посторонний в перечень не проходит.'
  - 'Я не проверял полный прогон пакета `internal/repo/kaname/pg` — поднималась ровно одна проба, названная в эксперименте.'
  - 'Состав от полосы посадки (103 новых `func Test`, 24 `t.Skip`, 33/33 ok) на веру не принят и не перемерялся целиком: мой счёт — по ЭТОМУ диффу и назван своим предикатом.'

# ── ЧТО ДАЛЬШЕ ─────────────────────────────────────────────────────────────
next:
  verdict_owner: go-style-reviewer
  return_to: 'автор диффа полосы (из задания)'
  after_fix: 'дайджест новый — роль повторяется на новом отпечатке; вердикт о другом отпечатке не переносится'
  also_needed:
    - 'integration-tester — проба транзакционной ветви двери, красная на a82933f1'
    - 'git-operator — семь предметов по перечню остатков (кросс-инструментальная работа)'
  owner_decision_requested: 'разделять ли задание вместо пятого круга — смотри recurrence_analysis'
