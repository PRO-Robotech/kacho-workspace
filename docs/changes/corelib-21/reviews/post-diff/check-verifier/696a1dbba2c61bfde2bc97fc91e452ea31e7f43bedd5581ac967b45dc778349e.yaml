# Приёмка работы-проверки, роль check-verifier. Имя файла — content_digest.
#
# Круг 3 по corelib #21. Судятся закрытие возврата круга 2 (F3, N3, N4 в
# 46e98326….yaml рядом) и дельта 85ef34f..dd8df2c (только scripts/hooks/inject.sh).
# Прежнее не переоткрыто. Все опыты поставлены 2026-09-24 в своём каталоге
# /var/tmp/cv21r3.fCC03a, каталог снят. Числа ниже — этого захода. Запись не
# закоммичена: её сводит пакетный коммит записей ревью (как 434f35a3 свёл круг 2).
schema_version: 1
change_id: corelib-21
holder: post-diff/check-verifier
owner: check-verifier
round: 3
mode: приёмка работы-проверки (проба хука отправки)
subject: >-
  дельта круга, только scripts/hooks/inject.sh (+104/−24). Дефекты сборки и vet
  кладутся в каждый пакет фикстуры, дефекты gofmt — в каждый файл Go индекса.
  -count=1 судится журналом исполнений TestInner и контролем «копия хука без
  -count=1». В шапке появился раздел «ЧЕГО ПРОБА НЕ РАЗЛИЧАЕТ» (флаги линтера и
  таймаут 5m против 10m) и расширен перечень исходов «2».

fingerprint:
  repo: PRO-Robotech/corelib
  branch: 'issue-21 (локальная; origin/issue-21 = 75af8e12, голова не отправлена)'
  base: f213439a409ecce4816d88121cf3301b9efbad33   # origin/32, перемерено fetch + rev-parse; предок HEAD (merge-base --is-ancestor)
  head: dd8df2c1e167bd17e74dafe849b9bc695177227d
  round_delta: '85ef34f..dd8df2c: scripts/hooks/inject.sh +104/−24; хук, провязка, прогонщик и ci.yml не тронуты'
  commits_first_parent: [fef469d, 75af8e1, f0f8375, bd59d73, 2b29ecf, 3290b6b, 5dc4984, 85ef34f, daba18b, 084e8fe, dd8df2c]
content_digest: sha256:696a1dbba2c61bfde2bc97fc91e452ea31e7f43bedd5581ac967b45dc778349e
digest_definition: |
  git -C tmp/corelib-21 diff --full-index $(git -C tmp/corelib-21 merge-base origin/32 HEAD)..HEAD | sha256sum
  (перемерено до опытов, совпадает с заданием)

verdict: accepted               # ✅ принято
blocks_landing: false
timestamp: '2026-09-24T00:40:00Z'

isolation:
  dir: '/var/tmp/cv21r3.fCC03a (mktemp -d, вне git-деревьев); TMPDIR=$dir/tmp, GOCACHE=$dir/gocache, GIT_CONFIG_GLOBAL — свой конфиг опыта'
  clones: 'git clone --no-local tmp/corelib-21 в $dir/real (судья — inject.sh этой копии, инъекции по одной, git checkout -- . между ними) и в $dir/mk (make probe-hooks); три синтетических модуля: $dir/lt (линтер), $dir/cc (кеш go test), $dir/vt (vet)'
  shared_machine: 'MemAvailable 32,6 ГБ; чужих golangci-lint и pre-push нет (pgrep нашёл только тексты чужих команд-ожиданий)'
  foreign_state: >-
    tmp/corelib-21 только читался: HEAD dd8df2c, `git status --porcelain --ignored`
    0 строк, stash 0 до опытов и после. В ws-825 добавлен только этот файл.
  removed: 'rm -rf каталога опытов, затем ls -d → «No such file or directory» (команда в возврате)'

premise:
  status: подтверждена
  how: >-
    git fetch + rev-parse в tmp/corelib-21: origin/32 = f213439a, предок HEAD;
    origin/issue-21 = 75af8e12 ≠ HEAD; дайджест 696a1dbb… совпал с заданием.

experiments:
  foreign_predicate_verbatim:
    inject.sh: 'bash scripts/hooks/inject.sh @dd8df2c ($dir/real) → rc 0, «утверждений 90 · сошлось 90 · разошлось 0», 16 с; «дефекты охвата разложены: сборка и vet — по 3 пакетам, gofmt — по 5 файлам»; контроль -count=1: «исполнена 1 раз из 2»'
    make_probe_hooks: 'make probe-hooks ($dir/mk, свежий клон) → rc 0, 90 из 90'
    shellcheck: 'shellcheck 0.11.0 -S warning pre-push install.sh inject.sh → rc 0'

  # Проба пробы: одна строка scripts/hooks/pre-push изменена sed (пустая
  # инъекция отсеяна git diff --quiet), судья — inject.sh копии. После серии
  # porcelain 0. «разошлось» — число, названные утверждения — свои.
  F3_closure:
    O2 go vet ./inner/...: 'rc 1, разошлось 1 — «охват vet: … в пакете .»'
    O3 go vet без корня (go list | tail -n +2): 'rc 1, разошлось 1 — «охват vet: … в пакете .»'
    O6 go build . ./inner: 'rc 1, разошлось 1 — «охват сборки: … в пакете inner/deep»'
    O1 go build ./inner/...: 'rc 1, разошлось 3 — «охват сборки: … в пакете .» (в круге 2 ловили только соседи)'
    O5 go build без корня: 'rc 1, разошлось 3 — «охват сборки: … в пакете .»'
    O7 go vet . ./inner: 'rc 1, разошлось 1 — «охват vet: … в пакете inner/deep»'
    O9 go build . ./inner/deep: 'rc 1, разошлось 1 — «охват сборки: … в пакете inner»'
    O10 go vet . ./inner/deep: 'rc 1, разошлось 1 — «охват vet: … в пакете inner»'
    reinjected P22 go vet .: 'rc 1, разошлось 2 — inner и inner/deep'
    reinjected P24 go build .: 'rc 1, разошлось 2 — inner и inner/deep'
  gofmt_per_file:
    P25 только первый файл обхода: 'rc 1, разошлось 5 — охват (5) и 4 файла поимённо'
    P25b только корень: 'rc 1, разошлось 4'
    G1 без *_test.go: 'rc 1, разошлось 3 — охват и оба файла проб'
    G2 все, кроме последнего: 'rc 1, разошлось 2 — охват и probe_test.go'
    G5 перечень полный, gofmt зовётся по 4 файлам: 'rc 1, разошлось 1 — «неформатированная строка в probe_test.go». Число «прочитано 5» при этом верно: подтверждено, что счёт идёт по перечню и держится только дефектом в каждом файле (шапка, строки 30–32)'
  N3_closure:
    O4 go test без -count=1: 'rc 1, разошлось 2 — «TestInner исполнена 0 раз из 2 — вердикт взят из кеша» и «инъекция «без -count=1» изменила копию хука»'
    P23 go test .: 'rc 1, разошлось 3 — охват go test, TestRed, журнал 0 из 2'
    mechanism: 'синтетический модуль, три прогона go test без -count=1: запись через syscall → 1 строка из 3, остальные (cached). Та же запись через os.OpenFile → тоже 1 из 3: журнал лежит вне корня модуля, и go test его не перепроверяет (computeTestInputsID: «Do not recheck files outside the module»). syscall не несущий, но и не вредит; комментарий inject.sh:323–325 верен в том, что говорит'
  N4_disclosure:
    real_linter_timeout: 'golangci-lint 2.12.2 --timeout=1ms → rc 4 («Timeout exceeded»); близнец --timeout=5m → rc 0, «0 issues». Утверждение шапки (строки 44–47) «истёкший таймаут — код 4, то есть красное» подтверждено; в хуке любой ненулевой код c_lint без why — красное (pre-push:167–174)'
  lawful_twins:
    T0 правка комментария хука: 'rc 0, 90 из 90'
    T1 go build $(go list ./...): 'rc 0, 90 из 90'
    T2 go vet $(go list ./...): 'rc 0, 90 из 90'
    T3 git ls-files -- *.go: 'rc 0, 90 из 90'
    T4 go test -count=1 ./... -short (флаги переставлены): 'rc 0, 90 из 90'
    O11 go vet -tests=false ./...: 'rc 0, 90 из 90. Это законный близнец, а не слепая зона: на модуле с битым тегом в vt_test.go и `go vet ./...`, и `go vet -tests=false ./...` дают rc 1 с этой находкой, то есть флаг файлов проб у go vet не отбрасывает'
    O4b -count 1 (через пробел, то же значение): 'rc 1, разошлось 2 — «инъекция «без -count=1» изменила копию хука» (провал предпосылки назван честно) и «контроль: … журнал не отличает кешированный прогон от свежего» (причина неверна: копия не изменялась). Та же форма, что у принятой инъекции -short; см. N5'

findings:
  - id: F3
    status: закрыт по своему предикату, исход (а)
    measured: 'O2, O3 и O6 дают ≥1 «разошлось» по своей проверке, и каждое называет пакет; T0 молчит. Сверх предиката красные ещё O1, O5, O7, O9, O10, P22, P24 и пять сужений gofmt. Шапка (строки 25–32) утверждает ровно это, и опыт с ней совпадает'

observations:   # неблокирующие
  - id: N3
    status: закрыт этой дельтой
    measured: 'O4 красное по предмету; контроль пробы на @dd8df2c — 1 из 2'
  - id: N4
    status: закрыт раскрытием
    measured: 'флаги линтера кроме --config/-c/--no-config названы в шапке (строки 43–44) как неразличаемые; код 4 при таймауте измерен настоящим линтером'
  - id: N5
    item: 'контроль -count=1 гонится и тогда, когда его инъекция не состоялась'
    measured: >-
      O4b: при законной записи `-count 1` первое красное честно называет
      провал предпосылки, а второе приписывает журналу слепоту, которой нет.
      Вердикт от этого не зеленеет, ложного зелёного нет. Предикат снятия, если
      автор возьмётся: не гнать контроль при not_same = ложь (либо void), тогда
      O4b даст одно утверждение с верной причиной. Та же форма, что у
      принятой прежде инъекции -short: круг её не переоткрывает

not_distinguished_by_fake:
  - 'подставной линтер: флаги кроме --config/-c/--no-config; в шапке названо'
  - 'инъекции -short и -count=1 держатся за текстовую форму « -флаг» в строке go test: другая законная запись даёт красное по предпосылке, а не молчание (O4b); ложного зелёного это не даёт'
  - 'контроль -count=1 требует, чтобы кеш go test работал у того, кто гонит пробу. Без кеша контроль краснеет, то есть отказ закрытый'
masks: >-
  маски, выписанные списки исключаемого и вычеты не найдены. Осмотрено: дифф
  85ef34f..dd8df2c целиком. Циклы дефектов идут по тем же want_pkgs и ls-files,
  что и близнец, и печатают своё число (3 пакета, 5 файлов). Пустым цикл быть
  не может: фикстуру без вложенных пакетов или без файлов Go отсекает void на
  строке 365, а нулевое общее число утверждений — void на строке 724.
  Послаблений не найдено.

counts:
  unit: 'опыт = одна команда со своим исходом, поставленная в этом заходе'
  own_experiments: 33   # inject дословно 1 · make probe-hooks 1 · shellcheck 1 · проба пробы 24 (T0–T4, O11, O4b + 17 сужений) · настоящий линтер 2 · кеш go test 2 · vet -tests 2
  tally: 'совпали с ожиданием приёмки 33: 17 сужений красные по своему предмету; 6 законных близнецов молчат (T0–T4, O11); O4b красное по предпосылке, как требует gate-checks-its-premise (N5 — только о тексте второго утверждения); 9 прочих замеров подтвердили утверждения шапки и автора'
  author_numbers: 'воспроизведены: inject 90 из 90 (дважды, два клона); контроль и T0 90 из 90; make probe-hooks rc 0; shellcheck 0. «17 инъекций — rc 1» перемерено своим набором: у меня 17 сужений предмета, все rc 1; сами дефекты у меня другие'
