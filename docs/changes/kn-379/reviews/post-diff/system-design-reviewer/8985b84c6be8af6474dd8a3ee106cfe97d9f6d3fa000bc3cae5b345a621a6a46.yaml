# Пост-дифф ревью распределённых свойств — роль system-design-reviewer, круг 1.
#
# Имя файла — content_digest. Прежние записи не правятся; сменился дайджест —
# новый запуск и новый файл (change-graph §2). Запись не коммитится (задание).
# Текст — на уровне свойства: репозиторий публичен.
schema_version: 1
change_id: kn-379
holder: post-diff/system-design-reviewer
owner: system-design-reviewer
round: 1
previous_record: null
subject: >-
  kaname#379 — отсечку отзыва-всех человека судит одно правило на трёх полосах
  выдачи токена и на полосе базового секрета; предел времени на внешнем чтении;
  исходы отказа.

fingerprint:
  repo: PRO-Robotech/kaname
  branch: '379'
  worktree: копия полосы tmp/kaname-379 воркспейса
  base: a889f108f22e1e42782b822677a4225467f3bdfc
  head: 5f51a6901a478357bdc30ddf4c4281aed536cc66
content_digest: sha256:8985b84c6be8af6474dd8a3ee106cfe97d9f6d3fa000bc3cae5b345a621a6a46
digest_definition: >-
  `git diff --full-index a889f108..5f51a6901 -- . ':!docs/changes' | sha256sum`;
  перемерено своей командой, совпало с заданием. Рабочее дерево чистое, кроме
  неотслеживаемых записей ревью.

verdict: accepted            # принято
blocks_landing: false
distributed_surface: true
schema_review_overlap: false
timestamp: '2026-09-23T13:05:51Z'

review_scope: >-
  дифф 57 файлов (+4425/-266) прочитан по коду продукта. Пройдены оси роли:
  атомарность, идемпотентность, параллелизм, координация, изоляция реплик, граф
  зависимостей, предел на внешнем вызове, край, границы слоёв.

experiments:
  - probe: TestIssuanceHookLanesReadTheCutoffUnderTheDeclaredLimit
    head: pass
    negative_control: >-
      обёртка предела у сборки полос хука снята — красное на обеих полосах
      (перепись «читают под пределом — 0 из 2»); правка откачена.
  - probe: TestBasicLane_CutoffThatCannotBeReadIsNotAGrant
    head: pass
    negative_control: >-
      у одного оператора авторитета предел снят — красное со сторожа пробы
      («глагол не ответил за предел с запасом»); правка откачена.
  - probe: TestBasicLane_RevokeAllCutoffRefusesASecretIssuedNoLaterThanIt
    head: pass
    note: перепись путей записи отсечки 4 из 4 исполнено, подач 5.
  - probe: >-
      TestNewBasicCredentialRepo_RefusesAnUndeclaredCallLimit, пробы переписи
      исходов internal_iam
    head: pass
experiments_where: отдельная отсоединённая копия на голове 379, снята после прогона.

axes:
  atomicity: >-
    закрыто. Новых записей нет; отсечка читается ТЕМ ЖЕ оператором, что строка
    удостоверения и состояние владельца, — окна между двумя чтениями нет. Запись
    отсечки — прежняя однооператорная монотонная вставка-слияние.
  idempotency: неприменимо — пути чтения, мутаций и Operation дифф не вводит.
  concurrency: >-
    закрыто. Проверки-затем-записи не введено; присоединение отсечки не
    размножает строки (ключ отсечки — первичный). Спорного пути
    чтения-правки-записи нет, заказа на пробу с конкурирующими горутинами нет.
  per_call_deadline: >-
    закрыто для всего, что дифф вводит или меняет: каждое обращение авторитета
    базового секрета к базе идёт под своим пределом, поданным конструктору
    обязательно; чтение отсечки на трёх полосах выдачи — одной обёрткой и одной
    величиной корня. Обе половины держатся пробами с отрицательным контролем.
  failure_outcomes: >-
    закрыто. «Отказ» и «авторитет не ответил» различимы по построению; истёкший
    предел и непрочитанная отсечка — «не ответил», не пропуск; вердикт правила —
    закрытый словарь из трёх, вне словаря — отказ; наружу один фиксированный
    отказ, причина только внутри.
  replica_isolation: >-
    закрыто. Перепись исходов — счётчики процесса для витрины, не источник
    истины; решение читается из базы на каждом вопросе.
  service_graph: >-
    закрыто. Нового межсервисного ребра нет; серверная сторона существующих
    внутренних глаголов, контракт не менялся.
  edge: закрыто. Новых монтирований нет; внутренние глаголы остаются на внутреннем слушателе.
  layers: >-
    замечаний, блокирующих посадку, нет; решение об отсечке в адаптере делегировано
    общему правилу, а не своей копии.

findings_critical: []

findings_important:
  - id: CUTOFF-AND-ANCHOR-FROM-TWO-CLOCKS
    blocks: false
    statement: >-
      момент отсечки и момент выдачи долговременного удостоверения берутся из
      разных часов, а граница правила включительна; точность границы ограничена
      расхождением часов. Класс есть и у прежней полосы персонального токена; дифф
      распространяет его на две новые полосы.
    requirement: >-
      отдельным предметом: оба момента из одних часов (часы базы) либо объявленный
      допуск расхождения с предикатом снятия.
  - id: HOOK-LANE-NEIGHBOUR-READS-WITHOUT-LIMIT
    blocks: false
    statement: >-
      на полосах хука под пределом теперь только чтение отсечки; соседние
      обращения тех же полос к базе идут с контекстом запроса поставщика. Вне диффа,
      до него так же.
    requirement: отдельным предметом с причиной и предикатом снятия.
  - id: DEADLINE-WRAPPER-ACCEPTS-NON-POSITIVE
    blocks: false
    statement: >-
      обёртка предела чтения отсечки не отвергает неположительную величину, в
      отличие от конструктора авторитета; сейчас недостижимо — оба вызывающих
      подают проверенную положительную величину.

orders: []
return_to: null
next: landing-reviewer (либо convergence-reviewer, если пакет схождения есть)
