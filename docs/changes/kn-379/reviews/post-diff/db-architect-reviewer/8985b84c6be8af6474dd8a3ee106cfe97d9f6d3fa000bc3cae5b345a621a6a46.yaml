# Пост-дифф ревью схемы и repo-слоя — роль db-architect-reviewer, круг 1.
#
# Имя файла — content_digest. Прежние записи не правятся; сменился дайджест —
# новый запуск и новый файл (change-graph §2). Запись не коммитится (задание).
# Текст — на уровне свойства: репозиторий публичен.
schema_version: 1
change_id: kn-379
holder: post-diff/db-architect-reviewer
owner: db-architect-reviewer
round: 1
mode: review
subject: >-
  kaname#379 — авторитет базового секрета читает отсечку отзыва-всех владельца
  тем же оператором, что строку удостоверения; каждое обращение к базе — под
  объявленным пределом.

fingerprint:
  repo: PRO-Robotech/kaname
  branch: '379'
  base: a889f108f22e1e42782b822677a4225467f3bdfc
  head: 5f51a6901a478357bdc30ddf4c4281aed536cc66
content_digest: sha256:8985b84c6be8af6474dd8a3ee106cfe97d9f6d3fa000bc3cae5b345a621a6a46
digest_definition: >-
  `git diff --full-index a889f108..5f51a6901a478357bdc30ddf4c4281aed536cc66 -- . ':!docs/changes' | sha256sum`;
  перемерено своей командой, HEAD копии совпал с головой.

verdict: accepted            # принято
blocks_landing: false
migrations_touched: 0
repo_sql_touched: 1          # прод-файл repo-слоя с SQL; прочие правки пакета — пробы
timestamp: '2026-09-23T13:02:33Z'
review_scope: >-
  по перечню путей диффа: миграций ноль; прод-SQL — один файл авторитета
  базового секрета, прочитан целиком по дельте; схема таблиц, на которые он
  опирается, сверена по миграциям дерева на голове. Пробы не гонялись: исход
  проверок объявляет не эта роль.

checked:
  - property: присоединение отсечки не размножает строку
    result: ok
    evidence: >-
      таблица отсечек держит первичный ключ по владельцу, внешнее присоединение
      даёт не больше одной строки; условий на присоединённую таблицу в предикате
      нет, внешнее присоединение не вырождается во внутреннее.
  - property: отсечка и строка удостоверения — один снимок
    result: ok
    evidence: >-
      читаются одним оператором на обеих полосах (резолв и живость); источник
      строки склеен из одного объявления на обе полосы, расхождение по источнику
      непредставимо.
  - property: якорь выдачи всегда представим
    result: ok
    evidence: >-
      момент выдачи — NOT NULL в обеих таблицах удостоверений, в месте не
      переписывается ни одним путём записи; у служебной учётки на месте отсечки
      типизированный NULL.
  - property: новый внутрисервисный инвариант без механизма базы
    result: none
    evidence: >-
      новых CAS, частичных UNIQUE, EXCLUDE, SKIP LOCKED нет; монотонность
      записи отсечки держит уже существующий однооператорный upsert; заказа на
      пробу с конкурирующими транзакциями нет. Путь «отсечку прочитать нельзя»
      закрыт пробой с конкурирующей транзакцией, держащей блокировку таблицы,
      включая контроль, что полоса машины о таблицу человека не спотыкается.
  - property: отображение отказов хранилища
    result: ok
    evidence: >-
      отказ несёт причину внутри и разворачивается в прежний единый sentinel
      (сравнений по равенству с ним в дереве ноль); ошибка базы и истёкший предел
      уходят наружу фиксированным текстом «состояние не установлено», текст
      драйвера — только в журнал.
  - property: границы домена и гигиена схемы
    result: ok
    evidence: >-
      внешних ключей через границу сервиса нет; ORM-артефактов нет; применённые
      миграции не тронуты.

findings: []
orders: []
distributed_overlap: true    # предел обращения и путь Unavailable — предмет system-design-reviewer
next: system-design-reviewer на тот же дайджест
