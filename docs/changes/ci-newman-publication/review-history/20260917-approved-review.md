# CI-NP-1 — независимое рассмотрение исправленного контракта

Дата: 2026-09-17. Автор: `/root/e2e_audit`; reviewer: `/root/hygiene_audit`.
Прочитаны полные документы, hashes сверены после последней авторской правки.
Документы автора не менялись; продуктовые исходники и holders не написаны.
Прежний CHANGES REQUESTED сохранён в `../newman-publication-review-round1.md`.

Приёмка: `docs/specs/sub-phase-CI-NP-1-newman-publication-acceptance.md`,
SHA256 `2c30dc5fb1ba6fb985b8e63918c5c4bd58a115d06bf31e35006acf737531e400`.
Замысел, редакция 4: `docs/specs/sub-phase-CI-NP-1-newman-publication-design.md`,
SHA256 `d60f34f6a2458e011f46cbe460310ed0e5071ee0635ae0fb188a70c387bc9181`.

## Acceptance-reviewer — APPROVED

Все десять GWT задают наблюдаемый исход и различают разрешение публикации,
тестовый вердикт, неполноту проверки и точный объём исторического исследования.
AR1 закрыт: гарантия новой публикации опирается на закрытую проекцию;
неизвестное явно объявленное кодирование даёт отказ, распознавание произвольной
неявной упаковки не обещано. Исторический CLEAN ограничен названной матрицей.
AR2 закрыт: входные keys/names/errors/log text и metadata также входят в границу;
публичные координаты производятся по закрытой схеме или доверенному каталогу.
AR3 закрыт: CI-NP-07 требует полный положительный собственный identity и каждый
объявленный платформенный шард, findings 0 и unmet 0; синтетический красный
CI-NP-01 сохраняется красным. Контракт #1804 не изменяется.

Конструктивность Given проверена реальными Newman 6.2.2 отчётами и действующими
читателями main. Независимый повтор 12 reader invocations дал:

| Сцена | exec-coverage | assert-suites-green |
|---|---:|---:|
| полный зелёный, 3 запроса / 3 утверждения | 0 | 0 |
| одно упавшее утверждение | 0 | 1 |
| записанное невыполненное условие, 2/3 запросов | 0 | 3 |
| ошибка скрипта, 2 утверждения | 0 | 1 |
| отсутствующий отчёт | 3 | 1 |
| чужой каталог с тем же числом трёх листьев | 3 | 1 |

Последняя пара особенно существенна: доверенное имя нельзя подставить до проверки
связи входного отчёта с текущим каталогом. Смена одного листа сохранила число,
но оба действующих читателя отвергли отчёт. Это подтверждает достижимость
положительного и отрицательного Given, а не готовность будущей проекции.
Третий reader newman-live исследован по коду, но здесь не исполнен: его реальное
участие остаётся обязательным для integration-tester.

Полный runtime CI-NP-07 и живое retention CI-NP-08–10 не подменяются локальной
фикстурой. Их производители существуют; точные revision/run/attempt/artifact
свидетельства предстоит получить. 485 metadata / 0 inspected bodies остаются
NOT_INSPECTED; это не число доказанных утечек и не разрешение удаления.

## Class-exposure-analyst — INITIAL RECORDED

Эта первая запись привязана к принятому отпечатку приёмки выше. Основание:
workspace ab74164e; Kachō b5fa093341f1fdbe96adfc6a9555482690968253;
Kaname 39628487099fe44990e9e03b09b725f3d7e37abb; corelib 34bc8104a832b67e53c868646ab2b1e0ac4c8562.
Классы взяты из code-authoring по именам разделов; новый класс не вводится.
Каждый holder ниже ещё требуется integration-tester, наличием текста он не доказан.

| ID / решение | Класс | Проверяемое условие на код | Требуемый holder |
|---|---|---|---|
| E1 / третий исход проверки | Умолчание, которое становится утверждением | Пустой, повреждённый, чужой, отсутствующий или неполный итог не разрешает upload и не даёт зелёный обязательный CI | CI-NP-03,05,06 |
| E2 / закрытая проекция | Контракт, который нельзя выразить | Ключи и строки выхода происходят только из закрытой схемы/доверенного каталога; произвольный текст снят независимо от узнавания секрета | CI-NP-01,02 |
| E3 / несколько написаний и упаковок | Контракт, который нельзя выразить | Все заявленные формы имеют различающий секрет/законный близнец; предел и неизвестная явная упаковка не CLEAN | CI-NP-02,03 |
| E4 / каталог и числовая сводка | Порядок операций внутри кода | Связь исходного отчёта проверена до подстановки доверенных идентификаторов; counts, verdict и coverage сохранены действующими readers | CI-NP-01,06,07 |
| E5 / metadata и диагностика | Инвариант и оснастка — это тоже прод-код | Filename/key/testname/error/manifest/SDK output не возвращают входной текст в публичный пакет или журнал | CI-NP-02 |
| E6 / checker отделён от upload | Решение и его следствие разнесены | Checker и транспорт работают с одним закрытым bytes snapshot; после check нет чтения mutable path | CI-NP-04 |
| E7 / официальный SDK с внутренним API | Чужой ответ: классификация и бюджет | Exact pin/lock/capabilities проверены; create/upload/finalize ошибки, отсутствие credentials и предел памяти запрещают публикацию, не становятся FINDING scanner | CI-NP-03,04 и SDK compatibility |
| E8 / общая реализация и два потребителя | Унификация и стоимость | Один corelib алгоритм, одна версия из module graph, архив несёт non-Go payload; временная materialization чистая и совпадает с этим pin; частная Kaname копия снята после работоспособной замены | CI-NP-05,07 и single-repo delivery |
| E9 / все workflow и shards | Инвариант и оснастка — это тоже прод-код | Перепись исполняемых публикаций полная и непустая, неизвестная динамическая форма не прощается; новый обход обнаружим | CI-NP-05,07 |
| E10 / асинхронная публикация красного прогона | Решение и его следствие разнесены | always позволяет обработать результат, но не скрывает красный, нулевой либо неполный тестовый исход | CI-NP-01,06,07 |
| E11 / историческое исследование | Умолчание, которое становится утверждением | Все страницы и доступные тела имеют точный исход; ошибка/истечение не считается сканированием; CLEAN ограничен измеренной матрицей | CI-NP-08,10 |
| E12 / удаление и повторная перепись | Порядок операций внутри кода | Удаление только content-verified и отдельно принятых точных объектов после повторной identity/digest сверки; exact 404 после; свежий срез перед посадкой | CI-NP-09,10 |

## Class-exposure-analyst — REVALIDATED

Пересверка привязана к замыслу d60f34f6… и не заменяет первую запись.
Все E1–E12 имеют конкретное решение:

| Класс | Решение замысла |
|---|---|
| E1 | Поток данных, пункты 2/7; NOT_EXECUTED запрещает upload |
| E2 | Закрытая проекция и доверенная связь; фиксированные schema/enum/ordinals |
| E3 | Carrier scanner и честная граница; bounded decoding и явный encoding |
| E4 | Каталог привязан до проекции; штатные readers на обеих формах |
| E5 | Производимые имена ZIP/manifest, закрытая диагностика; SDK вывод захвачен до вызова |
| E6 | Поток данных, пункты 4/5; private buffer и stream из тех же bytes |
| E7 | Измеренный exact @actions/artifact 6.2.1 seam; JS action node24 получает runtime credentials; Python env очищен |
| E8 | Владелец и доставка; corelib module pin, fresh runtime materialization и single-repo archive proof |
| E9 | Гейт провязки по parsed workflow и текущему объявленному составу |
| E10 | Поток данных и порядок; always не меняет тестовый итог |
| E11 | Retention и локализация; все страницы, четыре исхода и закрытый manifest |
| E12 | Retention: независимый landing manifest, повторная сверка перед действием, exact 404 и prelanding recensus |

Неразмеченных решений, маркеров отсрочки и неразрешённых архитектурных вопросов
в этом замысле не осталось. Невыполненные holders остаются будущей реализацией
доказательства, не выдаются за уже пройденные проверки.

## Design-reviewer — APPROVED

DR1/DR2 закрыты: безопасность основывается на закрытом публичном формате и на
проверке фактически отправляемого private buffer. Минимальный SDK transport общий
для двух продуктов, его протокол не копируется. Старый path uploader не может
считаться эквивалентной реализацией принятого byte binding.

Достижимость exact SDK seam независимо повторена на Node 26.8.1 и Node 24.21.0.
На Node24 фактическая uploadToBlobStorage/WaterMarkedUploadStream получила 34 bytes;
checked/received/returned SHA256 равны, один recording transport call, сеть 0.
Версия @actions/artifact 6.2.1, dependency lock SHA256
fcb99bf7fb824e8ddb93269eca3f3537427fdbd13c2e556383d3bda2e39f422c.
Node24 proof SHA256 217d8dabcc4e79be32b5cd323ec57b0beb669ee5c277448eb12f57e007d9d7a5.
Это proof API seam, не реальный upload и не RED отсутствующего инструмента.

Runtime delivery конструктивна: runs.using=node24 получает Actions credentials
внутри JS action, чего bare shell publisher не гарантирует. Это сверено по
[официальному NodeScriptActionHandler](https://github.com/actions/runner/blob/main/src/Runner.Worker/Handlers/NodeScriptActionHandler.cs)
и [ScriptHandler](https://github.com/actions/runner/blob/main/src/Runner.Worker/Handlers/ScriptHandler.cs).
Свежая runtime копия dependency из того же Go module pin допустима: отсутствие
коммитной копии, совпадение содержимого и установка lock без изменения module cache
являются исполняемыми обязательствами, а не предположением о форме архива.

## Следующий переход и границы

Review означает одобрение двух точных документов, а не GREEN продукта.
Автор оформляет append-only package и четыре внешних review события с точной
атрибуцией; root проверяет publication/readback и открывает holder route.
Затем независимый integration-tester строит именованные holders и первый RED
на годных фикстурах; source implementation начинается только после этого перехода.
Нельзя объявить runtime/retention выполненными локальным replay; нельзя удалить
неосмотренный пакет; нельзя менять исторические sealed evidence или использовать
legacy acceptance_path:null как разрешение retroapproval.

Локальные evidence: `constructibility-review.json`, 12 reader stdout/stderr,
`sdk-replay/proof.json`, `sdk-node24-replay/proof.json`; исходный capture и field
matrix автора сохранены отдельно. Новых product code/test файлов эта роль не создавала.
