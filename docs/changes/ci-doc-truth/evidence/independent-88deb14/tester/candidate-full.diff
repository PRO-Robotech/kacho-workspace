diff --git a/internal/repohygiene/catalogreachability_test.go b/internal/repohygiene/catalogreachability_test.go
index 6def223e2fb..b1fd7a2e0f6 100644
--- a/internal/repohygiene/catalogreachability_test.go
+++ b/internal/repohygiene/catalogreachability_test.go
@@ -27,12 +27,11 @@
 // смонтировано, и его строки резолвятся), а именно ради этого случая имена и
 // резервируют.
 //
-// ЧТО ИЗМЕРЕНО СЕЙЧАС, после выноса службы доступа в свой репозиторий (kacho#2616,
-// исход C, 2026-09-13): строк каталога 350, методов контракта 350, инертных 117 —
-// все 117 принадлежат службе, чей композиционный корень уехал, а каталог края
-// остался. Предикат переписи — сам гейт ниже, посадка
-// `TestCatalogReachability_InertRowsAreExactlyTheAllowedServices`; числа стареют,
-// перемеряй их прогоном, а не чтением этой строки.
+// ТЕКУЩУЮ ПЕРЕПИСЬ строк каталога, методов контракта и инертных строк печатает
+// `TestCatalogReachability_InertRowsAreExactlyTheAllowedServices`. После выноса
+// службы доступа в свой репозиторий (kacho#2616, исход C, 2026-09-13) её
+// композиционный корень уехал, а каталог края остался. Числа получают прогоном
+// этого гейта: он сверяет инертные строки с разрешёнными сервисами.
 package repohygiene
 
 import (
@@ -67,11 +66,11 @@ import (
 // вслед за ними уехали контракты и заглушки — их публикует МОДУЛЬ
 // `github.com/PRO-Robotech/kaname`, откуда анализатор их и читает (третий дом,
 // `apiStubHomes`). Каталог прав КРАЯ при этом остался здесь и по-прежнему несёт
-// 117 её строк из 350. Отсюда: каждая такая строка объявляет полосу для вызова,
+// её строки. Отсюда: каждая такая строка объявляет полосу для вызова,
 // который до края этого дерева не дойдёт, — и это не остаток, а раскладка.
 //
 // ЗДЕСЬ СТОЯЛО «когда контракт уедет вслед за реализацией, инвентарь опустеет
-// целиком». Контракт уехал, и инвентарь НЕ ОПУСТЕЛ: 117 строк на месте, потому
+// целиком». Контракт уехал, и инвентарь НЕ ОПУСТЕЛ: строки на месте, потому
 // что опустеть им было бы нечем — записи каталога никуда не уезжали, а
 // объявления методов читаются из модуля. Это тот самый класс, который корпус
 // ловит: предсказание пережило свой предмет. Инвентарь опустеет ровно тогда,
diff --git a/internal/repohygiene/grpcmountparity_test.go b/internal/repohygiene/grpcmountparity_test.go
index 0ebd6e15357..271d27653fe 100644
--- a/internal/repohygiene/grpcmountparity_test.go
+++ b/internal/repohygiene/grpcmountparity_test.go
@@ -66,13 +66,15 @@ import (
 // модуля. Написанное было верно для дерева, у которого второго дома нет, — а у
 // этого их три.
 //
-// ЧТО С ЗАПИСЯМИ СТАЛО, НАЗВАНО ЧИСЛОМ. Пакет `kaname.cloud.iam.v1` не
-// монтирует НИ ОДИН бинарь этого дерева, поэтому все 23 записи лежат ВНЕ
+// ЧТО С ЗАПИСЯМИ СТАЛО, ПОКАЗЫВАЕТ ПЕРЕПИСЬ. Пакет `kaname.cloud.iam.v1` не
+// монтирует НИ ОДИН бинарь этого дерева, поэтому его записи лежат ВНЕ
 // предмета анализатора монтирования (шапка `grpcmountparity.go`, §«Исключение
 // живёт, пока у него есть предмет») и попадают в графу переписи «исключений вне
-// предмета» — она печатает 23, а находок 0. Для СОСЕДНЕГО гейта, достижимости
-// каталога, те же записи остаются живыми и прощают 117 строк каталога прав из
-// 350: предикат у него свой, и там предмет у записи есть.
+// предмета». Текущий счёт печатает
+// `TestGRPCMountParity_EveryDeclaredServiceIsMounted`. Для СОСЕДНЕГО гейта,
+// достижимости каталога, те же записи остаются живыми: предикат у него свой,
+// и там предмет у записи есть. Число строк каталога и инертных среди них
+// печатает `TestCatalogReachability_InertRowsAreExactlyTheAllowedServices`.
 //
 // Запись самоистекает в обе стороны и по своему предикату, а не по чьему-то
 // решению: смонтирует кто-нибудь такой сервис — исключать станет нечего;
diff --git a/internal/repohygiene/subscriptionformshape.go b/internal/repohygiene/subscriptionformshape.go
index f967033e042..606675e8520 100644
--- a/internal/repohygiene/subscriptionformshape.go
+++ b/internal/repohygiene/subscriptionformshape.go
@@ -22,8 +22,8 @@
 //	                         причина её отсутствия названа в контракте.
 //	ИСХОД НЕЗАДАННОГО        начало — ветвление; исход НЕЗАДАННОЙ ветви назван и
 //	                         называет значение, объявленное этим же контрактом.
-//	НОСИТЕЛЬ — ВЫБОР         нагрузка события — ветвление ровно из двух ветвей,
-//	                         поэтому пустая нагрузка непредставима by construction.
+//	НОСИТЕЛЬ — ВЫБОР         форма объявляет ветвление ровно из двух ветвей;
+//	                         заполненность конкретного сообщения не проверяется.
 //	НОСИТЕЛЬ НАЗВАН ТИПОМ    ветвь состояния — именованный тип, а не свободная
 //	                         структура: у свободной ключи на проводе производятся
 //	                         от имён идентификаторов Go.
@@ -440,19 +440,19 @@ func AuditSubscriptionFormShape(
 		c.Assertions++
 		if _, ok := event.Oneof(e.CarrierOneof); !ok {
 			add("carrier-not-a-choice", event.Line,
-				"носитель нагрузки не выражен ветвлением `"+e.CarrierOneof+"`. Запретить "+
-					"прозой значение, которое допускает собственный тип, невозможно: без "+
-					"ветвления пустая нагрузка остаётся представимой, и подписчик прочтёт её "+
-					"как «у предмета не осталось полей»")
+				"носитель нагрузки не выражен ветвлением `"+e.CarrierOneof+"`. Форма обязана объявлять выбор: "+
+					"состояние ЛИБО признак, что состояния нет. "+
+					"Заполненность конкретного сообщения этот анализатор "+
+					"не проверяет")
 		} else {
 			branches := event.OneofBranches(e.CarrierOneof)
 			c.Assertions++
 			if len(branches) != 2 {
 				add("carrier-not-a-choice", event.Line,
 					fmt.Sprintf("ветвление носителя несёт %d ветв(ей), а обязано ровно две: "+
-						"состояние ЛИБО признак, что состояния нет. Одна ветвь возвращает "+
-						"представимость пустой нагрузки, третья — заводит исход, о котором "+
-						"подписчику не сказано, что с ним делать", len(branches)))
+						"состояние ЛИБО признак, что состояния нет. Здесь "+
+						"проверяется число объявленных ветвей, а не заполненность "+
+						"конкретного сообщения", len(branches)))
 			}
 			c.Assertions++
 			state, okState := event.Field(e.CarrierStateBranch)
diff --git a/internal/repohygiene/subscriptionformshape_injection_test.go b/internal/repohygiene/subscriptionformshape_injection_test.go
index d2737ea595b..4e8ac18fc2d 100644
--- a/internal/repohygiene/subscriptionformshape_injection_test.go
+++ b/internal/repohygiene/subscriptionformshape_injection_test.go
@@ -184,10 +184,9 @@ func requireKind(t *testing.T, findings []SubscriptionShapeFinding, kind string)
 // requireKindSaying требует находку названного вида, чей ТЕКСТ называет ИМЕННО
 // проверяемую ветвь.
 //
-// Вида МАЛО там, где вид вносится не одной ветвью. Единица счёта у этого
-// анализатора двойная и её нельзя смешивать: ВИДОВ находок двадцать, а МЕСТ
-// внесения двадцать два — два вида несут по две ветви каждый
-// (`carrier-not-a-choice`, `anchor-not-in-envelope`). Подпроба, утверждающая
+// Вида МАЛО там, где вид вносится не одной ветвью. ВИД находки и МЕСТО её
+// внесения — разные единицы: их определение и команды переписи находятся в
+// шапке `subscriptionformshape.go`, §«Единицы счёта». Подпроба, утверждающая
 // только вид, зеленеет, когда снятую ветвь подхватывает её соседка.
 //
 // Это измерено, а не предположено: сделав первую ветвь носителя недостижимой,
diff --git a/proto/corelib/subscription/subscription.proto b/proto/corelib/subscription/subscription.proto
index 5570ef665ee..59330898ae3 100644
--- a/proto/corelib/subscription/subscription.proto
+++ b/proto/corelib/subscription/subscription.proto
@@ -456,10 +456,10 @@ message SubscriptionEvent {
   // Носитель нагрузки — ВЫБОР ИЗ ДВУХ ВЕТВЕЙ: состояние ЛИБО признак, что
   // состояния нет.
   //
-  // Ветвление здесь потому, что запретить прозой значение, которое допускает
-  // собственный тип, невозможно. Пустая нагрузка НИКОГДА не означает «у предмета
-  // не осталось полей»: подставить пустой объект вместо признака недоступности
-  // в этой форме просто негде.
+  // Ветвление объявляет две альтернативы, но само по себе не требует выбрать
+  // ветвь или заполнить `state`. Пустая нагрузка НИКОГДА не означает «у предмета
+  // не осталось полей»: недоступность состояния передаётся отдельной ветвью
+  // `state_unavailable`, а не пустым объектом вместо неё.
   //
   // Подписчик, получивший непустую нагрузку, вправе читать её как ПОЛНОЕ
   // состояние предмета — и это единственный случай, когда он вправе так делать.
