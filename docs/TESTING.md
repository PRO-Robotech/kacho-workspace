# Практики тестирования кода → перенесено в skill `testing-code-coach`

> [!note] Этот документ упразднён (single source of truth)
> Методология тестирования **кода** (unit / integration: пирамида, AAA, fakes vs
> mocks, table-driven, property/mutation/fuzz, Clean-Architecture layering, анти-паттерны)
> теперь живёт в каноническом скиле и rule-модуле — не дублируется здесь:
>
> - **Skill** `.claude/skills/testing-code-coach/SKILL.md` — полная методология.
> - **Rule** `.claude/rules/testing.md` — строгий TDD (ban #12/#13), пирамида, инфраструктура.
> - Black-box / продуктовое тестирование — `.claude/skills/testing-product-coach/SKILL.md`.
>
> Оснастка синхронизируется во все репо через `./sync-tooling.sh` (источник истины —
> `kacho-workspace/.claude/`).
