# ВЕДОМОСТЬ ВЫНОСА — решения по предметам со ВТОРОЙ ПРОПИСКОЙ.
#
# Держатель нормы `arch-second-home-needs-a-decision`
# (`.claude/rules/architecture.md` §«Переиспользование»). Судят её
# `scripts/foundation-candidates/check-01-*` и `check-02-*`; измеряет
# `scripts/foundation-candidates/list-candidates.py`.
#
# ТРЕБОВАНИЕ ВЛАДЕЛЬЦА 2026-09-20, дословно:
#   «все общие библиотеки, которые могут быть вынесены должны быть вынесены
#    в corelib а не хорониться в проекте»
#
# ВЕДОМОСТЬ САМОИСТЕКАЕТ В ОБЕ СТОРОНЫ, как ведомость пар: кандидат без записи
# — находка, запись без предмета — тоже находка. Запись снимается ТЕМ ЖЕ
# изменением, которым снят её предмет.
#
# СЛОВАРЬ РЕШЕНИЙ ЗАКРЫТ — три:
#   move    вынос состоится; обязана называть задачу (`issue`);
#   keep    отказ от выноса. ДОВОД СУДИТСЯ ПО СУЩЕСТВУ, а не по непустоте:
#           непустота производится дописыванием любого текста, и `why:
#           "по усмотрению"` прежде проходил зелёным. Поэтому запись обязана
#           нести ДВА машинных факта:
#             class:     класс отказа из ЗАКРЫТОГО словаря восьми исключений
#                        (`scripts/foundation-candidates/_core.py` EXCLUSIONS);
#                        девятого нет по `arch-second-home-exclusions-closed`;
#             evidence:  координата `<продукт>:<путь>`, резолвящаяся в `.go`
#                        ИМЕННО ТОГО пакета, о котором запись, в стволе его
#                        продукта — дописать её нельзя, нужен настоящий файл;
#             why:       фраза для человека; несущей она больше НЕ является.
#           Граница названа вслух: ИСТИННОСТЬ класса для названной координаты
#           машинно не проверяется и держится вниманием — приёмкой записи
#           человеком и `check-verifier`;
#   sunset  предмет объявлен ПОД СНЯТИЕ; обязана называть задачу. Это же
#           признак ИСКЛЮЧЕНИЯ 8: живость класса критерий спросить не умеет,
#           и без записи он предписал бы вынос в пакет, который снимается.

ceiling:
  # ТОЧНЫЕ числа по факту, не потолок: ведомость, прощающая 42 при 36
  # находках, перестаёт быть храповиком (`testing.md` ledger-exact-not-ceiling).
  # Красное И на росте, И на просрочке: выросло — ухудшение; убыло — затяните
  # тем же изменением, которым сняли предмет.
  # 53 → 52 и 163 → 161 пересъёмкой 2026-09-22: УШЛА НЕ РАБОТА, УШЛО СХОДСТВО.
  # Ствол kacho сдвинулся f445aaaa554 → bec320cf47d одним коммитом (#2745,
  # «пробы носителя берут эфемерный порт»), и пара
  # `kacho:services/geo/cmd/kacho-geo/carrier_start_test.go` ↔
  # `kacho:services/nlb/cmd/kacho-loadbalancer/carrier_start_test.go`
  # перестала быть парой: нормализованных строк у nlb-копии стало 43 → 44,
  # общих осталось 35, J упал 0.700 → 0.686 при пороге 0.70. Обе копии на месте
  # — их РАЗВЕЛИ, а не вынесли. Числа здесь суть ВЕРХНЯЯ ГРАНИЦА второй
  # прописки (сказано в шапке `check-02`), и расхождение копий её опускает, не
  # исполняя нормы. Пара сидела ТОЧНО НА ПОРОГЕ, одна строка её и сняла.
  subjects: 52
  files: 161
  # 1 → 2 приёмкой 2026-09-21: неподвижная точка очереди бралась СНИЗУ, от
  # пустого множества, и предмет, чей единственный блокирующий импорт —
  # его же ВНЕШНИЙ ТЕСТОВЫЙ пакет, отсекался в первом круге и в очередь не
  # попадал никогда. Спрятанный предмет — пара состояний квоты:
  # `kacho:services/registry/internal/apps/kacho/quota/states.go` ↔
  # `kacho:services/storage/internal/apps/kacho/shared/quota/states.go`,
  # гасил её соседний `kinds_test.go` (`package quota_test`, импортирует свой
  # же пакет). Объявленное число было неверно на единицу; правится оно, а не
  # замер.
  actionable: 2
  measured: "kacho@bec320cf47d · kaname@cbbac984 · corelib@34bc810 · воркспейс@ee0055db"
  predicate: "RELICENSE_BUSL_DECIDED=1 RELICENSE_AGPL_DECIDED=1 J=0.70 python3 scripts/foundation-candidates/list-candidates.py"

# ПУСТАЯ ВЕДОМОСТЬ — ЦЕЛЬ, А НЕ ОТКАЗ. Сегодня очередь пуста ПО ПОСТРОЕНИЮ:
# решение владельца о перелицензировании выносимого не опубликовано (ярус 0
# нормы), и исключения 4а/4б держат весь класс. Их предикат снятия ВНЕШНИЙ —
# публикация решения записью, отдельно по BUSL-части и отдельно по
# AGPL-части. В день публикации очередь появляется, и каждая её строка обязана
# получить решение здесь.
decisions: []
