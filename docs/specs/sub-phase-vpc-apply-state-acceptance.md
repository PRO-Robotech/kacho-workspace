# Sub-phase vpc-apply-state — состояние применения в публичном контракте — Acceptance

> **Статус:** ✅ APPROVED (раунд 2 — оба блокирующих замечания раунда 1 закрыты по существу)
> Дата: 2026-08-14
> Ревьюер: acceptance-reviewer
> Эпик/issue: kacho#296 (последний незакрытый пункт задачи 29 эпика kacho#244)

> [!note] Что проверено ревьюером ПРОГОНОМ, а не чтением (раунд 2)
> Вердикт поставлен после самостоятельного перемера на `kacho@5df7da76`, а не по отчёту автора.
> Гейт тел исполнен: `go test ./gateway/internal/restmux/ -run TestNewmanCollectionsSendNoUnknownRequestFields -v`
> → `scanned 85 collections, 8771 requests, 2776 with a JSON body; 0 mis-addressed, 26
> body-on-absent-route (26 waived by name, 0 not)`, `PASS` — воспроизводится дословно. Отсутствие
> послабления у категории mis-addressed подтверждено чтением исходника гейта: `splitAbsentRouteBodies`
> применяется только к ветке `routeUnresolved`.
>
> Сошлись независимой проверкой: `22` запроса создания/обновления пакета и оба положительных
> контроля (`labels` — 7/7, `update_mask` — 7/7); `188` объявлений сообщений; `21` объявление
> переписи §3.3; `17` против `22` (разбор совпадает); четыре мёртвых списочных ответа — RPC `0`,
> не-сгенерированных Go-файлов `0` при контроле `ListNetworksResponse` → `1` и `4`; `3` строки
> `ReportIntentApplied` вне генерации и проб; предикат §1 — `1` попадание; `looksLikeApplyState`
> — ровно одно объявление; отсутствие HTTP-аннотаций у `InternalDataplaneService` (`0`) при
> контроле `network_service.proto` (`8`). Существование RPC у новых сценариев проверено:
> `InternalNetworkService.GetNetwork`, `Attach`, `Detach` объявлены.
>
> **Ревьюер ошибался дважды, и это записано, чтобы замечание не воскресло.** Раунд 1 требовал
> сценария, неконструируемого by construction, и содержал число `2` там, где предикат даёт `1`.
> Оба опровергнуты автором фактом; опровержение принято. Отдельно: при перемере ревьюер трижды
> получил расхождение с документом (`1` Go-файл вместо `0`, `4` вместо `6`, `69` строк вместо `3`)
> — каждый раз виноват был предикат ревьюера, а не документ: исключение генерации писалось как
> `^\./pkg/api/`, тогда как пути в выводе идут без `./`. Документ оказался прав во всех трёх.

## Обзор

Шов интеграции с датаплейном построен целиком со стороны платформы: намерение выдаётся
проекцией с ревизией, исполнитель забирает его потоком и отчитывается о применении, отчёт
сводится к паре «применено ли намерение текущей ревизии» и «класс причины отказа». Всё это
живёт в базе и в серверной проекции — и **не видно тому, кого касается**: арендатор смотрит
на свой ресурс как ни в чём не бывало, тогда как исполнитель его не применил.

Приёмка закрывает вторую половину: поле в публичных сообщениях семи ресурсов vpc, его
заполнение из уже готовой проекции и перечень поверхностей, где оно заполняется, а где нет.
Арендатору сообщается **ровно две вещи** — применено ли намерение текущей ревизии и, если
нет, класс причины из закрытого словаря. Ни ревизии, ни имени узла, ни кода ядра на этой
поверхности нет и появиться неоткуда.

Работа разбита на две стадии. **Стадия 1 снимает три значения статуса интерфейса, у которых
нет производителя и чьи комментарии заявляют ровно тот предмет, который вводит стадия 2** —
иначе продукт на время между стадиями отвечал бы на вопрос «доехало ли моё изменение до
сети» двумя способами, из которых один не производит никакой код. Со словаря **столбца** та
же стадия снимает четыре значения: к трём добавляется `'STATUS_UNSPECIFIED'`, стоявшее там с
`0001` и тоже без производителя, — плюс мёртвый дизъюнкт `status IS NULL` при `NOT NULL`-
столбце (§1.3.1, решение D6). Каждая стадия production-complete в своих границах и имеет
собственный DoD.

---

## §1. Что уже есть в дереве — измерено, а не пересказано

Все числа и утверждения ниже перемерены на `kacho@5df7da76` — это `origin/main` на
2026-08-14 15:37 (+05). Каждый предикат прогонялся через `git show origin/main:<путь>`, то
есть против ревизии, а не против рабочей копии, которая у соседней сессии может стоять на
чём угодно.

> [!note] Прежняя редакция пиннила ревизию задом наперёд
> Здесь стояло «`kacho@6e5e8bfa` (ветка `merge-287`, один коммит **поверх**
> `origin/main@5df7da76`)». Отношение обратное: `6e5e8bfa` — **предок** `5df7da76`
> (`git merge-base --is-ancestor 6e5e8bfa 5df7da76` завершается успехом), а деревья у них
> совпадают целиком (`git diff --stat 6e5e8bfa 5df7da76` — пусто). Числа от этого не
> поменялись, но пин, называющий предка потомком, посылает следующего читателя мерить не
> там; «ветка `merge-287`» — и вовсе координата чужой рабочей копии, которой у него нет.

| Что | Где | Предикат |
|---|---|---|
| Проекция «отчёт → две величины» | `services/vpc/internal/apps/kacho/api/dataplane/public_state.go` | `PublicApplyState{Applied, Reason}`, вывод сравнением ревизий |
| Закрытый словарь классов, единый на приём и на проекцию | там же, `KnownFailureReasons` — 6 значений | `CAPACITY · CONFLICT · UNSUPPORTED · DEPENDENCY_NOT_READY · TRANSIENT · EXECUTOR_INTERNAL` |
| Чтение партией одним стейтментом | `services/vpc/internal/repo/dataplane/public_state.go` | `Store.PublicApplyStates(ctx, ids)` — одно соединение, `LEFT JOIN`, `NOT i.withdrawn` |
| Состав проекции заперт | `.../dataplane/public_state_test.go:183` | `TestPublicProjectionCarriesExactlyTwoFacts` — падает на третьем поле |
| Гейт пары «поле ↔ заполнитель» | `services/vpc/internal/dto/toproto/dataplane_apply_pairing_test.go` | сегодня печатает `ОТКРЫТЫЙ ДОЛГ`; краснеет, как только появится любая одна половина |
| Проекция намерения и подтверждение | `services/vpc/internal/migrations/0032_dataplane_intent_projection.sql` | таблицы `dataplane_intent` / `dataplane_apply`, семь триггеров |
| Приём отчёта | `proto/kacho/cloud/vpc/v1/internal_dataplane_service.proto` | `ReportIntentApplied`, `system_admin` @ `cluster` |

**Публичного поля состояния применения нет ни в одном сообщении vpc.** Предикат:

```sh
for f in $(git ls-tree -r --name-only origin/main proto/kacho/cloud/vpc/v1/ | grep -v '/internal_'); do
  git show origin/main:$f | grep -niE "apply|applied|dataplane|failure_reason" | sed "s|^|$f:|"
done
```

даёт **1** попадание — `address_service.proto:357`, английское `may not apply` в прозе
комментария к признаку SMTP; объявлений поля нет ни одного. Контроль в обратную сторону:
тот же предикат по `internal_dataplane_service.proto` даёт **30** строк, среди них
`ApplyOutcome`, `ApplyFailureReason` и оба поля отчёта, — то есть предикат такие объявления
видеть умеет и «ноль» означает отсутствие, а не слепоту.

> [!note] Здесь стояло «**2** попадания, оба — слова `apply` и `reason`»
> Попадание одно, и второе слово в предикате не участвует вовсе: там стоит
> `failure_reason`, а не `reason`. Число было верным для какого-то другого прогона, а
> пояснение — для какого-то третьего; вместе они создавали вид измеренности там, где
> измерения не было. Перемерено на `5df7da76` командой выше.

### 1.1. Виды объектов, у которых есть намерение, — ровно семь

Триггеры миграции 0032 навешены на `networks`, `subnets`, `network_interfaces`,
`security_groups`, `route_tables`, `gateways`, `addresses`; ограничение
`dataplane_intent_kind_known` перечисляет те же семь и отвергает восьмой. Это и есть
граница поля: **оно появляется ровно там, где существует строка намерения**. `CidrGroup`
намерения не имеет и поля не получает; `AddressPool` — Internal-ресурс и на публичной
поверхности не стоит.

### 1.2. Компонента-исполнителя в дереве НЕТ

Предикат: `grep -rn "ReportIntentApplied" --include=*.go .` вне `pkg/api/` и вне проб даёт
**3** строки, и все три — серверный хендлер `services/vpc/.../dataplane/handler.go`.
Вызывающей стороны не существует; рёбра сетевого оператора сняты решением владельца
2026-08-09 (`polyrepo.md` §runtime-edges).

**Это меняет то, как строятся Given.** «Исполнитель отчитался» — не событие компонента, а
вызов `InternalDataplaneService.ReportIntentApplied`, и других способов его произвести нет.
Следствие для проверок разобрано в §6.

### 1.3. Статус интерфейса заявляет предмет, которого не производит

`NetworkInterface.Status` объявляет шесть значений. Предикат по не-тестовому дереву vpc
(`grep -rn "NIStatus[A-Za-z]*"` минус домен/хелперы/маппер, плюс
`grep -rhoE "status\s*=\s*'[A-Z_]+'" services/vpc/internal/repo/kacho/pg/*.go`) даёт **три**
места записи и **два** достижимых значения:

| Место записи | Значение |
|---|---|
| создание интерфейса — вставка через перевод `helpers.NIStatusName` | `AVAILABLE` |
| CAS привязки к машине — литерал в SQL | `ACTIVE` |
| CAS отвязки — литерал в SQL | `AVAILABLE` |

`PROVISIONING`, `FAILED`, `DELETING` производителя не имеют **ни одного**. Контроль:
`kachomock` пишет `ACTIVE`/`AVAILABLE`, но это дублёр — его импортируют только пробы
(предикат `grep -rln kachomock --include=*.go` даёт файлы самого пакета, его интерфейс и
`*_test.go`).

**Первая строка таблицы требует точности, и от неё зависит стадия 1.** Вставка пишет не
константу, а результат перевода: `services/vpc/internal/repo/kacho/pg/network_interface.go`
подставляет `helpers.NIStatusName(n.Status)`, и у этого перевода есть **ветвь по
умолчанию**, возвращающая `'STATUS_UNSPECIFIED'`. Сегодня она недостижима — единственный
вызывающий вставки один (`.../api/networkinterface/create.go`, где несколькими строками выше
статус выставлен в `domain.NIStatusAvailable`), — но недостижима она **по одному
вызывающему**, а не по построению.

При этом комментарии контракта у трёх мёртвых значений заявляют **датаплейн**:
«создаётся / программируется data-plane», «программирование data-plane не удалось»,
«удаляется (data-plane сносит wiring)». То есть контракт уже отвечает на вопрос стадии 2 —
и отвечает значением, которого не выдаёт никто.

Потребителей у этих трёх значений нет: в консоли попадания на `PROVISIONING` принадлежат
статусу машины и словарям цвета, которые ключуются строкой и терпят неприход значения; в
провайдере ресурса сетевого интерфейса нет вовсе; в newman-кейсах vpc — **0** утверждений
об этих значениях — и, если точнее, **ни об одном** значении статуса интерфейса вообще:
`git grep -c "PROVISIONING\|DELETING\|ACTIVE\|AVAILABLE" origin/main -- services/vpc/tests/newman/cases/`
не даёт ничего ни на снятых значениях, ни на живых. Контроль, чтобы «пусто» не означало «не
читали»: тот же обход по тем же **17** файлам находит `networkInterfaces` в **7** из них.

**Следствие для стадии 1, названное прямо:** живое значение статуса сквозной пробой сегодня
не закреплено ничем, поэтому APPLY-04 заводит первое такое утверждение, а не подтверждает
существующее. Без него сужение перечисления прошло бы через весь сквозной набор незамеченным
— включая случай, когда оно сузило бы лишнего.

### 1.3.1. Словарь СТОЛБЦА шире перечисления контракта на два элемента

Ограничение из `0001_initial.sql` (строки 480–481) и определение столбца (строка 468) стоят
в одном блоке, поэтому их противоречие видно без перехода между файлами — предикат:
`git show origin/main:services/vpc/internal/migrations/0001_initial.sql | sed -n '457,481p'`.

```sql
    status             text         NOT NULL DEFAULT 'AVAILABLE',
    ...
    CONSTRAINT network_interfaces_status_check
        CHECK (status IS NULL OR status IN ('PROVISIONING','ACTIVE','AVAILABLE','FAILED','DELETING','STATUS_UNSPECIFIED')),
```

Оба лишних элемента надо решить **явно** — иначе стадия 1 сузит контракт и **не** сузит базу,
то есть оставит ровно то расхождение, ради устранения которого затевается:

| Значение столбца | Кто производит | Замечание |
|---|---|---|
| `NULL` | **никто, by construction** | столбец `NOT NULL DEFAULT 'AVAILABLE'` — дизъюнкт `status IS NULL` мёртв с первого дня и читается как разрешение, которого нет |
| `'STATUS_UNSPECIFIED'` | **никто сегодня** | достижим ветвью по умолчанию перевода выше, если у вставки появится второй вызывающий |

Решение по обоим — D6.

### 1.4. Прецедент, по которому решается «статус или не статус»

Миграция `0003_drop_security_group_status.sql` сняла статус группы безопасности с
формулировкой «у SG нет provisioning-lifecycle — статус никем не наблюдался». Предмет тот
же: значение без производителя. Разница со стадией 2 в том, что **у состояния применения
производитель есть** — приём отчёта, — поэтому поле законно; но оно про применение одной
ревизии намерения, а не про жизненный цикл ресурса.

---

## §2. Решения

Развилки решены здесь; по каждой названы довод, цена и предикат пересмотра.

### D1. Поле стоит на самом ресурсе, а не в отдельном RPC

**Решение.** Состояние применения — поле публичных сообщений семи ресурсов, а не отдельный
`GetApplyState`.

**Довод.** Арендатор задаёт вопрос «доехало ли моё изменение» в тот же момент, когда
смотрит на ресурс. Отдельный RPC означал бы второй вызов на каждый просмотр и **отдельный
пакетный контракт** для списка из пятидесяти строк — иначе страница превратилась бы в
пятьдесят обращений. Пакетное чтение у нас уже есть и рассчитано ровно на страницу.

**Цена.** Семь сообщений меняются вместо одного нового; поле попадает в те проекции этих
сообщений, где оно не заполняется, — поэтому §3.3 перечисляет их поимённо, а не оставляет
пустоту толковать читателю.

**Предикат пересмотра.** Если у платформы появится поверхность, где состояние применения
нужно **без** ресурса (например, сводка по проекту), она заводится отдельным RPC поверх той
же проекции, а не переносом поля.

### D2. Форма поля — сообщение, а не два скаляра

**Решение.**

```protobuf
// apply_state.proto
message ApplyState {
  bool applied = 1;
  ApplyFailureReason reason = 2;
}
```

и на ресурсе — `ApplyState apply_state = N;`.

**Довод.** Различать нужно **три** состояния, а не два: «платформа утверждает, что
применено», «платформа утверждает, что не применено» и «платформа не делает утверждения
вовсе». У скалярного `bool` третьего представления нет — незаполненное и `false`
неотличимы по проводу, и «нет данных» немедленно стало бы читаться как «не применено». У
поля-сообщения присутствие выражает третье состояние by construction.

Это же снимает риск фабрикации: путь, не получивший ответа от проекции, отдаёт **пустое**,
а не правдоподобное.

**Цена.** Клиент обязан проверить присутствие, прежде чем читать `applied`. На публичном
крае это дёшево и однозначно: маршалер публичного мультиплексора собирается с
`EmitUnpopulated=true` (`gateway/internal/restmux/mux.go`), поэтому незаполненное
поле-сообщение приезжает как `"applyState": null`, а заполненное — объектом, у которого
`applied` присутствует всегда, в том числе со значением `false`.

**Предикат пересмотра.** Если публичная проекция когда-нибудь станет обязана заполняться
**всегда** и всеми путями, третье состояние исчезнет и форму можно упростить. Сегодня оно
конструируемо (сценарий APPLY-13), значит форма нужна.

### D3. Словарь классов отказа — ОДИН, переиспользуется, а не переобъявляется

**Решение.** Публичное поле ссылается на **тот же** `kacho.cloud.vpc.v1.ApplyFailureReason`,
которым пользуется приём отчёта. Объявление перечисления переезжает из
`internal_dataplane_service.proto` в новый нейтральный файл
`proto/kacho/cloud/vpc/v1/apply_state.proto`; внутренний контракт его импортирует.
Полное имя типа не меняется — значит ни провод, ни JSON не трогаются.

**Довод.** Второй перечень разошёлся бы с первым **молча**, и разошёлся бы там, где
расхождение не видно: класс, добавленный в публичный контракт и забытый в проверке приёма,
отвергался бы у исполнителя; класс, забытый в публичном, не доезжал бы до арендатора.
Серверный код это уже знает — `KnownFailureReasons` заведён единым источником именно с этой
мотивировкой. Заводить в контракте то, чего избежали в коде, — регресс.

Оставить перечисление в файле внутреннего сервиса тоже нельзя: тогда публичные сообщения
импортировали бы файл, чьё имя называет внутренний сервис, и справочник API арендатора
ссылался бы на него на каждом из семи ресурсов. Это стареет плохо и множится с каждым новым
потребителем.

**Цена.** Перенос символа между файлами способен дать находку `buf breaking` — проверка
собрана с категорией `FILE` (`proto/buf.yaml`), а она следит и за раскладкой символов по
файлам. **Утверждать за инструмент здесь нечего и не нужно**: перечень объявленных разрывов
устроен так, что решение принимает он, а не автор приёмки.

**Правило заполнения перечня — единственное, что связывает исполнителя.** В
`proto/declared-breaks.yaml` вносятся **ровно те** находки, которые выдал `buf breaking`, с
полями `rule/path/symbol/reason/issue` (`issue: kacho#296`), и ни одной сверх: находка без
записи роняет прогон, запись без находки роняет его тоже. Поэтому «сколько записей» —
не предмет решения приёмки, а вывод инструмента; ноль записей означает, что находок не
было, и это законный исход.

Запись **самоистекающая**: она обязана быть снята тем же изменением, которое поднимет базу
сравнения. Реальная цена ломающего изменения равна нулю: потребителя у внутреннего контракта
в дереве нет (§1.2).

**Предикат пересмотра.** Если у публичной и внутренней сторон когда-нибудь разойдутся
предметы (исполнитель научится называть классы, которых арендатору знать не нужно), словари
расходятся — но **явно**, отдельным решением, с проекцией одного в другой и с краснеющим
утверждением о полноте проекции.

### D4. `ApplyOutcome` на публичную поверхность НЕ выходит

**Решение.** Публичное поле несёт `bool applied`, а не перечисление исхода.

**Довод.** У исхода два значения, и второе выражается парой «`applied=false` + класс». Третье
представление того же факта было бы вторым написанием одного значения: два способа сказать
«не применено», не нормализуемые друг в друга.

**Цена.** Если у исхода когда-нибудь появится третье значение (например, «применено
частично»), `bool` придётся менять на перечисление — ломающее изменение.

**Предикат пересмотра.** Появление третьего исхода в `dataplane_apply_outcome_known`.

### D5. Ревизия наружу НЕ выходит — ни в каком виде

**Решение.** Публичная проекция несёт два факта и **не** несёт ни ревизию намерения, ни
ревизию отчёта, ни время отчёта.

**Довод — про безопасность, а не про лаконичность.** Последовательность ревизий **одна на
весь домен** (`dataplane_intent_revision_seq`, миграция 0032; выбрана единой намеренно —
исполнитель продолжает поток с одной позиции). Значит номер ревизии на объекте одного
арендатора есть счётчик мутаций **всего облака**: два чтения с интервалом дают темп
изменений у всех остальных. Это ровно тот вид сведений, который правило двух проекций
относит к внутренней поверхности, — арендатор не должен выводить из своего ресурса ничего о
чужих.

Время отчёта не выходит по той же причине в более слабой форме: оно датирует активность
исполнителя.

**Цена.** Арендатор не может отличить «отчёта не было вовсе» от «отчёт был про прошлую
ревизию». Обе ситуации значат одно — «об этом изменении исполнитель ещё ничего не сказал», —
поэтому потеря информации нулевая для того, кто спрашивает про своё изменение.

**Держится.** Составом `PublicApplyState` (проба `TestPublicProjectionCarriesExactlyTwoFacts`
уже падает на третьем поле) и зеркальной пробой по дескриптору `ApplyState` (сценарий
APPLY-10).

**Предикат пересмотра.** Если последовательность ревизий станет пер-проектной, довод о
кросс-арендной утечке отпадёт, и вопрос можно ставить заново. Сегодня она доменная —
предикат `grep -n "CREATE SEQUENCE" services/vpc/internal/migrations/0032_*.sql`.

### D6. Мёртвые значения статуса интерфейса снимаются: три с контракта, четыре со столбца — стадия 1

**Решение.** `NetworkInterface.Status` теряет `PROVISIONING`(1), `FAILED`(4), `DELETING`(5);
номера и имена резервируются. Остаются `STATUS_UNSPECIFIED`, `ACTIVE`, `AVAILABLE`.
Комментарий поля переписывается на то, чем оно является: **состояние привязки** — `ACTIVE`
интерфейс привязан к машине, `AVAILABLE` не привязан.

**Довод.** Три снимаемых значения не производит никто (§1.3), а их комментарии заявляют
предмет стадии 2. Оставить их — значит выпустить контракт, отвечающий на один вопрос двумя
способами, из которых один мёртв: арендатор, увидевший поле применения, справедливо
спросит, чем оно отличается от `status: FAILED`, и правильного ответа не будет.

**Порядок стадий выведен отсюда же.** Снятие идёт **первым**, поэтому момента, когда в
контракте стоят оба ответа, не существует ни на одном коммите.

**Цена.** Снятие значений перечисления — ломающее изменение контракта; находки `buf
breaking` вносятся в `declared-breaks.yaml` по тому же правилу, что в D3 (ровно те, что выдал
инструмент). Плюс миграция, сужающая `network_interfaces_status_check`.

**Сужение идёт до ДВУХ значений, а не до трёх, и это решение о базе, а не о контракте:**

```sql
CONSTRAINT network_interfaces_status_check CHECK (status IN ('ACTIVE','AVAILABLE'))
```

Обоснование по каждому снимаемому элементу словаря столбца — их четыре, а не три (§1.3.1):

| Снимается | Довод |
|---|---|
| `'PROVISIONING'`, `'FAILED'`, `'DELETING'` | производителя нет, комментарии заявляют предмет стадии 2 |
| `status IS NULL` | дизъюнкт мёртв с первого дня: столбец `NOT NULL DEFAULT 'AVAILABLE'`. Оставить его значило бы перенести в новую миграцию условие, которое ничего не разрешает и читается как разрешение |
| `'STATUS_UNSPECIFIED'` | «не сказано» не является состоянием привязки: интерфейс либо привязан, либо нет, третьего нет. Достижимо оно только ветвью по умолчанию перевода — то есть выражало бы не состояние, а промах вызывающего |

**Токен `STATUS_UNSPECIFIED` остаётся в перечислении контракта** — proto3 требует нулевого
значения, и снять его нельзя. Расхождения «два места об одном предмете» тут нет: в контракте
ноль означает «поле не заполнено» (свойство провода), в столбце он не означал бы ничего.
Чтобы токен не приезжал арендатору иначе как из незаполненного поля, **обратный перевод
теряет корзину «прочее»**: `helpers.NIStatusFromName` сегодня возвращает
`NIStatusUnspecified` на **любой** нераспознанный текст, то есть молча превращает
расхождение словарей в правдоподобное значение. После сужения нераспознанный текст в столбце
невозможен, а значит его появление — расхождение, и оно обязано быть шумным: перевод
возвращает ошибку, чтение отвечает фиксированным непрозрачным `INTERNAL`. Это то же решение,
что D8, применённое к соседней таблице, — и принимается оно здесь именно потому, что стадия 1
отнимает у столбца последнее значение, которым эта корзина маскировалась.

**Обратное заполнение** объявляется **в самой миграции** и выводится из авторитета, а не
угадывается: строка с любым из четырёх снимаемых значений получает `ACTIVE`, если
`used_by_id` непуст, и `AVAILABLE` иначе, — привязка и есть то, что это поле выражает. Один
источник направления на все четыре: заводить второе правило для `'STATUS_UNSPECIFIED'` не из
чего, авторитет у него тот же. Число переписанных строк печатается всегда; ожидаемое — ноль,
и «ноль переписано» обязано быть отличимо от «не смотрели».

**Потребителей нет** — измерено в §1.3, включая консоль, провайдер и newman; у последнего
утверждений о статусе интерфейса нет вовсе, поэтому APPLY-04 заводит первое, а не наследует
существующее.

**Предикат пересмотра.** Появление производителя хотя бы одного из снятых значений в
не-тестовом дереве. Такого производителя стадия 2 не создаёт: она пишет **другое** поле.

### D7. Отсутствие строки намерения у живого ресурса — «утверждения нет», а не отказ

**Решение.** Если проекция не вернула строку по идентификатору ресурса, который чтение
отдаёт, поле приезжает **незаполненным** (`applyState: null`), чтение при этом успешно; факт
попадает в наблюдаемость отдельным счётчиком.

**Довод.** Строка намерения выдаётся AFTER-триггером **в той же транзакции**, что и мутация
ресурса, а миграция 0032 заполнила её всем, что было создано раньше. Значит у живого ресурса
она есть by construction — кроме одного законного случая: удаление в полёте. Чтение увидело
ресурс, параллельное удаление зафиксировалось, намерение стало снятым, а снятые строки
проекция намеренно исключает (их ревизия принадлежит самому снятию). Ронять на этом чтение
значило бы отдавать `INTERNAL` на штатной гонке.

Заполнять при этом «не применено» тоже нельзя: для сломанной проекции это была бы
правдоподобная неправда, а правдоподобное отсутствие не замечают.

**Цена.** Незаполненное поле имеет два прочтения — «удаляется» и «проекция сломана», — и
различить их в момент чтения нечем. Поэтому цена оплачивается наблюдаемостью, а не молчанием:
установившийся ненулевой темп таких попаданий, не объяснимый удалениями, есть сигнал дефекта.
«Ноль за всю жизнь» обязано быть так же заметно, как ноль доставленных строк очереди.

**Предикат пересмотра — ДВА факта, и нужны оба.** Гонка перестала бы быть выразимой, только
если бы ресурс и его проекция читались **одним снимком**. Для этого требуется, чтобы (а)
проекция читалась **той же транзакцией**, что и ресурс, и (б) эта транзакция шла на изоляции,
дающей общий снимок. Сегодня не выполнено **ни одно**:

| Факт | Как проверить | Сегодня |
|---|---|---|
| (а) общая транзакция | `grep -n "pool.Query" services/vpc/internal/repo/dataplane/public_state.go` | проекция читается **своим соединением из пула**, вне читательской транзакции ресурса |
| (б) изоляция снимка | `grep -n "func (r \*Repository) Reader" -A4 services/vpc/internal/repo/kacho/pg/repository.go` | `BeginTx` с `AccessMode: pgx.ReadOnly` и **без** `IsoLevel`, то есть `read committed` |

> [!note] Прежняя редакция называла только (б) — и этого мало
> Она предлагала «перевести чтения ресурса на изоляцию, дающую общий снимок», ссылаясь на
> строку открытия читательской транзакции. Само по себе это гонку не убрало бы: проекция
> в ту транзакцию **не входит**, она читается отдельным соединением, поэтому её снимок
> сменился бы независимо от изоляции ресурса. Предикат описывал механизм, которого в
> потоке нет, — и, сработав, снял бы решение, не устранив его причину.

### D8. Строка проекции негодной формы роняет чтение целиком

**Решение.** Значение вне закрытого словаря либо несогласованная пара «исход ↔ класс»
приводят к отказу всего чтения с фиксированным непрозрачным текстом, а не к пропуску одной
записи и не к «не применено».

**Довод.** Такое значение может появиться ровно одним способом — словарь базы разошёлся с
контрактом. Свернув расхождение в «не применено», проекция сообщила бы арендатору
правдоподобную неправду и пережила бы собственный предмет; пропустив запись — отдала бы
страницу, о полноте которой нечего сказать. Серверная половина так себя и ведёт уже сегодня.

**Цена.** Один испорченный ряд делает ресурс нечитаемым до починки. Это осознанный размен:
расхождение словарей обязано быть шумным.

---

## §3. Контракт

### 3.1. Поле

```protobuf
// В КАЖДОМ из семи публичных сообщений ресурсов vpc:

  // Состояние применения намерения ТЕКУЩЕЙ ревизии ресурса — только чтение.
  //
  // Незаполненное поле означает «платформа не делает утверждения об этом
  // объекте» (намерение снимается) и НЕ означает «не применено».
  //
  // Заполняется чтениями ресурса: `Get` и списочные RPC этого сервиса.
  // НЕ заполняется: `Operation.response`, поток намерения исполнителя,
  // внутренние проекции — перечень и причина в §3.3 приёмки.
  ApplyState apply_state = N;
```

Номера, не пересекающие ни занятых, ни зарезервированных (замерено по каждому сообщению):
`Network` 11 · `Subnet` 20 · `NetworkInterface` 21 · `SecurityGroup` 12 · `RouteTable` 9 ·
`Gateway` 11 · `Address` 24.

**Имя обязано содержать `apply`.** Это не вкусовщина: гейт пары
(`dataplane_apply_pairing_test.go`, `looksLikeApplyState`) опознаёт поле по вхождению `apply`
в имя поля либо в имя его типа. Поле, названное иначе, оставит гейт зелёным навсегда — то
есть пара «поле ↔ заполнитель» перестанет быть под наблюдением ровно в тот момент, когда
наблюдение впервые понадобится.

### 3.2. JSON публичного края

| Состояние | Тело ответа |
|---|---|
| утверждения нет (намерение снимается) | `"applyState": null` |
| намерение в работе | `"applyState": {"applied": false, "reason": "APPLY_FAILURE_REASON_UNSPECIFIED"}` |
| применено | `"applyState": {"applied": true, "reason": "APPLY_FAILURE_REASON_UNSPECIFIED"}` |
| не применено, класс назван | `"applyState": {"applied": false, "reason": "APPLY_FAILURE_REASON_CAPACITY"}` |

`reason` присутствует всегда, потому что публичный маршалер выдаёт незаполненные поля;
«класса нет» выражается токеном `APPLY_FAILURE_REASON_UNSPECIFIED`, а не отсутствием ключа.

### 3.3. Перечень поверхностей — полный, с решением по каждой

Перепись сделана по механизму, а не по диффу: перечислены **все** объявления полей типа
одного из семи ресурсов во всех контрактах vpc, плюс семь `Get`, отдающих голое сообщение.
Предикат переписи (даёт **21** объявление; расклад по сообщениям — ниже):

```sh
for f in $(git ls-tree -r --name-only origin/main proto/kacho/cloud/vpc/v1/); do
  git show origin/main:$f | awk -v F="$(basename $f)" '
    /^message [A-Za-z]+/ {msg=$2}
    /^\s*(repeated )?(Network|Subnet|NetworkInterface|SecurityGroup|RouteTable|Gateway|Address) [a-z_]+ = [0-9]+;/ {print F, msg, $0}'
done
```

**Колонка «утверждение» обязательна у каждой строки обеих таблиц.** Поверхность без
идентификатора сценария — это и есть непроверяемое «ни одна лишняя»: перечень называет
решение, а упасть на его нарушении нечему. Счёт замыкается арифметикой: 14 + 5 = 19 строк,
19 идентификаторов, ни одной строки без идентификатора и ни одного идентификатора без строки.

**Заполняют — 14 поверхностей, и у каждой есть RPC:**

| Поверхность | Сколько | Решение | Утверждение |
|---|---|---|---|
| `Get` семи ресурсов (`Network`, `Subnet`, `NetworkInterface`, `SecurityGroup`, `RouteTable`, `Gateway`, `Address`) | 7 | заполняет | APPLY-11 (newman-половина, по кейсу на ресурс) |
| `ListNetworksResponse`, `ListSubnetsResponse`, `ListNetworkInterfacesResponse`, `ListSecurityGroupsResponse`, `ListRouteTablesResponse`, `ListGatewaysResponse`, `ListAddressesResponse` | 7 | заполняет | APPLY-12 (по кейсу на список) |

Предикат живости, прогнанный по каждой: `grep "returns (<ответ>)" proto/kacho/cloud/vpc/v1/*.proto`
даёт по единице на все семь списочных ответов, и по одному объявлению `rpc Get … returns
(<Ресурс>)` на каждый из семи ресурсов.

> [!warning] Четыре вложенных списочных ответа выглядят поверхностями и ими НЕ являются
> `ListNetworkSubnetsResponse`, `ListNetworkSecurityGroupsResponse`,
> `ListNetworkRouteTablesResponse`, `ListAddressesBySubnetResponse` объявлены в контрактах и
> несут `repeated` семи ресурсов — по форме неотличимы от живых списков выше. **Их не
> возвращает ни один RPC, и на них не ссылается ни один не-сгенерированный файл Go.**
>
> Предикат с контролем в обе стороны: для каждого из четырёх — RPC `0`, не-generated
> Go-файлов `0`; для контрольного `ListNetworksResponse` той же формы — RPC `1`, Go-файлов
> `4`. То есть предикат живые ответы видеть умеет, а эти четыре мертвы.
>
> **Следствие для приёмки:** сценария «вложенный список несёт состояние» здесь нет и быть не
> может — предусловие неконструируемо, вызвать нечего. Заявить их заполняющими значило бы
> потребовать заполнителя для пути, который не исполняется никогда, и получить утверждение,
> которое не сможет покраснеть. Первая редакция этого раздела ровно так и была написана, и
> ошибка вскрылась только предикатом живости, а не чтением.
>
> Сами мёртвые сообщения — **отдельный предмет** (§4): поле состояния появится в них
> автоматически, потому что живёт на сообщении ресурса, а не на ответе; но заполнителя они не
> получают и в перепись поверхностей не входят.

Неперечисленная поверхность и есть то, из-за чего класс переживает свою починку, — поэтому
перепись сделана по механизму (все объявления полей типа семи ресурсов во всех контрактах
vpc), а не по диффу, и каждая найденная строка проверена на живость отдельно.

**Не заполняют — 5 поверхностей, и каждая говорит об этом в комментарии своего поля:**

| Поверхность | Почему | Утверждение |
|---|---|---|
| `Operation.response` (ресурс, упакованный в `Any` резолвером операций) | в момент завершения операции исполнитель заведомо ещё не отчитался — поле всегда несло бы одно и то же и читалось бы как «операция готова, но что-то не так». Плюс опрос операции — самый горячий путь, и вопрос к проекции на каждом опросе не окупается ничем | APPLY-21 |
| `DataplaneIntent` (поток намерения, `:9091`) | это отчёт **самого исполнителя**; возвращать его обратно значит завести второй источник того же факта и замкнуть петлю | APPLY-20 |
| `GetInternalNetworkResponse.network` | внутренняя проекция читает таблицы напрямую; публичная сводка ей ничего не добавляет | APPLY-24 |
| `AttachNetworkInterfaceResponse.network_interface` | предмет ответа — исход привязки, а не состояние сети; на момент ответа отчёта по новой ревизии заведомо нет | APPLY-25 |
| `DetachNetworkInterfaceResponse.network_interface` | то же основание, зеркально | APPLY-25 |

> [!note] Три из этих пяти прежде стояли без утверждения — и «ни одна лишняя» было словом
> `Operation.response` и поток намерения свои сценарии несли (APPLY-21, APPLY-20), а
> `GetInternalNetworkResponse` и оба ответа привязки — нет. Пропущенная поверхность и есть
> место, где заполнитель провязывается «заодно» и остаётся незамеченным: заполнить лишнее
> ничего не роняет, а гейт пары (`dataplane_apply_pairing_test.go`) обходит **семь сообщений
> ресурса** и о поверхностях не знает вовсе — ему достаточно **одного** вызова заполнителя
> где угодно в дереве. Утверждения APPLY-24 и APPLY-25 заведены ровно за этим.

Все пять живы, и это проверено, а не предположено — иначе перечень незаполняющих поверхностей
описывал бы намерение, а не код. Четыре из них — сообщения ответов, и предикат у них тот же,
что у заполняющих: `AttachNetworkInterfaceResponse`, `DetachNetworkInterfaceResponse`,
`GetInternalNetworkResponse` — по одному RPC и по одному не-сгенерированному файлу Go;
`WatchIntentResponse` — один RPC и семь файлов. Пятая, `Operation.response`, — не сообщение
ответа, а упаковка в `Any`, и живость у неё своя: резолвер операций vpc делает **7** таких
упаковок, по одной на вид ресурса.

**Вход поля не принимает нигде.** Запросы создания и обновления семи ресурсов **плоские**:
предикат переписи выше не находит **ни одного** поля типа ресурса в
`Create*Request`/`Update*Request`, при том что самих таких запросов **17**. Прислать
состояние применения невозможно by construction, а не по договорённости.

Число **17** — это `Create*Request`/`Update*Request` в **семи публичных файлах сервисов**
ресурсов (`{network,subnet,network_interface,security_group,route_table,gateway,address}_service.proto`).
По всему каталогу `proto/kacho/cloud/vpc/v1/` их **22**: пять сверх — `CidrGroup` (2) и
Internal-поверхности `AddressPool`/`OwnedAddress` (3). Оба числа названы, потому что предикат
`grep -E '^message (Create|Update)[A-Za-z]*Request'` по каталогу даёт 22, и читатель,
проверивший «17» им, решил бы, что документ ошибся.

**Это свойство контракта, и держит его APPLY-15b/APPLY-23, а не сквозная проба.** Сценария
«клиент прислал `applyState` в теле» здесь нет и быть не может: такой запрос уронил бы живой
гейт продукта `TestNewmanCollectionsSendNoUnknownRequestFields` — разбор в APPLY-15b.

`update_mask`, содержащая `apply_state`, отвергается как неизвестное поле существующей
дисциплиной маски: `apply_state` не входит ни в известный набор маски, ни в перечень
неизменяемых полей, поэтому получает generic-отказ, а не контракт-тон про неизменяемость, —
и это верно, потому что поле не «неизменяемое», а вовсе не входит в запрос.

### 3.4. Стоимость

Заполнение стоит **один стейтмент на страницу**, а не на строку: `PublicApplyStates`
принимает партию идентификаторов. Стоимость принадлежит запросу и не является функцией
популяции проекта. Порядок в списочном хендлере не меняется — проверка формы страницы
по-прежнему стоит до сужения по правам и до обращения к хранилищу; вопрос к проекции
задаётся **после** того, как страница отобрана и сужена, то есть по идентификаторам, которые
арендатор и так увидит.

---

## §4. Out-of-scope

Отложенное названо явно, чтобы «не сделано» не выглядело «забыто».

- **Компонент-исполнитель.** Приёмка описывает то, что видит арендатор; кто и как применяет
  намерение к сети — предмет отдельной работы. Отсюда же следует §6.
- **Показ состояния в консоли.** Поле появляется в контракте; страница ресурса его пока не
  рисует. Правило консоли «поле без источника не показывается» теперь выполняется в другую
  сторону — источник появился, и показ становится законным предметом следующей задачи.
- **Поверхность провайдера инфраструктуры.** Атрибут только для чтения на девяти ресурсах
  vpc — отдельное изменение со своей приёмкой и своими пробами модулей.
- **Сводка по проекту** («сколько объектов не применено») — см. предикат пересмотра D1.
- **Кросс-доменное распространение.** Шов датаплейна сегодня есть только у vpc; распространять
  форму поля на compute/nlb/storage до появления у них намерения — значит заводить поле без
  заполнителя, то есть ровно тот дефект, который ловит гейт пары.
- **Четыре мёртвых списочных ответа контракта** — `ListNetworkSubnetsResponse`,
  `ListNetworkSecurityGroupsResponse`, `ListNetworkRouteTablesResponse`,
  `ListAddressesBySubnetResponse`. Найдены переписью поверхностей этой приёмки (§3.3):
  объявлены, не возвращаются ни одним RPC, не упомянуты ни одним не-сгенерированным файлом
  Go. Это самостоятельная находка класса «мёртвый тип», а не часть состояния применения, и
  снимается она **своей** задачей: снятие сообщения из контракта — ломающее изменение со
  своей записью в перечне объявленных разрывов и своим порядком (потребители → словари →
  объявление). Смешивать её с этой работой нельзя — два предмета в одном изменении
  перестают быть прослеживаемыми по отдельности.

  **Предикат, по которому находка закрывается:** для каждого из четырёх имён
  `grep -rn "returns (<имя>)" proto/` даёт `0` и `grep -rl "<имя>" --include=*.go .` вне
  `pkg/api/` даёт `0` — либо имени нет в дереве вовсе. Контроль обязателен: тот же предикат
  на живом `ListNetworksResponse` обязан дать ненулевые числа, иначе «ноль» означает «предикат
  ничего не читает».

---

## §5. Сценарии

Идентификатор трассируется в имена проб. Колонка «где» — уровень, на котором сценарий
**конструируем**; обоснование выбора — §6.

### Стадия 1 — снятие мёртвых значений статуса интерфейса

**APPLY-01 · Статус интерфейса не претендует на датаплейн** — где: гейт по дескриптору

**Given** контракт `kacho.cloud.vpc.v1.NetworkInterface` собран из дерева
**When** проба обходит значения перечисления `NetworkInterface.Status`
**Then** множество имён равно `{STATUS_UNSPECIFIED, ACTIVE, AVAILABLE}`
**And** номера 1, 4, 5 и имена `PROVISIONING`, `FAILED`, `DELETING` объявлены `reserved`
**And** положительный контроль: та же проба видит `ACTIVE` — иначе «трёх значений нет» было
бы неотличимо от «дескриптор не прочитан»

---

**APPLY-02 · Снятое значение не выразимо и в базе** — где: интеграция (Postgres)

**Given** миграции применены до последней
**When** в `kacho_vpc.network_interfaces` предпринимается вставка строки со `status =
'PROVISIONING'`
**Then** база отвергает запись нарушением ограничения `network_interfaces_status_check`
**And** то же для `'FAILED'`, `'DELETING'` и `'STATUS_UNSPECIFIED'` — все четыре снятых
значения столбца, а не три: `'STATUS_UNSPECIFIED'` стоял в словаре с `0001` и производителя
не имеет (§1.3.1)
**And** та же вставка со `status = 'AVAILABLE'` проходит — положительный контроль на то, что
ограничение сужено, а не сломано
**And** вставка с явным `status = NULL` отвергается как нарушение `NOT NULL`, а **не** как
нарушение `network_interfaces_status_check` — дизъюнкт `status IS NULL` снят потому, что
разрешал невозможное, и проба обязана показать, что запрет остался у того ограничения,
которое его и держало

---

**APPLY-02b · Нераспознанный текст статуса роняет чтение, а не превращается в «не сказано»** — где: интеграция

**Given** ограничение `network_interfaces_status_check` снято на время пробы, и в строку
интерфейса записан текст вне словаря
**When** клиент вызывает `Get` этого интерфейса
**Then** gRPC **INTERNAL**, на публичном крае HTTP **500**
**And** текст ответа фиксированный и непрозрачный: ни значения из столбца, ни имени таблицы,
ни имени колонки; утверждается **сообщение**, а не только код
**And** положительный контроль: строка со `status = 'ACTIVE'` в том же прогоне читается
успешно и отдаёт `ACTIVE`

> Держит вторую половину D6. До стадии 1 обратный перевод возвращал `STATUS_UNSPECIFIED` на
> **любой** нераспознанный текст — корзину «прочее», которая превращала расхождение словарей
> в правдоподобное значение и переживала бы собственный предмет. Сузив словарь столбца до
> двух значений, стадия 1 отнимает у этой корзины последнее законное содержимое, поэтому
> молчание в ней становится чистым сокрытием.

---

**APPLY-03 · Обратное заполнение выводит значение из авторитета** — где: интеграция

**Given** до применения миграции в таблице лежат две строки со снятыми значениями: одна с
непустым `used_by_id`, другая с пустым
**When** миграция применяется
**Then** первая строка получает `ACTIVE`, вторая — `AVAILABLE`
**And** миграция печатает число переписанных строк
**And** на дереве без таких строк число равно нулю, и это печатается — «ноль переписано»
отличимо от «не смотрели»

---

**APPLY-04 · Живое значение статуса продолжает работать** — где: newman

**Given** арендатор создал интерфейс в своей подсети
**When** клиент вызывает `GET /vpc/v1/networkInterfaces/{id}`
**Then** HTTP **200** / gRPC **OK**
**And** `status` равен `AVAILABLE`

> Вторая половина — переход в `ACTIVE` — сквозной пробой vpc **не конструируется**:
> публичных глаголов привязки у интерфейса нет (сняты с контракта), привязку делает compute
> вызовом `InternalNetworkInterfaceService.Attach` на `:9091`. Поэтому она стоит отдельным
> сценарием ниже, а не дописана сюда «до кучи»: Given, которого в этой среде не создать,
> превратил бы кейс в утверждение, которому нечего вызвать.

---

**APPLY-04b · Переход статуса привязки сохранён** — где: интеграция (gRPC, `:9091`)

**Given** интерфейс создан и не привязан, его `status` равен `AVAILABLE`
**When** вызывается `InternalNetworkInterfaceService.Attach`
**Then** `status` становится `ACTIVE`
**And** после `Detach` он возвращается к `AVAILABLE`
**And** ни в одном из трёх состояний он не принимает снятых значений — положительный
контроль на то, что сужение перечисления не сломало живой переход

---

### Стадия 2 — состояние применения в публичном контракте

**APPLY-05 · Свежее намерение видно как «в работе»** — где: newman

**Given** арендатор создал сеть, операция завершилась без ошибки
**When** клиент вызывает `GET /vpc/v1/networks/{networkId}`
**Then** HTTP **200** / gRPC **OK**
**And** `applyState` не равен `null`
**And** `applyState.applied` равен `false`
**And** `applyState.reason` равен `APPLY_FAILURE_REASON_UNSPECIFIED`

> Это положительный контроль ко всем отрицаниям ниже: он доказывает, что поле доезжает до
> арендатора вообще. Без него «класса отказа не видно» было бы неотличимо от «поля нет».

---

**APPLY-06 · Подтверждённое применение видно арендатору** — где: интеграция

**Given** сеть создана и её намерение имеет действующую ревизию `R`
**And** исполнитель вызвал `InternalDataplaneService.ReportIntentApplied` (`:9091`) с
`resource_id = <networkId>`, `revision = R`, `outcome = APPLY_OUTCOME_APPLIED`
**When** читается публичная проекция сети
**Then** `apply_state` заполнен
**And** `apply_state.applied` равен `true`
**And** `apply_state.reason` равен `APPLY_FAILURE_REASON_UNSPECIFIED`

---

**APPLY-07 · Отказ исполнителя доходит до того, кого касается** — где: интеграция

**Given** сеть создана, действующая ревизия намерения `R`
**And** исполнитель отчитался `outcome = APPLY_OUTCOME_FAILED`,
`reason = APPLY_FAILURE_REASON_CAPACITY` по ревизии `R`
**When** читается публичная проекция сети
**Then** `apply_state.applied` равен `false`
**And** `apply_state.reason` равен `APPLY_FAILURE_REASON_CAPACITY`

**And** тот же сценарий повторён для каждого из **пяти** остальных классов словаря
(`CONFLICT`, `UNSUPPORTED`, `DEPENDENCY_NOT_READY`, `TRANSIENT`, `EXECUTOR_INTERNAL`) — иначе
«класс доезжает» было бы доказано на одном значении и распространено на пять непроверенных

---

**APPLY-08 · Устаревший УСПЕХ не выдаётся за состояние нового намерения** — где: интеграция

**Given** исполнитель подтвердил применение ревизии `R` (`applied = true` виден)
**When** арендатор меняет ресурс — `PATCH`, меняющий метку, — и намерение получает ревизию
`R+1`
**And** читается публичная проекция
**Then** `apply_state.applied` равен `false`
**And** `apply_state.reason` равен `APPLY_FAILURE_REASON_UNSPECIFIED`

---

**APPLY-09 · Устаревший ОТКАЗ не приписывается новому намерению** — где: интеграция

**Given** исполнитель отчитался отказом с классом `CONFLICT` по ревизии `R`
**When** арендатор меняет ресурс и намерение получает ревизию `R+1`
**And** читается публичная проекция
**Then** `apply_state.applied` равен `false`
**And** `apply_state.reason` равен `APPLY_FAILURE_REASON_UNSPECIFIED`, а **не** `CONFLICT`

> Тонкая половина правила, и она стоит отдельным сценарием намеренно: приписав старый класс
> новому намерению, платформа заставила бы арендатора чинить причину, которой у его правки
> нет. От APPLY-08 отличается тем, что там устаревшее утверждение доброе и его потеря
> безобидна, а здесь — злое и его перенос вреден.

---

**APPLY-10 · Публичная проекция несёт ровно два факта** — где: гейт по дескриптору

**Given** контракт `kacho.cloud.vpc.v1.ApplyState` собран из дерева
**When** проба обходит его поля
**Then** имена полей равны `["applied", "reason"]` в этом порядке
**And** проба падает и **называет добавленное поле**, если их становится три
**And** положительный контроль: та же проба видит `applied` — «третьего поля нет» отличимо от
«дескриптор не прочитан»

> Держит D5. Инфра-чувствительное приезжает в такие проекции по одному полю за раз, и каждое
> по отдельности выглядит безобидной подробностью: сначала ревизия «для отладки», потом время
> отчёта «чтобы понять, свежий ли», потом идентификатор попытки.

---

**APPLY-11 · Все семь ресурсов несут поле** — где: гейт по дескриптору + newman

**Given** контракты семи публичных сообщений vpc собраны из дерева
**When** проба обходит их поля
**Then** каждое несёт поле типа `ApplyState`
**And** newman-половина: `Get` каждого из семи созданных ресурсов отдаёт непустой `applyState`
— по кейсу на ресурс, а не один кейс на сети с распространением вывода на шесть
непроверенных

> Здесь стояла ещё клауза «`CidrGroup` поля **не** несёт». Она снята и заменена на APPLY-23,
> потому что была отрицанием **без производителя**: ни стадия 2, ни какое-либо изменение в
> её границах не добавляет поле в `CidrGroup`, поэтому клауза верна всегда и упасть не
> способна. Хуже того, выбор именно `CidrGroup` произволен — объявлений сообщений верхнего
> уровня в пакете **188**, и назвать одно значит промолчать об остальных 187. Отрицание, которое
> действительно может покраснеть, формулируется переписью: множество сообщений с полем
> **равно** семи, а не «включает семь и не включает одно названное».

---

**APPLY-12 · Список несёт то же состояние, что и одиночное чтение** — где: newman

**Given** в проекте созданы три сети
**When** клиент вызывает `GET /vpc/v1/networks?projectId=…`
**Then** HTTP **200** / gRPC **OK**
**And** у каждого элемента страницы `applyState` не равен `null`
**And** значение `applyState` каждого элемента **совпадает** со значением, которое отдаёт
`GET /vpc/v1/networks/{id}` того же ресурса

> Закрывает класс «одно чтение поле заполняет, другое нет»: список — дешёвый и поэтому
> частый путь, и клиент, обновляющий состояние из него, прочёл бы пустоту как утрату.
> Повторяется для **каждого** из семи списков — по одному кейсу на ресурс, а не один кейс на
> сети с распространением вывода на шесть непроверенных.
>
> Вложенных списков сети здесь нет намеренно: их RPC не существует (§3.3), и кейс на них был
> бы утверждением, которому нечего вызвать.

---

**APPLY-13 · «Утверждения нет» отличимо от «не применено»** — где: интеграция

**Given** ресурс существует, а его намерение снято (удаление в полёте)
**When** читается публичная проекция этого ресурса
**Then** чтение успешно
**And** `apply_state` **не заполнен** — на публичном крае это `"applyState": null`
**And** счётчик наблюдаемости «чтение без строки намерения» увеличивается на единицу

**And** отрицательная половина: у живого ресурса того же прогона поле заполнено — иначе
«не заполнено» было бы верно всегда и сценарий не различал бы ничего

---

**APPLY-14 · Значение вне словаря роняет чтение, а не превращается в «не применено»** — где: интеграция

**Given** в `kacho_vpc.dataplane_apply` запись обходным путём приведена к форме, которой
контракт не знает (ограничение снято на время пробы)
**When** клиент вызывает `Get` этого ресурса
**Then** gRPC **INTERNAL** — и, поскольку глагол публичный, на крае это HTTP **500**
**And** текст ответа — фиксированный и непрозрачный: ни текста драйвера, ни имени таблицы, ни
имени колонки; утверждается **сообщение**, а не только код, иначе рефактор вернул бы утечку
при зелёной пробе
**And** положительный контроль: строка годной формы в том же прогоне читается успешно

---

**APPLY-15 · Маска не принимает поле** — где: newman

**Given** арендатор владеет сетью
**When** клиент вызывает `PATCH /vpc/v1/networks/{id}` с телом `{"updateMask": "applyState"}`
**Then** HTTP **400** / gRPC **INVALID_ARGUMENT**
**And** отказ называет поле — путь маски приезжает в сервис как `apply_state` (protojson
переводит camelCase в имя поля контракта), поэтому в тексте отказа стоит `apply_state`, а не
`applyState`; утверждается **сообщение**, а не только код

> **Ключ `updateMask` — законный ключ запроса**, а его значение — строка маски, поэтому этот
> кейс проходит гейт тел (см. ниже). `UpdateNetworkRequest` привязан как `body: "*"`, и
> `update_mask` в нём есть; поле маски — `google.protobuf.FieldMask`, а well-known-типы гейт
> в спуск не берёт намеренно.

---

**APPLY-15b · Прислать состояние применения нечем** — где: гейт по дескриптору (см. APPLY-23)

**Given** контракты vpc собраны из дерева
**When** проба обходит все **22** `Create*Request`/`Update*Request` пакета
**Then** ни один из них не несёт поля, опознаваемого предикатом состояния применения
**And** положительный контроль тем же обходом: `labels` находится в каждом из **семи**
`Create*Request` ресурсов, `update_mask` — в каждом из **семи** `Update*Request`; «поля нет»
отличимо от «запросы не прочитаны»

> [!warning] Сквозной половины у этого утверждения нет — и её нельзя завести, не уронив
> живой гейт продукта
> Здесь стояло: «`PATCH` с телом, содержащим `applyState`, и пустой маской завершается
> **200**, а последующий `Get` показывает состояние, выведенное платформой». Такой
> newman-кейс **неисполним**, и это свойство дерева, а не придирка к формулировке.
>
> `gateway/internal/restmux/newman_body_contract_test.go`
> (`TestNewmanCollectionsSendNoUnknownRequestFields`) статически сверяет тело **каждого**
> запроса **всех** коллекций с message того RPC, который его обслуживает. Ключ `applyState`
> полем `UpdateNetworkRequest` не является (запросы плоские, §3.3), поэтому запрос попадёт в
> категорию **mis-addressed**, а у неё послабления нет **by construction**: поимённый
> перечень `absentRouteBodyWaiver` применяется только к другой категории — телу в маршрут,
> которого край не обслуживает. Здесь маршрут как раз обслуживается.
>
> Замер на `origin/main@5df7da76` (прогон, не чтение):
> `go test ./gateway/internal/restmux/ -run TestNewmanCollectionsSendNoUnknownRequestFields -v`
> → `scanned 85 collections, 8771 requests, 2776 with a JSON body; 0 mis-addressed, 26
> body-on-absent-route (26 waived by name, 0 not)`, `PASS`. То есть кейс стал бы **первой**
> находкой этой категории за всё время жизни гейта и уронил бы его.
>
> **Исходов было три, и выбран третий.** Ослабить гейт — снять единственную защиту от класса
> «край ответил 200, поле не применено», ради проверки того же класса на одном поле.
> Завести для mis-addressed поимённое послабление — то же самое, только с видом порядка.
> Перенести утверждение на **дескриптор** — и оно становится **сильнее**: покрывает все
> **22** запроса создания и обновления пакета, а не один `PATCH` сети, и держится без стенда.
>
> Потери утверждения при переносе нет: пустая маска «молча игнорирует неизменяемое из тела»
> — верно для полей, которые в теле **есть**; `apply_state` в теле запроса нет вовсе, и
> протокол отбрасывает неизвестный ключ ещё на разборе, до сервиса. Проверять «значение не
> сохранилось» там, где значение некуда положить, значит утверждать про путь, которого нет.

---

**APPLY-16 · Приём отчёта на внешнем крае недостижим** — где: гейт

**Given** край собран из дерева
**When** проба обходит зарегистрированные публичные маршруты и дескрипторы vpc
**Then** ни один публичный маршрут не ведёт в `InternalDataplaneService`
**And** ни один метод этого сервиса не несёт HTTP-аннотации
**And** положительный контроль: та же проба находит публичные маршруты сети и находит
HTTP-аннотации у публичных методов — «не найдено» отличимо от «таблица маршрутов и
дескрипторы не прочитаны»

> **Сквозной половины у этого сценария нет намеренно.** Запрос к пути, которого не существует,
> отвечает **404 всегда** — и когда метод не смонтирован, и когда его смонтировали по другому
> адресу. Такое утверждение зелено при любом состоянии дерева, то есть упасть не способно.
> Мониторить надо **таблицу маршрутов**, а не ответ на выдуманный адрес.

---

**APPLY-17 · Право писать отчёт не совпадает с правом его читать** — где: интеграция (gRPC, `:9091`)

**Given** внутренний слушатель поднят с обязательной проверкой прав
**When** принципал уровня чтения (`system_viewer`) вызывает `ReportIntentApplied`
**Then** gRPC **PERMISSION_DENIED** (HTTP-половины у пары нет — см. APPLY-18)
**And** тот же принципал успешно открывает поток намерения — положительный контроль: отказ
относится к глаголу, а не к принципалу вообще
**And** принципал уровня администратора (`system_admin`) отчёт записывает успешно

---

**APPLY-18 · Отчёт про невыданную ревизию отвергается** — где: интеграция

**Given** ресурс существует, его действующая ревизия `R`
**When** исполнитель отчитывается по ревизии `R + 100`
**Then** gRPC **FAILED_PRECONDITION**
**And** публичная проекция ресурса не меняется — `applied` остаётся `false`, класса нет

**And** отчёт по несуществующему объекту даёт gRPC **NOT_FOUND** — своя база, своя строка,
строки нет

**And** отчёт с исходом `FAILED` без класса причины даёт gRPC **INVALID_ARGUMENT**, и отказ
называет поле

> **HTTP-статус здесь не утверждается, и это не пропуск.** `InternalDataplaneService` не несёт
> HTTP-аннотаций и на краю не зарегистрирован (APPLY-16), поэтому статуса у этих отказов нет
> **ни одного**: производителя не существует. Приписать им 400 и 404 значило бы ждать исхода,
> которого край произвести не может, — то есть завести утверждение, неспособное покраснеть.
> Пара «HTTP + gRPC» обязательна там, где ответ идёт через край; у методов `:9091` вторая
> половина пары отсутствует by construction.

---

**APPLY-19 · Стоимость страницы принадлежит запросу** — где: интеграция

**Given** в проекте созданы пятьдесят сетей
**When** клиент запрашивает страницу `pageSize = 50`
**Then** число обращений к таблицам проекции за время запроса равно **одному**
**And** при `pageSize = 1` оно равно тому же одному — стоимость не является функцией размера
страницы построчно
**And** пустая страница обращения к проекции не делает вовсе

---

**APPLY-20 · Поток намерения поля не несёт** — где: интеграция

**Given** сеть создана и исполнитель отчитался об успешном применении
**When** исполнитель открывает поток намерения с нулевой позиции
**Then** тело сети в потоке приходит **без** заполненного `apply_state`
**And** остальные поля сети заполнены — положительный контроль: тело собрано, а не пусто

> Держит решение §3.3. Заполнив здесь, платформа вернула бы исполнителю его же отчёт и
> завела бы второй источник одного факта.

---

**APPLY-21 · Ответ операции поля не несёт, и это объявлено** — где: newman

**Given** арендатор создал сеть и опрашивает операцию до завершения
**When** клиент читает завершившуюся операцию
**Then** HTTP **200** / gRPC **OK**
**And** ресурс внутри `response` приходит с `applyState`, равным `null`
**And** `Get` того же ресурса сразу после этого отдаёт заполненный `applyState` —
положительный контроль: разница между поверхностями намеренная, а не поломка заполнителя

---

**APPLY-22 · Гейт пары «поле ↔ заполнитель» сошёлся** — где: гейт

**Given** обе половины изменения на месте
**When** исполняется `TestDataplaneApplyFieldAndItsFillerAppearTogether`
**Then** проба сообщает, что пара сошлась, и **называет** и поля, и вызывающих
**And** объём осмотренного напечатан: сколько полей в скольких сообщениях, сколько файлов
прод-кода разобрано и сколько вызовов в них найдено

**And** обе половинные инъекции доказаны отдельно: снятие вызовов заполнителя при
сохранённом поле роняет пробу с текстом про мёртвое поле; снятие поля при сохранённых
вызовах роняет её с текстом про чтение, которое никуда не едет

> **Чего этот гейт НЕ утверждает — и почему рядом стоит APPLY-23.** Он спрашивает про
> **существование пары**: есть ли поле хоть где-то среди семи сообщений ресурса и есть ли
> хоть один вызов заполнителя хоть где-то в не-тестовом дереве. Ни «на всех ли семи», ни «на
> тех ли поверхностях» он не отвечает и отвечать не должен — предикат поля намеренно широк,
> а перечень сообщений в нём **выписан**, а не выведен. Полнота и отсутствие лишнего —
> предмет APPLY-23 и построчных утверждений §3.3.

---

**APPLY-23 · Поле живёт ровно на семи сообщениях и больше нигде** — где: гейт по дескриптору

**Given** дескрипторы пакета `kacho.cloud.vpc.v1` собраны из дерева
**When** проба обходит **все** сообщения пакета, включая вложенные, и собирает те, что несут
поле по предикату состояния применения
**Then** множество найденных сообщений **равно** семи ресурсным (`Network`, `Subnet`,
`NetworkInterface`, `SecurityGroup`, `RouteTable`, `Gateway`, `Address`)
**And** проба падает и **называет имя** и лишнего сообщения, и недостающего — «не равно»
бесполезно без того, какой стороны не хватает
**And** объём осмотренного напечатан: сколько сообщений обойдено (объявлений верхнего уровня
в пакете — **188**, предикат
`git ls-tree -r --name-only origin/main proto/kacho/cloud/vpc/v1/ | xargs -I{} git show origin/main:{} | grep -cE '^message [A-Za-z]'`)
**And** зеркальный контроль: тот же обход с предикатом `project_id` возвращает **непустое**
множество — «ноль лишних» отличимо от «предикат ни разу не применялся»

> **Предикат берётся ТОТ ЖЕ, что у гейта пары** (`looksLikeApplyState` из
> `dataplane_apply_pairing_test.go`), импортом, а не копией. Два предиката об одном предмете
> разошлись бы молча и разошлись бы там, где расхождение не видно: поле, названное так, что
> его видит один и не видит другой, оставило бы зелёными оба.
>
> Этот сценарий закрывает сразу три вещи, и все три раньше держались словом: половину «ни
> одна лишняя» на уровне контракта; отрицание про `CidrGroup` из APPLY-11 (теперь оно
> утверждается про **все** 187 остальных сообщений, а не про одно названное); и APPLY-15b —
> запросы создания и обновления входят в обход и обязаны оказаться вне множества.

---

**APPLY-24 · Внутренняя проекция сети поля не несёт** — где: интеграция (gRPC, `:9091`)

**Given** сеть создана и исполнитель отчитался об успешном применении — то есть заполнителю
**есть что** положить, и «пусто» не может получиться само
**When** вызывается `InternalNetworkService.GetNetwork`
**Then** `network.apply_state` **не заполнен**
**And** `network.id`, `network.project_id` и `vrf_id` заполнены — положительный контроль:
ответ собран, а не пуст
**And** публичный `Get` той же сети в том же прогоне отдаёт `apply_state` заполненным —
разница между поверхностями намеренная, а не поломка заполнителя

---

**APPLY-25 · Ответы привязки и отвязки поля не несут** — где: интеграция (gRPC, `:9091`)

**Given** интерфейс создан, исполнитель отчитался об успешном применении его намерения
**When** вызывается `InternalNetworkInterfaceService.Attach`
**Then** `network_interface.apply_state` в ответе **не заполнен**
**And** `network_interface.status` равен `ACTIVE`, `used_by_id` непуст — положительный
контроль: ответ несёт исход привязки, ради которого и вызывался
**And** то же для `Detach`: `apply_state` не заполнен, `status` равен `AVAILABLE`
**And** публичный `Get` того же интерфейса между этими вызовами отдаёт `apply_state`
заполненным

> APPLY-24 и APPLY-25 заведены потому, что три поверхности из девятнадцати не имели ни
> одного падающего утверждения (§3.3). Заполнить лишнюю поверхность — ошибка **тихая**: она
> ничего не роняет, потому что заполнение всегда выглядит как «стало больше данных». Гейт
> пары её не видит by construction — ему довольно одного вызова заполнителя где угодно.

---

## §6. Где какой сценарий проверяется — и почему выбора нет

Кажется естественным проверить всё сквозной пробой через край. **Для сценариев, чей Given —
отчёт исполнителя, это невозможно, и это свойство дерева, а не удобства:**

1. компонента-исполнителя не существует (§1.2), поэтому отчёт производится только вызовом
   `ReportIntentApplied`;
2. `InternalDataplaneService` не несёт **ни одной** HTTP-аннотации и не зарегистрирован на
   крае — предикат в APPLY-16; следовательно через публичный REST этот вызов недостижим;
3. глагол требует `system_admin` @ `cluster` — арендатор его не имеет и иметь не должен.

Поэтому: **APPLY-02, 02b, 03, 04b, 06…09, 13, 14, 17…20, 24, 25 — интеграционные** (реальный
Postgres в контейнере, реальные use-case и репозиторий, реальный внутренний слушатель там,
где проверяются права). **APPLY-04, 05, 12, 15, 21 и newman-половина 11 — сквозные** через
край: их Given арендатор строит сам. **APPLY-01, 10, 11, 15b, 16, 22, 23 — гейты** по
дескрипторам и дереву, потому что их предмет — свойство контракта и кода, а не поведение
одного прогона.

**Ещё один уровень выбирается не удобством, а живым гейтом продукта.** APPLY-15b стоит на
дескрипторе, потому что его сквозная форма — запрос с ключом `applyState` в теле — попадает в
категорию, у которой в `TestNewmanCollectionsSendNoUnknownRequestFields` послабления нет by
construction, и стала бы первой такой находкой на 8771 запросе. Разбор — в самом APPLY-15b;
здесь это названо, чтобы «почему не newman» не пришлось выяснять заново.

**Пара «HTTP-статус + gRPC-код» утверждается везде, где ответ проходит через край**, и только
там. Отображение кода в статус задаётся библиотекой шлюза — край собран без собственного
обработчика ошибок, — поэтому множество производимых статусов конечно и от сервиса не
зависит. У методов внутреннего слушателя второй половины пары нет вовсе (APPLY-18).

Это не сокращение объёма: каждый сценарий проверяется там, где его предусловие
**конструируемо**. Сценарий, требующий состояния, которого в среде не создать, — негодный
сценарий, а не повод ослабить утверждение.

**Чего здесь нет и почему это записано, а не умолчано:** сквозной пробы «арендатор увидел
отказ исполнителя» не существует до появления исполнителя либо до отдельного шага прогона,
создающего это условие вызовом на внутренний слушатель. Это открытый долг с предикатом
снятия — появление любого из двух, — а не зелёная суита.

---

## §7. DoD

### Стадия 1 — снятие мёртвых значений статуса

- [ ] `NetworkInterface.Status` несёт три значения; номера 1, 4, 5 и их имена `reserved`
- [ ] комментарий поля описывает **привязку**, а не программирование сети
- [ ] ветви снятых значений убраны из домена и из обоих переводов имени в значение и обратно
- [ ] обратный перевод (`NIStatusFromName`) **не имеет корзины «прочее»**: нераспознанный
      текст столбца — ошибка, а не `STATUS_UNSPECIFIED`; чтение отвечает фиксированным
      непрозрачным `INTERNAL` (утверждается сообщение — APPLY-02b)
- [ ] миграция сужает `network_interfaces_status_check` до `status IN ('ACTIVE','AVAILABLE')`
      — сняты **четыре** значения словаря столбца и мёртвый дизъюнкт `status IS NULL`;
      направление обратного заполнения объявлено в самой миграции и выводится из `used_by_id`
      единым правилом на все четыре; число переписанных строк печатается всегда
- [ ] записи в `proto/declared-breaks.yaml` соответствуют находкам `buf breaking` один в
      один — ни находки без записи, ни записи без находки
- [ ] `buf lint` и генерация зелёные, дифф генерации содержит только затронутый контракт
- [ ] сценарии APPLY-01, 02, 02b, 03, 04, 04b красные до изменения, зелёные после

### Стадия 2 — поле состояния применения

- [ ] `apply_state.proto` заведён; `ApplyState` и `ApplyFailureReason` объявлены в нём;
      внутренний контракт импортирует, полное имя типа не изменилось
- [ ] поле добавлено в семь публичных сообщений с номерами из §3.1; имя содержит `apply`
- [ ] **множество сообщений с полем равно семи** — APPLY-23 зелёный, доказан инъекцией в обе
      стороны: поле, добавленное восьмому сообщению, и поле, снятое у одного из семи, роняют
      пробу с именем виновника. Это и есть «ни одна лишняя» на уровне контракта
- [ ] **у каждой из 19 поверхностей §3.3 в колонке «утверждение» стоит идентификатор
      сценария**, и этот сценарий существует. Проверяется счётом: 14 заполняющих + 5
      незаполняющих = 19 строк, 19 идентификаторов; строка без идентификатора и идентификатор
      без строки одинаково видны. Прежняя формулировка («ни одна не пропущена и ни одна
      лишняя») была словом: три поверхности из девятнадцати не имели утверждения вовсе
- [ ] заполнение провязано на все **14** живых заполняющих поверхностей — семь `Get`
      (APPLY-11) и семь списков (APPLY-12)
- [ ] пять незаполняющих поверхностей несут это в комментарии своего поля и покрыты
      APPLY-20, 21, 24, 25
- [ ] четырём мёртвым списочным ответам §3.3 заполнитель **не** провязан — вызвать их нечем,
      и вызов туда был бы кодом без исполнителя
- [ ] стоимость — один стейтмент на страницу; порядок «форма → права → хранилище» не изменён
- [ ] `TestDataplaneApplyFieldAndItsFillerAppearTogether` сообщает, что пара сошлась;
      обе половинные инъекции проверены
- [ ] предикат поля у APPLY-23 и у гейта пары — **один символ**, взятый импортом; копии нет
      (предикат: `grep -rn "func looksLikeApplyState" services/vpc/` даёт ровно одно
      объявление)
- [ ] APPLY-15b зелёный: ни один из **22** `Create*Request`/`Update*Request` пакета поля не
      несёт, положительный контроль по `labels`/`update_mask` сработал
- [ ] `TestPublicProjectionCarriesExactlyTwoFacts` и его зеркало по дескриптору `ApplyState`
      зелёные; каждое доказано инъекцией третьего поля
- [ ] интеграционные пробы против настоящей базы: APPLY-06…09, 13, 14, 17…20, 24, 25
- [ ] newman: APPLY-05, 12, 15, 21 и половина 11; кейсы прошли `validate-cases.py`,
      внесены в `CASES-INDEX.md`, имена несут `{{runId}}`, за собой убирают
- [ ] `go test ./gateway/internal/restmux/ -run TestNewmanCollectionsSendNoUnknownRequestFields`
      остаётся зелёным и печатает **0 mis-addressed**: ни один заведённый кейс не шлёт краю
      ключа, которого RPC не читает
- [ ] каждая проба написана **до** кода и показана падающей по нужной причине
- [ ] `go test ./... -race`, `golangci-lint run`, `buf lint`, генерация, сквозной набор vpc —
      зелёные; вердикт прочитан по числам, а не по слову
- [ ] trail задачи `obsidian/kacho/KAC/issue-296.md` обновлён; записка ресурса и записка шва
      называют новую поверхность

### Общее для обеих стадий

- [ ] `docs/` сервиса vpc описывает поле: три состояния, словарь классов, перечень
      заполняющих и незаполняющих поверхностей
- [ ] ни один артефакт изменения не содержит сборки «координата + условие, при котором защита
      не действовала + следствие для постороннего»: оба репозитория публичны
