# Sub-phase XC-9 (дисциплина выдачи прав группе) — Acceptance

> **Статус: APPROVED** — 2026-08-10, `acceptance-reviewer` (ban #1), ревизия проверки
> `92ca6af9d` / воркспейс `ce4379e`. **На чём держится:** повторным **исполнением** проверены
> закрытие всех шести пунктов вердикта и живость плана — каждая координата колонки
> «приземлено» §4 резолвится в дереве, каждое воспроизводимое число сошлось (99/99 на трёх
> ревизиях с обоими зеркалами · 23/2/31 с зеркалом 21 и контролем на несуществующем токене ·
> 4+14+5=23 и 9 многоэлементных литералов из 11 · 21 имя метрики, из них 0 про принципала,
> при контроле в обе стороны и сверке 21 имени с 21 конструктором · 1 файл называет величину,
> вне `*.go` — 0 · 9+2+4+1=16 при 16 заголовках сценариев), а конструируемость XC-9-14
> перепроверена по схеме и коду (`CreateGroupRequest` поля членов не несёт; ни один
> структурный гейт создания привязки не требует непустоты группы; «пустой список субъектов» —
> отдельный, действующий отказ). Разбор по кругам — §10.4 (шесть пунктов) и §10.5 (этот круг).
> **Что APPROVED санкционирует — §0.2 и колонка «ОСТАЛОСЬ» §4, не документ целиком.**
> Предыдущие круги: ревью 2026-08-10 — **CHANGES REQUESTED**, правки внесены тем же днём;
> закрыто **6 из 6** (§10.2 п. 1-4 и §10.3 п. 5-6), возвращённых нет, поэтому §1.1-§1.4,
> §2.1, §3.2 и §3.3 не тронуты.
> **Сверх вердикта закрыто этим же заходом** (не спрашивалось, класс тот же): §5.0 — карта
> производителей, из **16** сценариев **9** уже имеют пробу в дереве, ещё **2** — наполовину,
> поэтому буквальное исполнение §5 завело бы дубли; §1.6 — выдача написана в наших источниках
> **тремя** формами, и гейт Ф3, знающий одну, прочитал бы 4 источника из 23; §8 пополнен
> критериями 12-14.
> **Что изменилось в объёме работ:** Ф2 **закрыта целиком** (оба адреса приземлены), Ф1
> приземлена **наполовину** (кортежи и объекты меряются; принципалы — нет, показания не сняты),
> Ф3 — перепись выполнена, перевод **ноль** по существу, остаётся **гейт**. APPROVED ставит
> `acceptance-reviewer` и никто другой (ban #1).
> **Даты:** написан — 2026-08-09 · ревью и правка — 2026-08-10.
> **Эпик/тикет:** KAC-XC-9 (cross-cutting, redesign-2026) — номер завести до старта остатка Ф1.
> **Монорепо:** `project/kacho` (`github.com/PRO-Robotech/kacho`). **База сверки документа —
> `17def09eb`**; всё, что помечено «приземлено», и все числа, названные при правке, — с
> **`92ca6af9d`** (ствол после влития 87 коммитов). Две ревизии названы раздельно намеренно:
> база старше приземления прибора на 49 минут, и именно этот разрыв породил пункт 1 вердикта.
> **Род документа:** **ПРИЁМКА, САНКЦИОНИРУЮЩАЯ РАБОТУ.** В отличие от XC-6 (канон-и-измерение)
> этот документ несёт сценарии Given-When-Then и после APPROVED разрешает кодировать — §0.2.
> **Источник решения:** сравнительный замер форм модели прав — **XC-8**, форма **B (группа)**,
> рекомендация «брать безусловно» (XC-8 §6).
> **Нормативка (не дублируется в тело — ссылки по названию раздела):**
> - `.claude/rules/data-integrity.md` — §«Authz-материализация owner-доступа» (в т.ч. B14 —
>   `Group#member` как outbox-emit + eventual consistency), §Outbox-drainer concurrency
> - `.claude/rules/security.md` — §«Авторизация живёт в МОДЕЛИ, а не в самодельных проверках»,
>   §«Три уровня супер-доступа», §Hardening-инварианты п.8 (мягкий проход обязан нести счётчик)
> - `.claude/rules/testing.md` — §«Гейт на класс», §E2E, §«Regression-lock на уровне обсервабла»
> - `.claude/rules/api-conventions.md` — §«Принято-и-проигнорировано», update_mask discipline

---

## §0 Предмет

### §0.1 Что решено замером и почему это не миграция

XC-8 сравнил пять форм модели прав на одном стенде и одних данных. Форма **B — выдача
группе** — единственная, у которой выигрыш **не требует изменения модели**: отношение
членства уже принято **каждым глаголом каждого типа** (перепись — §1.1), поэтому «выдать
роль группе» — сегодняшняя, работающая, покрытая пробами возможность продукта.

Значит предмет XC-9 — **не** новая конструкция и **не** миграция, а **дисциплина**: право
выдаётся **группе**, люди добавляются **в группу**. Работа состоит из трёх вещей, и ни одна
из них не меняет ни контракта, ни схемы БД: **прибор**, которым дисциплину видно; **норма**,
записанная ровно в одном месте; **свои посевы и фикстуры**, приведённые к ней, с гейтом,
который не даёт следующему их откатить.

### §0.2 Что APPROVED этого документа разрешает — и чего не разрешает

**Разрешает:** кодировать **остаток** фаз §4 по сценариям §5, читая их вместе с картой
производителей §5.0, — строгим TDD (падающий тест до кода, пара RED→GREEN с фактическим
выводом — ban #12), где красное относится к **дельте**, а не к тому, что дерево уже держит.
Каждая фаза **production-complete в своих границах** (ban #14): отложенное вынесено в
`Out-of-scope` следующей фазы явно. На день правки остаток — вторая величина прибора и
показания на стенде (Ф1) плюс гейт по дереву (Ф3); Ф2 закрыта.

**Не разрешает:**

- **менять модель прав.** Ни нового типа, ни нового отношения, ни правки существующего. Форма
  B тем и выбрана, что модель ей не нужна; всякая правка модели мятит её идентификатор и
  требует переката — это форма **D**, и она решается **замером фактических N** (XC-8 §6),
  а не этой приёмкой;
- **менять контракт `AccessBinding`.** Потолок «1..32 субъекта на привязку» — опубликованное
  поведение; дисциплина не понижает его и не вводит нового отказа тенанту (§3.2);
- **переводить чужие данные.** Существующие привязки не переписываются: дисциплина действует
  **вперёд**, на то, что выдаётся впредь, и на **наши собственные** посевы;
- **закрывать вопрос формы D.** Он остаётся открытым до чисел прибора Ф1 — и именно поэтому
  прибор стоит первым, а не последним. Половина этих чисел уже есть (кортежи и объекты
  меряются, §4); вторая — число перечисленных принципалов — и есть то, чего для решения по D
  не хватает, поэтому остаток Ф1 не откладывается за Ф3.

---

## §1 Что уже верно в дереве — измерено, а не предположено

Каждое число сопровождается предикатом со **своим ограничением**: что он читает, какова
единица счёта, какую структуру обходит целиком.

### 1.1 Отношение членства принято КАЖДЫМ глаголом КАЖДОГО типа — 99 из 99

Предикат (единица счёта — **объявление глагола** в каноническом тексте модели; обходится файл
целиком, комментарии в счёт не идут, потому что искомое — тело объявления):

```
awk '/^type /{t=$2} /define v_/{n++; if ($0 ~ /group#member/) g++} END{print n, g}' \
    proto/kacho/cloud/iam/v1/fga_model.fga
```

→ **99 объявлений, 99 с `group#member`**. Разложение: **24** типа несут по четыре глагола
чтения/списка/правки/удаления (96), плюс три особых (`v_create`, `v_addtargets`,
`v_removetargets`). Типов в модели **30**; шесть глаголов не несут by construction —
структурные (у них нет тенантской поверхности).

**Зеркальная проверка** (без неё «99 из 99» неотличимо от сломанного поиска): тот же обход,
считающий объявления **без** `group#member`, даёт **0**; тот же обход по подстроке, которой в
файле нет, даёт `0 0` — то есть счётчик не «залипает» на положительном ответе.

**Что из этого следует:** «выдать роль группе» **уже** выразимо на любом ресурсе любого
домена. Обратное («сначала расширить модель») было бы работой без предмета.

### 1.2 Группа-субъект даёт ОДИН кортеж на объект × глагол, независимо от числа людей

Субъект привязки форматируется в единственном месте — `services/iam/internal/domain/subject.go`
(`FGASubjectRef`), и оно же обслуживает построитель кортежей выдачи и реконсайлер. Группа
превращается в **userset** `group:<id>#member`; пользователь и служебная учётка — в конкретного
принципала. Godoc этой функции прямо требует **байтовой симметрии** двух её потребителей: иначе
выдача и отзыв члена целились бы в разные записи, и отзыв оставлял бы стоячий доступ.

**Следствие для арифметики XC-8:** множитель числа субъектов сворачивается к единице **на
стороне выдачи** — люди перестают быть множителем и становятся строками членства.

### 1.3 Привязка несёт 1..32 субъекта; членство меняется отдельным RPC

- `domain.MaxSubjectsPerBinding = 32`, отказ вне диапазона — `INVALID_ARGUMENT` с контрактным
  текстом. На чтении сервер заполняет **и** список субъектов, **и** легаси-пару (одиночный
  субъект) — это уже действующая совместимость, XC-9 её не трогает.
- Членство меняется `AddMember` / `RemoveMember`. Право на изменение решает **модель**: край
  проверяет глагол правки на объекте группы. Самодельной проверки в сервисе нет — прежняя была
  снята по `security.md` §«Авторизация живёт в МОДЕЛИ».
- Оба пути **симметричны**: намерение на запись и намерение на удаление кортежа членства
  кладутся в **той же** writer-транзакции, что и строка членства, и применяются дренажом
  at-least-once. Откат транзакции снимает и строку, и намерение — осиротевшего не остаётся.

### 1.4 Аудит «кто на самом деле имеет доступ» от группы не страдает

`ExpandAccess` разворачивает userset в **конкретных** принципалов и намеренно не возвращает
сам userset как принципала. То есть ответ на вопрос «кто получил это право» после перехода на
группы остаётся таким же, каким был при перечислении людей, — и это надо не «сохранить», а
**зафиксировать пробой** (§5, XC-9-08), потому что именно этот ответ обычно и теряют при
введении косвенности.

### 1.5 Как дисциплина применяется в НАШИХ посевах сегодня

Единица счёта — **декларативный источник** (генерируемые коллекции не считаются: они выход, а
не вход). Предикат — выписан целиком, чтобы его не пришлось угадывать:

```
git grep -l accessBindings \
  | grep -vE '\.go$|docs-site|ui-future|\.mdx$|\.proto$|(^|/)docs/|tests/newman/collections/'
```

→ **23**. Ограничение названо вместе с числом: он ловит **упоминание** пути выдачи в источнике,
а не число созданных привязок, поэтому годится как перепись мест и **не годится** как счёт
выдач. Каталог `docs/` исключается **на любой глубине** — это и есть та часть, которую первая
редакция оставила на догадку (врезка ниже).

| Что | Число | Чем получено |
|---|---:|---|
| декларативных источников, создающих привязки | **23** = 17 кейсов e2e iam + 2 вспомогательных скрипта их набора + 2 скрипта проверки посадки + 2 скрипта посева | предикат выше |
| из них применяют субъект-группу | **2** — и оба там, где **предметом пробы** является сама группа или равенство каналов выдачи | `grep -l SUBJECT_TYPE_GROUP` по этим 23; зеркало `grep -L` → **21**, сумма сходится; контроль на несуществующем токене → **0** |
| наибольшее число упоминаний выдачи в одном источнике | **31** | `grep -c accessBindings` по тем же 23, максимум |

> [!note] Первая редакция называла здесь «21 и 1» — числа получены до того, как предикат
> дописали до конца
> Разница не в дереве, а в **ограничении**: первый заход отбрасывал ещё и вспомогательные
> скрипты набора проб, не сказав об этом, и считал группу по одному написанию из двух.
> Оставляю разбор, потому что он и есть иллюстрация к §8 п.1: число, у которого не выписано,
> что предикат **не** читает, воспроизводится не тем, кто его перепроверяет, а тем, кто
> угадает то же самое умолчание.

> [!warning] И ровно это здесь повторилось: «прозы `docs/`» читалось как каталог верхнего уровня
> Буквальное прочтение (`^docs/`) даёт **26**, а не 23; лишние три — страницы компонентов под
> `services/iam/docs/components/`, то есть та же проза, но не в корне. Числа `23 / 2 / 31`
> **не менялись** — они и получены исключением прозы на любой глубине; менялась только запись
> ограничения, которая раньше требовала угадать глубину. Оставляю оба прочтения названными,
> потому что документ уже сделал себе это замечание врезкой выше и всё равно наступил на него
> вторично: пока предикат пересказан словами, он воспроизводится не тем, кто проверяет, а тем,
> кто угадает умолчание. Теперь он выписан командой целиком.

**Что это число значит и чего НЕ значит.** Оно **не** означает «двадцать мест нарушают
дисциплину»: выдача одному актору, чей состав никогда не меняется, группой не улучшается —
это чистая косвенность. Оно означает ровно одно: **предмет Ф3 надо измерить, а не
предположить**, и §4 ставит это условием, а не пожеланием.

### 1.6 Выдача написана в НАШИХ источниках тремя разными формами — это предпосылка гейта Ф3

Измерено на `92ca6af9d`; единица счёта — те же 23 источника §1.5; разбиение **исчерпывающее**
(4 + 14 + 5 = 23), и это его зеркальная проверка:

| Форма записи выдачи в источнике | Источников |
|---|---:|
| литерал `"subjects": [ … ]` **с более чем одним элементом** (9 таких литералов из 11 всего) | **4** |
| легаси-пара `subjectType`/`subjectId` **или** `subjects` из переменной | **14** |
| выдачи не авторствует вовсе (генератор набора, читатели страниц, проверки посадки) | **5** |

Предикат — разбор литерала `"subjects"\s*:\s*\[…\]` и наличие `"subjectType"` регулярным
выражением по тексту источника; ограничение названо: он читает **написание**, а не исход
запроса, поэтому годится как перепись форм и **не годится** как счёт выданных прав.

**Зачем это здесь, а не в §4.** Гейт XC-9-12, распознающий только литерал `subjects[]`,
прочитал бы **4 источника из 23** и напечатал «ноль находок» — не потому, что дерево чисто, а
потому, что четырнадцать мест написаны другой формой того же действия. Это тот самый класс
«проверка с нулём находок, не читающая целый вид предмета», и он назван **до** написания
гейта, а не после. Отсюда же требование XC-9-12 печатать объём осмотренного: «прочитано 23 из
23» и «прочитано 4 из 23» дают одинаковый вердикт и разный смысл.

Пятая строка разбиения несёт отдельную ловушку: среди этих источников есть **синтетические
фикстуры внутри инъекции чужого гейта** (`deploy/scripts/assert-teardown-frees-parent.py`
строит тело `POST /iam/v1/accessBindings` прямо в пробе). Гейт Ф3 обязан не считать их
находками — и это ровно тот законный близнец, на котором он должен молчать (XC-9-13).

---

## §2 Дисциплина — формулировка и её единственный адрес

### 2.1 Правило

> **Право, которое получает больше одного принципала ИЛИ состав получателей которого может
> измениться, выдаётся ГРУППЕ. Люди и служебные учётки добавляются в группу.**
>
> Перечисление принципалов в самой привязке остаётся законным ровно для одного случая:
> получатель **один** и меняться не будет — например, служебная учётка модуля, чья личность
> и есть предмет выдачи.

**Порог назван предикатом, а не вкусом.** Выигрыш формы B — в свёртке множителя субъектов и,
что важнее, в **цене изменения состава**: убрать одного из группы — одна запись; убрать
одного из перечисления — снятие кортежей по всем объектам роли. Поэтому дисциплина
срабатывает не от «многих», а от **двух** и от **изменчивости**: право «дежурного» на одного
человека, который завтра сменится, уже подпадает.

### 2.2 Где это записано — два адреса, два РАЗНЫХ предмета

Два места об одном предмете — запрещены; два места о **разных** предметах — обязательны, и
разница здесь не формальная:

| Адрес | Предмет | Кому адресовано | Состояние на `92ca6af9d` / воркспейс `ce4379e` |
|---|---|---|---|
| docs-site **iam**, страницы `AccessBinding` и `Group` | **как пользоваться платформой**: выдавать группе, менять состав членством | арендатору, у которого нет нашего репозитория | **приземлено** (`ed5d7ec2c`): раздел «Кому выдавать — группе или перечислением», страница `Group` на него **ссылается** и не пересказывает |
| `.claude/rules/data-integrity.md` §B18 | **как мы пишем свои посевы, фикстуры и приёмки** | инженеру, который заводит следующий посев | **приземлено и переписано этим заходом** — см. врезку ниже |

Правило-инженерное **ссылается** на страницу продукта по названию и **не пересказывает** её;
страница продукта не воспроизводит устройство материализации. Признак расхождения механический:
правка одного делает формулировку другого устаревшей молча — если он срабатывает, значит
предмет всё-таки один и одно из мест лишнее.

> [!warning] Признак сработал на самом этом документе — второй адрес был вторым ИЗЛОЖЕНИЕМ
> Первая редакция B18 воспроизводила отсюда **шесть** элементов: правило §2.1, предикат §1.1
> с зеркалом, пять чисел §3.1, перемещение поверхности выдачи §3.2, требование к вырожденному
> составу XC-9-14 и границы охвата §0.2 — и заканчивалась фразой «здесь она не
> пересказывается». Механический признак, названный тремя абзацами выше, сработал в первый же
> раз: уточнение «10× относится к кортежам, 7.4× — к байтам» пришлось вносить **в три
> документа одним заходом**, а вместе с ним в нормативку уехала и ревизия замера, на которой
> харнесса нет вовсе (§3.1).
>
> **Исход:** B18 сведён к норме, предикату её применимости и двум адресам — приёмке (числа,
> цена, сценарии) и странице продукта (арендатору). Числа замера, разбор цены, поверхность
> выдачи и вырожденный состав остаются **здесь**, в одном месте, где их и правят. Правило
> `@import`-ится в каждую сессию, поэтому цена лишнего изложения там выше, чем где-либо ещё:
> неверная координата оттуда читается чаще любой другой в корпусе.
>
> Пункт был **возвращён** приёмкой XC-8 (её §10, вердикт п.2: «правит не этот документ, а
> `data-integrity.md` §B18, который в этой же волне переписывается заходом XC-9») — то есть у
> него был ровно один исполнитель, и это заход XC-9. Если бы обе приёмки сослались друг на
> друга, правило осталось бы неверным при двух «закрытых» замечаниях.

---

## §3 Цена дисциплины — названа полностью, включая ту, которую XC-8 не мерил

Форма, у которой названа только выигрышная сторона, — не решение, а реклама.

### 3.1 Цена на чтении — измерена

Числа **не из дерева**; провенанс — XC-8 (владелец замера, здесь только заимствование):
харнесс `tools/authzformbench/` на ревизии **`11f664c20`**, посадка — OpenFGA над Postgres в
контейнерах, один узел, `N=1000, S=10, M=4`, p50/p95, прогрев отброшен, 120 ячеек, 0
не-измеренных. Версии и команда воспроизведения — в шапке XC-8; здесь они не дублируются по
той же причине, по какой сведён B18 (§2.2).

> [!warning] Здесь стояла ревизия `ebedae536` — на ней харнесса нет вовсе
> Предикат: `git ls-tree -r ebedae536 -- tools/authzformbench/ | wc -l` → **0**; каталог
> появляется в `11f664c20` (**10** файлов) и к `17def09eb` несёт **11**. То есть критерий §8
> п.2 («прогоном харнесса на названной ревизии») был **неисполним буквально**: назван коммит,
> на котором прогонять нечем.
>
> **Числа таблицы не тронуты** — правится координата, а не измерение. Они и не могли быть
> сняты на `ebedae536`: измеряющего кода там нет, а значит ложной была именно ссылка. Ту же
> ложную координату несла нормативка (§2.2) — она снята тем же заходом; в документе-владельце
> XC-8 её исправил свой заход, и обе шапки теперь называют `11f664c20`.
>
> **Отличие от подгонки задним числом:** ревизия здесь — не результат замера, а адрес
> инструмента, и адрес был проверяемо невозможен. Величины, полученные прогоном, остаются как
> есть; если XC-8 когда-нибудь уточнит их повторным прогоном — таблица следует за
> документом-владельцем, а не правится здесь.

> [!note] Повторный прогон ревьюера XC-8 дал разброс — числа названы обеими сторонами
> XC-8 §9.1: независимый прогон воспроизвёл объём **точно** (A `40000` кортежей / `5968751` Б,
> B `4010` / `814121` Б), а два отношения дал иными — полная страница **1.40×** против 1.37× и
> отзыв **170×** против 171×. Отношения в таблице ниже — арифметика на числах §4 XC-8
> (212→290 = 1.37; 617/3.6 = 171), и они верны для той таблицы; расхождение — разброс между
> прогонами, а не ошибка одного из них. Названо здесь, чтобы «1.37×» из этого документа и
> «1.40×» из чужого отчёта не выглядели противоречием, за которым погонятся как за находкой.

| Сторона | A (перечисление) | B (группа) | отношение |
|---|---:|---:|---|
| выдача привязки | 8740 мс | 1067 мс | быстрее **8×** |
| отзыв одного субъекта | 617 мс | 3.6 мс | быстрее **171×** |
| переразметка ста объектов | 836 мс | 92 мс | быстрее **9×** |
| одна партия чтения | 26.4 мс | 33.3 мс | медленнее **1.26×** |
| полная страница | 212 мс | 290 мс | медленнее **1.37×** |
| кортежей | 40 000 | 4 010 | меньше **10×** |
| логических байт | 5.97 МБ | 0.81 МБ | меньше **7.4×** |

> [!note] Уточнение к формулировке XC-8 §6 — «объём в 10 раз меньше» верно для КОРТЕЖЕЙ, не для байт
> Десятикратно сокращается **число кортежей** (40 000 → 4 010); логический объём строк падает
> в **7.4** раза (5.97 → 0.81 МБ). Обе величины есть в таблице §4 замера, и обе верны; в
> рекомендации они слились в одно число. Разница не косметическая: емкостное планирование
> считают байтами, а стоимость обхода — кортежами, и подстановка одного вместо другого даёт
> ошибку в треть, и ошибётся тот, кто возьмёт «объём» для ёмкостного планирования. Здесь
> названы обе; **XC-8 §6 приведён тем же изменением** — исправлять только у себя значило бы
> оставить неверное число в документе-владельце, где его и прочитают.

**Худший измеренный случай — 37% на полной странице в тысячу объектов.** Он же самый редкий:
типичная партия теряет ~7 мс. Кэш вердиктов в замере **не участвовал** (XC-8 §7 п.5) — в
проде положительные вердикты кэшируются, и это работает **в пользу** формы B; величина
сглаживания не мерена и здесь не додумывается.

### 3.2 Цена в безопасности — XC-8 её НЕ мерил, и это главное, что добавляет XC-9

Дисциплина **перемещает** поверхность выдачи, а не устраняет её. Пока право выдаётся
перечислением, «дать доступ» умеет тот, у кого есть полномочие выдачи на области. Когда право
выдано группе, «дать доступ» умеет **ещё и** тот, кто вправе менять состав этой группы: он
никого не выдаёт — он добавляет.

**Это не дыра и не регрессия.** Путь гейтится моделью (глагол правки на объекте группы), а не
самодельной проверкой; для ролей административного и редакторского яруса действует отдельное
требование полномочия выдачи на области — оно уже в коде и XC-9 его **не ослабляет**. Но
следствие обязано быть **сказано вслух и закреплено пробами**, потому что оно меняет ответ на
вопрос «кого проверять, разбирая, откуда у человека доступ»: к списку выдающих добавляются
редакторы состава.

**Что из этого вытекает как требование, а не как замечание:**

1. право править состав группы, держащей выдачу, — **такое же** полномочие, как выдача, и
   должно выдаваться с тем же вниманием (сценарий XC-9-06);
2. изменение состава обязано быть **наблюдаемо** — в тех же терминах, что и выдача
   (XC-9-07): «кто, кого, в какую группу»;
3. защита от усиления через группу для старших ролей обязана иметь **регрессию**, а не
   только реализацию: она снимается одной строкой и молча (XC-9-06б).

### 3.3 Цена в согласованности — окно видимости

Членство материализуется **eventually-consistent** (`data-integrity.md` B14): намерение
кладётся в writer-транзакции, применяется дренажом. Значит «добавили в группу → доступ есть»
и «убрали → доступа нет» верны **в ограниченном окне**, а не мгновенно. Это ровно та же
дисциплина, что уже действует для кортежа владельца, и клиент закрывает её ограниченным
повтором, **никогда** серверным барьером (ban #9). Сценарии §5 обязаны утверждать **исход в
окне**, а не мгновенность, иначе они будут флейкать и их ослабят — тем самым способом, каким
проба перестаёт что-либо проверять.

---

## §4 Объём работ — что ОСТАЛОСЬ, и чем помечено уже сделанное

> [!important] Здесь стояла очередь из трёх нетронутых фаз — к моменту ревью ни одна не была
> нетронутой: Ф2 приземлена **целиком**, Ф1 **наполовину**, у Ф3 выполнена **перепись**
> База сверки документа (`17def09eb`, 2026-08-09 21:41) старше приземления прибора
> (`7b866cd75`, 22:30) на 49 минут и старше страницы продукта (`ed5d7ec2c`, 2026-08-10 00:43)
> на три часа. Документ был верен, когда писался, и перестал быть верным раньше, чем его
> прочитал ревьюер. **План, в отличие от измерения, обязан описывать предстоящее:** очередь,
> ставящая сделанное, посылает исполнителя писать второй прибор рядом с существующим — это то
> же утверждение, пережившее свой предмет, только ценой не в неверную строку, а в дубль в
> коде. Поэтому таблица ниже разделена на **приземлено** (с координатой, чтобы найти) и
> **осталось** (что именно санкционирует APPROVED).

| Фаза | Приземлено — координата | ОСТАЛОСЬ (это и санкционируется) | Полна, когда | Out-of-scope следующей |
|---|---|---|---|---|
| **Ф1 — прибор** | кортежи и объекты: recorder `services/iam/internal/observability/metrics/binding_materialization_recorder.go`, провязан `services/iam/cmd/kacho-iam/wiring.go:218`, потребляется полным проходом `…/access_binding/reconcile/reconcile.go:447` и форвардом (`forward.go`, `ed5d7ec2c`); шесть проб `…/reconcile/materialization_size_test.go` утверждают **значение** на трёх нагрузках; issue #128 закрыт | **число перечисленных привязкой принципалов** — величина, ради свёртки которой затевается дисциплина, не публикуется; **показания на живом стенде** не сняты | новая величина публикуется тем же реестром и **тем же** recorder'ом (не вторым рядом); её проба утверждает **значение**; снятые показания названы числом с посадкой | гейт по дереву — Ф3 (Ф2 закрыта, переносить в неё нечего) |
| **Ф2 — норма** | оба адреса §2.2: страница продукта (`ed5d7ec2c`) и `data-integrity.md` §B18 (воркспейс `aa2db00`, сведён к ссылке этим заходом) | **ничего** — фаза закрыта | — | приведение самих посевов — Ф3 |
| **Ф3 — свои посевы и гейт** | перепись предмета выполнена (`ed5d7ec2c`, тело коммита): из 23 источников §1.5 более чем одному принципалу выданы **3** пары (роль, область), и все три **намеренны** — там канал выдачи и есть предмет пробы | **гейт по дереву** (XC-9-12/13) — его нет; **перевод источников — ноль**, и это исход, а не пропуск | гейт доказан инъекцией в обе стороны, печатает объём осмотренного, читает **все три** формы записи выдачи (§1.6) и несёт **самоистекающий** перечень трёх намеренных исключений | перевод чужих данных — не делается никогда (§0.2) |

**Ф1 — почему остаток не косметика.** Прибор сегодня отвечает на «во сколько кортежей
развернулась привязка», но не на «на скольких принципалов она выписана», а вся арифметика
формы B — это отношение второго к первому. Без второй величины «много кортежей» неотличимо от
«много объектов», и сравнение форм на живых данных нечем сделать. Измерено на `92ca6af9d`:
имён метрик у iam **21** (предикат — `Name:` у `prometheus.*Opts{` в `services/iam`), из них
упоминают субъекта или принципала — **0**; контроль предиката в обратную сторону: два
известных имени `kacho_iam_binding_materialization_{objects,tuples}` он находит, то есть
«ноль» здесь — про предмет, а не про сломанный поиск.

**Ф1 — почему «показания не сняты» тоже проверяемо, а не на слово.** Ни один потребитель
величины в дереве её не называет: `git grep -l binding_materialization` даёт **один** файл —
сам recorder; вне `*.go` — **0**. Контроль: файлов дашбордов и правил тревог в дереве **49**,
то есть категория потребителей существует и пуста именно для этой величины.

**Почему прибор всё ещё первым.** Он — вход в отложенное решение по форме D (XC-8 §6, порядок
п.1: узнать фактические N). Половина входа уже есть, вторая половина — в остатке Ф1; пока
второй величины нет, D нельзя ни принять, ни отвергнуть, и это единственная причина, по
которой остаток Ф1 не откладывается за Ф3.

**Ф3 — что значит «ноль переведённых» и почему это исход, а не пропуск.** Перепись назвала
три пары (роль, область), выданные более чем одному принципалу, и все три оказались
**намеренными**: каждый принципал несёт там отдельную пробу, а канал выдачи и есть предмет
проверки — прямая выдача служебной учётке **противопоставлена** членству в группе, и перевод
на группу снял бы различие, ради которого пробы написаны. Дисциплина §2.1 к ним не
применяется by construction: её условие — «одно и то же право нескольким», а здесь разным
принципалам намеренно даны **разные каналы** одного права.

> [!note] Число «3 пары» — **заимствованное**, и его предикат в источнике не выписан
> Оно названо телом коммита `ed5d7ec2c` (единица счёта — пара «роль × область», не файл и не
> литерал), и я его **не воспроизводил**: собственный проход по написанию даёт другую единицу
> и другое число (§1.6 — 4 источника с многосубъектным литералом), потому что меряет форму
> записи, а не выданное право. Поэтому **условие полноты Ф3**: перепись пересказывается не
> прозой, а **самим гейтом** — он и есть механизированный предикат этой переписи, его прогон
> печатает и объём осмотренного, и находки, и три исключения. Пока гейта нет, «ноль
> переведённых» держится на чужом замере с неназванным предикатом — это записано так, а не
> выдано за собственное измерение.

---

## §5 Сценарии Given-When-Then

Обозначения: «в окне» — в пределах ограниченного бюджета повтора клиента (§3.3), никогда не
«мгновенно». Каждый сценарий указывает уровень, на котором закрепляется.

### 5.0 У большинства сценариев УЖЕ ЕСТЬ производитель — заводится только дельта

Та же ошибка, что в §4, но на уровне проб: сценарий, у которого в дереве уже есть проба, при
буквальном исполнении породит вторую такую же. Перепись на `92ca6af9d`; единица счёта —
**именованный производитель** (заголовок e2e-кейса либо имя Go-теста). Ограничение названо:
предикат — поиск по заголовкам кейсов и именам/шапкам тестов, поэтому он устанавливает
**существование** производителя того же предмета, а **не** достаточность его утверждений;
поэтому у каждой покрытой строки отдельно названа дельта.

| Сценарий | Производитель в дереве | Что заводится заново (дельта) |
|---|---|---|
| XC-9-01 | e2e `rbac-subject-channel-equivalence.py` («group-member … identical to user-direct», «group grant scoped to members … non-member denied»); integration GM-1 в `…/access_binding/group_member_fga_integration_test.go` | **два члена разной природы** (человек + служебная учётка) в одной группе, каждый читает под своей личностью |
| XC-9-02 | e2e там же («group membership flip: addMember → gains access») | привязка **не трогается** — это утверждается; и «ровно один кортеж» — упирается в остаток Ф1 |
| XC-9-03 | e2e flip (removeMember → access removed, на двух пробах разной природы); integration GM-4 | **парный положительный контроль на втором члене** — сегодня в группе один, и «пропало у всех» неотличимо от «пропало у нужного» |
| XC-9-04 | e2e там же («revoke group binding: member loses access») | **два члена**; сохранность членства после отзыва; идемпотентность повторного отзыва |
| XC-9-05 | e2e три канала одной роли на одной области + две пробы изоляции принципалов | уровень **integration** и **вектор** вердиктов (чужой принципал · объект вне области · глагол, которого роль не даёт) поэлементно + контроль переворота |
| XC-9-06 | e2e `iam-group.py` («AddMember … as jwtNoBindings → 403 or 404», симметрично RemoveMember) | **парный положительный контроль** (тот же принципал с правом правки — успешно) и утверждение, что второй, самодельной проверки нет |
| XC-9-06б | unit `TestCreate_E32b_GroupSubject_NoGrantAuthority_Denied`; страж `requireGrantAuthority` на **каждом** Create | **инъекция** (снять требование → проба обязана упасть и назвать координату) + e2e |
| XC-9-07 | **производителя нет**: путь изменения состава не пишет ни строки наблюдаемости (предикат — ни одного вызова логгера/аудита в `…/api/group/add_member.go`), при том что подсистема аудита у iam есть | сценарий целиком |
| XC-9-08 | e2e `iam-rbac-subjects.py` («ExpandAccess on a group-subject grant → concrete group members»); 26 файлов дерева касаются `ExpandAccess` | «после удаления одного из группы разворот **в окне** перестаёт его называть» |
| XC-9-09 / 10 | **половина**: кортежи и объекты держат шесть проб `…/reconcile/materialization_size_test.go` (три нагрузки, значение, пустой проход как ноль) | вторая величина (принципалы) и обе стороны инъекции **на ней** — §4 |
| XC-9-10б | **производителя нет** | сценарий целиком: показания на живом стенде |
| XC-9-11 | норму держит обзор; отрицательный контроль **наблюдался** (§2.2) | — |
| XC-9-12 / 13 | **производителя нет** | гейт целиком (§4, остаток Ф3) |
| XC-9-14 | integration GM-1/GM-2 на **живом** OpenFGA: без кортежа членства проверка отвечает отказом, с ним — разрешением (пара «дефект / его отсутствие») | e2e через край; **создание привязки на пустую группу не отвергается**; разворот даёт **пустой список**; второй принципал — из ДРУГОЙ группы со своей выдачей на той же области |

**Что из этого следует для APPROVED.** Сценарии ниже читаются **вместе с этой таблицей**:
исполнитель заводит дельту, а не текст сценария целиком, и в паре RED→GREEN показывает, что
красное относится к дельте. Проба, повторяющая существующего производителя, — не «полнота», а
второй кодек того же утверждения: разойдутся они молча и именно там, где расхождение не видно.

### Группа A — дисциплина работает (положительная линия)

#### XC-9-01 · Выдача группе даёт доступ каждому члену
*Given* группа с двумя членами — пользователь и служебная учётка — и роль, дающая чтение
ресурса в проекте
*When* создаётся **одна** привязка, чей единственный субъект — эта группа
*Then* в окне **оба** члена читают ресурс, и **каждый** — под своей личностью
*And* субъект, в группу не входящий, получает отказ — и это утверждается **в том же прогоне**,
иначе положительная половина зеленела бы на «разрешено всем»
*And* уровень: e2e через край.

#### XC-9-02 · Добавление в группу выдаёт доступ без правки привязки
*Given* действующая привязка на группу и третий принципал вне её
*When* он добавляется в группу — **привязка не трогается**
*Then* в окне он получает ровно тот доступ, что даёт роль привязки
*And* число кортежей, порождённых этим добавлением, равно **одному** (утверждается прибором
Ф1, а не подсчётом в тесте — иначе тест меряет свою же арифметику)
*And* уровень: e2e + integration на реконсайлере.

#### XC-9-03 · Удаление из группы снимает доступ и не оставляет стоячего
*Given* член группы, имеющий доступ через привязку на группу
*When* он удаляется из группы
*Then* в окне доступ пропадает **на всех** объектах области привязки, а не на том, который
проверили первым — проверяются минимум два объекта разной природы
*And* доступ **остальных** членов не изменился (парный положительный контроль: без него
«пропал у всех» неотличимо от «пропал у нужного»)
*And* уровень: e2e + integration.

#### XC-9-04 · Отзыв привязки на группу снимает доступ у всех членов
*Given* группа с двумя членами и действующая привязка на неё
*When* привязка отзывается
*Then* в окне доступ пропадает у обоих, **членство сохраняется** (группа — не право)
*And* повторный отзыв идемпотентен: тот же исход, без ошибки
*And* уровень: e2e.

### Группа B — эквивалентность форм (без неё сравнение бессмысленно)

#### XC-9-05 · Группа и перечисление отвечают ОДИНАКОВО
*Given* два независимых набора данных с одной ролью и одной областью: в первом право выдано
перечислением двух принципалов, во втором — группе, куда те же двое входят
*When* обоим наборам задаётся **один и тот же** список вопросов, включающий отрицательные:
чужой принципал · объект вне области · глагол, которого роль не даёт
*Then* векторы вердиктов **совпадают** поэлементно
*And* отдельно утверждается, что вектор перечисления совпадает с **намерением фикстуры** —
иначе обе формы согласились бы на неверном
*And* отдельно утверждается, что снятие права **переворачивает** хотя бы один элемент вектора
— иначе отрицательная половина вакуумна
*And* уровень: integration (закрепляет предпосылку рекомендации XC-8, а не её числа).

### Группа C — безопасность: цена, названная в §3.2

#### XC-9-06 · Состав группы правит только тот, кому это позволяет модель
*Given* группа, держащая выдачу, и принципал **без** права правки этой группы
*When* он пытается добавить себя в группу
*Then* отказ, и решение принято **моделью** — в сервисе нет второй, самодельной проверки того
же (регрессия на `security.md` §«Авторизация живёт в МОДЕЛИ»)
*And* тот же принципал, получив право правки штатной выдачей, добавляет себя успешно — без
этой половины сценарий не отличает «запрещено» от «сломано»
*And* уровень: e2e.

#### XC-9-06б · Усиление через группу для старших ролей остаётся закрытым
*Given* принципал, вправе создавать привязки на области, но **без** полномочия выдачи для
роли административного/редакторского яруса
*When* он пытается выдать такую роль **группе**
*Then* отказ — тем же полномочием, что и при выдаче человеку; форма субъекта поблажки не даёт
*And* проба **красная**, если требование полномочия снять (доказывается инъекцией: строка
убирается, сценарий обязан упасть и **назвать** координату)
*And* уровень: unit на use-case + e2e.

#### XC-9-07 · Изменение состава наблюдаемо в тех же терминах, что и выдача
*Given* включённая наблюдаемость
*When* принципал добавляет и затем удаляет члена группы
*Then* оба события видны с указанием **кто · кого · в какую группу**, и одного из трёх не
хватает — это находка
*And* «ноль изменений состава за всё время» отличимо от «наблюдаемость не подключена»
(`security.md` §Hardening п.8 — мягкий проход обязан нести счётчик)
*And* уровень: unit на recorder + проба на живом стенде.

#### XC-9-08 · Ответ «кто имеет доступ» не портится косвенностью
*Given* право, выданное группе из двух членов
*When* запрашивается разворот доступа по этой привязке
*Then* возвращаются **конкретные** принципалы, а не сам userset
*And* после удаления одного из группы разворот в окне перестаёт его называть
*And* уровень: integration.

### Группа D — прибор (Ф1)

> **Половина этой группы уже закреплена — не пишите её заново.** Кортежи и объекты меряются и
> проверены **значением**: `…/access_binding/reconcile/materialization_size_test.go` держит шесть
> проб, включая рост на трёх разных нагрузках, пустой проход как ноль и одно наблюдение на
> выдачу при делегировании форварда полному пути. Предмет сценариев ниже — **новая** величина
> (число перечисленных принципалов) и её сопоставление с уже измеряемыми. Написать инъекцию на
> закрытой половине значит доказать гейт на том, что и так держится.

#### XC-9-09 · Размер материализации виден снаружи и меняется в правильную сторону
*Given* две привязки одной роли на одной области: одна на группу из десяти, другая
перечислением десяти принципалов
*When* обе материализованы
*Then* прибор показывает у первой **меньше** кортежей, чем у второй, и отношение согласуется с
арифметикой формы (число объектов × число глаголов против того же, умноженного на число
принципалов)
*And* прибор показывает **число перечисленных принципалов** отдельно от числа кортежей — иначе
«много кортежей» неотличимо от «много объектов». **Это и есть остаток Ф1:** величина не
публикуется (0 из 21 имени метрики iam, §4), поэтому первая половина сценария сегодня
проверяема, а вторая — нет, и именно она делает сценарий падающим до кода (ban #12)
*And* новая величина публикуется **тем же** recorder'ом, что кортежи и объекты: второй recorder
рядом с существующим дал бы две величины об одном предмете, расходящиеся молча
*And* проба утверждает **значение** величины, а не факт её регистрации: величина,
зарегистрированная и никогда не обновляемая, — тот же класс, что гейт без производителя
*And* уровень: unit + integration.

#### XC-9-10 · Прибор доказан на заведомо разном входе
*Given* один и тот же сценарий, прогнанный с удвоенным числом объектов
*When* снимаются показания
*Then* число кортежей выросло **ровно вдвое**, а число принципалов **не изменилось**
*And* обратная проба: при удвоении числа принципалов растёт второе, а не первое
*And* **обе стороны инъекции пишутся на НОВОЙ величине**: рост кортежей от объектов уже
закреплён (`TestBindingMaterializationSizeGrowsWithMatchedObjects` и соседи), и повторить его
значило бы доказать инъекцию на уже зелёном. Различающая пара здесь — «удвоили принципалов»
против «удвоили объекты»: без второй величины первый вход не меняет **ни одного** показания,
и это тот самый вход, на котором проба обязана быть красной до кода
*And* уровень: unit (это проверка **предпосылки** прибора; без неё его показания — украшение).

#### XC-9-10б · Показания сняты на живом стенде, а не только в проце́ссе
*Given* стенд в production-посадке с приземлённым прибором
*When* создаются привязки заведомо разного размера и снимаются показания
*Then* названы **числа** — распределение кортежей, объектов и принципалов — с посадкой и
ревизией, на которой сняты
*And* «величина есть в реестре» **не** засчитывается за «показания сняты»: сегодня величину не
называет ни один потребитель в дереве (один файл — сам recorder, вне `*.go` — ноль, при 49
файлах дашбордов и правил тревог), то есть прибор пока некому прочитать
*And* уровень: проба на живом стенде (§4, остаток Ф1).

### Группа E — норма и её удержание (Ф2, Ф3)

#### XC-9-11 · Норма записана один раз, и её адреса не пересказывают друг друга
*Given* два адреса §2.2 — **оба приземлены** (страница продукта `ed5d7ec2c`; §B18 сведён к
ссылке этим заходом)
*When* правится один из них
*Then* второй **не** становится ложным — потому что он ссылается, а не воспроизводит
*And* проверяется поиском второго определения одного предмета: формулировка правила
инженерного не содержит инструкции арендатору, страница продукта не содержит устройства
материализации
*And* **отрицательный контроль у этого сценария уже наблюдался, и он был красным**: первая
редакция §B18 воспроизводила шесть элементов приёмки, и уточнение одного числа пришлось
вносить в три документа сразу (§2.2). То есть сценарий не вакуумен — он **различал**, и
различил на живом входе
*And* уровень: обзор, критерий §8 п.6.

#### XC-9-12 · Новый посев не может завести перечисление там, где нужна группа
*Given* гейт по дереву над декларативными источниками §1.5
*When* в дерево добавляется источник, выдающий одну роль на одной области **нескольким**
принципалам перечислением
*Then* гейт **красный** и называет файл и строку
*And* **законный близнец молчит**: источник, выдающий одному принципалу, и источник, выдающий
группе, гейт не трогает
*And* гейт печатает **объём осмотренного** (сколько источников прочитано, сколько признано
выдающими), чтобы «ноль находок» было отличимо от «ноль прочитанного»
*And* гейт читает **все три формы записи выдачи**, измеренные в §1.6 — литерал `subjects[]`,
легаси-пару `subjectType`/`subjectId` и `subjects` из переменной. Гейт, знающий только первую,
прочитает **4 источника из 23** и напечатает «ноль находок» на дереве, где четырнадцать мест
написаны другой формой того же действия; это утверждается **пробой на каждую форму**, а не
намерением
*And* гейт несёт проверку **своей предпосылки**: распознавание вызова выдачи привязано к форме,
которая в дереве может смениться, поэтому при её исчезновении гейт обязан **упасть**, а не
молча перестать что-либо находить — и предпосылка проверяется тем же прогоном, что и находки
*And* перечень исключений (три намеренные пары §4) **самоистекает**: запись, которой больше
нечего исключать, — находка, а не остаток оформления
*And* уровень: гейт `internal/repohygiene`.

#### XC-9-13 · Существующее перечисление на одного не объявляется нарушением
*Given* источник, выдающий право **одной** служебной учётке, чья личность и есть предмет
выдачи
*When* прогоняется гейт §XC-9-12
*Then* он молчит
*And* это утверждается **сценарием**, а не подразумевается: без него первый же проход
«унифицирует» и заведёт группу из одного члена — косвенность без выигрыша, то есть ровно тот
оверинжиниринг, который дисциплина должна была предотвратить
*And* второй законный близнец — **синтетическая фикстура внутри инъекции чужого гейта**:
в дереве есть проверка посадки, которая строит тело запроса выдачи прямо в своей пробе (§1.6,
пятая строка разбиения). Гейт обязан молчать и на ней: иначе первый же его прогон краснеет на
чужом тесте, и снимут не находку, а сам гейт
*And* уровень: гейт (положительный контроль).

### Группа F — вырожденный состав

#### XC-9-14 · Пустая группа не даёт доступа НИКОМУ — и это отличимо от «всё сломано»
*Given* группа **без единого члена** и действующая привязка, чей единственный субъект — эта
группа
*When* доступ к объекту области запрашивают двое: посторонний принципал и принципал, состоящий
в **другой** группе, у которой есть **своя** выдача на той же области
*Then* отказ обоим — пустое членство означает «право не получил никто», а **не** «условие
получателя нечем проверить, значит пропускаем»
*And* парный положительный контроль **в том же прогоне**: тот же принципал, добавленный в эту же
группу, в окне получает ровно тот доступ, что даёт роль привязки. Без него «отказано всем»
неотличимо от «выдача вообще не материализовалась», и сценарий зеленел бы на полностью сломанном
*And* создание привязки на пустую группу — **не** отказ: группа наполняется после выдачи, и
ошибка на этом шаге сделала бы дисциплину неприменимой к штатному порядку «сначала право, потом
люди». Отдельно утверждается, что пустой **список субъектов** привязки по-прежнему отвергается —
это другой предмет (форма запроса), и смешение двух пустот здесь и есть ошибка
*And* разворот доступа (XC-9-08) по такой привязке возвращает **пустой** список принципалов — не
сам userset, не отказ и не ошибку
*And* уровень: e2e + integration.

> **Почему этот сценарий обязателен, а не «полнота ради полноты».** Вырожденный получатель —
> канонический вход того класса, где пустое множество читается как «не сужаем» вместо
> «не разрешаем» (`security.md` §«Кто вправе ГОВОРИТЬ ЗА пользователя», §«Отношение, выполнимое
> подстановочным знаком»). Дисциплина XC-9 **вводит** косвенность между правом и людьми, значит
> она вводит и состояние «людей ещё нет» — и именно на нём отказ и всеразрешение выглядят
> одинаково со стороны выдающего.

---

## §6 Чем это будет держаться

| Что | Механизм | Что покраснеет | Состояние |
|---|---|---|---|
| дисциплина в **наших** источниках | **гейт по дереву** (XC-9-12/13), доказан инъекцией в обе стороны | новый источник, выдающий нескольким перечислением — с именем файла и строки | **не заведён** — остаток Ф3 |
| размер материализации **виден** (кортежи, объекты) | **прибор**, шесть проб `…/reconcile/materialization_size_test.go` | величина, переставшая обновляться; двойной доклад одной выдачи; расхождение с арифметикой формы | **держит** (`7b866cd75`, `ed5d7ec2c`) |
| выигрыш формы **измерим** (принципалы против кортежей) | **прибор**, вторая величина (XC-9-09/10) | удвоение числа принципалов, не изменившее **ни одного** показания | **не заведена** — остаток Ф1 |
| прибор **прочитан**, а не только зарегистрирован | **показания на живом стенде** (XC-9-10б) | «величина есть в реестре», выданное за снятые показания | **не снято** — остаток Ф1 |
| безопасность не ослабла | **пробы** XC-9-06/06б, снятие требования полномочия ловится инъекцией | попытка выдать старшую роль группе без полномочия — успехом | страж стоит на **каждом** Create; unit `TestCreate_E32b_GroupSubject_NoGrantAuthority_Denied` есть — остаток: **инъекция и e2e** |
| ответ аудита не испортился | **проба** XC-9-08 | разворот, вернувший userset вместо принципалов | e2e держит разворот на группе — остаток: **исчезновение удалённого в окне** (§5.0) |
| эквивалентность форм | **проба** XC-9-05 с отрицательной половиной и контролем переворота | форма, которая отвечает **иначе** — она не «быстрее», она другая | e2e держит три канала — остаток: **вектор на уровне integration** (§5.0) |
| вырожденный состав не открывает доступ | **проба** XC-9-14 с парным положительным контролем | привязка на группу **без членов**, давшая доступ хоть кому-то; разворот, вернувший userset вместо пустого списка | integration на живом OpenFGA держит пару «без кортежа членства — отказ / с ним — разрешение»; остаток: **край, пустой разворот, создание на пустую группу** (§5.0) |
| изменение состава наблюдаемо | **проба** XC-9-07 | «ноль изменений состава», неотличимое от неподключённой наблюдаемости | **производителя нет** — путь не пишет ни строки (§5.0) |

**Три требования к каждому гейту здесь — исполнение `testing.md` §«Гейт на класс», а не его
пересказ:** доказательство инъекцией **в обе стороны**; проверка **собственной предпосылки** и
печать объёма осмотренного; **въезд вместе с фиксом**, а не раньше — гейт, влитый красным «на
время», и есть тот способ, которым исключение переживает свой предмет.

---

## §7 Что явно ВНЕ охвата

1. **Форма D (контейнер) и любая правка модели.** Решается замером фактических N, вход в
   решение — прибор Ф1. XC-9 не приближает и не отдаляет это решение, он даёт ему числа.
2. **Форма C (роль-отношение).** Отвергнута замером: половина выигрыша D при сопоставимой цене
   чтения **и** сужение выразительности роли (XC-8 §6).
3. **Перевод существующих арендаторских привязок.** Не делается. Дисциплина действует вперёд.
4. **Понижение потолка «1..32».** Контракт не меняется (§0.2).
5. **Управление жизненным циклом групп как продуктовая тема** — вложенность, синхронизация с
   внешним каталогом, срок действия членства. Здесь только выдача и состав.
6. **Нагрузочная характеристика под конкуренцией.** XC-8 мерил последовательно (§7 п.3 там);
   поведение полного прохода под параллелью — отдельный предмет со своей приёмкой.
7. **Кэш вердиктов как предмет настройки.** Он работает в пользу формы B, величина не мерена,
   и подгонять его под этот вывод нельзя — это было бы измерением, подстроенным под гипотезу.

---

## §8 Критерии APPROVED

1. **Каждое число §1 воспроизводится названным предикатом** на базе сверки; ревьюер прогоняет
   не менее двух, включая зеркальную проверку §1.1.
2. **Каждое число §3.1 несёт провенанс** и **не смешивается** с деревянными: сказано, что оно
   получено прогоном харнесса, на какой ревизии и при какой посадке. Число без провенанса —
   `CHANGES_REQUESTED`.
3. **Уточнение по объёму (§3.1, врезка) сохранено**: «10×» относится к кортежам, «7.4×» — к
   байтам. Округление одного до другого — `CHANGES_REQUESTED`.
4. **Цена §3.2 названа и покрыта сценариями.** Приёмка, называющая только выигрыш, не готова
   независимо от качества сценариев.
5. **Каждая фаза §4 production-complete в своих границах**, и у каждой сказано, что вынесено в
   `Out-of-scope` следующей. Фаза, чей предмет может оказаться пустым (Ф3), несёт **явное
   разрешение закрыться числом ноль** — иначе исполнитель выдумает работу.
6. **Норма §2.1 имеет ровно один адрес на предмет** (§2.2), и признак расхождения назван
   механически. Два места, пересказывающих друг друга, — `CHANGES_REQUESTED`.
7. **У каждого отрицательного сценария есть парный положительный** (XC-9-01, 03, 06, 13, 14).
   Одиночное отрицание зеленеет сильнее всего тогда, когда сломано всё.
8. **У каждого гейта названы обе стороны инъекции** — возвращаемый дефект и законный близнец,
   на котором гейт обязан молчать.
9. **Ни один сценарий не утверждает мгновенности** там, где действует ограниченное окно (§3.3);
   и ни один не «терпит» оба исхода сразу — утверждение, принимающее и успех, и отказ, не
   является утверждением.
10. **Сказано прямо, что APPROVED разрешает и чего не разрешает** (§0.2), и запреты
    фальсифицируемы: названо, каким документом решается каждое из отложенного.
11. **Пригодность к публикации:** в документе нет сборки «точная координата + условие, при
    котором защита не действовала + следствие для постороннего». §3.2 описывает **действующую**
    и **работающую** конструкцию и её следствие для того, кто разбирает доступ, — это контракт,
    а не рецепт.
12. **§4 описывает ПРЕДСТОЯЩЕЕ, а исполненное помечено координатой.** Очередь, ставящая
    сделанное, — то же утверждение, пережившее предмет, только ценой в дубль в коде: исполнитель
    заводит второй прибор рядом с существующим. Фаза без колонки «приземлено» либо с фазой,
    приземлённой целиком и не помеченной, — `CHANGES_REQUESTED`. Проверяется прямо: каждая
    координата колонки «приземлено» обязана резолвиться в дереве.
13. **Ни один сценарий не заводит второго производителя того же утверждения.** Сценарии
    читаются вместе с §5.0: у покрытых названа **дельта**, и пара RED→GREEN показывает, что
    красное относится к дельте, а не к тому, что уже держится. Проба-дубль — не полнота, а
    второй кодек одного утверждения, который разойдётся с первым молча.
14. **Числа, взятые у чужого замера, помечены как заимствованные** — с единицей счёта и
    оговоркой, воспроизводились ли они здесь. Заимствованное число, поданное как собственное
    измерение, — `CHANGES_REQUESTED` независимо от того, верно ли оно (§4, врезка про «3 пары»).

---

## §9 Открытые вопросы

**Один, и он не блокирует остаток Ф1** (Ф2 закрыта, §4).

**Порог для гейта Ф3: «более одного принципала» — достаточный ли признак?** Правило §2.1 несёт
**два** условия — множественность и изменчивость состава, — и гейт по дереву умеет проверить
только первое: изменчивость не выражена в источнике ничем, что можно прочитать. Варианты:
(а) гейт ловит только множественность, изменчивость остаётся вопросом обзора; (б) источник,
выдающий одному, обязан **объявить** это намерение (комментарий заданной формы), и тогда гейт
ловит оба, но платит требованием к каждому такому месту.

Предпочтение автора — **(а)**, и оно записано как предпочтение, а не как решение: (б)
превращает гейт в требование к оформлению, а требования к оформлению снимаются первым же
проходом, который сочтёт их шумом. Но выбор — продуктовый, и принимает его владелец.

**Что к этому вопросу добавила перепись (вход, а не ответ).** Три намеренные пары §4 — это
ровно те места, где вариант (а) даёт находку, которую придётся **исключить поимённо**: там
множественность есть, а нарушения нет, потому что разным принципалам намеренно даны разные
**каналы** одного права. То есть цена (а) — не «изменчивость не проверяется», а
**самоистекающий перечень из трёх записей**, и он обязан падать, когда исключать станет
нечего. Вариант (б) этот перечень убрал бы, переложив его в комментарий у каждого места. Выбор
по-прежнему за владельцем; здесь названа его настоящая цена, а не гипотетическая.

**Чего в §9 намеренно НЕТ.** Вопроса «переводить ли существующие привязки» — он не открыт, он
**решён отрицательно** в §0.2 и §7 п.3. Вопроса «брать ли форму B» — он решён замером XC-8;
этот документ исполняет решение, а не переоткрывает его.

---

## §10 Ревью 2026-08-10 — что проверено исполнением и что ждёт правки

Ревизия проверки — `92ca6af9d` (ствол после влития 87 коммитов), не база сверки документа.

### 10.1 Что подтверждено и переделки не требует

- **§1.1 — 99/99** воспроизведено названным предикатом на `92ca6af9d`, на базе `17def09eb` и
  на `ebedae536`; **обе зеркальные проверки** дали `0`. Разложение сверено поимённо и сходится
  точно: 24 типа несут по четыре глагола CRUD (24×4=96), плюс `v_create` у `registry_registry`
  и `v_addtargets`/`v_removetargets` у `nlb_target_group` — итого 99 при 30 типах модели.
- **§1.2/§1.3/§1.4** — координаты резолвятся, утверждения верны:
  `services/iam/internal/domain/subject.go:22` (`group:<id>#member`, godoc требует байтовой
  симметрии двух потребителей), `subject.go:65` (`MaxSubjectsPerBinding = 32`, пустой
  `subjects[]` → `INVALID_ARGUMENT` в `NormalizeSubjects`), `add_member.go:118` /
  `remove_member.go:103` — симметрия подтверждена: одно и то же тело кортежа
  (`memberFGATuple`) в одном и том же замыкании писателя.
- **§1.5 — 23 / 2 / 31** воспроизведены. Два источника с субъектом-группой — ровно те, что
  описаны словами («предметом пробы является сама группа или равенство каналов выдачи»).
- **Конструируемость XC-9-14 установлена по коду, а не предположена** (это было отдельным
  требованием к ревью): пустая группа конструируема **by construction** — в `CreateGroupRequest`
  поля членов нет вовсе, состав набирается только `AddMember`; создание привязки на такую
  группу ничем не отвергается — ни `create.go`, ни `structural_gates.go` не проверяют ни
  существования, ни непустоты группы; разворот доступа отдаёт **конкретных** принципалов и
  отбрасывает userset-листья, то есть на пустой группе даёт пустой список. Разделение «пустого
  членства» и «пустого списка субъектов» в сценарии верно: второе — действующий отказ.
- **XC-9-06б имеет предмет в дереве:** страж усиления через группу стоит в
  `access_binding/create.go:195` (`requireGrantAuthority` на **каждом** Create), и у него уже
  есть unit `TestCreate_E32b_GroupSubject_NoGrantAuthority_Denied`. Сценарий добавляет к нему
  доказательство инъекцией и e2e — это остаток, а не пустое место.
- Парный положительный контроль стоит у **каждого** отрицательного сценария (01, 03, 06, 13, 14);
  ни один сценарий не утверждает мгновенности и ни один не принимает оба исхода.

### 10.2 Что ждёт правки (блокирует APPROVED)

1. **Ф1 описывает СДЕЛАННОЕ как предстоящее.** Прибор в значительной части приземлён:
   `services/iam/internal/observability/metrics/binding_materialization_recorder.go` публикует
   `kacho_iam_binding_materialization_objects` и `..._tuples`, провязан в
   `services/iam/cmd/kacho-iam/wiring.go:218`, потребляется в `reconcile.go:447`, и его unit
   (`materialization_size_test.go`) утверждает **значение** на трёх разных нагрузках, держа
   рядом неотбираемый объект-приманку — то есть ровно то, чего требует XC-9-09. Issue #128
   **закрыт**. База сверки документа (`17def09eb`, 21:41) старше приземления (`7b866cd75`,
   22:30) на 49 минут — документ был верен при написании и перестал быть верным к ревью.
   **Остаток реален и другой:** прибор **не публикует число перечисленных принципалов** —
   ту самую величину, ради свёртки которой затевается дисциплина (в каталоге метрик iam нет
   ни одного упоминания субъекта/принципала), и показания на живом стенде не сняты. Переписать
   Ф1 на этот остаток, иначе исполнитель заведёт второй recorder рядом с существующим.
2. **Ф2 наполовину приземлена, и об этом не сказано.** Второй адрес нормы —
   `.claude/rules/data-integrity.md` §B18 — записан **тем же коммитом**, что и текущая редакция
   этого документа (`aa2db00`). §4 подаёт оба адреса как предстоящие.
3. **§2.2 не выполнен: второй адрес оказался вторым ИЗЛОЖЕНИЕМ.** B18 воспроизводит правило
   §2.1, предикат §1.1 с зеркалом, пять чисел §3.1, перемещение поверхности выдачи §3.2,
   требование к вырожденному составу XC-9-14 и «вперёд + потолок 1..32» из §0.2 — двадцать
   строк, — и завершается фразой «здесь она не пересказывается». Механический признак
   расхождения, названный **в самом §2.2**, срабатывает: уточнение «10× кортежей против 7.4×
   байт» пришлось вносить **в три места сразу**. Свести B18 к формулировке правила, одному
   предикату и ссылке; числа, цена в безопасности и вырожденный состав остаются здесь.
   По §8 п.6 это `CHANGES_REQUESTED` — критерий документа против самого документа.
4. **Ложная координата замера уехала в нормативку.** B18 несёт «харнесс `tools/authzformbench/`
   @ `ebedae536`», §3.1 здесь — то же самое. На `ebedae536` каталога нет вовсе (появился в
   `11f664c20`). Правится одним изменением с XC-8 §9.2 п.1-2.

### 10.3 Не блокирует, но правится тем же заходом

5. **Предикат §1.5 воспроизводится только с угаданным умолчанием.** «Прозы `docs/`» в обратных
   кавычках читается как путь верхнего уровня; буквальное прочтение даёт **26**, а не 23 —
   лишние три суть `services/iam/docs/components/{06-group,08-access-binding,10-operations}.md`.
   С исключением прозы `docs/` **на любой глубине** получается ровно 23 и заявленное разложение.
   Написать ограничение так, как оно имелось в виду: иначе число воспроизводит не тот, кто
   проверяет, а тот, кто угадает то же умолчание — замечание, которое документ уже сделал
   сам себе врезкой в §1.5.
6. **XC-9-09/10 стоит привязать к остатку из п.1** — назвать, что объекты и кортежи уже
   измеряются и проверены значением, а доказательству инъекцией подлежит **новое** измерение
   (число принципалов). Иначе обе стороны инъекции будут написаны на уже закрытой половине.

### 10.4 Исход по каждому пункту (правка 2026-08-10, ревизия сверки `92ca6af9d` / воркспейс `ce4379e`)

Вердикт §10.1-10.3 оставлен дословно: это запись ревьюера, и переписывать её под свои правки
значило бы стереть то, чем правки вызваны. Ниже — что с каждым пунктом стало.

| # | Предмет | Исход | Где смотреть |
|---:|---|---|---|
| 1 | Ф1 описывала сделанное как предстоящее | **закрыт** | §4 разделён на «приземлено — координата» и «ОСТАЛОСЬ»; остаток назван двумя измеримыми вещами: величина «принципалов» (0 из 21 имени метрики iam, контроль предиката в обе стороны) и показания на стенде (1 файл называет величину, вне `*.go` — 0, при 49 файлах дашбордов и правил); §5 группа D переписана на остаток, добавлен XC-9-10б |
| 2 | Ф2 наполовину приземлена | **закрыт с уточнением: она приземлена ЦЕЛИКОМ** | ревью назвало один адрес (B18, `aa2db00`); второй — страница продукта — приземлён `ed5d7ec2c` (раздел «Кому выдавать», `Group` ссылается и не пересказывает). §4: у Ф2 остаток **ничего** |
| 3 | §2.2 не выполнен — второй адрес был вторым изложением | **закрыт** | B18 сведён к норме, предикату и двум адресам; §2.2 несёт колонку состояния и врезку с разбором. Пункт был **возвращён** приёмкой XC-8 (её §10 п.2) именно этому заходу — иначе у него не было бы исполнителя вовсе |
| 4 | Ложная координата замера в §3.1 и в нормативке | **закрыт** | §3.1: провенанс — `11f664c20` (предикат невозможности `ebedae536` назван, числа не тронуты); B18 координату замера больше не несёт вовсе — числа живут в приёмке |
| 5 | Предикат §1.5 воспроизводим только с угаданным умолчанием | **закрыт** | предикат выписан командой целиком; числа `23 / 2 / 31` **не менялись**, названы оба прочтения (26 против 23) и три файла разницы; у строки «2» появился свой предикат с зеркалом (21) и контролем на несуществующем токене |
| 6 | XC-9-09/10 привязать к остатку | **закрыт** | группа D несёт предупреждение «половина закреплена — не пишите заново», обе стороны инъекции перенесены на новую величину, добавлен XC-9-10б про снятие показаний |

**Сверх вердикта — найдено этим же заходом и закрыто здесь же.** Ревью не спрашивало, но
класс тот же, что в п.1, и на уровне проб он дороже:

- **§5.0 — карта производителей.** Сценариев в §5 — **16** (01, 02, 03, 04, 05, 06, 06б, 07,
  08, 09, 10, 10б, 11, 12, 13, 14). Разбивка, а не одно число, потому что одно зависело бы от
  того, что зачитывать: **9** имеют производителя того же предмета целиком (01-06б, 08, 14 —
  e2e каналов выдачи и группы, integration на живом OpenFGA, unit стража усиления); **2**
  покрыты **наполовину** (09 и 10 — измеряются кортежи и объекты, не принципалы); **4** не
  имеют производителя вовсе (07, 10б, 12, 13); **1** держится обзором, а не пробой (11).
  9+2+4+1 = 16. Буквальное исполнение §5 завело бы до одиннадцати дублей — ровно то, от чего
  п.1 предостерегал в отношении прибора. У каждой покрытой строки названа **дельта**; предикат
  переписи и его ограничение названы вместе с ней.
- **§1.6 — три формы записи выдачи** в наших источниках (4 / 14 / 5 из 23, разбиение
  исчерпывающее). Гейт Ф3, знающий одну форму, прочитал бы 4 источника из 23 и напечатал «ноль
  находок». Требование к гейту дописано в XC-9-12 **до** его написания.
- **§8 пополнен тремя критериями** (12, 13, 14): план описывает предстоящее; сценарий не
  заводит второго производителя; заимствованное число помечено заимствованным.
- **Число «3 пары» помечено заимствованным** (§4, врезка): оно из тела `ed5d7ec2c`, его
  предикат там не выписан, и я его не воспроизводил — собственный проход по написанию даёт
  другую единицу счёта. Условие полноты Ф3 переписано так, что перепись пересказывается **самим
  гейтом**, а не прозой.

**Что осталось незакрытым и почему.** Ничего из §10.2-10.3. Открытым остаётся §9 — выбор
порога для гейта Ф3, и это **вопрос владельца**, а не невыполненная правка; заход добавил к
нему настоящую цену варианта (а) вместо гипотетической. Статус документа **не** меняется:
APPROVED ставит `acceptance-reviewer`.

### 10.5 Круг проверки закрытия — 2026-08-10, вердикт **APPROVED**

Предмет круга — **закрытие шести пунктов**, а не документ заново. Проверялось исполнением на
`92ca6af9d`; числа, уже воспроизведённые прошлым кругом, второй раз не воспроизводились.

**Закрытие по существу, а не по тексту — проверено на каждом пункте:**

| # | Чем проверено, что закрыто **по существу** |
|---:|---|
| 1 | §4 несёт колонку «приземлено» с координатами, и **все** резолвятся: recorder-файл на месте, провязка `wiring.go:218` (`WithSizeRecorder`), потребление `reconcile.go:447` (`ObserveBindingMaterialization`), форвард `forward.go:214` (`r.observeSize`), **шесть** проб в `materialization_size_test.go` — ровно шесть `func Test`. Остаток назван двумя измеримыми вещами и обе перемерены (ниже) |
| 2 | оба адреса Ф2 живы: `access-binding.mdx:24` несёт якорь `{#grant-to-group}`, `group.mdx:22` **ссылается** на него и не пересказывает |
| 3 | прочитан **текущий текст** B18, а не заявление о нём: чисел замера, ревизии харнесса, §3.2 и требования к вырожденному составу там больше нет — осталась норма, предикат применимости с зеркалом и два адреса. Предикат B18 прогнан **как написан в правиле** (он короче предиката §1.1): `99 99`, зеркало `0`, контроль на несуществующем токене `99 0` |
| 4 | `ebedae536` не встречается ни в `.claude/rules/`, ни в §3.1; в XC-6/XC-8 он остался только там, где **сам является предметом разбора**. XC-8 §6 несёт обе величины (10× кортежей / 7.4× байт) |
| 5 | предикат §1.5 выписан командой и воспроизводится дословно: **23**, из них с группой **2**, зеркало `grep -L` → **21**, контроль на несуществующем токене → **0**, максимум упоминаний **31** (`iam-access-binding-redesign.py`). Оба прочтения ограничения названы верно: буквальное `^docs/` даёт **26**. На базе `17def09eb` предикат тоже даёт **23** — числа действительно не менялись |
| 6 | группа D несёт предупреждение о закрытой половине; обе стороны инъекции XC-9-10 стоят на **новой** величине; XC-9-10б заведён |

**Живость плана (требование круга).** Утверждение из плана и утверждение из измерений взяты
и проверены по дереву порознь: план — «прибор приземлён, остаток — вторая величина»
(подтверждено координатами выше); измерение — «величина не публикуется» (перемерено ниже).
Расхождения «сделанное описано как предстоящее» **не осталось**.

**Измерение не подделано под сегодня.** §3.1 правил **координату инструмента**, а не
величины: числа замера остались как были, и это сказано врезкой. Две ревизии — база сверки
`17def09eb` и ревизия приземления `92ca6af9d` — названы раздельно, и разрыв между ними
объяснён. §1.1 сходится **на обеих**.

**Частичное не выдано за целое.** Разбивки сошлись поимённо: §5.0 — 9 целиком + 2 наполовину
+ 4 без производителя + 1 обзором = **16** при **16** заголовках сценариев в §5 (14 строк
таблицы, потому что две строки покрывают по паре); §1.6 — 4 + 14 + 5 = **23**. Строка 09/10
помечена **половиной**, а не покрытой; Ф1 в §4 — **наполовину**, а не закрытой.

**Предикаты — два взяты и прогнаны с контролем в обе стороны:**

- **«0 из 21 имени метрики»** (тот, что просили проверить особо). Прямая сторона: имён —
  **21**, из них упоминающих субъекта/принципала/члена — **0**. Обратная: два известных имени
  `kacho_iam_binding_materialization_{objects,tuples}` предикат **находит** (2), на
  несуществующем токене даёт **0**. Дополнительно проверено, что предикат не слеп к **целому
  виду** предмета: конструкторов метрик в `services/iam` ровно **21** (2 Counter + 8
  CounterVec + 1 Gauge + 6 GaugeVec + 2 Histogram + 2 HistogramVec), то есть каждому
  конструктору соответствует прочитанное имя, `promauto` не используется; в `pkg/` метрик про
  субъекта тоже **0**. Предикат годен, «ноль» — про предмет.
- **«три формы записи выдачи»** (§1.6) — предикат воспроизведён независимо: **4 / 14 / 5**,
  сумма **23**; многоэлементных литералов **9** из **11**. Законный близнец из пятой строки
  жив: `deploy/scripts/assert-teardown-frees-parent.py` действительно строит тело
  `POST /iam/v1/accessBindings` внутри своей пробы (строки 211/230).

**Две неточности — НЕ блокирующие, правятся при следующем касании.** Обе — украшение верных
находок: вывод не меняется, если число убрать совсем, и работа, которую санкционирует
APPROVED, от них не зависит.

1. **«49 файлов дашбордов и правил тревог» (§4 и XC-9-10б) — совпадение по имени, а не по
   референту.** Число воспроизводится (поиск по именам `dashboard|alert…rule|grafana`), но
   **все 49** — это `ui-future/dashboard/**`, микрофронтенд с таким именем. Настоящих
   графановских дашбордов в дереве **0**, объектов `PrometheusRule` — **0**. Поэтому вывод
   контроля («категория потребителей существует и пуста именно для этой величины») **неверен
   в первой половине**: категория отсутствует целиком. Измерение, которое контроль обслуживал,
   при этом подтверждено **прямо и независимо** — `git grep -l binding_materialization` даёт
   **1** файл (сам recorder), вне `*.go` — **0**, — а истинное положение дел («слоя потребления
   метрик в дереве нет вовсе») делает вывод §4 не слабее, а сильнее. Заменить контроль на
   честный либо снять его вместе с числом.
2. **«26 файлов дерева касаются `ExpandAccess`» (§5.0)** — 26 верно для **`services/iam`**;
   по всему дереву — **40**. Область предиката не названа. Несущее утверждение строки
   (производитель XC-9-08 существует) проверено отдельно и верно: кейс с заголовком
   «ExpandAccess on a group-subject grant → … concrete group members» на месте.

**Почему это не держит документ в DRAFT.** Ни одна из двух не меняет ни объёма работ
(остаток Ф1 — величина «принципалов» и показания на стенде; остаток Ф3 — гейт), ни границ
§0.2, ни единого сценария §5. Держать санкционирующий документ закрытым ради двух
подпирающих чисел значило бы отложить работу ради формулировки — при том что оба числа
названы здесь поимённо и потому не переживут следующего касания молча.

**Что остаётся открытым.** Только §9 — порог для гейта Ф3, **вопрос владельца**, а не
невыполненная правка. Он не блокирует остаток Ф1 и не мешает начать Ф3: гейт пишется по
варианту (а) с самоистекающим перечнем из трёх намеренных исключений, пока владелец не решит
иначе.
