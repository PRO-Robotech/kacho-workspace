# Приёмка — SB-1: Kaname собирается и поднимается ВНЕ дерева монорепо

> **Статус:** DRAFT — ожидает вердикта `acceptance-reviewer`. Вердикт выводится из
> внешнего события, а не из слова в этой строке.

Гейт запрета #1 (`.claude/rules/00-kacho-core.md`). Линия — вынос службы управления
доступом отдельным продуктом **Kaname**; замысел линии — `docs/changes/standalone-iam/design.md`
(этот документ на него **опирается** и его не пересказывает).

**Предмет — ровно один вопрос:** что значит «поднимается вне дерева монорепо» и чем
это доказывается. Пункт готовности линии объявлен выполненным; ниже показано замером,
что он доказан наполовину, и названо, чем закрывается остаток.

**Что предметом НЕ является** (у каждого свой владелец, здесь они не решаются):
раскладка модулей пакета изменения · содержание модели доступов
(`MODEL-MANIFEST.md`) · выбор посадки личности `external`/`own` · лицензионная
политика продукта · перенос кода из монорепо.

---

## 1. Единица наблюдения и «Дано» всей приёмки

**Единица наблюдения — арендатор чужого облака.** У него есть склонированный
опубликованный репозиторий `PRO-Robotech/kaname` и больше ничего нашего: ни
монорепо рядом, ни нашего стенда, ни наших профилей, ни наших ранеров.

Это не формальность, а несущее условие, и вот почему. **Всё, чем «сборка вне
дерева» доказана сегодня, исполняется В МОНОРЕПО** — там, где рядом лежат файлы,
которых у арендатора нет. Проверка, прогнанная в монорепо, о клоне арендатора не
утверждает ничего: она отвечает на другой вопрос.

Поэтому **`Дано` каждого сценария ниже — чистый клон без монорепо рядом**, и это
записано в каждом сценарии дословно, а не подразумевается.

> [!important] «Под Ready» доказательством НЕ является — ни в одном сценарии
> Готовый под означает «процесс не падал», а не «посадка та, что объявлена».
> Этот класс уже стоил ложного зелёного: гейт посадки читал карту настроек вместо
> живого процесса и объявил стенд поднятым, пока служба на нём работала в другой
> посадке и держала незашифрованное соединение к своей базе
> (`.claude/rules/security.md` §«Гейт посадки проверяет ЖИВОЙ ПРОЦЕСС»).
>
> Ни одно «Тогда» этой приёмки не опирается на `Ready`. Каждое опирается на то,
> что **что-то производит текстом**: код возврата рецепта · собственное заявление
> процесса о посадке · отказ стража старта · ответ эндпоинта · `pg_stat_ssl` со
> стороны базы.

---

## 2. Замеры, на которых стоит приёмка

Числа сняты 2026-09-06 на: клон `PRO-Robotech/kaname` @ `5618c88`; дерево продукта
`PRO-Robotech/kacho` @ `ba79fdc519` (ветка `release/kaname`); воркспейс @ `84e635c9`.
Стенд — kind v0.32.0, Kubernetes v1.36.1, helm v4.2.4, docker 29.8.0.

Каждое число — с командой; **повторяемо**, а не пересказано.

| # | Величина | Значение | Предикат |
|---|---|---|---|
| З1 | Go-сборка клона | **rc=0** | `go build ./...` в клоне |
| З2 | Рецепт сборки клона | **rc=2** | `make` в клоне → `Makefile:13: ../../provenance.mk: нет такого файла` |
| З3 | Сборка образа из клона | **rc=0** | `docker build -t kacho-iam:dev .` в клоне |
| З4 | Проверка чарта | **rc=0** | `helm lint deploy/` |
| З5 | Рендер чарта | **rc=0** | `helm template kaname deploy/` |
| З6 | Установка в боевой посадке | **rc=0, `STATUS: deployed`** | `helm install kaname deploy/ -n kaname -f deploy/values.yaml -f deploy/values.prod.yaml` |
| З7 | Готовность выката после З6 | **rc=1**, `timed out waiting for the condition` | `kubectl -n kaname rollout status deploy/iam --timeout=20s` |
| З8 | Пробы чарта, прогнанные в клоне | **6 прошли, 3 упали** | `go test ./deploy/... -v`, счёт по `^--- PASS` / `^--- FAIL` |
| З9 | Заглушек `.example.invalid` в боевом профиле | **6** (плюс 1 вхождение в поясняющем комментарии) | `grep -n 'example\.invalid' deploy/values.prod.yaml` |
| З10 | Объектов Secret, которых чарт не заводит | **3** | `kacho-iam-server-tls`, `kacho-iam-authn`, `kacho-umbrella-pg-iam` |
| З11 | Стендов монорепо, ставящих чарт службы | **0** | `grep -c iam deploy/stacks.txt` в дереве продукта |
| З12 | Не-тестовых ссылок на чарт службы в дереве продукта | **1** (комментарий в его же `values.prod.yaml`) | `grep -rn 'services/iam/deploy' --include='*.yaml' --include='*.sh' --include='Makefile'` |
| З13 | Гейтов на переносимость РЕЦЕПТА сборки | **0** | `grep -rln 'provenance\.mk' --include='*_test.go'` → только `deploy/stand_provenance_declaration_test.go`, предмет которого — провенанс стенда, не переносимость рецепта |

### З2 — механизм назван, а не угадан

`provenance.mk` лежит в **корне монорепо**; `services/iam/Makefile` включает его как
`../../provenance.mk`, и из `services/iam/` это резолвится в корень. В клоне тот же
Makefile лежит **в корне**, поэтому `../../` уходит за пределы репозитория. Правка
формы не требуется — требуется решение, чем рецепт объявляет провенанс, когда
объявлять его нечем.

### Расхождение с исходной постановкой — названо, а не сглажено

Постановка называла три оси расхождения двух чартов (имя · версия · лицензия).
Перемер даёт другое, и разница существенна:

| Координата | имя | версия | лицензия |
|---|---|---|---|
| дерево продукта, `services/iam/deploy/Chart.yaml` | `kaname` | 0.1.0 | AGPL-3.0-or-later |
| дерево продукта, `deploy/helm/umbrella/charts/kaname/Chart.yaml` | `kaname` | 1.2.0 | BUSL-1.1 |
| **клон арендатора**, `deploy/Chart.yaml` | **`iam`** | 0.1.0 | AGPL-3.0-or-later |

В дереве продукта **сегодня** осей расхождения **две**, а не три: имя сведено
коммитом `b81adf2760` («служба управления доступом называет себя своим именем»).
Ось имени жива **между опубликованным артефактом и деревом продукта**: выгрузка
снята ДО переименования. Предикат: `git log -1 -S'name: kaname' -- services/iam/deploy/Chart.yaml`.

**Наблюдаемое следствие для арендатора, а не для нас** (замер З6): при имени релиза
`kaname` чарт производит `deployment.apps/iam`, `service/iam`, `configmap/iam-config`.
То есть `kubectl rollout status deploy/kaname` отвечает `NotFound`, и арендатор,
идущий за именем продукта, не находит его рабочей нагрузки.

---

## 3. Scope

| # | Что закрывается | Состояние |
|---|---|---|
| **F1** | Сборка вне дерева: рецепт и образ | сценарии ниже |
| **F2** | Подъём в БОЕВОЙ посадке: установка и готовность выката | сценарии ниже |
| **F3** | Предусловия, которых чарт не заводит | сценарии ниже |
| **F4** | Отказ восстанавливает следующий шаг арендатора | сценарии ниже |
| **F5** | Судьба двух чартов | сценарии ниже |
| **F6** | Свидетельства, которые арендатор прогоняет САМ | сценарии ниже |

---

## 4. Сценарий → производитель

**Раздел стоит ПЕРЕД сценариями намеренно.** Три приёмки этого корпуса подряд
получили `CHANGES REQUESTED` по одному классу — «Тогда» без производителя, — и
каждый круг стоит около получаса (`.claude/rules/multi-agent-flow.md` §«Класс,
отвергший работу ДВАЖДЫ, переезжает в форму задания»). Перепись производителей
сделана **первой, до единого сценария**.

**Координаты даны так, как их видит АРЕНДАТОР** — от корня его клона. Где полезно,
рядом назван адрес того же файла в дереве продукта.

| сценарий | что требует «Тогда» | производитель | координата у арендатора | состояние |
|---|---|---|---|---|
| SB-1-01 | код возврата Go-сборки | `go build` | — (инструмент) | есть |
| SB-1-02 | код возврата рецепта и текст отказа | `make` + `Makefile` | `Makefile` | есть, **производит отказ** |
| SB-1-03 | образ собран из клона | `docker build` + `Dockerfile` | `Dockerfile` | есть |
| SB-1-04 | рендер чарта | `helm template` | `deploy/` | есть |
| SB-1-05 | установка возвращает успех | `helm install` | `deploy/` | есть |
| SB-1-06 | выкат НЕ доходит до готовности | `kubectl rollout status` | — (инструмент) | есть |
| SB-1-07 | выкат доходит до готовности | `kubectl rollout status` | — (инструмент) | есть |
| SB-1-08 | отказ монтирования называет объект | kubelet, событие `FailedMount` | — (инструмент) | есть |
| SB-1-09 | отказ создания контейнера называет объект | kubelet, событие `Failed` | — (инструмент) | есть |
| SB-1-09а | умолчание имени объекта не называет наш релиз | `deploy/values.yaml` (`db.passwordSecretName`, строка 19) | `deploy/values.yaml` | есть, **производит наше имя** |
| SB-1-10 | накат схемы отвергает нешифрованную базу | бинарь `kacho-migrator` | `cmd/migrator` | есть |
| SB-1-11 | страж старта отказывает и называет КАЖДУЮ ручку | бинарь `kacho-iam`, разбор настроек | `cmd/kacho-iam` | есть |
| SB-1-11а | ключ каждого отказа находится в документации | таблица `INSTALL.md` §3 | `INSTALL.md` | есть |
| SB-1-12 | процесс печатает СВОЮ посадку | `kacho-iam`, первые строки лога | `cmd/kacho-iam` | **ждёт производителя** — задача-преемник **SB-1-T1** |
| SB-1-13 | база подтверждает шифрование | представление `pg_stat_ssl` самой базы | `INSTALL.md` §4 п.3 (запрос) | есть; **не наблюдался** — условие не создано |
| SB-1-14 | боевой профиль удовлетворяет стража | `TestProdProfile_SatisfiesTheBootGuard` | `deploy/prod_profile_test.go` | есть |
| SB-1-15 | страж жив без профиля (контроль) | `TestProdProfile_TheGuardIsLiveWithoutIt` | `deploy/prod_profile_test.go` | есть |
| SB-1-16 | каждая ручка профиля несущая | `TestProdProfile_EveryKnobIsLoadBearing` | `deploy/prod_profile_test.go` | есть |
| SB-1-17 | пробы клона прогоняются в клоне | `go test ./deploy/...` | `deploy/*_test.go` | есть, **производит 3 отказа** |
| SB-1-18 | имя чарта совпадает с именем продукта | — | — | **ждёт производителя** — задача-преемник **SB-1-T2** |
| SB-1-19 | опубликованный артефакт сходится с деревом продукта | — | — | **ждёт производителя** — задача-преемник **SB-1-T3** |
| SB-1-20 | рецепт сборки переносим (гейт на класс) | — | — | **ждёт производителя** — задача-преемник **SB-1-T4** |

**Итог переписи: сценариев 22; производитель есть у 18; `ждёт производителя` — 4.**
Сценарии, помеченные `ждёт производителя`, **покрытыми не считаются** и в счёт
готовности пункта линии не идут.

> [!important] «Производитель есть» и «наблюдалось» — РАЗНЫЕ оси, и смешивать их нельзя
> Первая редакция их смешала: SB-1-13 стоял в числе «есть», а его носитель был
> объявлен «условие не создано», — то есть перепись засчитывала в покрытие то,
> чего никто не видел. Колонка «состояние» говорит **только** о производителе:
> существует ли в дереве то, что эту величину порождает. Наблюдалась ли она в
> этой сессии — сказано в самом сценарии и, где нет, повторено в колонке словами
> «не наблюдался».

**Множество ID переписи и множество разделов тела совпадают в обе стороны.**
Предикат (обе команды обязаны давать пусто):

```sh
grep -oE '^\| SB-1-[0-9]+[а-я]?' ДОК | sed 's/^| //' | sort -u > /tmp/c
grep -oE '^### SB-1-[0-9/]+[а-я]?' ДОК | sed 's/^### //' \
  | tr '/' '\n' | sed 's/^\([0-9]\)/SB-1-\1/' | sort -u > /tmp/b
comm -23 /tmp/c /tmp/b   # в переписи, нет в теле
comm -13 /tmp/c /tmp/b   # в теле, нет в переписи
```

На первой редакции они давали **3** и **2**: SB-1-12, SB-1-13, SB-1-20 стояли в
переписи без тела, а SB-1-09а и SB-1-11а несли полный Given-When-Then и в
перепись не попали — причём SB-1-09а объявлял **собственный вердикт** «сценарий
красный», не будучи посчитанным нигде.

### Координаты производителей резолвятся в клоне — сверено машинно

Машинной сверки у этой таблицы нет (её предмет — **SB-1-T3**), но однократный
прогон сделан, и его результат стоит здесь **промежуточным свидетельством**, а не
заменой гейта: свидетельство верно на своей ревизии и стареет молча, гейт — нет.

Замер 2026-09-06 в клоне `PRO-Robotech/kaname` @ `5618c88`: все **7** координат
производителей резолвятся — `Makefile`, `Dockerfile`, `deploy/`, `cmd/migrator`,
`cmd/kacho-iam`, `deploy/prod_profile_test.go`, `INSTALL.md` (**7 из 7**).
Рядом: файлов проб чарта в клоне **5**, объявлений `func TestProdProfile_*` —
**3** (те самые SB-1-14/15/16). Предикат: `[ -e <путь> ]` по каждой координате;
`ls deploy/*_test.go | wc -l`; `grep -c '^func TestProdProfile_' deploy/*_test.go`.

### Почему эта таблица НЕ объявляет колонку «чем держится» — решение, а не небрежность

Проверка `scripts/docs-gate/check-03-holding-claim-resolves.py` берёт под надзор
таблицу, у которой первая колонка озаглавлена `кейс`/`сценарий` **и** есть колонка
`чем держится`, и требует, чтобы каждый путь резолвился **в индексе монорепо**.

Производители этой приёмки живут в **клоне арендатора**, и их координаты там
другие (`Makefile`, а не `services/iam/Makefile`). Объявить колонку `чем держится`
значило бы цитировать монорепо-координаты — то есть ровно то, что эта приёмка
объявляет негодным доказательством (§1). Поэтому колонка названа
**производитель**, надзор не включается, и это сказано вслух, чтобы не читалось
как обход проверки.

**Цена решения названа:** машинной сверки координат у этой таблицы нет. Закрывается
задачей-преемником **SB-1-T3** (проверка сходимости опубликованного артефакта с
деревом продукта), которой такая сверка и принадлежит по предмету.

> [!warning] Освобождение оказалось ШИРЕ своего довода — и ровно та проверка, что
> осталась за бортом, поймала бы расхождение
> Довод выше («координаты арендатора не резолвятся в индексе монорепо») верен и
> относится к требованию **P4** — резолву путей и имён проверок. Но у гейта
> требований шесть, и **P6** говорит о другом: множество кейсов таблицы обязано
> совпадать с множеством кейсов, объявленных сценариями. К монорепо-координатам
> P6 не имеет отношения **вовсе**, и никакой довод этой приёмки его не отменял.
>
> Цена измерена, а не предположена: расхождение **3 и 2**, названное выше, — это
> ровно то, что P6 объявляет находкой. Освобождение, взятое по одному требованию,
> молча накрыло все шесть; предмет запрета остался без надзора, и нашёл его
> человек, а не гейт.

**Отсюда норма для следующего, кто выведет таблицу из-под проверки:** довод
обязан быть предъявлен **по каждому требованию**, которое проверка несёт, а не по
тому, об которое споткнулся автор. Требование, не названное в доводе, из-под
надзора не выходит — оно просто остаётся непроверенным.

**Чем P6 держится здесь вместо гейта:** предикатом `comm` в обе стороны (§4,
выше). Он прогоняется руками и назван в предикате закрытия этой приёмки —
то есть свойство удержано **порядком работ**, а не обещанием.

### Задачи-преемники

> [!important] `SB-1-T1`…`SB-1-T4` — имена ЭТОГО документа, а не координаты трекера
> Задачи как issue **ещё не заведены**: предикат `gh issue list` по обоим
> репозиториям на 2026-09-06 даёт по ним пусто. Идентификаторы ниже существуют
> пока только здесь, и остатки О3/О4/О5 ссылаются на них **как на имена**.
> Номера появятся при посадке линии — тогда же эти имена и заменяются номерами.
> Сказано затем, чтобы читатель не искал их в трекере сейчас и не счёл их
> потерянными: приёмка называет предмет и предикат снятия, а координату ему даёт
> посадка.

| ID | Предмет | Предикат снятия |
|---|---|---|
| **SB-1-T1** | Процесс обязан печатать своё заявление о посадке первыми строками лога (режим · шифрование до базы · состояние взаимного TLS). `INSTALL.md` §4 п.2 это **обещает**; производителя в дереве нет. | Поднятый процесс печатает три величины; проба клона утверждает их наличие |
| **SB-1-T2** | Имя, которое чарт даёт рабочей нагрузке, обязано совпадать с именем продукта | `helm install <rel>` производит `deployment.apps/<имя продукта>`; проба утверждает |
| **SB-1-T3** | Опубликованный артефакт обязан сходиться с деревом продукта по имени, версии и лицензии чарта | Гейт сравнивает `Chart.yaml` артефакта и `services/iam/deploy/Chart.yaml`; расхождение — находка |
| **SB-1-T4** | Гейт на переносимость РЕЦЕПТА сборки: рецепт службы, выносимой отдельным продуктом, не вправе включать файл вне своего каталога | `grep '^include \.\./' services/*/Makefile` → пусто; гейт доказан инъекцией в обе стороны |

---

## F1 — Сборка вне дерева: рецепт и образ

**Контекст.** «Собирается вне дерева» распадается на три разных утверждения, и
сегодня они дают три разных ответа. Различать их обязательно: одно зелёное
закрывало собой два других.

### SB-1-01 (положительный) — исходники компилируются

- **Given** чистый клон `PRO-Robotech/kaname`; монорепо рядом нет; каталог `../..` репозиторию не принадлежит
- **When** арендатор выполняет `go build ./...`
- **Then** код возврата **0**, и ни одна ошибка не называет пути вне клона

### SB-1-02 (отрицательный, КРАСНЫЙ сегодня) — рецепт сборки отказывает первой строкой

- **Given** тот же клон
- **When** арендатор выполняет `make` — первую команду, которую называет всякий рецепт
- **Then** код возврата **0**
- **Сегодня**: код возврата **2**, отказ `Makefile:13: ../../provenance.mk: нет такого файла`. Сценарий красный **по существу**, а не по форме: производитель (`Makefile`) есть и производит отказ.
- **Положительный близнец** (обязателен, иначе отрицание зеленело бы на пустом дереве): `make` в дереве продукта из `services/iam/` завершается успехом — то есть отказ даёт **место**, а не поломка рецепта

### SB-1-03 (положительный) — образ собирается из клона

- **Given** тот же клон; docker доступен
- **When** `docker build -t kacho-iam:dev .`
- **Then** код возврата **0**, образ существует
- **Замечание для рецензента:** это и есть та половина, из-за которой пункт линии выглядел закрытым. `Dockerfile` несёт `COPY . .` и самодостаточен; рецепт — нет. Утверждения «собирается» у них **разные**, и зелёное одного не переносится на другое.

### SB-1-20 (**ждёт производителя SB-1-T4**) — переносимость рецепта держится гейтом, а не вниманием

- **Given** дерево продукта; служба объявлена выносимой отдельным продуктом
- **When** прогоняется гейт переносимости рецепта
- **Then** рецепт службы не включает файлов вне своего каталога:
  `grep -l '^include \.\./' services/*/Makefile` → пусто
- **Сегодня**: гейта нет (замер З13), и его отсутствие — причина, по которой З2
  дожил до выгрузки. Класс «собирается у нас, не собирается у арендатора»
  наблюдаем только там, где кто-то попробует собрать вне дерева; вниманием он не
  держится, потому что в монорепо рецепт исправен **всегда**.
- **Почему это сценарий, а не строка остатка.** Остаток О1 говорит «рецепт не
  переносим» — про **сегодняшнее состояние дерева**. Этот сценарий говорит
  «переносимость удержана **впредь**» — про механизм. Починка О1 без SB-1-T4
  закрывает экземпляр и оставляет класс: следующая выносимая служба принесёт его
  снова, и снова незамеченным.

---

## F2 — Подъём в БОЕВОЙ посадке

**Контекст.** Рендер-зелёный ≠ поднимается. Стражи старта отказывают на неполной
конфигурации, поэтому чарт, который рендерится, в боевой посадке уходит в цикл
перезапусков (`.claude/rules/security.md` §«`values.prod` ОБЯЗАН реально boots»).

### SB-1-04 (положительный) — чарт рендерится

- **Given** тот же клон
- **When** `helm lint deploy/` и `helm template kaname deploy/`
- **Then** оба кода возврата **0**

### SB-1-05 (положительный) — установка принимается

- **Given** тот же клон; пустой кластер; предусловия F3 **не созданы**
- **When** `helm install kaname deploy/ -n kaname -f deploy/values.yaml -f deploy/values.prod.yaml`
- **Then** код возврата **0**, `STATUS: deployed`

### SB-1-06 (отрицательный) — «установлено» НЕ означает «поднялось»

- **Given** состояние после SB-1-05
- **When** `kubectl -n kaname rollout status deploy/iam --timeout=20s`
- **Then** код возврата **1**, текст `timed out waiting for the condition`
- **Положительный близнец — SB-1-07**, и отличие ровно одно: предусловия F3 созданы. Без него отрицание зеленело бы на любом отказе выката.
- **Утверждается ТЕКСТ, а не только код возврата, и это несущее различие.** `rollout status deploy/iam` даёт rc=1 с `timed out waiting for the condition` — выкат существует и не сходится; `rollout status deploy/kaname` даёт rc=1 с `NotFound` — рабочей нагрузки с таким именем нет вовсе (SB-1-18). Два разных предмета под одним кодом возврата: сверять надо текст.
- **Зачем этот сценарий.** Он закрепляет **различие** между двумя исходами, которые арендатор иначе прочтёт как один: `helm install` сказал «Install complete», и никакая строка вывода helm не сообщает, что под не поднимется никогда. Утверждать надо выкат, а не установку.

### SB-1-07 (положительный, ЖДЁТ УСЛОВИЯ) — выкат доходит до готовности

- **Given** тот же клон; предусловия F3 созданы **все**; PostgreSQL 16+ **с TLS**; внешний поставщик удостоверений доступен по трём объявленным адресам; шесть заглушек `.example.invalid` заменены (замер З9)
- **When** `kubectl -n kaname rollout status deploy/<имя> --timeout=300s`
- **Then** код возврата **0**
- **И** выполняются SB-1-12 (заявление процесса о посадке) и SB-1-13 (подтверждение со стороны базы) — они объявлены **отдельными сценариями** ниже, а не клаузами здесь
- **Условие не создано в этой сессии:** внешний поставщик удостоверений не поднимался. Это **«не выполнилось»**, а не красный вердикт: о поведении в полной боевой посадке сценарий вердикта не имеет. Как далеко дошло — SB-1-08…SB-1-11.

### SB-1-12 (**ждёт производителя SB-1-T1**) — процесс объявляет СВОЮ посадку

- **Given** условия SB-1-07 созданы; процесс стартовал
- **When** арендатор читает первые строки лога службы
- **Then** служба печатает три величины: режим · шифрование до собственной базы ·
  состояние взаимного TLS обоих слушателей
- **И** напечатанное совпадает с тем, что арендатор задал профилем
- **Сегодня**: `INSTALL.md` §4 п.2 это **обещает** («В первых строках лога служба
  печатает режим, режим шифрования до базы и состояние взаимного TLS»), а
  производителя в дереве нет. Обещание документа производителем не является.
- **Почему это отдельный сценарий, а не клауза SB-1-07.** Заявление процесса — то
  единственное, что отличает «посадка та, что объявлена» от «под не падал», и это
  предмет всей врезки §1. Клауза внутри чужого сценария наследует его состояние:
  пока SB-1-07 «условие не создано», отдельного вердикта у неё нет, а в переписи
  она числится покрытой.

### SB-1-13 (положительный, УСЛОВИЕ НЕ СОЗДАНО) — шифрование подтверждает БАЗА, а не служба

- **Given** условия SB-1-07 созданы; PostgreSQL поднят **с TLS**
- **When** арендатор спрашивает **базу**, а не службу:

  ```sql
  SELECT ssl, version FROM pg_stat_ssl s
    JOIN pg_stat_activity a USING (pid)
   WHERE a.datname = current_database() AND a.usename = 'iam';
  ```
- **Then** `ssl = true`
- **Производитель есть и он НЕ наш**: представление `pg_stat_ssl` принадлежит
  PostgreSQL. Именно поэтому свидетельство годно — настройкой нашего чарта его
  подделать нельзя, и это ровно тот класс независимого подтверждения, которого
  требует `.claude/rules/security.md` §«Гейт посадки проверяет ЖИВОЙ ПРОЦЕСС».
  `INSTALL.md` §4 п.3 даёт запрос, но производителем является база.
- **Не наблюдался в этой сессии**: база поднималась без TLS (это дало SB-1-10).
  «Производитель есть» и «величина наблюдалась» — разные оси; смешение их и было
  дефектом первой редакции переписи.

---

## F3 — Предусловия, которых чарт не заводит

**Контекст.** Боевой профиль ссылается на **три** объекта Secret (замер З10),
которых чарт не создаёт и не может: два несут материал удостоверяющего центра и
секреты арендатора, третий — пароль его базы. Отказ наступает **до старта
процесса**, поэтому ни одна строка, которую печатает служба, до арендатора не
доходит: процесса ещё нет.

> [!important] Это ЧЕТВЁРТАЯ стадия отказа, а `INSTALL.md` §5 называет три
> Документ прямо предупреждает, что перечень §3 покрывает только стадию
> **настройки**, и называет ещё две — **посадку** и **сборку**. Все три судит
> процесс. Отказ ниже случается раньше их всех: объекта нет, том не смонтирован,
> контейнер не создан. Раздел «Как читать отказ» на него не отвечает, потому что
> читать нечего.

### SB-1-08 (отрицательный) — недостающий материал TLS называется по имени

- **Given** клон; установка по SB-1-05; объектов Secret нет
- **When** арендатор смотрит события пода
- **Then** событие `FailedMount`, дословно: `MountVolume.SetUp failed for volume "tls-server" : secret "kacho-iam-server-tls" not found`
- **И** имя объекта названо — то есть предмет восстановим

### SB-1-09 (отрицательный) — недостающий пароль базы называется по имени

- **Given** объект `kacho-iam-server-tls` создан; остальные — нет
- **When** арендатор смотрит события пода
- **Then** событие `Failed`, дословно: `spec.initContainers{migrate}: Error: secret "kacho-umbrella-pg-iam" not found`

### SB-1-09а (отрицательный) — умолчание имени объекта не вправе называть НАШ стенд

- **Given** клон; `deploy/values.yaml`
- **When** арендатор читает `db.passwordSecretName`
- **Then** умолчание не содержит имени нашего релиза
- **Сегодня**: умолчание — `kacho-umbrella-pg-iam`, то есть имя объекта **зонтичного релиза монорепо**. У арендатора зонтичного релиза нет; координата нашего стенда уехала в поставляемый артефакт умолчанием. Сценарий красный.
- **Положительный близнец:** `db.host`/`db.name` — те же значения осмысленны у арендатора и переопределяются штатно; то есть красное даёт **имя нашего релиза**, а не сам факт умолчания.

### SB-1-10 (отрицательный) — накат схемы отвергает нешифрованную базу

- **Given** все три объекта Secret созданы; PostgreSQL поднят **без TLS**; профиль несёт `repository.postgres.sslMode: require`
- **When** запускается контейнер инициализации `migrate`
- **Then** он завершается с отказом, дословно:

  ```
  Error: database connection check failed: failed to connect to `user=iam database=kacho_iam`:
  10.96.235.25:5432 (kacho-umbrella-pg-iam): tls error: server refused TLS connection
  ```
- **И** под НЕ становится готовым
- **Положительный близнец:** та же установка против базы **с TLS** — накат проходит, и отказ приходит уже с другой стадии. Отличие ровно одно: предлагает ли база шифрование. Без близнеца отрицание зеленело бы и на базе, недоступной вовсе.
- **Зачем.** Это единственное в приёмке **наблюдённое** подтверждение, что боевая посадка исполняется, а не только объявлена: `sslMode: require` отвергает базу, которая шифрование не предлагает. Проверка не подделывается настройкой — отказ приходит со стороны соединения.

---

## F4 — Отказ восстанавливает следующий шаг

**Контекст.** Отказ, называющий предмет, но не говорящий, что делать дальше, —
находка (`.claude/rules/00-kacho-core.md` ban #18, ось «разработчик»). Ниже —
проверка этого свойства на самом строгом месте: страже старта.

### SB-1-11 (положительный) — страж старта называет КАЖДУЮ ручку и способ её задать

- **Given** образ, собранный по SB-1-03; ни одна обязательная величина не задана
- **When** `docker run --rm kacho-iam:dev`
- **Then** код возврата **1**, процесс **не стартует**
- **И** отказ приходит **одним списком всех найденных проблем**, а не по одной за перезапуск
- **И** каждая строка называет **ключ настройки И переменную окружения** — то есть «как задать», а не только «что не так». Наблюдено дословно, например: `production mode: authn.hook-shared-secret is empty (set authn.hook-shared-secret-env / KACHO_IAM_HOOK_TOKEN)`
- **И** каждая строка называет **почему**, а не только «обязательно». Наблюдено дословно: `authn.identity-provider is not declared (allowed, verbatim: external, own) — this field decides whether the identity provider's addresses are required at all, and there is no default in code on purpose: defaulting one way would demand addresses of a profile that has none, and the other way would silently drop those demands from a profile nobody updated`
- **И** отказ **не печатает значений секретов** — только имена ручек (`INSTALL.md` §5 «Чего в отказе не будет»)

**Вердикт по F4 — ОБЛАСТЬЮ, а не целиком.** У отказа стража старта (SB-1-11)
свойство **выполняется**, и вот число: из **5** ключей, названных отказом,
в таблице `INSTALL.md` §3 находятся **5** (`api-server.registry-token.service` 2
вхождения, `authn.trusted-forwarder-sans` 2, `authn.hook-shared-secret` 1,
`authn.jwks-encryption-key-hex` 1, `authn.identity-provider` 1). Каждая строка
отказа называет предмет, способ задать и причину — все три оси сразу.

У отказа **предусловий** (F3, SB-1-08/09) свойство **не выполняется**: состав
объекта не восстановим ниоткуда — **0** из 3 объектов и **0** из 3 их ключей
названы в `INSTALL.md` (замер и предикат — О7, §6). Обобщать вердикт SB-1-11 на
весь F4 значило бы заявить шире сделанного: производители у двух отказов разные,
и меряется каждый отдельно.

### SB-1-11а (отрицательный) — обещание `INSTALL.md` о стадиях отказа исполнимо

- **Given** тот же образ
- **When** арендатор идёт за отказом в `INSTALL.md` §3
- **Then** отказ, полученный в SB-1-11, находится по названному ключу настройки
- **Сегодня** исполнимо для стадии **настройки**; для стадий **посадки** и **сборки** документ честно предупреждает, что перечня нет. Отказ F3 (объект Kubernetes отсутствует) не покрыт ни одной из трёх стадий — предмет **SB-1-T1** в части наблюдаемости и отдельной строкой ниже в «Что остаётся».

---

## F5 — Судьба двух чартов

**Контекст и решение.** Чартов службы в дереве продукта **два**, и они расходятся
по двум осям (замер §2). Опубликованный артефакт получил **тот, который не ставит
ни один стек**: `deploy/stacks.txt` не называет его ни разу (З11), а не-тестовых
ссылок на него в дереве продукта **одна** — комментарий в его же `values.prod.yaml`
(З12). Все проверочные наборы чартов рендерят **вендоренный** `charts/kaname`.

**РЕШЕНИЕ (предлагается этой приёмкой; принимает владелец):** к арендатору едет
**`services/iam/deploy`** — тот, что уже уехал. Довод — не инерция, а предмет:

| ось | `services/iam/deploy` | `deploy/helm/umbrella/charts/kaname` |
|---|---|---|
| адресат | **одиночная установка** в чужом кластере | сабчарт нашей умбреллы |
| несёт ли боевой профиль отдельного продукта | **да**, `values.prod.yaml` со стражем каждой ручки | нет: посадку задаёт умбрелла |
| зависит ли от `global.*` умбреллы | **нет** | да |
| оставляет ли пространство имён ставящему | **да**, `TestChartTemplatesLeaveTheNamespaceToWhoeverInstalls` | не предмет |
| ставится ли нашими стендами | нет (З11) | да |

То есть «не ставится ни одним стеком» — не дефект выбора, а **следствие адресата**:
этот чарт и не должен ставиться нашей умбреллой. Дефект в другом — в том, что
**никто не ставит его вовсе**, поэтому его боевая посадка не имела производителя
до этой приёмки.

**Расхождение по двум осям снимается так:**

| ось | решение |
|---|---|
| **имя** | ось уже сведена в дереве продукта (`b81adf2760`); в артефакте — нет. Снимается пересъёмкой артефакта; удержание — **SB-1-T3** |
| **версия** (0.1.0 против 1.2.0) | **не сводится**: у чартов разные адресаты и разные жизненные циклы. Версия чарта отдельного продукта — версия **этого продукта**, версия сабчарта — версия его места в умбрелле. Объявляется решением, а не выравнивается |
| **лицензия** (AGPL-3.0-or-later против BUSL-1.1) | **не сводится и не является расхождением**: `services/iam/deploy` едет в публикуемый продукт под его лицензией, сабчарт остаётся частью монорепо под её лицензией. Один файл — один адресат — одна лицензия |

### SB-1-18 (**ждёт производителя SB-1-T2**) — имя рабочей нагрузки совпадает с именем продукта

- **Given** клон; установка релиза с именем `kaname`
- **When** `kubectl -n <ns> get deploy -o name`
- **Then** имя рабочей нагрузки — имя продукта
- **Сегодня**: `deployment.apps/iam` (замер §2). Производителя, который сравнивал бы эти два имени, в дереве нет.

### SB-1-19 (**ждёт производителя SB-1-T3**) — артефакт сходится с деревом продукта

- **Given** опубликованный артефакт и `services/iam/deploy` дерева продукта
- **When** сравниваются имя, версия и лицензия чарта
- **Then** расхождений нет
- **Сегодня**: расходится имя (`iam` против `kaname`). Производителя нет.

---

## F6 — Свидетельства, которые арендатор прогоняет САМ

**Контекст.** Приёмка обязана назвать, что арендатор может проверить **у себя**,
без нашего стенда и без нашего конвейера. Это и есть проверяемая половина пункта
«поднимается вне дерева».

### SB-1-14/15/16 (положительные) — боевой профиль проверяется в клоне

- **Given** чистый клон; монорепо рядом нет
- **When** `go test ./deploy/... -run TestProdProfile`
- **Then** проходят все три: `TestProdProfile_SatisfiesTheBootGuard` (профиль удовлетворяет стража), `TestProdProfile_TheGuardIsLiveWithoutIt` (**положительный контроль**: без профиля страж отказывает — иначе первый зеленел бы на выключенном страже), `TestProdProfile_EveryKnobIsLoadBearing` (снятие любой ручки роняет посадку)
- **Замечание.** Пара «удовлетворяет» + «страж жив без него» — ровно та двусторонняя форма, которой требует `.claude/rules/testing.md` §«Гейт на класс» п.2. Она уже в клоне и уже работает.

### SB-1-17 (отрицательный, КРАСНЫЙ сегодня) — поставленные пробы прогоняются там, куда поставлены

- **Given** чистый клон; монорепо рядом нет
- **When** `go test ./deploy/...`
- **Then** код возврата **0**
- **Сегодня**: **6 прошли, 3 упали** (замер З8). Упавшие отказывают не по существу, а **структурно** — они адресуют дерево, которого у арендатора нет:
  - `open ../../../services/iam/Dockerfile: no such file or directory` — проба поднимается на три уровня вверх, в раскладку монорепо;
  - `осмотрено 0 чартов служб с базой — обход беспредметен, вердикт недействителен`;
  - `no production-class stack was examined — the census read nothing and would report success no matter what the profiles say`.
- **Это и есть предмет всей приёмки в одном месте.** Пробы **честно** объявляют свой обход беспредметным вместо того, чтобы промолчать зелёным, — то есть ведут себя правильно. Неверна не проба, а то, что она **поставлена** туда, где её предмета нет.
- **Исходов три, четвёртого нет** (ban #11): научить пробу читать свой клон · **не поставлять** её арендатору · завести предмет с предикатом снятия. «Оставить как есть» исходом не является: арендатор, прогнавший `go test ./...` в свежем клоне, получает красное и не может отличить его от дефекта продукта.
- **Положительный близнец — те же 6 прошедших проб в том же прогоне.** Он несущий: без него красное было бы неотличимо от «обход не состоялся вовсе» (нет Go, нет модуля, каталог переехал). Шесть зелёных доказывают, что обход **живой**, а падают ровно те три, чей предмет лежит вне клона. Предикат: `go test ./deploy/... -v`, счёт по `^--- PASS` и `^--- FAIL`.
- **Прецедент того же выбора уже есть в дереве:** `workload_identity_single_source_test.go` лежит в `services/iam/deploy/` и в артефакт **не поехал** (сверка перечней файлов: у продукта 14, у арендатора 13). То есть механизм «не поставлять» существует и применён — просто применён не ко всем трём.

---

## 5. Что арендатор делает своими руками: от клона до первого успешного вызова

Перечень — **измеренный**, а не предполагаемый: шаги 1–3, 5–7, 9 исполнены в этой
сессии; шаги 4, 8, 10, 11 прочитаны в `INSTALL.md` и **не исполнялись** (условие не
создано — внешний поставщик удостоверений не поднимался).

| # | Шаг | Исполнено здесь |
|---|---|---|
| 1 | `git clone` опубликованного репозитория | да |
| 2 | Собрать образ: `docker build -t kacho-iam:dev .` (**не** `make` — З2) | да |
| 3 | Поднять PostgreSQL 16+ **с TLS** (боевой профиль несёт `sslMode: require`) | поднят **без** TLS — дал SB-1-10 |
| 4 | Поднять внешнего поставщика удостоверений (Kratos + Hydra) либо выбрать посадку `own` | нет |
| 5 | Создать Secret с паролем базы (умолчание имени — `kacho-umbrella-pg-iam`, SB-1-09а) | да |
| 6 | Создать Secret `kacho-iam-server-tls` (`tls.crt`, `tls.key`, `ca.crt`) | да |
| 7 | Создать Secret `kacho-iam-authn` (общий сговор хуков, ключ обёртки ключницы) | да |
| 8 | Заменить **6** заглушек `.example.invalid` в `values.prod.yaml` (З9) | нет |
| 9 | `helm install … -f values.yaml -f values.prod.yaml` | да |
| 10 | Подать модель доступов манифестом (`MODEL-MANIFEST.md`) | нет |
| 11 | Проверить исход по `INSTALL.md` §4: миграции · заявление о посадке · `pg_stat_ssl` · метрики · первый вызов через свой край | частично (п. 1–3 недостижимы без шага 4) |

**Число шагов — 11.** Из них **7** — заведение условий, которых чарт не создаёт и
создать не может: это шаги **3, 4, 5, 6, 7, 8, 10** (база с TLS · поставщик
удостоверений · три объекта Secret · шесть заглушек · модель доступов). Остальные
четыре — 1, 2, 9, 11 — клон, сборка, установка и проверка. **Ни одно из семи не
проверяется до установки**: `helm install`
принимает конфигурацию, ссылающуюся на три несуществующих объекта, и возвращает
`STATUS: deployed` (З6).

**Это и есть мера удобства, и её надо знать до того, как её посчитает клиент.**
Она не «плохая» сама по себе — служба управления доступом в чужом облаке
обязана требовать материал удостоверяющего центра и поставщика личности. Но
одиннадцать шагов, из которых семь молчаливо-обязательные, — это число, которое
продукт обязан либо сократить, либо **назвать заранее**. Сегодня `INSTALL.md` §1
называет предусловия **классами** («внутренний удостоверяющий центр»), а не
объектами, которые арендатор обязан создать: предикат `grep 'kacho-iam-server-tls'
INSTALL.md` даёт **0**.

---

## 6. Что остаётся — предметом, а не обещанием

Ни один пункт ниже не отложен «на потом»: у каждого назван предикат снятия
(ban #11 — сделать сейчас · снять вместе с предметом · завести ПРЕДМЕТ).

| # | Остаток | Предикат снятия |
|---|---|---|
| О1 | Рецепт сборки не переносим (З2) | `make` в чистом клоне → rc=0; удержание — **SB-1-T4** |
| О2 | Три поставленные пробы структурно неисполнимы в клоне (З8) | `go test ./deploy/...` в чистом клоне → rc=0 |
| О3 | Имя рабочей нагрузки не совпадает с именем продукта | **SB-1-T2** |
| О4 | Артефакт разошёлся с деревом продукта по имени чарта | **SB-1-T3** |
| О5 | Заявление процесса о посадке обещано `INSTALL.md` §4 и не имеет производителя | **SB-1-T1** |
| О6 | Умолчание имени объекта с паролем базы называет наш зонтичный релиз | `grep 'kacho-umbrella' deploy/values.yaml` → 0 |
| О7 | `INSTALL.md` не называет ни один из трёх объектов Secret и ни один их ключ | предикат ниже: **три имени И состав каждого**, состав засчитывается только рядом со своим объектом |
| О8 | Полная боевая посадка (SB-1-07) не наблюдалась: условие не создано | Прогон с поставщиком удостоверений; `rollout status` → rc=0 **и** `pg_stat_ssl.ssl = true` |

### Предикат О7 — почему счёт по слову НЕ ГОДИТСЯ, и это измерено

Первая редакция закрывала О7 предикатом `grep -c 'kacho-iam-server-tls' INSTALL.md`
→ ≥1. Он зеленеет от **одного** упоминания **одного** имени из трёх — то есть не
способен обнаружить собственный предмет.

Расширение «три имени и их ключи счётом вхождений» тоже негодно, и вот число:
три токена из шести в `INSTALL.md` **уже встречаются** — `password` (строка 167),
`hook-shared-secret` (90), `jwks-encryption-key-hex` (91), — но **в другом
значении**: это ключи НАСТРОЙКИ с переменными окружения, а не ключи данных внутри
объекта Secret. Счёт по слову на фикстуре «имена названы, состав не назван» даёт
**6 токенов из 6** и говорит ЗЕЛЁНОЕ, тогда как предмета нет. Это класс
`.claude/rules/testing.md` §«Гейт на класс» п.7 — распознаватель судит по слову,
а не по тому, о чём слово сказано.

**Годный предикат** требует имя объекта И его состав **рядом** (±12 строк):

```sh
check() { obj=$1; shift
  ln=$(grep -n -- "$obj" INSTALL.md | head -1 | cut -d: -f1)
  [ -n "$ln" ] || { echo "НЕТ объекта: $obj"; return 1; }
  for k in "$@"; do
    sed -n "$((ln>12?ln-12:1)),$((ln+12))p" INSTALL.md | grep -q -- "$k" \
      || echo "$obj назван, состав не назван: $k"
  done; }
check kacho-iam-server-tls  tls.crt tls.key ca.crt
check kacho-iam-authn       hook-shared-secret jwks-encryption-key-hex
check kacho-umbrella-pg-iam password
```

**Доказан в три стороны, а не в две** (замер 2026-09-06):

| прогон | вход | исход |
|---|---|---|
| A | `INSTALL.md` как есть | **красное**: «объектов названо 0 из 3, недостач 3» |
| B | тот же файл плюс таблица «объект → ключи» | **зелёное**: 3 из 3, недостач 0 |
| C | тот же файл плюс имена объектов **без** состава | **красное**: 3 из 3 названо, недостач **6** |

Третий прогон — несущий: на нём счёт по слову даёт зелёное, а этот предикат
красное. Без него «расширенный» предикат был бы неотличим от годного.

### Что О7 значит для F4 — и почему это не отдельная придирка

F4 объявлен выполненным по стражу старта (SB-1-11), и это верно: он называет
ручку, способ её задать и причину. Но **отказ F3 — другой**, и по нему свойство
F4 сегодня НЕ выполняется целиком: арендатор, получив дословное
`secret "kacho-iam-server-tls" not found`, знает **имя** объекта и не может
узнать его **состав** — ни из отказа (kubelet состава не знает), ни из
`INSTALL.md` (замер А выше: 0 из 3). Следующий шаг восстановлен наполовину:
«создайте объект» ясно, «с какими ключами» — нет.

Это не смягчает вердикт по SB-1-11 и не отменяет его: у двух отказов **разные
производители**, и свойство у каждого меряется отдельно. Сказано здесь затем,
чтобы «F4 выполняется» не читалось шире сделанного.

> [!note] О8 — это «не выполнилось», а не красный вердикт
> Внешний поставщик удостоверений в этой сессии не поднимался, поэтому о поведении
> службы в **полной** боевой посадке вердикта нет — ни зелёного, ни красного. Третья
> категория в успех не засчитывается и из вердикта не вычитается
> (`.claude/rules/testing.md` §«Чтение вердикта»). Наблюдённое — цепочка
> SB-1-08…SB-1-11: установка принимается, выкат до готовности не доходит, отказы
> называют предмет.

---

## 7. Трассировка

| Требование | Где закрыто |
|---|---|
| `Дано` — чистый клон без монорепо и без нашей инфраструктуры | §1; повторено в `Given` каждого сценария |
| каждое «Тогда» имеет производителя в дереве | §4, перепись: 22 сценария, 18 с производителем, 4 `ждёт производителя`; множества ID переписи и тела сверены `comm` в обе стороны |
| ни одно «Тогда» не опирается на «под Ready» | §1, врезка; SB-1-06 закрепляет различие установки и выката |
| подъём в боевой посадке (`helm install` + готовность выката) | F2, SB-1-05…SB-1-07 |
| шаги арендатора с числом | §5, **11 шагов** |
| отказ восстанавливает следующий шаг | F4, SB-1-11 — **областью**: у стража старта выполняется (5 из 5 ключей), у отказа предусловий нет (0 из 3 объектов), см. О7 |
| судьба двух чартов | F5, решение и таблица по трём осям |
| раздел «сценарий → производитель» перед сценариями | §4 |
