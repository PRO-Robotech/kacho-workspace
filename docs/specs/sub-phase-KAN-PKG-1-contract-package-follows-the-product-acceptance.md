# Sub-phase KAN-PKG-1 — пакет контракта доступа следует за продуктом: приёмка

**Статус:** DRAFT (раунд 1) — ожидает вердикта `acceptance-reviewer`.
**Ревьюер:** `acceptance-reviewer` — единственный gate APPROVED (не заказчик).
**Задача:** `PRO-Robotech/kacho#2133`. **Решение, которое исполняется:** Р14 замысла
перехода (`docs/changes/standalone-iam/design.md` §7.4) — здесь оно не пересматривается и
не пересказывается.
**Пакет изменения:** `docs/changes/standalone-iam/`.
**Дерево продукта, на котором сняты ВСЕ числа:** `PRO-Robotech/kacho` @
`1ef0fc968f`, ветка `release/kaname` (та же ревизия, на которой снят замер §7.4 замысла).
**Дерево воркспейса:** `PRO-Robotech/kacho-workspace`, ветка `lane/acc-2133` от `main`.

---

## О чём этот документ и о чём он НЕ

Р14 решило **направление**: имя пакета контракта доступа переезжает вместе со службой, а
конкретное имя решает веха. Веха — эта. Документ решает **имя** и **разрез**, и описывает
**наблюдаемое поведение** каждого перехода: что видит вызывающий по gRPC, оператор, край,
соседняя служба и проверка дерева.

**Документ НЕ утверждает:**

- что переход исполнен — на названной ревизии не переименовано ни одно объявление
  (`git grep -c '^package kacho.cloud.iam.v1;' -- 'proto/kacho/cloud/iam/**/*.proto' | wc -l`
  → **39** из 39);
- что у каждого сценария есть производитель. Перепись производителей идёт **§2, до единого
  сценария**; сценарии без производителя помечены `ждёт производителя`, **в покрытие не
  засчитываются**, и по каждому назван третий исход — задача-преемник с номером;
- что решения соседних приёмок этой линии верны. Одно из них **опровергнуто замером**, и
  это сказано прямо в §3 вместе с задачей на приведение к факту.

**Тон отказов и REST-пути арендатора этот документ не меняет.** Пути объявлены `/iam/v1/…`
и бренда не несут — они остаются дословно; это положительный близнец, а не умолчание.

---

## 1. Перепись — числа задачи против собственного замера

Единица счёта названа у каждой строки: две переписи одного предмета расходятся ровно
тогда, когда единицы разные, и различить это можно только назвав обе. Ревизия одна на всю
таблицу и стоит в шапке. `И<n>` — идентификатор замера этого документа.

| И | величина | задача #2133 | мой замер | предикат | сошлось? |
|---|---|---:|---:|---|---|
| **И1** | `.proto` с объявлением пакета | 39 | **39** | `git grep -c '^package kacho.cloud.iam.v1;' -- 'proto/kacho/cloud/iam/**/*.proto' \| wc -l` | да |
| **И2** | `.proto` в каталоге контракта всего | — | **39** | `git ls-tree -r --name-only <rev> -- proto/kacho/cloud/iam \| grep -c '\.proto$'` | все 39 несут пакет |
| **И3** | объявлений `go_package` / различных форм | 39 / 1 | **39 / 1** | `git grep -c 'option go_package' -- 'proto/kacho/cloud/iam/**/*.proto' \| wc -l`; `… \| sort -u \| wc -l` | да |
| **И4** | самоимпортов (строк `import "kacho/cloud/iam/…"`) | 26 | **26** | `git grep -h '^import' -- 'proto/kacho/cloud/iam/**/*.proto' \| grep -c 'kacho/cloud/iam/'` | да |
| **И5** | порождённых файлов под `pkg/api/kacho/cloud/iam` | 84 (83+1) | **83** + **1** в `pkg/api/kacho/iam` | `git ls-tree -r --name-only <rev> -- pkg/api/kacho/cloud/iam \| wc -l` | да, и один — чужой (см. ниже) |
| **И6** | прод-`.go` **вне** `pkg/api`, импортирующих стабы | 182 | **142** | `git grep -l 'pkg/api/kacho/cloud/iam/v1' -- '*.go' ':!*_test.go' ':!pkg/api/**' \| wc -l` | **нет — разные единицы** |
| **И7** | тестовых `.go`, импортирующих стабы | — | **230** | тот же предикат по `'*_test.go'` | вне единицы задачи |
| **И8** | записей каталога решений с именем iam | 190 | **116** в **каждой** из **2** байт-идентичных копий | `grep -c '"fqn": "kacho\.cloud\.iam\.v1\.' gateway/internal/middleware/embed/permission_catalog.json` | **нет — предикат задачи каталога не читает** |
| **И9** | всего записей в каталоге | — | **350** | `grep -c '"fqn":' …/permission_catalog.json` | — |
| **И10** | записей allowlist края (прод) | — | **78** | `grep -c '/kacho\.cloud\.iam\.v1\.' gateway/internal/allowlist/list.go` | — |
| **И11** | строк порождённой таблицы REST-маршрутов | — | **104** из 308 | `grep -c 'kacho\.cloud\.iam\.v1' gateway/internal/middleware/rest_route_table_gen.go` | — |
| **И12** | файлов, произносящих полное имя метода | 108 | **108** | `git grep -l '/kacho\.cloud\.iam\.v1\.' \| wc -l` | да |
| **И13** | вхождений точечной формы по дереву / файлов | — | **3101** / **319** | `git grep -o 'kacho\.cloud\.iam\.v1' -- . \| wc -l`; `… -l \| wc -l` | — |
| **И14** | файлов **консоли**, знающих имя | — | **18** | `git grep -l -E 'kacho[./]cloud[./]iam' -- 'ui-future/**' \| wc -l` | вне перечня задачи |
| **И15** | файлов **гейтов дерева**, знающих имя | 2 | **31** (из них не-тестовых **6**) | `git grep -l -E 'kacho\.cloud\.iam\.v1\|kacho/cloud/iam/v1' -- internal/repohygiene tools \| wc -l` | **нет — разные единицы** |
| **И16** | клиентских страниц службы: вхождений / файлов | 3 / 2 | **3** / **2** | `git grep -c 'kacho\.cloud\.iam\.v1' -- 'services/iam/docs/content/**'` | да |
| **И17** | записей в `proto/declared-breaks.yaml` | 0 | **0** | `git show <rev>:proto/declared-breaks.yaml \| grep -c '^  - rule:'` | да |
| **И18** | различных форм записи имени в дереве | — | **три**: точечная **3101**, путевая **866**, подчёркнутая **1798** | `git grep -o -E 'kacho\.cloud\.iam\.v1\|kacho/cloud/iam/v1\|kacho_cloud_iam_v1' -- . \| …` | форм больше, чем названо |

### Расхождения — разбираются уточнением единицы, а не выбором правдоподобного

**И6 (182 против 142) — единицы разные, обе величины верны.** 182 — это все файлы, знающие
путь стабов, **включая сами порождённые стабы** (40 из них импортируют друг друга): 142 + 40
= 182, и разбивка задачи это подтверждает («порождённые стабы (40)»). Моя единица —
**прод-`.go` вне `pkg/api`**, потому что именно они суть **потребители**, которых надо
перевести руками; порождённые переводит генерация.

**И8 (190 против 116×2) — предикат задачи каталога НЕ ЧИТАЕТ, и это не придирка.** Предикат
`git grep -o '/kacho\.cloud\.iam\.v1\.' -- 'gateway/**'` считает форму **с ведущим слешем**;
в самом файле каталога такой формы **ноль** (`grep -c '/kacho\.cloud\.iam\.v1\.'
…/permission_catalog.json` → **0**), потому что ключ записан как `"fqn":
"kacho.cloud.iam.v1.AccountService/Create"` — слеш стоит перед **методом**, а не перед
пакетом. Разбивка 190: **78** записей allowlist (прод) + **112** вхождений в тестовых файлах
края. То есть число задачи верно как «сколько раз край произносит полное имя метода» и
неверно как «сколько записей каталога». Затронутых **записей каталога** — 116, и они лежат в
**двух** копиях, обязанных быть байт-идентичными: строк к правке **232**, предметов — 116.

**И15 (2 против 31) — та же природа.** 2 — это гейты, **импортирующие стабы**; 31 — гейты,
**знающие имя** (полное имя метода в фикстуре, путь `.proto`, литерал отбора популяции).
Вторая единица здесь несущая: именно среди этих 31 живут места, отбирающие популяцию
литералом приставки (§3, F4).

**И5 — один из «84» порождённых файлов чужой.** `pkg/api/kacho/iam/authz/v1/authz_options.pb.go`
— это **словарь аннотаций** `kacho.iam.authz.v1`, чей дом решён отдельно (Р12 замысла,
`kacho#2089`, переезд в `corelib`). В объём этой вехи он не входит; порождённых файлов
контракта доступа — **83**.

**И14 и И18 — то, чего перечень задачи не называл.** Фраза задачи «кто ломается — перечень
закрыт … вне их — никого» верна для своей единицы (импортёры Go-стабов) и **шире неё** как
утверждение: консоль стабов не импортирует и при этом произносит имя в **18** файлах, а форм
записи имени в дереве **три**, а не одна. Третья форма — `kacho_cloud_iam_v1` (**1798**
вхождений, **39** файлов) — живёт **только** в порождённых стабах и производится генерацией
из пути файла, поэтому руками не правится ни разу; названа она затем, чтобы перепись остатка
её не пропустила и не объявила ноль там, где не смотрела.

---

## 2. Перепись производителей — ДО единого сценария

Каждое «Тогда» требует того, что **что-то в дереве производит**. Таблица идёт первой именно
поэтому: три приёмки этой линии подряд получили `CHANGES REQUESTED` по одному классу —
«Тогда» без производителя.

**Колонка называется «производитель», а не «чем держится».** Различие не косметическое:
`scripts/docs-gate/check-03-holding-claim-resolves.py` судит **таблицу состояния кейса**
закрытым набором (`держится` · `наполовину` · `не начат`). Здесь предмет другой —
производитель у большинства сценариев **существует и производит сегодня СТАРОЕ значение**,
а состояние сценария при этом «не исполнен». Уложить это в тот набор нельзя, не соврав в
одной из колонок.

| сценарий | что требует «Тогда» | производитель в дереве | есть? |
|---|---|---|---|
| KAN-PKG-01 | вызов по новому полному имени резолвится на реальный бэкенд | `gateway/internal/proxy/server.go` (`RoutableDomain`, `Resolver`); проба `gateway/cmd/api-gateway/route_wiring_parity_test.go` `TestRouteWiring_EveryAllowedMethodResolvesOnTheRealBackends` | есть |
| KAN-PKG-02 | каталог контракта совпадает с именем пакета | `buf lint` правило `PACKAGE_DIRECTORY_MATCH` (в `STANDARD`, в `except` `proto/buf.yaml` не значится) | есть |
| KAN-PKG-03 | генерация порождает стабы нового корня | `proto/buf.gen.yaml` (`inputs.paths`) + `buf generate` | есть |
| KAN-PKG-04 | прежнее полное имя не резолвится на крае | `Resolver` → `codes.NotFound`; `gateway/internal/proxy/route_refusal.go` — байт-идентичный отказ впереди авторизации | есть |
| KAN-PKG-05 | REST-путь арендатора не изменился | 110 аннотаций `google.api.http` в контракте + `gateway/internal/middleware/rest_route_table_gen.go` | есть |
| KAN-PKG-06 | каталог, разошедшийся с контрактом, роняет прогон | `internal/repohygiene/catalogparity_test.go` `TestCatalogMatchesTheAnnotationsItWasGeneratedFrom` | есть |
| KAN-PKG-07 | две вшитые копии каталога байт-идентичны | тот же тест, вторая половина; цель `make -C gateway permission-catalog-check` | есть |
| KAN-PKG-08 | перечень пакетов каталога объявляет новый пакет | тот же тест: `catalogProtoPackages` выписан осознанно, строка без источника — находка | есть |
| KAN-PKG-09 | вызов нового имени получает решение о доступе штатно | `gateway/internal/middleware/authz.go` — ветка отсутствующей записи каталога (`missReason`) | есть |
| KAN-PKG-10 | allowlist не несёт ни одной записи прежнего написания | `gateway/internal/allowlist/parity_test.go` `TestAllowlist_NoEntryWithoutAnRPC` и `TestAllowlist_EveryPublicRPCIsRoutable` | есть |
| KAN-PKG-11 | ни один Internal-метод не стал маршрутизируемым | `gateway/internal/allowlist/parity_test.go` `TestAllowlist_NoInternalRPCIsRoutable`; `route_wiring_parity_test.go` `TestRouteWiring_InternalStaysUnroutedOnTheSameWiring` | есть |
| KAN-PKG-12 | разрыв контракта объявлен, а не обнаружен | `tools/declaredbreak/cmd/adjudicate-declared-breaks` + `proto/declared-breaks.yaml`; `buf breaking` (`FILE` в `proto/buf.yaml`) | есть |
| KAN-PKG-13 | на стволе после вливания перечень разрывов пуст | тот же адъюдикатор: запись без предмета — красное | есть |
| KAN-PKG-14 | стабы в синхроне с контрактом | `scripts/ci-local.sh` шаг `generate-diff`; `.github/workflows/ci.yaml` | есть |
| KAN-PKG-15 | генерация не потеряла дерево контракта молча | **производителя нет** — `generate-diff` судит `git status` после регенерации и «непорождённое» не видит by construction | **ждёт производителя** (`kacho#2137`) |
| KAN-PKG-16 | проверка секретоносных полей продолжает видеть контракт службы | `internal/repohygiene/secretbearingsurface_test.go` печатает перепись осмотренного | есть |
| KAN-PKG-17 | единый предикат популяции запрета #6 продолжает видеть службу | `deploy/scripts/e2e-ban6-domains.py` печатает `proto_files_read` и `domains_with_contract` | есть |
| KAN-PKG-18 | ни одна проверка не отбирает популяцию литералом приставки | **производителя нет** — гейта на этот класс в дереве нет | **ждёт производителя** (`kacho#2138`) |
| KAN-PKG-19 | генераторы края собирают каталог и таблицу маршрутов, видя новый корень | `gateway/scripts/lib/stage-proto-tree.sh` печатает «деревьев скопировано N» и падает на нуле; `gateway/scripts/check-domain-generation.sh` | есть |
| KAN-PKG-20 | тип в `Operation.metadata`/`response` разрешается по новому имени | `internal/repohygiene/operationanyresolution_test.go` (два гейта разрешимости) | есть |
| KAN-PKG-21 | консоль не произносит прежнего имени | `ui-future/shared` и модули: типы и запросы — обычный прогон `npm test` + `typecheck` | есть |
| KAN-PKG-22 | клиентские страницы службы называют новый пакет | сборка сайта документации службы (`0 broken links`) | есть |
| KAN-PKG-23 | перепись остатка прежнего имени даёт ноль по четырём координатам | **производителя нет** — держатель переписи объявлен без исполнителя | **ждёт производителя** (`kacho#2126`) |
| KAN-PKG-24 | провайдер инфраструктуры и пять служб платформы собираются и зелены | `go build ./...`, `go test ./... -short`, `golangci-lint` | есть |

**Итог переписи, названный точно:** сценариев **24**; производитель есть у **21**, нет у
**3** (KAN-PKG-15, KAN-PKG-18, KAN-PKG-23). Ни один из трёх в покрытие не засчитывается; у
каждого назван третий исход — задача-преемник с разрешимым номером (`kacho#2137`,
`kacho#2138`, `kacho#2126`).

**Две строки этой таблицы проверены прогоном, а не прочтением, и это стоит назвать.**
KAN-PKG-02: правило `PACKAGE_DIRECTORY_MATCH` прогнано инъекцией в обе стороны на
синтетическом дереве с копией `lint`-раздела `proto/buf.yaml` — пакет в чужом каталоге даёт
код **100** и называет оба каталога, пакет в своём даёт код **0** и пустой вывод.
KAN-PKG-12: разрыв прогнан `buf breaking --against` на синтетике — переименование пакета
вместе с каталогом даёт находки вида `Previously present file "…" was deleted.`, то есть
семейство `FILE_NO_DELETE`, по **одной на файл**; код **100**.

---

## 3. Решение имени — `kaname.cloud.iam.v1`

Р14 решило направление и оставило имя вехе. Веха решает так:

| ось | было | становится |
|---|---|---|
| пакет контракта | `kacho.cloud.iam.v1` | **`kaname.cloud.iam.v1`** |
| каталог контракта | `proto/kacho/cloud/iam/v1/` | **`proto/kaname/cloud/iam/v1/`** |
| путь стабов | `pkg/api/kacho/cloud/iam/v1` | **`pkg/api/kaname/cloud/iam/v1`** |
| ключ бэкенда края | `iam` | **`iam`** (не меняется) |
| REST-путь арендатора | `/iam/v1/…` | **`/iam/v1/…`** (не меняется) |

**Каталог меняется не «заодно», а по требованию проверки.** `PACKAGE_DIRECTORY_MATCH` входит
в `STANDARD` и в `except` раздела `lint` файла `proto/buf.yaml` не значится; пакет
`kaname.cloud.iam.v1`, оставленный в прежнем каталоге, даёт отказ, называющий оба каталога.
Путь стабов следует за каталогом by construction: генерация объявлена с `paths=source_relative`
и выходом `../pkg/api`.

### Адъюдикация по пяти вопросам — назван признак ОТКАЗА, а не одобрения

| вопрос | ОТВЕРГАЕТСЯ, когда | исход |
|---|---|---|
| **благо продукта** | единственный выигрыш — меньше работы **сейчас** | **не отвергнуто.** Выигрыш не в объёме: продукт перестаёт называть себя двумя корнями. Сегодня чарт, схема, точка входа и каталог приложения произносят `kaname`, а контракт — `kacho`; после перехода корень один |
| **конкурентоспособность** | выигрыш формулируется только как «как у …» | **не отвергнуто.** Ось своя — **предсказуемость контракта**: форма `<корень>.<домен>.<версия>` остаётся ОДНА на все домены края, и вызывающий, знающий один домен, знает все. Сравнений ни с кем не приводится |
| **перспективность** | ошибка **необратима**: отзыв ломает внешне-адресуемую координату либо требует миграции данных арендатора | **не отвергнуто сегодня**, и окно названо событием, а не датой — §3.2 |
| **безопасность** | решение расширяет поверхность, и расширение **не названо** | **не отвергнуто:** расширений **два**, оба названы и оба сужены — §3.3 |
| **производственность** | есть режим, годный только для стенда, либо часть, отложенная «на потом» | **не отвергнуто:** разрез один, окно двух имён запрещено, отложенного нет — §4 |

### 3.1. Почему корень `kaname.cloud`, а не `kaname`

**Довод первый — продукт не вправе называть себя двумя способами.** Корень
`kaname.cloud` продуктом уже выбран: решение Р10 замысла переводит домен доверия
`spiffe://kacho.cloud` → `spiffe://kaname.cloud` (**233** вхождения на этой ревизии;
`git grep -o 'spiffe://kacho\.cloud' | wc -l`). Пакет контракта и домен доверия — две записи
одного и того же: **имени полномочия, которым продукт себя называет**. Разные корни в них —
это ровно тот класс, который корпус ловит: продукт противоречит себе, и различие никем не
решалось.

**Довод второй — измеренный, и он решающий.** Ключ бэкенда край **выводит позиционно из
имени пакета**, а не берёт из объявленной карты:

```go
// release/kaname:gateway/internal/proxy/server.go — RoutableDomain
if !strings.HasPrefix(fullMethod, "/kacho.cloud.") { return "", false }
pkgParts := strings.Split(parts[1], ".")
if len(pkgParts) < 4 { return "", false }
return pkgParts[2], true
```

Прогон дословной копии этой функции с **одно-фактной** инъекцией — расширен только перечень
допустимых корней, правило сегментов не тронуто:

| полное имя | ключ бэкенда, который выводит край |
|---|---|
| `/kacho.cloud.iam.v1.AccountService/Get` | `iam` |
| `/kaname.iam.v1.AccountService/Get` | **`v1`** |
| `/kaname.cloud.iam.v1.AccountService/Get` | `iam` |

Трёхсегментный корень даёт ключ `v1`, которого в карте бэкендов нет: метод не резолвится, и
вызывающий получает отказ, скрывающий существование. Починить это можно только **вторым
правилом разбора имени** на крае — то есть двумя формами об одном предмете, расходящимися
молча. Четырёхсегментная форма оставляет правило разбора **одно** и требует от края ровно
одного: чтобы перечень допустимых корней стал **объявленным закрытым множеством** вместо
литерала.

**Это не «меньше работы сейчас».** Объём правки контракта у обеих форм одинаков — 39
объявлений, 39 `go_package`, 26 самоимпортов, 83 порождённых файла. Различается **форма
края**: одна форма разбора против двух.

### 3.2. Перспективность: окно закрывается событием, а не датой

Обратимость проверена на этой ревизии, предикат назван у каждой строки:

| что проверялось | замер | предикат |
|---|---|---|
| выпуски платформы | **0** | `gh api repos/PRO-Robotech/kacho/releases --jq length` |
| семантические метки | **0** из 13 (все — `archive/*`) | `gh api repos/PRO-Robotech/kacho/tags --jq '[.[].name]'` |
| перечень версий модуля в общедоступном зеркале | **пусто** | `curl 'https://proxy.golang.org/github.com/!p!r!o-!robotech/kacho/@v/list'` |
| третье дерево, объявляющее контракт | `.proto` у `PRO-Robotech/kaname` — **0** | `gh api 'repos/PRO-Robotech/kaname/git/trees/HEAD?recursive=1' --jq '[.tree[].path\|select(endswith(".proto"))]\|length'` |
| видимость репозиториев службы и фундамента | `kaname` — **приватный**; `corelib` — **приватный и пустой** | `gh repo view PRO-Robotech/<r> --json visibility,isEmpty` |

**Оговорка, без которой замер выглядит шире, чем он есть.** Модуль платформы **публичен**, и
зеркало модулей держит псевдоверсию (`@latest` отвечает `v0.0.0-20260905141452-2171a6690a81`),
а `go.mod` службы пинит другую. Обе прослеживаются к нашей же работе — потребителями они не
являются, но формулировка обязана быть точной: обратимо **не «пока никто не узнал», а пока
никто не поставил**. Предикат закрытия окна — событие: **первая установка у стороннего
оператора**.

**Один предикат обратимости провалил собственный контроль и потому не используется.** Поиск
по коду GitHub (`gh api search/code`) отвечает **0** и на строку, которая заведомо есть в
публичном дереве платформы 3101 раз. Инструмент это дерево не индексирует, значит его ноль
не отличает «потребителей нет» от «не прочитано ничего», и как свидетельство он не годится ни
в одну сторону. Названо здесь затем, чтобы следующий не воспроизводил его как замер.

### 3.3. Безопасность: два расширения поверхности, оба названы и сужены

**Первое — перечень допустимых корней на крае.** Литерал `"/kacho.cloud."` становится
множеством. Пустое множество обязано означать **«никого»**, а не «не сужаем»: это перечень
**разрешающего** вида (тот же разбор, что у перечня владельцев журнала подписки), и
безопасен он by construction только при таком чтении. Сценарии KAN-PKG-04 и KAN-PKG-11
утверждают обе стороны.

**Второе — каталог решений о доступе меняет ключ у 116 записей в двух копиях.** Направление
отказа безопасное: метод, чьё имя сменилось, а запись нет, получает отказ **fail-closed** —
но поверхность меняется **вся разом**, поэтому переименование обязано идти **одним изменением
с каталогом**. Это ограничение разреза, а не деталь исполнения (§4).

**Ban #15 не смягчается и он тут ЗА решение.** Внешняя адресация **ресурса** идёт по
неизменяемому `id`; имя пакета адресует **службу**, а не ресурс, и ни одна долговечная
координата арендатора на нём сегодня не построена — арендаторов нет (§3.2).

### 3.4. Отвергнутые варианты

**Отвергнуто — `kaname.iam.v1`.** Это действующее решение W1 соседней приёмки
`sub-phase-WIRE-1-kaname-wire-identity-acceptance.md` (✅ APPROVED), и она **сама объявляет
свои решения `W<n>` опровержимыми**. Довод W1 — «сегмент `cloud` описывал раскладку доменов
Kachō, а у Kaname домен один» — не учитывает позиционного вывода ключа бэкенда: замер §3.1
даёт ключ `v1`. Следствие сильнее спора о вкусе: сценарий **WIRE-1-01** («клиент новой
сборки получает ответ») при написании W1 остаётся **без производителя**. Приведение WIRE-1
§F1 к факту заведено задачей `PRO-Robotech/kacho-workspace#525`; до её закрытия два
действующих текста об одном предмете существуют, и это названо, а не скрыто.

**Отвергнуто — `kaname.v1`.** Ось **благо продукта**: домен исчезает из имени вовсе, и ключ
бэкенда становится невыводимым ни при какой позиции. Ось **перспективность**: второй домен
контракта у Kaname (журнал, аудит) потребовал бы ломающего изменения ровно того же объёма
второй раз.

**Отвергнуто — оставить `kacho.cloud.iam.v1`.** Отвергнуто **не здесь**: это Р14 замысла, и
веха его не пересматривает.

**Отвергнуто — сменить и домен на `access`.** Ось **благо продукта**: ключ бэкенда `iam`
и REST-путь `/iam/v1/` пришлось бы менять вместе с ним, а REST-путь бренда не несёт и в
предмет вехи не входит. Единственный выигрыш — вкус.

---

## 4. Разрез: одно изменение, окно двух имён запрещено

**Разрез один.** Р14 запрещает окно, в котором принимаются оба имени; веха это исполняет и
добавляет то, что из объёма следует: **в одном изменении** идут контракт, генерация, каталог
решений о доступе (обе копии), allowlist, таблица маршрутов, все потребители стабов, отбор
популяции проверок и клиентские страницы службы.

**Почему каталог не может ехать следом.** Записи ключуются полным именем метода. Между
переименованием и правкой каталога у **116** методов записи нет, а метод без записи получает
отказ fail-closed. Направление безопасное, состояние — нет: это отказ всей поверхности
службы, и выглядит он дефектом прав, а не следствием переименования.

**Чем разрез держится в самом изменении:** гейт `TestCatalogMatchesTheAnnotationsItWasGeneratedFrom`
роняет прогон **в обе стороны** — контракт без каталога и каталог без контракта. Он же
сверяет байт-идентичность двух копий. То есть «одним изменением» — не пожелание, а
единственное состояние, в котором прогон зелёный.

**Что в разрез НЕ входит и почему** (каждое — свой предмет со своей ценой):

| вне объёма | владелец | почему отдельно |
|---|---|---|
| словарь аннотаций `kacho.iam.authz.v1` | Р12 замысла, `kacho#2089` | уезжает в `corelib` как **нейтральный** словарь: он не принадлежит ни одному продукту |
| домен отказа `iam.kacho.cloud` | `kacho#2099` | **значение времени выполнения**, а не схема провода; переименовывается по своей причине |
| домен доверия, заголовки личности, якорь кластера, типы провайдера | Р10 замысла, `kacho#2087` | четыре поверхности провода, у каждой своя цена ошибки |
| клеймы токена | `kacho#2127` | своя поверхность |
| координаты контрактов фундамента | П4 замысла | граница фундамента, отдельное решение |
| REST-пути `/iam/v1/…` | — | бренда **не несут**; не трогаются, и это утверждается сценарием KAN-PKG-05 |

**Отложенного нет.** Три сценария без производителя (KAN-PKG-15, KAN-PKG-18, KAN-PKG-23) —
не отсрочка, а третий исход: у каждого заведён **предмет** с разрешимым номером и предикатом
снятия. Они в покрытие не засчитываются, и планирование их не ждёт: ни один не является
предусловием разреза.

---

## 5. Scope — шесть поверхностей

| # | поверхность | что переходит | цена ошибки |
|---|---|---|---|
| **F1** | контракт и его порождение | 39 объявлений пакета, 39 `go_package`, 26 самоимпортов, каталог `proto/`, 83 порождённых файла, входы генерации | без каталога и входов генерации стабы не порождаются **молча** |
| **F2** | маршрутизация края | перечень допустимых корней, позиционный вывод ключа бэкенда, 78 записей allowlist, 104 строки таблицы REST-маршрутов | метод не резолвится, и отказ неотличим от «метода не существует» |
| **F3** | каталог решений о доступе | 116 записей × 2 байт-идентичные копии, перечень пакетов каталога | метод без записи получает отказ fail-closed — вся поверхность службы разом |
| **F4** | популяции проверок дерева | девять мест, отбирающих популяцию литералом приставки | дерево выходит из-под наблюдения **молча**: перепись показывает ноль, потому что сузилась популяция |
| **F5** | объявление разрыва | 39 записей `FILE_NO_DELETE` на время ветки, пусто на стволе | разрыв, обнаруженный вместо объявленного, роняет прогон и читается как дефект |
| **F6** | потребители | 142 прод-файла, 18 файлов консоли, 2 клиентские страницы, тип в `Operation.metadata`/`response` | ответ операции перестаёт разрешаться; страница называет чужой продукт |

---

## F1 — контракт и его порождение

### KAN-PKG-01 — вызов по новому полному имени резолвится (положительный)

**Дано** край и служба, пересобранные из одного дерева; перечень допустимых корней края
объявлен и содержит корень продукта.
**Когда** клиент вызывает `/kaname.cloud.iam.v1.AccountService/Get` с существующим `id`
учётной записи.
**Тогда** край выводит ключ бэкенда `iam` и направляет вызов службе; приходит ответ с
ресурсом.
**И** проба провязки маршрутов утверждает то же самое **по каждому** разрешённому методу, а
не по одному: у метода из allowlist обязан резолвиться реальный бэкенд.

### KAN-PKG-02 — каталог контракта совпадает с именем пакета (положительный)

**Дано** контракт, переехавший в `proto/kaname/cloud/iam/v1/`.
**Когда** прогоняется `buf lint` на дереве контрактов.
**Тогда** находок ноль, код возврата **0**.
**И** тот же контракт, оставленный в прежнем каталоге, даёт код **100** и находку, называющую
**оба** каталога — ожидаемый и фактический. Односторонняя проверка зеленела бы на дереве, где
`lint` выключен вовсе.

### KAN-PKG-03 — генерация видит новый корень (положительный)

**Дано** объявление генерации, чей перечень входов назвал корень продукта.
**Когда** каталог порождённых стабов удалён целиком и прогнан `buf generate`.
**Тогда** стабы `pkg/api/kaname/cloud/iam/v1/` возвращаются в прежнем числе — **83** файла.
**И** ни одного файла не появляется под прежним путём `pkg/api/kacho/cloud/iam/`.

### KAN-PKG-15 — генерация не теряет дерево контракта молча (отрицательный) · `ждёт производителя`

**Дано** корень дерева контрактов, **не** названный в перечне входов генерации.
**Когда** прогоняется генерация и следом сверка «стабы в синхроне с контрактом».
**Тогда** прогон обязан **упасть**, назвав необъявленный корень.
**И** он этого сегодня не делает: генерация выходит успехом и не печатает ни строки, а сверка
судит состояние дерева **после** регенерации и «непорождённое» не видит by construction.
**Третий исход:** предмет заведён — `kacho#2137`; в покрытие сценарий не засчитан.

---

## F2 — маршрутизация края

### KAN-PKG-04 — прежнее полное имя не резолвится нигде (отрицательный)

**Дано** край, пересобранный вместе со службой.
**Когда** клиент прежней сборки вызывает `/kacho.cloud.iam.v1.AccountService/Get`.
**Тогда** приходит `NOT_FOUND` — тот же отказ, которым край отвечает на несуществующий метод,
**байт-в-байт**.
**И** отказ приходит на **первом** вызове, а не по истечении срока.
**И** он не отличается от отказа на любой другой неизвестный методу путь: различимый отказ
здесь был бы признаком существования административной поверхности.

> **Почему `NOT_FOUND`, а не `UNIMPLEMENTED`.** Это свойство **края** и оно намеренное:
> резолвер отвечает `ok=false`, а обработчик переводит это в `NOT_FOUND`, чтобы неизвестный
> метод и скрытая поверхность были неразличимы. `UNIMPLEMENTED` — свойство gRPC-библиотеки на
> **прямом** вызове службы мимо края; на публичной полосе его не бывает. Соседняя приёмка
> требует здесь `UNIMPLEMENTED`, не называя полосы; расхождение заведено
> `PRO-Robotech/kacho-workspace#525`.

### KAN-PKG-11 — окно двух имён отсутствует, и это проверяется с обеих сторон (отрицательный)

**Дано** перечень допустимых корней края объявлен закрытым множеством.
**Когда** в нём ищут прежний корень.
**Тогда** его там нет ни в одном виде — ни отдельной записью, ни приставкой, ни умолчанием.
**И** пустой перечень означает **«никого»**: край, поднятый с незаполненным перечнем,
отвергает вызовы, а не пропускает их все.
**И** ни один метод `Internal*` не стал маршрутизируемым: проверка, считающая разность
«публичное в дескрипторах» минус «есть в перечне», остаётся зелёной в обе стороны.

### KAN-PKG-10 — allowlist приведён и не отстал (положительный близнец к KAN-PKG-04)

**Дано** переименованный контракт.
**Когда** прогоняется сверка перечня разрешённых методов с дескрипторами.
**Тогда** записи прежнего написания отсутствуют: запись без соответствующего RPC — находка.
**И** каждый публичный RPC нового написания в перечне есть: RPC без записи — тоже находка.
Обе стороны обязательны — односторонняя сверка зеленела бы на пустом перечне.

### KAN-PKG-05 — путь арендатора не изменился (положительный близнец)

**Дано** край, пересобранный вместе со службой.
**Когда** клиент обращается по REST к `/iam/v1/accounts`.
**Тогда** ответ тот же, что и до перехода: путь, метод, форма тела и код совпадают.
**И** порождённая таблица маршрутов несёт **104** строки службы, у каждой шаблон начинается
на `/iam/v1/`, и ни один шаблон не изменился.

---

## F3 — каталог решений о доступе

### KAN-PKG-06 — каталог, разошедшийся с контрактом, роняет прогон (отрицательный, НЕСУЩИЙ)

**Дано** контракт переименован, а каталог решений о доступе не перегенерирован.
**Когда** прогоняется сверка каталога с аннотациями, из которых он порождён.
**Тогда** прогон **красный**, и находка называется **по каждой** из 116 строк: аннотация
нового написания без строки каталога — в одну сторону, строка каталога прежнего написания
без аннотации — в другую.
**И** находка называет исходы: перегенерировать каталог (обе копии) либо вернуть аннотацию;
правка порождаемого файла руками исходом не является.
**И** перепись прогона печатает **обе** величины — строк каталога и аннотированных RPC:
«находок ноль» обязано быть отличимо от «прочитано ноль».

### KAN-PKG-07 — две вшитые копии байт-идентичны (положительный)

**Дано** каталог перегенерирован.
**Когда** сверяются копия края и копия посева службы.
**Тогда** они совпадают **побайтово**; расхождение — находка, называющая обе координаты.
**И** прогон печатает число сверенных копий и их размер: «сверено 0» не должно читаться как
«сверено».

### KAN-PKG-08 — перечень пакетов каталога объявляет новый пакет (положительный)

**Дано** перечень пакетов, чьи RPC попадают в каталог; он **выписан** осознанно, а не выведен
из каталога.
**Когда** контракт переименован, а перечень назвал новый пакет.
**Тогда** обход аннотаций находит все методы службы, и расхождений ноль.
**И** тот же перечень, оставленный с прежним именем, даёт **красное**: обход не находит ни
одного метода службы, и каждая её строка каталога объявляется строкой без источника. Именно
это делает выписанный перечень предметом, а не удобством.

### KAN-PKG-09 — арендатор получает решение о доступе, а не отказ «нет записи» (положительный)

**Дано** каталог и контракт переехали одним изменением; вызывающий имеет право на метод.
**Когда** он вызывает метод нового написания через край.
**Тогда** решение о доступе принимается штатно и вызов доходит до службы.
**И** тот же вызывающий **без** права получает отказ, называющий право, — а не отказ,
означающий отсутствие записи в каталоге. Различить их обязательно: первый — работа контроля,
второй — его отсутствие.

---

## F4 — популяции проверок дерева

### KAN-PKG-16 — проверка секретоносных полей продолжает видеть контракт службы (положительный)

**Дано** контракт переехал под новый корень.
**Когда** прогоняется проверка полей, несущих секреты на поверхности.
**Тогда** её перепись показывает объём осмотренного **не меньше** прежнего: число прочитанных
дескрипторов и найденных полей печатается, и уменьшение — находка, а не тишина.
**И** отбор популяции у неё берётся из объявленного словаря корней, а не из литерала
приставки: литерал оставил бы контракт службы вне обхода, и проверка честно напечатала бы
ноль находок по опустевшей популяции.

### KAN-PKG-17 — единый предикат популяции запрета #6 продолжает видеть службу (положительный)

**Дано** контракт переехал под новый корень.
**Когда** прогоняется единый предикат популяции запрета #6.
**Тогда** число доменов с контрактом и число прочитанных файлов контракта **не уменьшаются**,
и служба среди доменов названа.
**И** прогон, при котором служба из популяции выпала, обязан быть отличим от прогона, при
котором нарушений нет: обе величины печатаются рядом.

### KAN-PKG-18 — ни одна проверка не отбирает популяцию литералом приставки (отрицательный) · `ждёт производителя`

**Дано** дерево после перехода.
**Когда** ищется отбор популяции литералом приставки в проверках и генераторах.
**Тогда** таких мест ноль, и это утверждает гейт, а не перепись руками.
**И** гейта этого сегодня нет: мест **девять**, и восемь из них при сужении популяции молчат
(девятое — перечень пакетов каталога — самоистекает, см. KAN-PKG-08).
**Третий исход:** предмет заведён — `kacho#2138`; в покрытие сценарий не засчитан.

### KAN-PKG-19 — генераторы края собирают выход, видя новый корень (положительный)

**Дано** генераторы каталога решений и таблицы маршрутов.
**Когда** они собирают временное дерево контрактов и порождают выход.
**Тогда** дерево службы в стадию попадает, и число скопированных деревьев печатается; на нуле
скопированных прогон падает.
**И** домен службы присутствует в выходе; домен, эмитированный мимо объявленного отбора,
объявляется **находкой**, а не отбрасывается молча — иначе каталог вышел бы без записей
службы, то есть с отказом в доступе на всей её поверхности.

---

## F5 — объявление разрыва

### KAN-PKG-12 — разрыв объявлен, а не обнаружен (положительный)

**Дано** ветка перехода; контракт переименован вместе с каталогом.
**Когда** прогоняется сверка совместимости контракта против ствола и её вердикт читает
адъюдикатор объявленных разрывов.
**Тогда** каждая находка сопоставлена записи перечня; находка вне перечня — красное.
**И** записей **39** — по одной на файл контракта, все семейства `FILE_NO_DELETE`, и у каждой
`path` и `symbol` совпадают: у снятия файла предмет ровно один — сам файл.
**И** каждая запись несёт причину и **разрешимую** ссылку `kacho#2133`: по голому номеру
объявление не может истечь даже в принципе.

### KAN-PKG-13 — на стволе перечень разрывов пуст (отрицательный близнец)

**Дано** ветка влита; база сравнения поднялась.
**Когда** адъюдикатор прогоняется на стволе.
**Тогда** находок ноль **и** записей в перечне ноль: запись без предмета — тоже красное.
**И** это утверждается прогоном на стволе, а не памятью исполнителя: истёкшая запись
называется поимённо там же и тогда же, где её предмет истёк.

---

## F6 — потребители

### KAN-PKG-14 — стабы в синхроне с контрактом (положительный)

**Дано** контракт переименован и стабы порождены.
**Когда** прогоняется сверка «генерация не меняет дерево».
**Тогда** изменённых файлов ноль.
**И** сверка исполняется теми же плагинами, что и конвейер, — версиями из объявления модуля,
а не тем, что оказалось в путях поиска: иначе отказ означает «другой плагин», а не
«устаревшее дерево».

### KAN-PKG-24 — потребители собираются и зелены (положительный)

**Дано** переименованные стабы.
**Когда** собирается дерево и прогоняются короткие пробы.
**Тогда** **142** прод-файла вне порождённых стабов, ссылавшихся на прежний путь, собираются
по новому; сборка и пробы зелёные.
**И** это верно для **каждой** группы потребителей, а не для службы одной: край, пять служб
платформы, провайдер инфраструктуры и пакеты фундамента.

### KAN-PKG-20 — тип в ответе операции разрешается по новому имени (положительный)

**Дано** мутация службы, вернувшая операцию.
**Когда** клиент опрашивает операцию до завершения и читает её содержимое.
**Тогда** имя типа во вложенном значении несёт **новое** написание, и тип **разрешается**
двоичным файлом, который этот ответ производит.
**И** ни одно исполняемое дерева не потеряло связывания с порождённым пакетом: тип, который
владелец кладёт в ответ операции, обязан быть разрешим тем, кто его отдаёт.

> Эта поверхность **видна арендатору** и в перечне из трёх тенант-видимых поверхностей Р14
> не значилась. Литералов у неё в дереве почти нет — имя типа собирается из дескриптора во
> время выполнения, — поэтому перепись остатка по литералам её не измеряет вовсе.

### KAN-PKG-21 — консоль не произносит прежнего имени (положительный)

**Дано** консоль, собранная после перехода.
**Когда** прогоняются проверка типов и пробы её модулей.
**Тогда** они зелёные, и ни один из **18** файлов, знавших прежнее имя, его больше не несёт.
**И** прогон называет, сколько модулей исполнено: «не выполнилось» из-за неустановленных
зависимостей не засчитывается ни в зелёное, ни в красное.

### KAN-PKG-22 — клиентские страницы службы называют новый пакет (положительный)

**Дано** сайт документации службы.
**Когда** он собирается.
**Тогда** сборка проходит с нулём битых ссылок, а **3** вхождения имени пакета в **2** файлах
называют новый пакет.
**И** оператор, читающий страницу, не видит имени другого продукта: это и есть проверочный
вопрос критерия — увидит ли он это, не открывая наш исходный код.

### KAN-PKG-23 — перепись остатка даёт ноль по четырём координатам (отрицательный) · `ждёт производителя`

**Дано** переход исполнен.
**Когда** прогоняется перепись остатка прежнего имени.
**Тогда** она печатает **объём осмотренного** и даёт ноль по четырём координатам: объявление
пакета, `go_package`, полное имя метода в каталоге решений о доступе, имя пакета на
клиентских страницах службы.
**И** перепись знает **все три** формы записи имени — точечную, путевую и подчёркнутую:
форма, о которой она не знает, даёт молчание, неотличимое от отсутствия.
**Третий исход:** производителя переписи сегодня нет, предмет заведён — `kacho#2126`; в
покрытие сценарий не засчитан.

---

## 6. Задачи-преемники, заведённые этой приёмкой

| задача | предмет | почему не здесь |
|---|---|---|
| `kacho#2137` | корень дерева контрактов вне входов генерации не порождает ничего молча; сверка «стабы в синхроне» этого класса не ловит | предмет шире вехи: касается **любого** второго корня контрактов, а не только этого |
| `kacho#2138` | девять проверок и генераторов отбирают популяцию литералом приставки | правка девяти мест входит в разрез (F4); **гейт** на класс — отдельный предмет с собственной инъекцией |
| `kacho-workspace#525` | решение W1 соседней приёмки опровергнуто замером; её сценарий требует кода, которого край не производит | чужой документ: правит его владелец, вердикт перевыставляется на новое содержимое |
| `kacho#2126` | у переписи остатка чужого имени нет производителя | заведена до этой вехи; здесь только названа как исход KAN-PKG-23 |

---

## 7. Определение готовности

1. **Контракт**: 39 объявлений пакета, 39 `go_package`, 26 самоимпортов переведены; каталог
   контракта — `proto/kaname/cloud/iam/v1/`; `buf lint` зелёный.
2. **Генерация**: перечень входов назвал новый корень; 83 файла порождены под новым путём;
   под прежним путём файлов ноль; сверка «стабы в синхроне» зелёная.
3. **Край**: перечень допустимых корней объявлен закрытым множеством, прежнего корня в нём
   нет; 78 записей allowlist и 104 строки таблицы маршрутов переведены; обе сверки перечня с
   дескрипторами зелёные; проба провязки маршрутов зелёная.
4. **Каталог решений о доступе**: 116 записей переведены **в обеих** копиях, копии
   байт-идентичны, перечень пакетов каталога назвал новый пакет; сверка каталога с
   аннотациями зелёная в обе стороны.
5. **Популяции проверок**: девять мест отбора переведены на объявленный словарь корней;
   перепись каждого не уменьшилась.
6. **Разрыв**: 39 записей `FILE_NO_DELETE` объявлены на время ветки, ссылка разрешима; на
   стволе после вливания перечень **пуст**.
7. **Потребители**: 142 прод-файла, 18 файлов консоли, 2 клиентские страницы переведены;
   локальный прогон зелёный целиком (проверки типов, линт, сборка, короткие пробы).
8. **REST-пути арендатора не изменились** — утверждается сценарием, а не подразумевается.
9. **Trail**: записка задачи обновлена, задача закрыта артефактом.

---

## 8. Чего эта приёмка НЕ покрывает

- **Правильность решений Р10, Р12 и Р14** — они приняты замыслом и владельцем; веха
  исполняет Р14 и опирается на Р10 как на факт.
- **Поведение службы, поднятой ВНЕ дерева монорепо.** Все числа сняты на ветке `release/kaname`;
  подъём отдельного продукта — предмет П5 замысла и своей приёмки.
- **Домен отказа, домен доверия, заголовки личности, якорь кластера, типы провайдера, клеймы
  токена** — §4, таблица «вне объёма»: у каждой поверхности свой владелец и своя цена ошибки.
- **Словарь аннотаций** `kacho.iam.authz.v1` — Р12, `kacho#2089`.
- **Числа задачи `kacho#2133`, не воспроизведённые здесь.** Три из них разошлись с моими
  (И6, И8, И15); в каждом случае названы **обе** единицы и обе величины верны. Подгонять не
  стал: подгонка выдала бы догадку за замер.
- **Существование потребителей контракта вне наших репозиториев.** Замер §3.2 отвечает «не
  найдено», и предел замера назван: перепись велась по дереву платформы, а вовне спрашивались
  выпуски, метки, перечень версий модуля, видимость репозиториев и наличие третьего дерева
  контрактов. Поиск по коду GitHub провалил собственный контроль и в свидетельство не идёт.
- **Наблюдаемость перехода в рантайме.** Приёмка утверждает контракт и вердикты проверок;
  того, что оператор увидит переход в журналах и показаниях, она не требует — это предмет
  профиля наблюдаемости, а не имени пакета.
