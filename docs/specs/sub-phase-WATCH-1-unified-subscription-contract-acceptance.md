# Sub-phase WATCH-1 (единая форма подписки — контракт) — Acceptance

> **Статус: DRAFT**
> **Дата:** 2026-08-23
> **Ревьюер:** `acceptance-reviewer` (единственный, кто ставит APPROVED — ban #1)
> **Эпик:** #1016 · **Задача:** #1017 (Ф1) · **Роль исполнителя:** `proto-api-reviewer`
> **Монорепо:** `PRO-Robotech/kacho`. **База сверки документа — `958e8fe0d`** (`origin/main`,
> 2026-08-22 23:30 +0500). Все числа этого документа сняты на ней; предикат назван рядом
> с каждым. Числа задачи #1017 и эпика #1016 сняты на другом дереве и **разошлись** — §1.
>
> **Род документа:** приёмка, санкционирующая работу. После APPROVED разрешено кодировать
> **только** объём §2; всё, что в §3, — чужих фаз.
>
> **Нормативка (не пересказывается в тело — ссылки по названию раздела):**
> - `.claude/rules/api-conventions.md` — §«Принято-и-проигнорировано», §«Пустое значение
>   обязано означать „пусто“», §«By-lane code-split», §«Неисполнимая возможность: два правила
>   об одном поле», §«Форма ресурса — flat message + Operations»
> - `.claude/rules/00-kacho-core.md` — ban #6 (Internal не на внешнем крае), ban #9 (мутации
>   возвращают Operation), ban #11 (без отсрочек), ban #14 (production-grade), ban #15
>   (адресация по immutable `id`)
> - `.claude/rules/polyrepo.md` — §runtime-edges, запрет циклов, iam и geo как листья
> - `.claude/rules/security.md` — §«AuthN+AuthZ ВЕЗДЕ», §«Internal-vs-external»,
>   §«Отношение, выполнимое подстановочным знаком, не сужает НИЧЕГО»
> - `.claude/rules/testing.md` — §«Гейт на класс», §E2E
> - `.claude/rules/doc-truthfulness` (скил) — утверждение, пережившее свой предмет

---

## §1 Что перемерено — и чем постановка разошлась с деревом

Задача #1017 просит закрыть предикатом состояние дерева. Перед написанием сценариев
состояние перемерено. **Шесть расхождений**; каждое меняет объём Ф1, поэтому они стоят
первыми, а не в примечании.

| # | Постановка утверждает | Дерево `958e8fe0d` | Предикат |
|---|---|---|---|
| Р1 | форм подписки **две** | **одна** | `git grep -h 'returns (stream' origin/main -- proto \| wc -l` → `1` |
| Р2 | форму nlb предстоит снять | **уже снята** `77e3d0595` (`Closes #814`), влита в ствол `b9c72c429` **2026-08-22T03:51Z** | `git log origin/main --oneline -- proto/kacho/cloud/loadbalancer/v1/internal_resource_lifecycle_service.proto` |
| Р3 | «заготовить записи в `declared-breaks.yaml`» | заготовка **уронит адъюдикацию**: файл объявляет пустоту нормой ствола и «запись без находки — ТОЖЕ красное» | шапка `proto/declared-breaks.yaml` |
| Р4 | имена снятых форм стоят в надгробии | имени снятой формы nlb в надгробии **нет**; а соседняя запись утверждает, что эта форма **жива** | `grep -c 'FQN:'` → `32`; `grep 'FQN:' \| grep -i loadbalancer` → пусто; ложь — `internal/repohygiene/retiredrpcsurface_test.go:112` |
| Р5 | «ресурсный outbox у всех **семи** сервисов, включая `storage_outbox`» | `storage_outbox` **дропнут** миграцией `0011_drop_storage_outbox.sql`; у iam ресурсного `iam_outbox` нет вовсе | `git grep -l 'CREATE TABLE.*storage_outbox' origin/main -- 'services/storage/internal/migrations/*.sql'` даёт заведение и дроп |
| Р6 | DoD эпика п.4: `LISTEN` в `pkg/` вне стабов — «ровно один» | **семь** файлов уже сегодня | `git grep -l 'LISTEN' origin/main -- 'pkg/**/*.go' ':!*_test.go' ':!pkg/api/*' \| wc -l` → `7` |

**Почему постановка ошиблась — установлено временем, а не предположено.** Снятие формы nlb
влито в ствол **2026-08-22T03:51Z**; эпик #1016 заведён **2026-08-22T19:38Z**, задача #1017 —
**2026-08-22T19:41Z**. Разрыв — **15 часов 47 минут**. Значит замер, назвавший две формы, снят
**не со ствола**: на стволе к моменту записи их была одна. Класс известен корпусу — общую
рабочую копию читают как истину, а она отстаёт.

> **То же самое едва не случилось с этим документом, и это записано намеренно.** В начале
> разбора рабочая копия продукта стояла на ветке задачи и отставала от ствола на **99**
> коммитов; через полтора часа она уже стояла вровень. Число «99» было верным в момент замера
> и стало ложью к моменту письма — поэтому в таблице выше стоят **времена событий**, которые
> не устаревают, а не состояние чьей-то копии, которое устаревает за минуты. Проверяющему:
> все числа этого документа сняты предикатами по `origin/main` = `958e8fe0d`, а не по рабочему
> дереву; повторяй их так же.

**Следствие для Ф1 — объём меньше заявленного**: снимать предстоит **одну** прежнюю форму,
а не две.

**Следствие Р1, которое важнее самого числа: предикат готовности эпика перестал
различать.** DoD #1016 п.3 требует `git grep -h 'returns (stream' -- proto` → **1**. На
стволе это **уже верно** — и не потому, что эпик сделан, а потому, что вторая форма снята
чужой задачей. Предикат, зелёный до начала работы, не отличает «сделано» от «не начинали»:
он останется зелёным и на состоянии «общая форма объявлена, а домены её не взяли», и на
состоянии «общей формы нет вовсе». Ф1 обязана дать предикат, **способный упасть** — §4.3.

**Ещё три факта, влияющие на сценарии** (не расхождения — постановка о них молчит):

- **у живой формы осей две, а не три**: `WatchRequest` несёт `kinds` и `from_sequence_no`;
  ни `project_id`, ни `ids` в ней нет (`proto/kacho/cloud/compute/v1/internal_watch_service.proto`);
- **начального снимка нет ни в какой форме**: контракт сам оговаривает, что омит курсора —
  это полная выдача журнала, а не подписка на хвост; сигнала «ты догнал» нет;
- **журнал не чистится**: `git grep -in 'DELETE FROM compute_outbox' origin/main -- services/compute`
  → пусто. Значит «курсор старше удержанного» сегодня невоспроизводим — и именно поэтому
  класс невидим, а не отсутствует.

**Остаток снятой формы жив.** У nlb остались читатель журнала (`services/nlb/internal/repo/kacho/pg/lifecycle_feed.go`,
**308** строк, `LISTEN nlb_outbox`), его порт и раздел настроек. Контракта нет, читатель есть.
Это **не предмет Ф1** (см. §3), но приёмка называет его, чтобы Ф2 не завела второй читатель
рядом с этим.

---

## §2 Предмет Ф1 — что именно делается

Решение владельца 2026-08-22, дословно: **«формат подписки для всех должен быть един как
стандарт, определённый в `pkg/` или ещё где как переиспользуемый механизм; всё остальное
дропать и перевести на это»**.

Ф1 производит **три артефакта и ни одного больше**:

1. **Общая форма подписки, объявленная один раз** — в общем пакете контрактов, по образцу
   уже действующего `kacho.cloud.reference` (один файл, **9** импортирующих доменных
   контрактов; предикат: `git grep -l 'kacho/cloud/reference/reference.proto' origin/main -- proto | wc -l`).
   Домены **ссылаются** на неё, а не копируют.
2. **Правка `api-conventions.md`** — утверждение «Watch RPC не существует» приводится
   в соответствие с деревом, не отменяя полл операции.
3. **Записанное решение о снятии** прежней формы и о судьбе push-инвалидации — с порядком
   и предикатом, но **без исполнения снятия**.

Сервера нет, доменов нет, снятия нет — они в §3.

---

## §3 Out-of-scope — что Ф1 НЕ делает

| Не делает | Чья фаза | Почему не здесь |
|---|---|---|
| сервер потока в `pkg/` | Ф2 · #1018 | форма обязана быть утверждена до того, как под неё пишут сервер |
| перевод compute на общую форму | Ф3 · #1019 | перевод требует сервера |
| **снятие `compute.v1.InternalWatchService/Watch`** | после Ф3 | между объявлением общей формы и переводом модуля существует окно, где подписки нет ни в какой форме. Порядок — предмет сценария WATCH-1-17, а не пожелания |
| запись разрыва в `declared-breaks.yaml` | ветка, которая **ломает** (Ф3/Ф7) | ведомость на стволе обязана быть пуста; заготовка = запись без предмета = красное (Р3) |
| проекция потока в браузер | Ф4 · #1020 | без неё поток снова никто не возьмёт — причина названа эпиком |
| снятие поллинга в консоли | Ф5 · #1021 | предикат — **24** файла и **36** вхождений `refetchInterval` (`git grep -l/-h 'refetchInterval' origin/main -- ui-future`) |
| отзыв прав закрывает открытый поток | Ф6 · #1022 | Ф1 обязана лишь **не сделать это невыразимым** — WATCH-1-30 |
| перевод push-инвалидации iam | #1024 | Ф1 называет исход и проверяет применимость формы — WATCH-1-22…24 |
| серверный фильтр по меткам | #1025 | у задачи нет предмета, пока объём событий не измерен |
| уборка остатка снятой формы у nlb | отдельная задача | 308 строк читателя без контракта — находка, но не этой фазы; названа в §1, чтобы Ф2 не завела рядом второй |

---

## §4 Сценарии

**Два рода сценариев, и это сказано у каждого.**

- **(К) — контрактный**: проверяем **в Ф1**, потому что предмет утверждения — само
  объявление. Проверяется гейтом дерева, разбором контракта или `buf`.
- **(П) — поведенческий**: предмет — поведение сервера, значит проверяем **в Ф2 и позже**.
  В Ф1 у такого сценария проверяется **конструируемость**: назван элемент контракта, без
  которого сценарий не поставить, и этот элемент обязан существовать. Сводка — §6.

Сценарий без отказной стороны там, где она есть, не принимается: одностороннее утверждение
зеленеет на реализации, отвергающей всё.

---

### §4.1 Оси фильтра — только иммутабельные

**WATCH-1-01 · Конъюнкция трёх осей сужает (П)**

**Given** в журнале владельца лежат события по двум видам ресурса, в двух проектах, по трём
идентификаторам
**And** вызывающий имеет доступ ко всем перечисленным ресурсам
**When** клиент открывает подписку, задав одновременно `kinds` = один вид, `project_id` =
один проект, `ids` = один идентификатор
**Then** в потоке приходят **только** события, удовлетворяющие **всем трём** условиям сразу
**And** событие, удовлетворяющее двум из трёх, не приходит
**And** порядок событий — строго по возрастанию номера последовательности

> Отказная сторона у этого сценария — **следующий**: без него «фильтр сузил» неотличимо от
> «сервер вернул пусто по любой причине».

**WATCH-1-02 · Незаданная ось не сужает — и это её единственное значение (П)**

**Given** те же события
**When** клиент открывает подписку, не задав ни одной оси
**Then** приходят события всех видов, всех проектов и всех идентификаторов **в пределах
прав вызывающего**
**And** ответ не отличается от подписки, где каждая ось задана перечнем всех своих значений

> Это положительный контроль к WATCH-1-01. Без него отрицание зеленело бы на сервере,
> который не отдаёт ничего никогда.

**WATCH-1-03 · Вид вне закрытого словаря — ОТКАЗ, а не пустой поток (П)**

**Given** словарь видов владельца содержит перечисленные значения и не содержит значения `X`
**When** клиент открывает подписку с `kinds = [X]`
**Then** поток **не открывается**: вызывающий получает `INVALID_ARGUMENT` с именем поля и
названным значением
**And** он **не** получает открытый поток, в который никогда ничего не придёт

> **Почему это отдельный сценарий, а не педантизм.** Ровно этот дефект уже назван в дереве:
> комментарий действующей формы объясняет, что подписчик, попросивший снятый вид, получал бы
> «пустой поток вместо отказа — то есть молчание вместо ответа». Пустой поток — это
> утверждение «событий нет», которого сервер не имеет права делать про вход, который он не
> понял (`api-conventions.md` §«Пустое значение обязано означать „пусто“»).

**WATCH-1-04 · Негодный идентификатор в `ids` — отказ первым сообщением (П)**

**Given** подписка на владельца, у которого адресация идёт по immutable `id`
**When** клиент открывает подписку с `ids`, содержащим строку, не являющуюся
идентификатором ресурса
**Then** `INVALID_ARGUMENT` с названным значением — **до** того, как открыт поток и до любого
чтения журнала
**And** идентификатор правильной формы, которому не отвечает ни один ресурс, отказом
**не** является: подписка открывается и просто не приносит по нему событий

> Различие — из `api-conventions.md` §«By-lane code-split»: «негодная форма» и «такого нет»
> суть разные полосы, и подписка не вправе их смешивать. Вторая половина обязательна:
> без неё сервер, отвергающий всякий незнакомый идентификатор, прошёл бы сценарий.

**WATCH-1-05 · Проект, недоступный вызывающему, — отказ, а не молчание (П)**

**Given** вызывающий не имеет доступа к проекту `P`
**When** он открывает подписку с `project_id = P`
**Then** он получает отказ полосы доступа
**And** он **не** получает открытый поток, молчащий вечно
**And** текст и код отказа не позволяют отличить «проекта нет» от «доступа нет» — форма
отказа та же, что у обычного чтения этого владельца

> Последняя строка — не украшение: различимый текст превращает подписку в способ узнать
> существование чужого проекта, а именно это `security.md` §Hardening-инварианты и требует
> закрыть байт-идентичностью.

**WATCH-1-06 · В форме нет ни имени, ни меток — и сказано ПОЧЕМУ (К)**

**Given** объявленная общая форма подписки
**When** её читают
**Then** среди осей фильтра нет поля имени ресурса и нет поля меток
**And** рядом стоит причина: имя — мутабельный косметический label (ban #15), подписка по
нему **молча** перестаёт совпадать после переименования — ни события, ни ошибки; метки
мутабельны, ресурс входит и выходит из выборки, и без предыдущего состояния выход
неотличим от удаления
**And** сказано, чем это закрывается: адресный отбор — осью `ids`, метки — фильтром на
стороне клиента, потому что событие несёт полное состояние

> **Причина обязана стоять в самом контракте, а не только здесь.** Поле без записанной
> причины отсутствия заводит следующий — «для удобства», — и он будет прав по-своему.

**WATCH-1-07 · Ось с мутабельной семантикой не заводится молча (К)**

**Given** гейт дерева, знающий закрытый перечень осей общей формы
**When** в форму добавляют поле, чья семантика мутабельна (имя, метка, произвольная строка
описания)
**Then** прогон **краснеет** и называет имя поля
**And** добавление поля с иммутабельной семантикой в тот же перечень прогон **не** роняет

> Вторая половина — законный близнец. Без неё гейт ловит форму (появление поля), а не
> существо (мутабельность), и первый же законный ввод его отключит.

---

### §4.2 Курсор и позиция начала

**WATCH-1-08 · Курсор исключающий (П)**

**Given** в журнале лежат события с номерами `N-1`, `N`, `N+1`
**When** клиент открывает подписку с `from_sequence_no = N`
**Then** приходят события с номерами строго **больше** `N`
**And** событие с номером ровно `N` **не** приходит
**And** событие с номером `N-1` не приходит

**WATCH-1-09 · Переподключение не теряет и не задваивает (П)**

**Given** клиент получил события до номера `M` включительно и запомнил `M`
**And** пока он был отключён, владелец записал события `M+1` и `M+2`
**When** клиент переподключается с `from_sequence_no = M`
**Then** он получает `M+1` и `M+2` — оба, каждое **по одному разу**
**And** он **не** получает `M` повторно
**And** тот же исход наступает, если переподключение попало на **другую реплику** сервера

> Последняя строка связывает контракт с решением эпика: сервер обязан оставаться
> stateless — позиция принадлежит **клиенту**, а не подписке. Подписка, помнящая своего
> подписчика, к другой реплике не переподключается.

**WATCH-1-10 · Три состояния позиции различимы — отсутствие представимо отдельно от значения (К+П)**

**Given** объявленная форма
**When** клиент хочет выразить «начни с начала журнала», «начни с позиции `N`» и «начни с
текущего конца»
**Then** каждое из трёх выразимо **однозначно**, и ни одно не требует угадывать намерение по
значению
**And** «поле не задано» не означает молча одно из трёх

> **Почему это сценарий, а не деталь.** Сегодня три состояния слиплись в два: скалярное поле
> не различает омит и ноль, поэтому «не задал» и «с начала» — одно и то же значение на
> проводе. Действующий контракт это честно оговаривает и предупреждает, что омит — полная
> выдача журнала, а не дешёвая подписка на хвост. Оговорка не лечит: она объясняет ловушку,
> а не убирает её. Это ровно класс «отсутствие, представимое отдельно от значения», и
> закрывается он формой, а не комментарием.
>
> **Отказная сторона (К):** форма, в которой «с текущего конца» невыразимо иначе как
> «сначала спроси максимум, потом подпишись», сценарий **не проходит** — такой обход
> заводит гонку: между вопросом и подпиской журнал пополняется, и эти события теряются.

**WATCH-1-11 · Курсор ниже удержанного горизонта — явный отказ (П)**

**Given** владелец удерживает журнал начиная с номера `H` (события ниже `H` из журнала
удалены)
**When** клиент открывает подписку с `from_sequence_no = K`, где `K < H`
**Then** он получает **явный отказ** с указанием, что запрошенная позиция больше не
удерживается, и с номером, с которого подписка возможна
**And** он **не** получает поток, начинающийся с `H` молча
**And** клиент, запросивший `K >= H`, получает поток обычным порядком

> **Этот сценарий заводится СЕЙЧАС, хотя воспроизвести его на дереве нельзя.** Чистки
> журнала в прод-дереве нет (`git grep -in 'DELETE FROM compute_outbox' origin/main -- services/compute`
> → пусто), поэтому горизонт сегодня равен началу и класс невидим. Невидим — не значит
> отсутствует: журнал, который не чистят, растёт неограниченно, и чистка появится. Если к
> тому моменту контракт не умеет сказать «твоя позиция утрачена», клиент получит тихую
> недовыдачу и **запишет её как «изменений не было»**. Отказ дописать в контракт потом —
> ломающее изменение; сейчас — одно поле.
>
> **Законное состояние «горизонт не удерживается» обязано быть выразимо** (владелец не
> чистит журнал): тогда отказ не наступает никогда, и это свойство владельца, а не
> умолчание формы.

**WATCH-1-12 · Первое сообщение потока — позиция начала, а не событие (П)**

**Given** любая открытая подписка, с любыми осями и любой позицией
**When** сервер принял подписку
**Then** **первым** сообщением приходит позиция: номер, с которого сервер начал отдавать,
и признак того, догнал ли клиент хвост журнала на момент открытия
**And** только после неё идут события
**And** это верно и когда событий нет вовсе: позиция приходит, поток остаётся открыт

**WATCH-1-13 · «Событий не было» отличимо от «я подключился позже» (П)**

**Given** клиент открывает подписку «с текущего конца»
**And** за время наблюдения владелец не записал ни одного события
**When** клиент читает поток
**Then** он получил позицию `S` и после неё ничего
**And** по `S` он способен установить, что изменений после его подключения не было —
в отличие от случая, когда он подключился с позиции `S' < S` и события между `S'` и `S`
существовали, но подписка их не покрыла

> Это тот же класс, что и WATCH-1-03, но на другой оси: молчание не является ответом,
> пока клиент не знает, **о каком отрезке** оно сделано.

---

### §4.3 Форма объявлена ОДИН раз

**WATCH-1-14 · Число объявлений не растёт с числом доменов (К)**

**Given** общая форма объявлена в общем пакете контрактов
**When** к подписке подключают второй, третий, седьмой домен
**Then** число объявлений запроса подписки в дереве **не меняется**
**And** каждый домен **ссылается** на общий тип импортом, как это уже делают **9** доменных
контрактов с `kacho.cloud.reference`

**WATCH-1-15 · Предикат единственности СПОСОБЕН упасть (К)**

**Given** предикат, которым проверяется п. WATCH-1-14
**When** его прогоняют на состоянии дерева **до** Ф1
**Then** он даёт **не** тот результат, что после Ф1 — то есть различает состояния
**And** когда в домен возвращают собственное объявление запроса подписки, предикат
**краснеет** и называет файл
**And** когда домен добавляет импорт общего типа, предикат **молчит**

> **Почему сценарий вынесен отдельно.** Предикат эпика (`returns (stream` → `1`) на стволе
> уже даёт `1` (Р1) и потому зелен и до начала работы, и на состоянии «общей формы нет
> вовсе». Гейт, зелёный на всех состояниях, свойства не измеряет. Ф1 обязана поставить
> предикат, считающий **объявления запроса подписки**, а не потоковые методы, и доказать
> его инъекцией в обе стороны.

**WATCH-1-16 · Домен не заводит своего запроса подписки (К)**

**Given** гейт из WATCH-1-15
**When** в доменном контракте появляется собственный тип запроса подписки со своим набором
осей
**Then** прогон краснеет с именем домена и файла
**And** доменное сообщение **события** (полезная нагрузка, специфичная для владельца)
гейт **не** трогает — единой объявляется форма подписки, а не форма события

> Граница названа намеренно: унификация, взявшая слишком широко, потребовала бы от семи
> доменов одинаковой полезной нагрузки, которой у них нет.

---

### §4.4 Порядок снятия прежней формы

**WATCH-1-17 · Ф1 не снимает живую форму — окна без подписки нет (К)**

**Given** Ф1 влита в ствол
**When** замеряют поверхность подписки
**Then** `compute.v1.InternalWatchService/Watch` **объявлен, смонтирован и обслуживается** —
ровно как до Ф1
**And** общая форма объявлена рядом и ни одним доменом ещё не взята
**And** состояние «прежняя форма снята, общая не взята никем» **не наступает ни в одной
ревизии ствола**

> Это и есть порядок, зафиксированный сценарием, а не пожеланием: снятие идёт **после**
> перевода модуля (Ф3), потому что иначе между фазами существует ревизия, где подписки нет
> ни в какой форме, — и на неё может прийтись выкатка.

**WATCH-1-18 · Ведомость разрывов на стволе остаётся пустой (К)**

**Given** ведомость `proto/declared-breaks.yaml`, чья шапка объявляет: нормальное состояние
ствола — пусто, находка вне перечня — красное, **запись без находки — тоже красное**
**When** Ф1 влита
**Then** перечень ведомости **пуст**
**And** решение о снятии прежней формы записано в теле задачи #1017 и в §7 этого документа,
а **не** заготовкой в ведомости

> **Требование задачи #1017 «заготовить записи в `declared-breaks.yaml`» неисполнимо by
> construction** (Р3): заготовленная запись — это запись, у которой нет находки, и
> адъюдикация обязана на ней покраснеть. Два правила об одном предмете, из которых верно
> одно; верно правило ведомости, потому что оно исполняется конвейером. Запись заводит
> **та ветка, которая ломает**, и она же истекает при вливании.

**WATCH-1-19 · Имя уже снятой формы занесено в надгробие (К)**

**Given** форма `loadbalancer.v1.InternalResourceLifecycleService/Subscribe` снята со ствола
(Р2), а в надгробии `retiredRPCSurface` её имени **нет** (Р4)
**When** Ф1 влита
**Then** имя стоит в надгробии с причиной
**And** попытка вернуть это имя в контракт роняет прогон

> Ф1 берёт это на себя, хотя снимала форму другая задача. Основание: эпик обещает «остаётся
> одна форма», а имя, не зарезервированное надгробием, возвращается молча — и вернётся оно
> именно под замысел «а нам нужна подписка», то есть под предмет этого эпика.

**WATCH-1-20 · Утверждение надгробия, ставшее ложным, исправлено (К)**

**Given** запись `kacho.cloud.vpc.v1.InternalResourceLifecycleService/Subscribe` в надгробии
несёт в причине фразу «Живым остаётся `loadbalancer.v1.InternalResourceLifecycleService/Subscribe`»
(`internal/repohygiene/retiredrpcsurface_test.go:112`)
**And** названная форма снята
**When** Ф1 влита
**Then** причина приведена к дереву: названа снятой, с указанием, чем подписка выражается
теперь
**And** сама запись **остаётся** — надгробие не истекает, это его свойство

> Два места об одном предмете, из которых верно одно. Поучительно, что ложным стало не
> объявление, а **объяснение**: причина пережила предмет, на который ссылалась, и читается
> как действующее указание «вот живая форма, бери её».

**WATCH-1-21 · Снятие сопровождается разрывом в ломающей ветке (П → Ф3/Ф7)**

**Given** модуль переведён на общую форму (Ф3)
**When** ветка снимает прежнюю форму
**Then** в **этой ветке** ведомость разрывов несёт запись с обязательными полями, включая
`issue` разрешимой ссылкой и `reason`
**And** имя снятого метода попадает в надгробие
**And** после вливания запись из ведомости **исчезает**, потому что база сравнения поднялась
и предмета у неё не стало

---

### §4.5 Третий механизм — push-инвалидация iam

**Исход, который приёмка фиксирует: механизм ПЕРЕВОДИТСЯ, но не в Ф1; направление
переворачивается.**

Сегодня отношение таково: сервер объявлен в контуре края, клиент — в iam
(`services/iam/cmd/kacho-iam/subject_change_wiring.go`,
`services/iam/internal/clients/cache_invalidation_applier.go` — **2** прод-файла iam,
знающих контракт края; DoD эпика требует довести до нуля). То есть **iam звонит краю**, а
iam объявлен листом (`polyrepo.md`): его зовут, он не зовёт никого. Ребро обратное.

Единая форма снимает его **сменой направления, а не переводом вызова**: соединение
открывает **потребитель** (край), владелец о потребителе не знает. Ацикличность цела —
край и так зовёт iam на каждом запросе.

**WATCH-1-22 · Форма применима к предмету «отзыв доступа субъекта» (К)**

**Given** общая форма подписки
**When** её примеряют к владельцу, чьи события — не про ресурс, а про **субъект прав**
**Then** форма выразима: вид события — значение закрытого словаря **владельца**, а не
фиксированный перечень «ресурсы домена»
**And** ни одна ось не требует непустого идентификатора **ресурса** как условия открытия
подписки
**And** ось `project_id` допускает состояние «событие не принадлежит проекту» — иначе
предмет уровня аккаунта или кластера в форму не помещается

> **Отказная сторона (К):** форма, у которой идентификатор ресурса обязателен либо словарь
> видов заперт на ресурсные, сценарий **не проходит** — она делает #1024 невозможным, и
> обнаружится это на четыре фазы позже, когда чинить будет ломающим изменением.

**WATCH-1-23 · Направление зафиксировано контрактом, а не соглашением (К)**

**Given** общая форма
**When** читают её объявление
**Then** сказано: подписку открывает **потребитель** к **владельцу** событий; владелец
подписчиков не знает и никому не звонит
**And** сказано следствие: это единственное направление, при котором лист остаётся листом

**WATCH-1-24 · Исход по push-инвалидации назван С ПРЕДИКАТОМ (К)**

**Given** Ф1 влита
**When** читают §7 этого документа и тело задачи #1017
**Then** исход по третьему механизму назван: **перевести**, задача #1024, направление
потребитель→владелец
**And** назван предикат завершённости: прод-файлов iam, знающих контракт края, — **ноль**
(сегодня **2**)
**And** названо, что делать, **если** перевод не состоится: механизм остаётся вне единой
формы, и тогда причина записывается там же, с предикатом пересмотра, — а не остаётся
«пока так»

> Формулировка «остаётся вне единой формы» без предиката запрещена: она неотличима от
> забытого. Предмет уже показал, что переживает своё основание, — см. Р4 и WATCH-1-20.

---

### §4.6 Конвенция приводится к дереву

**WATCH-1-25 · «Watch RPC не существует» приведено к дереву (К)**

**Given** `.claude/rules/api-conventions.md` утверждает «**Watch RPC не существует**»
**And** в дереве потоковая подписка существует и обслуживается (Р1)
**When** Ф1 влита
**Then** утверждение приведено к дереву: подписка на изменения **есть**, она одна, она
внутренняя, и назван её предмет
**And** утверждение, опровергаемое деревом, в правилах не остаётся

**WATCH-1-26 · Правка НЕ отменяет полл операции (К)**

**Given** правленный раздел конвенции
**When** его читают
**Then** сказано: мутация по-прежнему возвращает `Operation`, и её исход узнаётся поллом —
подписка на операции не заводится
**And** сказано **почему одного потока ресурсов недостаточно**: событие пишется в той же
транзакции, что и ресурсная строка, поэтому при отказе операции события нет вовсе, и
наблюдатель ресурсов не отличит «ещё делается» от «упало»

> Без второй половины правка конвенции превращается в разрешение «подписывайся вместо
> поллинга везде», и следующий снимет полл операции, получив систему, в которой отказ
> невидим.

---

### §4.7 Периметр

**WATCH-1-27 · Форма объявлена внутренней (К)**

**Given** общая форма и метод подписки
**When** читают объявление и маршруты края
**Then** метод не выставлен на внешнем крае (ban #6)
**And** запись каталога прав для него существует — метод без записи каталога отвергается
в рантайме, то есть возможность объявлена и не работает

**WATCH-1-28 · Неназванный вызывающий отвергается безусловно (П)**

**Given** подписка, за которой стоит пообъектное сужение, а не проверка на метод
**When** подписку открывает вызывающий, чья личность не установлена
**Then** отказ — **всегда**, независимо от режима стенда и настроек
**And** вызывающий с установленной личностью и правами поток получает

> Положительная половина обязательна: без неё сценарий проходит на сервере, отвергающем
> всех.

**WATCH-1-29 · Сужение обязано сужать (П)**

**Given** подписка открыта
**When** механизм пообъектного сужения отсутствует либо не сужает
**Then** подписка **не открывается** — отказ, а не поток без сужения
**And** при работающем сужении подписка открывается и приносит только доступное вызывающему

> За потоком нет проверки на метод, на которую можно откатиться: «сужение выключено»
> означает «не сужено ничем». Это тот же класс, что «пустой перечень доверенных значит
> „доверяем всем“» (`security.md`).

**WATCH-1-30 · Поток допускает закрытие при отзыве прав (К — конструируемость для #1022)**

**Given** общая форма
**When** её примеряют к сценарию «у вызывающего отозвали доступ, пока поток открыт»
**Then** форма позволяет серверу **закрыть поток** с отличимым исходом, а не молча
перестать слать события
**And** клиент по этому исходу отличает «доступ отозван» от «сервер перезапустился» —
в первом случае переподключение бессмысленно, во втором обязательно

> Ф1 не реализует отзыв (#1022). Ф1 обязана не сделать его невыразимым: поток, у которого
> «перестал слать» и «закрыт по правам» — одно наблюдение, чинится ломающим изменением.

---

## §5 Чем каждое требование будет удержано

Требование без держателя — пожелание, и оно помечено как пожелание, а не выдано за
проверяемое.

| Требование | Держатель | Вид | Где |
|---|---|---|---|
| WATCH-1-01…05 | пробы соответствия против эталонного сервера | интеграционная проба | Ф2 · #1018 |
| WATCH-1-06 | разбор контракта: закрытый перечень осей + требование причины у отсутствующих | гейт дерева | Ф1 |
| WATCH-1-07 | тот же гейт, доказанный инъекцией в обе стороны | гейт + инъекция | Ф1 |
| WATCH-1-08…09 | пробы соответствия; переподключение — к **другой** реплике | интеграционная проба | Ф2 |
| WATCH-1-10 | разбор контракта: три состояния позиции различимы на проводе | гейт дерева | Ф1 |
| WATCH-1-11 | конструируемость в Ф1 (поле исхода есть); поведение — Ф2 | гейт + проба | Ф1 / Ф2 |
| WATCH-1-12…13 | пробы соответствия | интеграционная проба | Ф2 |
| WATCH-1-14…16 | предикат числа объявлений запроса подписки + гейт «домен не заводит своего» | гейт дерева + инъекция | Ф1 |
| WATCH-1-17 | сценарий порядка проверяется **на каждой ревизии ствола**: методов подписки, обслуживаемых хотя бы одним листенером, ≥ 1 | гейт дерева | Ф1, действует до конца эпика |
| WATCH-1-18 | адъюдикация ведомости разрывов (уже существует, зовётся конвейером) | существующий гейт | конвейер |
| WATCH-1-19…20 | надгробие `retiredRPCSurface` + его собственные пробы | существующий гейт | Ф1 |
| WATCH-1-21 | адъюдикация ведомости + надгробие | существующий гейт | Ф3/Ф7 |
| WATCH-1-22 | разбор контракта: ни одна ось не обязательна, словарь видов принадлежит владельцу | гейт дерева | Ф1 |
| WATCH-1-23 | **вниманием** — «кто открывает соединение» есть свойство замысла, предикатом не выражается; проверяется на ревью контракта | — | Ф1 |
| WATCH-1-24 | предикат «прод-файлов iam, знающих контракт края, — ноль» | предикат, исполняется в #1024 | #1024 |
| WATCH-1-25…26 | проверка свежести документации + ревью правки | хук + ревью | Ф1 |
| WATCH-1-27 | гейт достижимости каталога прав + перепись маршрутов края (оба уже существуют) | существующий гейт | Ф1 |
| WATCH-1-28…29 | пробы соответствия, обе половины | интеграционная проба | Ф2 |
| WATCH-1-30 | разбор контракта: исход закрытия отличим | гейт дерева | Ф1 |

**Три требования держатся вниманием, и это сказано прямо:** WATCH-1-23 (направление
соединения), выбор словаря видов у каждого владельца и формулировка причин в контракте
(WATCH-1-06). Предиката у них нет и не будет — это суждения о замысле, а не свойства текста.

---

## §6 Конструируемость: чем поведенческий сценарий обеспечен в контракте

Поведенческий сценарий, для которого в контракте нет элемента, — не сценарий, а пожелание:
его нельзя будет поставить и в Ф2. Ф1 проверяет **эту** таблицу.

| Сценарий | Элемент контракта, без которого он не ставится | Есть в действующей форме |
|---|---|---|
| WATCH-1-01 | оси `project_id` и `ids` | **нет** — сегодня только `kinds` |
| WATCH-1-03 | закрытый словарь видов + исход отказа | словарь есть, **отказа нет** |
| WATCH-1-04 | различение «негодная форма» и «такого нет» | **нет** |
| WATCH-1-05 | исход полосы доступа при открытии | есть частично |
| WATCH-1-08 | исключающая семантика курсора | **есть** |
| WATCH-1-09 | позиция принадлежит клиенту, сервер stateless | **есть** |
| WATCH-1-10 | три состояния позиции различимы | **нет** — омит и ноль слиты |
| WATCH-1-11 | исход «позиция утрачена» + номер удержанного горизонта | **нет** |
| WATCH-1-12…13 | первое сообщение потока несёт позицию и признак догона | **нет** |
| WATCH-1-22 | ни одна ось не обязательна; словарь принадлежит владельцу | **нет** — форма ресурсная |
| WATCH-1-30 | отличимый исход закрытия по правам | **нет** |

**Восемь строк из одиннадцати сегодня не обеспечены.** Это и есть содержание Ф1: не
«переименовать существующее в общее», а добавить то, без чего восемь сценариев
не поставить никогда.

---

## §7 Решения, которые приёмка фиксирует

1. **Форма одна, объявлена в общем пакете контрактов, домены её импортируют.** Образец —
   `kacho.cloud.reference` (**9** импортирующих доменных контрактов). Второго языка фильтров
   не заводится.
2. **Оси — `kinds` × `project_id` × `ids`, конъюнкция, все три необязательны.** Имени нет
   (ban #15), меток нет (фильтрует клиент). Причина стоит в контракте, а не только здесь.
3. **Позиция начала — три различимых состояния**, и первое сообщение потока её называет.
   Это то, что делает «событий не было» отличимым от «я подключился позже».
4. **Курсор исключающий, принадлежит клиенту.** Сервер stateless, поток переподключается к
   любой реплике.
5. **Порядок: объявить → сервер → перевести модуль → снять прежнюю форму.** Снятие идёт
   после перевода, и это проверяется гейтом (WATCH-1-17), а не помнится.
6. **Запись разрыва заводит ломающая ветка, не Ф1.** Требование #1017 «заготовить записи»
   отклонено как неисполнимое: ведомость на стволе обязана быть пуста (Р3).
7. **Имя снятой формы nlb заносится в надгробие в Ф1**, и ложная причина соседней записи
   правится там же (Р4, WATCH-1-19/20).
8. **Push-инвалидация iam переводится, направление переворачивается**: соединение открывает
   край, iam остаётся листом. Не в Ф1 — в #1024. Предикат завершённости: прод-файлов iam,
   знающих контракт края, — ноль (сегодня 2). Если перевод не состоится, причина
   записывается с предикатом пересмотра, а не остаётся умолчанием.
9. **Подписка не заменяет полл операции.** Отказ операции не порождает события вовсе.
10. **Предикат единственности переписывается** так, чтобы он различал состояния: считает
    объявления запроса подписки, а не потоковые методы (Р1, WATCH-1-15).

---

## §8 DoD Ф1

1. `buf lint` и `buf breaking` зелёные; генерация стабов воспроизводится плагинами из модуля.
2. Общая форма объявлена **один раз**; предикат единственности переписан и **доказан
   инъекцией в обе стороны** (красное на возвращённом доменном объявлении, молчание на
   импорте общего типа).
3. Гейты §5, приписанные Ф1, приземлены и доказаны инъекцией; каждый печатает объём
   осмотренного, чтобы «ноль находок» было отличимо от «ноль прочитанного».
4. Таблица §6 закрыта: у каждого поведенческого сценария есть элемент контракта, которым он
   ставится.
5. Имя снятой формы nlb — в надгробии; ложная причина соседней записи исправлена.
6. `api-conventions.md` §«Watch RPC не существует» приведён к дереву **вместе** с оговоркой
   про полл операции.
7. Ведомость разрывов на стволе **пуста**.
8. Решения §7 записаны в теле #1017; расхождения §1 внесены в #1017 и #1016 — иначе
   постановка продолжит опираться на устаревший замер.
9. Приёмка APPROVED от `acceptance-reviewer` до первой строки кода (ban #1).

---

## §9 Что эта приёмка НЕ утверждает

- Она **не** утверждает, что сегодняшняя форма compute плоха. Она отлажена, несёт
  пообъектное сужение, предел числа потоков и честные оговорки в комментариях. Предмет Ф1 —
  что она **частная**: две оси из трёх, без позиции начала, без исхода утраченного курсора,
  и не применима к предмету, который не является ресурсом домена.
- Она **не** утверждает чисел эпика по outbox и по консоли: `storage_outbox` дропнут, у iam
  ресурсного журнала нет (Р5), а `refetchInterval` шире названного — **24** файла, **36**
  вхождений. Перепись «у кого журнал уже есть» — предмет Ф2, и её надо снять заново.
- Она **не** назначает срок и не оценивает объём: это дело плана, а не приёмки.
