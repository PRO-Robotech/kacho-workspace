<!--
Copyright (c) PRO-Robotech
SPDX-License-Identifier: BUSL-1.1
-->

# Sub-phase F8 (церемонии личности рисует консоль — свои экраны вместо чужого приложения) — Acceptance

> **Статус:** DRAFT
> **Статическая форма:** DRAFT; действующий вердикт выводится из внешнего события и записи ревью
> (`.claude/rules/change-graph.md` §2 «Вердикт привязан к ОТПЕЧАТКУ, а не к документу»)
> **История review:** только дописывается; прежние строки не редактируются
> — **круг 1 · 2026-09-22 · ⛔ CHANGES_REQUESTED · редакция 1, отпечаток
> `95d91dfd733f9ea7343c640b80ee86e4096ce5e3b65849059a257ccc07f884e5`** (предикат:
> `sha256sum <путь>`; документ не закоммичен, поэтому ревизией отпечаток не привязан и
> сравним только с рабочим деревом). Блокирующих **восемь**, роды: SCOPE · COVERAGE ·
> NEGATIVE · TWIN · CONSTRUCTIBILITY ×2 · DoD · FORMAT. Записи ревью в дереве нет —
> вердикт передан сообщением оркестратора
> — **круг 2 · 2026-09-22 · ⛔ CHANGES_REQUESTED · редакция 2, отпечаток
> `cb4d4988d252ef7c9b1a71d9d880b94627d84b882efcb4e969d32bc3147ec681`.** Блокирующих
> **четыре**, и **три из четырёх — конструируемость**, то есть род повторился **второй круг
> подряд**. Поэтому правилась **форма работы**, а не замечания: четыре обязанности вошли в
> документ разделом **§1.0** и применяются ко всякому будущему утверждению о дереве
> (`.claude/rules/multi-agent-flow-orchestration.md` §14а). Записи ревью в дереве нет
> — **редакция 3 · 2026-09-22 · вердикта на неё НЕТ.** Сценариев стало **45**, решений —
> **16**; дельта — §16.2
> **Дата:** 2026-09-22
> **Эпик/issue:** эпик `PRO-Robotech/kacho#1266`; задача `#1274` (Ф8), находка о свидетеле `#2780`

- **Дом документа и основание выбора — §0.3.** Дом — воркспейс `docs/specs/`, и это решение,
  а не дрейф (`.claude/rules/polyrepo.md` note «У приёмки домов ДВА»).
- **Ревизии измерения — по дому, и без дома половина команд ниже даёт пустоту:**
  дерево платформы `PRO-Robotech/kacho@f445aaaa554` (ветка `main` рабочей копии) и
  `origin/main@bec320cf47d`; полоса развёртывания `wave/identity-own-w0-kacho@0e0a85f936e`
  (условная полоса раздачи и гейт живут ТАМ, а не в стволе — §1.6); дом службы
  `PRO-Robotech/kaname@497c44b7`.
- **Формат:** Given-When-Then, только markdown. Кода здесь нет по построению.
- **Публичный репозиторий:** оба ствола публичны; документ написан так, чтобы выдерживать
  публикацию (`.claude/rules/security-disclosure.md` §«Публичные артефакты»). Сборки «точная
  координата + условие + следствие» в нём нет, ссылок на рабочие каталоги задач — тоже.

---

## §0 Обзор

### 0.1 Что реализуется

Консоль получает **собственные экраны церемоний личности** — вход, регистрацию, выход,
параметры учётной записи и второй фактор — и ведёт их своими глаголами, а не отсылает человека
в чужое приложение. Сегодня церемонию входа физически резолвит чужой экран: своих страниц у
консоли **ноль** (§1.1), и снять чужого поставщика невозможно не потому, что что-то настроено
неверно, а потому, что переключать не на что.

Служба доступа свою половину уже несёт: **тринадцать глаголов** семейства `/iam/v1/auth/*`
объявлены одним перечнем, край их ретранслирует, и все тринадцать доступны браузеру на
происхождении консоли (§1.3). Приёмки самих церемоний одобрены в доме службы и здесь **не
переоткрываются** (§0.2). Предмет этого документа — **интерфейс, через который человек эти
церемонии проходит**.

### 0.2 Чего этот документ НЕ решает — две одобренные приёмки соседа

Две приёмки в `PRO-Robotech/kaname` несут сценарии **самих церемоний**, и обе называют экран
консоли своим выносом — дословно, а не по смыслу:

| приёмка соседа | её предмет | как она называет этот документ |
|---|---|---|
| `docs/engineering/acceptance/passwordless-login-with-access-key.md` (Ф13) | два глагола входа ключом доступа на полосе формы, вид признака формы у каждого, текст отказа | «экран входа ключом, кнопка рядом с паролем, обработка испытания браузером, снятие полей сериализации — 0 сценариев, консоль — потребитель края, **Ф8 `#1274`, OPEN**» |
| `docs/engineering/acceptance/access-keys-are-ours.md` (Ф7) | ключ доступа как запись службы: колонки, потолок, отзыв, признак обнаружимости | «экран церемонии, перечень ключей на экране, окно повышения — **Ф8 `#1274`**; предмет приёмки — что консоль **вызывает**, а не как выглядит» |

**Шов, чтобы два места не говорили об одном предмете.** Там — **что служба обещает** глаголом:
форма запроса, вид признака, код и текст отказа, что записывается и что отзывается. Здесь —
**что человек видит и что консоль зовёт**: какой адрес принадлежит консоли, какой экран на нём,
какое обращение он делает, что показывает на каждом исходе и куда уводит после. Ни один
сценарий §11 не утверждает о содержимом записи службы, о сроке сессии, о правиле пароля и о
тексте отказа как о **решении** — тексты отказов здесь цитируются как **вход** экрана, и
правятся они ТОЛЬКО в доме службы.

Предикат шва, проверяемый: в §11 нет ни одного «Тогда», чей производитель лежит в
`PRO-Robotech/kaname`. Все производители — браузерная проба, модульная проба консоли, перепись
дерева консоли и рендер посадки (§11.1).

Тем же швом отрезаны приёмки Ф3 (`login-lane-issues-our-session-and-logout-ends-it-server-side.md`),
Ф4 (`registration-and-its-three-consequences.md`), Ф5 (`recovery-of-access.md`) и Ф12 (второй
фактор): они решают **глаголы**, этот документ — **экраны**.

### 0.3 Дом документа — воркспейс, и вот основание

Домов у приёмки два, и выбор объявляется (`.claude/rules/polyrepo.md` note «У приёмки домов
ДВА, и это решение, а не дрейф»). Выбран воркспейс `docs/specs/`. Три довода, каждый — факт
дерева, а не вкус:

1. **Предмет лежит в `PRO-Robotech/kacho`, а не в службе доступа.** Правятся `ui-future/**`
   (экраны, маршруты, клиент церемонии), `ui-future/e2e/specs/**` (браузерные пробы) и
   перепись прод-файлов консоли. Дом `kaname/docs/engineering/acceptance/` предназначен для
   предмета **внутри одного сервиса службы доступа** — этот предмет им не является.
2. **Прецедент того же эпика измерен.** Приёмка F4d — предмет в дереве платформы (посадка,
   край, стенды) — лежит в воркспейсе: `docs/specs/sub-phase-F4d-identity-provider-posture-acceptance.md`,
   и на неё ссылается сам профиль посадки (`deploy/helm/umbrella/values.own.yaml`, шапка).
3. **Контуром пакета изменения работа не ведётся.** В `docs/changes/` пакетов четырнадцать, и
   `acceptance.md` есть ровно у одного (`standalone-iam`); у остальных — только `reviews/`.
   Заводить пятнадцатый пакет ради одного документа значило бы завести второе представление
   без предмета.

**Расщепления по репозиториям у предмета нет, и это сказано прямо, а не умолчано:** ни один
сценарий §11 не требует правки в `PRO-Robotech/kaname`. Тринадцать глаголов, на которые
опираются экраны, уже в дереве службы (§1.3); два глагола Ф13, которых там нет, вынесены в §12
вместе со своей под-фазой.

Запись ревью этого документа кладётся в
`docs/specs/reviews/sub-phase-F8-console-identity-ceremony-screens-acceptance/<sha256>.yaml`.

---

## §1 Что верно в дереве — измерено, а не предположено

Перепись производителей идёт **до единого сценария**: у каждого «Тогда» §11 обязан быть тот,
кто его производит, и перепись — единственный способ это обеспечить
(`.claude/rules/multi-agent-flow-orchestration.md` §14а).

### 1.0 Как в этом документе получено каждое число — четыре обязанности

**Род блокирующего «конструируемость» повторился два круга подряд, и потому правилась ФОРМА
работы, а не отдельные замечания** (`.claude/rules/multi-agent-flow-orchestration.md` §14а
«Класс, отвергший работу ДВАЖДЫ, переезжает в форму задания»). Ниже — метод, по которому
получено каждое число §1 и каждое «Дано» §11. Он записан здесь, а не в памяти автора, потому
что оба круга валились ровно на его неприменении.

| # | обязанность | чем круг 2 её доказал |
|---|---|---|
| **1** | **мерить по МЕСТАМ ВЫЗОВА, а не по шапке, комментарию или документации** | бюджет Р7 редакции 2 был построен на шапке `rate.go` («отказы входа и неподошедшее подтверждение смены пароля»). Код **шире**: след пишут **пять** мест, и два из них шапка не называет. Сторож F8-41 краснел бы **на верной реализации** |
| **2** | **каждый предикат прогонять В ОБЕ СТОРОНЫ — сузить и расширить, и печатать, что дала расширенная форма** (даже если ничего) | редакция 2 записала этот вывод верно — и тут же снова мерила **по написанию пути** (`/self-service/`). Обращений оказалось **пять**, а не четыре, и расширение до предмета дало **второй файл** |
| **3** | **на каждую ось, снимаемую ВРЕМЕНЕМ, искать остальные такие же — перечень обязан быть полным, а не состоять из найденных** | Р7 разобрало две оси. Осей **пять**, и три из них редакция 2 не рассматривала вовсе (§1.10) |
| **4** | **на каждое снятие предмета называть судьбу КАЖДОЙ проверки о нём** | переезд посева отнимает предмет у спецификации из **семи** тестов и у **двух** распознавателей в фикстуре. Ни одна из них не зовёт чужой поток — она его **мокает**, поэтому DoD 12 её не ловит (§1.5а) |

Две формы, которые из этого следуют и применены ниже дословно:

- **предикат по написанию** отвечает на вопрос «как это записано», **предикат по предмету** — на
  вопрос «что это делает». Числа, полученные первым, годятся только рядом со вторым, и оба
  печатаются;
- **«проверка не зовёт предмет» не значит «проверка от него не зависит»**: мок, фикстура,
  распознаватель и вшитая разметка зависят от предмета, не обращаясь к нему ни разу.

### 1.1 Своих экранов церемоний у консоли ноль — 51 страница, 0 церемоний

Единица счёта — **файл `.tsx` под `*/src/pages/*` в `ui-future`**, без проб и без
`node_modules`. Предикат назван вместе с единицей, потому что тремя строками выше в дереве
живёт соседний счёт («маршрутов 162»), и это **другой** предикат о другом предмете.

```sh
# PRO-Robotech/kacho@f445aaaa554, каталог ui-future
PRED() { find . -path '*/node_modules' -prune -o -path '*/src/pages/*' -name '*.tsx' -print \
         | grep -v node_modules | grep -v '\.test\.' ; }
PRED | wc -l
PRED | grep -Eic '(login|signin|sign-in|registration|signup|sign-up|recovery|forgot|verification|logout|consent)'
```

| ревизия | страниц | из них церемоний |
|---|---|---|
| рабочая копия `f445aaaa554` | **51** | **0** |
| `origin/main@bec320cf47d` | **51** | **0** |
| та же копия с подсаженной `shared/src/pages/auth/LoginPage.tsx` | **52** | **1** |

**Контроль в обе стороны исполнен, и это не формальность:** отрицание «церемоний ноль» на
предикате, вовсе не умеющем распознать церемонию, зеленело бы тождественно. Подсадка снята,
рабочая копия чиста.

Страницы не «не написаны» — они были и **сняты** задачей `#1225`. Разбор снятия лежит в дереве
(`ui-future/shared/src/pages/auth/README.md`) и заканчивается условием, которое настал черёд
исполнять: «если решение когда-нибудь переменится и консоль станет вести церемонию сама, то тем
же изменением обязано сняться проксирование этих адресов: способ должен остаться ОДИН».

> **Посылка задачи `#1274` опровергнута, и это несущее уточнение.** Её тело написано ДО `#1225`
> и утверждает: «Наши страницы написаны (Login 352, Register 321, Settings 381, Recovery 232,
> Logout 100 …) но маршрутов на них ноль». Первая половина на измеренных ревизиях **ложна**:
> страниц церемоний ноль, монтировать нечего. Вторая половина верна и сегодня:
> `grep -rn 'path="/login"' ui-future --include='*.tsx'` → **0**. Работа по `#1274` — не
> «смонтировать написанное», а **написать**.

### 1.2 Неизвестный адрес консоль уводит на панель — отказ выглядит успехом

Маршрутизатор оболочки объявляет замыкающее правило:

```
ui-future/host/src/App.tsx:103   <Route path="*" element={<Navigate to="/dashboard" replace />} />
```

Следствие, наблюдаемое человеком: на посадке, где полоса потоков личности не обслуживается,
адрес `/login` доходит до общего `location /`, тот отдаёт оболочку консоли, оболочка не находит
маршрута и **молча переводит на панель**. Ни `404`, ни строки в журнале — «вход не
открывается» и всё. Ровно тот же класс, что раздача уже закрыла на своей стороне
(шапка `deploy/tests/helm/console-serves-identity-flows-test.sh`: «отказ выглядит УСПЕХОМ»), —
но на уровне оболочки он открыт.

Этим же правилом сегодня оканчивается церемония повышения уровня: `StepUpModal` уводит браузер
на адрес, собранный из настройки поставщика (`shared/src/lib/kratos.ts` — `settingsUrl` над
`initFlowUrl`), и на посадке без блоков поставщика этот адрес попадает под то же замыкающее
правило.

### 1.3 Глаголов формы в дереве службы тринадцать, и все тринадцать достижимы браузеру

Перечень объявлен ОДНИМ местом и читается тремя (`kaname@497c44b7`,
`internal/handler/loginlanehttp/handler.go`, `Paths()`; край —
`gateway/internal/middleware/login_lane_paths.go`):

| # | глагол | путь | метод | вид признака формы |
|---|---|---|---|---|
| 1 | вход | `/iam/v1/auth/login` | POST | `login` |
| 2 | выход | `/iam/v1/auth/logout` | POST | `logout` |
| 3 | смена пароля | `/iam/v1/auth/password` | POST | `password` |
| 4 | признак формы | `/iam/v1/auth/csrf?form=<вид>` | GET | — |
| 5 | регистрация | `/iam/v1/auth/register` | POST | `register` |
| 6 | запрос кода восстановления | `/iam/v1/auth/recovery` | POST | `recovery` |
| 7 | завершение восстановления | `/iam/v1/auth/recovery/complete` | POST | `recovery-complete` |
| 8 | состояние второго фактора | `/iam/v1/auth/second-factor` | GET | `second-factor` |
| 9 | заведение второго фактора | `/iam/v1/auth/second-factor/enroll` | POST | `second-factor` |
| 10 | подтверждение второго фактора | `/iam/v1/auth/second-factor/confirm` | POST | `second-factor` |
| 11 | снятие второго фактора | `/iam/v1/auth/second-factor/remove` | POST | `second-factor` |
| 12 | перечеканка запасных кодов | `/iam/v1/auth/second-factor/backup-codes` | POST | `second-factor` |
| 13 | повышение уровня внутри сессии | `/iam/v1/auth/step-up` | POST | `step-up` |

Достижимость браузеру — свойство раздачи, а не догадка: полоса `location ~ ^/iam/v1/` стоит
**безусловно** и уводит на край (`ui-future/deploy/templates/configmap-nginx.yaml`). То есть у
экранов консоли производитель есть уже сегодня, и заводить его не нужно.

### 1.4 Производителей способа входа в дереве службы ТРИ, административного среди них нет

```sh
# kaname@497c44b7
grep -rn 'InsertLoginMethod\|ReplaceLoginVerifier' --include='*.go' internal/ | grep -v _test.go
```

Пишут способ входа человека ровно три места: **регистрация** (`api/registration/register.go`),
**смена пароля** (`api/humansession/change_password.go`) и **завершение восстановления**
(`api/humansession/recovery_complete.go`); четвёртое попадание — перечеканка материала на
успешном входе, она заменяет свой же. **Глагола, которым администратор выдал бы человеку
способ входа, в дереве нет.**

Отсюда следствие для §2 Р4 и для порядка под-фаз: у человека, у которого способа входа нет,
самостоятельный путь **ровно один** — восстановление доступа.

### 1.5 Путей посева ДВА, а не один — и второй нашёлся только содержательным предикатом

> **Правка круга 1, и она о методе, а не о числе.** Редакция 1 мерила посев **по имени
> фикстуры** (`registerAndSignIn|tenantWithProject|register\(`). Такой предикат отвечает на
> вопрос «кто зовёт нашу фикстуру», а не на вопрос «кто ведёт чужую церемонию», и **второй
> путь посева пропустил целиком**. Контроля в обе стороны против него применено не было —
> хотя §1.1 настаивает ровно на нём. Ниже мерено **содержательным** предикатом, и оба числа
> названы рядом, потому что они об разном.

```sh
# kacho@f445aaaa554, ui-future/e2e/specs
ls *.spec.ts | wc -l                                                                     # 23  ← ПРОБ
ls *.ts      | wc -l                                                                     # 28  ← файлов: 23 пробы + 5 модулей
# предикат ИМЕНИ фикстуры (редакция 1; без `sort -u` аргументы дублируются — отсюда 47)
grep -l -E 'registerAndSignIn|tenantWithProject|[^a-zA-Z]register\(' *.spec.ts *.ts \
  | sort -u | wc -l                                                                      # 24
# предикат ПО НАПИСАНИЮ пути (редакция 2 — и он ЗАНИЗИЛ)
grep -c '/self-service/' assurance.ts                                                    # 4
# предикат ПО ПРЕДМЕТУ, сужение: что этот модуль ОТПРАВЛЯЕТ поставщику
grep -n 'page.request\.' assurance.ts                                                    # 3 места, строки 111 · 117 · 125
# предикат ПО ПРЕДМЕТУ, расширение: кто вообще зависит от поставщика
grep -rln -iE 'kratos|\.ory|KRATOS' *.ts | sort -u        # assurance.ts + registration-refusal-named.spec.ts
git ls-files 'ui-future/e2e/specs/*' | grep -icE 'login|signin|auth|ceremony'            # 0
```

> **Две правки круга 2, и обе — об обязанностях §1.0.**
> **[обязанность 2]** Редакция 2 назвала **четыре** обращения, потому что считала **написание
> пути**. Обращений к поставщику **пять**: четыре потока (строки 141, 143, 153, 154) и
> **чтение уровня уверенности** `GET /sessions/whoami` (строка 125) — последнее под
> `expect.poll`, то есть **многократно за вызов**. Редакция 2 подставила чтение уровня
> **вместо** отправки потока повышения, а не рядом с ней. **Расширенная форма предиката дала
> второй файл** — `registration-refusal-named.spec.ts`, и его судьба разобрана в §1.5а.
> **[единица счёта]** «Двадцать восемь спецификаций» было неверной **единицей**: `*.ts` — 28
> **файлов**, из них проб `*.spec.ts` — **23**, остальные пять — модули. `assurance.ts` сам о
> себе говорит: «Модуль НЕ является пробой». Утверждение «ни одна не судит церемонию входа»
> истинно на обеих единицах, но называть надо ту, о которой оно сказано.

**Путь первый — через фикстуру.** Двадцать три спецификации из двадцати трёх (плюс сама
`fixtures.ts` — отсюда 24) строят «Дано» фикстурой `register()`, а та ведёт **двухшаговый
поток чужого поставщика**: поля `traits.email` и `traits.display_name`, пароль на втором шаге.

**Путь второй — мимо фикстуры, и он бьёт точно в предмет S2.** Модуль
`ui-future/e2e/specs/assurance.ts` ведёт потоки чужого поставщика **напрямую**, из браузерного
контекста, и обращений у него **пять**:

| # | что делает | строка |
|---|---|---|
| 1 | инициирует поток параметров | 141 |
| 2 | отправляет заведение одноразового кода способом `totp` | 143 |
| 3 | инициирует поток подтверждения со вторым фактором (`refresh=true&aal=aal2`) | 153 |
| 4 | отправляет подтверждение вторым фактором | 154 |
| 5 | **читает уровень уверенности** у сессии поставщика — под `expect.poll`, то есть многократно | 125 |

Импортёров два: `stepup.spec.ts` и `users.spec.ts`. По имени фикстуры модуль не находится
**by construction** — он её не зовёт.

То есть чужой поставщик сеет **ровно те две церемонии, которые переселяет S2**: заведение
второго фактора и повышение уровня.

И **ни одна из двадцати трёх проб** не судит саму церемонию входа: это находка `#2780`,
перемеренная здесь своим прогоном.

Три следствия, все несущие:

- снятие чужого экрана **до** переезда фикстуры убивает весь браузерный набор разом — не
  одной пробой, а всеми;
- снятие его **до** переезда `assurance.ts` убивает `stepup.spec.ts` и `users.spec.ts`
  отдельно и позже — то есть в момент, когда причину уже не свяжут со снятием;
- утверждать «вход работает» сегодня нечем: рендерный гейт
  (`ui-future/shared/src/test/identity-ceremony-carriers-rendered.test.ts`) доказывает, что
  полоса **отрендерилась**, а не что по ней **можно войти**, и сам это про себя говорит.

### 1.5а Судьба КАЖДОЙ проверки о снимаемом предмете — обязанность 4

Переезд посева (F8-20, F8-43) **отнимает предмет** у проверок, которые чужой поток не зовут ни
разу. DoD 12 их не ловит by construction: он спрашивает «зовёт ли спецификация чужой поток», а
они его **мокают** — вшитой разметкой и подставленным ответом.

Перепись, полученная расширенным предикатом §1.5:

| проверка | предмет | чем зависит от поставщика | судьба |
|---|---|---|---|
| `ui-future/e2e/specs/registration-refusal-named.spec.ts` — **7 тестов** в одной группе (перемерено строго: `grep -cE '^\s*test\('` → 7; семь имён названы в файле, восьмая строка — `test.describe`) | распознавание страницы отказа чужого поставщика и порядок решения (печенье и поле пароля решают **раньше** распознавателя) | форма отказа поставщика вшита дословно, и печенье его сессии подставляется в мок | **исход назван в §11: F8-44** |
| `identityRefusalFromText` в `ui-future/e2e/specs/fixtures.ts` | разбор текста страницы в пару «код · сообщение» | ищет форму отказа поставщика | **то же: F8-44** |
| `identityRefusalOnPage` в том же файле | тот же разбор, снятый с открытой страницы | то же | **то же: F8-44** |

**Исходов ровно два, и третьего быть не может** (`.claude/rules/00-kacho-core.md`, ban #11 —
исходов три, из которых «отложить» здесь недоступен: предмет снимается этой же работой):

1. **снять вместе с предметом одним изменением** — если после переезда распознавать нечего;
2. **перевести на признак, производимый деревом** — форму отказа **нашей** службы
   (`google.rpc.Status` `{code, message, details}`), с инъекцией, доказывающей, что
   распознаватель умеет и распознать, и не распознать.

**Решение — исход 2, и довод не в симпатии к коду.** Предмет этих семи тестов — не поставщик, а
**порядок решения**: «печенье решает раньше распознавателя» и «разбор берётся из текста
страницы, а не из её разметки». Оба свойства принадлежат **нашей** фикстуре и переживают смену
поставщика; снятие их вместе с формой отказа выбросило бы вместе с водой ребёнка — и выбросило
бы ровно тот класс, на котором фикстура однажды уже соврала («ждала печенья, а причина стояла
на том же экране»). Исход 1 отвергнут **с ценой**: он дешевле на один день и дороже на один
класс, который вернётся.

### 1.6 Условная полоса раздачи и её гейт живут в ПОЛОСЕ, а не в стволе

```sh
# kacho@f445aaaa554 (ветка main рабочей копии) и origin/main@bec320cf47d
grep -c 'if .Values.host.upstreams.kratosUi' ui-future/deploy/templates/configmap-nginx.yaml   # 0
ls deploy/tests/helm/console-serves-identity-flows-test.sh                                     # нет файла
# wave/identity-own-w0-kacho@0e0a85f936e — образец привязан к НАЧАЛУ строки: иначе в счёт
# попадает проза, называющая ручку, и число говорит о упоминаниях, а не об условиях
grep -cE '^\s*\{\{- if \.Values\.host\.upstreams\.kratosUi \}\}' \
  ui-future/deploy/templates/configmap-nginx.yaml                                              # 4
```

> **Правка круга 1.** Редакция 1 называла здесь **5**; строгий образец даёт **4**. Вывод
> раздела от этого не меняется и подтверждён независимо рендером §1.7 — но число было
> получено образцом, не отличавшим условие от упоминания о нём, и потому названо неверно.

Обусловленность полосы посадкой и гейт «объявлено ⇒ обслуживается» **готовы, но ещё не в
стволе**. В стволе полоса потоков стоит **безусловно**. Различать обязательно: «сделано» о
полосе — утверждение о ветке `wave/identity-own-w0-kacho`, а не о дереве, против которого
пойдут пробы.

### 1.7 Цепочка `own` консоль не разворачивает вовсе — и потому «ноль полос» на ней ничего не значит

Рендер цепочек умбреллы, `wave/identity-own-w0-kacho@0e0a85f936e`:

| цепочка | источников подчарта консоли | полос обслуживания чужого экрана | блоков `.ory` |
|---|---|---|---|
| `dev` (`values.dev.yaml`) | **30** | **1** | **2** |
| `prod` (`values.prod.yaml`) | **0** | 0 | 0 |
| `own` (`values.prod.yaml,values.own.yaml,values.own-stand.yaml`) | **0** | 0 | 0 |

Предикат: `helm template kacho-umbrella . <цепочка>` и счёт строк
`^[[:space:]]*location ~ \^/\(login`, `^[[:space:]]*location \^~ /\.ory/`,
`# Source: kacho-umbrella/charts/uif/`. Положительный контроль (`dev` → 1 и 2) исполнен: без
него «ноль» был бы неотличим от предиката, разучившегося считать.

Причина нуля названа: `uif.enabled` в `values.yaml` умбреллы — **false**, и включают консоль
только профили `dev`, `a8f60d`, `fe3455`. Профиль `values.own.yaml` её не включает и
`host.upstreams.kratosUi` не опустошает.

**Отсюда — правка условия готовности, объявленного заданием.** «На посадке `own` рендер даёт
0 полос обслуживания чужого экрана и 0 блоков `.ory`» на измеренной ревизии **истинно и
бессодержательно**: ноль там потому, что консоли на этой цепочке нет вовсе. Годный предикат
готовности обязан нести **знаменатель обхода**, и в §13 он записан так:

> на цепочке, где источников подчарта консоли **больше нуля**, полос обслуживания чужого экрана
> **ноль**, блоков `.ory` **ноль**, и браузерная проба проходит вход и регистрацию.

### 1.8 Прод-файлов консоли, ведущих к чужому поставщику, сорок семь

```sh
# kacho@f445aaaa554, ui-future; без node_modules, без проб и без e2e
grep -rl -iE 'kratos|\.ory|hydra' --include='*.ts' --include='*.tsx' . \
  | grep -v node_modules | grep -vE '\.test\.|/e2e/|/test/' | wc -l      # 47
```

Сорок семь — это **верхняя** граница: в неё попали файлы, где имя поставщика стоит в
комментарии или в перечне путей проксирования разработки. Живых мест, ведущих человека на адрес
церемонии поставщика, среди них **четыре**, и все четыре названы поимённо, потому что §11
требует от каждого исхода:

| место | что делает |
|---|---|
| `shared/src/contexts/AuthContext.tsx` | `login()` уводит браузер на адрес потока поставщика |
| `shared/src/lib/kratos.ts` | собирает адреса четырёх потоков поставщика |
| `shared/src/components/molecules/auth/StepUpModal/StepUpModal.tsx` | церемонию повышения уводит на адрес параметров поставщика |
| `host/src/utils/auth.ts` (и его близнец в `dashboard`) | кнопка «Войти» оболочки ведёт туда же |

### 1.9 Что человек получает от службы — форма ответа и форма отказа

Эти величины — **вход** экранов, и ниже они цитируются, а не решаются (§0.2):

- успех входа, регистрации и завершения восстановления: `200`, тело
  `{"user":{"id","email","displayName"},"session":{"expiresAt","assuranceLevel","emailVerified"}}`,
  печенья `kaname_session` и **новое** `kaname_form`;
- отказ — `google.rpc.Status` JSON `{code, message, details}`, `details` всегда массив;
- `400 / code 3` `Illegal argument <поле>: required` — незаполненное поле, включая `csrfToken`;
- `401 / code 16` `authentication failed` — **один текст на все причины** входа;
- `403 / code 7` `form token rejected` — признак формы чужого вида или чужого контекста;
- `429 / code 8` `too many attempts; try again later` плюс заголовок `Retry-After`;
- `503 / code 14` `request not performed; try again later` (у выхода свой —
  `logout not performed; try again later`);
- `401` от **края** с телом `{"code":16,"message":"session ended; sign in again"}` — исход
  F4d-23 на глаголах, читающих носитель, когда служба краю не ответила.

### 1.10 Оси, снимаемые ВРЕМЕНЕМ, — перепись ПОЛНАЯ, а не из найденных (обязанность 3)

Редакция 2 разобрала **две** оси и на том остановилась. Р7 заведено ровно про такие оси, значит
перечень обязан быть полным. Ниже — все пять, что трогают сценарии §11, каждая с читателем в
дереве и со средством построения, **не требующим ожидания**.

| # | ось | величина и координата объявления | читатель | снимается | средство построения без ожидания |
|---|---|---|---|---|---|
| 1 | попытки по **адресу** | `addressAttempts 5` · `addressWindow 15m` (`values.prod.yaml`, `kaname.config.authn.login.*`) | `attemptGate.check` | успешным входом (`ResetFailures` по оси адреса) **или** временем | **свой адрес у каждого отрицания** (Р7 ч. 1): чужого счёта сценарий не наследует |
| 2 | попытки по **источнику** | `sourceAttempts 50` · `sourceWindow 15m`, там же | `attemptGate.check` | **только временем** | **бюджет прогона + сторож** (Р7 ч. 2, F8-41) |
| 3 | **окно свежести** правки своих данных | `selfServiceFreshness 15m` (`values.own.yaml`) | `sf_enroll.go:84` (`now.Sub(s.LastPresentedAt) > d.Freshness`) и `sf_enroll.go:205` | предъявлением в церемонии повышения | **Р9**: предмет сценария — что делает КОНСОЛЬ, получив ответ, а не при каких условиях служба его выдаёт |
| 4 | **шаг одноразового кода** | шаг TOTP; принятый шаг запоминается (`RecordAcceptedStep`), повтор даёт `OutcomeReplayed` → `verdictMismatched` → **след по обеим осям** | `sf_present.go:98…114` | наступлением следующего шага, **только временем** | **Р9**: у каждого сценария свой человек и **не более одного** предъявления одноразового кода |
| 5 | **срок носителя сессии** | `sessionTtl 24h` (`values.prod.yaml`) | полоса сессии края | только временем | **Р9**: негодный носитель строится **подменой значения печенья**, а не ожиданием суток |

> **Что эта перепись стоила и почему она здесь.** Ось 4 не просто мешает ждать — повтор того же
> кода **считается попыткой** и тратит общую ось 2. Оснастка, которая её обходит ожиданием
> следующего шага (`nextWindow()` в `ui-future/e2e/specs/assurance.ts`), тем самым нарушает
> преамбулу §11 «ожидание — условия, не времени» и делает DoD «трижды подряд» зависимым от
> стенных часов. Переезд F8-43 обязан снять это, а не перенести.

---

## §2 Решения — ратифицировать явно, ревьюер проверяет каждое

### Р1. Принадлежность адреса церемонии консоли выражена РАЗДАЧЕЙ, и второй ручки не заводится

Консоль объявляет маршруты на все восемь адресов полосы потоков и **не спрашивает**, какая
посадка под ней. Довод — свойство разрешения имён раздачи, уже действующее: там, где посадка
объявила адрес интерфейса поставщика, полоса перехватывает запрос **до** оболочки, и маршрут
консоли недостижим by construction; там, где не объявила, запрос доходит до оболочки, и
маршрут работает.

**Цена названа.** Один образ консоли ведёт себя по-разному на двух посадках, и различие не
читается из её кода. Отвергнутый исход — **вторая ручка** («а вести ли церемонию самим»): она
разошлась бы с первой молча, и разошлась бы в сторону, где обе половины считают, что вход рисует
другая. Ровно этот довод уже записан в самой раздаче — «второй ручки «а обслуживать ли» не
заводится».

### Р2. Консоль своего суждения о СОДЕРЖИМОМ поля не выносит

Правило пароля, занятость адреса, годность кода и потолок темпа живут в службе. Экран
показывает то, что вернул глагол: имя поля из `Illegal argument <поле>: …` и текст отказа —
дословно.

**Цена названа и она не нулевая:** пустая форма стоит одного обращения, а отказ приходит после
нажатия, а не до. Отвергнутый исход — **повторить правило на клиенте**: два места об одном
предмете, из которых одно неизбежно устареет, причём устареет тихо — в сторону «мы пускаем то,
что сервер отвергнет» или «мы отвергаем то, что сервер пустил бы».

Что консоли **разрешено** без обращения: отметить поле, которое человек не заполнил вовсе.
Это тот же факт, который служба называет `required`, и другого правила он не вводит.

### Р3. Адрес, которого консоль не ведёт, отвечает НАЗВАННОЙ страницей, а не переводом на панель

Восемь адресов полосы потоков получают маршруты **все восемь**. Те, которых консоль на текущей
стадии не ведёт, отвечают собственной страницей «такого адреса здесь нет» с путём наружу.
Замыкающее правило `*` (§1.2) этим решением **не трогается**: радиус правки — восемь
именованных адресов, и он назван, чтобы починка не растеклась на весь маршрутизатор.

`/consent` собственной страницы не получает **никогда**: он специфичен для потока внешнего
сервера выдачи токена и уходит вместе с ним (`#1107`).

#### Р3а. Экран параметров учётной записи (S2) живёт НЕ на `/settings`, а на `/account/security`

**Блокирующее круга 1, и редакция 1 этого решения не приняла — она о нём молчала.** `/settings`
входит в те же восемь адресов, которые полоса перехватывает **до оболочки**
(`ui-future/deploy/templates/configmap-nginx.yaml` — `^/(login|registration|recovery|verification|settings|error|consent|logout)(/|$)`).
По Р1 это значит: на посадке сосуществования экран параметров на `/settings` **недостижим by
construction**, семь глаголов S2 не зовёт никто, а человек получает чужой экран настроек,
который наших глаголов не знает.

**Решение:** экран параметров учётной записи объявляется на `/account/security` — адресе, в
полосу не входящем. Адрес свободен: точное сравнение `/accounts` в
`host/src/utils/host-context.ts` — другой предмет, и префикса `/account/` в дереве консоли нет.

**Цена названа, и она не нулевая.** На посадке сосуществования мест параметров становится два:
чужое на `/settings` и наше на `/account/security`. Это терпимо ровно потому, что они **об
разных поставщиках**, а не об одном предмете: наше ведёт наши глаголы, чужое — чужие, и каждое
верно о своём. Терпимость **истекает**: предикат снятия — полоса снята с посадки ⇒ `/settings`
переводится на наш экран тем же изменением, и мест снова одно.

**Отвергнутый исход — вынуть `settings` из полосы.** Он сломал бы посадку сосуществования: на
ней чужой экран параметров перестал бы открываться, а наш ещё не появился бы, — то есть второй
фактор не завёлся бы ни одним способом. Необратимость здесь асимметрична, и дешёвая сторона —
свой адрес.

**Следствие для Р1:** принадлежность адреса выражена раздачей для **трёх** адресов церемонии
входа (`/login`, `/registration`, `/logout`), и **выбором адреса вне полосы** — для экрана
параметров. Исключение названо здесь, а не выведено читателем.

### Р4. Человек, заведённый чужой службой, идёт ТОЙ ЖЕ полосой; особой ветки нет и быть не может

Три довода, каждый — факт, а не соображение:

1. **Консоль не может различить его случай.** Отказ входа — один текст на все причины
   (§1.9). «Пароль неверен» и «способа входа у нас нет» приходят побайтово одинаково.
2. **Консоль не вправе его различать.** Различимость этих двух исходов и есть разведка чужих
   адресов: она отвечает на вопрос «а заведён ли такой человек» тому, кто пароля не знает.
3. **Путь у него один, и он не административный.** Производителей способа входа три, и
   административного среди них нет (§1.4). Значит единственный самостоятельный путь —
   восстановление доступа.

**Следствие, названное прямо, а не смягчённое:** до под-фазы S3 (восстановление) консоль этого
пути **не обещает**. Строка «не получается войти» появляется на экране входа **тем же
изменением**, что экран восстановления, и ни одной под-фазой раньше. Обещание пути, которого на
посадке нет, хуже его отсутствия — это ровно класс «объявление живо, исполнения нет», уже
стоивший вечера соседней полосе.

**Чего это решение НЕ берёт на себя.** Перенос проверочного материала людей чужой службы и
число тех, чей материал не переехал, — предмет приёмки F4d, стадия S2: там записано, что число
называется **перед** переводом стенда и в норме равно нулю. Консоль этого не проверяет, не
чинит и не смягчает; здесь оно названо потому, что при ненулевом значении под-фаза S1 оставляет
этих людей без пути, и решать это обязан тот, кто переводит стенд.

**Держатели обеих сторон — блокирующие круга 1, и редакция 1 оставляла Р4 прозой.** Решение без
держателя не отличается от намерения:

- **сторона «путь не обещается»** держит **F8-39**: на экране входа S1 нет пути ни на
  `/recovery`, ни на `/verification`. Без него ничто не мешает S1 поставить ссылку, которая по
  Р3 ответит «такого адреса здесь нет» — ровно то, что этот же абзац объявляет худшим
  отсутствия;
- **сторона «причины неразличимы»** держит **F8-40**: адрес, посевом **не** заведённый, даёт
  побайтово тот же экран, что неверный пароль. Без него безопасная половина Р4 живёт только в
  прозе, а проза не краснеет.

### Р5. Порядок под-фаз задан ГОТОВНОСТЬЮ ПРОИЗВОДИТЕЛЯ, а не удобством

Пятнадцать глаголов делятся по одному измеренному признаку — есть ли у сценария производитель
**сегодня**:

| готовность | глаголов | куда |
|---|---|---|
| производитель в дереве, внешнего ожидания нет | **11** | S1 (4) и S2 (7) — сценарии §11 |
| производитель в дереве, ждёт доставки письма (`#1773`, OPEN, `blocked`, P3) | **2** | S3 — §12 |
| производителя в дереве **нет** (два глагола Ф13) | **2** | S4 — §12 |

Отвергнутый исход — **разложить по экранам** («сначала все экраны в черновом виде, потом
доведём»): это запрещено прямо (ban #14), и на этом предмете особенно дорого — экран
восстановления, который принимает адрес и не доводит до кода, неотличим от работающего.

### Р6. Экран церемонии наблюдается ПЕРЕПИСЬЮ ОБРАЩЕНИЙ, а не разметкой

Каждый сценарий §11, утверждающий «консоль зовёт наш глагол», наблюдается переписью сетевых
обращений страницы (`apiCalls(page)` уже есть в `ui-future/e2e/specs/fixtures.ts`), а не
классом узла и не текстом кнопки. Довод — правило 12 канона консоли: проба утверждает
наблюдаемое поведение, а не разметку; проба на разметке зеленеет при сломанном обращении и
краснеет при смене отступа.

### Р7. Ось адреса у каждого сценария СВОЯ; ось источника — общий ресурс прогона с объявленным бюджетом

**Блокирующее круга 1: редакция 1 не знала, что потолок темпа двухосевой, и F8-09 был
неконструируем.** Перемерено (`kaname@497c44b7:internal/apps/kaname/api/humansession/rate.go`,
шапка и `attemptGate.check`; величины —
`deploy/helm/umbrella/values.prod.yaml`, `kaname.config.authn.login.*`, цепочка `own` их
наследует):

| ось | ключ | потолок | окно | снимается |
|---|---|---|---|---|
| адрес | нормализованная почта | **5** | **15m** | успешным входом (`ResetFailures` по оси адреса) |
| источник | адрес, выведенный краем | **50** | **15m** | **только временем** |

> **Правка круга 2 [обязанность 1].** Редакция 2 взяла перечень «что считается попыткой» из
> **шапки** `rate.go`. Код **шире шапки**, и бюджет из-за этого был занижен: сторож F8-41
> краснел бы **на верной реализации**, а первым побуждением было бы подправить число — тот
> самый дрейф, против которого Р7 и заведено. Ниже перечень взят **по местам вызова**.

Следы пишутся по **обеим** осям одной транзакцией (`recordFailure`). Мест вызова свободной
функции — **пять**, и шапка называет два из них:

| место вызова | глагол | при каком исходе | называет ли шапка |
|---|---|---|---|
| `login.go:411` | вход | отказ входа | да |
| `change_password.go:244` (через `:162`) | смена пароля | неподошедшее подтверждение | да |
| `sf_enroll.go:327` (через `:220`) | **подтверждение второго фактора** | неподошедший код (ветвь `default`) | **нет** |
| `recovery_complete.go:299` | завершение восстановления | отказ предъявления кода | нет (предмет S3) |
| `step_up.go:274` (через `refuse` → `recordRefusal`) | **повышение уровня** | **только** `verdictMismatched` — предикат `countsAsAttempt` (`sf_present.go:242`) | **нет** |

**Не** идут в счёт: отказ формы, отказ по частоте, успешный вход, исчерпание ёмкости
проверяющего и — на повышении — вердикт «второй фактор не заведён» (`verdictNotEnrolled`
предикату `countsAsAttempt` не отвечает; поэтому вклад F8-36 равен **нулю**, и это измерено, а
не предположено).

Ось источника **одна на весь набор**, потому что бегун у него один. Без решения
`F8-09` пересиживал бы окно **временем** — против §11 «ожидание — условия, не времени» и против
DoD «трижды подряд».

**Решение из двух частей:**

1. **Каждый отрицательный сценарий сеет СВОЙ адрес.** Тогда ось адреса у него своя, F8-09
   исчерпывает только её, и ни один сценарий не наследует чужого счёта. Окно временем не
   пересиживается ни разу.
2. **Ось источника объявлена бюджетом прогона и держится сторожем.** Бюджет — сумма отказов,
   идущих в счёт, по сценариям §11:

   | сценарий | место вызова | отказов в счёт |
   |---|---|---|
   | F8-05 неверный пароль | `login.go:411` | 1 |
   | F8-09 исчерпание оси адреса (шестое обращение даёт `429` и в счёт не идёт) | `login.go:411` | 5 |
   | F8-23 «вход прежним паролем даёт `401`» | `login.go:411` | 1 |
   | F8-24 неверный текущий пароль при смене | `change_password.go:244` | 1 |
   | F8-34 неверный код второго фактора при входе | `login.go:411` | 1 |
   | F8-40 адрес не заведён | `login.go:411` | 1 |
   | **F8-28 неподошедший первый код подтверждения** — **добавлен кругом 2** | **`sf_enroll.go:327`** | **1** |
   | F8-29 сессия не свежа · F8-36 повышать нечем | повышение: `verdictNotEnrolled` вне `countsAsAttempt` | **0** |
   | **итого за прогон** | | **11** |

   Три прогона подряд — **33** при потолке **50**; запас **17**, то есть **66 %** потолка.
   Сторож **F8-41** утверждает бюджет числом, — иначе первым о переполнении скажет чужой
   сценарий, упавший по чужой причине.

**Цена названа.** Бюджет — число в документе и в стороже, и при правке величин профиля он
устареет. Предикат снятия — **два держателя, потому что один прибор обе половины не достаёт**
(правка круга 2): браузерная проба доступа к профилям развёртывания **не имеет**, и «сторож
читает потолок из профиля» было бы требованием, которое нечем исполнить.

| половина | кто держит | что утверждает |
|---|---|---|
| **сколько тратит прогон** | сторож в наборе (F8-41) | число отказов, идущих в счёт, за прогон **≤ 11**, за три подряд **≤ 33**; величину он получает **переменной окружения**, которую ставит тот же рецепт подъёма, что применяет профиль |
| **хватает ли этого у профиля** | проба уровня развёртывания рядом с `deploy/tests/helm/` | переданная прогону величина **≤ 80 %** `sourceAttempts` профиля, против которого идёт прогон |

«Краснеет, когда бюджет приблизился» **квантифицировано**: порог — **80 %** потолка. На
измеренных величинах 33 из 50 — **66 %**, и до порога остаётся два сценария с одним отказом
каждый. Без числа «приблизился» было бы не признаком, а настроением.

### Р8. Проба несёт ID СЦЕНАРИЯ, а не только номер задачи

**Блокирующее круга 1.** Правило 12 канона консоли требует `// verifies #<N>` внутри вызова
теста — это ссылка на **задачу**. Её одной мало: §11.1 сопоставляет ID **файлам**, и по дереву
нельзя сказать, какой из F8-01…F8-45 не сел. Утверждение DoD «ни один сценарий не помечен
пропущенным» при этом непроверяемо by construction.

**Решение:** имя каждого теста начинается с ID сценария — `F8-04 · вход паролем проходит
целиком`, — а `// verifies #<N>` остаётся внутри вызова рядом. Тогда перепись имён тестов даёт
множество исполненных ID, и разность с §11 называется числом и поимённо.

**Цена:** имя теста становится связано с документом, и переименование сценария ломает
соответствие. Поэтому **ID не переприсваиваются** (§11 преамбула): ID — имя, а не порядковый
номер.

### Р9. У каждой оси времени названо СРЕДСТВО ПОСТРОЕНИЯ, и ни одно из них не есть ожидание

Перепись осей полна (§1.10). Р7 закрыло оси 1 и 2; здесь — оставшиеся три, и правило, общее для
всех будущих.

**Общее правило, которое и есть решение:** сценарий §11 судит **консоль**, а не службу. Значит
условие, которое служба создаёт временем, строится **подстановкой наблюдаемого ответа**, а не
пересиживанием окна: предмет сценария — что делает экран, получив такой ответ, а не при каких
обстоятельствах служба его выдаёт. Где это невозможно — сценарий меняет прибор и уходит к
владельцу условия, а не растягивает прогон.

| ось | средство | сценарий | почему это НЕ подмена предмета |
|---|---|---|---|
| **3. окно свежести** | проба подставляет ответ края с `reason` = `SESSION_NOT_FRESH` | **F8-29** | «Тогда» сценария — консоль открывает церемонию повышения и **возвращает на тот же шаг**. Условия выдачи отказа судит приёмка Ф12, и её сценарии здесь не переоткрываются (§0.2) |
| **4. шаг одноразового кода** | **у каждого сценария свой человек и НЕ БОЛЕЕ ОДНОГО предъявления одноразового кода** | **F8-27**, **F8-43** | два предъявления подряд одним человеком требуют следующего шага; повтор того же кода не просто отвергается, а **считается попыткой** и тратит ось 2 (`OutcomeReplayed` → `verdictMismatched` → `countsAsAttempt`) |
| **5. срок носителя** | значение печенья сессии **подменяется** негодным | **F8-12** | «Тогда» — форма входа отрисована и круг переадресаций не растёт; чем именно носитель негоден, экран не различает и различать не вправе |

**Цена названа.** Подстановка ответа делает сценарий слепым к тому, выдаёт ли служба этот ответ
на самом деле. Это терпимо ровно потому, что **служба уже имеет своего держателя** на каждый из
трёх ответов в доме Ф12/Ф3, и шов §0.2 запрещает заводить второго здесь. Отвергнутый исход —
«подождать 15 минут один раз за прогон»: он стоил бы DoD «трижды подряд» и сделал бы вердикт
зависимым от стенных часов, то есть от того, чего в предикате никто не объявлял.

**Держатель самого правила — F8-45:** в наборе нет ожидания стенными часами. Без него правило
осталось бы прозой, а первая же оснастка вернула бы `nextWindow()` обратно.

---

## §3 Что НЕ входит

Перечень закрытый; у каждой строки назван тот, кто это делает.

| не входит | почему | чей предмет |
|---|---|---|
| сами церемонии — что служба обещает глаголом, форма запроса, код и текст отказа, срок сессии, правило пароля | две одобренные приёмки и соседние; переоткрытие дало бы два места об одном предмете (§0.2) | `kaname` Ф3 · Ф4 · Ф5 · Ф7 · Ф12 · Ф13 |
| снятие подчарта чужого интерфейса личности из посадки и из дерева | необратимый шаг, и он не делается тем же изменением, что проверяемый | `#2777`, `#2735`, `#2733` |
| включение подчарта консоли на цепочке `own` и опустошение адреса чужого интерфейса | предмет развёртывания; здесь — только **предусловие прогона** (§4) | `#2777`, `#2735` |
| внесение условной полосы раздачи и её гейта в ствол | готово в полосе, не в стволе (§1.6) | полоса `wave/identity-own-w0-kacho` |
| восстановление доступа — два глагола и их экран | производитель ждёт доставки письма | **под-фаза S3**, §12 |
| вход ключом доступа — два глагола и их экран | глаголов в дереве нет | **под-фаза S4**, §12 |
| подтверждение адреса (`/verification`) | тот же блокатор — доставка письма | `#1772`/Ф6, §12 |
| `/consent` | специфичен для внешнего сервера выдачи токена | `#1107` |
| `/settings` — перевод его на наш экран | на посадке сосуществования полоса перехватывает его до оболочки, и чужой экран параметров обязан оставаться рабочим; наш живёт на `/account/security` (Р3а) | снятие полосы — `#2777`, `#2735`; предикат истечения — DoD 18 |
| замыкающее правило маршрутизатора `*` вне восьми адресов полосы | радиус назван в Р3 | отдельный предмет, §12 |
| снятие внешнего издателя токена | приём чужого токена и чтение печенья сессии — разные предметы | `#2734`, `#1107` |

---

## §4 Чего посеять нельзя — и почему это ТРЕТЬЯ категория, а не провал

Различать обязательно: «красное» — свойство дерева; «не выполнилось» — условие прогона не
создано (`.claude/rules/testing-verdict.md`).

**Предусловие прогона, без которого ни один сценарий §11 не исполним:** цепочка, которая
одновременно **разворачивает консоль** и **не обслуживает адреса церемоний чужим экраном**. На
измеренной ревизии такой цепочки нет ни одной (§1.7): где консоль есть — там стоит безусловная
полоса; где полоса условна — там консоли нет.

Три вещи, которые это предусловие создают, и все три — чужой предмет:

1. условная полоса раздачи и её гейт в стволе (полоса `wave/identity-own-w0-kacho`);
2. `uif.enabled: true` и пустой `host.upstreams.kratosUi` на цепочке `own` (`#2777`, `#2735`);
3. подъём самой цепочки `own` (`#2724` — у секрета второго фактора нет производителя; цепочка
   не поднималась ни разу).

Пока этого нет, исход прогона сценариев §11 — **«не выполнилось»**, и он отчитывается именно
так: числом сценариев, у которых условие не создано, а не красным и не зелёным. Документ это
предсказывает сам — значит зелёный отчёт без созданного условия есть находка о прогоне, а не о
дереве.

---

## §5 Под-фазы — граница каждой и почему она production-complete

| под-фаза | предмет | глаголов | граница: человек может пройти ЦЕЛИКОМ | предикат старта |
|---|---|---|---|---|
| **S1** | вход · регистрация · выход · признак формы; восемь адресов полосы получают маршруты; браузерный набор переезжает на наши экраны | 4 | завести себя, войти, выйти, вернуться; на каждом ветвлении — названный ответ | предусловие §4 |
| **S2** | параметры учётной записи **на `/account/security`** (Р3а): смена пароля · второй фактор (пять глаголов) · повышение уровня внутри сессии; переезд второго пути посева (§1.5) | 7 | сменить пароль, завести и подтвердить второй фактор, снять его, перечеканить запасные коды, пройти повышение, не покидая консоли | S1 села |
| **S3** | восстановление доступа: запрос кода и его предъявление; строка «не получается войти» на экране входа | 2 | человек без пароля возвращает себе доступ | **доставка письма** `#1773` |
| **S4** | вход ключом доступа: кнопка рядом с паролем, обработка испытания браузером | 2 | войти без пароля | **два глагола Ф13 в стволе платформы** |

Сценарии §11 покрывают **S1 и S2**. S3 и S4 вынесены в §12 с условием возврата — это
декомпозиция на production-complete под-фазы, а не урезание (ban #14).

**Почему S1 production-complete, хотя человеку без способа входа пути не даёт.** S1 **не
переводит ни одной посадки**: адреса церемоний достаются консоли ровно там, где посадка не
объявила чужой интерфейс, и создание такой посадки — чужой предмет (§4). На ней человек
заводит себя сам — регистрацией, — и полный круг «завёлся → вошёл → вышел → вернулся»
проходится. Случай «способ входа есть у чужой службы, а у нас нет» на переведённом стенде
закрыт предикатом F4d S2 и назван в Р4.

**Почему S2 отдельно от S1.** Граница — **носитель сессии**: четыре глагола S1 человек зовёт
**без** сессии (три из них край ретранслирует даже при молчащей службе), семь глаголов S2 —
**с** сессией, и молчание службы на них даёт F4d-23 на крае. Это не удобство раскладки, а
разные исходы на одном и том же отказе, и сценарии их различают (F8-10 против F8-25).

**Почему второй фактор и повышение — в одной под-фазе со сменой пароля.** Повышение уровня
срабатывает только у человека, у которого второй фактор заведён; заводится он экраном той же
под-фазы. Разведи их — и в промежутке повышение не вызывается ничем, то есть его сценарий
нечем посеять.

---

## §6 Отрицательный кейс меняет РОВНО ОДИН факт против близнеца

Правило `.claude/rules/change-graph.md` §6 и причина, по которой оно здесь жёстче обычного:
«отвергнуто» без близнеца неотличимо от «мёртво», а на этом предмете «мёртво» — самое
вероятное состояние: экран, который не зовёт ничего, отвергает **всё**.

Таблица близнецов §11.2 называет для каждого отрицательного сценария его положительного
близнеца и **единственный** изменённый факт. Сценарий, у которого таких фактов больше одного,
— находка круга, а не строгость.

---

## §7 Границы с соседними задачами

| задача | её предмет | граница |
|---|---|---|
| **`#2780`** — у церемоний входа консоли нет ни одного свидетеля | браузерная проба церемонии | **Исполняется здесь.** Пробы сценариев S1 несут `// verifies #2780` внутри вызова `test(…)`; предикат снятия задачи (проба существует · видена красной до фикса и зелёной после · ссылка внутри теста) — пункты DoD §13 |
| **`#2777`** — чужая консоль личности разворачивается и на посадке `own` | профиль посадки | **Предусловие §4.** Здесь не чинится; сюда приходит готовым |
| **`#2733`** — снятие ручек и проксирования к чужим службам из `ui-future` | настройки и полосы проксирования разработки | **Смежно и после.** Здесь снимаются **четыре живых места**, ведущих человека на адрес церемонии поставщика (§1.8); ручки проксирования, комментарии и перечни путей разработки — её предмет. Порядок: сначала консоль перестаёт **ходить**, потом снимаются ручки, которыми она ходила |
| **`#1225`** — снятие семи страниц церемоний | уже исполнено | Условие, записанное ею («способ должен остаться ОДИН»), исполняется здесь: адреса переходят к консоли тем же изменением, которым она начинает их вести |
| **Ф13 `#1282`**, **Ф7 `#1273`** | глаголы входа ключом и сами ключи | **S4**, §12. Кнопка ключа рядом с паролем появляется тем же изменением, что её глагол |
| **Ф5 `#1271`**, **`#1773`** | восстановление и доставка письма | **S3**, §12 |
| **рендерный гейт полосы личности** (`identity-ceremony-carriers-rendered.test.ts`) | носитель церемонии обязан быть отрендерен | **Не снимается и не ослабляется.** Наши экраны попадают в его перепись как носители, и он остаётся вторым свидетелем рядом с браузерной пробой. Что он НЕ ловит, он говорит сам, и здесь это не переизлагается |

---

## §8 Пять вопросов (ban #18) — «нет» на любом есть отказ

| ось | ответ |
|---|---|
| **благо продукта** | Да. Сегодня церемонию физически ведёт чужое приложение, и снять его нельзя — переключать не на что (§1.1). Это единственный корень того, что чужая служба стоит на всех стендах |
| **конкурентоспособность** | Да, и она **измерима здесь, а не на словах**. Экран входа — первое, что видит клиент. Чужой экран говорит чужим протоколом и чужими метками; узнаваемость продукта на нём отсутствует по построению. Сверх облика — две вещи, которых у чужого экрана нет и которые §11 требует: **достижимость каждого действия с клавиатуры** (F8-22) и **названный отказ на каждом ветвлении** вместо белого экрана (шестнадцать отрицательных сценариев S1) |
| **перспективность** | Да. Экраны строятся над **закрытым перечнем из тринадцати глаголов**, объявленным одним местом (§1.3). Новый способ входа — новая строка перечня и новый вид признака формы, а не новый поток с новым протоколом. Ф13 (вход ключом) входит в S4 добавлением кнопки, а не переписыванием экрана |
| **безопасность** | Да, и покрытие держателями названо построчно, а не общим словом. Признак формы предъявляется на каждой меняющей состояние форме, своего вида — **F8-07, F8-08**. Носитель сессии не гасится чужим сбоем — **F8-19, F8-25**. Адрес возврата закрыт против открытой переадресации во всех четырёх формах, включая протокол-относительную — **F8-13**. Причины отказа входа неразличимы, и это теперь **держится**, а не утверждается — **F8-40**. Печенья держит браузер, и консоль их не читает |
| **производственность** | Да. Производитель у всех одиннадцати глаголов S1+S2 в дереве и достижим браузеру (§1.3); у каждого «Тогда» §11 назван свой производитель (§11.1); условие прогона, которого ещё нет, названо третьей категорией, а не обещанием (§4) |

---

## §9 Асимметрия цены: где отказ при сомнении обязателен

| развилка | дорогая сторона | что делает консоль |
|---|---|---|
| адрес возврата после входа | открытая переадресация уводит человека с его сессией на чужое происхождение | адрес чужого происхождения **отвергается**, переход на корень консоли (F8-13) |
| отказ выхода | «вышли» на экране при живом носителе — человек уходит от чужого монитора уверенным, что вышел | экран **не показывает** выхода, пока служба его не подтвердила (F8-19) |
| молчание службы на глаголе с носителем | тихое гашение сессии превращает сбой соседа в потерю работы | носитель **цел**, отказ назван, сессия не гасится (F8-25) |
| различимость причин отказа входа | подсказывает, заведён ли адрес | один текст, и консоль его не обогащает (Р4) |
| путь для того, кто не может войти | обещание пути, которого на посадке нет | путь **не обещается** до S3 (Р4) |

---

## §10 Порядок работ: первой красной ложится ПЕРЕПИСЬ ОБРАЩЕНИЙ

Первым красным ложится не экран, а **свидетель**: браузерная проба F8-04, утверждающая, что
после отправки формы входа перепись обращений страницы содержит `POST /iam/v1/auth/login` и
**не содержит** ни одного адреса поставщика. На дереве до фикса она красная по причине, которую
называет сама (маршрута нет, страница уведена на панель — §1.2), а не по таймауту.

Довод порядка: перепись обращений — единственное утверждение, которое остаётся верным при любой
переделке облика экрана. Ляг первым экран — и каждая правка вёрстки переписывала бы пробу.

---

## §11 Сценарии

Соглашения, общие для всех:

- **происхождение** — origin консоли; все обращения идут туда же, куда раздача уводит
  `^/iam/v1/`;
- **«Дано» строится посевом**, а не предположением: человек заводится обращением пробы к
  глаголу регистрации (`POST /iam/v1/auth/register` с признаком формы, добытым
  `GET /iam/v1/auth/csrf?form=register`), состояние второго фактора — глаголами `enroll` и
  `confirm`. Экран ведёт только ту церемонию, которую сценарий судит;
- **«Тогда» утверждает наблюдаемое**: перепись сетевых обращений страницы, текст на экране,
  адрес страницы, наличие или отсутствие печенья — и **пару** «код ответа + `code` из
  `google.rpc.Status`» там, где сценарий про отказ;
- ожидание — **условия**, не времени; повторов у набора нет;
- **каждый отрицательный сценарий сеет СВОЙ адрес** (Р7 ч. 1): ось потолка темпа по адресу у
  него своя, и чужого счёта он не наследует;
- **имя теста начинается с ID сценария** (Р8), а `// verifies #<N>` стоит внутри вызова рядом;
- **ID не переприсваиваются.** Они выданы в порядке **заведения**, а не чтения: сценарии,
  добавленные кругом 1, несут F8-39…F8-43, добавленные кругом 2 — F8-44…F8-45, и все они
  стоят в своих группах. ID — **имя**, и
  переименование его при вставке соседа сделало бы записи ревью и имена проб неразрешимыми —
  тот же класс, что у соседней приёмки стоил примирения ярлыков задним числом.

### S1 — группа A. Адрес церемонии принадлежит консоли

**ID:** F8-01 — **экран входа отдаёт консоль там, где посадка не объявила чужой интерфейс**

**Given** цепочка разворачивает подчарт консоли (источников подчарта > 0)
**And** посадка не объявила адрес интерфейса чужого поставщика личности
**When** браузер открывает `/login`
**Then** отрисован экран входа консоли: поля адреса и пароля и кнопка отправки
**And** перепись обращений страницы не содержит ни одного адреса чужого поставщика
**And** адрес страницы остался `/login` — перевода на панель не произошло

**ID:** F8-02 — **там, где посадка объявила чужой интерфейс, `/login` отдаёт НЕ консоль** (близнец F8-01, различие — объявленность адреса чужого интерфейса)

> **Правка круга 1 [TWIN].** Редакция 1 меняла против F8-01 **два** факта — посадку **и
> прибор**: F8-01 судил браузер, F8-02 — рендер. Пара факта не изолировала, и утверждение
> «маршрут консоли недостижим» браузером не проверялось ни разу. Здесь прибор тот же, что у
> близнеца, и различие ровно одно. Сценарий конструируем **уже сегодня**, на цепочке `dev`:
> предусловие §4 ему не нужно, потому что именно объявленный чужой интерфейс он и требует.

**Given** цепочка разворачивает подчарт консоли (источников подчарта > 0)
**And** посадка **объявила** адрес интерфейса чужого поставщика личности
**When** браузер открывает `/login`
**Then** экран входа консоли **не** отрисован
**And** перепись обращений страницы не содержит ни `GET /iam/v1/auth/csrf?form=login`, ни `POST /iam/v1/auth/login`
**And** страница пришла от чужого интерфейса — то есть маршрут консоли запроса не получил

**ID:** F8-42 — **перепись рендера: полоса есть там и только там, где объявлен адрес чужого интерфейса**

**Given** перечень цепочек развёртывания
**When** рендер каждой цепочки читается целиком
**Then** на цепочке, объявившей адрес чужого интерфейса, полоса обслуживания адресов церемоний присутствует
**And** на цепочке, его не объявившей, полос **ноль**
**And** перепись печатает по каждой цепочке число источников подчарта консоли — знаменатель обхода, без которого «ноль полос» неотличим от «консоли здесь нет» (§1.7)

**ID:** F8-03 — **адрес полосы, которого консоль не ведёт, отвечает названной страницей** (близнец F8-01, различие — адрес)

**Given** условия F8-01
**When** браузер открывает `/verification`
**Then** отрисована страница консоли, называющая, что этого адреса здесь нет, и дающая путь наружу
**And** адрес страницы остался `/verification` — перевода на панель не произошло

### S1 — группа B. Вход

**ID:** F8-04 — **вход паролем проходит целиком и уводит на адрес возврата**

**Given** условия F8-01
**And** посевом заведён человек с адресом `e2e-<метка>@kacho.local` и паролем `<пароль посева>`
**When** браузер открывает `/login?returnTo=/dashboard`, заполняет адрес и пароль и отправляет форму
**Then** перепись обращений страницы содержит `GET /iam/v1/auth/csrf?form=login` и `POST /iam/v1/auth/login`
**And** не содержит ни одного адреса чужого поставщика
**And** браузер держит печенье сессии
**And** адрес страницы стал `/dashboard`

**ID:** F8-05 — **неверный пароль: назван один отказ, форма остаётся** (близнец F8-04, различие — значение пароля)

**Given** условия F8-04
**When** отправлена форма с тем же адресом и **другим** паролем
**Then** ответ `401`, `code` = 16, сообщение `authentication failed`
**And** на экране стоит этот текст
**And** печенья сессии нет, адрес страницы остался `/login`, поле адреса сохранило введённое

**ID:** F8-06 — **незаполненное поле названо службой по имени** (близнец F8-04, различие — пустой пароль)

**Given** условия F8-04
**When** отправлена форма с заполненным адресом и пустым паролем
**Then** ответ `400`, `code` = 3, сообщение `Illegal argument password: required`
**And** на экране отмечено поле **пароля**, названное этим ответом, и никакое другое

**ID:** F8-07 — **признак формы не предъявлен** (близнец F8-04, различие — признак формы не приложен)

**Given** условия F8-04
**When** форма отправлена без поля `csrfToken`
**Then** ответ `400`, `code` = 3, сообщение `Illegal argument csrfToken: required`
**And** на экране стоит названный отказ, а не пустой экран

**ID:** F8-08 — **признак формы чужого вида отвергнут, и экран даёт повторить** (близнец F8-04, различие — вид признака формы)

**Given** условия F8-04
**When** форма отправлена с признаком, добытым запросом `?form=logout`
**Then** ответ `403`, `code` = 7, сообщение `form token rejected`
**And** экран добывает свежий признак вида `login` и даёт отправить снова
**And** введённый адрес не потерян

**ID:** F8-09 — **потолок темпа: экран называет срок и не даёт бить в стену** (близнец F8-05, различие — число подряд идущих неверных предъявлений)

> **Правка круга 1 [CONSTRUCTIBILITY].** Потолок двухосевой (Р7). Сценарий исчерпывает
> **только ось адреса**, и адрес у него **свой**: пять неверных предъявлений на него и есть
> условие. Окно временем не пересиживается, счёт с соседей не наследуется, и вклад сценария в
> общую ось источника — **5**, учтённый бюджетом Р7 ч. 2 и держимый F8-41.

**Given** условия F8-04, но человек заведён посевом на **собственный** адрес этого сценария
**And** на этот адрес подряд отправлено **пять** форм с неверным паролем — потолок оси адреса исчерпан
**When** отправлена **шестая** форма
**Then** ответ `429`, `code` = 8, сообщение `too many attempts; try again later`, заголовок `Retry-After` присутствует
**And** экран называет срок из заголовка и до его истечения отправку не производит
**And** перепись обращений после отказа не растёт
**And** шестое обращение в счёт оси источника **не** идёт — отказ по частоте попыткой не считается

**ID:** F8-10 — **служба не ответила: отказ назван, введённое цело** (близнец F8-04, различие — доступность службы)

**Given** условия F8-04
**And** служба доступа краю не отвечает
**When** отправлена форма входа
**Then** ответ `503`, `code` = 14, сообщение `request not performed; try again later`
**And** на экране стоит этот текст и предложено повторить
**And** введённый адрес не потерян, печенья сессии нет

**ID:** F8-11 — **человек с живой сессией формы входа не видит**

**Given** условия F8-01
**And** у браузера есть действующая сессия, заведённая посевом
**When** браузер открывает `/login?returnTo=/dashboard`
**Then** формы входа на экране нет
**And** адрес страницы стал `/dashboard`

**ID:** F8-12 — **истёкшая сессия даёт форму, а не круг переадресаций** (близнец F8-11, различие — годность носителя)

**Given** условия F8-11, но значение печенья сессии **подменено негодным** — средство оси 5 (Р9): срока носителя проба не пересиживает
**When** браузер открывает `/login?returnTo=/dashboard`
**Then** отрисована форма входа
**And** число переходов адреса за время загрузки не растёт после первого
**And** экран не различает, **чем именно** носитель негоден, и различать не вправе

**ID:** F8-13 — **адрес возврата чужого происхождения отвергнут** (близнец F8-04, различие — значение `returnTo`)

> **Правка круга 1 [FORMAT].** Редакция 1 несла местозаполнитель, и сценарий по букве
> зеленел бы против настоящего дефекта: наивная проверка `startsWith('/')` отвергает
> `https://…` и **пропускает** `//…`. Формы перечислены поимённо, и вторая из них — та, ради
> которой сценарий существует. Предикат, который их закрывает, в дереве уже есть —
> `safeInternalPath` в `ui-future/shared/src/lib/redirect.ts` (резолв относительно
> происхождения и сравнение `url.origin`); сценарий делает его **держателем**, а не заводит
> второй.

**Given** условия F8-04
**When** вход проходит успешно, а `returnTo` принимает по очереди каждое из четырёх значений:
  - `https://evil.example/dashboard` — абсолютный адрес чужой власти
  - `//evil.example/dashboard` — **протокол-относительный**; наивная проверка «начинается с косой» его пропускает
  - `/\evil.example/dashboard` — с обратной косой, которую браузер нормализует в двойную
  - `javascript:alert(1)` — схема не из `http`/`https`
**Then** на каждом из четырёх переход произошёл на корень консоли, а не на присланный адрес
**And** происхождение страницы после перехода равно происхождению консоли
**And** положительный близнец `returnTo=/dashboard` (F8-04) переводит именно туда — отрицание не тождественно

**ID:** F8-39 — **на экране входа S1 нет пути ни на восстановление, ни на подтверждение адреса** (близнец F8-01, различие — что именно утверждается об отрисованном экране)

> Держатель стороны «путь не обещается» решения Р4. Обещание пути, которого на посадке нет, —
> класс «объявление живо, исполнения нет», и без этого сценария запрет живёт только в прозе.

**Given** условия F8-01, стадия **S1**
**When** браузер открывает `/login`
**Then** на экране нет перехода на `/recovery`
**And** на экране нет перехода на `/verification`
**And** положительный близнец: переход на `/registration` на экране **есть** — отрицание не тождественно и предикат «перехода нет» умеет распознать переход

**ID:** F8-40 — **адрес, не заведённый вовсе, даёт побайтово тот же экран, что неверный пароль** (близнец F8-05, различие — заведён ли адрес)

> Держатель стороны «причины неразличимы» решения Р4. Посев тривиален: его просто не делают.

**Given** условия F8-01
**And** адрес `e2e-<метка>-unknown@kacho.local` посевом **не** заведён
**When** на экране входа отправлена форма с этим адресом и любым паролем
**Then** ответ `401`, `code` = 16, сообщение `authentication failed`
**And** текст на экране **побайтово тот же**, что в F8-05
**And** экран не сообщает ничего, из чего читалось бы, заведён ли адрес: ни отдельного текста, ни отдельного поля, ни иной длительности ответа, наблюдаемой страницей

### S1 — группа C. Регистрация

**ID:** F8-14 — **регистрация заводит человека и сразу даёт сессию**

**Given** условия F8-01
**And** адрес `e2e-<метка>@kacho.local` в службе не заведён
**When** браузер открывает `/registration`, заполняет адрес и пароль и отправляет форму
**Then** перепись обращений содержит `GET /iam/v1/auth/csrf?form=register` и `POST /iam/v1/auth/register`
**And** не содержит ни одного адреса чужого поставщика
**And** браузер держит печенье сессии
**And** адрес страницы стал адресом панели

**ID:** F8-15 — **занятый адрес: отказ дословно, и экран не сообщает, занят ли адрес** (близнец F8-14, различие — адрес уже заведён)

**Given** условия F8-14, но адрес **заведён** посевом
**When** отправлена форма регистрации на тот же адрес
**Then** отказ предъявлен дословно тем текстом и кодом, которые вернул глагол
**And** экран не добавляет к нему ни слова о том, заведён ли этот адрес
**And** печенья сессии нет

**ID:** F8-16 — **пароль не отвечает правилу службы: имя поля из ответа** (близнец F8-14, различие — значение пароля)

**Given** условия F8-14
**When** отправлена форма с паролем, не отвечающим правилу службы
**Then** отказ `400`, `code` = 3, с именем поля `password` и текстом службы
**And** на экране отмечено поле пароля и стоит этот текст
**And** перепись обращений содержит `POST /iam/v1/auth/register` — консоль своего правила не применяла

**ID:** F8-17 — **признак подтверждённости адреса показан, и действия, которого на посадке нет, не предложено**

**Given** условия F8-14, регистрация прошла
**When** экран показывает состояние учётной записи
**Then** признак подтверждённости адреса взят из поля `emailVerified` ответа глагола
**And** действия «подтвердить адрес» на экране нет — на этой посадке его производителя не существует (§3)

### S1 — группа D. Выход

**ID:** F8-18 — **выход гасит носитель и возвращает на экран входа**

**Given** условия F8-01
**And** у браузера есть действующая сессия
**When** человек нажимает выход
**Then** перепись обращений содержит `GET /iam/v1/auth/csrf?form=logout` и `POST /iam/v1/auth/logout`
**And** печенья сессии у браузера больше нет
**And** адрес страницы стал `/login`

**ID:** F8-19 — **служба не подтвердила выход: экран не делает вид, что вышли** (близнец F8-18, различие — доступность службы)

**Given** условия F8-18
**And** служба доступа краю не отвечает
**When** человек нажимает выход
**Then** ответ `503`, `code` = 14, сообщение `logout not performed; try again later`
**And** на экране стоит этот текст
**And** печенье сессии у браузера **осталось**, адрес страницы не изменился

### S1 — группа E. Посев набора переезжает на наши экраны

**ID:** F8-20 — **«Дано» браузерного набора строится нашим экраном регистрации**

**Given** условия F8-01
**When** фикстура набора заводит человека
**Then** её перепись обращений содержит `POST /iam/v1/auth/register`
**And** не содержит ни одного адреса чужого поставщика
**And** прогон набора целиком даёт тот же вердикт трижды подряд

**ID:** F8-21 — **фикстура, не дошедшая до сессии, падает текстом с экрана** (близнец F8-20, различие — исход церемонии)

**Given** условия F8-20
**And** церемония отвергнута службой
**When** фикстура ждёт печенья сессии
**Then** она падает **текстом отказа, снятым с экрана**, а не сообщением об отсутствующем печенье
**And** в тексте падения назван шаг, на котором поток остановился

**ID:** F8-41 — **бюджет оси источника объявлен числом и не превышен прогоном**

> Сторож решения Р7 ч. 2. Без него первым о переполнении общей оси скажет чужой сценарий,
> упавший по чужой причине, — и разбор уйдёт не туда.

> **Правка круга 2.** Редакция 2 требовала от браузерной пробы читать профиль развёртывания —
> прибора у неё для этого нет. Половин две, и у каждой свой держатель (Р7); «приблизился»
> квантифицировано порогом **80 %**.

**Given** прогон сценариев §11 целиком
**And** величина бюджета передана прогону **переменной окружения**, которую ставит тот же рецепт подъёма, что применяет профиль
**When** перепись считает обращения, давшие отказ, идущий в счёт потолка темпа — по **пяти местам вызова** таблицы Р7, а не по шапке
**Then** их число за один прогон не превышает **11**
**And** за три прогона подряд не превышает **33**
**And** вторая половина — проба уровня развёртывания: переданная прогону величина составляет **не более 80 %** от `sourceAttempts` профиля (на измеренных величинах 33 из 50 — **66 %**)
**And** отрицательный близнец: подсаженный сценарий с лишним отказом делает сторожа красным **с числом и с именем сценария**

**ID:** F8-44 — **распознаватель отказа переезжает на форму отказа НАШЕЙ службы, а семь тестов о порядке решения выживают**

> Обязанность 4 (§1.0, §1.5а): переезд посева отнимает предмет у проверок, которые чужой поток
> **не зовут** — они его мокают. Исход назван здесь, а не оставлен «заодно».

**Given** набор после переезда посева (F8-20)
**When** фикстура разбирает страницу отказа
**Then** она распознаёт форму отказа **нашей** службы — `google.rpc.Status` `{code, message, details}` — и отдаёт пару «код · сообщение»
**And** страница консоли без отказа отказом **не** считается — положительная и отрицательная стороны предиката живы обе
**And** прежний порядок решения сохранён: печенье сессии и поле пароля решают **раньше** распознавателя
**And** форма отказа чужого поставщика в наборе не вшита ни в одном месте
**And** инъекция: подсаженный ответ нашей формы делает распознаватель **распознавшим**, подсаженная страница без отказа — **не распознавшим**; обе стороны краснеют на обезвреженном распознавателе


### S1 — группа F. Достижимость с клавиатуры

**ID:** F8-22 — **церемония входа проходится с клавиатуры целиком**

**Given** условия F8-04
**When** человек доходит до полей и кнопки отправки клавишей перехода и отправляет форму клавишей ввода
**Then** вход прошёл — перепись обращений та же, что у F8-04
**And** ни одно поле формы и ни одна кнопка не потребовали указателя

### S2 — группа G. Смена пароля

**ID:** F8-23 — **смена пароля внутри сессии проходит и перевыпускает носитель**

**Given** условия F8-01
**And** посевом заведён человек и у браузера есть его действующая сессия
**When** на экране параметров учётной записи отправлена форма смены пароля с текущим и новым паролем
**Then** перепись обращений содержит `GET /iam/v1/auth/csrf?form=password` и `POST /iam/v1/auth/password`
**And** ответ `200`, тело несёт `session`
**And** значение печенья сессии у браузера изменилось
**And** вход прежним паролем после этого даёт `401` `authentication failed`, новым — `200`

**ID:** F8-24 — **текущий пароль неверен: отказ назван, сессия цела** (близнец F8-23, различие — значение текущего пароля)

**Given** условия F8-23
**When** отправлена форма с неверным текущим паролем
**Then** отказ предъявлен дословно тем кодом и текстом, которые вернул глагол
**And** печенье сессии у браузера не изменилось
**And** вход прежним паролем по-прежнему даёт `200`

**ID:** F8-25 — **служба молчит на глаголе с носителем: край отвечает F4d-23, сессия не гасится** (близнец F8-23, различие — доступность службы)

**Given** условия F8-23
**And** служба доступа краю не отвечает
**When** отправлена форма смены пароля
**Then** ответ **края** `401` с телом `{"code":16,"message":"session ended; sign in again"}`
**And** на экране стоит названный отказ, а не белый экран
**And** печенье сессии у браузера **осталось** — консоль его не гасит

### S2 — группа H. Второй фактор

**ID:** F8-26 — **состояние второго фактора прочитано и показано**

**Given** условия F8-23
**When** открыт экран параметров учётной записи
**Then** перепись обращений содержит `GET /iam/v1/auth/second-factor`
**And** на экране показано состояние, взятое из ответа этого глагола

**ID:** F8-27 — **заведение второго фактора доводится до подтверждения и запасных кодов**

**Given** условия F8-23, второй фактор не заведён, и человек у сценария **свой** — средство оси 4 (Р9): одноразовый код предъявляется здесь **один раз**, следующего шага проба не ждёт
**When** человек заводит второй фактор и подтверждает его первым кодом
**Then** перепись обращений содержит `POST /iam/v1/auth/second-factor/enroll`, затем `POST /iam/v1/auth/second-factor/confirm`
**And** между ними экран показал материал заведения из ответа `enroll`
**And** после подтверждения экран показал запасные коды и предупредил, что показывает их один раз
**And** повторное чтение состояния показывает второй фактор заведённым

**ID:** F8-28 — **неверный первый код: заведение не завершено, запасных кодов нет** (близнец F8-27, различие — значение первого кода)

**Given** условия F8-27, заведение начато
**When** подтверждение отправлено с неверным кодом
**Then** отказ предъявлен дословно тем кодом и текстом, которые вернул глагол
**And** запасных кодов на экране нет
**And** чтение состояния показывает второй фактор **не** заведённым

**ID:** F8-29 — **сессия не свежа: консоль ведёт повышение и возвращает на тот же шаг** (близнец F8-27, различие — свежесть предъявления в сессии)

**Given** условия F8-27, а ответ края на заведение **подставлен** пробой — средство оси 3 (Р9): окна свежести проба не пересиживает, и условия его выдачи судит приёмка Ф12, а не этот документ
**When** человек заводит второй фактор
**Then** ответ несёт `reason` = `SESSION_NOT_FRESH` и текст `re-authentication required: present a credential again`
**And** консоль открывает церемонию повышения и зовёт `POST /iam/v1/auth/step-up`
**And** после успешного повышения человек возвращён на тот же шаг, а не на панель

**ID:** F8-30 — **снятие второго фактора с подтверждением кодом**

**Given** условия F8-23, второй фактор заведён посевом
**When** человек снимает второй фактор, предъявив код
**Then** перепись обращений содержит `POST /iam/v1/auth/second-factor/remove`
**And** чтение состояния показывает второй фактор не заведённым

**ID:** F8-31 — **перечеканка запасных кодов**

**Given** условия F8-30, второй фактор заведён
**When** человек перечеканивает запасные коды, предъявив код
**Then** перепись обращений содержит `POST /iam/v1/auth/second-factor/backup-codes`
**And** экран показал новый набор и предупредил, что прежний недействителен

**ID:** F8-32 — **снятие того, чего нет: отказ назван** (близнец F8-30, различие — заведён ли второй фактор)

**Given** условия F8-23, второй фактор **не** заведён
**When** отправлено снятие
**Then** ответ несёт `reason` = `SECOND_FACTOR_NOT_ENROLLED` и текст `second factor is not enrolled`
**And** на экране стоит этот текст

### S2 — группа I. Вход со вторым фактором и повышение уровня

**ID:** F8-33 — **вход человека с заведённым вторым фактором доходит до уровня 2**

**Given** условия F8-04
**And** у человека посевом заведён и подтверждён второй фактор
**When** экран входа принимает адрес, пароль и код второго фактора и отправляет форму
**Then** тело `POST /iam/v1/auth/login` несёт поле `secondFactor` с названным способом и кодом
**And** ответ `200`, `session.assuranceLevel` равен уровню 2
**And** браузер держит печенье сессии

**ID:** F8-34 — **неверный код второго фактора: тот же один текст отказа** (близнец F8-33, различие — значение кода)

**Given** условия F8-33
**When** форма отправлена с неверным кодом второго фактора
**Then** ответ `401`, `code` = 16, сообщение `authentication failed`
**And** экран не сообщает, какая из двух величин не подошла

**ID:** F8-35 — **повышение уровня по вызову края идёт НАШИМ глаголом**

**Given** условия F8-33, сессия уровня 1, второй фактор заведён
**And** край требует уровня 2 на запрошенном действии
**When** консоль отвечает на вызов края
**Then** перепись обращений содержит `POST /iam/v1/auth/step-up`
**And** не содержит ни одного адреса чужого поставщика
**And** после успешного повышения исходное действие повторено и прошло

**ID:** F8-36 — **повышать нечем: назван отказ и путь, а не пустое окно** (близнец F8-35, различие — заведён ли второй фактор)

**Given** условия F8-35, но второй фактор **не** заведён
**When** край требует уровня 2
**Then** ответ несёт `reason` = `SECOND_FACTOR_NOT_ENROLLED`
**And** на экране стоит этот текст и путь на экран заведения второго фактора
**And** окно повышения не остаётся пустым и не закрывается молча

**ID:** F8-43 — **посев второго фактора и повышения уровня переезжает на наши глаголы**

> Блокирующее круга 1 [CONSTRUCTIBILITY]: второй путь посева (§1.5) ведёт три церемонии чужого
> поставщика напрямую и переселяется **здесь**, а не «заодно». Его импортёры — две
> спецификации, и без переезда они падают позже прочих, то есть в момент, когда причину уже не
> свяжут со снятием чужого экрана.

**Given** условия F8-23, второй фактор не заведён, человек у сценария **свой**
**When** оснастка набора поднимает уровень уверенности сессии
**Then** её перепись обращений содержит `POST /iam/v1/auth/second-factor/enroll`, `POST /iam/v1/auth/second-factor/confirm` и `POST /iam/v1/auth/step-up`
**And** не содержит ни одного из **пяти** обращений к поставщику, перечисленных в §1.5 — включая **чтение уровня уверенности**: уровень читается ответом **нашего** глагола
**And** оснастка **не ждёт следующего шага одноразового кода** — средство оси 4 (Р9); ожидания стенными часами в ней не остаётся ни одного
**And** обе спецификации-импортёра (`stepup.spec.ts`, `users.spec.ts`) проходят на этой оснастке, и прогон трижды подряд даёт тот же вердикт

### S2 — группа J. Чужие адреса уходят из консоли

**ID:** F8-45 — **в наборе не осталось ожидания стенными часами**

> Держатель правила Р9. Без него правило — проза, и первая же оснастка вернёт ожидание шага
> одноразового кода обратно.

**Given** набор после S2
**When** перепись обходит пробы и модули набора
**Then** мест, ждущих истечения срока стенными часами, — **ноль**
**And** перепись печатает знаменатель обхода — число прочитанных файлов — и краснеет на пустом обходе
**And** инъекция: подсаженное ожидание делает перепись красной **с координатой**; на снятой подсадке — зелёной
**And** положительная сторона: ожидания **условия** перепись находит и находками не считает — иначе предикат зеленел бы, вовсе не умея отличить одно от другого

**ID:** F8-37 — **прод-файлов консоли, ведущих человека на адрес церемонии чужого поставщика, ноль**

**Given** дерево консоли после S2
**When** перепись обходит прод-файлы `ui-future` (без проб, без `e2e`, без `node_modules`)
**Then** файлов, ведущих браузер на адрес церемонии чужого поставщика, — **0**
**And** перепись печатает знаменатель обхода — число прочитанных прод-файлов, — и на пустом обходе краснеет

**ID:** F8-38 — **подсаженное обращение перепись краснит с координатой** (близнец F8-37, различие — одно подсаженное обращение)

**Given** дерево консоли после S2 с подсаженным обращением к адресу церемонии чужого поставщика
**When** исполняется та же перепись
**Then** она красная и называет координату подсаженного места
**And** на снятой подсадке — снова зелёная


---

## §11.1 Сценарий → производитель

У каждого «Тогда» назван тот, кто его производит; без производителя утверждение не проверяемо
и держателя не имеет.

| ID | что производит «Тогда» | координата в дереве | чем измерено |
|---|---|---|---|
| F8-01, **F8-02**, F8-03, F8-04…F8-19, F8-22, **F8-39**, **F8-40**, F8-33…F8-36 | живая страница и её перепись сетевых обращений | новая проба `ui-future/e2e/specs/identity-ceremony.spec.ts`, `// verifies #2780` внутри `test(…)`; перепись — `apiCalls(page)` из `ui-future/e2e/specs/fixtures.ts` | `npm --prefix ui-future/e2e test` против поднятой цепочки |
| **F8-42**, и знаменатель §1.7 | рендер цепочки развёртывания | `deploy/tests/helm/console-serves-identity-flows-test.sh` (сегодня — в полосе `wave/identity-own-w0-kacho`, §1.6) | прогон гейта по `deploy/stacks.txt` |
| F8-20, F8-21 | сама фикстура посева набора | `ui-future/e2e/specs/fixtures.ts` | прогон набора целиком, трижды подряд |
| **F8-43** | оснастка подъёма уровня уверенности — **второй путь посева** (§1.5) | `ui-future/e2e/specs/assurance.ts` и обе спецификации-импортёра (`stepup.spec.ts`, `users.spec.ts`) | прогон обеих спецификаций, трижды подряд |
| **F8-41** ч. 1 | сторож бюджета: счёт отказов по **пяти местам вызова** Р7 за прогон | проба-сторож рядом с `ui-future/e2e/specs/identity-ceremony.spec.ts`; величина бюджета приходит переменной окружения | прогон §11 целиком, трижды подряд |
| **F8-41** ч. 2 | сверка бюджета с потолком профиля — **другой прибор, потому что у браузерной пробы доступа к профилям нет** | проба уровня развёртывания рядом с `deploy/tests/helm/` | прогон по `deploy/stacks.txt` |
| **F8-44** | распознаватель отказа и порядок решения в фикстуре | `ui-future/e2e/specs/fixtures.ts` (`identityRefusalFromText`, `identityRefusalOnPage`) и `ui-future/e2e/specs/registration-refusal-named.spec.ts` — **7 тестов** | прогон обеих, плюс инъекция в обе стороны |
| **F8-45** | перепись ожиданий стенными часами по набору, со знаменателем и инъекцией | проба-перепись рядом с `ui-future/e2e/specs/` | `npm --prefix ui-future/e2e test` |
| F8-23…F8-32 | живая страница параметров учётной записи и её перепись обращений | новая проба `ui-future/e2e/specs/account-settings.spec.ts`, `// verifies #1274` внутри `test(…)` | `npm --prefix ui-future/e2e test` против поднятой цепочки |
| F8-37, F8-38 | перепись прод-файлов консоли с печатью знаменателя и инъекцией в обе стороны | новая проба рядом с `ui-future/shared/src/test/identity-ceremony-carriers-rendered.test.ts` | `npm run test` модуля `vpc` (прогон `shared` идёт им) |
| F8-01, F8-03 — объявленность восьми маршрутов | модульная проба маршрутизатора оболочки | новая проба рядом с `ui-future/host/src/App.test.tsx` | `npm --prefix ui-future/host run typecheck && npm --prefix ui-future/host test` |
| F8-27, F8-29 — наличие носителя церемонии в переписи | существующий рендерный гейт полосы личности | `ui-future/shared/src/test/identity-ceremony-carriers-rendered.test.ts` | тот же прогон модуля |

**Сценариев без производителя — ноль.** Проверяемо: каждая строка §11 присутствует в этой
таблице; сценарий, которого в ней нет, — находка круга.

## §11.2 Близнецы: один изменённый факт на каждое отрицание

| отрицательный | положительный близнец | единственное различие |
|---|---|---|
| F8-02 | F8-01 | объявлен ли посадкой адрес чужого интерфейса |
| F8-03 | F8-01 | какой из восьми адресов открыт |
| F8-05 | F8-04 | значение пароля |
| F8-06 | F8-04 | пароль пуст |
| F8-07 | F8-04 | признак формы не приложен |
| F8-08 | F8-04 | вид признака формы |
| F8-09 | F8-05 | число подряд идущих неверных предъявлений |
| F8-10 | F8-04 | отвечает ли служба краю |
| F8-12 | F8-11 | годен ли носитель сессии |
| F8-13 | F8-04 | происхождение адреса возврата |
| F8-39 | F8-01 | что именно утверждается об отрисованном экране |
| F8-40 | F8-05 | заведён ли адрес |
| F8-15 | F8-14 | заведён ли адрес |
| F8-16 | F8-14 | значение пароля |
| F8-19 | F8-18 | отвечает ли служба краю |
| F8-21 | F8-20 | исход церемонии посева |
| F8-24 | F8-23 | значение текущего пароля |
| F8-25 | F8-23 | отвечает ли служба краю |
| F8-28 | F8-27 | значение первого кода |
| F8-29 | F8-27 | свежесть предъявления в сессии |
| F8-32 | F8-30 | заведён ли второй фактор |
| F8-34 | F8-33 | значение кода второго фактора |
| F8-36 | F8-35 | заведён ли второй фактор |
| F8-38 | F8-37 | одно подсаженное обращение |

Отрицаний **24**, и у каждого близнец назван. Положительных сценариев **21**. Всего **45**.

Сверх таблицы: **F8-41** и **F8-13** несут отрицание и положительную сторону **внутри одного
сценария** — у сторожа бюджета близнец есть по построению (подсаженный лишний отказ), у
проверки адреса возврата четыре отрицательных формы стоят против положительной F8-04. Оба
названы здесь, чтобы «близнеца нет» не читалось как упущение. Тем же устроены **F8-44** и
**F8-45**, добавленные кругом 2: у каждого обе стороны предиката утверждены внутри сценария —
распознал и не распознал, нашёл ожидание и не спутал его с ожиданием условия.

---

## §12 Вне объёма — что отложено, с условием возврата

| отложено | в какую под-фазу | условие возврата | чей номер |
|---|---|---|---|
| экран восстановления доступа: запрос кода и его предъявление с новым паролем; строка «не получается войти» на экране входа | **S3** | доставка письма существует и проверяема человеком | `#1773` (OPEN, `blocked`, P3) над Ф5 `#1271` |
| экран входа ключом доступа: кнопка рядом с паролем, обработка испытания браузером, снятие полей сериализации; перечень ключей на экране | **S4** | два глагола Ф13 в стволе платформы и край их маршрутизирует | Ф13 `#1282`, Ф7 `#1273`, край `#2718` |
| экран подтверждения адреса (`/verification`) | вместе с S3 | та же доставка письма | Ф6 `#1272`, `#1773` |
| `/consent` | **никогда** | предмет уходит вместе с внешним сервером выдачи токена | `#1107` |
| снятие подчарта чужого интерфейса и перевод цепочек | соседняя задача развёртывания | необратимый шаг не делается тем же изменением, что проверяемый | `#2777`, `#2735`, `#2733` |
| замыкающее правило маршрутизатора `*` вне восьми адресов полосы | отдельный предмет | радиус правки назван в Р3 | заводится находкой, если наблюдается |

---

## §13 Definition of Done

**Общее для S1 и S2:**

1. `.proto` и регенерация — **не трогаются**: контракт глаголов уже в дереве службы, и ни один
   сценарий его не меняет. Пункт назван, а не опущен, чтобы «не требуется» не читалось как
   «забыли».
2. Схема БД и миграции — **не трогаются** по той же причине.
3. Первой ложится **красная** проба F8-04 (§10); её краснота на дереве до фикса названа выводом
   прогона и ревизией, а не словом.
4. Браузерные пробы лежат в `ui-future/e2e/specs`, несут `// verifies #2780` (S1) и
   `// verifies #1274` (S2) **внутри вызова `test(…)`**, повторов не имеют и ждут условия, а не
   времени.
4а. **Имя каждого теста начинается с ID сценария** (Р8) — `F8-04 · …`. Проверяемо переписью:
   множество ID, снятых с имён тестов набора, сравнивается с множеством ID §11, и разность
   печатается **поимённо в обе стороны** — сценарий без пробы и проба без сценария. Без этого
   пункт 10 непроверяем by construction: номер задачи один на десятки проб и не говорит, какой
   из F8-01…F8-43 не сел.
5. Прогон набора целиком даёт тот же вердикт **трижды подряд**.
6. `npm run typecheck && npm test` зелёные в каждом правленом модуле; прогон `shared` идёт
   прогоном `vpc`.
7. Перепись §11.1 F8-37 печатает **знаменатель обхода** и краснеет на пустом обходе; её
   способность упасть доказана инъекцией в обе стороны (F8-38), а не зеленью.
8. Рендерный гейт полосы личности не снят и не ослаблен; наши экраны входят в его перепись.
9. Записка ресурса в хранилище контекста дописана по правилам `vault.md`: затронутые экраны,
   глаголы и рёбра.
10. Ни один сценарий не помечен пропущенным, и это читается **переписью пункта 4а**, а не
    просмотром: разность множеств ID пуста в обе стороны. Отложенный исход отсутствует в дереве
    целиком (ban #11).
10а. **Бюджет оси источника не превышен** (Р7, F8-41): счёт отказов считается по **пяти местам
    вызова** `recordFailure`, а не по шапке; бюджет — **11** за прогон и **33** за три; сверку с
    потолком профиля держит **вторая** проба, уровня развёртывания, потому что у браузерной
    прибора для этого нет.
10б. **Ни одно ожидание в наборе не идёт по стенным часам** (Р9, F8-45): перепись печатает ноль
    со знаменателем обхода и доказана инъекцией в обе стороны. Каждая из пяти осей §1.10 имеет
    названное средство построения, и ни одно из них не есть ожидание.

**S1 дополнительно:**

11. Восемь адресов полосы потоков имеют маршруты **все восемь**; тех, что консоль ведёт, —
    три (`/login`, `/registration`, `/logout`), остальные отвечают названной страницей.
11а. На экране входа **нет** перехода ни на `/recovery`, ни на `/verification` (Р4, F8-39):
    путь, которого на посадке нет, не обещается.
12. Фикстура посева набора заводит человека **нашим** глаголом регистрации; ни одна
    спецификация набора не зовёт чужой поток.
13. Предикат снятия `#2780` исполнен полностью: проба существует · видена красной до фикса и
    зелёной после, обе ревизии названы · ссылка на задачу внутри вызова теста · **и имя теста
    несёт ID сценария**, чтобы по дереву было видно, какой именно сценарий этой пробой закрыт.

**S2 дополнительно:**

14. Четыре живых места §1.8 больше не ведут человека на адрес церемонии чужого поставщика;
    перепись F8-37 это печатает числом.
15. Церемония повышения уровня зовёт `POST /iam/v1/auth/step-up`, а не адрес поставщика.
16. **Второй путь посева переехал целиком — все ПЯТЬ обращений** (§1.5, F8-43): оснастка
    подъёма уровня уверенности (`ui-future/e2e/specs/assurance.ts`) ведёт `enroll`, `confirm` и
    `step-up` **нашими** глаголами, **уровень читает ответом нашего глагола** (пятое обращение,
    которое редакция 2 пропустила), ожидания следующего шага одноразового кода в ней не
    осталось, и обе спецификации-импортёра (`stepup.spec.ts`, `users.spec.ts`) на ней проходят.
    Производитель пункта — сама оснастка и прогон обеих спецификаций трижды подряд.
16а. **Судьба каждой проверки о снятом предмете названа и исполнена** (§1.5а, F8-44):
    `registration-refusal-named.spec.ts` (**7** тестов) и два распознавателя в
    `fixtures.ts` переведены на форму отказа **нашей** службы; форма отказа чужого поставщика
    в наборе не вшита ни в одном месте. Пункт заведён потому, что DoD 12 этих проверок **не
    ловит by construction**: они чужой поток не зовут — они его мокают.
17. **Перепись восьми адресов полосы исполнена ПОСЛЕ S2** и печатает по каждому: ведёт ли его
    консоль, на каком адресе живёт её экран этого предмета и перехватывает ли его полоса на
    посадке сосуществования. Требование введено потому, что `/settings` входит в восемь и по
    Р1 перехватывается до оболочки — а экран параметров живёт на `/account/security` (Р3а).
    Без переписи это решение не проверяемо ничем, и первое, что о нём скажет дерево, —
    недостижимый экран.
18. **Предикат снятия Р3а назван в дереве, а не в памяти:** запись рядом с маршрутом
    `/account/security` говорит, что при снятии полосы с посадки `/settings` переводится на
    этот экран тем же изменением. Без предиката терпимость к двум местам не истекает сама.

**Предикат готовности всей работы (исправленный, §1.7):**

> на цепочке, где источников подчарта консоли **больше нуля**, полос обслуживания чужого экрана
> **ноль**, блоков `.ory` **ноль**, и браузерная проба проходит вход и регистрацию на наших
> экранах.

Числитель без знаменателя здесь не принимается: «ноль полос» на цепочке, не разворачивающей
консоль, — не достижение, а отсутствие предмета.

---

## §14 Критерии для `acceptance-reviewer`

1. **Шов с двумя одобренными приёмками держится:** ни один «Тогда» §11 не имеет производителя
   в `PRO-Robotech/kaname`; ни одно решение соседа не переоткрыто (§0.2).
2. **Дом назван основанием, а не вкусом** (§0.3), и три довода проверяемы командой.
3. **Каждое число §1 несёт единицу счёта, предикат, ревизию и контроль в обе стороны там, где
   он возможен** (§1.1 — подсадка; §1.7 — положительный контроль на `dev`).
4. **У каждого «Тогда» назван производитель** (§11.1); сценариев без производителя ноль.
5. **У каждого отрицания назван близнец и единственное различие** (§11.2); различий больше
   одного — находка.
6. **Ban #14 соблюдён декомпозицией, а не урезанием:** у каждой под-фазы §5 названа граница
   «человек проходит целиком», а отложенное стоит в §12 с условием возврата.
7. **Ban #18 отвечен по всем пяти осям** (§8), и ответы по конкурентоспособности и
   перспективности опираются на сценарии, а не на прилагательные.
8. **Ban #2 соблюдён:** чужой поставщик назван как факт дерева, которое продукт снимает; ни
   одна конвенция не обоснована подражанием.
9. **Предикат готовности исправлен и несёт знаменатель** (§13): «ноль» без числа прочитанного
   к принятию не предлагается.
10. **Третья категория объявлена заранее** (§4): условие прогона, которого ещё нет, названо
    «не выполнилось», а не обещанием зелёного.
11. **Документ выдерживает публикацию:** сборки «координата + условие + следствие» в нём нет,
    ссылок на рабочие каталоги задач — тоже.

**Добавлено кругом 1 — по одному критерию на каждое блокирующее:**

12. **[SCOPE]** Адрес экрана S2 назван (Р3а, `/account/security`), цена и отвергнутый исход
    записаны, перепись восьми адресов стоит в DoD (п. 17) и её предикат истечения — в дереве
    (п. 18).
13. **[COVERAGE]** У стороны Р4 «путь не обещается» есть держатель — **F8-39**, и у него есть
    положительная сторона (переход на `/registration` на экране есть), иначе предикат
    «перехода нет» зеленел бы тождественно.
14. **[NEGATIVE]** У стороны Р4 «причины неразличимы» есть держатель — **F8-40**, и он
    утверждает **побайтовое** равенство экрана с F8-05, а не похожесть.
15. **[TWIN]** F8-02 судится **тем же прибором**, что близнец F8-01, и меняет ровно один факт;
    наблюдение рендером вынесено в отдельный сценарий **F8-42** со своим знаменателем.
16. **[CONSTRUCTIBILITY ×2]** Второй путь посева найден **содержательным** предикатом (§1.5) и
    переселяется сценарием **F8-43**; потолок темпа признан двухосевым (Р7), у каждого
    отрицания свой адрес, бюджет общей оси объявлен числом и держится **F8-41**.
17. **[DoD]** Имя теста несёт ID сценария (Р8), и разность множеств ID печатается поимённо в
    обе стороны (DoD 4а) — иначе «ни один сценарий не пропущен» непроверяемо.
18. **[FORMAT]** F8-13 перечисляет четыре конкретные формы адреса возврата, включая
    протокол-относительную, которую наивная проверка пропускает.

19. **Числа редакции 1, оказавшиеся неверными, исправлены С НАЗВАННОЙ ПРИЧИНОЙ, а не тихо:**
    §1.5 (24 различных вместо 47 — дублирование аргументов без `sort -u`) и §1.6 (4 вместо 5 —
    образец не отличал условие от упоминания о нём). Оба вывода при этом подтверждены
    независимо, и это сказано.

**Добавлено кругом 2 — род блокирующего повторился, поэтому критерии проверяют МЕТОД, а не
отдельные числа:**

20. **[обязанность 1]** Каждое утверждение о поведении службы выведено из **мест вызова**, а не
    из шапки: перечень Р7 — пять мест с координатами, и два из них шапка `rate.go` не
    называет. Бюджет пересчитан: **11 / 33**, а не 10 / 30.
21. **[обязанность 2]** Каждый предикат §1 прогнан **в обе стороны**, и сказано, что дала
    расширенная форма: §1.5 — сужение по написанию пути дало 4, расширение по предмету дало
    **пять обращений и второй файл**. Где расширение не дало ничего — это тоже напечатано.
22. **[обязанность 3]** Перепись осей, снимаемых временем, **полна**, а не состоит из найденных
    (§1.10 — пять осей с читателями), и у каждой названо средство построения, **не являющееся
    ожиданием** (Р9). Держатель правила — F8-45.
23. **[обязанность 4]** У каждой проверки о снимаемом предмете названа судьба (§1.5а), и исход
    выбран из **двух** законных с ценой отвергнутого. Держатель — F8-44.
24. **Документ верен о себе:** число новых сценариев, диапазоны ID и счётные строки §11.2
    сходятся с фактическим содержимым — проверяемо разбором файла, а не чтением.
25. **Единицы счёта названы там, где их две:** «проб 23» и «файлов 28» — разные величины, и
    утверждение говорит о той, о которой оно сказано.

---

## §15 Решения, принятые здесь и не оставленные развилкой

| # | решение | где |
|---|---|---|
| 1 | принадлежность адреса церемонии выражена раздачей; второй ручки нет | Р1 |
| 2 | консоль не выносит суждения о содержимом поля | Р2 |
| 3 | адрес, которого консоль не ведёт, отвечает названной страницей; радиус — восемь адресов | Р3 |
| 4 | человек чужой службы идёт той же полосой; особой ветки нет; путь восстановления не обещается до S3 | Р4 |
| 5 | порядок под-фаз задан готовностью производителя | Р5 |
| 6 | экран наблюдается переписью обращений, а не разметкой | Р6 |
| 7 | дом документа — воркспейс `docs/specs/` | §0.3 |
| 8 | предикат готовности несёт знаменатель обхода | §1.7, §13 |
| 9 | экран параметров учётной записи живёт на `/account/security`, а не на `/settings`; терпимость к двум местам истекает снятием полосы | Р3а |
| 10 | ось адреса у каждого отрицательного сценария своя; ось источника — общий ресурс прогона с объявленным бюджетом и сторожем | Р7 |
| 11 | имя теста начинается с ID сценария; ID не переприсваиваются | Р8, §11 преамбула |
| 12 | второй путь посева (оснастка подъёма уровня) переселяется в S2, а не «заодно» | §1.5, F8-43 |
| 13 | у каждой оси времени названо средство построения, и ни одно не есть ожидание; сценарий судит консоль, а не службу | Р9, §1.10 |
| 14 | распознаватель отказа переводится на форму отказа нашей службы, а не снимается вместе с предметом; отвергнутый исход назван с ценой | §1.5а, F8-44 |
| 15 | бюджет держат ДВА прибора: счёт — набор, сверку с потолком — проба развёртывания; порог «приблизился» — 80 % | Р7, F8-41 |
| 16 | метод получения чисел записан в документе как четыре обязанности, а не оставлен в памяти автора | §1.0 |

**Развилка, оставленная владельцу, одна, и она названа строкой возврата, а не решена здесь:**
перевод посадки `own` на разворачивание консоли (включение подчарта и опустошение адреса чужого
интерфейса) — предмет развёртывания, и без него ни один сценарий §11 не исполним (§4).

---

## §16 Дельта редакции 2 — что правит каждое блокирующее круга 1

Все восемь закрыты **добавлением**: ни один сценарий редакции 1 не снят, ни одно решение не
переоткрыто. Ниже — по одному исходу на каждое, с точным перечнем правленых мест.

| # | род | исход | правятся |
|---|---|---|---|
| 1 | SCOPE | принято **Р3а**: экран параметров учётной записи живёт на `/account/security`, а не на `/settings`; цена (два места на посадке сосуществования) и отвергнутый исход (вынуть `settings` из полосы) названы; предикат истечения — в дереве | Р3а (новая), §3 (строка `/settings`), §5 (граница S2), §13 пп. 17 и 18, §15 п. 9 |
| 2 | COVERAGE | заведён **F8-39** — держатель стороны Р4 «путь не обещается», с положительной стороной предиката | §11 группа B, §11.1, §11.2, Р4, §13 п. 11а, §14 п. 13 |
| 3 | NEGATIVE | заведён **F8-40** — держатель стороны Р4 «причины неразличимы»; утверждается **побайтовое** равенство экрана с F8-05 | §11 группа B, §11.1, §11.2, Р4, §8 (строка безопасности), §14 п. 14 |
| 4 | TWIN | **F8-02** переведён на прибор близнеца (браузер), различие сведено к одному факту; наблюдение рендером вынесено в **F8-42** со своим знаменателем | §11 группа A, §11.1, §14 п. 15 |
| 5 | CONSTRUCTIBILITY | §1.5 перемерена **содержательным** предикатом; найден второй путь посева — оснастка подъёма уровня, ведущая три церемонии поставщика напрямую; заведён **F8-43**, дан производитель и пункт DoD | §1.5 целиком, §5 (граница S2), §11 группа I, §11.1, §13 п. 16, §15 п. 12, §14 п. 16 |
| 6 | CONSTRUCTIBILITY | принято **Р7**: потолок темпа двухосевой; у каждого отрицания свой адрес; бюджет общей оси источника объявлен числом (10 за прогон, 30 за три при потолке 50) и держится **F8-41**, читающим потолок из профиля прогона | Р7 (новое), §11 преамбула, F8-09, §11.1, §13 п. 10а, §15 п. 10, §14 п. 16 |
| 7 | DoD | принято **Р8**: имя теста начинается с ID сценария; разность множеств ID печатается поимённо в обе стороны | Р8 (новое), §11 преамбула, §13 пп. 4а, 10, 13, §15 п. 11, §14 п. 17 |
| 8 | FORMAT | **F8-13** перечисляет четыре конкретные формы, включая протокол-относительную; названо, что закрывающий их предикат в дереве уже есть, и сценарий делает его держателем, а не заводит второй | §11 группа B, §14 п. 18 |

**Важные, не блокирующие — исполнены обе:**

- §1.5 давала **47** там, где различных **24**: образец применялся к перекрывающимся аргументам
  без `sort -u`. Исправлено, и рядом назван предикат, которым число получено;
- §1.6 давала **5** там, где условий **4**: образец не был привязан к началу строки и считал
  прозу, называющую ручку. Исправлено; вывод раздела подтверждён независимо рендером §1.7.

**Что редакция 2 НЕ трогает:** решения Р1, Р2, Р4 (по существу), Р5, Р6; дом документа §0.3;
шов с двумя одобренными приёмками §0.2; исправленный предикат готовности §13; границы под-фаз
§5, кроме дописанного адреса S2 и переезда второго пути посева. Сценарии редакции 1 сохраняют
свои ID: **ID не переприсваиваются** (§11 преамбула), и **пять** новых взяли F8-39…F8-43 в
порядке заведения.

**Перепись документа после правки:** сценариев **43** (положительных 19, отрицательных 24, у
каждого отрицания назван близнец и единственное различие); решений **12**; производитель назван
у **43 из 43**.

---

## §16.2 Дельта редакции 3 — правилась ФОРМА, потому что род повторился

Блокирующих круга 2 — четыре, и три из них одного рода. По
`.claude/rules/multi-agent-flow-orchestration.md` §14а это переносится **в форму задания**, а не
закрывается перечнем ответов. Поэтому главная правка редакции 3 — **§1.0**: четыре обязанности,
по которым получено каждое число, записаны в документе и применены ниже дословно. Всё
остальное — их частные случаи.

| # | обязанность / род | что было неверно | исход |
|---|---|---|---|
| 1 | **места вызова, а не шапка** | бюджет Р7 построен на шапке `rate.go`; код шире — след пишут **пять** мест, двух шапка не называет. Сторож краснел бы **на верной реализации** | Р7 перестроено на таблицу мест вызова с координатами; **F8-28** внесён; бюджет **11 / 33** вместо 10 / 30; вклад F8-29 и F8-36 измерен и равен **нулю** (`countsAsAttempt` не покрывает `verdictNotEnrolled`) |
| 2 | **предикат в обе стороны** | формулировка метода записана верно и **не применена к следующему измерению**: снова считалось написание пути. Обращений **пять**, а не четыре; чтение уровня подставлено **вместо** потока повышения | §1.5 несёт оба предиката и обе стороны; пять обращений перечислены таблицей со строками; расширение дало **второй файл** → §1.5а; F8-43 требует переезда **всех пяти** |
| 3 | **полный перечень осей времени** | Р7 разобрало две оси; осей **пять**, три не рассматривались | **§1.10** — перепись с читателями; **Р9** — средство построения на каждую, и ни одно не есть ожидание; правлены «Дано» F8-12, F8-27, F8-29, F8-43; держатель правила — **F8-45** |
| 4 | **судьба каждой проверки о снимаемом предмете** | переезд посева отнимает предмет у спецификации из **семи** тестов и **двух** распознавателей; DoD 12 их не ловит — они чужой поток не зовут, а **мокают** | **§1.5а** — перепись и два законных исхода; выбран перевод на форму отказа нашей службы, отвергнутый назван с ценой; держатель — **F8-44**; DoD 16а |

**Частности круга 2, закрытые тем же заходом:**

- **F8-41** разделён на **две половины с разными приборами**: счёт держит набор (величина
  приходит переменной окружения), сверку с потолком профиля — проба уровня развёртывания.
  Браузерная проба доступа к профилям не имеет, и прежнее требование было неисполнимо. Порог
  «приблизился» квантифицирован: **80 %**; на измеренных величинах — **66 %**;
- **документ был трижды неверен о себе** — новых сценариев редакции 2 было **пять**
  (F8-39…F8-43), а названо четыре. Исправлено во всех трёх местах; DoD 4а диапазон называл
  верно и не трогался;
- **единица счёта** «двадцать восемь спецификаций» заменена на верную: файлов `*.ts` — **28**,
  проб `*.spec.ts` — **23**, модулей — **5**. Утверждение истинно на обеих, но называть надо
  ту, о которой оно сказано.

**Что редакция 3 НЕ трогает:** все шесть блокирующих круга 1 приняты рецензентом и оставлены
как есть — Р3а, F8-39, F8-40, F8-02, Р8, держатель переадресации; решения Р1, Р2, Р4, Р5, Р6;
дом §0.3; шов §0.2; предикат готовности §13; оба исправленных числа §1.5 и §1.6 (перемерены
рецензентом и верны). **ID не переприсваивались**: круг 2 добавил F8-44 и F8-45 в порядке
заведения.

**Перепись документа после правки:** сценариев **45** (положительных 21, отрицательных 24);
решений **16**; производитель назван у **45 из 45**; осей времени — **5**, и у каждой названо
средство построения.

### О ветке и о двух записях ревью — принято к сведению, действий с моей стороны нет

Документ не отслеживается, а запись ревью круга 1 закоммичена на той же ветке: в дереве лежит
вердикт о содержимом, которого в дереве нет. Ветка названа по хуку оснастки, приёмку ищут по
эпику, и за время круга голова ушла тремя чужими коммитами. Разбор ветки назначен
оркестратором; переносить документ и **обе** записи ревью надо вместе. **Дом-репозиторий и путь
`docs/specs/` при этом верны и не меняются** — §0.3 не переоткрывается. Коммит — момент другого
исполнителя, и этот документ его не делает.
