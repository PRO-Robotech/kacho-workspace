# Sub-phase XC-7 (единый контур работы с iam — НОСИТЕЛЬ) — Acceptance

> **Статус: ✅ APPROVED — 2026-08-10, третий круг (`acceptance-reviewer`, ban #1).**
> **На чём держится:** Р7 закрыт — разбивка исполненного сведена к **одному** предикату (колонка «Полна, когда» §8.1), и третий круг прогнал его по таблице механически, а не прочитал. **Разбивка, которую он тогда получил (2026-08-10, на `92ca6af9d`): ✔-только — Ф0т; смешанные ✔/✗ — Ф2, Ф4; без отметок — Ф1, Ф3, Ф5, Ф6, Ф7, Ф8; 1 + 2 + 6 = 9 в охвате (Ф0 вне).** Это **факт о том круге**, а не о сегодняшнем дереве: с тех пор ствол ушёл, и **действующая разбивка живёт ровно в одном месте — итоге §8.1**, который выводится из той же колонки. Шапка её **не воспроизводит**: два места об одном предмете расходятся на первом же движении дерева, и расходились — см. врезку «Здесь стояли три разных разбивки». Шесть предметов Р1-Р6 закрыты исполнением ещё вторым кругом. Вердикты кругов **не правились**: §R · §R.1 · §R.2 · §R.3.
> **Что закрыто:** все **шесть** блокирующих предметов Р1-Р6 плюс три незакрывающих. Основание правок — **не чтение, а исполнение**: каждое утверждение о стволе перемерено на `92ca6af9d` предикатом, названным при числе, и предикат прогонялся зеркально (сколько **не** попало). Числа §2.1 **оставлены на своей базе `cf69249ed`** и не переписаны задним числом; всё, что стало иначе, вынесено отдельными таблицами с обеими ревизиями: **§2.4** (база → `92ca6af9d`), **§14.3** (`92ca6af9d` → `4d26e3300`, перемер после волны 2026-08-11) и **§2.5** — закрепления, которые меряет проверка, а не читатель.
> **Что осталось и почему:** **разбивку фаз здесь искать не надо — она в итоге §8.1 и только там** (перемер 2026-08-12 на `4d26e3300`; числа этой шапки в прошлых редакциях устаревали дважды именно потому, что она их дублировала). Что остаётся **не фазами**: три предмета, от которых зависит форма §4, но которые **не мои** и правятся своими владельцами — форма вывода карты в XC-2 §3.5 (та же переоценка, что Р2); отсылка XC-6 §0/Э1 за семантикой нулевого значения — она ведёт в **§4.2**, и там её нет **и никогда не было** (перемерено третьим кругом, §R.3): и тип, и семантика живут в §4.1, держатель — §4.6 О1; и снятие самодельного рубежа — **в-12, отдельный предмет с предикатом и своим тикетом**, приёмка на него не написана (§13 в-12).
> **Дата написания:** 2026-08-09 · **правки первого круга:** 2026-08-10
> **Эпик/тикет:** KAC-XC-7 (cross-cutting, redesign-2026). **Условие «номер завести до старта Ф1» обойдено**, и это записывается фактом, а не переписывается: Ф1 исполнена (`1c5535248`), а номера в трекере на 2026-08-12 нет. Заведён **журнал работ** — `obsidian/kacho/KAC/KAC-XC-7.md`, — и остаток назван там же: номер трекера отсутствует, предикат снятия — непустое поле `yt_url` этой записки.
> **Монорепо:** `project/kacho` (`github.com/PRO-Robotech/kacho`), **база сверки — `cf69249edb7902f4beeb30c0cf6098d6c2ae9130`** (та же, что у XC-6).
> **Как читались числа.** Ветка на момент письма — `agent/helm4-live-and-nic-e2e`, `HEAD = 0dba5ada6`, и он **на 2 коммита впереди базы** (`git rev-list --count cf69249ed..HEAD` → 2). Поэтому все перемеры сделаны формой `git grep <rev>` / `git show <rev>:<path>`, а **не** с рабочего дерева: замер чтением файла приписывался бы HEAD, а объявлен на базе.
> **Формат:** Given-When-Then · markdown · формы типов и сигнатур приводятся как **описание контракта**, файлов в дерево этот документ не заводит (ban #1).
> **Решения владельца внесены 2026-08-09.** Двенадцать вопросов §13 переведены из открытых в
> **решённые** — каждый с отвергнутой стороной, обратимостью и держателем. Тело приведено к ним:
> §0 · §1.4 · §1.7 · §4.1 · §4.4 · §4.5 · §5 уровень 1 п.4 · §6 (A5, A11) · §8.1 · §8.3 · §10 п.12 ·
> §11 (К-5, К-6, К-7, К-9, К-10) · §12 · §15. Два решения меняют **конструкцию**, а не только план,
> и это названо по месту: **в-6** снимает заявленную инверсию семантики пустого круга (утверждение
> переезжает с уровня языка на уровень отказа старта), **в-5** отменяет объявление `Lists` в
> дескрипторе. Открытых вопросов документ больше не содержит.
> **Нормативка (не дублируется в тело — ссылки по названию раздела):**
> - `.claude/rules/security.md` — §«Кто вправе ГОВОРИТЬ ЗА пользователя», §«AuthN+AuthZ ВЕЗДЕ», §«Production-mode обязателен ВЕЗДЕ», §Hardening-инварианты (#8, #9), §«Авторизация живёт в МОДЕЛИ», §«Публичные артефакты»
> - `.claude/rules/api-conventions.md` — §By-lane code-split, таблица reason-token, §«Принято-и-проигнорировано», §Gotcha'и
> - `.claude/rules/data-integrity.md` — §«Межсервисное намерение», §«Authz-материализация owner-доступа», §Outbox-drainer concurrency
> - `.claude/rules/testing.md` — §«Гейт на класс», §E2E
> - `.claude/rules/architecture.md` — §«Переиспользование через общий фундамент `pkg/`», §LEAN

---

## §R Ревью 2026-08-10 — `CHANGES REQUESTED`: что именно ждёт правки

Вердикт получен **исполнением**, а не чтением: база `cf69249ed` объявлена документом честно, но
ствол ушёл от неё на **87** коммитов (`git rev-list --count cf69249ed..HEAD` на `92ca6af9d`), и
проверялось состояние **ствола**, потому что по нему будут работать. Основание документа
устояло: воспроизведены П1 (15 = 14 вызовов + 1 в комментарии, на обеих ревизиях), П2, П3, П8
(три копии `internal/fgaboot/gate.go` живы и на стволе), П10 (0), П11 (8), П12, П13, П14 (**2**,
не 3), П4 и П6 на базе — П6 у четвёрки сошёлся **до строки** (3753/17). Из И-ПРО воспроизведён
разброс бюджетов помощника «дождись авторизации»: **6 определений в 6 файлах, 7 копий** (в одном
файле их две), бюджеты **6…48 с** — восьмикратный разброс подтверждён; моя первая единица счёта
(«файл») была неверной, число документа — верным.

**Блокирующее — шесть предметов.**

**Р1. Сделанное описано как предстоящее — не менее пяти предметов.** Перемерено `cf69249ed` → `HEAD`:
карта прав **выводится** у шести из семи (`pkg/authz/catalogderive`, коммиты `ca3354d94` → `569bea84c`);
рукописные карты 2199 строк в 6 файлах → 240 (по 40); `pkg/authz/catalogparity` **удалён** — то есть
§14 «492 строки — теряет предмет» уже исполнено, а координата П7 на стволе не резолвится. Сужатель
списков сведён в `pkg/listnarrow` (`924691519`, `fd0c22cfe`, `c4d733248`): у четвёрки 3753/17 → 587/12.
Звено восстановления паники — одно на платформу (`a99750f87`); интерсепторных копий у geo/storage/vpc/iam
на стволе нет, значит §1.4 «4 из 7», §14 «5 → 1» и §8.3 п.1 «три сервиса получают впервые» описывают
снятое состояние. Круг отправителей стал **типом** фундамента (`7f03e0bae`), страж — один на семь в
`Config.Validate` (`5573aa87f`). Закрытый набор приёма переехал в `pkg/authz/proxytuple` (`91850e208`,
`49851acb7`); полоса отказа — закрытый тип в `pkg/errors` (`96055cbd8`); окно отзыва — ручка у каждого
держателя кэша (`9bcf28b3b`). Это фазы Ф0т, Ф2, Ф4 и половина §4.4/§4.6. **Правка:** перевести
исполненное в прошедшее время с номерами коммитов, пересобрать §14 на стволе, оставить фазами только
то, чего в стволе нет (`pkg/servicecontract`, `pkg/servicehost`, `pkg/peer`, `pkg/kacho` — проверено:
на обеих ревизиях **0** файлов).

**Р2. §4.5 и §11 К-3 предписывают механизм, который ствол отверг своей же шапкой.** К-3 назвал две
стороны — генерация Go-файла и выбор строк из вшитого каталога — и выбрал вторую. Ствол взял **третью,
не названную ни одной стороной**: вывод из **дескрипторов proto, слинкованных в бинарь**. Шапка
`pkg/authz/catalogderive/derive.go` отвергает механизм §4.5 прямо: чтение JSON при старте потребовало бы
**третьей** вшитой копии, а копии расходятся. Поэтому §4.5 «канонический файл каталога переезжает в
`pkg/`» и §14 «экземпляров 2 → 3» — предписание завести копию, которой ствол намеренно избежал
(`git ls-tree -r --name-only HEAD | grep permission_catalog.json` → **2**). Исполнение §4.5 как написано
есть регресс. **Правка:** переписать К-3 третьей стороной как принятой, с сохранением обеих отвергнутых.

**Р3. §4.4 и §6 A7 предписывают вернуть снятое.** XC-7 требует закрытый тип **отношения** и утверждает
(A7): «отношение вне закрытого набора → не компилируется». Landed `pkg/authz/proxytuple` объявляет
решение **тройкой** (субъект, отношение, тип объекта) и пишет в собственной шапке, что проверка,
знающая только набор отношений, неверна **и в разрешающую сторону тоже**, и что возвращать проверку
членства одного отношения на путь записи нельзя. **Правка:** привести §4.4 и A7 к тройке; иначе
приёмка предписывает то, что дерево уже запретило.

**Р4. Владение осью личности настоящее — но документ-владелец отстал от своего потребителя.** XC-6
(§Э1, строка L1, шаг 3) называет XC-7 владельцем и указывает четыре несущих элемента; все четыре в
XC-7 **резолвятся** (§4.1 поле, §4.4 тип, §4.6 О1, §6 A5, §13 в-6) — ссылка в пустоте **не висит**.
Но XC-6 помечает шаг 3 «**исполнен** `7f03e0bae` + `5573aa87f`», а XC-7 описывает тип и О1 как
предстоящие и называет тип именем, отличным от landed. Исполнение §4.1 буквально завело бы **второе
имя** для существующего типа. **Правка:** §4.1/§4.4 назвать landed-тип и landed-предикат непустоты.

**Р5. Неназванная остаточная поверхность — на собственной оси владения.** Landed-страж круга несёт
**явный опт-ин на несужение**, не действующий в боевом режиме (введён тем же `5573aa87f`, ручка
объявлена в конфиге каждого сервиса). §5 п.8а и в-6 утверждают, что посадки без стража не бывает
**by construction**; с опт-ином это держится **правилом** «production везде», а не построением, и §9
эту поверхность не называет. По собственному критерию 7 документа — гейт, выданный за свойство
построения. **Правка:** назвать опт-ин строкой §9 и понизить формулировку в-6 с «by construction» до
«правилом, и вот каким».

**Р6. Величина отступлений разошлась внутри документа и с источником.** §0 и §2.3 говорят **20/8**,
§12 п.1 — **21/7**. Источник сегодня: XC-6 §4 — **20**, §4а — **9** (XC-6 сам пишет: «Число выросло с
восьми»). Три пары для одной величины, ни одна не полна. §15 критерий 2 требует сверять с источником
при каждом касании — документ не выполняет собственный критерий в §12.

**Не блокирует, но учесть.** П12 стал 72/68 (не несущее). Координаты §2.1, ведущие в
`services/vpc/internal/apps/kacho/check/`, на стволе не резолвятся — каталог переехал (`8d07b6dd2`).
Конфликта с XC-2 документ **не заявляет вовсе** (два упоминания, оба — раздача владения); предмет
назван **обеими** сторонами, но у XC-2: его §3.5 несёт отвергнутую сторону дословно и ссылается на
§13 в-5 — молчаливого разрешения нет. Однако форма вывода («из строк вшитого каталога») пережила свой
предмет **в обоих** документах, и Р2 придётся исполнить с обеих сторон.

**Что остаётся сильным и не требует правки:** §1.2, §1.3, §1.5 (разбор куба и три корзины
переносимости), §1.4 неточности 1-3, §5 с разделением на три уровня и честной границей «порядок
звеньев остаётся соглашением», §9 как непустой раздел остаточного риска, все тринадцать
отрицательных результатов §10 и форма §13 — шесть частей и отвергнутая сторона выдержаны по всем
двенадцати решениям.

## §R.1 Ответ автора (2026-08-10) — что сделано по каждому пункту

Вердикт §R выше **не правился**: он снят ревьюером на `92ca6af9d` своим предикатом, и переписывать
его под сегодняшний текст значило бы уничтожить измерение. Здесь — что изменено в теле и **чем это
проверено**. Каждое число ниже перемерено мной на том же `92ca6af9d`; предикат назван, и там, где
он мог соврать, назван **зеркальный** результат (сколько **не** попало).

| # | Что требовалось | Что сделано | Где | Чем проверено |
|---|---|---|---|---|
| **Р1** | перевести исполненное в прошедшее, пересобрать §14 на стволе, оставить фазами только отсутствующее | **закрыто целиком.** Заведена §2.4 (дельта база→ствол, обе ревизии названы); §8.1 несёт колонку состояния с коммитом на каждую фазу; §14 пересобрана двумя колонками — «на базе» и «в стволе»; §1.4, §1.5, §8.3 п.1 получили врезки «замер базы верен, ствол ушёл» | §2.4 · §8.1 · §14 · §1.4 · §1.5 · §8.3 | четыре пакета предложения (`pkg/servicecontract`, `pkg/servicehost`, `pkg/peer`, `pkg/kacho`) — `git ls-tree -r --name-only <rev> -- <p> \| wc -l` = **0** на обеих ревизиях; зеркально: тот же предикат на `pkg/listnarrow` даёт **9** файлов на стволе и **0** на базе, значит он умеет отвечать «есть» |
| **Р2** | переписать К-3 третьей стороной как принятой, обе отвергнутые сохранить | **закрыто.** Третья сторона (вывод из **дескрипторов proto, слинкованных в бинарь**) внесена в §4.5 и К-3 как принятая; обе прежние стороны сохранены с тем, чем были сильны; снято предписание «каталог переезжает в `pkg/`» и «экземпляров 2 → 3» | §4.5 · §11 К-3 · §14 | `git ls-tree -r --name-only <rev> \| grep permission_catalog.json` → **2** на обеих ревизиях (шапка `pkg/authz/catalogderive/derive.go` называет третью копию как **причину** отказа от чтения JSON) |
| **Р3** | привести §4.4 и A7 к тройке | **закрыто.** Решение приёма выражено **тройкой** (субъект · отношение · тип объекта); прежняя формулировка «отношение вне набора не компилируется» сохранена врезкой с причиной, почему она неверна **и в разрешающую сторону** | §4.4 · §6 A7 | шапка `pkg/authz/proxytuple/policy.go`: правило — `ValidateTuple`, и она прямо запрещает возвращать проверку членства одного отношения на путь записи |
| **Р4** | назвать landed-тип и landed-предикат непустоты | **закрыто.** §4.1 несёт `grpcsrv.TrustedForwarders` вместо имени, которого в дереве нет (мёртвое имя не воспроизводится ни здесь, ни там — оно читалось бы как координата); комментарий поля, объявлявший **инверсию** («нулевое значение = не доверять никому»), заменён на действующую семантику + `IsNarrowed()` как единственный предикат. Это же закрывает незакрывающее замечание XC-6 (§0/Э1 ссылалась в место, противоречившее второй половине той же ссылки) | §4.1 · §4.4 | `pkg/grpcsrv/trusted_forwarders.go` — тип, метод `IsNarrowed()`, раздел шапки «Семантика транспорта этим типом НЕ меняется» |
| **Р5** | назвать опт-ин строкой §9, понизить «by construction» | **закрыто.** §9 получил пункт 12 (опт-ин на несужение вне боевого режима); §5 п.8а и в-6 понижены с «by construction» до «правилом, и вот каким», с называнием того, чем правило держится и где кончается | §9 п.12 · §5 п.8а · §13 в-6 | `pkg/grpcsrv/trusted_forwarders.go`, `ForwarderGate.DevTrustAny` + `Require`: вне боевого режима опт-ин снимает отказ, в боевом — не читается |
| **Р6** | свести величину отступлений с источником | **закрыто, и источник за это время ушёл ещё раз.** Было три пары (20/8, 21/7, источник 20/9). Сегодня по источнику — **20 отступлений, 9 находок**; все три места приведены к нему, а движение показано, а не затёрто | §0 врезка · §2.3 · §12 п.1 · §15 критерий 2 | XC-6 §4: `awk '/^## §4 /,/^## §5/' <файл> \| grep -cE '^\\\| *[0-9]+ *\\\|'` → **20**; XC-6 §4а — **9** пунктов «а»…«и», и сам XC-6 §15 п.5 пишет «число выросло с восьми». Зеркально: тот же счётчик строк на §4а даёт **0**, потому что находки там не таблица, а список, — предикат таблицы на списке молчит, и это не «находок нет» |

**Незакрывающие — тоже закрыты, каждое своим способом.** П12 (гейты репо-гигиены) стал **72/68** —
внесён в §2.4, при этом П12 в §2.1 оставлен на базе как **60/56**. Координаты §2.1, ведущие в
`services/vpc/internal/apps/kacho/check/`, на стволе не резолвятся — каталог переехал на общий путь;
это в §2.4, и по этому же поводу правило записи координат §2 получило оговорку про срок годности
пути. **Заявленный конфликт с XC-2 — находка, а не предмет:** конфликта документ **не заявляет
вовсе** (четыре упоминания XC-2, все — раздача владения), а спорная форма вывода **у обеих сторон
одна и та же** и **обе** её пережили — разбор в §11 К-3.

## §R.2 Ревью второго круга (2026-08-10) — `CHANGES REQUESTED`: закрыто шесть, держит один

Круг был **проверкой закрытия**, а не перечиткой: документ целиком заново не перепроверялся, числа,
воспроизведённые первым кругом, второй раз не воспроизводились. Проверялось ровно то, что §R.1
объявляет закрытым, — **исполнением на `92ca6af9d`**, своим предикатом, с контролем в обе стороны.

**Закрыто по существу — шесть из шести. Чем проверено (мой предикат, не пересказ §R.1):**

| # | Что утверждает §R.1 | Чем воспроизведено | Исход |
|---|---|---|---|
| Р1 | §2.4 — дельта с обеими ревизиями; §8.1 — колонка состояния; §14 — уплачено/предстоит | `git ls-tree -r --name-only <rev> -- <pkg> \| wc -l` на обеих ревизиях: `pkg/servicecontract` **0/0**, `pkg/servicehost` **0/0**, `pkg/peer` **0/0**, `pkg/kacho` **0/0**; зеркало `pkg/listnarrow` **0 → 9**; `pkg/authz/catalogparity` **3 → 0**; `internal/repohygiene` **60/56 → 72/68**; `git grep -ln "authz\.RPCMap"` + `wc -l` по каждому файлу: **7 деклараций на обеих**, строк **2235 → 276** | ✔ числа §2.1 **не переписаны**, дельта отдельной таблицей — измерение сохранено |
| Р2 | третья сторона (вывод из дескрипторов, слинкованных в бинарь) внесена как принятая | `git ls-tree -r --name-only <rev> \| grep permission_catalog.json` → **2/2**; шапка `pkg/authz/catalogderive/derive.go` §«Why the annotations and not the generated JSON» называет третью копию причиной отказа от чтения JSON | ✔ предписание «2 → 3» снято, регресс не заказывается |
| Р3 | решение приёма выражено **тройкой** | `pkg/authz/proxytuple/policy.go` — `ValidateTuple(callerDomain, subject, relation, object)`; гейт `internal/repohygiene/proxytupleintent_test.go` несёт `TestProxyRegistrationTriplesAreAcceptedByOwner` **и** `TestEveryAcceptedRelationHasAnEmitter` | ✔ приёмка больше не предписывает снятую проверку членства одного отношения |
| Р4 | §4.1 называет landed-тип и landed-предикат | `pkg/grpcsrv/trusted_forwarders.go:100` — `func (f TrustedForwarders) IsNarrowed() bool`; §4.1 несёт `grpcsrv.TrustedForwarders` + действующую (не инвертированную) семантику нуля | ✔ второе имя для существующего типа не заводится |
| Р5 | опт-ин назван строкой §9, «by construction» понижено | `pkg/grpcsrv/trusted_forwarders.go:140` — `DevTrustAny`; `:168` — `if !g.Production && g.DevTrustAny` (в боевом режиме **не читается**); §9 п.12 заведён, §5 п.8а и в-6 несут «правилом, и вот каким» | ✔ поверхность названа там, где по критерию 7 обязана быть названа |
| Р6 | все три места приведены к источнику | XC-6 §4: `awk '/^## §4 /,/^## §5/' <файл> \| grep -cE '^\\\| *[0-9]+ *\\\|'` → **20**; §4а: `grep -cE '^[а-я]\. \*\*'` → **9**; в XC-7 величина стоит в §0, §2.3, §12 п.1 — **везде 20/9** | ✔ и сверх того: контроль предиката прогонялся в обе стороны, см. врезку ниже |

> [!note] Контроль предиката: моя первая зеркальная проба была **вакуумной**, и это стоит записать
> Зеркало к счётчику строк я взял как `awk '/^## §4а/,/^## §5/'` и получил **0** — то есть ровно
> тот ответ, которого ждал. Ноль был ложным: заголовок находок — `### §4а`, а не `## §4а`, поэтому
> диапазон **не начинался вовсе**. Верное зеркало — счёт по строкам самой секции (1031-1143): там
> **0** нумерованных строк таблицы и **9** пунктов «а»…«и», а тот же буквенный предикат на §4
> даёт **0**. Оба направления сошлись только со второй попытки. Класс: зеркальная проба,
> отвечающая «ноль» по причине, не имеющей отношения к предмету, — она подтверждает вывод тем
> увереннее, чем меньше на самом деле измерила.

**Живость проверена по обеим сторонам.** Со стороны измерений — семь величин §2.4 выше. Со стороны
плана — гейты, объявленные §16 как **существующие сегодня**, резолвятся все: `panicrecoverywiring_test.go`
(+ парная `_injection_`), `trustedforwarderscircle_test.go` (`TestEveryServiceDeclaringTheCircleRefusesToStartUnnarrowed`),
`listnarrowersingleton_test.go`, `listpaginationorder_test.go`, `hideexistence_test.go`,
`proxytupleintent_test.go`. Ни одна координата §16 не ведёт в пустоту.

**Владение осью личности (L1) сохранено — проверено адресно, потому что на него ссылается XC-6.**
Все четыре несущих элемента у XC-7 на месте и резолвятся: **тип** — §4.1 (`grpcsrv.TrustedForwarders`),
**держатель** — §4.6 О1 (отказ старта по тому же предикату, что читает транспорт), **сценарий** —
§6 A5, **решение** — §13 в-6 с понижением обоснования и сохранённой отвергнутой стороной. Ссылка
XC-6 §Э1 больше не ведёт в место, где сказано обратное. Остаточное, **не блокирующее и не моё**:
XC-6 адресует семантику нуля в §4.2, а после правки Р4 она живёт в §4.1 — координата соседа
устарела на пол-раздела, правится у него.

**Держит документ один предмет.**

**Р7. Разбивка исполненного по фазам названа тремя способами, и ни один не выводится из таблицы §8.1.**
Это не формулировка: разбивка — **несущий результат** Р1 и прямой ответ на вопрос «сколько осталось».
- **шапка:** «из восьми фаз три исполнены в стволе целиком, одна на 6/7, четыре не начаты»;
- **§8.1 итог:** «две исполнены целиком (Ф0т, Ф4 **в части** единственной реализации), одна на 6 из 7,
  **пять** не начаты (Ф1, Ф3, Ф5, Ф6, Ф7, Ф8 — **шесть строк**)»;
- **таблица §8.1** (единственное место с предикатом — колонка «Полна, когда»): в охвате XC-7 девять
  фаз (Ф0 — вне охвата); **целиком исполнена одна** — Ф0т; **частичны две** — Ф2 (✗ iam) и Ф4
  (✗ «проводка остаётся значением композиционного корня, а не полем принятого дескриптора»);
  **не начаты шесть** — Ф1, Ф3, Ф5, Ф6, Ф7, Ф8.

Шапка получает «три целиком», засчитывая **все три** клетки, чья ячейка начинается словом
«ИСПОЛНЕНА», — то есть считает **слово**, а не исход, при том что две из трёх несут в той же строке
явный ✗; и тут же считает Ф2 **второй раз** как «одну на 6/7». «Четыре не начаты» занижает остаток
на две фазы. §8.1 пишет «две целиком» и сам себя опровергает оговоркой «в части», а «пять не начаты»
не выводится ни из шести перечисленных строк, ни из четырёх независимых (Ф1, Ф6, Ф7, Ф8).

Почему это блокирует, хотя правка — два предложения: документ **сам** объявляет разбивку дороже
одного числа (§14: «одна колонка была бы честна ровно до 2026-08-06 и с тех пор молча завышала
объём предстоящего»). Здесь та же величина **занижает** остаток — в строке, озаглавленной «Что
осталось и почему», то есть в единственном месте, которое прочтёт планирующий. Утверждённой такая
строка становится утверждением о дереве, у которого нет предиката.

**Правка (механическая, без переоткрытия предметов):** назвать разбивку **одним** способом, выведя
её из колонки «Полна, когда» — «целиком одна · частичны две (какие и чем) · не начаты шесть», — и
привести к ней шапку и §8.1. Слово «ИСПОЛНЕНА» в ячейках Ф2 и Ф4 либо снабдить долей прямо в
ячейке, либо не засчитывать в счёт целых.

**Не блокирует, учесть при той же правке.** §14.1: «уплачено порядка **5 300** не-тестовых строк
(1959 + 492 + 3166)» — названные слагаемые дают **5617**. Направление ошибки безопасное (занижает
уплаченное, не завышает), слагаемые напечатаны и позволяют читателю пересчитать, поэтому вердикта
это не меняет; но число, не сходящееся со своими же слагаемыми, — та же порода, что Р7.

**Что второй круг НЕ трогал и трогать не будет:** тело §1-§13 и §15-§16, числа §2.1 на их базе,
формулировки §R и §R.1. **Новых предметов сверх Р7 не заводится намеренно.** Основание — не
впечатление о качестве: шесть блокирующих предметов первого круга закрыты **исполнением**, и
проверка закрытия это подтвердила по каждому. Круг, который на такой развязке рождает новый список,
измеряет не документ, а себя; задача этого — довести схождение до вердикта, а не расширить перечень.

## §R.3 Ревью третьего круга (2026-08-10) — ✅ `APPROVED`

Круг был **проверкой закрытия Р7**, а не перечиткой: тело §1-§16 и числа §2.1 не перепроверялись,
новых предметов не заводилось. Проверялось ровно то, что правка объявляет закрытым, — **своим
предикатом по таблице**, а не чтением текста.

**Р7 закрыт. Чем проверено.** Прогнан обход строк §8.1 по колонке «Полна, когда» с механическим
подсчётом отметок (`awk` по разделителям таблицы, счёт ✔ и ✗ в четвёртой колонке каждой строки):

| Исход по предикату | Фазы | Сколько |
|---|---|---:|
| только ✔, ни одного ✗ | Ф0т | **1** |
| смешанные ✔ и ✗ | Ф2 (✗ iam), Ф4 (✗ проводка — значение композиционного корня) | **2** |
| отметок нет | Ф1, Ф3, Ф5, Ф6, Ф7, Ф8 | **6** |

1 + 2 + 6 = **9** — ровно охват (Ф0 своей же клеткой объявлен вне). Шапка и итог §8.1 дают это же
число теми же именами фаз; **третьего счёта в документе нет**: §14 фазы не считает, а его строки
«предстоит» соответствуют остаткам один-к-одному (клетка iam → Ф2, четыре проводки → Ф4,
`pkg/peer` → Ф6, восемь файлов → Ф8). Прежние три разбивки живут только в §R.2 и во врезке §8.1 —
в прошедшем времени, как разбор ошибки, а не как утверждение о дереве.

**Остаток не завышен — проверено отдельно, потому что правка шла в эту сторону.** Ключевые «не
начаты» перемерены на `92ca6af9d`: `pkg/servicecontract` **0**, `pkg/servicehost` **0**, `pkg/peer`
**0**, `pkg/kacho` **0**; зеркально `pkg/listnarrow` — **9**, то есть предикат умеет отвечать «есть».
Клетка Ф2 подтверждена адресно: у iam карты нет вовсе (единственное касание `catalogderive` в iam —
константа освобождения в посеве прав, не вывод своей карты).

**Не блокирует — три, все в сторону уточнения, ни одно не меняет решение планирующего.**
1. **Ф6 размечена грубее соседей.** Её landed-половина (закрытый тип полосы, `96055cbd8`; §14.2 —
   **13** приведённых точек) названа **прозой без ✔**, поэтому предикат отметок относит фазу к «не
   начата», тогда как Ф2 и Ф4 с такой же долей размечены смешанно. Направление безопасное — остаток
   **завышается**, а не занижается, и строка сама называет свою половину там, где её прочтут. При
   следующем касании: либо дать Ф6 ту же разметку, либо сказать в итоге, что счёт идёт по **предмету**
   фазы, а не по её условию полноты.
2. **§14.1 «порядка 5 300» против собственных слагаемых** (1959 + 492 + 3166 = **5617**) — назван
   ещё вторым кругом и не поправлен. Занижает **уплаченное**, слагаемые напечатаны, остатка не
   касается.
3. **Врезка §8.1 забирает в себя закрывающий абзац.** После «Предикат теперь один и назван» строки
   продолжаются без `>`, и по ленивому продолжению абзац о дискриминаторе («предсказал, что именно
   уедет вперёд» — сильнейшее свидетельство §8.1) читается **внутри** врезки о прошлой ошибке.

**Соседу, а не сюда: координата XC-6 не резолвится — и не резолвилась никогда.** Второй круг
записал это как «устарела на пол-раздела»; перемер точнее. §4.2 XC-7 — «Носитель входящего пути» на
**всех четырёх** ревизиях файла (`6757594`, `deb176e`, `19bedd1`, `7ea9240`), включая ту, что
предшествует правке Р4. То есть сужение XC-6 «за семантикой нулевого значения ходить в §4.2 и О1,
**НЕ** в §4.1» увело читателя **от единственного места, где предмет лежал**, а не к нему. Сегодня
§4.1 несёт и landed-тип (`grpcsrv.TrustedForwarders`), и действующую семантику с `IsNarrowed()`, а
§4.6 О1 — держателя. У XC-6 **15** вхождений «§4.2»; несущих — семь (строки 311, 351, 844, 848, 850,
1249, 1353), и они **между собой не сходятся**: четыре места зовут §4.1 «домом типа», а 1249 и 1353
называют типом §4.2. Отдельно: XC-6 строка 55 воспроизводит **мёртвое имя типа** в обратных кавычках —
ровно то, чего врезка §4.1 велит не делать. XC-6 **APPROVED**, предмет чужой: правится отдельным
касанием у своего владельца. На вердикт XC-7 не влияет — здесь предмет лежит в одном месте и назван
верно.

**Почему APPROVED, а не четвёртый круг.** Единственный держащий предмет закрыт **тем способом,
которым требовалось** — не переписью формулировок, а сведением к одному предикату, выводимому из
таблицы. Три оставшихся замечания — уточнения разметки и арифметики уплаченного; ни одно не меняет
того, что прочтёт планирующий: **одна фаза целиком, две частичны, шесть не начаты, и носителя —
предмета, ради которого документ писался, — в дереве нет**. Круг, который на такой развязке держит
документ в DRAFT, измеряет себя, а не документ.

**Что APPROVED покрывает и что нет.** Покрывает **контракт** XC-7: предмет, формы §4, порядок фаз
§8.1, отказы старта, сценарии §6, критерии §15. **Не покрывает** — и это названо прямо: три
незакрывающих пункта выше, координату соседа (чужой файл) и конструируемость того, чего в дереве
нет: `pkg/servicecontract`, `pkg/servicehost`, `pkg/peer`, `pkg/kacho` — **0** файлов, поэтому Ф1
обязана начаться с посева формы дескриптора и доказать, что все десять отказов старта **строятся**,
прежде чем в них поедут шаги XC-6.

**Следующий шаг:** `class-exposure-analyst` по Ф1 (до планирования и до первой строки кода), затем
`superpowers:writing-plans`.

---

## §0 Предмет — и шов с XC-6, который ревьюер обязан проверить ПЕРВЫМ

**Предмет XC-7 — НОСИТЕЛЬ, а не канон.** XC-6 отвечает на вопрос «что обязано быть одинаковым и какая клетка эталон». XC-7 отвечает на другой вопрос: **какая конструкция делает разночтение невозможным**, то есть на чём канон держится, когда его никто не помнит.

| Вопрос | Владелец | Где |
|---|---|---|
| Что обязано быть одинаковым по семи осям; какая клетка эталон; какие отступления **намеренны** (**20**) и какие места — находки (**9**) | **XC-6** | `sub-phase-XC-6-iam-touchpoint-canon-acceptance.md` §2-§4 |
| Какой **класс** механизма держит каждую ось (тип · генерация · сверка · конформанс) | **XC-6 §5.0-5.1** | там же |
| Какой **артефакт** является этим механизмом, где он живёт, какова его форма, в каком порядке звенья, и как на него переезжают семеро | **XC-7 (этот документ)** | §3-§8 |
| Машинный признак полосы отказа как контракт | XC-1 | `sub-phase-XC-1-error-reason-token-acceptance.md` |
| Библиотека интеграции authz, очередь, материализация | XC-2 | `sub-phase-XC-2-unified-authz-integration-library-acceptance.md` |
| Набор глаголов принадлежит типу | XC-3 | `sub-phase-XC-3-verb-set-belongs-to-type-acceptance.md` |

**Правило, которым шов держится (и по которому его проверять).** XC-7 **не повторяет ни одного канона К1…К7**. Там, где документ должен был бы сказать «обязано быть так», он **называет К** и трактует его как **значение, въезжающее в поле дескриптора**. Отсюда проверяемый критерий проектирования, он же противоядие классу «унификация читает намеренную узость как общий случай»:

> **Каждое из 20 намеренных отступлений XC-6 §4 обязано выражаться ЗНАЧЕНИЕМ поля дескриптора, а не ветвлением внутри контура. Отступление, которое нельзя выразить значением, означает, что неверен ДЕСКРИПТОР, а не сервис.**

Примеры применения этого правила: geo с нулём кортежей — это `Emits: NotApplicable("глобальный каталог оси размещения, пообъектных грантов нет by construction")`, а не `if service == "geo"`; registry с четырьмя отношениями — четыре элемента типизированного набора; iam с партией 50 — значение поля; асимметричное правило полос — свойство сверки, а не исключение в ней.

> [!note] Величина отступлений: 21/7 → 20/8 → **20/9**, и это третий пересчёт, а не третья опечатка
> Здесь последовательно стояли **21/7** и **20/8**. Оба были верны в свой день и оба **пережили
> источник**: сегодня XC-6 §4 несёт **20** строк, а §4а — **9** пунктов «а»…«и». Движение второй
> половины разобрано в самом источнике: восьмую находку произвёл **перевод** пункта из §4 в §4а
> (отступление защитимо, но адреса записи по названному месту нет), девятую — **закрытие**
> восьмой, потому что запись, обязанная заполнить строку по каждому соседу, назвала клетку,
> которую перепись по своей оси не видела (XC-6 §15 п.5: «число выросло с восьми»).
>
> **Предикат, которым это сверяется, и его зеркало.** Отступления — строки таблицы §4:
> `awk '/^## §4 /,/^## §5/' <файл> | grep -cE '^\| *[0-9]+ *\|'` → **20**. Находки — **не** таблица,
> а список пунктов буквой, поэтому тот же счётчик строк даёт на §4а **ноль**, и «ноль» здесь
> означает «предикат не про этот вид предмета», а не «находок нет»: их считает перечисление
> «а»…«и» и утверждение §15 п.5 самого источника. Ровно этим и опасно перенесённое число — у него
> нет предиката, поэтому расходится оно **молча**.
>
> Отдельно и без изменений: пункт 18 §4 после решения владельца (XC-6 §9.5) потерял **половину**
> предмета, но строку сохранил, поэтому 20 остаётся 20 — счёт отступлений меняется только когда
> строка теряет предмет **целиком**.

**Место, где XC-7 предлагал ИЗМЕНИТЬ XC-6, названо здесь, а не спрятано в теле, — и владелец решил его иначе, чем предлагали обе стороны.** Предметом был второй источник для iam: XC-6 планировал шагом 4 описательную карту прав, XC-7 предлагал вместо неё **объявление `Lists` в дескрипторе**. **Решение 2026-08-09 (§13 в-5): ни то, ни другое — карта ВЫВОДИТСЯ из каталога у всех семи**, второго источника не заводится ни у кого, а у шестерых он снимается. Обе отвергнутые стороны сохранены в §11 К-5 и §13 в-5: решение, из которого вычеркнули отвергнутое, через месяц читается как единственно возможное. Следствия для конструкции — §4.1 (поля `Lists` в дескрипторе нет), §4.5 (вывод у всех семи), §8.1 (Ф2).

---

## §1 Цель словами владельца и разбор аналогии с кубом

### 1.1 Цель — дословно

> «сделать так, чтобы был единый флоу работы с iam для сервисов и пользователей. Что-то подобное как k8s handler pipeline, где каждый запрос проходит определённый флоу и вне зависимости от бэкенда имеет целостность. Так как это не куб и вообще распределённый бэкенд, где у каждого свой бинарь с API, то как в кубе единый флоу программный не сделать, но хочется приблизиться к единому флоу или SDK или ещё чему-либо, где фактор ошибки будет минимален при создании нового сервиса или доработке старого, чтобы не было разночтений в реализации и не было десятков реализаций».

Три требования читаются раздельно и держатся разными конструкциями: **единый флоу входящего запроса** (§3 C1-C4), **SDK для пользователей** (§3 C6), **минимальный фактор ошибки при заведении нового и доработке старого** (§7).

### 1.2 Что в кубе даёт целостность — по существу, а не по впечатлению

**Цепочку строит одна функция** — `DefaultBuildHandlerChain(apiHandler, c *Config) http.Handler`. Она написана «изнутри наружу», поэтому последняя обёртка — самая внешняя. Позиции, которые несут смысл (а не косметику):

| Позиция | Звено | Почему именно здесь |
|---|---|---|
| внешняя | инициализация аудита | контекст исхода обязан существовать **раньше**, чем что-либо может упасть, иначе паника неаудируема |
| ↓ | восстановление после паники | ловит панику **любого** звена ниже, включая сами фильтры |
| ↓ | «маршруты смонтированы» | отличает «маршрута нет» от «маршруты ещё не смонтированы» — иначе гонка старта читается как 404 |
| ↓ | отметка приёма и верхняя граница обработки | точка отсчёта всех бюджетов; принудительный обрыв всего, что не объявлено долгоживущим (**данные, а не код**) |
| ↓ | **разбор пути в `RequestInfo`** | глагол/группа/ресурс/пространство/имя резолвятся **ОДИН РАЗ на запрос** |
| ↓ | аутентификация | провал уходит в отдельный обработчик со **своим** аудитом — основной аудит стоит ниже и до него неаутентифицированный не доходит |
| ↓ | аудит | ниже аутентификации: событие обязано нести **установленного** пользователя |
| ↓ | олицетворение | само спрашивает того же авторизатора «вправе ли X говорить за Y» и подменяет пользователя, так что авторизация ниже видит уже подменённого |
| ↓ | приоритет и справедливость (ограничитель одновременности) | классифицирует по **уже установленному** субъекту и стоит **перед** авторизацией — шторм авторизации ограничен by construction |
| внутренняя | авторизация | читает `RequestInfo` из контекста, **сам путь не разбирает** |

Роcтер звеньев между минорными версиями дрейфует; **дрейфует роcтер, не позиционные инварианты** — и это отдельный урок: перечень звеньев ведёт себя как данные, а взаимный порядок — как контракт.

**Механизмов невозможности обойти — ДВА, и это разные механизмы.**

**(а) Мультиплексор находится внутри обёртки.** Регистрирующая сторона получает **мультиплексор**, а не сервер; установить обработчик можно только *в* мультиплексор, а мультиплексор уже завёрнут. Поверхность регистрации и поверхность цепочки — **разные объекты**, и цепочка владеет мультиплексором.

**(б) Валидирующая приёмка передаётся ВНУТРЬ персистентности как колбэк.** Приёмка **не является звеном цепочки**. Обработчик создания вызывает `Create` хранилища, **передавая валидацию параметром**, и хранилище не умеет писать, не позвав её. Автор ресурса не «обязан не забыть позвать валидацию» — он её **вообще не зовёт**.

**(в) Набор глаголов выводится из реализованных интерфейсов.** Способность объявляется **типом**, проводка выводится.

**Честная оговорка, без которой тезис §1.4 не проверяется:** поле конфигурации, хранящее построитель цепочки, — **публичное и перезаписываемое**. Цепочку в кубе **можно** собрать иначе, и производные серверы это делают. Невозможность там не абсолютная, а **высотная**: то, что варьируется (ресурсы), и то, что варьироваться не должно (цепочка), правятся в разных местах, разными ролями, в разное время.

**Делегирование: цепочка исполняется РОВНО ОДИН РАЗ.** Следующему серверу в цепи делегирования передаётся обработчик **без** обёртки. Куб намеренно не прогоняет фильтры дважды. **Мы намеренно прогоняем** (`security.md` §«AuthN+AuthZ ВЕЗДЕ»). Это осознанное расхождение, и именно оно порождает ось паритета XC-6 L2 **по построению**, а не по недосмотру.

### 1.3 Что приносит CRD

**Приносит только данные:** структурная схема, область (пространство/кластер), имена, версии и стратегия конверсии, подресурсы из закрытого набора, печатные колонки, умолчания, декларативные правила валидации, выбираемые поля.

**Не приносит:** ни одного фильтра и ни одной позиции в цепочке; ни режима аутентификации, ни модели авторизации; ни хранилища; ни собственных глаголов; ни своей сериализации; ни своей семантики наблюдения.

**Единственная точка впрыска кода — вебхук**, и **даже она объявляется данными**: позиция ограничена закрытым множеством, контракт, таймаут и политика отказа заданы платформой. Расширение — это **объявленный вызов в фиксированной позиции**, а не сегмент цепочки.

### 1.4 Тезис оркестратора — ПРИНЯТ ПО ВЫВОДУ, отвергнут в трёх формулировках

> Тезис: «в кубе единство даёт НЕ единый бинарь, а то, что цепочку НЕЛЬЗЯ СОБРАТЬ ИНАЧЕ; kube-apiserver строит handler chain одной функцией, ресурс не может зарегистрироваться в обход; CRD приносит СХЕМУ, а не свой pipeline. Отсюда: цель достижима на N бинарях, если цепочка станет НЕСОБИРАЕМОЙ РУКАМИ, а сервис будет приносить только ДАННЫЕ».

**Вывод принят: цель достижима на N бинарях.** Формулировка неточна в четырёх местах, и каждая неточность уводит проект к неверному артефакту.

**Неточность 1 — «нельзя собрать иначе» ложно как факт** (§1.2, оговорка). Верно другое: **до этой высоты не поднимается тот, кто добавляет ресурс.** Инвариант — **разделение высот**, а не невозможность. Разница практическая: «сделать несобираемой» толкает к запечатыванию сборщика, тогда как работающий механизм — **дать варьирующей стороне словарь, на котором чужая цепочка невыразима**.

**Неточность 2 — самая сильная гарантия куба вообще не в цепочке.** Цепочка защищает **мультиплексор**; невозможность пропустить валидацию даёт передача приёмки **параметром в запись**. Тезис этого не видит, а это самый ценный перенос — и он лежит **вне** контура iam (§12, вынесен в XC-8).

**Неточность 3 — свидетельство названо не то.** «Монолит строит цепочку одной функцией» — почти тавтология: процесс один, и это не показывает, что свойство переживает N бинарей. Нагрузку несёт **агрегированный** сервер API: свой бинарь, свой порт, своя цепочка, — и куб **не принуждает** его ничем, он предлагает библиотеку и **делегирует решение о доступе**. То есть механизм, который тезис предлагает заимствовать, куб применяет там, где процесс один, и **не применяет там, где процессов много**, — то есть у нас. Тезис устоял, но по прецеденту в **аналогичной** топологии, а не в процитированной.

**Неточность 4 — поправка к посылке о нашем дереве, и она УСИЛИВАЕТ вывод.** Тезис говорит: «семеро собрали похоже — это свойство авторов». **Измерение говорит: они собрали НЕ похоже.**

| Звено спины | Несут | Не несут | Предикат (база `cf69249ed`) |
|---|---|---|---|
| восстановление после паники в цепочке | **4 из 7** — iam, vpc, storage, geo | **3 из 7** — compute, nlb, registry | `git grep -l "recover()" <rev> -- 'services/*.go' \| grep -v _test`, затем отбор **интерсепторных** носителей; контроль в обе стороны прогнан: тот же предикат находит и не-интерсепторные места (два use-case), значит «ноль» у трёх — это ноль, а не сломанный поиск |
| верхняя граница обработки как звено | **1 из 7** — vpc | 6 из 7 | замер проекта «цепочка», предикат назван там; мной не перемерялся |
| ограничитель одновременности перед авторизацией | **0 из 7** | 7 из 7 | того же замера; `pkg/authz/rate_limiter.go` — про бюджет **отказов**, другой предмет |
| самодельный админ-гейт на внутреннем слушателе | **1 из 7** — compute | 6 из 7 (vpc его **снял как дефект**) | того же замера; **решено 2026-08-09 — снять, отдельной приёмкой** (§13 в-12) |

**Ни одно из четырёх не записано как решение** ни в одном `known-divergences`. Значит рамка «предотвратить будущий дрейф» неверна: **дрейф уже произошёл — и произошёл в звеньях, незаметных ровно до того дня, когда они понадобятся.**

> [!note] Первая строка на стволе больше не верна — и замер базы от этого не портится
> «4 из 7» снято на `cf69249ed` и **на ней остаётся верным**. За 87 коммитов ствол ушёл: звено
> восстановления сведено в **одно** на платформу (`a99750f87`), и интерсепторных носителей у
> сервисов не осталось **ни одного**. Предикат тот же, обе ревизии: `git grep -l "recover()" <rev>
> -- 'services/*.go' | grep -v _test` → база **11** файлов, ствол **7**; ушедшие четыре — именно
> интерсепторные (`services/geo/cmd/kacho-geo/panicrecover.go`, `services/iam/internal/grpcmw/recovery.go`,
> `services/storage/cmd/storage/interceptors.go`, `services/vpc/internal/handler/recovery_interceptor.go`).
> **Зеркальная проверка обязательна и сделана:** оставшиеся семь вхождений — не «недосведённые
> копии», а use-case-уровень (постфиксация коммита, три `api/`-случая), то есть предикат по-прежнему
> находит и не-интерсепторные места, значит его «ноль» по интерсепторам — это ноль, а не сломанный
> поиск.
>
> Что из этого следует для §1.4 и **не** отменяется: тезис «семеро собрали НЕ похоже» опирался на
> **четыре** строки таблицы, и три из них (граница обработки 1 из 7, ограничитель 0 из 7,
> самодельный рубеж 1 из 7) на стволе не изменились — проверено §2.4. Первая строка теперь
> свидетельствует о другом и не менее сильном: расхождение из четырёх было **сведено вручную,
> отдельным изменением, спустя месяцы**, а не предотвращено конструкцией — то есть цена дрейфа
> уплачена, а его источник (каждый корень собирает цепочку сам) остался на месте.

### 1.5 Три корзины переносимости

**(1) Переносимо ПРЯМО.**

| Механизм куба | Что это у нас | Состояние на базе |
|---|---|---|
| цепочка строится одной функцией из конфигурации | `pkg/servicehost` строит срез звеньев по дескриптору | 7 композиционных корней, **14 вызовов** конструктора сервера (§2.1) |
| область резолвится ОДИН РАЗ, все ниже читают одно значение | резолв `(object_type, object_id, relation, полоса)` в контекст | сегодня резолвят **двое** — край по каталогу и сервис по карте (XC-6 L2) |
| паника ловится самым внешним звеном | звено восстановления | 4 из 7, **5 реализаций** одного звена (4 в сервисах + одна на крае) — **исполнено в стволе `a99750f87`: одна реализация, §2.4** |
| верхняя граница обработки как звено | звено срока | 1 из 7 (**на стволе без изменений**) |
| fail-closed на незамапленном методе | «нет записи ⇒ отказ» | **уже 7/7 и одной реализацией** — фиксировать как канон, а не «чинить» |
| валидация передаётся ВНУТРЬ записи колбэком | writer-транзакция принимает `Validate` параметром | предмета нет — вынесено в XC-8 (§12) |

**(2) Переносимо В ИЗМЕНЁННОЙ ФОРМЕ.**

| В кубе | У нас превращается в |
|---|---|
| один бинарь владеет цепочкой | **один ПАКЕТ владеет цепочкой, а композиционный корень не может выразить другую**; механизм — не сокрытие сборщика, а **словарь**: сервис передаёт дескриптор и получает `error` |
| регистрация только в мультиплексор | сервис получает **`grpc.ServiceRegistrar`** (интерфейс с единственным методом), а не сервер; до сборки цепочки дотянуться нечем — **свойство компилятора** |
| цепочка приёмочных плагинов | список **объявленных** валидаторов ресурса, исполняемый общим писателем (XC-8) |
| структурная схема + декларативные правила | proto как схема + **объявленные опциями** форматные проверки, исполняемые общим звеном |
| аудит по стадиям, последняя — «ответ завершён» | то же **плюс обязательная стадия, ключуемая по `Operation.id`** (§1.7) |
| цепочка исполняется ровно один раз | **намеренно инвертировано**: край и сервис оба энфорсят ⇒ обе цепочки обязаны быть **одной функцией с разными данными**, чтобы «дважды» не означало «двумя реализациями» |
| способность объявляется интерфейсом, маршруты выводятся | полоса RPC объявляется **опцией proto**, каталог **генерируется** — у нас уже так на крае (295 строк); изменённая форма: то же объявление обязано порождать и карту сервиса |

**(3) НЕ переносимо — цена названа честно.**

1. **Единый слой хранения с одной моделью согласованности.** У нас БД-на-сервис. **Цена:** единообразие *содержания* пути записи недостижимо; унифицируема **форма** писателя, не его содержание. Общее «звено хранения» было бы образцовой унификацией по самой снисходительной семантике.
2. **Общая судьба отказа.** В кубе недоступность авторизатора означает, что не обслуживается **ничто**. При N бинарях частичный отказ даёт **частично энфорсящуюся платформу**. **Цена:** «платформа либо энфорсит, либо лежит» недостижимо; достижимо «каждый бинарь либо энфорсит, либо отказывает» — это и есть наш fail-closed 7/7. Остаток обязан быть **назван**, а не замазан.
3. **Единый словарь глаголов и грамматика пути.** У нас 295 разнородных прав и суффикс-действия — намеренная конвенция. **Цена эргономическая, не защитная:** вход авторизации навсегда останется **объявлением на RPC**, а не выводом из пути; значит объявление обязано генерироваться, а остаточный риск «новый RPC без опции» закрыт fail-closed (§4.6 О2).
4. **Наблюдение и версия ресурса.** Watch RPC у нас не существует по решению. Не переносимо и не нужно.
5. **Гарантия порядка приёмки внутри процесса.** Наша «приёмка» частью уходит по сети к владельцу, и провалидированный у соседа объект вправе измениться до нашего коммита. **Цена: межсервисный TOCTOU неустраним.** Названо явно, иначе следующий попытается «починить» это подтверждающим барьером на `Operation.done` — ban #9, фантом-ресурс.

### 1.6 Первое место, где куб нам НЕ образец: права во внешнем хранилище

В кубе решение о доступе принимает **внутрипроцессный** авторизатор, обслуживаемый из кэша. Отсюда три свойства, которых у нас нет: решение занимает микросекунды; оно eventually-consistent **by construction** и никто этого не настраивает; состояния «авторизатор недоступен» **не существует**.

Что меняется у нас:

1. **Порядок меняется не там, где кажется.** У куба авторизация стоит поздно, потому что дёшева. У нас это сетевой вызов с бюджетом, поэтому: (i) она обязана стоять **после** личности и **после резолва области** — ей нужны оба; (ii) ограничитель одновременности обязан стоять **перед** ней — у куба это защита себя, у нас это защита **чужой** доступности; (iii) следовательно потолок выводится из ёмкости **модели**, а не из нашей.
2. **«Недоступно» — первоклассный вердикт с решением** (`DecisionUnavailable → PermissionDenied`, 7/7 одной реализацией). Ближайший аналог кубовой «неготовности» — **готовность процесса, выведенная из достижимости ребра модели**, плюс отказ старта при невзведённой ручке ребра.
3. **Окно кэша = окно отзыва, и оно наше, а не библиотечное** ⇒ обязано быть **объявлено** (XC-6 К3 п.3). **Исполнено в стволе `9bcf28b3b`**: окно стало ручкой у каждого держателя кэша, а не умолчанием библиотеки, которое никто не выбирал. Для контура это меняет роль требования — с «завести» на «унаследовать»: дескриптор несёт величину полем без умолчания (§4.1 `CacheWindow`), и переоткрыть её собственным умолчанием носитель не вправе, иначе вернёт ровно то состояние, из которого её только что вынули.
4. **Второй спрашивающий обязан задать ТОТ ЖЕ вопрос.** Паритет «каталог ↔ карта» — не гигиена, а **единственное, что придаёт смысл двойному вопросу**. Отсюда же следует, почему клетка iam без второго источника — не мелочь. **Два решения владельца от 2026-08-09 стоят здесь вместе и не случайно:** второй энфорсер **остаётся** (в-3 — внутренний периметр не доверенный), а проблема паритета снимается **не отказом от второй проверки, а единственностью источника** (в-5 — карта выводится из каталога). То есть двойной вопрос сохраняется, а «двумя реализациями» перестаёт быть его синонимом.

### 1.7 Второе место: мутации асинхронны

**Граница проходит по `w.Commit()`.** Синхронная часть — личность → область → формат → право → приёмка/peer-validate → запись в транзакции → создание `Operation` — отображается на кубовый запрос один в один. После ответа клиенту остаётся: материализация owner-кортежа, асинхронный исполнитель, компенсация при частичном отказе.

Три следствия, которых у куба нет:

1. **Под чьей личностью идёт хвост.** Куб отвечает «под своей». Наши правила отвечают **иначе и намеренно**: асинхронное продолжение несёт личность **инициатора**, захваченную в момент запроса. Значит захваченная личность — **часть записи операции**, и работа контура не заканчивается вердиктом: он обязан произвести долговременную запись «кто это разрешил», сделанную **в той же транзакции**, что и операция.
2. **Аудит обязан иметь вторую стадию, ключуемую по `Operation.id`.** У куба «ответ завершён» — конец истории; у нас на этом месте история только начинается.
3. **У вердикта появляется горизонт годности.** Право может быть отозвано между ответом и действием исполнителя. Прежде здесь стояло «оба варианта плохи по-своему и обязаны быть решены, а не унаследованы умолчанием»; **решение принято 2026-08-09** (§13 в-2): **операция авторизована в момент приёмки**, перепроверки в момент эффекта нет. Правила это и подразумевали — теперь оно **записано решением**, а не выведено из умолчания, и потому его можно и оспорить, и сослаться на него. Следствие для контура остаётся прежним и усиливается: захваченная личность инициатора и вердикт — **часть записи операции**, сделанная в **той же** транзакции, иначе «в момент приёмки» нечем доказать.

---

## §2 Как получены числа

**Три происхождения, и они не смешиваются.**

- **(И-XC6)** — из приёмки XC-6 с её предикатом. По директиве не перемеряются.
- **(И-ПРО)** — из шести проектов носителя, сведённых этим документом. Атрибутируются как **их** замер с их предикатом; там, где я перемерял, сказано отдельно.
- **(И-ПЕР)** — **перемерено мной** на базе `cf69249ed`; предикат назван при числе.

**Правило записи координат — то же, что в XC-6 §1.2** (исполнение `security.md` §«Публичные артефакты»): координата `файл:строка` ставится на **канонической** реализации; на пробеле называются **сервис, число мест и требуемое свойство** — без адреса и без описания следствия для постороннего. Три места из-под правила выведены нормативкой: комментарий у гейта, текст отказа при refuse-to-start, имя и текст падения теста.

### 2.1 Перемеры (И-ПЕР)

| # | Величина | Значение | Предикат на `cf69249ed` |
|---|---|---:|---|
| П1 | Конструкций сервера в сервисах, не-тестовых | **15 вхождений = 14 вызовов + 1 в комментарии** | `git grep -c "grpcsrv\.NewServer(" <rev> -- 'services/*.go' \| grep -v _test` → 8 файлов; у семи по 2 (по слушателю), пятнадцатое — строка **внутри примера в `doc.go`** одного сервиса |
| П2 | Прямых вызовов конструктора upstream-сервера | **2** живых на крае + **1** законный в общем фундаменте + **1 в комментарии** `pkg/authz` | `git grep -c "grpc\.NewServer(" <rev> -- 'services/*.go' 'gateway/*.go' 'pkg/*.go' \| grep -v _test` |
| П3 | Композиционных корней, уже передающих `grpc.ServiceRegistrar` | **2 из 7** (geo, storage) | `git grep -c "grpc.ServiceRegistrar" <rev> -- 'services/*.go' \| grep -v _test` |
| П4 | Пакеты `internal/check`: файлов и не-тестовых строк | **6 пакетов, 3203 строки** (geo 272 · storage 350 · registry 427 · compute 521 · nlb 631 · vpc 1002); у iam — **0** | обход `git ls-tree -r --name-only <rev> -- services/<s>` по `internal/(apps/kacho/)?check/`, `git show <rev>:<f> \| wc -l` |
| П5 | Файлов, объявляющих карту прав | **7 деклараций в 6 сервисах** (у vpc две: карта + отдельный файл scope-filtered) | `git grep -ln "authz\.RPCMap" <rev> -- 'services/*.go' \| grep -v _test` |
| П6 | Сужатель списков четвёрки | **3753 строки в 17 файлах** (vpc 832/3 · compute 890/5 · nlb 980/4 · storage 1051/5) | тот же обход по `internal/authzfilter/` — **совпало с XC-6 §1.3 до строки** |
| П7 | Пакет сверки карты с каталогом | **492** строки (285 без комментариев и пустых; весь пакет с тестами 852) | `git show <rev>:pkg/authz/catalogparity/catalogparity.go \| wc -l` |
| П8 | Копии загрузочного гейта | **3 файла по 45 строк**; **исполняемая часть побайтово равна** (md5 совпал после снятия комментариев), сами файлы различны — комментарии на **разных языках** | `md5sum` полного файла и `grep -v '^\s*//' \| grep -v '^package' \| md5sum` |
| П9 | Файлов-клиентов к соседу-kacho, не-тестовых | **39** (35 в `services/*/internal/clients/`, 4 на крае) | `git grep -l "PRO-Robotech/kacho/pkg/api/kacho/cloud/" <rev> -- ...` |
| П10 | `pkg/` импортирует `services/` | **0** | `git grep -l "PRO-Robotech/kacho/services" <rev> -- 'pkg/*.go'` |
| П11 | Не-gRPC слушателей в сервисах | **8 файлов** | `git grep -l "http\.Server{" <rev> -- 'services/*.go' \| grep -v _test` |
| П12 | Гейты `internal/repohygiene` | **60 файлов, из них 56 `_test.go`** | `git ls-tree -r --name-only <rev> -- internal/repohygiene` |
| П13 | Производителей подменного извлекателя субъекта в проде | **0** (одно вхождение в тесте общего фундамента, одно упоминание в комментарии прод-файла) | `git grep -n "SubjectExtractor:" <rev> -- '*.go'` |
| П14 | Экземпляров файла каталога прав в дереве | **2**, побайтово равные (край + сид iam), держатся гейтом `permission-catalog-check` | `git ls-tree -r --name-only <rev> \| grep permission_catalog.json` |

### 2.2 Расхождения между шестью проектами — оба референта названы, надёжный выбран

| # | Величина | Числа проектов | Разрешение |
|---|---|---|---|
| Р1 | конструкции сервера | **14** («цепочка») против **15** («новый сервис») | Оба верны для разных референтов. **Надёжна единица «вызов»: 14.** Пятнадцатое вхождение — **строка в комментарии-примере**. Это расхождение само по себе довод: **гейт обязан разбирать AST, а не текст** (см. также П2, где вхождение в `pkg/authz` тоже комментарий) |
| Р2 | пакет сверки карты с каталогом | **492** («цепочка») против **315** («типы», «новый сервис») | 492 воспроизводится; 315 **не воспроизводится ни одним из двух предикатов** (ближайшее — 285 без комментариев и пустых). Надёжно **492**, единица — строки не-тестового файла пакета |
| Р3 | объём пакетов `internal/check` | **3203** («цепочка», пакеты целиком) против **465** («новый сервис», только фабрики) | Оба верны, референты разные. Предмет унификации — **пакет**, надёжно **3203**; из них 465 — та часть, что уже сегодня почти дословно повторена шесть раз |
| Р4 | гейты репо-гигиены | **60** («типы», XC-6) против **56** («новый сервис») | 60 файлов, из них 56 `_test.go`. Оба верны; для утверждения «механизм существует, я его не изобретаю» годится любое |
| Р5 | копии загрузочного гейта | «3 копии, **побайтово равные**» («цепочка») | Уточнено: файлы **различны**, равна **исполняемая часть**. Формулировка проекта верна для референта «код без комментариев»; для референта «файл» — нет. Существенно, потому что при сведении удаляется код, а расходятся **комментарии**, и один из них может быть тем самым «утверждением, пережившим свой предмет» |
| Р6 | точки вызова соседа | **155** («клиент/SDK») | Мой узкий предикат даёт **73**; референты различны, число не несущее — **в расчёте цены не используется**. Несущая величина — **39 файлов** (П9), она воспроизвелась |

### 2.3 Числа, перенесённые как есть (И-XC6, не перемеряются)

295 строк каталога (224 edge-checked · 11 scope-filtered · 60 exempt) · 205 ключей в 6 картах, у iam карты нет · 3753 строки сужателя у четвёрки, 536 у iam, 436 у registry, в `pkg/` ни строки · 4 точки эмиссии машинного признака полосы, эмитируется 1 токен из 5, одним сервисом · закрытый набор приёмной стороны — 4 отношения, живёт под `internal/` владельца · круги отправителей 8/4/2/1/1/1/1, состав намеренно разный · маркер версии у 5 регистраторов происходит **пятью** способами, форма подписи различается **пятью** способами ⇒ общий порт **сегодня невыразим** · **20** намеренных отступлений, **9** находок (движение 21/7 → 20/8 → 20/9 и предикат — врезка §0; здесь число **сверено с источником**, а не перенесено).

> **Все числа этого перечня, кроме последнего, относятся к базе `cf69249ed`** и не перемеряются по
> директиве. **Четыре из них с тех пор изменились** — сужатель четвёрки, набор приёмной стороны и
> его дом, эмиссия токенов полосы, и там, где перечень говорит «в `pkg/` ни строки», теперь есть
> `pkg/listnarrow`. Дельта — §2.4, с обеими ревизиями. Последнее число (20/9) стоит здесь **не**
> как И-XC6-перенос: величина живёт в источнике, который правится независимо, поэтому она сверяется
> при каждом касании (§15 критерий 2), а не наследуется вместе с базой.

### 2.4 Что изменилось в стволе после базы — дельта, а не правка чисел выше

**Таблица §2.1 не переписана и переписана не будет.** Числа сняты на `cf69249ed` и ценны привязкой
к ней: подделанные под сегодняшнее дерево, они перестают быть измерением, а `git show <rev>:<path>`
перестаёт быть способом их проверить. Всё, что стало иначе, живёт **здесь**, с обеими ревизиями.

**Ревизия ствола — `92ca6af9d`**, `git rev-list --count cf69249ed..92ca6af9d` → **87**. Каждая
строка ниже перемерена **на обеих** ревизиях одним и тем же предикатом; там, где предикат мог дать
ложный ноль, назван зеркальный результат.

| Величина | На базе `cf69249ed` | На стволе `92ca6af9d` | Предикат (одинаков для обеих колонок) | Чем сделано |
|---|---:|---:|---|---|
| карта прав: рукописных объявлений | **7 в 6 сервисах, 2235 строк** | **7 в 6 сервисах, 276 строк**; из них шесть карт по **40** строк — не карта, а перечень proto-пакетов + вызов вывода | `git grep -ln "authz\.RPCMap" <rev> -- 'services/*.go' \| grep -v _test`, затем `git show <rev>:<f> \| wc -l`. **Число деклараций не изменилось — изменилось их содержание**, и предикат по имени типа этого бы не показал; поэтому строки считаны отдельно | `ca3354d94` → `569bea84c` |
| пакет сверки карты с каталогом (**П7**) | **492** строки; координата резолвится | **пакета нет**; координата П7 на стволе **не резолвится** | `git ls-tree -r --name-only <rev> -- pkg/authz/catalogparity` → база 3 файла, ствол **пусто** | там же |
| приём «восстановить данные, исполнив замыкание» | внутри пакета сверки, 492 строки | **жив**, 100 строк, переехал в `pkg/authz/catalogderive/scope.go` | `git grep -n "ObjectExtractor func" <rev> -- 'pkg/authz/*.go'` → на **обеих** ревизиях `authz.ObjectExtractor` остаётся **функцией** | приём не снят, а перенесён — см. §4.4 |
| сужатель списков у четвёрки (**П6**) | **3753 строки в 17 файлах** | **587 в 12**; общая реализация — `pkg/listnarrow`, **9** файлов | обход `internal/authzfilter/` по `git ls-tree`, не-тестовые, `wc -l`. Зеркально: `pkg/listnarrow` на базе — **0** файлов, значит предикат «есть общая реализация» умеет отвечать «нет» | `924691519`, `fd0c22cfe`, `c4d733248` |
| сужатель iam / registry | 536 / 436 | **536 / 0 по этому предикату** — у registry сужение живёт вне каталога `internal/authzfilter/` | тот же обход. **Ограничение названо:** предикат читает один каталог, поэтому «0» у registry означает «не здесь», а не «нет» — И-XC6 §2.3 меряет его своим предикатом и даёт 436 | — |
| звено восстановления паники | 4 из 7, **5 реализаций** | **одна на платформу**, интерсепторных копий у сервисов **0** | `git grep -l "recover()" <rev> -- 'services/*.go' \| grep -v _test` → 11 / 7; зеркало — врезка §1.4 | `a99750f87` |
| круг отправителей | сырой срез строк, предикат непустоты у каждого свой | **тип** `grpcsrv.TrustedForwarders` + **один** предикат `IsNarrowed()`; страж — один на семь, в `Config.Validate` | `git show <rev>:pkg/grpcsrv/trusted_forwarders.go` → на базе файла нет | `7f03e0bae`, `5573aa87f` |
| закрытый набор приёма | 4 отношения под `internal/` владельца, недостижимы языком | **`pkg/authz/proxytuple`**, доступен обеим сторонам; решение — **тройка**, не отношение | `git ls-tree -r --name-only <rev> -- pkg/authz/proxytuple` → база пусто, ствол 4 файла | `91850e208`, `49851acb7` |
| машинный признак полосы | 4 точки эмиссии, 1 токен из 5 | **закрытый тип** `errors.Reason` в `pkg/errors`, словарь из пяти закрыт компилятором | `git show <rev>:pkg/errors/reason.go` → на базе файла нет | `96055cbd8` |
| окно отзыва вердикта | 3 из 6 берут умолчание библиотеки неявно | **ручка у каждого держателя кэша** | `git show --stat 9bcf28b3b` — конфиги трёх сервисов + `pkg/authz/revocation_policy.go` + гейт | `9bcf28b3b` |
| гейты `internal/repohygiene` (**П12**) | **60 файлов, 56 `_test.go`** | **72 файла, 68 `_test.go`** | `git ls-tree -r --name-only <rev> -- internal/repohygiene`, счёт по имени | волна 08-08…08-10 |
| каталог `check` у vpc | `services/vpc/internal/apps/kacho/check/` | **`services/vpc/internal/check/`** — прежний путь на стволе **не резолвится** | `git ls-tree -r --name-only <rev> -- services/vpc \| grep -E 'internal/(apps/kacho/)?check/'` → 17 файлов на обеих, **разный путь** | `8d07b6dd2` |

**Что НЕ изменилось — и это тоже измерение, а не умолчание.** Одним и тем же предикатом на обеих
ревизиях: конструкций сервера **15 = 14 вызовов + 1 в комментарии** (П1) · корней, передающих
`grpc.ServiceRegistrar`, — **2** (П3, geo и storage) · копий загрузочного гейта — **3** (П8) ·
не-gRPC слушателей — **8** (П11) · производителей подменного извлекателя субъекта в проде — **0**
(П13) · экземпляров файла каталога — **2** (П14) · `pkg/` → `services/` — **0** (П10). Ни один из
четырёх пакетов, которые предлагает этот документ (`pkg/servicecontract`, `pkg/servicehost`,
`pkg/peer`, `pkg/kacho`), в дереве **не существует** — на обеих ревизиях по **0** файлов.

> [!important] Срок годности у координаты короче, чем у числа
> Правило записи координат §2 говорило, куда координату **ставить**, и молчало о том, что она
> **портится**. За 87 коммитов из четырнадцати перемеров координату потеряли два: П7 — вместе с
> пакетом, П4 — вместе с переездом каталога, при том что **числа обоих остались верны на своей
> ревизии**. Отсюда добавление к правилу, стоящее дороже самих двух случаев: координата в этом
> документе читается **вместе с ревизией, на которой снята**, и проверяется формой
> `git show <rev>:<path>`, а не открытием файла в рабочем дереве. Проверка чтением файла
> приписала бы замер `HEAD`, объявив его на базе, — это тот самый класс, ради которого §2 и
> завёл форму `git grep <rev>`.

### 2.5 Закреплённые утверждения — числа, которые меряет гейт, а не читатель

**Зачем таблица заведена.** §2.1 и §2.4 честны и проверяемы — но проверяет их
**человек**, повторяя предикат руками. За одну ночь 2026-08-11 в этом документе так
устарели шесть утверждений сразу, и ни одно не было ошибкой на момент письма: их
пережил предмет. Число о дереве нечем удержать, пока его не меряет проверка.

Строки ниже **закреплены**: `scripts/docs-gate/check-03-acceptance-tree-claims.py`
читает число **из этой таблицы** и меряет его сам — предикатом, зарегистрированным под
тем же идентификатором, **на той ревизии, которую называет строка**. Расхождение —
находка с обоими числами. Число хранится **здесь**, а не в гейте: две копии числа
разошлись бы молча, и гейт стерёг бы собственную.

**Гейт не краснеет от движения дерева.** Замер привязан к ревизии, поэтому верен на ней
навсегда; краснеет он ровно в двух случаях — утверждение неверно на своей ревизии либо
документ перестал его нести. Самоистечение в обе стороны: идентификатор без предиката —
находка, предикат без строки — тоже.

**Ревизия закрепления обязана быть ДОСТИЖИМА ИЗ `main` продукта — иначе закрепить не
удалось.** Первая редакция этой таблицы пиннила двенадцать строк на `4d26e3300`: это
была вершина **локального** дерева в момент письма, и `git rev-parse HEAD` показывал
именно её. На `main` продукта такого коммита нет — он живёт только вершиной открытой
ветки, и `git merge-base --is-ancestor 4d26e3300 origin/main` отвечает «нет». Следствие
измерено, а не предположено: прогон проверки на клоне `main` даёт **12 находок из 16**
(«ревизия не резолвится»), а вместе с веткой, которую по здешней дисциплине удаляют
после слияния, исчезла бы и последняя возможность её достать. Закрепление, чью
координату читатель не может резолвить, ничего не закрепляет: повторить замер нечем ни
человеку, ни гейту.

Поэтому двенадцать строк перепиннены на `ccc6918ef` — вершину `main` продукта, предка
всякого будущего `main`. **Числа при этом не изменились ни в одной из двенадцати строк**
(перемерено теми же предикатами), поэтому всё, что документ говорит о `4d26e3300` в
прозе, остаётся верным: перепиннена координата, а не результат. Три исторические
ревизии — `563f92852`, `5cad2f9e0`, `cf69249ed` — проверены тем же вопросом и предками
`main` **являются**, поэтому оставлены как были.

Отсюда же требование к среде: дерево продукта выкладывается job'у гейта **полной
историей** (`fetch-depth: 0`). Выкладка по умолчанию несёт один коммит вершины, и на ней
не резолвится **ни одна** из четырёх ревизий — это тоже прогнано, а не выведено.

**Координаты `файл:строка` в клетках §8.1 читаются со СВОЕЙ ревизией** — это правило §2.4, и
перемер 2026-08-12 его подтвердил: три координаты цепочки, названные при замере `5cad2f9e0`, и
координата поля сужателя, названная при `5d3edd305`, на своих ревизиях **точны все четыре**, а на
`4d26e3300` две из них сместились вместе с текстом файла. Открывать их в рабочем дереве нельзя —
только формой `git show <rev>:<path>`.

| # | Величина | Ревизия | Число | Предикат |
|---|---|---|---:|---|
| **Д1** | сервисов, поднимающих gRPC-контур носителем | `ccc6918ef` | **6** | вызов `servicehost.Serve(` в прод-коде `services/`, счёт по каталогу сервиса |
| **Д2** | то же на ревизии замыкания контура | `563f92852` | **6** | тот же |
| **Д3** | сервисов, поднимающих не-gRPC поверхность профилем носителя | `ccc6918ef` | **7** | вызов `servicehost.ServeSurface(` в прод-коде `services/` |
| **Д4** | объявлений поверхности | `ccc6918ef` | **11** | вхождений `servicecontract.Surface{` в прод-коде `services/` |
| **Д5** | не-gRPC слушателей своей сборки (П11) | `ccc6918ef` | **0** | прод-файлов `services/` с литералом `http.Server{` |
| **Д6** | то же на ревизии вооружения гейта Ф5 | `5cad2f9e0` | **8** | тот же |
| **Д7** | обёрток вывода карты прав | `ccc6918ef` | **3** | файлов `services/**/permission_map.go`, не тесты |
| **Д8** | то же на ревизии замера клетки Ф2 | `563f92852` | **3** | тот же |
| **Д9** | конструкций сервера в сервисах | `ccc6918ef` | **2** | вхождений `grpcsrv.NewServer(` в прод-коде `services/` |
| **Д10** | вызовов конструктора сервера у края | `ccc6918ef` | **2** | вхождений `grpc.NewServer(` в прод-коде `gateway/` |
| **Д11** | копий загрузочного гейта (П8) | `ccc6918ef` | **1** | файлов `internal/fgaboot/gate.go` в дереве |
| **Д12** | то же на базе | `cf69249ed` | **3** | тот же |
| **Д13** | прод-файлов, взявших носитель полосы | `ccc6918ef` | **21** | прод-файлов `services/`, импортирующих `pkg/peer` |
| **Д14** | проводок сужателя полем дескриптора | `ccc6918ef` | **6** | вхождений `Narrowers:` в прод-коде `services/` |
| **Д15** | экземпляров файла каталога прав (П14) | `ccc6918ef` | **2** | файлов `permission_catalog.json` в дереве |
| **Д16** | самодельный административный рубеж (в-12) — жив ли | `ccc6918ef` | **1** | объявлений `func assertAdminAccess(` в прод-коде `services/`; истекает САМО: снимут рубеж — предикат даст **0** и закрепление покраснеет |

---

## §3 Архитектура контура — шесть слоёв и один вынесенный

```
             ┌──────────────────────────────────────────────┐
  C4 ГЕНЕРАЦИЯ │ опции proto → каталог прав (295 строк)      │
             └───────────────┬──────────────────────────────┘
                             │ строки выбираются по ВЫВЕДЕННОМУ домену
                             ▼
  C2 ДЕСКРИПТОР  ── данные о процессе ──►  C1 НОСИТЕЛЬ ВХОДЯЩЕГО ПУТИ
       ▲   (Serve возвращает error, а не сервер; сервис получает ServiceRegistrar)
       │                                      │
  C3 ЗАКРЫТЫЕ ТИПЫ ─────────────────────────►─┘
       (отношение · круг · токен полосы · маркер версии · полоса списка)

  C5 КОНФОРМАНС И ГЕЙТЫ ── поверх всего, ЧИТАЮТ дескриптор
  C6 НОСИТЕЛЬ ИСХОДЯЩЕГО ПУТИ (pkg/peer) + SDK (pkg/kacho) — зеркальная конструкция
  ─────────────────────────────────────────────────────────────────
  C7 ПИСАТЕЛЬ (валидация параметром записи) — ВЫНЕСЕН в XC-8, §12
```

| Слой | Предмет | Артефакт | Чем держится | Чего НЕ закрывает |
|---|---|---|---|---|
| **C1** | входящий запрос: цепочка и слушатели | `pkg/servicehost.Serve` | **компилятор** (тип возврата и тип аргумента регистрации) + **отказ старта** | правильность содержания аннотации; не-gRPC поверхность |
| **C2** | что сервис объявляет о себе | `pkg/servicecontract.Descriptor` | **компилятор** (нет полей интерсепторного типа) + конструктор, отвергающий пустую ось | значения осей — их канон принадлежит XC-6 |
| **C3** | значения, которыми говорят с iam | 5 закрытых типов в `pkg/` | **компилятор**: неверное значение не имеет представления | что значение **применено** там, где нужно |
| **C4** | карта прав | каталог из proto + вывод карты | **единственность источника**: расходиться нечему | верность самой аннотации в proto |
| **C5** | применение | гейты `internal/repohygiene` + конформанс, параметризуемый дескриптором | **инъекция в обе стороны** + проверка предпосылки | то, что гейт не читает; предпосылка меняется — гейт обязан краснеть на ней |
| **C6** | исходящий вызов и пользователь | `pkg/peer`, `pkg/kacho` | компилятор (нет экспортируемого конструктора соединения) + AST-гейт | внешние системы (иная полоса, 42 файла) |

**Кто чего НЕ закрывает — раздельно, чтобы слои не считались взаимозаменяемыми.** Тип отвечает за то, **что можно записать**; отказ старта — за то, **что записанное согласовано**; гейт — за то, **что записанное применено**. Всё, что про **применение** (монтаж, регенерация, посадка, адопция), типом не закрывается **никогда**, и попытка закрыть даёт ровно ту конструкцию, которая выглядит защитой и не может упасть.

---

## §4 Формы

### 4.1 Дескриптор — сервис приносит ДАННЫЕ

```go
package servicecontract // pkg/servicecontract

// Axis — ось, у которой ПУСТОЕ ЗНАЧЕНИЕ ЗАКОННО. Заводится только для таких
// осей (их четыре), иначе это церемония: остальные — обязательные поля,
// отвергаемые конструктором.
//
// Смысл: «пусто» и «не применимо» неразличимы без явного «почему», а XC-6 §5.2
// требует, чтобы необъяснённая пустая клетка была НАХОДКОЙ.
type Axis[T any] struct{ /* поля неэкспортируемые */ }

func Value[T any](v T) Axis[T]
func NotApplicable[T any](because string) Axis[T] // because == "" → New отказывает
func (a Axis[T]) Get() (T, bool)
func (a Axis[T]) NotApplicableBecause() (string, bool)

// Spec — по-процессная плоскость. Полей типа grpc.UnaryServerInterceptor /
// grpc.StreamServerInterceptor / grpc.ServerOption здесь НЕТ: невыразимость
// чужой цепочки и есть механизм.
type Spec struct {
    Service     ServiceName        // закрытый тип, не строка
    // L1: круг отправителей. Тип — landed `grpcsrv.TrustedForwarders`, НЕ новый:
    // заводить второе имя для существующего типа значило бы получить ровно ту
    // вторую величину, ради устранения которой он и заведён.
    // Семантика нулевого значения — «круг НЕ сужен» (действующая семантика
    // библиотеки, решение в-6); «забыл настроить» ловит О1, а не тип.
    // Непустота мерится ЕДИНСТВЕННЫМ предикатом `IsNarrowed()` — тем же, что
    // читает транспорт.
    Forwarders  grpcsrv.TrustedForwarders
    Authz       AuthzSource        // перечисление: ViaIAM | Self — НЕ nil-перегрузка
    CheckEdge   PeerEdge           // L3: адрес + транспорт, задаётся ЯВНО, не выводится
    CacheWindow time.Duration      // L3: окно отзыва, без умолчания
    ClientBudget time.Duration     // L3: срок вызова там, где клиент достижим вне звена

    // L4: что сервис эмитит владельцу прав. Элемент — landed `proxytuple.Relation`,
    // и он ЛИШЬ ПОЛОВИНА решения: приём судится ТРОЙКОЙ (§4.4), поэтому
    // проверяет намерение `proxytuple.ValidateTuple`, а не членство в наборе.
    Emits         Axis[[]proxytuple.Relation]
    Registers     Axis[[]fgatype.ObjectType]           // типы, регистрируемые у владельца
    // L6: ПРОВОДКА сужателя по методу — реализация, а не перечень.
    // Перечень сужаемых методов и их полосу даёт КАТАЛОГ (решение в-5):
    // второго объявления не существует, поэтому расходиться нечему.
    // Предикат страницы — ЗНАЧЕНИЕ сужателя, а не константа контура (решение
    // XC-6 §9.2: предикаты остаются разными).
    Narrowers     Axis[map[MethodFQN]ListNarrower]
    HideExistence Axis[map[fgatype.ObjectType]NotFoundFormat] // L7

    Existence  authz.ExistenceProbe // порт; nil — законное значение, объявляемое через Axis у HideExistence
    BootGate   *bootgate.Gate       // nil ⇒ очереди регистраций нет
    Registrar  iamproxy.Registrar   // nil ⇒ сервис не пишет кортежей

    Mode config.Mode
    PublicCreds, InternalCreds credentials.TransportCredentials
    Logger *slog.Logger
    Metrics *observability.Registry
}

// Descriptor — принятый Spec. Поле неэкспортируемое: литералом не собрать.
type Descriptor struct{ /* неэкспортируемое */ }

// New отказывает, НАЗЫВАЯ каждую незаполненную ось поимённо.
func New(s Spec) (Descriptor, error)
```

**Поля `Domain` НЕТ намеренно** — оно **выводится** (§11 К-2).

> [!warning] Здесь стояло другое имя типа и комментарий «нулевое значение = НЕ ДОВЕРЯТЬ НИКОМУ» — неверны обе половины
> **Имя.** Поле объявлялось типом из пакета, которого в дереве нет и не будет: круг стал типом
> общего фундамента под другим именем. Исполнить строку буквально значило бы **завести второе имя
> для существующего типа** — то есть вернуть ту самую двойственность величины, ради устранения
> которой тип и вводился. Мёртвое имя здесь **не воспроизводится**: строка в обратных кавычках
> читается как координата, и следующий пойдёт искать её в дереве.
>
> **Семантика.** Комментарий объявлял **инверсию** смысла пустого круга — ровно ту, которую
> владелец **отверг** решением в-6 в этом же документе. То есть §4.1 и §13 в-6 были двумя местами
> об одном предмете, из которых верно одно, и ошибка была тем опаснее, что §4.1 короче и читается
> раньше. Обнаружено не мной: XC-6, сверяя свою ссылку на владельца оси, наткнулся на то, что
> ссылка «объявленная семантика нулевого значения (§4.1, §4.2)» ведёт в место, противоречащее
> второй половине той же ссылки.
>
> Действующая семантика: пустой круг означает «**не сужаем**», и меняет это не тип, а **отказ
> старта** (О1) по тому же предикату, который читает транспорт. Разделение названо в §5 п.4 и
> п.8а, а его цена — в §9 п.12.

**Поля `Lists` больше нет — оно отменено решением в-5** (2026-08-09). Здесь стояло объявление «метод + полоса + предикат страницы», и оно было **вторым источником** ровно того, что уже объявляет каталог: перечня сужаемых методов и их полосы. Владелец выбрал **один** механизм — вывод карты из каталога у всех семи, — поэтому дескриптор несёт **реализацию** сужателя, а не его перечень. Разница проверяемая: при объявлении перечня расхождение «объявлено одно, в каталоге другое» **выразимо** и требует сверки; при проводке-без-перечня оно невыразимо, а зеркальная пара отказов старта (О3/О4) сверяет **проводку против каталога**, то есть код против единственного объявления.

**Полнота дескриптора держится тремя механизмами разной силы, и они не смешиваются:**
`Axis[T]` делает необъяснённую пустоту **непредставимой** в принятом дескрипторе — это построение; `exhaustruct`, **суженный шаблоном включения ровно до `Spec`**, делает **отсутствие поля** ошибкой линта во всех семи — это гейт этапа сборки (линтер сегодня в конфигурации не включён, включение точечное, репо-широкого шума не даёт); заявление «не применимо» **судится каталогом** — `NotApplicable` на оси сужателей при наличии строки `scope_filtered` того же домена есть **находка**, поэтому исключение истекает само.

### 4.2 Носитель входящего пути — одна точка входа

```go
package servicehost // pkg/servicehost

// Registrar приносит ТОЛЬКО обработчики. Тип не позволяет добавить звено —
// это и есть фиксированный слот (аналог мультиплексора, а не сервера).
type Registrar func(grpc.ServiceRegistrar)

// Serve поднимает ОБА слушателя. Возвращает error, а НЕ *grpc.Server:
// у вызывающего не остаётся объекта, к которому можно приделать своё звено.
func Serve(ctx context.Context, d servicecontract.Descriptor, public, internal Registrar) error
```

**Как носитель узнаёт служимый набор RPC** (это несёт условие отказа О2). `grpc.ServiceRegistrar` — интерфейс `RegisterService(*ServiceDesc, any)`. Носитель передаёт **записывающую** обёртку: она снимает `sd.ServiceName` и имена методов, затем делегирует настоящему серверу. Получается **точное** множество служимых полных имён. **Служить RPC и не отдать его дескриптор нельзя — это одна операция.**

Форма применима без правки регистрации: **2 из 7** корней уже передают `grpc.ServiceRegistrar` (П3).

### 4.3 Порядок звеньев — один, с причиной на позицию

| # | Звено | Почему здесь | Сегодня |
|---|---|---|---|
| 1 | инициализация записи об исходе | контекст исхода существует раньше, чем что-либо падает | — |
| 2 | восстановление после паники | ловит панику всего ниже, включая звенья | 4 из 7, **5 реализаций** |
| 3 | метрики и идентификатор запроса | наблюдаемость обязана видеть и отказ звена | по-разному |
| 4 | верхняя граница обработки | предел, ниже которого всё имеет бюджет | 1 из 7 |
| — | *(слот ограничителя одновременности — см. §10 п.12: позиция зафиксирована, звено в Ф1 НЕ вводится)* | классифицирует по **установленному** субъекту и защищает **чужую** ёмкость | 0 из 7 |
| 5 | личность пира по сертификату | чей это пир | 7 из 7 |
| 6 | переданная личность, сужённая кругом | вправе ли пир говорить за другого | 7 из 7 |
| 7 | **резолв области один раз в контекст** | несущее звено: всё ниже читает **одно** значение | резолвят двое порознь |
| 8 | решение о доступе | читает резолв, сам не резолвит | 7 из 7, одна реализация |
| 9 | загрузочный гейт мутаций | приём Create только при поднятом пути доставки | 3 кода, 1 с производителем |
| 10 | журнал исхода, включая `Operation.id` | вторая стадия аудита (§1.7) | — |

**Профиль края — ТА ЖЕ функция с другими данными** (свой ограничитель, отсутствие звена 9, свои формы отказа). «Энфорсим дважды» перестаёт означать «двумя реализациями».

### 4.4 Закрытые типы — что перестаёт иметь представление

**Из пяти три уже landed — под ДРУГИМИ именами, и это правит не косметику, а предписание.** Строка,
называющая имя, которого в дереве нет, читается как «завести», а не «использовать»: исполнение дало
бы второе имя для существующего типа. Поэтому колонка «состояние» ниже — часть контракта, а не
справка.

| Тип | Дом | Состояние (`92ca6af9d`) | Нулевое значение | Что становится невыразимо |
|---|---|---|---|---|
| **правило приёма кортежа** — `proxytuple.ValidateTuple`, решение по **ТРОЙКЕ** (субъект · отношение · тип объекта) | `pkg/authz/proxytuple` | **landed** (`91850e208`, `49851acb7`); отношение — тип `proxytuple.Relation`, набор **три** элемента | `Relation("")` в наборе отсутствует ⇒ отвергается правилом | кортеж, который приёмная сторона не примет: расхождение эмиссии с приёмом перестаёт быть прозой в комментариях шести файлов и рукописной копией набора у пробы |
| **круг отправителей** — `grpcsrv.TrustedForwarders`, предикат непустоты `IsNarrowed()` | `pkg/grpcsrv` | **landed** (`7f03e0bae`, `5573aa87f`) | **семантика библиотеки НЕ меняется** (решение в-6): пустой круг по-прежнему означает «не сужаем». Тип не переопределяет смысл нулевого значения — он делает величину **единственной** | расхождение стража, самоотчёта и транспорта: величина одна, читают её все трое, сверять **не с чем**. «Забыл настроить ⇒ доверяю всем» закрывается **не типом, а отказом старта** (О1) — уровень построения, не языка (§5) |
| **машинный признак полосы** — `errors.Reason`, код лежит **внутри** полосы | `pkg/errors` | **landed** (`96055cbd8`); словарь из пяти закрыт компилятором | конструктора вне полосы нет | код без токена; `NOT_FOUND` на чужой полосе; пустой id, уезжающий в чужую полосу; и — потому что код внутри — ответ, машинно заявляющий одну полосу, а кодом другую |
| **маркер версии источника** | `pkg/` (дом выбирается вместе с первым потребителем) | **не заведён**: величина ездит полем запроса, тип её не держит | конструктор **только** из штампа БД внутри writer-транзакции; из часов процесса конструктора **НЕТ** | «взять свежие часы на синхронной стороне»; «не послать версию вовсе» |
| **область как ДАННЫЕ вместо замыкания** | `pkg/authz` | **не заведена**: `authz.ObjectExtractor` на стволе по-прежнему `func` | — | область, отличающаяся от каталога **молча** |

**Про последний — довод пришлось перемерить, и он ослаб, но не исчез.** В первой редакции здесь
стояло: область хранится замыканием, поэтому общий фундамент несёт **492 строки** (П7), чья
единственная задача — **восстановить данные из кода, исполнив его**, и замена замыкания данными
«лишает этот предикат предмета». Первая половина верна и сегодня: `authz.ObjectExtractor` остаётся
функцией на обеих ревизиях. Вторая — **нет**: пакет сверки удалён вместе с самой сверкой (у неё
исчез предмет — источник стал один), но **приём пережил свой пакет**. Он живёт в
`pkg/authz/catalogderive/scope.go`, **100** строк: резолвит тип запроса из глобального реестра
proto, **вызывает** замыкание на нулевом сообщении и — что важнее — вынужден ловить панику, потому
что замыкание, зависящее от полей запроса, на пустом сообщении **не имеет статического ответа**.
Именно эта ветка и есть довод: данные ответ имеют всегда. Так что величина «492 → 0» была бы
подтасовкой; честно — **492 → 100, и техника осталась**, потому что осталась её причина.

> [!warning] Здесь стояло «отношение вне закрытого набора → не компилируется», и дерево это уже запретило
> Формулировка объявляла предметом решения **отношение**. Landed-правило принимает решение
> **тройкой**, и его собственная шапка говорит, почему проверка по одному отношению неверна **и в
> разрешающую сторону тоже**: публичное чтение выражается парой «подстановочный субъект +
> read-глагол», которую ни одно иерархическое отношение не покрывает и для которой отдельного
> признака не существует — **кортеж и есть видимость**. То есть проверка членства отношения
> пропустила бы то, что обязана отвергнуть, и отвергла бы то, что обязана пропустить, а выглядела
> бы строже прежнего.
>
> Тем же местом закрыт и вопрос, откуда набор виден: набор жил под `internal/` владельца и был
> **недостижим языком**, из-за чего пятеро потребителей знали правило прозой, а единственная проба
> приёмной стороны несла **рукописную копию**. Копия, которую нельзя скомпилировать против
> оригинала, расходится молча, и цена расхождения — не красный тест, а очередь, где отказ в правах
> классифицируется как временный и голова партиции держит всё за собой.
>
> Практический вывод для приёмки: **исполнять §4.4 в прежней редакции — регресс.** Требование к
> носителю формулируется от тройки, а не от отношения (§6 A7).

### 4.5 Карта прав ВЫВОДИТСЯ, а не пишется

**Источник вывода — АННОТАЦИИ дескрипторов proto, слинкованных в бинарь**, а не строки вшитого
файла каталога. Это **третья** сторона, которой не называл ни один из двух вариантов К-3, и она
принята — потому что её выбрало дерево и потому что её довод сильнее обоих (разбор — §11 К-3).

```go
package catalogderive // pkg/authz/catalogderive — landed: ca3354d94 → 569bea84c

// Derive строит authz.RPCMap из per-RPC аннотаций тех методов, чьи дескрипторы
// уже есть в бинаре этого процесса. Вход — перечень proto-пакетов, которые
// сервис обслуживает; это ЕДИНСТВЕННОЕ, что он о правах заявляет, и заявляет он
// не право, а «какие RPC я поднимаю».
func Derive(protoPackages ...string) (authz.RPCMap, error)

// Формы с явным отступлением (Override / MapWithOverrides) НЕТ — после решения
// в-7 у неё ноль потребителей; предикат заведения — во врезке ниже.
```

Механика на аннотированный метод: полное имя → дескриптор метода → тип запроса; имя поля из
`scope_extractor.from_request_field` → поле сообщения (**нет поля или поле не строковое — ошибка
построения, а не рантайма**); тип объекта и отношение кладутся как есть; `scope_filtered` и
`<exempt>` дают соответствующие полосы. Отказом старта (а не отказом на первом запросе) отвечают
**четыре** состояния собранного бинаря: аннотация называет несуществующее поле · proto-пакет не
слинкован · ключ метода задвоен · метод не несёт аннотации вовсе. Все четыре одинаковы на каждом
старте, и каждое иначе выглядело бы как отказ по правам.

**Вывод применяется у ВСЕХ СЕМИ, включая iam** — решение владельца в-5 (2026-08-09). **Исполнено у
шести**: geo · storage · registry · compute · nlb · vpc берут карту выводом, и рукописная у каждого
схлопнулась до **40** строк (перечень proto-пакетов + вызов). **Клетка iam открыта**: карты у него
нет вовсе, сравнивать вывод не с чем, поэтому его включение доказывается конформансом, а не
равенством с несуществующим артефактом. Порядок работ и место, где ожидается красное, — XC-6 §6
шаг 4 (expand → migrate → contract).

> [!warning] Здесь предписывалось «канонический файл каталога переезжает в `pkg/`, экземпляров становится три» — исполнять это регресс
> Прежняя редакция выводила карту **из строк вшитого файла** каталога и оттого требовала третьего
> экземпляра файла: край и сид iam вшивают по копии, общему фундаменту понадобилась бы своя.
> Цена объявлялась приемлемой («все три под уже существующим гейтом байт-идентичности»).
>
> **Дерево пошло другим путём и объяснило, почему.** Шапка landed-пакета отвергает чтение JSON при
> старте прямо: файл каталога **сам генерируется** из этих аннотаций, значит читать его — выводить
> производное из производного **ценой третьей копии**, а копии расходятся. Дескрипторы же уже лежат
> в бинаре: сервис линкует сгенерированные стабы тех RPC, которые обслуживает, и стабы несут опции
> дословно. Вывод из них **не заводит ни одного нового артефакта** — и, что дороже, превращает
> перепись «каталог == аннотации» из тавтологии в настоящую сверку **генератора**.
>
> Замер, снятый на обеих ревизиях: `git ls-tree -r --name-only <rev> | grep permission_catalog.json`
> → **2** и там, и там. То есть предписание «2 → 3» не просто не исполнено — оно **не будет**
> исполнено, и оставить его значило бы поставить будущему исполнителю работу, которую дерево
> сознательно не сделало. Обе прежние стороны спора сохранены в §11 К-3 вместе с тем, чем каждая
> была сильна.

> [!warning] `MapWithOverrides` НЕ строится: решение в-7 отняло у него потребителей
> Форма с явным отступлением писалась под пять расхождений реестра «карта ↔ каталог» — это были
> её единственные кандидаты в потребители. Владелец решил (в-7): **права сторона сервиса там, где
> он строже, и каталог правится под сервис**, — значит расхождения закрываются **правкой
> каталога**, а не объявлением отступления в выводе. Потребителей у `Override` остаётся **ноль**,
> и по LEAN (ban #11) абстракция с нулём потребителей не заводится: она выглядела бы законной
> дверью для того самого «вывод расширил сервисную сторону», который решение и запрещает.
> **Предикат заведения назван, чтобы отказ не был вечным:** появилось расхождение, которое
> **владелец решил в пользу сервиса и при этом не согласился править каталог**, — тогда `Override`
> вводится **вместе со своим первым потребителем** и с записью причины на строку.

**Файл каталога остаётся там, где он есть, — два экземпляра, оба под гейтом байт-идентичности** (П14,
величина не изменилась ни на базе, ни на стволе). Прежнее направление слоёв («общий фундамент
ссылается на артефакт края путём-константой, и вывод сделал бы эту ссылку несущей») **перестало быть
предметом**: вывод не читает файл вовсе, поэтому ссылаться не на что. Предпосылка, из которой
следовал переезд, снята вместе с механизмом, который её порождал.

### 4.6 Отказы старта — исчерпывающий перечень

Каждый закрывает **уже случившееся**, и у каждого **производитель входа — каждое поднятие процесса**, а не фикстура. «Ноль срабатываний» здесь означает «ноль поднятий», а не мёртвую ветку.

| # | Условие отказа | Ось XC-6 | Закрывает уже случившееся |
|---|---|---|---|
| **О1** | круг отправителей пуст **по тому же предикату, что читает транспорт** | L1 | «пустой список = не сужаем, а не запрещаем» — класс найден в 4 сервисах за сутки, дважды комментарий рядом лгал; и подкласс «страж меряет не то, что транспорт» |
| **О2** | служится RPC, у которого нет строки каталога его домена | L2 | RPC вне карты — нерабочий при любых грантах; сегодня это молчаливый рантайм-отказ |
| **О3** | строка каталога объявляет `scope_filtered`, дескриптор сужателя не несёт | L2/L6 | сужателя нет вовсе |
| **О4** | дескриптор несёт сужатель на RPC, которого каталог таковым не объявлял | L2/L6 | проводка сужателя, потерянная при переносе слоёв (**зеркальная половина О3 — без неё пропажа невидима**) |
| **О5** | каталог называет тип домена среди скрывающих существование, форма отказа не объявлена | L7 | ресурс, отвечающий своей, отличимой формой ⇒ оракул существования |
| **О6** | ребро проверки прав объявлено, а адрес/транспорт **не задан явно** (в т.ч. выведен из чужого адреса) | L3 | контроль, ходивший по собранному из чужого адресу и не отказавший **ни разу за свою жизнь** (`security.md` §Hardening #9) |
| **О7** | окно кэша вердиктов не задано явно | L3 | «число, которого никто не выбирал»: 3 из 6 брали умолчание библиотеки неявно. **Предмет закрыт в стволе `9bcf28b3b`** — окно объявлено ручкой у каждого держателя кэша; в §4.6 условие остаётся, потому что носитель обязан его **унаследовать**, а не переоткрыть |
| **О8** | production-посадка: mTLS выключен на живом ребре / `sslmode` слабее `require` / звено решения не в цепочке | — | уже 7/7 семью реализациями — **переезжает в одну** |
| **О9** | у выведенного домена **ноль** строк каталога | L2 | «раскатка в ноль целей неотличима от „всё синхронно“»: ноль целей — **отказ**, а не успех |
| **О10** | ось дескриптора пуста и не объявлена «не применимо, потому что …» | все | XC-6 §5.2 требование 2 |

### 4.7 Носитель исходящего пути

```go
package peer // pkg/peer

// Lane — намерение вызова. Закрытый набор. Определяет ВСЁ остальное:
// срок, право на повтор, классификацию отказа, форму ответа наружу.
type Lane uint8
const (
    LaneValidate Lane = iota + 1 // чужой id на request-path мутации; fail-closed
    LaneRead                     // чтение справочника/зеркала на read-path
    LaneRegister                 // намерение владельцу прав; повтор — у дренажа, НЕ здесь
)

type Spec struct{ Peer, Method, Resource string; Lane Lane }

type Conn struct{ /* неэкспортируемое; экспортируемого конструктора, кроме Open, НЕТ */ }
func Open(ctx context.Context, cfg Config) (*Conn, error) // отказывает в СТАРТЕ, не в вызове

// Do — ЕДИНСТВЕННЫЙ способ позвать соседа. Вызывающий передаёт МЕТОД-ЗНАЧЕНИЕ
// сгенерённого стаба, но НЕ зовёт его: ctx строит Do. Поэтому «забыть срок»
// или «потерять личность» невыразимо.
func Do[Req, Resp any](ctx context.Context, c *Conn, spec Spec,
    stub func(context.Context, Req, ...grpc.CallOption) (Resp, error), req Req) (Resp, Outcome)

// Классификация закрытая, БЕЗ корзины «прочее».
type Outcome uint8
const (
    OK Outcome = iota
    Missing      // у владельца нет ресурса        → терминально
    StateRefused // ресурс есть, состояние не то   → терминально
    Denied       // отказ в правах                 → ТЕРМИНАЛЬНО
    Malformed    // владелец счёл ввод негодным    → терминально
    Unavailable  // владелец недоступен            → временно
    Unclassified // код вне словаря → терминально И ГРОМКО (никогда «временно»)
)

// Status — ЕДИНСТВЕННЫЙ способ вывести полосу наружу; машинный признак
// ставится ЗДЕСЬ, поэтому «забыть токен» невозможно: другого пути нет.
func (o Outcome) Status(spec Spec, msg string) error
```

Форма аргумента `stub` не изобретается — сгенерённые клиенты имеют ровно её, поэтому метод присваивается без обёрток.

**Сроки выводятся из полосы и берутся по САМОЙ УЗКОЙ семантике из существующих** — строго уже обеих сегодняшних композиций:

| Полоса | бюджет операции | срок попытки | повтор |
|---|---:|---:|---|
| `LaneValidate` | 5 c | бюджет / число попыток | только `Unavailable` |
| `LaneRead` | 5 c | бюджет / число попыток | только `Unavailable` |
| `LaneRegister` | 30 c | бюджет / число попыток | **нет** — повтор принадлежит дренажу |

Общий бюджет закрывает 30-секундный хвост тех, у кого срок стоит **внутри** повтора; срок попытки закрывает случай, когда одна зависшая попытка съедает весь бюджет у тех, у кого срок стоит **снаружи**. **Ни одна из пяти сегодняшних реализаций не имеет обоих.**

**Величины закреплены решением владельца в-10 (2026-08-09): 5 c у всех семи**, и вместе с ним **снят кэш положительного существования проекта** — у полосы нет собственной памяти о чужом ресурсе, поэтому удалённый проект перестаёт проходить проверку немедленно. Кэш жил у одного сервиса из пяти, нигде не был записан решением и замера в свою пользу не имел (LEAN). Возврат возможен — **только** после замера и с записью, и он будет виден как новое поле дескриптора, а не как тихая правка одного клиента.

**Побочное следствие, закрывающее находку XC-6 §4а «г» по построению:** токены полос производит `Outcome.Status`, поэтому из пяти канонических токенов начинают эмитироваться **все пять**, и «три токена, названные в трёх местах так, как если бы они эмитировались» перестают быть ложью.

### 4.8 Клиентская сторона для пользователя

`pkg/kacho` — пять предметов, каждый выведен из инцидента:

1. **Дозвон fail-closed.** Небезопасный транспорт задаётся явно и только явно (существующий SDK одного домена при отсутствии опций подставляет insecure — это написано в его собственном комментарии).
2. **Токен только через iam.** В конфигурации SDK **нет поля** для адреса провайдера ⇒ запрет прямого диала перестаёт быть правилом в документе и становится невыразимостью.
3. **Ожидание операции, из которого нельзя выйти с фантомом:**
   ```go
   // Await возвращает РАСПАКОВАННЫЙ ответ. Операция с ошибкой становится
   // ошибкой Go. Добраться до предвыделенного id, минуя проверку, из типа НЕЛЬЗЯ.
   func Await[T proto.Message](ctx context.Context, c *Client, op *operationv1.Operation) (T, error)
   ```
   Закрывает класс «фантомный id из `metadata` операции, завершившейся ошибкой».
4. **Ограниченный повтор на окне согласованности** — один раз, с бюджетом из одного места. Сегодня эта логика существует **только** внутри генераторов e2e и уже разошлась: помощник «дождись авторизации» — **7 копий** с бюджетами от 6 до 48 секунд (восьмикратный разброс), помощник «дождись появления в списке» — **4 копии** (И-ПРО, замер проекта «клиент/SDK»).
5. **Полосы вместо чисел.** Пользователь получает `Missing`/`Denied`/`Unavailable`, а не 400, который приходит и от валидации, и от состояния.

**Граница «генерируется / пишется руками».** Генерируется всё, что **выводимо из контракта proto**, включая сам факт асинхронности (возвращаемый тип `Operation` ⇒ нужен `Await`), поэтому пер-ресурсные обёртки **генерируются**. Руками пишется **один** ядровой слой: полосы, сроки, классификация, окно согласованности, жизненный цикл токена. Граница проходит здесь потому, что дрейф заводится там, где рукописного много (7 доменов × N ресурсов), и не заводится там, где рукописное одно.

---

## §5 Почему разночтение станет НЕВОЗМОЖНЫМ — три уровня и честная граница

Довод конструктивный, по одному механизму на утверждение. **Уровни не смешиваются: первые два — свойства построения, третий — гейт, и выдавать третий за первый нельзя.**

### Уровень 1 — язык: невозможность абсолютная

1. **Примитивы платформы недостижимы из сервиса.** Сборщик цепочки переезжает в `pkg/authz/internal/chain`. Правило Go: пакет `a/b/internal/c` импортируем **только** из поддерева `a/b`. Носитель живёт в поддереве — ему можно; композиционный корень сервиса — вне, значит `go build` **отказывает**. Проверяющий здесь **компилятор**, а не тест, который можно ослабить. Обойти можно только перенеся сервис внутрь `pkg/authz/`, что видно в диффе как перемещение каталога.
2. **Нет объекта, к которому приделать звено.** `Serve` возвращает `error`; регистрация получает `grpc.ServiceRegistrar` — интерфейс с единственным методом. Восьмой сервис, желающий свой порядок, не «нарушит правило» — **ему не на чем его записать**.
3. **Неверное значение не имеет представления.** Пространство отношений закрыто и **вне пакета не расширяется**: конструктора из произвольной строки нет, десериализация сверяется с тем же набором. То же для токена полосы, круга, маркера версии.

   > [!note] «Четыре плюс одна пара» — было верно на базе; в стволе иерархических отношений **три**
   > Landed-набор (`pkg/authz/proxytuple`) несёт `project`, `parent`, `owner` — плюс отдельно
   > стоящая пара публичного чтения, которую **не покрывает ни одно** иерархическое отношение.
   > Четвёртое (ярус аккаунта) снято **намеренно**, и его отсутствие несущее: модуль, чей
   > указатель принадлежности — аккаунт, в дереве отсутствует, а принимаемое отношение, которого
   > никто не эмитит, нельзя наблюдать ни работающим, ни сломанным — до него ничего не доезжает, —
   > зато следующий читатель принимает его за живую возможность продукта. Держится это
   > самоистекающим гейтом: запись набора, потерявшая эмиттера, роняет проверку.
   >
   > Для §5 существенно **не число**, а то, что оно теперь меняется **только правкой пакета**, и
   > правка эта краснеет с двух сторон. Число здесь и не должно было стоять: оно живёт в дереве, а
   > не в приёмке, — ровно поэтому прежняя редакция его и пережила.
4. **~~Забытое значение перестаёт быть разрешающим.~~ — УТВЕРЖДЕНИЕ СНЯТО С ЭТОГО УРОВНЯ решением в-6 (2026-08-09).** Здесь стояло: «сделать нулевое значение самым строгим; круг с нулевым значением ложен для каждого; это не „мы проверим“, а „неверное состояние не является состоянием“». Владелец решил **не менять** семантику пустого круга в общей библиотеке, поэтому свойство **языком не держится** и переезжает на уровень 2 (п. 8а): забытую настройку ловит **отказ старта**, который есть у 7 из 7 и который переезжает в одну реализацию. Формулировку нельзя было оставить «как есть, только помягче»: она — ровно тот класс, который §15 критерий 7 запрещает, — **гейт, выданный за свойство построения**. Что от неё остаётся верным: Go действительно не позволяет запретить нулевое значение, и там, где семантику нуля выбирает **новый** тип (`proxytuple.Relation`, `errors.Reason`, маркер версии), она выбрана самой строгой — п. 3 выше. Первые два landed, и в обоих нулевое значение действительно отвергается: пустое отношение в наборе отсутствует, пустая полоса не имеет конструктора.
5. **Величина перестаёт существовать в двух экземплярах.** Сегодняшнее расхождение стража с транспортом возможно потому, что величин **две** (длина сырой строки и число записей). У типа она одна, и её читают все трое. Разойтись нечему **не потому, что сверяют, а потому, что сверять не с чем**.

### Уровень 2 — построение: отказ старта с настоящим производителем входа

6. **Область резолвится один раз, потому что резолвер один и результат в контексте.** Второй резолв не «запрещён» — он бессмыслен: значение уже лежит. Расхождение двух резолверов исчезает вместе со вторым резолвером.
7. **Решение об области перестаёт иметь второго автора.** Сегодня их два: каталог (данные, генерируются) и карта (код, пишется). Фактическое требование есть **пересечение**, не записанное нигде. После вывода автор один: расхождение не «маловероятно» — **у него нет носителя**.
8. **Пропуск шага не доживает до обслуживания запроса** — десять условий §4.6, вход каждого — **каждое поднятие**.
8а. **Забытый круг отправителей ловится здесь, а не языком** (переехало с уровня 1 по решению в-6). Семантика библиотеки остаётся прежней — пустой круг означает «не сужаем», — поэтому единственный держатель свойства «забытая настройка не становится всеразрешающей» есть **О1**: отказ старта по **тому же предикату**, который читает транспорт. Основание владельца: такой отказ **уже существует у 7 из 7**, а production-mode обязателен и на локальном стенде.

> [!warning] Здесь стояло «значит посадки без стража не бывает **by construction**» — это правило, а не построение
> Разница не словесная, и §15 критерий 7 запрещает её стирать: «by construction» означает, что
> исход **не представим**, а тут он представим и представлен явно. Landed-страж несёт **опт-ин на
> несужение** — поле, объявленное в конфиге каждого сервиса; вне боевого режима оно снимает отказ,
> а в боевом **не читается вовсе**. То есть свойство держится **правилом** «production-mode везде,
> включая локальный стенд» плюс тем, что опт-ин намеренно не действует там, где это правило
> применяется. Правило можно нарушить, построение — нет.
>
> Опт-ин заведён не по недосмотру, и его довод честный: страж, молчащий вне боевого режима, — это
> ветка, которая на локальном стенде **не исполняется ни разу**, поэтому «забыл выставить круг»
> обнаруживается только на боевом профиле, где цена ошибки максимальна. Опт-ин переводит пустой
> круг из **умолчания** в **явную просьбу** — это выигрыш, и он остаётся выигрышем даже после того,
> как назван поверхностью. Поверхность — §9 п.12.

Цена честная и названа: между «нет стража» и «есть дыра» остаётся два шага, а не один, — процесс, поднятый мимо контура (§9 п.8), и небоевая посадка с испрошенным опт-ином (§9 п.12), — тогда как инверсия закрыла бы оба.

### Уровень 3 — гейт: то, что языком не закрыть

9. **Прямой вызов конструктора upstream-сервера.** Импорт `google.golang.org/grpc` не запрещается ничем. → AST-гейт: вызов вне общего фундамента — находка; реестр исключений сегодня **2** (край, П2), и **запись, которой нечего исключать, сама находка**. Разбор по AST, не по тексту — доказано собственным деревом: **из 15 вхождений имени конструктора одно стоит в комментарии** (П1), и текстовый гейт покраснел бы на нём.
10. **Что цепочка смонтирована на обоих слушателях.** Носитель гарантирует форму, но не то, что корень применил её к обоим. → расширение существующего гейта монтажа.
11. **Что сгенерированный артефакт свеж.** Генерация — шаг сборки, а шаг сборки можно не выполнить. → существующий гейт байт-идентичности остаётся.
12. **Что процесс стартовал в заявленной посадке.** Посадку живого процесса не описывает **ничто в коде**. → гейт по **живому процессу** и по шифрованию со стороны БД (выведено из ложного зелёного, где под не перекатывался, потому что настройки ехали без контрольной суммы, и boot-guard не сработал, **потому что старта не было**).
13. **Что новая полоса отказа получила токен.** Конструктор один, но никто не обязан его звать. → конформанс-гейт по пакетам клиентов.

### Что ОСТАЁТСЯ соглашением — названо, а не спрятано

**Порядок звеньев внутри общей функции.** Его не держит ни тип, ни компилятор — держит то, что функция **одна** и правка её видна как правка контура, а не как правка сервиса. Это и есть кубова́ «высота», а не невозможность, и это честная граница проекта.

**Три требования к каждому гейту — не пересказ, а ссылка:** XC-6 §5.3 (инъекция в обе стороны с называнием координаты · проверка собственной предпосылки и печать объёма осмотренного · гейт въезжает вместе с фиксом, а не раньше). Обе стороны инъекции по каждому гейту XC-7 названы в §6.

---

## §6 Сценарии приёмки (Given-When-Then)

Каждый сценарий несёт **чем доказывается** и **обе стороны инъекции**. Сценарий без законного близнеца ловит форму, а не существо, и первый ложный срабат его отключит.

**A1 — цепочку нельзя выразить.**
*Given* восьмой сервис заводится по контуру и хочет собственный порядок звеньев.
*When* автор пытается передать носителю своё звено.
*Then* **сборка не компилируется**: в дескрипторе нет ни одного поля интерсепторного типа, а `Serve` возвращает `error`.
*Доказательство:* компиляция пробного пакета, объявляющего такое поле, — ошибка типа.
*Инъекция:* (а) добавить в `Spec` поле интерсепторного типа → гейт формы дескриптора краснеет и называет поле; (б) добавить поле-порт (`ExistenceProbe`) → молчит.

**A2 — сборщик недостижим.**
*Given* композиционный корень любого сервиса.
*When* он импортирует сборщик цепочки из общего фундамента.
*Then* `go build ./...` **отказывает** (правило `internal/`).
*Инъекция:* (а) вернуть импорт в один корень → сборка красная, координата — имя пакета; (б) тот же импорт из `pkg/authz/serve` → сборка зелёная.

> [!note] Сценарий сдан **другим механизмом**, и половина (б) его инъекции предмета не имеет
> В дереве сборщик — неэкспортируемые функции `pkg/servicehost`, а не пакет под `internal/`.
> *Then* держится (сборка отказывает), но по правилу экспорта, а не по правилу `internal/`; половина
> (б) требует пакета, которому импорт **разрешён**, а такого пакета при неэкспортируемом имени нет
> вовсе. Сравнение двух механизмов в обе стороны, остаток фазы и координаты — во врезке «Ф5
> перемерена 2026-08-10» (§8.1); здесь оно не пересказывается, чтобы два места об одном предмете
> не разошлись на первом же уточнении.

**A3 — служимый RPC без строки каталога не поднимается.**
*Given* сервис регистрирует RPC, у которого нет строки каталога его домена.
*When* процесс стартует.
*Then* `Serve` **отказывает в старте**, называя полное имя метода (текст отказа оператору — из-под правила публичности выведен нормативкой).
*Доказательство:* записывающий регистратор снимает точное множество служимых имён; служить и не отдать дескриптор — одна операция.
*Инъекция:* (а) снять аннотацию у одного RPC и регенерировать каталог → старт красный, имя названо; (б) законно объявить RPC `<exempt>` → старт зелёный.

**A4 — объявленное сужение обязано быть предъявлено, а непредъявленное — отвергнуто.**
*Given* строка каталога объявляет `scope_filtered`.
*When* дескриптор не несёт сужателя на этот метод — **или** несёт сужатель на метод, которого каталог таковым не объявлял.
*Then* отказ старта в **обоих** направлениях.
*Почему обе половины:* без второй пропажа проводки невидима — ровно тот случай, который уже был.
*Инъекция:* (а) убрать один сужатель → красный; (б) объявить сужатель ровно на объявленные каталогом методы → зелёный.

**A5 — пустой круг отправителей не доживает до обслуживания запроса.**
*Given* профиль развёртывания не задал круг.
*When* процесс стартует.
*Then* **отказ старта** (О1) по **тому же предикату непустоты**, который читает транспорт, — не по длине сырой строки настройки.
*Что изменилось против прежней редакции:* здесь стояло «и даже если бы старт случился, нулевое значение круга не допускает никого» — то есть заявлялась **инверсия** действующей семантики библиотеки. **Решением в-6 (2026-08-09) инверсия отменена:** пустой круг по-прежнему означает «не сужаем», и свойство держит **отказ старта**, а не тип. Практическое следствие для сценария: он проверяет **старт**, и утверждать что-либо о поведении процесса, поднявшегося мимо контура, не вправе — это названо остаточным риском (§9 п.8), а не закрыто.
*Инъекция:* (а) профиль с пустым значением → красный, назван сервис и ручка; (б) профиль с непустым кругом → зелёный; (в) **обязательный третий контроль** — вырожденное значение (одинокая запятая): предикат стража, самоотчёта и транспорта обязан дать **один и тот же** ответ «пусто», иначе повторяется класс «страж меряет не то, что транспорт»; (г) **четвёртый, добавлен третьим кругом** — тот же пустой профиль **в боевом режиме, но с испрошенным опт-ином на несужение**: отказ обязан остаться, потому что в боевом режиме опт-ин не читается. Без (г) сценарий подтверждает только то, что отказ бывает, и молчит о единственном способе его не получить (§9 п.12).

**A6 — карта прав не пишется.**
*Given* домен с N строками каталога.
*When* носитель строит карту.
*Then* карта выведена; рукописной карты как файла **не существует**; имя поля, которого нет в сообщении запроса, — ошибка **построения**, а не рантайма.
*Инъекция:* (а) подложить строку с несуществующим именем поля → построение красное, названы метод и поле; (б) законная строка → молчит.

**A7 — кортеж, который приёмная сторона не примет, не доезжает до очереди.**
*Given* сервис строит намерение регистрации.
*When* он собирает **тройку** — субъект, отношение, тип объекта.
*Then* тройка судится **одним** правилом общего фундамента, тем же, что исполняет приёмная сторона: отношение вне набора отвергается типом, а **сочетание**, которого владелец не принимает, отвергается правилом на сборке намерения — до записи строки очереди. Неизвестное отношение в уже сохранённой строке падает при десериализации — **терминально и в конкретной строке**, а не бессрочно и во всей партии.
*Закрывает:* очередь, в которой ни одна строка никогда не была доставлена, потому что отказ в правах классифицировался как временный и голова партиции блокировала всё за собой.
*Инъекция:* (а) законное отношение **в незаконном сочетании** (тип объекта, для которого владелец его не принимает) → красный, координата названа; (б) то же отношение в законном сочетании → молчит; (в) **контроль в разрешающую сторону, без которого сценарий неверен**: пара «подстановочный субъект + read-глагол» — законная видимость, которую **ни одно** иерархическое отношение не покрывает; проверка обязана её **пропустить**. Без (в) сценарий закрепляет проверку по членству отношения, то есть ровно ту, которая отвергает законное и пропускает незаконное.

> [!warning] Здесь стояло «отношение вне закрытого набора → не компилируется», и это неверно в ОБЕ стороны
> Формулировка называла предметом решения отношение. Правило приёма, живущее в дереве, решает
> **тройкой**, и его шапка объясняет, почему проверка по одному отношению ошибается **и в
> разрешающую сторону**: кортеж и есть видимость, отдельного признака «публично» не существует.
> Разбор — §4.4; здесь важно следствие для приёмки: сценарий, требующий компиляторного отказа по
> отношению, был бы **зелёным на дефекте** и красным на законном.

**A8 — маркер версии нельзя взять с часов процесса.**
*Given* синхронный регистратор.
*When* он строит запрос регистрации.
*Then* маркер конструируется **только** из штампа БД writer-транзакции; конструктора из часов процесса **нет**; нулевое значение конструктор не производит.
*Закрывает:* пять разных происхождений маркера у пяти регистраторов и «доказанный вход, не сработавший ни разу на 367 регистрациях».
*Инъекция:* (а) вернуть конструирование из часов → не компилируется; (б) штамп транзакции → зелёно.

**A9 — токен полосы нельзя не поставить.**
*Given* клиент соседа получил отказ.
*When* он выводит полосу наружу.
*Then* код, токен, домен и метаданные выбираются **полосой**; по отдельности они недоступны; пустой id отвергается.
*Инъекция:* (а) голый `status.Error` с кодом полосы в пакете клиентов → конформанс-гейт красный, координата названа; (б) конструктор полосы рядом → молчит.

**A10 — неклассифицированный ответ соседа не считается временным.**
*Given* сосед ответил кодом вне словаря.
*When* вызывающий классифицирует.
*Then* исход — **отдельное** состояние: терминальное и громкое, никогда «повтори позже».
*Закрывает:* класс «корзина „прочее“ классифицирует терминальное как временное» — сегодня он жив на исходящем пути у одного сервиса и **пять раз починен вручную** на пути регистрации.

**A11 — необъяснённая пустая ось не доживает до старта.**
*Given* дескриптор, где ось сужателей пуста (после решения в-5 это **проводка**, а не перечень методов — перечень даёт каталог).
*When* автор не объявил «не применимо, потому что …».
*Then* конструктор отказывает, **называя ось**; объявление с пустой причиной тоже отвергается.
*И зеркально:* объявленное «не применимо» **судится каталогом** — при наличии строки `scope_filtered` того же домена это находка.
*Инъекция:* (а) стереть причину → красный; (б) законное «не применимо» у домена без таких строк → зелёный.

**A12 — прямой конструктор сервера не проходит мимо контура.**
*Given* прод-дерево.
*When* где-то вне общего фундамента вызывается конструктор upstream-сервера.
*Then* AST-гейт краснеет и называет координату; реестр исключений — **2 записи** (край), запись без предмета сама находка.
*Инъекция:* (а) вернуть вызов в сервис → красный; (б) **упоминание имени конструктора в комментарии** → **молчит** (иначе гейт красит сегодняшнее дерево — П1/П2).

**A13 — гейт печатает объём осмотренного.**
*Given* любой гейт этого контура.
*When* он завершается без находок.
*Then* он **утверждает**, сколько корней/файлов/функций осмотрел; «ноль находок» отличимо от «ноль прочитанного».
*Обоснование:* «раскатка в ноль целей печатала одиннадцать «пропущено» и выходила успехом» — невыполнение было **ненаблюдаемым**.

**A14 — конформанс параметризуется дескриптором, а не пишется под сервис.**
*Given* восьмой сервис объявил дескриптор.
*When* прогоняется конформанс-набор.
*Then* исполняются **те же** пробы, что у семи, с его значениями; поля дескриптора, которого нет в закрытом наборе, не существует.

**A15 — восьмой сервис не может «завестись мимо».**
*Given* автор пишет свой композиционный корень с нуля.
*When* он собирает сервер сам.
*Then* он не может построить карту прав (её строит только вывод из каталога), не получит проводки `Existence` и сужателей и упрётся в A12.
*Честная граница:* это **длиннее прямого пути**, а не невозможно (§9 п.8).

**A16 — ожидание операции не выдаёт фантом.**
*Given* пользователь SDK создаёт ресурс.
*When* операция завершилась ошибкой.
*Then* `Await` возвращает **ошибку Go**; добраться до предвыделенного идентификатора мимо этой проверки из типа нельзя.

---

## §7 Путь восьмого сервиса и путь доработки седьмого

Числа этого раздела — **И-ПРО**: замер проекта «новый сервис» по самому молодому сервису дерева (он же единственный, чей контур iam чинился преимущественно **внутри видимой истории монорепо**). Предикаты названы там; мной перемерены только П4/П5/П6/П12, и они сошлись.

### 7.1 Что было забыто при рождении сервиса — и дочинивалось потом

За полтора месяца после того, как сервис начал обслуживать запросы, в его контур iam въехали **десять различимых осей**, **каждая из которых была обязательным свойством, известным на момент заведения**: отсутствие RPC в карте прав · отсутствие production boot-guard целиком · якорь карты на ярусе там, где предмет — объект · отсутствие сужателя списков · список, называющий объекты, которые его же `Get` отвергает · отказ в правах, классифицированный как временный · комментарий, утверждающий обратное коду · несуженный круг отправителей без отказа старта · внутренний список без пообъектной авторизации · двухобъектный RPC, спрашивающий про один объект из двух.

**Забыть их было можно, потому что ничто не мешало сервису собраться и подняться без них.**

### 7.2 Заведение восьмого сервиса

**Колонка «сегодня» снята на базе `cf69249ed` и с тех пор частью изменилась — обе величины стоят
рядом, потому что зачесть чужую работу в свою пользу значило бы завысить выигрыш плана.**

| | на базе `cf69249ed` | на стволе `92ca6af9d` | после XC-7 | чем держится «после» |
|---|---:|---:|---:|---|
| шагов | **26** | **24** | **7** | 19 из 26 — написание копии того, что уже есть у соседа |
| из них **молча пропускаемых** | **19** | **17** | **0** | десять условий §4.6 + компилятор |
| рукописных карт прав | 1 файл, десятки ключей | **40 строк** (перечень proto-пакетов + вызов вывода) | **0** | поле дескриптора вместо перечня в файле |
| рукописных сужателей | 5 файлов | **0 своих** — берётся `pkg/listnarrow` | **0** | проводка становится полем дескриптора: сегодня взять реализацию можно, **не взять — тоже** |
| рукописных фабрик клиента проверки | 1 пакет (~60-100 строк) | без изменений | **0** | носитель |
| рукописных стражей старта | 1 | **0 своих** — один на семь в `Config.Validate` | **0** | `New` + `Serve` |

Семь оставшихся шагов: аннотировать RPC в proto · регенерировать каталог одной целью · зарегистрировать маршруты на крае · объявить дескриптор · реализовать обработчики · при новом типе объекта — модель прав и её проводка · набор e2e.

### 7.3 Доработка седьмого — она частотнее, и выигрыш здесь ЧЕСТНО умереннее

| | сегодня | после |
|---|---:|---:|
| добавить один RPC — мест | **6-9** | **3-5** |
| из них молча пропускаемых | **5** | **0** |
| добавить один ресурс (5 RPC + новый тип) | **≈13 правок в 9 файлах** | **5 мест**, из них два — по одному действию на ресурс, а не на RPC |

**Разницу 9→5 я не стану выдавать за большую.** Половина оставшихся неустранима: аннотация — данные, которые кто-то обязан назвать; e2e — утверждение о поведении, которое обязан написать человек.

**Настоящая величина — не число шагов, а число молчащих пропусков: 19 из 26 → 0 из 7 при заведении и 5 из 9 → 0 из 5 при доработке.** Владелец говорит «фактор ошибки», а не «трудоёмкость», и мерить надо это. Меряя только шаги, честный вывод был бы «выигрыш умеренный».

> [!important] Два молчащих пропуска ушли до XC-7 — и то, КАК они ушли, есть довод за XC-7
> Из девятнадцати за волну 08-06…08-10 закрылись два: «забыл написать карту прав» (карта
> выводится) и «забыл сузить круг отправителей» (страж один на семь). Оба закрыты **отказом
> старта**, а не типом, — то есть тем самым уровнем 2, который §5 и называет держателем.
>
> Существенно другое. Оба ушли **не потому, что кто-то шёл по плану**, а потому, что дефект
> отдельно нашли, отдельно измерили по дереву и отдельно починили во всех семи — три раза подряд,
> руками, за одну волну. Это и есть цена отсутствия носителя, выраженная не в строках, а в
> **повторах одной работы**: каждый следующий класс придётся ловить так же, поодиночке, пока
> «завестись мимо» дешевле, чем завестись по контуру. XC-7 не обещает найти классы за людей — он
> обещает, что найденный класс закрывается **один раз**, в одном месте, а не семью правками с
> перекличкой по сервисам.

---

## §8 Миграция семи: expand → migrate → contract

### 8.1 Порядок и его дискриминатор

Конфликт порядка с планом XC-6 (§11 К-9) разрешается **проверяемым признаком**, а не вкусом:

> **Тип держится независимо от того, кто собирает цепочку. Единственная реализация — нет: ей нужен вызывающий, а «кто вызывает» и есть носитель.**

Отсюда: **типовые** шаги XC-6 (круг отправителей — шаг 3; закрытый набор отношений — шаг 7) идут **до** носителя: они дёшевы, закрывают уже случившиеся классы и держатся компилятором при любом сборщике. **Единственно-реализационные** (сужатель — шаг 6; форма регистратора — шаг 9; тип токена — шаг 8) идут **после**: иначе каждый останется значением, которое сервис волен не использовать.

**Очередь описывает ПРЕДСТОЯЩЕЕ.** Исполненная фаза, оставленная в будущем времени, — то же
утверждение, пережившее свой предмет, только на уровне плана: следующий исполнитель берёт её в
работу и обнаруживает, что работа сделана, — в лучшем случае потратив день, в худшем **сделав
второй раз** и заведя второе имя рядом с первым (ровно то, чем грозила прежняя редакция §4.1).
Поэтому колонка состояния — первая, и в ней стоит коммит, а не слово.

| Фаза | Состояние на `92ca6af9d` | Предмет | Полна, когда |
|---|---|---|---|
| **Ф0** | **вне охвата XC-7** | шаги 0-2 XC-6 (правда прозы · страж ребра — **у двух завести и у третьего расширить до обоих боевых режимов**, XC-6 §9.5 · производитель ручки загрузочного гейта у двух) — идут первыми независимо (решение в-11: до шага 3 планы раздельны) | по критериям XC-6 |
| **Ф0т** | **ИСПОЛНЕНА** — круг `7f03e0bae` + `5573aa87f`; правило приёма `91850e208` + `49851acb7` | XC-6 шаги 3 и 7 — типы круга и правила приёма в общий фундамент | ✔ компилятор отвергает сырой срез; ✔ рукописная копия набора у пробы **исчезла** (правило переехало из `internal/` владельца в `pkg/authz/proxytuple`, импорт стал возможен). **Отличие от плана, названное, а не затёртое:** правило судит **тройку**, а не отношение (§4.4) — то есть фаза сдана строже, чем объявлялась |
| **Ф1** | **ИСПОЛНЕНА** — носитель `1c5535248`, первый переведённый `0fcba93a0`; замер на `5d3edd305`: `pkg/servicecontract` **4** файла, `pkg/servicehost` **6**, проб у носителя **28** | `pkg/servicecontract` + `pkg/servicehost` + все десять отказов старта; перевод **одного** сервиса | AST-гейт: в `services/*/cmd/**` **ноль** вызовов конструктора сервера и **ноль** значений интерсепторного типа; каждое условие §4.6 доказано инъекцией в обе стороны; конформанс переведённого зелёный |
| **Ф2** | **ЧАСТИЧНА** — рукописной карты нет ни у одного из семи, но вывод из аннотаций у iam отсутствует; перемерено на `563f92852`, перепроверено на `4d26e3300` | вывод карты **у всех семи** (в-5) — из **аннотаций дескрипторов**, не из строк файла (§4.5). **Предусловие в-7 превращено в работу:** каталог правится под сервис по пяти записям реестра **до** переключения (XC-6 §6 шаг 4) | ✔ рукописной карты как файла нет ни у одного из семи; ✔ пакет сверки **потерял предмет и удалён**; ✗ **у iam карта не выведена** — он вне контура решением владельца (Ф3), и его карта приходит из вшитого файла каталога, а не из аннотаций: это **следствие принятого решения**, а не остаток работы, и в §14.2 оно стоит именно так. **Перечень источников — перемерен 2026-08-12 (Д7/Д8), прежний был неверен НА СВОЕЙ ЖЕ ревизии:** обёртка в 40 строк, чьё тело — одна строка `catalogderive.MustDerive(protoPackages...)`, стоит у **трёх** (`vpc`/`compute`/`nlb`) — **3** и на `563f92852`, и на `4d26e3300`; у `storage` тот же вывод зовётся из проверки настроек, отдельного файла нет; у `registry` и `geo` файла нет **вовсе** — карту им выводит сам носитель (`pkg/servicehost` зовёт вывод по служимым доменам); у `iam` нет ни обёртки, ни носителя — его карта приходит из **вшитого файла каталога**, то есть из второго экземпляра того же объявления, а не из аннотаций дескрипторов. Реестр расхождений — предмет XC-6. **Прежняя редакция держала «✗ iam» и была неверна**, а мой первый перемер едва не подтвердил её: предикат `PermissionMap()` ловил ОПРЕДЕЛЕНИЕ функции-обёртки и потому объявлял её «рукописной» у трёх сервисов сразу. Число получено чтением тела, а не именем |
| **Ф3** | **ИСПОЛНЕНА** — шесть переводов, `91366ae16` → `563f92852`; седьмой (iam) выведен из контура решением владельца 2026-08-10 | остальные шесть на носитель, **проверка по КАЖДОМУ ресурсу**, не по статусу ветки | ✔ на носителе **шесть** сервисов (compute · geo · nlb · registry · storage · vpc) — предикат **Д1/Д2**: вызов `servicehost.Serve(` в прод-коде `services/`, **6** и на `563f92852`, и на `4d26e3300`. **Прежний предикат этой клетки — `git grep -l pkg/servicehost -- services/` — различающую силу утратил** (см. врезку ниже) и здесь не воспроизводится; ✔ в перечне гейта принятия осталась **одна** запись — iam, и она вида «решение», а не «ожидает», с отдельной пробой на СВОЁ основание; ✔ каждый перевод прошёл адверсарную проверку, и три круга дали находки, которые иначе уехали бы в ствол: снятый рубеж mTLS у registry, признаваемый на публичном слушателе админский заголовок у compute, выключенная отсечка шторма у nlb — все три восстановлены вместе с удалёнными пробами |
| **Ф4** | **ИСПОЛНЕНА целиком** — единственная реализация `924691519`, `fd0c22cfe`, `c4d733248`; поле дескриптора появилось вместе с Ф1 (`1c5535248`) | сужатель в общий фундамент | ✔ `pkg/listnarrow` — одна реализация, четвёрка сжалась 3753/17 → 587/12; ✔ проводка стала **полем принятого дескриптора** (`Narrowers Axis[map[MethodFQN]ListNarrower]`, `pkg/servicecontract/contract.go:378`), то есть «сервис волен не взять» держится конструкцией, а не обзором. Замер на `5d3edd305`: сужатель зовут **26** прод-файлов в **пяти** сервисах (vpc 9 · nlb 7 · storage 5 · compute 3 · registry 2) |
| **Ф5** | **ИСПОЛНЕНА ДРУГИМ МЕХАНИЗМОМ** — приехала вместе с носителем (`1c5535248`); перемерено на `5cad2f9e0`, **остаток закрыт 2026-08-11** (`e2941effc` + `0641edd8`), перепроверено на `4d26e3300` | **contract**: сборщик недостижим из композиционного корня, AST-гейт вооружён | ✔ сборщик недостижим — `unaryChain`, `streamChain`, `serverPair` **неэкспортируемы** (`pkg/servicehost/serve.go:515` · `:528` · `:134`); вся экспортированная поверхность пакета — `Registrar`, `Serve`, `IsGatedMutation`, и ни одно из трёх не собирает цепочку и не отдаёт сервер; ✔ дверь дескриптора закрыта гейтом A1 (`internal/repohygiene/descriptorchainlink_test.go:56`, печатает перепись: полей **22**, импортов grpc **1**); ✔ AST-гейт вооружён — `internal/repohygiene/servicehostadoption_test.go`, перепись печатается (на `5cad2f9e0`: корней **14**, не-тестовых файлов **57**, находок **34**, исключений **5**), инъекция в обе стороны, ловушка-проза и самоистечение — `servicehostadoption_injection_test.go`; ✔ **край вошёл в обход** (`0641edd8`, 2026-08-11): перепись прогона на `4d26e3300` — корней **18**, не-тестовых файлов **75**, находок **16**, записей **2**, из них ожидают перевода **0**, а две — решение владельца оставить край вне контура, с письменной причиной и **внешним** предикатом истечения, который держит отдельная проба (`internal/repohygiene/edgecontourdecisionbasis_test.go`, `TestCarrierStillCannotExpressAProxyEdge`); ✔ **восемь файлов не-gRPC поверхности предмет потеряли** — П11 на `4d26e3300` даёт **0** (Д5; на `5cad2f9e0` было **8** — Д6). **«Исключений ноль» — условие Ф3, а не этой фазы:** пять записей реестра были ровно пятью непереведёнными сервисами, и сегодня их **ноль** |
| **Ф6** | **ИСПОЛНЕНА** — `pkg/peer` **3** файла, замер `e2941effc` | `pkg/peer` + перевод вызывающих + гейт на прозу полос | ✔ носитель полосы в дереве (`Classify` · `Outcome.Status` · `Reason`), потребителей — **21** прод-файл в **пяти** сервисах (compute · nlb · registry · storage · vpc); ✔ адверсарная проверка дала две находки, обе закрыты в том же круге: решение авторизации оседало в кэше без личности (новый предикат `CallerIndependent`, отделённый от `RefusedReference`) и проза недоступности получала подстановку идентификатора, которого полоса не называет (`NamesResource` — решение о назывании ресурса стало ОДНИМ, а не тремя порознь); ✔ класс закрыт гейтом дерева `internal/repohygiene/peerprosverb.go` — разбор AST, доходит до вызывающих обёртки, проверяет собственную предпосылку и печатает перепись (на `e2941effc`: 1124 файла · 3 литерала · 14 значений · вне зрения **0**; прогон на `4d26e3300`: **1133** файла, литералов, значений и слепых зон — те же 3 · 14 · **0**, то есть выросла только перепись дерева, а не предмет) |
| **Ф7** | **ВНЕ ПРЕДМЕТА КОНТУРА** — решение 2026-08-11, обоснование в клетке справа | `pkg/kacho`; существующий SDK одного домена **вбирается и удаляется** | Фаза снята с очереди XC-7, а не исполнена, и это решение, а не долг. **Предмет XC-7 — ВХОДЯЩИЙ путь**: кто зовёт сервис и как проверяются права. SDK — путь ИСХОДЯЩИЙ: как внешний интегратор зовёт наши API (`services/vpc/pkg/sdk/vpc/doc.go` называет адресатом именно интегратора). Это ортогональные предметы, и «единый вход в iam» от наличия SDK не зависит ни в одну сторону. Замер `563f92852`: `pkg/kacho` — **0** файлов, SDK домена — **12** файлов у одного `vpc`, импортёров внутри дерева **0** (их и не должно быть — поверхность внешняя). Заводить общий SDK ради исполнения клетки значило бы написать шесть обёрток без единого потребителя — тот же мёртвый код этажом выше, которым едва не кончилась Ф5. Возвращается в очередь по ВНЕШНЕМУ факту: появится второй домен со своим SDK либо интегратор, которому нужен единый клиент |
| **Ф8** | **ИСПОЛНЕНА** — профиль `pkg/servicehost.ServeSurface` + `servicecontract.Surface`; замер `e2941effc` | **HTTP-профиль ТОЙ ЖЕ функции** — владелец взял предмет (решение в-1): не-gRPC слушатели входят в контур **отдельным профилем**, а не полями общего дескриптора | ✔ профиль поднимает **11** объявленных поверхностей (Д4) в **семи** сервисах (Д3), поднимаемых **восемью** вызовами `servicehost.ServeSurface(` — у `registry` их два; ✔ самодельных серверов в `services/*/cmd/**` **ноль** — П11 на `4d26e3300` даёт **0** файлов (Д5); ✔ решение об аутентификации на каждой поверхности объявлено данными и печатается самоотчётом при подъёме; ✔ выключение поверхности стало ОБЪЯВЛЕНИЕМ с причиной, а не молчанием; ✔ гейт `internal/repohygiene/nongrpcsurfaceprofile_test.go` — 14 корней, 59 файлов, находок **0**, записей исключений **0**, инъекция в обе стороны. **Адверсарная проверка дала две находки, обе закрыты в том же круге:** привязка порта уезжала в горутину (отказ становился кодом возврата процесса, успевшего проработать при мёртвой поверхности) — подъём разделён на синхронную привязку и ожидание, и неверное использование стало непредставимым; и гейт диалекта подавлений считал директиву внутри синтетической фикстуры подавлением этого файла — научен различать код и строковый литерал |

**Итог по очереди, разбивкой, а не одним числом** — и разбивка выводится из колонки «Полна,
когда» выше, а не считается по словам: в охвате XC-7 **девять** фаз (Ф0 вне охвата).

**Перемерено 2026-08-12 на `4d26e3300`, по тому же единственному предикату — колонке «Полна,
когда»: ✔-только — исполнена целиком, смешанные ✔/✗ — частична.**

**Целиком исполнены семь** — Ф0т, Ф1, Ф3, Ф4, **Ф5**, Ф6, Ф8. **Частична одна** — **Ф2**:
её предмет объявлен как «вывод карты **у всех семи**», а у iam карта из аннотаций не выводится.
**Снята с очереди одна** — Ф7, решением 2026-08-11 как ортогональный предмет (исходящий путь
против входящего), с внешним предикатом возврата.

**✗ у Ф2 — следствие решения, а не остаток работы, и это НЕ повод считать её полной.** Iam выведен
из контура владельцем (Ф3, 2026-08-10); засчитать «снято решением» за «исполнено» значило бы
сделать ровно то, что документ ловит в других местах, — подменить исход словом. Закрытие Ф2
требует решения, и сторон **две**: либо iam переводится на носитель (тогда закрывает Ф3), либо
предмет Ф2 сужается решением владельца до **шести**, и «у всех семи» переписывается вместе с в-5.
**Молчание третьей стороной не является.**

> [!note] Здесь стояло «исполнены семь, частична одна (Ф5)» — перемер 2026-08-11 на `e2941effc`, и он устарел через час
> Прежний итог держал Ф5 частичной по двум ✗, и **оба потеряли предмет в ту же ночь**, причём
> первый — через **час и семь минут** после ревизии, на которой снят замер:
>
> - «восемь файлов не-gRPC поверхности без объявленного состояния» — П11 обнулился на самом
>   `e2941effc` (Д5 → **0**, Д6 на `5cad2f9e0` → **8**), то есть ✗ был просрочен уже в момент,
>   когда его переписывали;
> - «край вне обхода гейта» — край вошёл в обход на `0641edd8` (2026-08-11 06:08, замер снят на
>   `e2941effc`, 05:01), и два его вызова конструктора перестали быть **невидимыми**: они стоят
>   двумя записями решения с письменной причиной и внешним предикатом истечения, который держит
>   отдельная проба. Вызовов по-прежнему **2** (Д10) — изменилось не их число, а то, что теперь
>   их кто-то судит.
>
> Урок, ради которого врезка остаётся вместо правки на месте: **отметка остатка стареет быстрее,
> чем число.** Число на своей ревизии верно навсегда; ✗ утверждает про **сегодня** и потому
> обязано нести ревизию так же, как число. Оба этих ✗ ревизию несли — и всё равно читались как
> действующие, потому что итог их пересказывал **без** неё. Отсюда §2.5: величины, по которым
> судят о состоянии фаз, закреплены и меряются гейтом, а не читателем.

**Ф2 перемерена 2026-08-12 (Д7/Д8): перечень источников в её клетке был неверен НА СВОЕЙ ЖЕ
ревизии** — названы шесть сервисов, обёртка стоит у **трёх**, и `3` даёт предикат и на
`563f92852`, и на `4d26e3300`. Предмет фазы («рукописной карты нет ни у одного из семи») от
этого не меняется — меняется утверждение о том, **чем** карта получена у каждого; разбор в
клетке. Второе следствие: ✔ «iam карту получил» снят и заменён на ✗ со ссылкой на решение —
иначе клетка Ф2 и строка §14.2 про iam остаются двумя местами об одном предмете, из которых
верно одно.

> [!note] Предикат клетки Ф3 утратил различающую силу той же ночью — и сегодня отвечает другое
> Клетка называла предикатом `git grep -l pkg/servicehost -- services/` без проб и получала
> **6**. На `4d26e3300` тот же предикат даёт **7**: `iam` импортирует пакет носителя — но ради
> **профиля не-gRPC поверхности** (Ф8), а не ради gRPC-контура. То есть предикат перестал
> отличать «переведён на носитель» от «пользуется профилем поверхности», и, оставленный как есть,
> он опроверг бы решение владельца о выводе iam из контура — числом, которое означает не то.
>
> Различающий предикат — **Д1/Д2**: вызов `servicehost.Serve(`. Он даёт **6** на обеих ревизиях
> и перечисляет тех же шестерых. Контроль в другую сторону у него есть: на `5cad2f9e0` он
> отвечает **2** (geo, storage), на `cf69249ed` — **0**.
>
> Класс, а не случай: **предикат по ИМЕНИ ПАКЕТА меряет принадлежность к его области, а не
> использование механизма.** Стоит завести в том же пакете второй механизм — и число начинает
> считать оба. Предикат обязан целиться в **вызов**, ради которого фаза заводилась.

> [!note] Число «13 мест» у Ф8 не воспроизводится, и предиката при нём названо не было
> Клетка утверждала «профиль поднимает **13** мест в **семи** сервисах». Шесть предикатов на
> `e2941effc` и на `4d26e3300` дают: вызовов `servicehost.ServeSurface(` — **8**; объявлений
> `servicecontract.Surface{` — **11**; вызовов `NewSurface(` — **8**; прод-файлов, где
> встречается тип, — **10**; значений `SurfaceDescriptor` — **8**; объявлений `Reach:` — **11**.
> Тринадцати не даёт ни один. Число, у которого нет предиката, — впечатление, и отличить его от
> замера можно ровно одним способом: потребовать предикат **при** числе.
>
> Клетка приведена к двум закреплённым величинам — **11** объявлений (Д4) в **семи** сервисах
> (Д3), поднимаемых восемью вызовами. Перепись гейта Ф8 при этом воспроизвелась дословно:
> 14 корней, 59 файлов, находок 0, записей 0.

**Ф2 закрыта перемером на `563f92852`**, и здесь же снимается ошибка прежнего итога: он держал
✗ по `services/iam/internal/authzmap/permissions_to_relations.go`, тогда как предмет Ф2 — **карта
прав RPC**, а этот файл переводит **разрешения роли в отношения модели**. Разные предметы:
первый отвечает на «какое право требует этот вызов», второй — «во что материализуется эта роль».
Файл жив и жить обязан; к очереди XC-7 он отношения не имеет, и держать его в итоге как
незакрытую клетку значило считать чужой предмет своим долгом. **Перемер 2026-08-12: 248 строк
на обеих ревизиях (`563f92852` и `4d26e3300`); прод-потребителей пакета вне его самого — 10 (не
пять: прежнее число предиката при себе не несло, и повторить его нечем; предикат нового —
`git grep -l internal/authzmap -- services/iam/` без проб, без вспомогательного кода тестов и
без файлов самого пакета).**

> [!note] Состояние перемерено 2026-08-10 на `5d3edd305` — две клетки пережили свой предмет
> Таблица писалась на `92ca6af9d` и с тех пор **не двигалась**, объявляя Ф1 «не начатой» при нуле
> файлов носителя. На дереве носитель есть (`pkg/servicecontract` 4 файла, `pkg/servicehost` 6,
> 28 проб), первый сервис переведён, а гейт принятия живёт и печатает перепись: **14** корней,
> **58** не-тестовых файлов, **6** объявленных исключений — ровно шесть непереведённых сервисов,
> у каждого письменная причина, и запись самоистекает (исключение без предмета — находка,
> `internal/repohygiene/servicehostadoption_test.go`). Вместе с Ф1 закрылась и Ф4: сужатель стал
> **полем дескриптора**, чего в прежней редакции не хватало для ✔.
>
> Урок ровно тот, о котором предупреждает соседняя врезка: колонка состояния честна **до одной
> даты**. Прежде она завышала остаток; ошибиться она может в обе стороны, поэтому перед работой по
> этой таблице состояние **перемеряется**, а не читается. Предикаты для перемера названы в самих
> клетках.

> [!note] Ф5 перемерена 2026-08-10 на `5cad2f9e0` — предмет исполнен, но **не тем механизмом**, и клетка держала две разные фазы за одну
> **Что здесь стояло:** «**не начата** · contract: сборщик в `internal/chain`, AST-гейт вооружён ·
> исключений ноль, кроме двух записей края с письменной причиной и предикатом снятия». На момент
> письма это было **верно во всём**: носителя в дереве не было, `pkg/servicecontract` и
> `pkg/servicehost` несли по нулю файлов, гейта усыновления не существовало. Формулировка не
> устарела «по невнимательности» — её пережил предмет.
>
> **Чем исполнено.** Ф1 привезла носитель, и вместе с ним — оба механизма Ф5, но **другой
> конструкцией**, чем планировалась. Планировался переезд сборщика в `pkg/authz/internal/chain`
> (правило `internal/`); в дереве сборщик — **неэкспортируемые функции** `pkg/servicehost`.
> Координаты и перепись — в клетке выше.
>
> **Сравнение двух механизмов, в обе стороны, потому что это не одно и то же.** Пришедший
> **строже по охвату**: `internal/` пускает к сборщику всё поддерево владельца — сценарий A2 §6
> прямо на это и рассчитывал, объявляя законным близнецом инъекции «тот же импорт из
> `pkg/authz/serve` → сборка зелёная»; неэкспортируемое имя не пускает **никого**, кроме самого
> пакета, и законного близнеца такой формы у инъекции больше нет. Пришедший при этом **слабее по
> долговечности**: `internal/` — свойство **пути**, оно переживает переименование; неэкспортируемое
> имя — свойство **идентификатора**, и одна заглавная буква его снимает, а гейта на экспортированную
> поверхность `pkg/servicehost` в дереве нет (перепись: гейтов, читающих этот пакет, — **6**, ни один
> не судит его экспорт). Радиус этой слабости ограничен и назван: `Serve` интерсепторов не принимает,
> поэтому экспортированным сборщиком нельзя воспользоваться, не собрав **свой** сервер, — а это
> ловит AST-гейт уровня 3. То есть уровень 1 подстрахован уровнем 3, и вычёркивать разницу нельзя:
> она реальна, просто не одна держит свойство.
>
> **Единица гейта сменена осознанно, и причина записана в самом гейте**
> (`internal/repohygiene/servicehostadoption_test.go`, раздел «На что гейт целится и почему НЕ на
> `grpc.NewServer`»): прямых вызовов `grpc.NewServer` в не-тестовом коде **сервисов** — ноль на обеих
> ревизиях, поэтому гейт по ним был бы зелен всегда и не держал бы ничего. Реальная единица —
> `grpcsrv.NewServer` **плюс значения интерсепторного типа**; вторая половина закрывает путь
> «перестал звать конструктор, но цепочку собираю и передаю дальше». Это строже §5 п.9, а не мягче.
>
> **Что осталось (единственное), с числом и координатами.** Обход гейта кончается на
> `services/*/cmd/**`, и **край в него не входит**. Два живых вызова конструктора на крае
> воспроизведены (П2 держится на этой ревизии): `gateway/internal/proxy/server.go:111` и
> `gateway/cmd/api-gateway/internal_grpc_listener.go:108`. Сегодня они не находки и не записи — они
> **невидимы**, а §9 п.2 этого же документа запрещает ровно такое состояние словами «Молчаливое
> „гейт их не видит“ запрещено». Оговорка, чтобы не завысить остаток: край не вовсе бесхозен — гейт
> звена паники выводит область как `services/*/cmd` **плюс `gateway/cmd`**, — но его предмет
> восстановление паники, а не владение цепочкой.
>
> **Вторая половина того же остатка — восемь файлов не-gRPC поверхности, и она уже просрочена.**
> §9 п.2 требует, чтобы **между Ф5 (гейт вооружён) и Ф8** у каждого из восьми было **явное**
> состояние: либо профиль уже поднимает файл, либо он стоит записью с причиной и предикатом снятия.
> Гейт вооружён — значит обязательство действует **сейчас**. Ни одна из двух ветвей не выполнена:
> П11 держится (**8** файлов на `5cad2f9e0`, предикат §2.1), все восемь **входят** в обход гейта
> (они среди осмотренных **57**), но не судятся им — единица гейта `grpcsrv.NewServer` плюс значения
> интерсепторного типа, а `http.Server{}` не то и не другое. Резче всего это видно на двух
> **переведённых** сервисах: `services/geo/cmd/kacho-geo/observability.go` и
> `services/storage/cmd/storage/serve.go` — их gRPC-контур внутри носителя, а не-gRPC слушатель рядом
> не описан ничем. Здесь же второе место об одном предмете: §16 (строка «C1 носитель») описывает
> реестр как «**2** записи края **плюс 8** файлов не-gRPC поверхности», а клетка Ф5 — только как две
> записи края. Верно §16; клетка называла меньше, чем требует §9 п.2.
>
> **Закрытие остатка требует решения, которого этот перемер не принимает,** и обе стороны дают
> один и тот же артефакт — запись с письменной причиной и предикатом снятия, — но разный текст
> причины: либо край берёт Ф8/ось паритета XC-6 L2 (тогда предикат снятия — «вызова больше нет»),
> либо цепочка края объявляется **намеренно своей** навсегда (§1.2: край и сервис энфорсят оба, и
> это осознанное расхождение — тогда предикат снятия «появился третий вызов вне записей»). Молчание
> не является третьей стороной.
>
> **Отдельно — дефект самой клетки, а не работы:** её условие полноты («исключений ноль») было
> условием **Ф3**, а не Ф5. Пять записей реестра — это ровно пять непереведённых сервисов (iam, vpc,
> compute, nlb, registry; переведены geo — `services/geo/cmd/kacho-geo/serve.go:129` и storage —
> `services/storage/cmd/storage/serve.go:268`), и обнулить их может только перевод, то есть Ф3.
> Клетка, чьё условие полноты держит чужую фазу, **не может быть закрыта своей работой** — и пока
> это не замечено, читается как «Ф5 не начата», хотя её собственный предмет стоит в дереве.
>
> ---
>
> **Этот перемер закрыт волной 2026-08-11 — перепроверено 2026-08-12 на `4d26e3300`.** Всё выше
> остаётся верным **на `5cad2f9e0`** и не переписывается; ниже — что стало иначе, обеими
> ревизиями:
>
> - **решение, которого перемер не принимал, принято**: край взял **вторую** сторону — цепочка
>   края объявлена намеренно своей, потому что форма прокси носителю **невыразима**. Две записи
>   стоят в перечне гейта с письменной причиной, а основание истекает от **внешнего** факта
>   (появления у дескриптора поля обработчика неизвестной службы) и держится отдельной пробой
>   `TestCarrierStillCannotExpressAProxyEdge`. Вызовов у края по-прежнему **2** (Д10) — изменилось
>   не число, а то, что они перестали быть невидимыми;
> - **вторая половина остатка потеряла предмет**: П11 на `4d26e3300` даёт **0** (Д5) против **8**
>   на `5cad2f9e0` (Д6). Названные здесь два переведённых сервиса поднимают свои не-gRPC
>   слушатели профилем носителя (Ф8), а не своей сборкой;
> - **условие полноты, державшее чужую фазу, обнулилось само**: записей «ожидают перевода» в
>   перечне гейта — **0**, потому что непереведённых сервисов в контуре не осталось (Ф3);
> - **расхождение с §16 снято**: §16 описывал реестр как «2 записи края плюс 8 файлов», клетка —
>   как две записи; сегодня верны обе половины первой формулировки только в части края, а
>   восьмёрка предмет потеряла. §16 приведён к этому;
> - **и одна ссылка вела не туда**: цитата «Молчаливое „гейт их не видит“ запрещено» приписана
>   выше §9 п.2 — она стоит в **§8.3 п.2**. §9 п.2 говорит об аннотации, называющей не то
>   отношение. Мелочь, но того же рода: координата, которую никто не открыл.

> [!note] Здесь стояли три разных разбивки, и ни одна не выводилась из таблицы
> Шапка получала «три целиком», засчитывая каждую клетку, чья ячейка начинается словом
> «ИСПОЛНЕНА», — то есть считала СЛОВО, а не исход, при том что две из трёх несут в той же строке
> явный ✗; и тут же считала Ф2 второй раз как «одну на 6/7». «Четыре не начаты» занижало остаток
> на две фазы. Этот итог писал «две целиком» и сам себя опровергал оговоркой «в части».
>
> Документ объявляет разбивку дороже одного числа именно потому, что одна колонка «была бы честна
> ровно до одной даты и с тех пор молча завышала объём предстоящего». Здесь та же величина остаток
> ЗАНИЖАЛА — в строке, озаглавленной «Что осталось и почему», то есть в единственном месте, которое
> прочтёт планирующий. Предикат теперь один и назван: колонка «Полна, когда». Считать это «половиной сделано» нельзя:
исполненное — то, что держится **компилятором и единственностью источника**, а неначатое — ровно
**носитель**, ради которого документ и писался. То есть сделана та часть, которую можно было
сделать **типами, не трогая сборку цепочки**, — и это в точности предсказано дискриминатором §8.1
(тип держится без сборщика, единственная реализация — нет). Дискриминатор не только разрешил спор
о порядке, но и **предсказал, что именно уедет вперёд**, — и это его сильнейшее свидетельство.

**Ф1 первой среди ОСТАВШИХСЯ структурных, потому что она носитель**: в её поля въезжают шаги XC-6 6, 8, 9. В обратном порядке каждый из них останется значением, которое сервис волен не использовать — и Ф4 это уже **показала на дереве**: единственная реализация сужателя есть, а обязанности её взять нет.

**Первым переводится сервис с нулём сужателей — решено (в-9).** Тогда Ф1 не тянет за собой Ф4 и остаётся полной сама (ban #14). Отвергнутая сторона — начать с самого молодого, чей контур измерен по десяти осям, — сохранена в §13 в-9 вместе с тем, чем она была сильна: перевод сразу проверил бы носитель на худшем случае.

### 8.2 Чем доказывается, что старый путь ИСЧЕЗ

Не «мы им не пользуемся», а три независимых доказательства:

1. **Компилятор.** Импорт сборщика из сервиса → `go build ./...` отказывает. Обходится только перемещением сервиса внутрь поддерева фундамента, что видно в диффе как перемещение каталога.
2. **Гейт с проверкой СВОЕЙ предпосылки.** Он (а) утверждает, что сборщик **не экспортирован** из общего фундамента — вернут наружу, гейт краснеет **на предпосылке**, а не молчит; (б) обходит `services/*` и край по AST, требуя ноль ручных сборок; (в) печатает объём переписи.
3. **Инъекция в обе стороны** — по каждому гейту §6.

**Самоистечение.** Список исключений один — сужатели, и он истекает сам в **обе** стороны (О3/О4). Реестр прямых конструкторов — тоже: запись, которой нечего исключать, находка.

### 8.3 Что ломается при переводе — проверять ДО, а не после

1. **Наблюдаемое поведение меняется у шести сервисов**, получающих верхнюю границу обработки. Она делает видимым то, что сегодня молчит; e2e-кейс обязан быть написан **до** правки, иначе «стало иначе» неотличимо от «сломалось».

   > [!note] Здесь стояло «у трёх сервисов, получающих звено восстановления» — предмета больше нет
   > Замер базы («4 из 7 несут звено, трое получат впервые») был верен на `cf69249ed` и остаётся
   > верным на ней. В стволе звено сведено в одно на платформу (`a99750f87`, §2.4), интерсепторных
   > копий у сервисов **0**, и «получают впервые» получили — **вне** этого плана и раньше него.
   > Оставленная строка ставила бы работу, которой нет, а вместе с ней требовала e2e-кейса «до
   > правки», которой не будет: то есть производила **долг из ничего**. Что осталось предметом:
   > верхняя граница обработки, **1 из 7** на обеих ревизиях, — и это единственная половина пункта,
   > которую перемер подтвердил.
2. **Запрет конструирования сервера в сервисах ломает всё, что монтирует не-gRPC поверхность** — **8 файлов** (П11), из них у двух сервисов на этих слушателях принимаются **решения об аутентификации**. **Решено (в-1): отдельный профиль той же функции, фаза Ф8.** Это значит, что между Ф5 (гейт вооружён) и Ф8 у восьми файлов обязано быть **явное** состояние: либо профиль уже поднимает их, либо каждый стоит **записью в реестре исключений гейта** с причиной и предикатом снятия. Молчаливое «гейт их не видит» запрещено — иначе самая security-нагруженная поверхность остаётся вне контура, будучи объявленной внутри него.

   > [!note] Окно между Ф5 и Ф8 закрыто — перемер 2026-08-12
   > Требование верно и остаётся нормой **для следующей такой пары фаз**; его собственный
   > предмет исчерпан. **8** файлов — замер на `5cad2f9e0` (Д6), на `4d26e3300` предикат П11
   > даёт **0** (Д5): все поверхности поднимает профиль носителя, реестр исключений его гейта
   > пуст, находок ноль. Оставленная в настоящем времени, строка ставила бы в очередь работу,
   > которой нет, — и требовала бы «явного состояния» у файлов, которых больше не существует.
3. **У одного сервиса в цепочке есть звенья, которых нет ни у кого.** Если они не выражаются полем — **неверен дескриптор**, и это ловится на Ф1, а не после.
4. **Вывод карты «как есть» решил бы пять расхождений реестра в пользу каталога**, а две из них устроены так, что сервис спрашивает **пообъектное** отношение, а каталог — кластерное; автоматический вывод **расширил бы** сервисную сторону — ровно «унификация по самой снисходительной семантике». **Решено (в-7): права сторона сервиса там, где он строже; каталог правится под сервис.** Поэтому у Ф2 не оговорка и уже не «предусловие-ожидание», а **работа с названным местом красного**: гейт «выведенное равно рукописному» обязан покраснеть ровно на этих пяти записях, и правка каталога стоит **до** переключения (XC-6 §6 шаг 4). Сведение к кластерному наблюдателю запрещено прямо.
5. **Инверсия семантики пустого круга ОТМЕНЕНА (в-6)** — и вместе с ней снят весь этот класс поломки: внутрипроцессные фикстуры, полагавшиеся на пустоту, **не ломаются**, перечень их устанавливать не нужно. Что остаётся от пункта: перенос величины в тип — сам по себе безопасен, потому что смысла нуля не меняет; а защита от забытой настройки держится отказом старта (О1), см. §5 п.8а. **Проверено исполнением, а не рассуждением:** Ф0т в стволе исполнена (`7f03e0bae`, `5573aa87f`), и решение подтвердилось — переезд величины в тип прошёл без ломки фикстур, потому что смысл нуля остался прежним. Цена, которую при этом заплатили и которую прежняя редакция не предвидела: понадобился **явный опт-ин** на несужение вне боевого режима, иначе страж, сработавший на **каждом** старте, остановил бы локальные посадки. Опт-ин — новая поверхность, и она названа (§9 п.12), а не оставлена в цене фазы.
6. **Ветка «цепочка без звена решения в dev»** в носителе не выражается: `Serve` либо собрал, либо отказал. **Решено (в-8): снести.** Локальный стенд без iam перестанет подниматься — это и есть цель `security.md` §Production-mode ВЕЗДЕ, а не побочный ущерб. Проверять **до** правки: чем поднимается локальный стенд сегодня и что именно перестанет работать у того, кто им пользуется как приёмом отладки; замена приёма (поднять iam вместе со стендом) обязана существовать **раньше**, чем ветка снята, иначе решение исполняется остановкой чужой работы.
7. **Порядок при правке модели прав обязателен**: расширить приём → сменить эмиссию → сузить. Правка мятит идентификатор модели и требует переката — **один раз, на свободном стенде**.
8. **Половинчатый перевод — самая дорогая ошибка.** Пока часть сервисов на носителе, а часть нет, поверхностей правки **две**, и старая **ненаблюдаема**. Поэтому Ф5 — **один** коммит на всё дерево, а не «по сервису».

Изменений схемы БД — **ноль**. Изменений публичного контракта — **одно** (аддитивный признак первой доставки, XC-6 шаг 9).

---

## §9 Что ОСТАНЕТСЯ возможным сделать неправильно

Раздел обязателен: замысел, после которого «ошибиться нельзя нигде», не бывает, а **неназванная остаточная поверхность — это та, которую никто не будет проверять**.

| # | Что можно сделать неправильно | Чем ловится | Остаточный риск |
|---|---|---|---|
| 1 | **Аннотация называет не то поле** — на object-self RPC указано поле проекта вместо поля объекта. Поле существует, строковое, вывод карты проходит | **только конформанс под обычным тенантом**: субъект с пообъектным грантом получает отказ, субъект с грантом на проект — проходит там, где не должен | **высокий и неустранимый носителем.** Администратор кластера удовлетворяет оба отношения сразу ⇒ дефект виден **только** обычному тенанту |
| 2 | **Аннотация называет не то отношение** — ярус вместо глагола | гейт «глагол на object-self»: строка каталога, чей тип объекта не проектный и не кластерный, обязана нести глагол. Вход — каталог, производитель есть | низкий после гейта; сегодня не покрыт |
| 3 | **Отношение выполнимо подстановкой** — справочное отношение на RPC, который справочником не является | существующий гейт по модели + требование письменного обоснования на строку | средний: обоснование пишет человек |
| 4 | **Сужатель предъявлен, но отвечает неверно** (возвращает всё) | носитель требует **наличие**, не правильность → конформанс + пообъектная проба с дублёром, **не снисходительнее настоящего** | средний |
| 5 | **Новый тип объекта смоделирован, но отношения выведены неверно** (каскад там, где его быть не должно) | ничем механическим — это решение о модели | **высокий.** Предмет iam, не носителя |
| 6 | **Профиль развёртывания не задаёт ручку**, под не перекатывается, boot-guard не запускается — **потому что старта не было** | гейт посадки по **живому процессу** и по шифрованию со стороны БД | средний: «под Ready» доказательством посадки **не является** |
| 7 | **Двухобъектный RPC** — спросили про один адресат из двух | подъём пообъектной пробы одного сервиса на уровень дерева | низкий после подъёма |
| 8 | **Сервис заведён вообще мимо носителя** — свой корень, свой сервер | A12 + отсутствие способа построить карту | низкий: **обходной путь длиннее прямого**, но он существует |
| 9 | **Порядок звеньев внутри общей функции изменён неверно** | ничем, кроме обзора: функция одна, но она правится | низкий по частоте, высокий по радиусу — **единственное место, где ошибка бьёт во все семь сразу** |
| 10 | **Дескриптор объявляет «не применимо» там, где каталог молчит** (домен без строк нужной полосы) | не ловится: судить нечем | низкий; назван, чтобы не считался закрытым |
| 11 | **Значение оси выбрано неверно** (окно кэша, бюджет, партия) | ничем механическим: значение законно | средний — предмет обзора, для этого оно и стало **видимым** |
| 12 | **Небоевая посадка поднята с испрошенным опт-ином на несужение круга.** Опт-ин объявлен у каждого сервиса, вне боевого режима снимает отказ старта О1, в боевом не читается | **правилом** «production-mode ВЕЗДЕ, включая локальный стенд» + гейтом посадки по **живому процессу** (не по объекту настроек: правка настроек без переката пода оставляет процесс с прежним окружением) | **средний и назван поздно.** Это **моя собственная ось владения** (XC-6 L1), и до третьего круга поверхность на ней не была названа ни одной строкой §9 — при том что §5 п.8а объявлял свойство держащимся **построением**. Класс «гейт, выданный за свойство построения» найден в документе, который его же и запрещает (§15 критерий 7) |

**Итог по остаточному риску честно:** носитель закрывает **пропуск** шага и **неверное значение**, но не закрывает **неверное содержание** аннотации и **неверную семантику модели**. Из десяти осей, на которых обжёгся самый молодой сервис, конструкцией закрываются **семь**, ещё **две** переходят на гейты с настоящим производителем, **одна** остаётся человеческим решением с конформанс-проверкой.

**И одна поверхность добавлена не разбором сервиса, а разбором этого документа** (п. 12). Отсюда
следствие для формы §9, которое дороже самого пункта: раздел остаточного риска обязан
перечитываться **после** каждого понижения уровня в §5. Понижение «это держал тип» → «это держит
отказ старта» → «это держит правило» **производит** остаточную поверхность по построению, и если
§9 не прибавил строки, то либо понижение не записано, либо поверхность не названа.

---

## §10 Отрицательные результаты — сохранены намеренно

Отрицательный результат, вычеркнутый ради стройности, **вернётся работой**.

1. **Типизация процесса (typestate) на порядке «личность → область → право» — церемония.** Порядок **уже** невыразим иначе: реализация одна, ни один сервис её не пересобирает, подменных извлекателей субъекта в проде **0** (П13). Расщепление одной функции на три фазы дало бы свойство, которое у неё уже есть, — «форма без содержания» в чистом виде. Typestate работает **только** на сборке цепочки, и там достаточно трёх функций, а не дробной формы.
2. **Опция подменного извлекателя субъекта — расширительная точка без потребителя.** Производителей в проде **0**, а ради неё в проде живут защитные ветви, чей собственный комментарий говорит, что они «для подменённой реализации». Честных исходов два: закрыть типом или **снять опцию целиком**. Рекомендуется **снять**: тип, введённый чтобы дисциплинировать несуществующего вызывающего, — абстракция с нулём потребителей.
3. **«Свидетельство первой доставки» как ТИП — отвергнуто в предложенной форме.** Требуемое свойство («принимается только в конъюнкции с вердиктом приёмной стороны по данным») **уже выполняется, и сильнее, чем дал бы тип**: вход быстрого пути — вердикт собственного SQL приёмной стороны, а запрос **вообще не несёт поля**, которым это можно было бы сообщить. То есть «не слово вызывающего» держится **отсутствием слота**. Завести тип свидетельства значит **завести провод**, по которому это слово можно будет сказать. Вместо него типизируется соседняя величина — **происхождение маркера версии** (§4.4).
4. **Расширяемость контура (именованные точки вставки) — специфицирована, но НЕ строится.** Единственный кандидат в потребители — один сервис, и его нужды выразимы **данными** (потолок яруса уже данные каталога; политика вызывающего — множество RPC). Предикат заведения назван: **появился второй сервис, чьё звено не сводится к строке каталога**.
5. **Генерация регистрации обработчиков из proto — отвергнута.** LEAN: потребитель один, машина велика, а интерфейс с единственным методом уже отнимает доступ к цепочке. Сверх этого генерация закрывает лишь «сервис зарегистрировал не то, что объявил», а это ловится О2.
6. **Дескриптор файлом (YAML/proto) — отвергнут.** Он стал бы **третьим** источником при двух побайтово равных копиях каталога и гейте дрейфа между ними; и он физически не несёт половину осей — креды, порты, пробы суть Go-значения, поэтому файл всё равно пришлось бы сшивать с проводкой, **а шов и есть место дрейфа**.
7. **Механического детектора «сборки координата+условие+следствие» для публичного текста НЕТ** — измерено, а не предположено: блочный лексиконный предикат провалил контроль **в обе стороны** на размеченном наборе. Работает перечисление с доказательством на пункт.
8. **Единый состав кругов отправителей — отвергнут** (XC-6 §4 п.4): сужение сломало бы законную передачу личности инициатора на одном из рёбер; расширение — расширяет доступ.
9. **Сведение окна отзыва 2s к 5s — отвергнуто:** это **строже** потолка, то есть намеренно узкая клетка; свести «заодно» значило бы расширить.
10. **Пообъектный опрос одного сервиса (партия 1 при веере 8) — НЕ квалифицирован.** Отличие от партии 100 при веере 5 может быть решением, а может быть дефектом; в дереве причина не записана. Обобщать не установив — ровно тот класс, от которого предостерегает бриф. Ф4 имеет по нему **предусловие чтения**, а не догадку.
11. **Общий порт материализации — сегодня невыразим** (XC-6 L5: пять форм подписи). XC-7 даёт только тип маркера версии; сведение подписи — отдельный предмет со своим замером.
12. **Ограничитель одновременности перед авторизацией — НЕ вводится в Ф1.** Позиция в порядке зафиксирована, чтобы при введении звено не встало не туда, но само звено требует **измеренной** ёмкости модели: потолок, выведенный неверно, превращает нагрузку в отказы на **положительном** пути. Введение — отдельная под-фаза с нагрузочным контролем. **Владелец числа назван решением в-4 (2026-08-09): потолок — ОДНО число во внешнем фундаменте**, выведенное из ёмкости модели; не семь чисел по сервисам. Предикат пересмотра — **изменение посадки iam**. Это снимает вопрос «чья ёмкость», но **не** снимает требования измерить её: число без замера, лежащее в фундаменте, — то же неверное число, только в одном экземпляре.
13. **Разница «шагов 9→5» при доработке — умеренная**, и выдавать её за большую нельзя (§7.3).

---

## §11 Где шесть проектов противоречат друг другу — оба названы, разрешение обосновано

| # | Предмет | Сторона А | Сторона Б | Разрешение и основание |
|---|---|---|---|---|
| **К-1** | что делает цепочку несобираемой | «тип возврата — система типов, компилятор» | «языком не достигается; работает единственный производитель + гейт по дереву» | **Оба верны о разном.** Разложено на **три уровня** (§5): правило `internal/` и тип регистрации — компилятор, **абсолютно**, для наших средств; вызов upstream-конструктора — **гейт**, потому что импорт чужого пакета язык не запрещает. Выдавать третий уровень за первый — та же ошибка, что в исходной формулировке тезиса |
| **К-2** | домен: объявляется или выводится | «`Domain string` / `ProtoPackages []string` в дескрипторе» | «выводить из зарегистрированных дескрипторов сервисов» | **Выводится.** Записывающий регистратор уже снимает имя сервиса, чей префикс и есть proto-пакет. Объявление вводит второй источник, опечатка в котором молча выбирает **ноль** строк каталога; отказ старта (О9) это поймает, но диагностика хуже, а второго источника не должно быть в принципе |
| **К-3** | карта прав: откуда берётся | (А) «генерация Go-файла на сервис: компилятор проверяет имена полей, рантайм не зависит от целостности вшитого» | (Б) «выбор строк при старте из вшитого **файла** каталога» | **Ни А, ни Б — принята ТРЕТЬЯ сторона, которой не называл никто: вывод из АННОТАЦИЙ дескрипторов proto, слинкованных в бинарь** (landed `ca3354d94` → `569bea84c`). Разбор — врезка под таблицей |
| **К-4** | явное «не применимо» с причиной для всех осей | «`Axis[T]` на каждую ось» | «обычные поля + отвергающий конструктор + линтер» | **Гибрид по признаку.** `Axis[T]` — только там, где **пустое значение законно** (эмиссия, регистрируемые типы, списки, формы скрытия) — их четыре; остальные — обязательные поля. Иначе обёртка на восьми полях в семи объявлениях становится церемонией (LEAN) |
| **К-5** | второй источник для iam | XC-6 шаг 4: **описательная карта прав**, сверяемая с каталогом | XC-7: **объявление `Lists` в дескрипторе** | **Решено владельцем 2026-08-09 (§13 в-5) — ни А, ни Б: карта ВЫВОДИТСЯ из каталога у всех семи.** Спор был о том, какой второй источник завести; ответ — **не заводить никакого** и снять существующий у шестерых. Разбор сторон сохранён, потому что он и есть довод за решение: описательная карта — артефакт, **который никто не исполняет**, поэтому ловит дрейф каталога от объявления, но не дрейф объявления от поведения; `Lists` **исполняется** носителем, но остаётся вторым объявлением того, что уже объявлено каталогом. Оба варианта решали бы одну задачу двумя механизмами — решение выбирает **один** |
| **К-6** | позиция ограничителя одновременности | «сразу после срока, до личности» | (куб) «после установленной личности, до авторизации» | **После личности.** Ограничитель обязан классифицировать по **установленному субъекту**, иначе один шумный тенант делит корзину со всеми. Плюс §10 п.12: в Ф1 звено не вводится вовсе, а **владелец его числа назван решением в-4** — фундамент, одно число, предикат пересмотра — изменение посадки iam |
| **К-7** | брать ли не-gRPC слушатели в контур | «самая security-нагруженная поверхность, сегодня вне всех осей» | «цепочка там другая; втягивание делает дескриптор объектом-богом» | **Решено владельцем 2026-08-09 (§13 в-1): отдельный профиль ТОЙ ЖЕ функции (Ф8), в общий дескриптор не втягивать.** Промежуточный вариант выбран потому, что сохраняет «одна функция, разные данные» и не раздувает дескриптор; довод стороны Б («объект-бог») принят как **критерий простоты**, довод стороны А (поверхность вне всех осей) — как **обязанность взять предмет**, а не оставить его вне охвата |
| **К-8** | writer / приёмка параметром записи | «сильнейший перенос из куба, брать» | «предмет — путь записи, а не контур iam» | **Вынесено в XC-8** (§12). Внутри XC-7 остаётся только его iam-обращённая часть: маркер версии из штампа транзакции, захват личности и намерение — в **той же** транзакции. Иначе XC-7 стал бы вторым нормативным местом для `data-integrity.md` |
| **К-9** | порядок относительно плана XC-6 | XC-6 §6: 12 шагов, носителя среди них нет | XC-7: носитель первым, шаги XC-6 въезжают полями | **Разрешено признаком §8.1** (тип держится без сборщика, единственная реализация — нет) и **подтверждено решением в-11**: шаги 0-2 XC-6 идут независимо, **начиная с шага 3 план ОДИН**. Держать два значило бы перерезать одну работу по разным осям — два места об одном предмете |
| **К-10** | бюджет полосы валидации | «5 c — предсказуемый путь запроса» | «30 c — переживает перекат пода соседа» | **Решено владельцем 2026-08-09 (§13 в-10): 5 c у всех семи, с отдельным сроком попытки**; и **кэш положительного существования проекта СНЯТ**. Самая узкая семантика из существующих; перекат соседа лечится дренажем, а не путём запроса |

> [!important] К-3: третья сторона — и почему обе первые сохранены, а не вычеркнуты
> **Чем была сильна А (генерация Go-файла).** Единственный вариант, где имена полей проверяет
> **компилятор**, а не старт: ошибка в аннотации не доживает до сборки образа. Довод настоящий и
> сегодня не выполнен — принятая форма ловит такую ошибку **на старте**, fail-closed, то есть
> позже. Проиграл он арифметике артефактов: семь сгенерированных файлов — семь проверок несвежести,
> каждая со своим гейтом, и каждая может не выполниться, потому что генерация — шаг сборки.
>
> **Чем была сильна Б (чтение вшитого файла).** Каталог уже существует, уже вшит дважды и уже
> удерживается побайтовым гейтом — то есть механизм присмотра **не надо изобретать**, он написан и
> роняет сборку. Самый дешёвый и самый проверенный путь.
>
> **Почему выиграла третья.** Файл каталога **сам генерируется** из тех же аннотаций. Читать его —
> выводить производное из производного, и ценой **третьей вшитой копии** (край и сид iam несут по
> одной), а копии расходятся. Дескрипторы уже лежат в бинаре: сервис линкует стабы тех RPC, которые
> обслуживает, и стабы несут опции дословно. Отсюда три следствия, ни одно из которых не давали ни
> А, ни Б: **новых артефактов ноль** (замер: экземпляров файла каталога **2** на обеих ревизиях);
> вывод **не может устареть**, потому что не является артефактом; а перепись «каталог ==
> аннотации» из тавтологии превращается в настоящую сверку **генератора**. Довод А не отброшен —
> он переведён в форму отказа старта: четыре состояния собранного бинаря (несуществующее поле ·
> несвязанный пакет · задвоенный ключ · метод без аннотации) отказывают в старте, а не на первом
> запросе.
>
> **И это находка о ДВУХ документах, а не об одном.** Форму «вывод из строк вшитого каталога»
> несут **обе** стороны шва: она стоит и здесь, и в XC-2 §3.5, куда попала тем же решением в-5.
> То есть утверждение пережило свой предмет **синхронно в двух местах**, и это ровно тот случай,
> когда «два места об одном предмете» не спасают друг друга: они согласованы между собой и оба
> разошлись с деревом. Правку XC-2 ведёт его владелец — здесь она **названа**, а не сделана
> (вторая правка одного файла затирает первую).
>
> **Отдельно — про заявленный конфликт с XC-2, которого нет.** Ревью третьего круга проверяло
> утверждение «XC-7 конфликтует с XC-2 про карту прав». Утверждение **не подтвердилось предметом**:
> XC-2 упоминается в теле XC-7 четыре раза, и все четыре — раздача владения (§0 таблица, §12 п.2,
> §12 п.3), ни одного — как спор. Конфликт по этой оси был и **закрыт решением в-5**, после чего
> XC-2 §3.5 перенёс отвергнутую сторону («второй выход плагина») в свою врезку дословно и сослался
> на этот раздел. Записываю это как **находку**, а не как повод молча удалить строку: утверждение
> о конфликте пережило свой предмет, и, не будучи перемеренным, оно поставило бы в очередь работу
> по сведению двух документов, которые уже сведены. Ровно этим опасны утверждения о **чужих**
> документах — их проверяют реже собственных.

---

## §12 Что вне охвата — чей это предмет

1. **Каноны семи осей, эталоны клеток, 20 намеренных отступлений, 9 находок** — **XC-6**. XC-7 ссылается и трактует их как значения полей.

   > [!note] Здесь стояло «21 отступление, 7 находок» — та же величина, что в §0, но на **два** пересчёта старше
   > Документ нёс её в **трёх** местах и в **двух** разных редакциях одновременно: §0 и §2.3
   > говорили 20/8, §12 — 21/7, а источник (XC-6 §4 и §4а) на тот момент — 20/9. Три пары для одной
   > величины, ни одна не полна, и это при том, что §15 критерий 2 **этого самого документа**
   > требует сверять число с источником при каждом касании. То есть собственный критерий не
   > выполнялся в собственном теле, и не выполнялся он **тише всего** именно там, где число стоит
   > мимоходом, в перечислении чужого владения. Урок дороже трёх цифр: **число, перенесённое в
   > раздел «вне охвата», перестаёт кем-либо перечитываться** — его никто не считает своим.
   > Предикат пересчёта — врезка §0; сверять им, а не памятью.
2. **Токен полосы как контракт** — **XC-1**; **библиотека интеграции, очередь, материализация** — **XC-2**; **глаголы принадлежат типу** — **XC-3**.
3. **Писатель (валидация параметром записи, захват личности, намерение и маркер в одной транзакции как ОБЩАЯ форма)** — **XC-8**, отдельная приёмка. Это сильнейший перенос из куба (§1.4 неточность 2), и он **не отменён, а вынесен**: его предмет — путь записи, владелец инвариантов — `data-integrity.md`.
4. **Модель прав как политика** — три уровня супер-доступа, каскад против плоской материализации, кто вправе снять чужую выдачу — `security.md`. Дескриптор объявляет, **чем** сервис говорит; **что это значит** — не его вопрос.
5. **Не-gRPC слушатели** — **8 файлов** (П11), из них у двух сервисов на этих поверхностях принимаются решения об аутентификации. Ни одна из семи осей XC-6 их не меряет, и цепочка §4.3 их не покрывает. **Владелец взял предмет 2026-08-09** (§13 в-1): они входят в контур **отдельным профилем той же функции** — фаза Ф8. То есть пункт остаётся здесь не как «вне охвата навсегда», а как **вне Ф1-Ф7**: у него есть фаза, число мест и условие полноты. Строка сохранена, потому что до Ф8 эта поверхность действительно не покрыта, и притворяться обратным нельзя.
5а. **Самодельный админ-гейт на внутреннем слушателе одного сервиса** — **решено снять** (§13 в-12), но **отдельной приёмкой**, а не побочным эффектом унификации: снятие меняет, кто вправе звать мутации, и это решение о доступе, а не об оформлении контура.
6. **Аудит-журнал как продукт** — вторая стадия по `Operation.id` названа требованием (§1.7), но её схема, хранение и срок — отдельный предмет.
7. **Клиенты к внешним системам** (**42 файла**, И-ПРО: хранилище прав, провайдер токенов, реестр образов) — иная полоса: там нет ни нашего каталога, ни наших токенов.
8. **Сведение формы подписи регистратора** — общий порт сегодня невыразим (XC-6 L5).
9. **Нагрузочная характеристика** — вне охвата; XC-7 фиксирует лишь, что величина обязана быть **видна снаружи** там, где по ней принимается вердикт.

---

## §13 Решения владельца — приняты 2026-08-09; открытых вопросов не осталось

Раздел назывался «Открытые вопросы владельцу» и содержал двенадцать, порождённых **носителем**
(шесть вопросов XC-6 §9 здесь не повторялись и решены **там**, той же датой). **Все двенадцать
решены владельцем через оркестратора 2026-08-09.**

**Формат един, и отвергнутая сторона в нём обязательна.** Решение, из которого её вычеркнули,
через месяц читается как единственно возможное — и тогда его пересматривают с нуля, не зная
довода, который однажды его перевесил. Каждая запись несёт: **предмет · что решено · почему (по
критериям владельца: производительность · безопасность · скорость реализации · простота) · что
отвергнуто и чем это было сильно · обратимость · чем держится.**

**Два решения меняют конструкцию, а не только план,** и это отмечено здесь, а не спрятано в теле:
**в-6** отменяет заявленную инверсию семантики пустого круга (утверждение переезжает с уровня
языка на уровень отказа старта — §5 п.4 и п.8а), **в-5** отменяет объявление `Lists` в дескрипторе
(§4.1) и отнимает потребителей у формы с явным отступлением (§4.5).

> [!important] Четыре решения ИСПОЛНЕНЫ в стволе, и два из них исполнение уточнило — записи правятся, решения нет
> Состояние на `92ca6af9d`: **в-5** исполнено у 6 из 7 · **в-6** исполнено · **в-7** — предмет
> перешёл в работу Ф2 · **в-10** частью исполнено (кэш существования снят вместе с картой). Записи
> §13 приведены к этому, и в двух местах **обоснование** пришлось поправить, а **решение —
> нет**, и разница названа по месту:
>
> - **в-5**: выбранная владельцем **единственность** источника подтвердилась, а названный им
>   **носитель** («строки вшитого каталога») оказался лишним звеном — дерево вывело карту из
>   аннотаций дескрипторов. Решение сильнее своей формулировки, поэтому правится формулировка.
> - **в-6**: «посадки без стража не бывает by construction» опровергнуто **исполнением самого
>   решения** — стражу понадобился явный опт-ин вне боевого режима. Решение то же, обоснование
>   другое, остаточная поверхность названа (§9 п.12).
>
> Общее у обоих и есть урок: **решение проверяется не в момент принятия, а в момент исполнения**, и
> проверяется в нём именно то, что при принятии казалось второстепенным, — не «что решить», а «чем
> это будет держаться». Запись, у которой есть часть «чем держится», это переживает; запись без неё
> после исполнения читалась бы как ошибочная целиком.

### в-1. Не-gRPC слушатели

**Предмет.** Берёт ли контур восемь файлов не-gRPC поверхности, у двух сервисов принимающих
решения об аутентификации.

**Что решено.** **Берёт — ОТДЕЛЬНЫМ ПРОФИЛЕМ той же функции** (Ф8). В общий дескриптор их не
втягивать.

**Почему.** *Простота (решающий довод владельца):* объект-бог есть оверинжиниринг — дескриптор,
несущий поля обеих поверхностей, перестаёт быть закрытым набором осей и становится свалкой.
Принцип «**одна функция, разные данные**» уже применён к профилю края (§4.3) и здесь просто
**распространяется** — это не новая конструкция, а второй потребитель существующей.
*Безопасность:* поверхность перестаёт быть вне всех осей и вне всех гейтов, но получает **свои**
формы отказа и свой аудит, а не чужие. *Скорость:* Ф1 не растёт.

**Что отвергнуто и чем это было сильно.** (а) Втянуть в общий дескриптор — сильно тем, что тогда
поверхность закрывается **сразу** и одним механизмом, без отдельной фазы, которую можно не
доделать. (б) Оставить вне охвата — сильно честностью: у контура другая цепочка, и объявить
покрытие, не имея его, хуже, чем не покрывать. Оба довода учтены в форме решения: предмет **взят**
(против (б)), но своим профилем (против (а)).

**Обратимость.** Высокая в обе стороны, пока профиль — данные: слить профили обратно в один
дескриптор или, наоборот, отложить Ф8 можно без правки контура. Необратимым делает только одно —
**объявить поверхность покрытой раньше Ф8**: тогда следующий читатель перестанет её проверять.

**Чем держится.** Ф8 с названным условием полноты (§8.1) и **реестром исключений AST-гейта**:
между Ф5 и Ф8 каждый из восьми файлов обязан стоять записью с причиной и предикатом снятия
(§8.3 п.2), иначе непокрытость становится невидимой.

### в-2. Личность и право асинхронного хвоста

**Предмет.** Перепроверять ли право в момент эффекта или считать операцию авторизованной в момент
приёмки.

**Что решено.** **Операция авторизована В МОМЕНТ ПРИЁМКИ.** Перепроверки в момент эффекта нет.
Записать это как **решение** — сегодня оно существует как умолчание.

**Почему.** *Простота и скорость:* перепроверка в момент эффекта роняет **длинные** операции
поздно и **невоспроизводимо** (исход зависит от того, когда сработал отзыв), и усложняет
компенсацию: компенсирующий шаг сам нуждается в праве, которого уже нет. *Производительность:*
исполнитель не получает зависимости от модели на каждом шаге. *Безопасность:* окно между приёмкой
и эффектом ограничено сроком операции, а не бессрочно; захваченная личность инициатора и вердикт
лежат **в той же транзакции**, что и операция, поэтому «кто это разрешил» доказуемо.

**Что отвергнуто и чем это было сильно.** Перепроверка в момент эффекта. Сильно тем, что это
семантика, которую **ожидает администратор**: отозвал право — работа в полёте остановилась. Довод
не пустой и не снят: он означает, что у нас отзыв **не останавливает** уже принятую операцию, и
это обязано быть сказано администратору прямо, а не обнаружено им.

**Обратимость.** Средняя. Ввести перепроверку позже можно, но она меняет **контракт операции**:
операция, которая раньше всегда доходила, начнёт падать поздно, и всякая компенсация обязана
уметь работать без права. То есть обратный ход дороже прямого — и именно поэтому решение
записывается, а не остаётся умолчанием: умолчание нельзя оспорить, потому что его негде прочитать.

**Чем держится.** Записью вердикта и личности инициатора в **той же** транзакции, что и операция
(§1.7), и отсутствием у исполнителя пути спросить модель заново — если такой путь появится, он
станет второй семантикой рядом с этой.

### в-3. Второй энфорсер

**Предмет.** Остаётся ли сервис вторым энфорсером или внутренний слушатель энфорсит один раз.

**Что решено.** **Сервис остаётся ВТОРЫМ энфорсером** — действующее правило `security.md`
§«AuthN+AuthZ ВЕЗДЕ»; внутренний периметр не доверенный.

**Почему.** *Безопасность:* защита от бокового движения — единственное, что стоит между
скомпрометированным соседом и данными; «internal = trusted» прямо назван запрещённым допущением.
*Простота:* решение **не** порождает работы — оно подтверждает существующее.

**Что отвергнуто и чем это было сильно.** Энфорсить один раз, как в кубе. Сильно двумя доводами, и
второй серьёзнее первого: (а) двойной вопрос стоит round-trip на каждый внутренний RPC; (б)
двойной энфорсмент **порождает проблему паритета by construction** — если спрашивают двое, они
обязаны спрашивать одно и то же, а это и есть ось L2 со всеми её расхождениями. **Владелец снял
довод (б) не отказом от второй проверки, а решением в-5:** после вывода карты у обоих
спрашивающих **один источник**, расходиться нечему, и паритет перестаёт быть проблемой,
оставаясь свойством. Это и есть содержательная связь двух решений, и она названа, чтобы в-3 не
читалось как «довод проигнорирован».

**Обратимость.** Полная по коду и **запретная по существу**: снять вторую проверку значит вернуть
внутренний периметр в доверенные, что противоречит нормативке; такое решение принимается правкой
`security.md`, а не приёмкой.

**Чем держится.** Правилом (нормативка) и гейтом посадки: цепочка обоих слушателей обязана
включать звено решения, а сервис без него не стартует.

### в-4. Потолок одновременности перед авторизацией

**Предмет.** Чья ёмкость задаёт потолок и кто владеет числом.

**Что решено.** **Одно число во ВНЕШНЕМ ФУНДАМЕНТЕ**, выведенное из **ёмкости модели**. Владелец
числа — фундамент; **предикат пересмотра — изменение посадки iam**. Не семь чисел по сервисам.

**Почему.** *Производительность:* мы защищаем **чужую** ёмкость, и исчерпание модели
конвертируется в fail-closed на **положительном** пути — то есть неверный потолок превращает
нагрузку в отказы там, где всё исправно. *Простота:* одно число вместо семи, каждое из которых
пришлось бы пересматривать отдельно и ни одно — вовремя. *Скорость:* пересмотр — правка одного
места по названному предикату.

**Что отвергнуто и чем это было сильно.** Потолок по ёмкости **своего** процесса, по числу на
сервис. Сильно тем, что своя ёмкость измерима **у себя** и не требует знания о чужой посадке;
общий потолок делает каждый сервис зависимым от чужого числа, и при росте посадки iam семеро
остаются с потолком, который занижен. Довод и породил требование **предиката пересмотра** — он не
отброшен, а превращён в условие.

**Обратимость.** Высокая: число — данные. Дорогим делает не смена значения, а смена **владельца**:
разъехавшись на семь чисел, вернуться к одному уже нельзя без переписи всех семи посадок.

**Чем держится.** Расположением величины в фундаменте (одно место), названным предикатом
пересмотра и §10 п.12: звено **не вводится**, пока ёмкость не измерена — число без замера в
фундаменте остаётся неверным числом, только в одном экземпляре.

### в-5. Второй источник для iam

**Предмет.** Чем подтверждается обещание девяти строк каталога, снимающих проверку на крае:
описательной картой (XC-6 шаг 4) или объявлением `Lists` в дескрипторе (XC-7).

**Что решено — НИ ТЕМ, НИ ДРУГИМ.** **Карта ВЫВОДИТСЯ из каталога у ВСЕХ СЕМИ.** Второго источника
не заводится ни у кого; у шестерых существующий **снимается**. **Один механизм вместо двух.**

**Почему.** *Простота (решающий довод):* спор шёл о том, какой второй источник лучше, — ответ
снимает сам вопрос; там, где источник один, расхождение **невыразимо**, и не нужны ни сверка, ни
её реестр. *Безопасность:* сегодня девять строк снимают проверку на крае под обещание, которое
**нечем проверить**, — после вывода обещание и реализация суть одно. *Скорость:* исчезает 492
строки пакета сверки и семь деклараций карт; работа сводится к одному пакету вывода и
переключениям. *Производительность:* вывод исполняется **один раз при старте**, на пути запроса
ничего не прибавляется.

**Что отвергнуто и чем это было сильно.** (а) **Описательная карта** — сильно тем, что механизм
сверки **уже написан и уже роняет сборку**, а посадка стоила бы одного файла и одной пробы; это
самый дешёвый и самый проверенный путь. (б) **Объявление `Lists`** — сильно тем, что оно
**исполняется** носителем (из него строится проводка) и потому связывает объявление с **поведением**,
чего описательная карта не даёт. Оба довода верны и оба проиграли одному: каждый заводит **второе**
место об одном предмете, а решение выбирает конструкцию, где второго места нет.

**Обратимость.** Асимметричная и названа честно. Вернуться от вывода к рукописным картам
**дорого**: их придётся написать заново у семерых (у iam — впервые), а вместе с ними воскресить
сверку и её реестр. Зато обратный ход почти никогда не нужен: единственный сценарий — расхождение,
которое владелец решил в пользу сервиса и **не согласился** править каталог; для него назван
предикат заведения явного отступления (§4.5), и вводится оно **вместе с первым потребителем**.

**Чем держится.** Единственностью источника (расходиться нечему), отказами старта О2/О3/О9 и
зеркальной парой О3/О4 (проводка против каталога — в обе стороны).

**Исполнено у 6 из 7** (`ca3354d94` → `569bea84c`), и решение подтвердилось по двум пунктам сразу.
Первый: пакет сверки **удалён**, а не оставлен «на всякий случай» — предвидение было верным, у него
не осталось предмета, и, доживи он до вывода, он сверял бы вывод сам с собой и зеленел бы всегда.
Второй, которого решение **не** предвидело и который стоит записать: форма источника оказалась не
той. Решение говорило «из строк вшитого каталога», дерево вывело **из аннотаций дескрипторов,
слинкованных в бинарь** (§4.5, §11 К-3) — то есть выбранная владельцем **единственность** источника
сохранилась и усилилась, а названный им **носитель** источника оказался лишним звеном. Это ровно
тот случай, когда исполнение делает решение сильнее, чем его формулировка, — и потому формулировка
правится, а решение остаётся.

**Открыто:** клетка **iam**. Карты у него нет вовсе, поэтому включение — не переключение, а первое
заведение, и сверить его не с чем: доказывается конформансом (проводка сужателя против строк
каталога, в обе стороны).

### в-6. Семантика пустого круга отправителей

**Предмет.** Инвертировать ли смысл пустого круга в общей библиотеке — «не доверять никому» вместо
действующего «не сужаем».

**Что решено.** **НЕ МЕНЯТЬ.** Семантика библиотеки остаётся прежней.

**Почему.** *Простота и скорость:* инверсия ломает **действующий контракт** общей библиотеки и
внутрипроцессные фикстуры, полагавшиеся на пустоту, — то есть цена платится там, где дефекта нет.
*Безопасность:* защиту даёт **уже существующий отказ старта**, и он есть у **7 из 7**; production-mode
обязателен **и на локальном стенде**. Иными словами, инверсия закрывала бы то, что уже закрыто,
ценой поломки того, что работает.

> [!warning] Здесь стояло «поэтому посадки, где страж не сработал, **не бывает**» — исполнение решения показало, что бывает
> Обоснование опиралось на то, что страж срабатывает всегда. Landed-реализация (`5573aa87f`)
> действительно срабатывает на **любом** старте, а не только в боевом режиме, — и именно поэтому
> ей понадобился **явный опт-ин на несужение**, действующий вне боевого режима: без него страж
> остановил бы локальные in-process посадки, где сужать нечего.
>
> **Решение от этого не меняется, меняется его обоснование** — и разница существенна, потому что
> следующий читатель принимает решение по обоснованию, а не по слову «не менять». Верное:
> посадка, где круг не сужен, **представима** — она требует небоевого режима и **испрошенного**
> опт-ина. Свойство держится не отсутствием такой посадки, а тем, что (а) опт-ин надо попросить, он
> не умолчание, и (б) в боевом режиме он **не читается вовсе**, поэтому ручкой, снимающей защиту на
> боевом стенде, не является. Это правило плюс граница его применения, а не построение (§5 п.8а),
> и остаточная поверхность названа (§9 п.12).
>
> Отвергнутая сторона от этого **усиливается**, и честно это признать: инверсия закрыла бы и её.
> Довод не отброшен — он остаётся ценой решения, а не аргументом против исполненного.

**Что отвергнуто и чем это было сильно.** Инверсия. Сильно и по существу: забытая настройка
становилась бы **глухой**, а не всеразрешающей, и свойство держалось бы **языком**, а не проверкой
— то есть на уровень выше. Довод остаётся верным для **остаточного** случая: процесс, поднятый
мимо контура (§9 п.8), инверсией был бы закрыт, а отказом старта — нет. Этот остаток назван, а не
замазан.

**Обратимость.** Высокая **сейчас** и падающая со временем: пока фикстур, полагающихся на пустоту,
немного, инверсию можно ввести позже; чем дольше живёт нынешняя семантика, тем больше кода на ней
основано. Поэтому решение не «никогда», а «не этим изменением», и цена промедления названа.

**Чем держится.** Отказом старта О1 по **тому же предикату**, который читает транспорт (иначе
повторится класс «страж меряет не то, что транспорт»), гейтом посадки по живому процессу и
градуировкой сужения круга у всех семи.

### в-7. Пять расхождений реестра «карта ↔ каталог»

**Предмет.** Чья сторона права в пяти записанных расхождениях — каталога или сервиса.

**Что решено.** **Права сторона СЕРВИСА там, где он строже. Каталог правится под сервис.**
Сведение к кластерному наблюдателю **запрещено** — оно расширяет доступ.

**Почему.** *Безопасность (решающий довод):* в двух из пяти сервис спрашивает **пообъектное**
отношение, а каталог — кластерное; вывод «как есть» **расширил бы** доступ к состоянию конкретного
объекта — ровно «унификация по самой снисходительной семантике», класс, который мы ловим.
*Простота:* правило одной фразой — «строже побеждает» — вместо разбора каждой записи заново.
*Скорость:* правка каталога дешевле, чем ослабление сервиса плюс компенсирующие проверки.

**Что отвергнуто и чем это было сильно.** Сторона каталога. Сильно тем, что **каталог читает
оператор** и по нему выдаются права: расхождение означает, что выданное по каталогу право не
работает, и человек не понимает почему. Довод не снят — он выполняется **тем же** решением, но в
другую сторону: каталог приводится к сервису, значит оператор снова читает правду.

**Обратимость.** Средняя. Каждая правка каталога — правка того, что спрашивает край; вернуться
можно, но возврат **расширяет** доступ, поэтому обратный ход обязан идти как решение о доступе, со
своим разбором, а не как «откат оформления».

**Чем держится.** Гейтом «выведенное равно рукописному» на фазе migrate — он обязан **покраснеть**
ровно на этих пяти записях (XC-6 §6 шаг 4), и реестром расхождений, который **истекает сам**:
запись, потерявшая предмет, роняет гейт.

### в-8. Ветка «цепочка без звена решения в dev»

**Предмет.** Сохранить ли ветку, позволяющую поднять стенд без звена решения о доступе.

**Что решено.** **СНЕСТИ.** Production-mode **ВЕЗДЕ**, включая локальный стенд.

**Почему.** *Безопасность:* dev-insecure посадка уже объявлена долгом, а носитель без ветки делает
поднятие стенда без проверки прав **неконструируемым** — это сильнее любого правила, потому что
не требует, чтобы его помнили. *Простота:* одна ветка вместо двух, и `Serve` либо собрал, либо
отказал — без третьего состояния. *Скорость:* удаление ветки дешевле её сопровождения.

**Что отвергнуто и чем это было сильно.** Сохранить. Сильно практикой: сегодня это **рабочий приём
отладки** — локальный стенд поднимается без iam. Довод не отброшен, он переведён в **условие
исполнения**: замена приёма (поднять iam вместе со стендом) обязана существовать **раньше**, чем
ветка снята, иначе решение исполняется остановкой чужой работы (§8.3 п.6).

**Обратимость.** Низкая и намеренно: вернуть ветку значит вернуть конструируемость небезопасной
посадки. Если возврат когда-нибудь понадобится, он обязан идти как правка `security.md`, а не как
удобство разработчика.

**Чем держится.** Конструкцией `Serve` (третьего состояния нет), отказами старта §4.6 и
production-boot-guard'ами, которые уже есть у 7 из 7.

### в-9. Первый переводимый сервис

**Предмет.** Кого переводить на носитель первым: сервис с нулём сужателей или самый молодой.

**Что решено.** **Того, у кого НОЛЬ сужателей.**

**Почему.** *Скорость:* Ф1 остаётся **полной без Ф4** и сдаётся раньше — фаза не тянет за собой
чужой предмет (ban #14: полнота в своих границах). *Простота:* первый перевод проверяет **носитель**,
а не носитель вместе с сужателем; при отказе видно, что именно не сошлось.

**Что отвергнуто и чем это было сильно.** Самый молодой сервис. Сильно тем, что **именно на нём
измерены десять осей**, которые были забыты при рождении, и перевод сразу проверил бы носитель на
**худшем** случае — то есть дал бы более сильное свидетельство. Довод сохраняет силу как аргумент
за **второй** перевод: его разумно ставить сразу после первого, пока конструкция ещё правится
дёшево.

**Обратимость.** Полная: порядок переводов — план, а не конструкция. Единственная невозвратная
трата — время, если первый перевод окажется слишком лёгким и не найдёт ни одного дефекта носителя;
поэтому вторым и берётся худший случай.

**Чем держится.** Условием полноты Ф1 (§8.1): она объявлена полной **без** Ф4, и если перевод
потянет сужатель, фаза по собственному критерию не сдаётся.

### в-10. Бюджет полосы валидации и кэш существования проекта

**Предмет.** 5 c против 30 c на полосе валидации; и кэш положительного существования проекта у
одного сервиса.

**Что решено.** **5 c у всех семи** (с отдельным сроком попытки). **Кэш положительного
существования проекта — СНЯТЬ.**

**Почему.** *Производительность и предсказуемость:* путь запроса получает предсказуемый предел, а
не 30-секундный хвост; отдельный срок попытки закрывает случай, когда одна зависшая попытка
съедает весь бюджет. *Безопасность:* удалённый проект перестаёт проходить проверку **немедленно**,
а не в течение окна кэша. *Простота (по кэшу):* он есть **у одного из пяти**, **нигде не записан
как решение**, и замера в его пользу нет — то есть это абстракция без основания (LEAN).

**Что отвергнуто и чем это было сильно.** (а) 30 c — сильно тем, что переживает **перекат пода
соседа**: при 5 c перекат iam превращается в отказы на запросах. Довод отведён не отрицанием, а
адресом: перекат лечится **дренажем и повтором на своей полосе**, а не бюджетом пути запроса.
(б) Кэш — сильно тем, что снимает нагрузку с iam на **самом частом** вызове платформы; это
настоящий выигрыш, и он назван. **Условие возврата записано в самом решении владельца:** если
замер покажет нужду — вернуть **осознанно и с записью**, а не втихую у одного сервиса.

**Обратимость.** Высокая по обоим: бюджет — величина, кэш — конструкция на один вызов. Правило
возврата уже названо, и оно единственное, что отличает осознанный возврат от рецидива: **сначала
замер, потом кэш, и запись рядом**.

**Чем держится.** Полосой как источником сроков (§4.7: срок выводится из полосы, отдельного пути
задать свой у вызывающего нет) и тем, что снятый кэш **не имеет места, куда вернуться молча**, —
его возвращение видно как новое поле дескриптора.

### в-11. Сведение планов XC-6 и XC-7

**Предмет.** Один план или два.

**Что решено.** **Шаги 0-2 XC-6 идут независимо; начиная с шага 3 план ОДИН**, с порядком §8.1.

**Почему.** *Простота:* два плана перерезают одну работу по разным осям и разъезжаются молча —
расхождение прозы с прозой не даёт конфликта при мёрже. *Скорость:* шаги 0-2 не зависят от
носителя, стоят ≤6 мест каждый и закрывают уже случившиеся классы — держать их заложниками
APPROVED носителя значило бы отложить дешёвое ради дорогого.

**Что отвергнуто и чем это было сильно.** Два плана целиком. Сильно тем, что XC-6 может быть
APPROVED **раньше** и стартовать сам, не дожидаясь ни одной строки XC-7. Довод сохранён и
исполнен — именно он и породил границу «до шага 3».

**Обратимость.** Полная: план — не конструкция. Но цена расхождения растёт со временем, поэтому
граница названа **номером шага**, а не «пока удобно».

**Чем держится.** Тем, что порядок разрешён **признаком** (§8.1: тип держится без сборщика,
единственная реализация — нет), а не предпочтением; и тем, что колонка «Решение §9» в XC-6 §6
ведёт в этот документ, а не в третье место.

### в-12. Самодельный админ-гейт на внутреннем слушателе

**Предмет.** Живой самодельный рубеж у одного сервиса, классифицирующий по уровню отношения.

**Что решено.** **СНЯТЬ — но ОТДЕЛЬНОЙ ПРИЁМКОЙ**, не побочным эффектом унификации.

**Почему.** *Безопасность:* `security.md` §«Авторизация живёт в МОДЕЛИ» прямо запрещает
самодельные решения о доступе — они не выдаются, не ограничиваются областью, не отзываются, не
видны в аудите и не понимают машинных принципалов; другой сервис уже снял такой гейт **как
дефект**; модель эти же RPC гейтит. *Простота:* минус один рубеж, который классифицировал по
уровню отношения, а не по предмету ответа.

**Что отвергнуто и чем это было сильно.** Снять **заодно**, в рамках перевода на носитель. Сильно
экономией: рубеж всё равно попадает под правку, и отдельная приёмка выглядит церемонией. Отвергнуто
потому, что снятие **меняет, кто вправе звать мутации**, — это решение о доступе, и оно обязано
иметь свои сценарии и свой RED→GREEN. Порядок здесь тот же, что и везде: **сначала выразить
ограничение в модели, потом снимать самодельное**; обратный порядок расширяет доступ.

**Обратимость.** Асимметричная: снять легко, вернуть — значит завести самодельную проверку заново,
что запрещено нормативкой. Поэтому предпосылка снятия обязана быть **установлена по каждому RPC**
(что именно проверяет модель и что рубеж добавляет сверх этого), а не принята на веру.

**Чем держится.** Отдельной приёмкой с этой предпосылкой, правилом `security.md` и наблюдаемым
регрессом: обычный тенант по-прежнему не проходит, администратор облака проходит.

> [!important] Состояние на 2026-08-12: приёмки нет, предмет в дереве жив — заведён отдельным предметом
> Решение принято 2026-08-09 и с тех пор **не исполнено ни в одну сторону**: приёмка не написана,
> рубеж не снят. Молчание здесь было бы третьей стороной, которой у решения нет, поэтому остаток
> назван предметом с причиной и предикатом снятия —
> **`PRO-Robotech/kacho-workspace#147`**.
>
> **Замер на `4d26e3300`, предикат при числе:** `git grep -n 'assertAdminAccess' -- services/ pkg/
> gateway/` без проб → **определение** (`services/compute/internal/handler/tenant_interceptor.go`)
> и **два вызывающих** — по одному на каждую полосу интерсептора того же файла. Сервис тот же,
> что называет таблица §1.4 («1 из 7» — compute; у vpc такой же рубеж **снят как дефект**).
>
> **Предикат снятия** — тот же поиск даёт пусто в не-тестовом дереве **и** существует APPROVED-приёмка
> на снятие. До тех пор строка §0 «остаётся в-12» верна, а этот раздел не читается как исполненный.
> **Почему предмет, а не строка «предстоит»:** строка в документе за себя не отвечает и переживает
> своё основание — ровно то, что этот перемер и нашёл в шести других местах.

---

## §14 Цена — числом мест

**Таблица разделена на две части, потому что часть цены УЖЕ УПЛАЧЕНА.** Одна колонка «мест» была
бы честна ровно до 2026-08-06 и с тех пор молча завышала объём предстоящего: читатель, взяв
«7 400 строк к сведению», планировал бы работу, из которой больше половины сделана. Разбивка
«столько уплачено, столько предстоит» дороже одного числа — и именно потому, что она не даёт
зачесть чужое как своё.

### 14.1 Уплачено в стволе — предмет закрыт, координата коммита названа

| Предмет | Было (`cf69249ed`) | Стало (`92ca6af9d`) | Чем |
|---|---|---|---|
| Реализаций звена восстановления | **5** (4 в сервисах + край) | **1** на платформу; интерсепторных копий у сервисов **0** | `a99750f87` |
| Рукописные карты прав | 7 деклараций, **2235** строк | 7 деклараций, **276** строк — из них шесть по **40** (перечень пакетов + вызов вывода) | `ca3354d94` → `569bea84c` |
| Пакет сверки карты с каталогом | **492** строки | **удалён** — предмет исчез вместе с двойственностью источника | там же |
| Сужатель списков у четвёрки | **3753** строки в **17** файлах | **587** в **12**; общая реализация `pkg/listnarrow` — **9** файлов | `924691519`, `fd0c22cfe`, `c4d733248` |
| Круг отправителей | сырой срез, предикат непустоты у каждого свой | **тип** + **один** предикат `IsNarrowed()`; страж один на семь | `7f03e0bae`, `5573aa87f` |
| Закрытый набор приёма | 4 отношения под `internal/` владельца · **6** прозаических пересказов · **1** рукописная копия у пробы | **1 пакет** `pkg/authz/proxytuple`, доступен обеим сторонам; решение — **тройка** | `91850e208`, `49851acb7` |
| Токен полосы | 4 точки эмиссии, **1 из 5** токенов имеет производителя | **закрытый тип** в `pkg/errors`, словарь пяти закрыт компилятором | `96055cbd8` |
| Окно отзыва вердикта | 3 из 6 берут умолчание библиотеки неявно | **ручка у каждого держателя кэша** | `9bcf28b3b` |

**Уплачено порядка 5 300 не-тестовых строк** (1959 по картам + 492 по сверке + 3166 по сужателю
четвёрки) — и уплачено **не этим планом**, а волной 08-06…08-10. Приписывать эту экономию XC-7
было бы присвоением чужого замера.

### 14.2 Предстояло на `92ca6af9d` — то, чего в дереве не было

> [!important] Таблица датирована и НЕ переписывается — действующий остаток в §14.3
> Она снята на `92ca6af9d` и верна на ней; колонка «Состояние» говорит о **том** дереве.
> Читать её как сегодняшний остаток нельзя: волна 2026-08-11 закрыла из тринадцати строк
> пять, а две изменила в другую сторону (число выросло, потому что предмет исполнен шире
> заявленного). Перемер — §14.3, обеими ревизиями и с предикатом на строку.

| Предмет | Мест | Источник | Состояние |
|---|---:|---|---|
| Композиционные корни под перевод | **7** корней, **14** конструкций сервера | П1 | без изменений на обеих ревизиях |
| Сервисов, получающих верхнюю границу обработки впервые | **6** | И-ПРО | без изменений (**1 из 7** несёт) |
| Копии загрузочного гейта | **3 → 1** (исполняемая часть уже совпадает) | П8 | **3** на обеих ревизиях |
| Пакеты `internal/check` | **6 пакетов, 3203 строки** на базе; на стволе — те же шесть, у vpc по **новому** пути | П4 + §2.4 | предмет жив; часть — сервисная склейка, которая **переезжает**, а не исчезает |
| Карта прав у **iam** | **1** клетка — единственная незакрытая из семи | Ф2 | у iam карты нет вовсе; включение доказывается конформансом |
| Сужатель как **поле дескриптора** | **4** проводки | Ф4 | единственная реализация есть, **обязанности её взять — нет** |
| Клиенты к соседям | **39 файлов → один носитель** | П9 | `pkg/peer` — **0** файлов |
| Формы подписи регистратора | **5 → 1** | И-XC6 | сегодня невыразимо (§10 п.11) |
| Приведение вызывающих к типу полосы | **13** точек в 5 сервисах приведены; конформанс-гейт — нет | И-ПРО | тип landed, гейт «полоса выводится только конструктором» — нет |
| Область как данные вместо замыкания | приём жив, **100** строк | §4.4 | `authz.ObjectExtractor` остаётся `func` |
| Не-gRPC поверхность | **8** файлов | П11 | Ф8, **8** на обеих ревизиях |
| Строк каталога прав | **295 — без изменений** | И-XC6 | — |
| Экземпляров файла каталога | **2 — и остаётся 2** | П14 | предписание «2 → 3» снято: вывод файл не читает (§4.5) |

**Граф импортов — циклов нет и не заводится:** `pkg/ → services/` = **0** (П10, на обеих ревизиях). Поднимаемое зависит только от `pkg/*` и сгенерённых стабов.

### 14.3 Перемер 2026-08-12 на `4d26e3300` — что из §14.2 пережило свой предмет

**Обе ревизии названы, числа §14.2 не тронуты.** Пять строк из тринадцати предмет потеряли,
две изменились **в другую сторону** — число выросло, потому что предмет исполнен шире, чем
предполагала строка. Восемь величин закреплены §2.5 и меряются гейтом; остальные несут предикат
здесь.

| Строка §14.2 | На `92ca6af9d` | На `4d26e3300` | Предикат | Исход |
|---|---:|---:|---|---|
| Композиционные корни под перевод | 7 корней, **14** конструкций | **1** корень (iam), **2** конструкции | Д9 | остаток — один сервис, выведенный из контура решением (Ф3); край — **2** вызова (Д10), объявлены двумя записями решения |
| Верхняя граница обработки впервые | **6** | **1** (iam) | сервисов с полем `HandlingBudget:` в дескрипторе, прод `services/` → **6** (`cf69249ed` — **0**) | шесть получили её звеном носителя; предмет закрыт для всех, кто в контуре |
| Копии загрузочного гейта | **3** | **1** | Д11 / Д12 | **предмет закрыт** |
| Пакеты `internal/check` | 6 пакетов, **3203** строки (база) | **4** пакета, **12** файлов, **692** строки | обход `services/*/internal/check/`, не тесты, `wc -l` | сжались вчетверо; предмет жив |
| Карта прав у **iam** | **1** клетка | **1** клетка | Д7 + отсутствие вывода у iam | **не остаток работы, а следствие решения** вывести iam из контура (Ф3); держать её долгом значило бы обещать отменённое |
| Сужатель как поле дескриптора | **4** проводки | **6** | Д14 | обязанность взять держится **конструкцией** (отказ старта О3/О4), а не обзором; число выросло, потому что проводку объявили и там, где сужаемых методов нет (`geo` — «не применимо») |
| Клиенты к соседям | `pkg/peer` — **0** файлов | **3** файла в пакете, **21** прод-потребитель в **5** сервисах | Д13 + `git ls-tree -- pkg/peer` | узкая единица П9 (файлов в `services/*/internal/clients/`): **35** на `cf69249ed` → **29** |
| Формы подписи регистратора | **5 → 1** | не перемерялось | И-XC6 | остаётся невыразимым (§10 п.11); чужой замер, за свой не выдаётся |
| Приведение вызывающих к типу полосы | **13** точек, гейта нет | **21** прод-файл в **5** сервисах | Д13 | гейт «полоса выводится только конструктором» есть **для ребра geo** (`internal/repohygiene/peerlanecanon_test.go`, `TestGeoPeerLaneIsBuiltByTheClosedType`); **у всех вызывающих — нет** |
| Область как данные вместо замыкания | **100** строк, тип — `func` | **100** строк, тип — `func` | `git show <rev>:pkg/authz/catalogderive/scope.go \| wc -l`; `type ObjectExtractor func` в `pkg/authz/types.go` | без изменений |
| Не-gRPC поверхность | **8** файлов | **0** | Д5 / Д6 | **предмет закрыт** фазой Ф8 |
| Строк каталога прав | **295** | **295** | элементов массива в `permission_catalog.json` края | без изменений |
| Экземпляров файла каталога | **2** | **2** | Д15 | без изменений |

> [!note] Здесь стояло «первым переносится словарь — он разблокирует остальное». Он перенесён, и предсказание проверилось
> Утверждение было прогнозом и **сбылось**: словарь (правило приёма + круг + токен полосы) переехал
> в общий фундамент первым, до всякого носителя, и снял именно тот языковой барьер, ради которого
> назывался, — рукописная копия набора у пробы исчезла не потому, что её вычеркнули, а потому что
> импорт стал возможен. Строка снимается не как ошибочная, а как **исполненная**; оставленная в
> будущем времени, она ставила бы в очередь сделанное. Что из неё остаётся нормой: порядок
> «сначала словарь, потом носитель» подтверждён исполнением, и дискриминатор §8.1, из которого он
> выведен, получил своё сильнейшее свидетельство.

---

## §15 Критерии APPROVED

**A. Шов.**
1. Ни один канон К1…К7 XC-6 в XC-7 **не переопределён**. Проверяется поиском второго нормативного определения одного предмета; единственное предложенное изменение (§11 К-5) было названо **явно, обеими сторонами** и вынесено владельцу — и **решено им 2026-08-09** третьим вариантом (в-5), отвергающим обе стороны.
2. Каждое из **20** отступлений XC-6 §4 проверено на выразимость **значением поля** дескриптора; отступление, невыразимое значением, названо как дефект дескриптора, а не сервиса. **Число сверяется с источником при каждом касании** (XC-6 §4 — строки таблицы, §4а — перечень пунктов буквой; предикаты — врезка §0).

   > [!warning] Критерий был написан по собственной ошибке — и следующим кругом нарушен снова, в трёх местах сразу
   > Первая редакция ставила здесь: «здесь стояло „21“ при 20 в источнике, и разошлось оно молча,
   > потому что перенесённое число не имеет предиката». Урок был верен, вывод из него сделан —
   > и **не применён к собственному телу**: документ продолжал нести величину в трёх местах, из
   > которых §0 и §2.3 говорили 20/8, §12 — 21/7, а источник — 20/9. То есть критерий,
   > сформулированный по этому классу, был нарушен в **том же документе, где формулируется**.
   >
   > Отсюда усиление, которое дороже самой цифры: **критерий обязан называть ЧИСЛО МЕСТ, в
   > которых величина встречается**, а не только требовать сверки. Величина живёт в §0 (таблица
   > владения + врезка), §2.3 и §12 п.1 — **три** места; правка любого из них без остальных
   > заводит расхождение внутри одного файла, где его не поймает ни мёрж, ни гейт связности.
   > Пока предикат не назван вместе с местами, «сверять при каждом касании» проверяемым
   > требованием не является.
3. Ни одно намеренное отступление не сведено унификацией. Проверяется построчно: §4 XC-6 против §4.1 и §10 XC-7.

**B. Основание.**
4. Каждое число несёт **происхождение** (И-XC6 / И-ПРО / И-ПЕР) и предикат; ревьюер воспроизводит **не менее пяти**, включая **хотя бы одно** из И-ПЕР и **хотя бы одно** из И-ПРО.
5. Все **шесть** расхождений между проектами (§2.2) названы **обоими** референтами, надёжный выбран **с обоснованием**, и ни одно не разрешено молча.
6. Числа И-ПРО, которые я не перемерял, **помечены как чужой замер** и не выданы за собственный.
6а. **Величины, по которым судят о состоянии фаз, ЗАКРЕПЛЕНЫ (§2.5) и меряются проверкой, а не читателем.** Введено 2026-08-12 после того, как за одну ночь устарели шесть утверждений сразу, ни одно из которых не было ошибкой на момент письма. Проверяется прогоном `scripts/docs-gate/check-03-acceptance-tree-claims.py`: число берётся **из документа**, предикат — из реестра гейта, ревизия — из строки закрепления. Требование к автору: **число без предиката в этот документ не вносится**, а число, по которому кто-то будет планировать работу, вносится **закреплением**. Самоистечение в обе стороны — идентификатор без предиката и предикат без строки суть находки.
6б. **Отметка остатка (`✗`) несёт ревизию так же, как число.** Число на своей ревизии верно навсегда; `✗` утверждает про **сегодня**, поэтому стареет быстрее. Пересказ отметки в итоге **без** ревизии — то же самое утверждение, пережившее свой предмет, и именно так оба ✗ фазы Ф5 читались действующими после того, как их предмет исчез.

**C. Конструкция.**
7. Каждое утверждение «станет невозможно» отнесено к **уровню** (язык / построение / гейт), и ни один гейт не выдан за свойство построения (§5).
8. По каждому гейту названы **обе** стороны инъекции: возвращаемый дефект **и** законный близнец, на котором гейт обязан **молчать**.
9. Каждый гейт печатает **объём осмотренного**, и «ноль находок» отличимо от «ноль прочитанного» (A13).
10. Ни один гейт не въезжает **раньше** своего фикса; для каждого сегодня-красного сказано, с какой фазой он въезжает.
11. Названо, что остаётся **соглашением** (порядок звеньев внутри одной функции), а не выдано за невозможность.

**D. Полнота предложения.**
12. Каждая фаза §8.1 **production-полна в своих границах** (ban #14): названо, чем именно она полна, и ни одна не оставляет «остальные потом».
13. Ни одна предложенная абстракция не имеет **одного** потребителя; там, где кандидат имел одного, он **отвергнут** и предикат его заведения назван (§10 п.4, п.5).
14. Раздел §9 «что останется возможным сделать неправильно» — **непуст**, и по каждому пункту названо, чем ловится и каков остаточный риск.
15. Все **тринадцать** отрицательных результатов §10 сохранены; ни один не вычеркнут ради стройности.

**E. План и цена.**
16. У каждой фазы названы: число мест, что ломается, и как это проверить **до** правки, а не после.
17. Порядок относительно XC-6 разрешён **признаком** (§8.1), а не предпочтением.
18. **Каждое решение §13 несёт все шесть частей формы** — предмет · что решено · почему по критериям · **что отвергнуто и чем это было сильно** · обратимость · чем держится. Запись без отвергнутой стороны — `CHANGES_REQUESTED`: через месяц она читается как единственно возможная, и её пересматривают с нуля.
18а. **Решение проведено по телу.** Проверяется поиском мест, где документ продолжает описывать отвергнутую сторону как действующую: §4.1 (`Lists`), §4.4 и §5 (инверсия круга), §4.5 (форма с отступлением), §6 (A5, A11), §8.1 и §8.3 (фазы и предусловия), §10 п.12, §11, §12. Документ, где §13 говорит «решено», а §8 планирует отвергнутое, — два места об одном предмете, и он **хуже**, чем был до внесения решения.
18б. **Решение, меняющее КОНСТРУКЦИЮ, а не только план, названо в шапке.** Сегодня таких два (в-5 и в-6), и оба меняют утверждения, отнесённые в §5 к уровню языка или построения. Понижение уровня («это держал тип» → «это держит отказ старта») обязано быть записано **явно**: невысказанное, оно оставляет в документе гейт, выданный за свойство построения, — то, что запрещает критерий 7.

**F. Пригодность к публикации.**
19. В документе нет ни одной сборки «точная координата + условие, при котором защита не действовала + следствие для постороннего». Проверяется **перечислением с доказательством на пункт** — механического детектора нет, и это измерено (§10 п.7).
20. Правило записи координат применено последовательно: координаты — на канонических реализациях и на общих артефактах; на пробелах — сервис, число мест и требуемое свойство.
21. Ни одна ссылка не ведёт в место, которого нет; ни одно назначение не названо внутренним — непубличных мест в git **нет by construction**.

---

## §16 Трассируемость

Колонка «существующие сегодня» перемерена на `4d26e3300` (прежняя редакция таблицы — на
`92ca6af9d`): часть того, что стояло в «предлагаемых», переехала налево, и оставить её справа
значило бы предлагать написанное. **Правило колонок:** налево едет то, у чего в дереве есть
**артефакт с именем**; направо остаётся то, у чего его нет, — и «эталон» артефактом не является
(`testing.md` §«Гейт на класс», п. 6).

| Слой | Что закрывает из XC-6 | Гейты, существующие сегодня (`4d26e3300`) | Гейты, предлагаемые |
|---|---|---|---|
| C1 носитель | L1 форма · L3 стражи · порядок звеньев · О1-О10 | production boot-guard 7/7; **страж круга — один на семь в `Config.Validate`**; **гейт провязки звена восстановления** (`internal/repohygiene`, с пробой инъекции); гейт монтажа; гейт посадки по живому процессу; **AST-гейт ручной сборки** (`servicehostadoption_test.go`; перепись — корней 18, файлов 75, записей **2**, из них ожидают перевода **0**, решение владельца **2**; основание записей истекает от внешнего факта и держится `TestCarrierStillCannotExpressAProxyEdge`); **гейт формы дескриптора** (`descriptorchainlink_test.go`, `descriptorfieldreader_test.go`, `descriptorprobe_test.go`); **гейт не-gRPC профиля** (`nongrpcsurfaceprofile_test.go`; перепись — 14 корней, 59 файлов, находок 0, записей 0) | — |
| C2 дескриптор | конформанс XC-6 §5.2 требования 1-2 | **отвергающий конструктор** (`servicecontract.NewSpec` / `NewSurface` — отказ с перечислением всех находок разом); **суд «не применимо» каталогом** (О3: заявление о неприменимости истекает на первой строке `scope_filtered`) | линтер на закрытую структуру |
| C3 типы | L1 круг · L4 набор · L5 маркер · L7 токен | 7 гейтов дрейфа модели; гейт «глагол объявлен там, где читается»; **«у каждого принимаемого отношения есть эмиттер»** и **«тройка намерения принимается владельцем»** — оба с самоистечением; **«словарь полос закрыт на пяти»**; **гейт маркера версии регистрации** (`ownerregisterversion_test.go` + проба инъекции) | компилятор — на **маркере версии** (гейт есть, языкового запрета нет); гейт полноты эмиссии токенов по пакетам клиентов |
| C4 генерация | L2 паритет | `permission-catalog-check`; **вывод карты у шести из семи** — расходиться нечему by construction; отказ старта на четырёх состояниях бинаря | гейт «каждый сервис — потребитель вывода» (вторая проверка прежней сверки, которая **не** поглощается единственностью источника). **Клетка iam из «предлагаемых» снята: она не гейт и не долг, а следствие решения** вывести iam из контура (Ф3) |
| C5 гейты | L6 порядок · L7 полоса | AST-гейт порядка пагинации; гейт дрейфа таблиц скрытия; обратный гейт недостижимой строки; **`pkg/listnarrow` как единственная реализация**; **сверка проводки сужателя с каталогом в обе стороны** — О3/О4 отказом старта (`pkg/servicehost/audit.go`, `auditNarrowers`), обязанность взять держится конструкцией; **гейт «код не противоречит тексту»** для прозы полос (`peerprosverb.go`, разбор AST) | гейт «код не противоречит тексту» вне полос ответа соседа |
| C6 исходящий путь + SDK | L7 эмиссия (**1 из 5 → 5 из 5**) · находка XC-6 §4а «г» | **закрытый тип полосы** в `pkg/errors` — эмиссия по построению; **«полоса выводится только конструктором» для ребра geo** (`peerlanecanon_test.go`, `TestGeoPeerLaneIsBuiltByTheClosedType`) | тот же конформанс **у всех вызывающих**: тип есть и у ребра geo проверен, обязанности звать его вместо голого статуса у остальных — нет |

---

> **Напоминание о посадке.** Разбор пробелов §1.4, §7.1 и §9 в **собранной** форме (координата + условие + следствие) в сообщение коммита и в vault **не идёт** — `security.md` §«Признак восстановимости». В коммит идёт то, что теперь верно: «сервис больше не может собрать свою цепочку — конструкция одна, и она отказывает в старте при несогласованном объявлении», со ссылкой на дифф. Дифф и есть постоянный адрес разбора для всякого, у кого есть код.
