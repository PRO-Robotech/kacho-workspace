# Sub-phase KAN-AUTHN-1 (публичный слушатель принимает предъявленное удостоверение) — Acceptance

> **Статус:** CHANGES REQUESTED — `acceptance-reviewer`, круг 1, 2026-09-06 (ban #1). **Вердикт круга 1 остаётся действующим: нового не выносилось**, поэтому кодировать по этому документу по-прежнему **нельзя**. Оба блокирующих замечания §12 закрыты правкой — ответ автора и числа круга в §13; ожидается круг 2. Объём §7, замысел §2 и порядок §8 не менялись. Прежние объявления шапки сохранены: круга 1 — в §12, круга 2 — в §13.
> **Дата:** 2026-09-06 · **Ревьюер:** `acceptance-reviewer` (вердикт) · **Автор правки круга 2:** `acceptance-author`.
> **Задача:** продукт #2077. **Разблокирует:** #1109 (свой мультиплексор REST) → #1111. **Линия:** `release:standalone-iam`.
> **Ревизии, на которых сняты ВСЕ числа §1:** монорепо `github.com/PRO-Robotech/kacho`, ветка `release/kaname` @ `caa48b770d`; воркспейс `lane/acc-2077` от `issue-382-change-package`. Замер 2026-09-06.
> **Замысел линии:** `docs/changes/standalone-iam/design.md` — этот документ на него **опирается** и его не пересказывает и не правит.
> **Род документа:** **ПРИЁМКА, САНКЦИОНИРУЮЩАЯ РАБОТУ** — несёт сценарии Given-When-Then и после APPROVED разрешает кодировать объём §7 в порядке §8. Кода не содержит.
> **Публичный репозиторий:** документ написан так, чтобы выдерживать публикацию (`security.md` §«Публичные артефакты»). Предмет — **отсутствующая возможность**, а не обход: отсечка анонима отвергает всех без личности, то есть дефект даёт лишний отказ, а не лишнее разрешение. Сборка «координата + условие, при котором защита не действовала + следствие для постороннего» здесь не пишется; требования сформулированы как требования, а не как описание того, что сегодня проходит. Отдельно см. §10.

## 0. Обзор

Служба доступа выносится самостоятельным продуктом **Kaname**, устанавливаемым в чужом облаке.
Личность на её публичном слушателе производится ровно двумя способами, и оба предполагают
**нашу** инфраструктуру рядом: клиентский сертификат проверенного пира и личность,
**переданная** разрешённым отправителем. В чужом облаке нет ни нашего края, чтобы передать, ни
модульного сертификата у человека — арендатору **нечем назваться**.

При этом служба чеканит токены сама, публикует набор ключей проверки и несёт авторитет отзыва.
Больше того: **полная проверка нашего собственного токена в дереве уже написана** — она стоит на
поверхности, которую служба **предоставляет другим**, и не стоит на пути её **собственного**
публичного вызова (§1.4). То есть предмет — не «написать проверяющего с нуля», а **поставить
читателя предъявленного на публичную цепочку** и снабдить его тем, чего у выданной наружу
проверки нет по построению: адресатом, типом токена и коротким кешем.

Эта приёмка закрепляет наблюдаемое поведение такого читателя: что он принимает, что отвергает,
чем отказы **неразличимы между собой**, почему отзыв обязан читаться **на предъявлении**, и
почему прежний путь при этом не отзывается.

---

## 1. Что верно в дереве сегодня

Каждое число снято названным предикатом на ревизии `caa48b770d`. Единица счёта названа у
каждого. Предикаты прогнаны **с контролем в обе стороны**: предикат, который умеет находить,
обязан это показать на заведомом примере, иначе «ноль находок» неотличимо от «ноль
прочитанного».

### 1.1 Читателя предъявленного удостоверения на публичной цепочке нет — и предикат задачи считает НЕ ТО

Задача #2077 приводит два предиката и оба дают ноль. **Оба воспроизведены, оба дали ноль.**
Единица счёта — не-тестовый файл Go, читающий метаданные `authorization`.

```sh
git grep -c '"authorization"' -- 'services/iam/**/*.go' ':!*_test.go'   # вывод пуст → 0 файлов
git grep -c '"authorization"' -- 'pkg/grpcsrv/**/*.go'  ':!*_test.go'   # вывод пуст → 0 файлов
```

**Второй предикат при этом негоден, и это надо сказать прямо.** Шаблон `pkg/grpcsrv/**/*.go`
требует промежуточного каталога, поэтому файлы, лежащие в `pkg/grpcsrv` **непосредственно**,
в предмет не попадают вовсе. Контроль:

```sh
git grep -l '"authorization"' -- 'pkg/grpcsrv/**/*.go' ':!*_test.go' | wc -l   # 0
git grep -l '"authorization"' -- 'pkg/grpcsrv/*.go'    ':!*_test.go' | wc -l   # 1
```

Единственное найденное вхождение — **перечень ключей, чьи значения не пишутся в журнал**, то
есть маркер «не логировать», а не читатель. Вывод задачи от этого не меняется, **её предикат —
меняется**: он под-читывает на один файл, и под-читанный файл читателем тоже не является.
Полоса, по которой предикат должен ходить, — каталог целиком (`-- pkg/grpcsrv`), а не шаблон
с двумя звёздами. Это тот же класс, что `testing.md` §«Твой СОБСТВЕННЫЙ разборщик — тоже
распознаватель»: форма записи, о которой выражение не знает, даёт не красное и не зелёное, а
молчание.

Тем же предикатом по **всему** дереву читателей ровно два, и оба вне Kaname:
`gateway/internal/middleware/auth.go` (край платформы) и упомянутый перечень в `pkg/grpcsrv`.

### 1.2 Звенья публичной цепочки — восемь, по порядку

Каталог композиционного корня **переехал**: задача называет `services/iam/cmd/kacho-iam/serve.go`,
в дереве это `services/iam/cmd/kaname/serve.go` (сборка `publicUnary`). Предикат:
`git grep -n 'publicUnary' -- services/iam`.

| # | звено | что производит |
|---|---|---|
| 1 | измеритель задержки | ряд длительности и кода **на весь** вызов, включая отказ по правам |
| 2 | перехват паники | паника ниже по цепочке → `Internal` на **один** запрос вместо падения процесса |
| 3 | разметка отказа | машиночитаемая причина на `PermissionDenied`; **иные коды не трогает** |
| 4 | личность из проверенного клиентского сертификата | личность модуля из SAN сертификата пира |
| 5 | личность, **переданная** разрешённым отправителем | личность конечного пользователя — **только** если (4) доказало проверенного пира из круга |
| 6 | политика вызывающего | кому вообще позволено звать этот RPC на этом слушателе |
| 7 | отсечка анонима | отказ мутации без личности; **чтение и белый список пропускаются** |
| 8 | дверь | пообъектный вопрос к модели доступа |

Звенья 4–5 — пара `PrincipalExtract*` общего транспортного пакета. **Читателя предъявленного
арендатором удостоверения среди восьми нет ни одного.**

### 1.3 Поверхность, которую это делает недосягаемой, — тринадцать публичных служб

Единица счёта — объявление `service` в контракте домена.

```sh
git grep -h '^service ' -- 'proto/kacho/cloud/iam/v1/*.proto' | wc -l              # 22
git grep -h '^service ' -- 'proto/kacho/cloud/iam/v1/*.proto' | grep -c '^service Internal'  # 9
```

**13** публичных служб против 9 внутренних. Ни одну из тринадцати арендатор чужого облака
позвать не может: назваться нечем, и отказ приходит честный и бесполезный.

### 1.4 Проверка нашего токена в дереве ЕСТЬ — но не на пути собственного запроса

Это главное уточнение к постановке задачи, и оно удешевляет работу.

| механизм | где стоит | что проверяет | достижим ли арендатором |
|---|---|---|---|
| авторитет отзыва (интроспекция) | **внутренний** слушатель, требует клиентского сертификата | подпись своим набором ключей, издателя, срок, критические заголовки, **отзыв** | нет |
| проверка на пути запроса | **край платформы**, не Kaname | оба издателя, с кешем набора ключей | нет — края в чужом облаке нет |
| проверка входящего **утверждения клиента** | полоса выдачи Kaname | подпись клиента перед выдачей ему токена | да, но это **вход в выдачу**, а не предъявление уже выданного |

Предикаты: `git grep -n 'func (h \*Handler) judge' -- services/iam/internal/handler/tokenintrospecthttp`;
`git grep -n 'func (v \*JWTVerifier) Verify' -- gateway/internal/middleware`.

Пакет авторитета отзыва объявляет в своей шапке ровно ту норму, ради которой заведена ось 4
решения (§2): контроль, действующий на выдаче и не действующий на предъявлении, отзывом не
является. **Авторитет построен; потребителя у него на публичной цепочке нет.**

**Чего у авторитета нет по построению** — и почему он не годится «как есть»: он объявляет
**отступления** от единого перечня проверок, и отступления эти суть адресат и тип токена,
названные свойствами **поверхности предъявления**, а не издателя. Публичный слушатель и есть та
поверхность. Кеша у авторитета тоже нет — на каждый вопрос идёт обращение к хранилищу.

### 1.5 Единый перечень проверок токена существует; носителей четыре, проб согласия — две

Перечень — `pkg/tokenpolicy`, одиннадцать проверок: подпись · допустимость подписи · связь
заголовка с ключом · издатель · адресат · тип токена · срок · момент начала действия ·
идентификатор ключа · критические заголовки · отзыв.

```sh
git grep -ln 'func (.*) DeclaredChecks()' -- '*.go' ':!*_test.go' | wc -l   # 4 носителя
git grep -ln 'MissingChecksExcept'        -- '*_test.go' | grep -v pkg/tokenpolicy | wc -l  # 2 пробы
```

Из четырёх носителей два принадлежат Kaname и **оба несут пробу согласия** с перечнем; два
принадлежат платформе и пробы согласия в своих пакетах **не несут**. Читатель, заводимый этой
приёмкой, — третий носитель Kaname, и он обязан нести пробу согласия по образцу двух уже
существующих. Расхождение двух платформенных носителей — **не предмет этой приёмки**, оно
названо в §11.

### 1.6 Возможность объявлена оператору и читателя не имеет

Посадка `own` уже объявляет оператору две величины, чьё назначение в её же документации названо
приёмом **на входе**: издателя, признаваемого своим, и перечень принимаемых подписей. Обе
величины судятся стражем старта. Читателя у перечня подписей в дереве **нет**:

```sh
git grep -n 'AllowedAlgorithmList' -- '*.go' ':!*_test.go'   # 3 строки, все в ОДНОМ файле
git grep -l 'AllowedAlgorithmList' -- '*.go' ':!*_test.go' | wc -l   # 1 — сам объявляющий файл
```

То есть величина объявлена, задокументирована, провалидирована при старте — и **не читается ни
одной проверкой**. Это `api-conventions.md` §«Неисполнимая возможность»: два правила об одном
поле, из-за которых объявленное не исполняется ни при каком входе. Заводимый читатель закрывает
это **по построению**: он и есть тот, кто обе величины читает.

### 1.7 Посадка `own` в дереве развёртывания не выбрана ни разу

```sh
git grep -n 'identityProvider:' -- deploy          # 1 вхождение, значение external
git grep -c 'identityProvider: own' -- deploy      # 0
```

Умолчания у ручки нет намеренно; базовый профиль чарта объявляет `external`, и **ни один**
профиль его не переопределяет. Следствие для этой приёмки: сценарии посадки `own` наблюдаются на
профиле, которого сегодня в дереве нет, и он заводится вместе с работой по §7. Это назван**о**, а
не подразумевается.

---

## 2. Решение о форме проверки — ПРИНЯТО, здесь оно не переоткрывается

Задача #2077 прямо говорит, что оси 2 и 3 её предиката требуют отдельного решения о **форме**.
Решение принято владельцем линии и приводится здесь как **вход** приёмки. Сценарии §4 описывают
его наблюдаемым поведением; спор о самой форме предметом этого документа не является.

| ось | решение | почему это несущее |
|---|---|---|
| **1. что читается** | предъявитель в метаданных запроса, стандартной формой | иной формы у арендатора нет — он приходит обычным клиентом |
| **2. чем проверяется подпись** | собственным реестром ключей службы | служба сама издатель ⇒ **внешней зависимости у проверки нет by construction**; для установки в чужом облаке это и есть условие исполнимости |
| **3. что ещё проверяется** | срок · момент начала действия · издатель (обязан быть собственным) · адресат | адресат и тип — свойства **поверхности предъявления**; у выданного наружу авторитета они объявлены отступлением (§1.4), и закрыть их обязан именно этот читатель |
| **4. отзыв читается НА ПРЕДЪЯВЛЕНИИ** | читателем служит полоса интроспекции, с коротким кешем; недоступность авторитета — **fail-closed** | контроль на выдаче отзывом не является: предъявленное проходило бы до истечения срока, и это состояние **не сходится само**. Окно тогда задаёт срок кеша, а не срок удостоверения |
| **5. отказы неразличимы** | все отказы аутентификации **побайтово** одинаковы | различимый отказ здесь — оракул: он сообщает предъявителю, какая половина неверна |
| **6. страж старта** | отказ в пуске, когда проверка включена, а материал не задан | величина, которую построение подставляет молча, предметом стража быть не может: он зелен при любом входе |
| **7. прежний путь не отзывается** | переданная личность от разрешённого отправителя остаётся; пустой круг по-прежнему означает «не сужаем», а не «запрещаем» | за нашим краем этот путь законен, и отзыв его сломал бы платформу ради установки, которой края не нужно |
| **8. обе формы разом — отказ** | предъявленный токен **и** переданная личность в одном запросе → fail-closed | неоднозначность о том, **кто звонит**, догадкой не разрешается |

**Что из этого НЕ является решением о реализации.** Приёмка утверждает наблюдаемое: какой исход
видит арендатор, оператор и соседняя служба. Достигается ли чтение отзыва обращением по полосе
интроспекции или тем же авторитетом внутри процесса — предмет замысла реализации, а не этого
документа; сценарии §4 верны при обоих и в обоих случаях требуют одного: **fail-closed при
недоступности авторитета** и **окно, равное сроку кеша**.

---

## 3. Сценарий → производитель

**Правило раздела.** Каждое «Тогда» требует текста, кода возврата, строки или значения, которое
**что-то в дереве производит**. «Производитель» здесь — **механизм**, эмитирующий наблюдаемое **на
той поверхности, которую сценарий наблюдает**, а не конкретная строка. Где производителя нет, он
назван, сценарий помечен `ждёт производителя` и **покрытым не считается**: исполнить его нельзя,
и в зачёт покрытия он не идёт.

| Семейство | Что наблюдает | Производитель сегодня | Состояние |
|---|---|---|---|
| **VER** — верификация предъявленного | арендатор чужого облака | **нет на этой поверхности.** Механизм проверки нашего токена в дереве есть (§1.4), перечень проверок есть (§1.5), харнесс публичной цепочки есть — читателя на публичной цепочке нет | **ждёт производителя** — заводится этим переходом |
| **DUP** — обе формы разом | арендатор | **нет**: чтобы отвергнуть неоднозначность, обе формы должны быть представимы, а первая не читается | **ждёт производителя** |
| **REV** — отзыв на предъявлении | арендатор + оператор | **нет на этой поверхности.** Авторитет отзыва построен и объявляет ровно эту норму (§1.4); потребителя на публичном пути нет, кеша нет | **ждёт производителя** |
| **DENY** — неразличимость отказов | арендатор | **нет**: отказов верификатора не существует. Базовый отказ по правам сегодня побайтово одинаков у трёх производителей публичной цепочки — образец есть, предмет отсутствует | **ждёт производителя** |
| **BOOT** — отказ старта | оператор | **есть**: страж настройки отвергает пуск списком, и в нём уже живёт образец формы «контроль включён, материал не задан»; таблица требований посадки `own` — тот же механизм; перечень обязательных величин **порождается** и сверяется гейтом | производитель есть, **ось заводится этим переходом** |
| **FWD** — прежний путь не отозван | соседняя служба, наш край | **есть и держится**: поведенческие замки публичной цепочки и страж размещения (§5) | производитель есть |
| **WIRE** — провязка читателя | инженер | **есть**: страж, читающий композиционный корень, и харнесс, собирающий цепочку **боевым** сборщиком | производитель есть, **новая проверка заводится этим переходом** |
| **POL** — согласие с единым перечнем | инженер | **POL-01: есть** — единый перечень и форма пробы согласия (§1.5). **POL-02: нет** — величину сегодня не читает ни одна проверка (§1.6) | смешанное, помечено по сценариям |
| **OPS** — что видит оператор чужого облака | оператор | **нет**: шаг проверки подъёма в руководстве установки предписывает проверять **через ваш край**, а в чужом облаке края нет | **ждёт производителя** |

**Сценариев всего 31. Ждут производителя — 22** (`VER` 12 · `DUP` 1 · `REV` 4 · `DENY` 2 ·
`POL-02` 1 · `OPS` 2). Все двадцать два заводят производителя **этим же переходом**, но
производитель у них **не один**: у девятнадцати это читатель предъявленного на публичной
цепочке, у KAN-OPS-01 — шаг руководства установки (§7 п. 7), у KAN-OPS-02 — измерители
(§7 п. 8), а KAN-POL-02 закрывается читателем и наблюдается переписью. **Производитель есть у 9**
(`BOOT` 3 · `FWD` 3 · `WIRE` 2 · `POL-01` 1).

**Механизм каждой проверки в дереве уже есть — недостаёт его на этой поверхности.** Единый
перечень (§1.5) закрыт и содержит одиннадцать проверок; два платформенных носителя исполняют все
одиннадцать, авторитет отзыва — девять из одиннадцати, объявив адресат и тип **отступлением с
причиной** (§1.4). Поэтому ни одна из проверок не изобретается этой приёмкой: заводится
**потребитель** на публичной цепочке. Предикат состава перечня:

```sh
git grep -n -A14 'func MandatoryChecks' -- pkg/tokenpolicy/policy.go   # 11 проверок
```

Отдельно: **состояние держателя** (кто эти сценарии закрепит пробой) — другая ось, и она в §5.
Сегодня держатся два сценария, наполовину — один; остальные 23 не начаты, и это ожидаемо: кода по
приёмке до APPROVED не пишется.

---

## 4. Сценарии

Единица наблюдения — **арендатор чужого облака**, у которого нет ни нашего края, ни нашего
стенда, ни модульного сертификата. Всё, что он не может проверить у себя, наблюдаемым поведением
не является.

### VER — верификация предъявленного удостоверения

**KAN-VER-01 — годный токен собственной чеканки называет вызывающего** · E · *ждёт производителя*

**Given** установка Kaname в чужом облаке, посадка `own`: внешнего поставщика удостоверений нет вовсе
**And** арендатор получил токен у самой службы её штатной полосой выдачи
**And** токен не истёк, момент начала его действия наступил, он не отозван
**When** арендатор зовёт публичный RPC, предъявив этот токен обычной формой предъявителя
**Then** вызов доходит до обработчика
**And** вызывающий назван — это тот субъект, которому токен выдан, а не подставная системная личность
**And** дальше действуют обычные правила доступа: **право звать** этот RPC вопросом аутентификации не решается
**And** ни один шаг проверки не обращался к внешнему поставщику удостоверений — в посадке `own` это **свойство построения**, а не наблюдение: поставщика в установке нет вовсе. У оператора оно наблюдается тем, что профиль его установки не объявляет ни одного внешнего источника ключей

> **Положительный контроль всего семейства.** Без него шесть отрицаний ниже зеленели бы на
> установке, которая отвергает вообще всё.

**KAN-VER-02 — истёкший токен отвергнут** · E · *ждёт производителя*

**Given** те же условия, что в KAN-VER-01
**And** тот же токен, отличающийся от годного **ровно одним фактом**: срок его действия истёк
**When** арендатор предъявляет его на том же RPC
**Then** вызов до обработчика не доходит
**And** отказ побайтово равен отказам семейства — тем же тринадцати, что перечисляет KAN-DENY-01

**KAN-VER-03 — токен, чья подпись не сходится, отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: его идентификатор ключа резолвится в ключ собственного реестра, но подпись этим ключом **не сходится**
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** решение принято **без** обращения к какому-либо внешнему авторитету: реестр ключей — собственный

> **Почему «подпись не сходится», а не «подписан не нашим ключом».** Прежняя формулировка меняла
> **два** факта разом — и разрешение идентификатора ключа, и сходимость подписи, — а какой из
> двух дал отказ, такой сценарий не различает. Разрешение идентификатора вынесено своим
> односфактным отрицанием: KAN-VER-10.

**KAN-VER-04 — токен с чужим адресатом отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен нашей чеканки, нашим издателем, годный по сроку и подписи, но выданный **для другого адресата** — той полосы, которая публичным слушателем не является
**When** арендатор предъявляет его на публичном RPC
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства

> Это тот сценарий, ради которого адресат назван осью 3 решения: у авторитета отзыва он объявлен
> **отступлением** и по построению не проверяется (§1.4). Токен, годный для одной поверхности,
> не обязан быть годен для другой.

**KAN-VER-05 — токен, чьё время ещё не наступило, отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: момент начала его действия в будущем
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** допуск на расхождение часов применён **тот же**, что применяют остальные проверяющие продукта — второго значения этой величины не заводится

**KAN-VER-06 — подпись вне перечня принимаемых отвергнута** · E · *ждёт производителя*

**Given** те же условия
**And** установка объявила перечень принимаемых подписей (§1.6)
**And** токен, отличающийся от годного ровно одним фактом: его подпись в объявленный перечень не входит
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** решение принято по **объявленному установкой** перечню, а не по тому, что заявлено в самом токене

**KAN-VER-07 — издатель, не являющийся собственным, отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: его издателем назван не тот, которым установка объявила себя
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** незаданный издатель установки означает **не** «любой наш», а отказ старта (KAN-BOOT-01), поэтому состояния «принимаем издателя, которого никто не выбирал» не существует

**KAN-VER-08 — токен, выпущенный не для этой полосы, отвергнут по типу** · E · *ждёт производителя*

**Given** те же условия
**And** токен нашей чеканки, нашим издателем, годный по сроку, подписи и адресату, но объявляющий **тип**, который для публичного слушателя не предназначен
**When** арендатор предъявляет его на публичном RPC
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** **отсутствие** типа и **несовпадение** типа дают один и тот же исход — отказ: «тип не назван» не означает «любой»

> **Второй из двух предметов оси 3 решения** (§2), и без него ось закрыта наполовину. У авторитета
> отзыва тип объявлен **отступлением с причиной**: тип говорит, для какой поверхности токен
> выпущен, и сверяет его та поверхность, а не издатель (§1.4). Публичный слушатель и есть эта
> поверхность — здесь отступления быть не может.

**KAN-VER-09 — алгоритм заголовка, не закреплённый за найденным ключом, отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: объявленный в его заголовке алгоритм входит в перечень принимаемых (§1.6), но **не равен** тому, который закреплён за ключом, найденным по идентификатору этого же токена
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** способ проверки подписи выбирает **ключ**, а не заголовок: значение из заголовка служит только сверке и никогда — выбором

> **Это та проверка, чьё отсутствие §1.6 предъявляет как «неисполнимую возможность»**, и §7 п. 1
> санкционирует её прямо. Односфактность здесь несущая: «алгоритм допустим» (KAN-VER-06) и
> «алгоритм закреплён за ключом» — **разные** отказы, и сценарий меняет ровно второй. Слить их в
> один значило бы вернуть ту же двухфактность, из-за которой правится KAN-VER-03.

**KAN-VER-10 — идентификатор ключа, не резолвящийся в собственном реестре, отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: его идентификатор ключа называет ключ, которого в собственном реестре службы нет
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** форма идентификатора ограничена **до** того, как он попадёт в поиск по реестру, в повод обновления снимка ключей и в журнал
**And** отказ по **негодной форме** идентификатора побайтово равен отказу по **неизвестному** идентификатору: различимость «мусор» и «не наш» сама была бы оракулом

> Положительный близнец — KAN-VER-01, где тот же идентификатор резолвится. С KAN-VER-03 сценарий
> разведён намеренно: там идентификатор резолвится и не сходится подпись, здесь до подписи дело
> не доходит вовсе.

**KAN-VER-11 — параметр, помеченный обязательным к пониманию и не исполняемый, отвергает токен целиком** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: его заголовок помечает **обязательным к пониманию** параметр, которого читатель не исполняет
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** отвергается **весь** токен, а не игнорируется помеченный параметр: принять его значило бы исполнить условие, которого читатель не понял
**And** перечень параметров, которые читатель умеет исполнять и потому вправе принять помеченными, **объявлен**; пустой перечень означает «любой помеченный отвергается» и является решением, а не заготовкой

**KAN-VER-12 — НЕ помеченный обязательным неизвестный параметр игнорируется** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: его заголовок несёт неизвестный читателю параметр, **не** помеченный обязательным к пониманию
**When** арендатор предъявляет его
**Then** вызов доходит до обработчика, и вызывающий назван — исход тот же, что у KAN-VER-01

> **Положительный близнец KAN-VER-11, и он обязателен, а не для симметрии.** Требование
> двустороннее: помеченное обязательным — исполнить либо отвергнуть токен целиком; **не**
> помеченное неизвестное — игнорировать. Ужесточение первой половины без второй ломает
> совместимость, а без этого сценария читатель, отвергающий **любой** незнакомый параметр,
> проходил бы KAN-VER-11 как правильный.

### DUP — две формы личности в одном запросе

**KAN-DUP-01 — предъявленный токен и переданная личность разом отвергаются** · E · *ждёт производителя*

**Given** установка Kaname за нашим краем: круг разрешённых отправителей непуст, край в нём назван
**And** запрос, несущий **одновременно** годный предъявленный токен и переданную личность от разрешённого отправителя
**When** запрос приходит на публичный слушатель
**Then** вызов отвергнут
**And** отказ побайтово равен отказам семейства VER
**And** ни одна из двух личностей не выбрана — ни «предъявленная главнее», ни «переданная главнее»

> **Положительные близнецы — два, и каждый отличается ровно одним фактом.** KAN-VER-01: тот же
> запрос **без** переданной личности — принят. KAN-FWD-01: тот же запрос **без** предъявленного
> токена — принят. Без обоих утверждение «отвергается неоднозначность» зеленело бы на установке,
> отвергающей любую из форм по отдельности.

### REV — отзыв читается на предъявлении

**KAN-REV-01 — сквозь обе стороны: записали отзыв → предъявили → отказ** · E · *ждёт производителя*

**Given** арендатор, у которого есть годный токен, и этим токеном он **только что успешно** звал публичный RPC (KAN-VER-01)
**And** срок этого токена **не истёк** и до его истечения остаётся заметное время
**When** отзыв этого удостоверения записан штатным путём
**And** по истечении объявленного срока кеша арендатор предъявляет **тот же самый** токен на том же RPC
**Then** вызов отвергнут
**And** отказ побайтово равен отказам семейства VER

> **Сценарий обязан пройти СКВОЗЬ обе стороны одним прогоном.** Две пробы по половине — «отзыв
> записался» и «отвергнутое отвергается» — этого класса не ловят: каждая зелена о своей половине,
> а вопрос в том, **про один ли предмет** они говорят (`security.md` §«Разрыв, невидимый ни с
> одной стороны по отдельности»). Проба, не предъявляющая удостоверение после записи отзыва,
> сценария не закрывает.

**KAN-REV-02 — тот же токен без отзыва продолжает приниматься** · E · *ждёт производителя*

**Given** те же условия, что в KAN-REV-01, и тот же токен
**And** отличие **ровно одно**: отзыв не записан
**When** арендатор предъявляет токен после того же интервала ожидания
**Then** вызов доходит до обработчика, и вызывающий назван

> Положительный близнец KAN-REV-01. Без него утверждение «после отзыва отвергается» зеленело бы
> на читателе, отвергающем **всё** по истечении кеша.

**KAN-REV-03 — недоступный авторитет отзыва даёт отказ, а не проход** · E · *ждёт производителя*

**Given** арендатор с годным, неотозванным токеном
**And** авторитет, у которого спрашивают про отзыв, недоступен
**And** положительного вердикта по этому токену в кеше нет
**When** арендатор предъявляет токен
**Then** вызов отвергнут
**And** неполученный ответ **не** засчитан как «не отозван»
**And** отказ отличим **в журнале и в измерителях** — но не в том, что видит предъявитель

**KAN-REV-04 — окно отзыва задаёт срок кеша, а не срок удостоверения** · E · *ждёт производителя*

**Given** арендатор с годным токеном, чей срок жизни **заметно длиннее** объявленного срока кеша
**And** он только что успешно звал RPC — положительный вердикт закеширован
**When** отзыв записан
**And** арендатор предъявляет тот же токен **сразу**, не дожидаясь истечения кеша
**Then** вызов **доходит** до обработчика — положительный вердикт по этому токену закеширован, и объявленный срок кеша не истёк; это и есть объявленное окно, а не дефект
**And** после истечения объявленного срока кеша тот же токен отвергается (KAN-REV-01)
**And** **отрицательный** вердикт не кешируется: восстановленный доступ не ждёт истечения записи
**And** часы прогона **управляемы**: обе половины окна — до истечения кеша и после — наблюдаются детерминированно, а не выжидаются

> **Утверждение решительное намеренно, и это несущее.** Формулировка «вызов **ещё может** дойти»
> истинна при любом исходе — в том числе на читателе, у которого кеша нет вовсе и отзыв
> спрашивается на каждом предъявлении. Такой читатель проходил бы **все четыре** сценария REV,
> при том что короткий кеш объявлен осью 4 решения (§2) и стоит в объёме §7 п. 1. Утверждение,
> принимающее оба исхода, утверждением не является — это тот самый класс, который документ сам
> цитирует в KAN-DENY-02 и §6.
>
> Довод «окно недетерминированно» здесь не проходит: §5 назначает держателем пробу **с
> управляемыми часами**, а при управляемых часах исход внутри окна определён. Сценарий обязан
> **краснеть** на читателе без кеша — и краснеет: сразу после записи отзыва такой читатель
> отвергнет вызов, которого этот сценарий требует пропустить.

### DENY — отказы аутентификации неразличимы между собой

**KAN-DENY-01 — все отказы аутентификации побайтово одинаковы** · E · *ждёт производителя*

**Given** набор предъявлений, каждое негодное **своей** причиной: истёк · подпись не сходится · чужой адресат · время не наступило · подпись вне перечня · чужой издатель · чужой тип · алгоритм не закреплён за ключом · идентификатор ключа не резолвится · помеченный обязательным параметр не исполняется · отозван · авторитет отзыва недоступен · обе формы разом
**When** каждое предъявляется на одном и том же RPC
**Then** ответы **побайтово равны между собой** — совпадают код возврата, текст **и** приложенные к ответу подробности
**And** равенство утверждается **сравнением байтов**, а не совпадением кодов: два отказа с одним кодом и разными подробностями различимы
**And** ни один ответ не называет, **какая** половина предъявленного неверна
**And** перечень причин **закрыт и сходится с семейством отрицаний**: тринадцать причин отвечают KAN-VER-02…12, KAN-REV-01, KAN-REV-03 и KAN-DUP-01 — отказ, не попавший в перечень, неразличимости ничем не обязан, и это дыра, а не умолчание

> **Почему подробности названы отдельно.** На публичной цепочке стоит звено, приписывающее
> машиночитаемую причину отказам **по правам** (§1.2, звено 3). Отказ аутентификации, оформленный
> тем же кодом, получил бы такое приложение автоматически, и два отказа, равные по тексту, стали
> бы различимы по приложенному. Утверждение о равенстве обязано покрывать **весь** ответ.

**KAN-DENY-02 — годное предъявление даёт ДРУГОЙ исход** · E · *ждёт производителя*

**Given** тот же RPC и годный токен KAN-VER-01
**When** он предъявляется
**Then** исход **отличается** от общего отказа KAN-DENY-01

> Положительный близнец. Без него «все отказы одинаковы» — тождественная истина на установке,
> которая отвечает одним и тем же на **всё**.

> **Что это утверждение НЕ требует.** Отказ аутентификации не обязан быть побайтово равен отказу
> **авторизации**: это ответы на разные вопросы, и ни один из них не сообщает, существует ли
> названный субъект. Предмет требования — неразличимость **внутри** аутентификации.

### BOOT — страж старта

**KAN-BOOT-01 — проверка включена, материал не задан → служба не поднимается**

**Given** профиль установки, объявивший посадку `own` и включивший проверку предъявленного
**And** величина, без которой проверка не имеет предмета, не задана
**When** оператор поднимает службу
**Then** служба **не стартует**
**And** отказ называет **ключ настройки** и причину, по которой без него не пускаемся
**And** отказ приходит **вместе с остальными** найденными проблемами настройки, а не по одной за перезапуск
**And** значения самой величины в отказе нет — служба называет имя ручки, но никогда её содержимое

> Формулировка отказа — часть контракта оператора, и образец этой формы в страже уже живёт
> (§3, семейство BOOT). Текст отказа при отказе старта под запрет §10 **не подпадает**: без него
> стенд не поднять.

**KAN-BOOT-02 — материал задан → служба поднимается**

**Given** тот же профиль, отличающийся **ровно одним фактом**: величина задана
**When** оператор поднимает службу
**Then** служба стартует
**And** её собственное заявление о посадке называет, что проверка предъявленного включена

> Положительный близнец KAN-BOOT-01: без него «отказывает в старте» зеленело бы на страже,
> отвергающем любой профиль.

**KAN-BOOT-03 — перечень обязательных величин порождается и содержит новые ручки**

**Given** руководство установки, чей перечень обязательных величин **порождён** из таблицы стража, а не выписан
**When** к стражу добавлены величины проверки предъявленного
**Then** порождённый перечень содержит их строки
**And** сверка порождённого с таблицей стража сходится, и расхождение роняет прогон

### FWD — прежний путь не отзывается

**KAN-FWD-01 — переданная личность от разрешённого отправителя по-прежнему принимается**

**Given** установка Kaname за нашим краем, круг разрешённых отправителей непуст
**And** проверенный пир, названный в круге, передаёт личность конечного пользователя
**And** предъявленного токена в запросе **нет**
**When** запрос приходит на публичный слушатель
**Then** вызывающим назван **конечный пользователь**, а не системная личность

> Положительный близнец KAN-DUP-01, отличающийся ровно одним фактом (нет предъявленного токена).

**KAN-FWD-02 — переданная личность от непроверенного пира по-прежнему отбрасывается**

**Given** та же установка
**And** пир, чей сертификат не проверен либо в круге не назван, передаёт личность конечного пользователя
**When** запрос приходит на публичный слушатель
**Then** переданная личность **отброшена**, и вызывающий остаётся системной личностью
**And** введение читателя предъявленного этого не изменило

**KAN-FWD-03 — семантика пустого круга не изменилась**

**Given** установка, чей круг разрешённых отправителей пуст
**When** служба поднимается и принимает запрос с переданной личностью от проверенного пира
**Then** поведение **то же, что до введения читателя предъявленного**: пустой круг означает «не сужаем», а не «запрещаем»
**And** отдельный страж старта, требующий явно объявить круг либо явно от сужения отказаться, продолжает действовать без изменений

> **Что этот сценарий охраняет.** Соблазн «раз появился предъявитель, переданную личность можно
> и запретить» ломает платформу ради установки, которой края не нужно. Ось 7 решения (§2) это
> запрещает, и здесь она превращается в замок.

### WIRE — провязка читателя

**KAN-WIRE-01 — читатель стоит в публичной цепочке, и это утверждает дерево**

**Given** композиционный корень, собирающий цепочку публичного слушателя
**When** прогоняется страж размещения, читающий этот корень
**Then** страж требует присутствия читателя предъявленного в публичной цепочке
**And** снятие читателя из цепочки роняет прогон **с именем виновника**
**And** страж печатает объём осмотренного и падает на пустом обходе

**KAN-WIRE-02 — харнесс цепочки видит именно ту цепочку, что собирает боевой корень**

**Given** харнесс, которым проверяются поведенческие сценарии VER, DUP и REV
**When** читатель предъявленного добавлен в публичную цепочку
**Then** харнесс собирает цепочку **боевым** сборщиком и потому видит читателя
**And** публичная и внутренняя цепочки в харнессе перестают быть тождественными, если читатель стоит только на публичной

> **Ловушка, ради которой сценарий заведён.** Сегодня харнессы обеих цепочек собираются одним и
> тем же вызовом и потому **тождественны по телу**. Читатель, добавленный в публичную цепочку
> мимо общего сборщика, окажется вне харнесса — и все пробы VER, DUP и REV станут **вакуумными**,
> оставаясь зелёными. Это `testing.md` §«Гейт на класс», п. 6: тест, чей заголовок шире тела.

### POL — согласие с единым перечнем проверок

**KAN-POL-01 — состав проверок читателя сходится с единым перечнем**

**Given** единый перечень проверок токена (§1.5) и два уже существующих носителя Kaname, каждый со своей пробой согласия
**When** заводится третий носитель — читатель предъявленного
**Then** он объявляет свой состав проверок, и проба сверяет его с перечнем **в обе стороны**
**And** проверка, которую он не исполняет, обязана быть объявлена **отступлением с причиной** — иначе отступление неотличимо от пропуска
**And** перечень объявленных отступлений у этого читателя **пуст, и пуст осознанно**: он исполняет все одиннадцать проверок перечня — включая адресат и тип токена, объявленные отступлением у авторитета отзыва (§1.4), и включая связь заголовка с ключом, идентификатор ключа и критические заголовки
**And** каждой из одиннадцати проверок отвечает **своё односфактное отрицание** в §4, а каждому отрицанию — строка в закрытом перечне причин KAN-DENY-01: покрытие перечня и покрытие неразличимости расходиться не вправе

**KAN-POL-02 — объявленная оператору величина обрела читателя** · *ждёт производителя*

**Given** посадка `own`, объявившая перечень принимаемых подписей (§1.6)
**When** арендатор предъявляет токен, чья подпись в перечень не входит
**Then** он отвергнут **по этому перечню** (KAN-VER-06)
**And** величина, до сих пор не читавшаяся ни одной проверкой, имеет читателя в не-тестовом дереве

> Это закрытие `api-conventions.md` §«Неисполнимая возможность» на конкретном поле: исход
> «реализовать» из трёх законных выбран, потому что читатель заводится этим же переходом.

### OPS — что видит оператор и арендатор чужого облака

**KAN-OPS-01 — проверка подъёма исполнима без нашего края** · *ждёт производителя*

**Given** оператор чужого облака, поднявший Kaname по руководству установки
**And** ни нашего края, ни нашего стенда у него нет
**When** он выполняет шаг руководства «служба отвечает по своему публичному слушателю»
**Then** шаг исполним **средствами, которые у него есть**: он получает токен у самой службы и зовёт им публичный вызов
**And** руководство не предписывает ему проверять через край, которого в его установке не существует

> **Что здесь наблюдается.** Сегодня этот шаг руководства адресован установке, у которой край
> есть. Для посадки `own` он **не исполним** — и это первое, обо что арендатор чужого облака
> спотыкается. Предикат готовности выноса — «отдельный клон поднимается в боевой посадке» —
> этим шагом и проверяется.

**KAN-OPS-02 — отказ не рассказывает предъявителю, чем токен негоден** · *ждёт производителя*

**Given** любое из негодных предъявлений KAN-DENY-01
**When** оно отвергнуто
**Then** **наружу** уходит суждение, и только оно
**And** подробность — какая именно проверка не сошлась — доступна оператору **в журнале и измерителях** его установки
**And** различение исходов в измерителях считает **и выданные, и отвергнутые**: ноль в одном ряду иначе отвечает сразу на два вопроса — «отказов не было» и «сюда никто не приходил»

---

## 5. Состояние сценариев

Таблица заведена **сейчас**, а не после реализации: сценарий без строки уходит из счёта молча.
Состояние здесь — про **держателя** (что закрепит сценарий пробой), и это **другая ось**, чем
производитель в §3. Два сценария держатся уже сегодня, потому что утверждают поведение, которое
эта работа обязана **не сломать**.

| Сценарий | Уровень | Состояние | Чем держится |
|---|---|---|---|
| KAN-VER-01 | E | не начат | поведенческая проба публичной цепочки: годный токен даёт названного вызывающего |
| KAN-VER-02 | E | не начат | та же проба, вход отличается ровно сроком |
| KAN-VER-03 | E | не начат | та же проба, вход отличается ровно ключом подписи |
| KAN-VER-04 | E | не начат | та же проба, вход отличается ровно адресатом |
| KAN-VER-05 | E | не начат | та же проба, вход отличается ровно моментом начала действия |
| KAN-VER-06 | E | не начат | та же проба, вход отличается ровно подписью; перечень принимаемых задан установкой |
| KAN-VER-07 | E | не начат | та же проба, вход отличается ровно издателем |
| KAN-VER-08 | E | не начат | та же проба, вход отличается ровно типом токена; отсутствие типа — отдельный вход того же прогона |
| KAN-VER-09 | E | не начат | та же проба, вход отличается ровно алгоритмом заголовка при том же ключе |
| KAN-VER-10 | E | не начат | та же проба, вход отличается ровно идентификатором ключа; негодная форма и неизвестный идентификатор сверяются на побайтовое равенство |
| KAN-VER-11 | E | не начат | та же проба, вход отличается ровно помеченным обязательным параметром |
| KAN-VER-12 | E | не начат | та же проба, вход отличается ровно НЕ помеченным неизвестным параметром; исход положительный |
| KAN-DUP-01 | E | не начат | проба публичной цепочки на запросе с двумя формами личности разом; оба положительных близнеца рядом |
| KAN-REV-01 | E | не начат | сквозная проба одним прогоном: запись отзыва, затем предъявление того же токена |
| KAN-REV-02 | E | не начат | та же проба, отличается ровно отсутствием записи отзыва |
| KAN-REV-03 | E | не начат | проба с недоступным авторитетом: отказ, а не проход |
| KAN-REV-04 | E | не начат | проба с управляемыми часами: до истечения кеша и после |
| KAN-DENY-01 | E | не начат | проба, сравнивающая ответы **побайтово**, включая приложенные подробности |
| KAN-DENY-02 | E | не начат | тот же прогон: годное предъявление даёт другой исход |
| KAN-BOOT-01 | I | не начат | проба стража настройки: включённая проверка без материала роняет старт и называет ключ |
| KAN-BOOT-02 | I | не начат | та же проба, профиль отличается ровно заданной величиной |
| KAN-BOOT-03 | P | не начат | гейт сверки порождённого перечня обязательных величин с таблицей стража |
| KAN-FWD-01 | I | держится | `services/iam/cmd/kaname/serve_internal_principal_trust_test.go` · `TestPublicChain_HonorsVerifiedConsumerForwarder` |
| KAN-FWD-02 | I | держится | `services/iam/cmd/kaname/serve_internal_principal_trust_test.go` · `TestPublicChain_DropsForgedPrincipal_HonorsVerified` |
| KAN-FWD-03 | P | наполовину | `services/iam/cmd/kaname/trusted_forwarders_wiring_test.go` · `TestServe_ForwarderAllowListComesFromConfig` — держится происхождение круга из конфигурации; **семантика пустого круга своего замка на публичной цепочке не имеет**, он заводится этим переходом |
| KAN-WIRE-01 | P | не начат | страж размещения, читающий композиционный корень; доказан инъекцией в обе стороны |
| KAN-WIRE-02 | P | не начат | проба тождества: харнесс собирается боевым сборщиком, снятие читателя из него роняет прогон |
| KAN-POL-01 | I | не начат | проба согласия с единым перечнем по образцу двух существующих носителей |
| KAN-POL-02 | P | не начат | перепись: величина имеет читателя в не-тестовом дереве |
| KAN-OPS-01 | E | не начат | сквозной прогон по руководству установки в посадке `own`, без края |
| KAN-OPS-02 | E | не начат | проба измерителей: выданные и отвергнутые считаются раздельно |

Уровни: **E** — наблюдаемо арендатором либо оператором; **I** — проба компонента или стража;
**P** — свойство дерева, закрепляемое гейтом.

---

## 6. Классы риска, которые предмет задевает

Каждый назван вместе со сценарием, который его закрывает, и с признаком, по которому нарушение
видно **в момент написания кода**, а не после инцидента.

| Класс | Признак в момент написания | Закрывает |
|---|---|---|
| контроль на выдаче без энфорсмента на предъявлении | у записи отзыва нет читателя на пути запроса | KAN-REV-01 |
| разрыв, невидимый ни с одной стороны по отдельности | две пробы по половине: «отзыв записался» и «отвергнутое отвергается» | KAN-REV-01 |
| мягкий проход при отказе зависимости | недоступный авторитет читается как «не отозван» | KAN-REV-03 |
| оракул различимого отказа | два отказа аутентификации, различимые кодом, текстом **или** подробностями | KAN-DENY-01 |
| отрицание без положительного близнеца | утверждение «все отказы одинаковы» без годного предъявления рядом | KAN-DENY-02, KAN-REV-02, KAN-BOOT-02 |
| принято-и-проигнорировано / неисполнимая возможность | величина объявлена оператору и не читается ни одной проверкой | KAN-POL-02 |
| вакуумная проба: заголовок шире тела | харнесс собирает цепочку **не** боевым сборщиком | KAN-WIRE-02 |
| два места об одном предмете | второй перечень проверок токена рядом с единым | KAN-POL-01 |
| величина, подставляемая построением молча | у стража нет предмета: незаданного состояния не бывает | KAN-BOOT-01 |
| пустой список как «не сужаем» | попытка переопределить семантику пустого круга заодно | KAN-FWD-03 |
| внешняя зависимость у решения о доступе | проверка подписи спрашивает кого-то за пределами установки | KAN-VER-01, KAN-VER-03 |
| отзыв прежнего пути | переданная личность перестала приниматься | KAN-FWD-01, KAN-FWD-02 |

---

## 7. Объём работы, который APPROVED санкционирует

1. **Читатель предъявленного** на публичной цепочке: разбор предъявителя, проверка подписи
   собственным реестром ключей, срока, момента начала действия, издателя, адресата, типа,
   допустимости подписи и связи заголовка с ключом; чтение отзыва **на пути запроса** с коротким
   кешем положительного вердикта; fail-closed при недоступности авторитета.
2. **Отказ единой формы** — побайтово одинаковый для всех причин, включая приложенные к ответу
   подробности.
3. **Отказ обеих форм разом** — предъявленный токен вместе с переданной личностью.
4. **Ось стража старта**: проверка включена, материал не задан → отказ в пуске списком, с именем
   ключа; новые строки в порождаемом перечне обязательных величин.
5. **Профиль посадки `own`**, объявляющий это поле, — сегодня посадка `own` в дереве
   развёртывания не выбрана ни разу (§1.7).
6. **Провязка и её стражи**: присутствие читателя в публичной цепочке; харнесс, собирающий цепочку
   боевым сборщиком; проба согласия с единым перечнем проверок.
7. **Шаг руководства установки**, исполнимый без нашего края.
8. **Измерители**: выданные и отвергнутые считаются раздельно.

### Чего в объём НЕ входит

- **Свой мультиплексор REST** (#1109) — он этой приёмкой **разблокируется**, а не выполняется.
- **Отзыв или изменение прежнего пути** переданной личности — ось 7 решения запрещает прямо.
- **Пообъектная авторизация** — другой предмет, закрыт своей задачей.
- **Расписание ротации ключей подписи**: механизм в дереве есть, живого расписания нет. Это
  **отдельный предмет**, названный в §11, а не отсрочка внутри этого.
- **Проба согласия у двух платформенных носителей перечня проверок** (§1.5) — предмет чужой
  линии, чинится своей задачей (§11).
- **Пересмотр формы проверки** — решение §2 принято; переоткрытие требует своего решения владельца.

---

## 8. Порядок исполнения после APPROVED

Строгий TDD (ban #12): красная проба до кода, по каждому пункту.

1. `class-exposure-analyst` по этому документу — до первой строки кода.
2. Стражи провязки (KAN-WIRE-01, KAN-WIRE-02) — **первыми**: без них поведенческие пробы
   рискуют оказаться вакуумными, и цена этого выясняется только на сведённом дереве.
3. Поведенческие пробы VER, DUP, DENY на харнессе публичной цепочки — красные.
4. Читатель предъявленного; проба согласия с перечнем (KAN-POL-01).
5. Чтение отзыва на пути запроса и сквозная проба REV — **одним** прогоном, не двумя по половине.
6. Ось стража старта и профиль `own` (BOOT).
7. Шаг руководства и измерители (OPS).
8. `proto-api-reviewer` не требуется: контракт не меняется. `go-style-reviewer`,
   `system-design-reviewer` и `security-surface` — обязательны.

---

## 9. Определение готовности приёмки

1. каждый сценарий несёт производителя либо помечен `ждёт производителя` — §3; помеченных
   **22**, и все двадцать два заводят производителя этим же переходом;
2. каждый сценарий имеет строку в таблице состояния — §5, строк **31**, сценариев **31**;
3. у каждого отрицания назван положительный близнец, отличающийся **ровно одним** названным
   фактом — VER-01 для семейства VER, VER-01 и FWD-01 для DUP, REV-02 для REV, DENY-02 для DENY,
   BOOT-02 для BOOT; у KAN-VER-11 близнец **свой** (VER-12), потому что требование двустороннее;
4. состав проверок читателя сходится с единым перечнем **в обе стороны**: одиннадцати
   обязательным проверкам отвечают одиннадцать односфактных отрицаний §4, отступлений не
   объявлено, и закрытый перечень причин KAN-DENY-01 несёт **тринадцать** причин — по одной на
   каждое отрицание семейств VER, REV и DUP;
5. каждое число §1 снято названным предикатом на названной ревизии, и негодность одного из
   предикатов задачи названа прямо, а не обойдена — §1.1;
6. решение о форме (§2) приведено как **вход**, а не выведено этим документом;
7. документ выдерживает публикацию — §10.

---

## 10. Публичность

Оба репозитория публичны. Предмет этого документа — **отсутствующая возможность**, и это делает
его безопаснее обычного: дефект даёт **лишний отказ**, а не лишнее разрешение, и раскрываемого
состояния у него нет.

Что здесь **написано**: предмет, принятое решение, следствие для контракта, требования к
поведению читателя, координаты уже существующих механизмов, которые он переиспользует.

Чего здесь **нет** и не будет: сборки «точная координата + условие, при котором защита не
действовала + следствие для постороннего»; описания того, какой именно вход сегодня проходит
какую именно поверхность; порядка действий, которым этим можно воспользоваться. Требования
сформулированы как требования — «отказы обязаны быть неразличимы», — а не как описание того,
что сегодня различимо.

**Три места из-под запрета выведены** и выхолащивать их не следует: текст отказа оператору при
отказе старта (KAN-BOOT-01) — без него стенд не поднять; имя и текст падения пробы — это
описание защищаемого свойства; комментарий у стража — без него страж снимут как непонятный.

---

## 11. Что остаётся за пределами этого документа

Названо предметом, а не обещанием: у каждого пункта есть признак и предикат, и каждый заводится
**своей** задачей — чужой предмет в чужом изменении не чинится.

| Остаток | Признак | Предикат снятия |
|---|---|---|
| два платформенных носителя единого перечня проверок токена не несут пробы согласия | носителей 4, проб согласия 2 (§1.5) | у каждого носителя есть проба, сверяющая состав с перечнем в обе стороны |
| живого расписания ротации ключей подписи нет | механизм ротации вызывается однократно при подъёме, если активного ключа нет вовсе | ротация исполняется по расписанию, и её исполнение наблюдаемо |
| посадка `own` не выбрана ни одним профилем развёртывания | `git grep -c 'identityProvider: own' -- deploy` → 0 | профиль, на котором сценарии OPS исполнимы, существует |
| предикат задачи #2077 негоден по единице счёта | шаблон с двумя звёздами не видит файлов верхнего уровня каталога (§1.1) | предикат в теле задачи заменён на обход каталога целиком |

**Ни один из четырёх не является отсрочкой внутри этого предмета** (ban #11): у каждого свой
предмет, своя причина и свой предикат снятия. Первый и второй — чужих линий; третий заводится
объёмом §7 п. 5; четвёртый — правка тела задачи.

---

## 12. Ревью, круг 1 — ❌ CHANGES REQUESTED

> **Ревьюер:** `acceptance-reviewer` · **дата:** 2026-09-06 · **вердикт:** ❌ CHANGES REQUESTED.
> **Ревизии рецензии:** монорепо `release/kaname` @ `da18ac9929` (голова на день рецензии;
> документ снимал числа на `caa48b770d`, между ними **9** коммитов); воркспейс `lane/acc-2077`
> @ `f3305ffb`, содержимое побайтово равно `issue-382-change-package`.
> **Прежнее объявление шапки, замещённое этим кругом** (история добавляется, не переписывается):
> `**Статус:** DRAFT — вердикт выносит acceptance-reviewer (единственный approve-gate; заказчик
> подключается на шаге 7, к финальному smoke/e2e). До вердикта запрет #1 действует: кода по
> этому документу не пишется.`

### 12.1 Исход проверки посылок — числа §1 ПЕРЕМЕРЕНЫ, устаревших нет

Каждый предикат §1 прогнан **дословно** и на **обеих** ревизиях — `caa48b770d` (названа
документом) и `da18ac9929` (голова на день рецензии). Обе стороны названы вслух: «перемерил и
сошлось» иначе неотличимо от «не перемерял».

| § | величина документа | `caa48b770d` | `da18ac9929` | исход |
|---|---|---|---|---|
| 1.1 | оба предиката задачи → 0 | 0 · 0 | 0 · 0 | сошлось |
| 1.1 | контроль: `**/*.go` → 0, `*.go` → 1 | 0 · 1 | 0 · 1 | сошлось |
| 1.2 | звеньев публичной цепочки — 8 | 8 | 8 | сошлось |
| 1.3 | служб 22, из них `Internal` — 9 ⇒ 13 публичных | 22 · 9 | 22 · 9 | сошлось |
| 1.5 | носителей 4, проб согласия 2 | 4 · 2 | 4 · 2 | сошлось |
| 1.6 | `AllowedAlgorithmList` — 3 строки, 1 файл | 3 · 1 | 3 · 1 | сошлось |
| 1.7 | `identityProvider:` — 1 вхождение, `own` — 0 | 1 · 0 | 1 · 0 | сошлось |

**Устаревших чисел нет ни одного**, и это не совпадение: девять коммитов дрейфа трогают
конвейер, пробы посадки и умолчания чарта, но ни одной величины, на которую документ опирается.
Ручка посадки (§1.7) была под наибольшим риском — файлы значений в дрейфе изменены — и **не**
изменилась.

**Качественные утверждения проверены отдельно и подтвердились, а два — сильнее заявленного:**

- **§1.1 верно и о своём предмете, и о предикате задачи.** Единственное вхождение —
  перечень ключей, чьи значения не пишутся в журнал; читателем оно не является. Негодность
  предиката задачи (шаблон с двумя звёздами не видит файлов верхнего уровня каталога)
  воспроизведена контролем в обе стороны.
- **§1.2 сошлось поэлементно**: 3 звена + пара извлечения личности + 3 звена = 8; разметка
  отказа действительно не трогает коды, кроме `PermissionDenied`.
- **§1.4 подтверждено самим деревом, и это сильнейшее свидетельство документа**: авторитет
  отзыва объявляет отступлениями ровно адресат и тип токена, и **причина отступления сама
  требует** того читателя, который заводится этим переходом. Шапка того же пакета объявляет
  норму оси 4 дословно. Кеша у него нет.
- **§1.6 сильнее, чем сказано в документе.** Величина не только не читается ни одной
  проверкой — **текст отказа стража старта утверждает, что на этом перечне держится проверка
  связи заголовка с ключом**, а такой проверки на этой поверхности нет. То есть это
  «неисполнимая возможность» плюс комментарий, противоречащий коду. Это усиливает KAN-POL-02
  и прямо связано с блокирующим замечанием 12.2.
- **§5: все три названных существующих держателя резолвятся** — файлы и имена проб совпадают
  дословно.

### 12.2 БЛОКИРУЮЩЕЕ 1 — решение §2 называет ДВА предмета, сценарий получил ОДИН

**Координата.** §2 ось 3 (приведена документом как принятый **вход**): «адресат и тип —
свойства поверхности предъявления; у выданного наружу авторитета они объявлены отступлением
(§1.4), и **закрыть их обязан именно этот читатель**». §7 п. 1 санкционирует сверх того «типа,
допустимости подписи и связи заголовка с ключом». KAN-POL-01 требует согласия состава проверок
читателя с единым перечнем **в обе стороны**.

**Предикат.** Единый перечень обязателен целиком — его состав в дереве:

```sh
git grep -n -A14 'func MandatoryChecks' -- pkg/tokenpolicy/policy.go   # 11 проверок
```

Сопоставление одиннадцати с семейством VER + REV даёт: подпись → KAN-VER-03 · допустимость
подписи → KAN-VER-06 · издатель → KAN-VER-07 · адресат → KAN-VER-04 · срок → KAN-VER-02 ·
момент начала → KAN-VER-05 · отзыв → KAN-REV-01. **Покрыто 7 из 11.** Без сценария остаются
**тип токена**, **связь заголовка с ключом**, **критические заголовки** и **идентификатор
ключа** (последний в лучшем случае слит с KAN-VER-03).

**Почему это блокирует, а не является придиркой.** Дело не в арифметике покрытия, а в двух
следствиях:

1. **документ противоречит собственному принятому решению.** Ось 3 называет ровно две вещи,
   которые этот читатель существует закрыть, — адресат **и тип**. Адресат получил KAN-VER-04
   с отдельной врезкой, объясняющей, почему он несущий. Тип не получил ничего. Из двух
   названных предметов наблюдаемым поведением закреплён один;
2. **перечень причин KAN-DENY-01 закрыт девятью пунктами** («истёк · чужой ключ · чужой
   адресат · время не наступило · подпись вне перечня · чужой издатель · отозван · авторитет
   отзыва недоступен · обе формы разом») и четырёх названных причин **не содержит**. То есть
   отказ по типу токена, по связи заголовка с ключом и по критическим заголовкам **ничем не
   обязан** быть неразличим с остальными — при том что неразличимость отказов есть
   центральная гарантия документа (§2 ось 5). Сценарий, который должен был бы поймать оракул,
   этих отказов не наблюдает.

Отдельно и в пользу автора: **связь заголовка с ключом** — это ровно та проверка, чьё
отсутствие §1.6 предъявляет как «неисполнимую возможность». Она санкционирована §7, объявлена
стражем старта, названа в тексте его отказа — и не имеет ни сценария, ни строки в перечне
неразличимости.

**Применимый исход — один из трёх, выбор за автором:**

- **переписать «Тогда» под производимое** — завести недостающие односфактные отрицания по
  образцу KAN-VER-02…07 (производитель у них тот же читатель, он заводится этим переходом,
  поэтому нового производителя заказывать не нужно) **и** расширить перечень причин
  KAN-DENY-01 до полного;
- **сузить обещание явно** — объявить недостающие проверки **отступлениями с причиной** в §7
  и в KAN-POL-01, ровно тем механизмом, которым авторитет отзыва объявляет свои. Тогда
  отсутствие сценария законно, потому что отсутствует и сама проверка;
- **снять задачей-преемником** — вынести их своей задачей с признаком и предикатом, добавив
  строку в §11.

«Оставить как есть» исходом не является: сегодня §7 их санкционирует, KAN-POL-01 их требует, а
§4 их не наблюдает.

### 12.3 БЛОКИРУЮЩЕЕ 2 — KAN-REV-04 не может упасть на отсутствии кеша

**Координата.** KAN-REV-04, первое «Тогда»: «вызов **ещё может дойти** до обработчика — это
объявленное окно, а не дефект».

**Предикат — рассуждением о представимом мире, проверяется чтением четырёх сценариев REV.**
Возьмём реализацию, у которой кеша нет вовсе и отзыв спрашивается на каждом запросе:

| сценарий | исход без кеша | почему проходит |
|---|---|---|
| KAN-REV-01 | проходит | после ожидания токен отвергнут — да |
| KAN-REV-02 | проходит | отзыва нет, токен принят — да |
| KAN-REV-03 | проходит | положительного вердикта в кеше нет **by construction** |
| KAN-REV-04 | проходит | «может дойти» верно и когда не доходит |

**Ни один сценарий документа не краснеет на реализации без кеша** — при том что короткий кеш
объявлен осью 4 решения (§2) и стоит в объёме §7 п. 1 («с коротким кешем положительного
вердикта»). Утверждение, принимающее оба исхода, утверждением не является — это тот самый
класс, который документ сам цитирует в KAN-DENY-02 и в §6.

Довод «окно недетерминированно» здесь не проходит: §5 назначает KAN-REV-04 держателем «пробу
**с управляемыми часами**», а при управляемых часах исход внутри окна детерминирован.

**Применимый исход:** **переписать «Тогда» под производимое** — сделать первое утверждение
решительным («вызов доходит до обработчика, потому что положительный вердикт закеширован и
срок кеша не истёк»), чтобы отсутствие кеша роняло сценарий. Заказывать производителя не
нужно: кеш уже в объёме §7 п. 1.

### 12.4 Не блокирующее — учесть при правке

1. **§3, сводная фраза** «производитель у них один — читатель предъявленного» точна для 14 из
   17 помеченных и неточна для KAN-OPS-01 (шаг руководства), KAN-OPS-02 (измерители) и
   отчасти KAN-POL-02: их производители — §7 пп. 7 и 8. Сам объём §7 их перечисляет верно,
   расходится только обобщение.
2. **KAN-VER-01, последнее «И»** («ни один шаг проверки не обращался к внешнему поставщику»)
   в посадке `own` истинно **by construction** — поставщика в этой установке нет. Это не
   ошибка, но и не наблюдение: стоит либо назвать его свойством построения, либо сказать, чем
   оно наблюдается у оператора.
3. **§1.6 стоит усилить** тем, что нашла рецензия: текст отказа стража утверждает наличие
   проверки, которой нет. Это укрепляет KAN-POL-02 и связывает его с 12.2.

### 12.5 Ось публикуемости — ПРОЙДЕНА

Проверено, а не принято на веру: документ прочитан как посторонним, и отдельно проверено
**само основание** его довода — что дефект даёт лишний отказ, а не лишнее разрешение.

- **Основание истинно.** Последнее звено публичной цепочки — пообъектная дверь — строго
  fail-closed и стоит на **каждом** вызове обеих полос, а не только на мутациях. Поэтому
  «назваться нечем» оканчивается отказом, и состояния, которым можно воспользоваться, из
  документа не восстанавливается.
- **Сборки «координата + условие, при котором защита не действовала + следствие для
  постороннего» в документе нет.** Требования сформулированы как требования; какой вход
  сегодня какую поверхность проходит, документ не описывает.
- **Ближайшее к границе место** — строка звена 7 в §1.2, называющая, что отсечка анонима
  пропускает чтение. Границу она не переходит: следствия из неё не выводится, а соседняя
  строка той же таблицы называет дверь, которая на чтении и стоит. Оставить как есть.
- **Три выведенных из-под запрета места** (§10) названы верно и выхолащиванию не подлежат.

Тем же требованием связан и **текст этой рецензии**: он называет предметы и требования и не
собирает восстановимого состояния.

### 12.6 Что осталось неустановленным

Названо, чтобы «не проверено» не читалось как «проверено и сошлось»:

- **истинность объявленного вердикта машинно не проверяется** — распознаватель набора
  `docs-gate` судит **объявление** в шапке, а не одобрение. Это его известный предел, а не
  свойство этого документа;
- **§1.2 «звеньев восемь»** проверено чтением композиционного корня, а не прогоном: у числа
  звеньев цепочки исполняемого предиката в дереве нет;
- **замысел линии** (`docs/changes/standalone-iam/design.md`, файл на ветке присутствует)
  **не рецензировался** — он вне предмета этой рецензии, документ на него опирается законно;
- **соседние задачи линии** (#2086, #2090…#2096) не разбирались — чужой предмет.

### 12.7 Что делать дальше

`acceptance-author` закрывает 12.2 и 12.3 — обе правки в существующей структуре документа
(семейство VER уже устроено ровно под односфактные отрицания, KAN-DENY-01 — под перечень
причин), после чего круг 2. **Объём §7, решение §2 и порядок §8 под сомнение не ставятся**;
§1 перемерен и правки не требует.

---

## 13. Ответ автора на круг 1 — правка внесена, круг 2

> **Автор:** `acceptance-author` · **дата:** 2026-09-06 · **предмет:** ровно два блокирующих
> замечания §12.2 и §12.3. Запись **добавлена**, а не заменяет §12: прежний вердикт относится к
> прежней редакции и остаётся её вердиктом дословно.
> **Ревизия дерева продукта, на которой проверялись производители:** `2d2b508076`. Обе ревизии
> рецензии (`caa48b770d`, `da18ac9929`) в этом дереве резолвятся; величины §1.5 на всех трёх
> совпадают — носителей **4**, проб согласия **2**.
>
> **Прежнее объявление шапки, замещённое этой правкой** (история добавляется, не переписывается):
> `**Статус:** CHANGES REQUESTED — acceptance-reviewer, круг 1, 2026-09-06 (ban #1). Кодировать
> по этому документу **нельзя**: два блокирующих замечания в §12. Оба структурные и дешёвые —
> объём §7 и замысел §2 под сомнение не ставятся, правится покрытие сценариями. Прежнее
> объявление шапки сохранено в §12.`
>
> **Вердикта эта запись не выносит.** Автор приёмки не является approve-gate: круг 2 выносит
> `acceptance-reviewer`. До него запрет #1 действует.

### 13.1 Исход проверки посылок круга 1 — обе стороны названы

| посылка §12 | исход |
|---|---|
| единый перечень содержит **11** проверок | **сошлось** — `git grep -n -A14 'func MandatoryChecks' -- pkg/tokenpolicy/policy.go` |
| покрыто сценариями **7** из 11 | **сошлось** поэлементно: подпись · допустимость подписи · издатель · адресат · срок · момент начала · отзыв |
| без сценария — тип, связь заголовка с ключом, критические заголовки, идентификатор ключа | **сошлось**; идентификатор ключа был **слит** с KAN-VER-03, как и предполагала рецензия |
| §2 ось 3 называет **два** предмета — адресат и тип | **сошлось** дословно |
| перечень причин KAN-DENY-01 закрыт **девятью** и четырёх не содержит | **сошлось** |
| KAN-REV-04 истинен при любом исходе; читатель без кеша проходит все четыре REV | **сошлось** — разобрано в §13.3 |
| правка помещается **внутрь существующей структуры** | **сошлось**: новых разделов §4 не заведено, семейство VER продолжено, перечень причин расширен на месте |
| «нового производителя заказывать не нужно» | **сошлось, и сильнее заявленного** — см. §13.2: механизм **каждой** из четырёх проверок в дереве уже есть, причём три из четырёх объявлены **всеми четырьмя** носителями перечня; недостаёт потребителя на этой поверхности |

### 13.2 Ответ на §12.2 — выбран исход «переписать под производимое», по всем четырём

**Почему не «сузить обещание отступлением».** Отступление обязано называть, **что делает
проверку неприменимой здесь**, а не когда её собираются добавить. Такой причины нет ни у одной из
четырёх, и это измерено по всем четырём носителям перечня (§1.5), а не выведено из одного:

| проверка | сколько носителей из 4 её **объявляют** | кто и почему **отступает** |
|---|---:|---|
| связь заголовка с ключом | **4** | никто |
| идентификатор ключа | **4** | никто |
| критические заголовки | **4** | никто |
| тип токена | **3** | авторитет отзыва — с причиной: тип сверяет **поверхность предъявления**, а не издатель |

Публичный слушатель и есть та поверхность, на которую причина единственного отступления и
указывает. Объявить отступление здесь значило бы сказать, что тип не сверяет **никто**.

Отдельно, чтобы число не читалось шире, чем оно есть: у двух платформенных носителей объявление
несёт **десять** записей статически, а одиннадцатая — отзыв — попадает в него тогда и только
тогда, когда его объявила хотя бы одна запись издателя. Для довода выше это ничего не меняет:
все четыре спорные проверки стоят в статической части.

**Почему не «снять задачей-преемником».** Преемник законен там, где предмет чужой (§11 — четыре
таких). Здесь предмет свой: §2 ось 3 называет тип прямо, §7 п. 1 санкционирует тип, допустимость
подписи и связь заголовка с ключом, а §7 п. 6 санкционирует пробу согласия с единым перечнем —
и согласие это **двустороннее**. То есть объём уже обязывает читателя ко всем одиннадцати:
исполнить либо объявить отступление. Третьего состояния нет, и вынести часть перечня в преемника
значило бы оставить читателя, который своей же пробе согласия не удовлетворяет.

**Найденные производители — по одному на проверку.** «Производитель» здесь тот же, что в §3:
механизм, эмитирующий наблюдаемое. Ни одна из четырёх проверок этой приёмкой **не изобретается**
— заводится её потребитель на публичной цепочке.

| проверка | сценарий | механизм в дереве | чем проверено |
|---|---|---|---|
| тип токена | KAN-VER-08 | объявляют три носителя из четырёх; четвёртый — авторитет отзыва — объявляет отступлением **с причиной**, называющей поверхность предъявления | `git grep -n 'CheckTokenType' -- '*.go' ':!*_test.go'` |
| связь заголовка с ключом | KAN-VER-09 | объявляют **все четыре** носителя | `git grep -n 'CheckKeyBoundAlgorithm' -- '*.go' ':!*_test.go'` |
| идентификатор ключа | KAN-VER-10 | объявляют **все четыре**; форма идентификатора ограничивается **до** использования, разрешение — по собственному реестру записи | `git grep -n 'CheckKeyID' -- '*.go' ':!*_test.go'` |
| критические заголовки | KAN-VER-11 + KAN-VER-12 | объявляют **все четыре**; исполняются **общей** функцией платформы, несущей обе стороны требования сразу; перечень исполняемых параметров объявлен и сегодня пуст — решением, а не заготовкой | `git grep -n 'CriticalHeadersUnderstood' -- '*.go' ':!*_test.go'` |

**Что изменено в §4.** Пять новых односфактных сценариев по образцу KAN-VER-02…07 — KAN-VER-08
(тип) · KAN-VER-09 (связь заголовка с ключом) · KAN-VER-10 (идентификатор ключа) · KAN-VER-11
(помеченный обязательным параметр) · KAN-VER-12 (**положительный** близнец к KAN-VER-11).
KAN-VER-03 **сужен** до одного факта: прежняя редакция меняла и разрешение идентификатора, и
сходимость подписи разом.

**KAN-VER-12 заведён не для симметрии.** Требование о помеченных обязательными параметрах
двустороннее: ужесточение одной половины без другой ломает совместимость. Без положительного
близнеца читатель, отвергающий **любой** незнакомый параметр, проходил бы KAN-VER-11 как
правильный — то есть отрицание зеленело бы на слишком строгой реализации.

**Перечень причин KAN-DENY-01 расширен вместе со сценариями — с 9 до 13**, и к нему добавлено
утверждение о том, что перечень **сходится с семейством отрицаний**. Иначе повторилась бы ровно
та дыра, которую нашла рецензия: покрытие перечня проверок шире покрытия неразличимости, и
отказ, не попавший в перечень, неразличимости ничем не обязан.

**KAN-POL-01 уточнён**: перечень отступлений этого читателя **пуст осознанно** — все одиннадцать
исполняются; и каждой проверке отвечает своё отрицание, а каждому отрицанию — строка перечня
причин.

**§7, §2 и §8 не тронуты.** Объём уже санкционировал всё внесённое: п. 1 называет тип,
допустимость подписи и связь заголовка с ключом прямо, разрешение идентификатора входит в
проверку подписи собственным реестром ключей, а п. 6 требует согласия с перечнем в обе стороны.

### 13.3 Ответ на §12.3 — выбран исход «переписать под производимое»

Первое утверждение KAN-REV-04 сделано **решительным**: вызов **доходит** до обработчика, потому
что положительный вердикт закеширован и объявленный срок кеша не истёк. Добавлено требование
**управляемых часов** — оно уже стояло в §5 как держатель, но в самом сценарии названо не было,
а именно оно делает исход внутри окна определённым.

**Проверка, что сценарий теперь способен упасть.** Читатель без кеша спрашивает авторитет на
каждом предъявлении, поэтому сразу после записи отзыва он вызов **отвергнет** — а сценарий
требует пропустить. KAN-REV-04 краснеет, и блокирующее «ни один сценарий не краснеет на
реализации без кеша» снято.

**Что осталось верным и не правилось.** KAN-REV-03 по-прежнему выполним на читателе без кеша:
его предусловие «положительного вердикта в кеше нет» такой читатель удовлетворяет by
construction. Это не делает его вакуумным **относительно собственного предмета** — он утверждает
fail-closed при недоступности авторитета, и на читателе, засчитывающем неполученный ответ за
«не отозван», он краснеет. Отличать кеш от его отсутствия — работа KAN-REV-04, и теперь она
делается.

### 13.4 Замечания §12.4 — что сделано и что нет

| # | замечание | исход |
|---|---|---|
| 1 | сводная фраза §3 «производитель у них один» неточна для KAN-OPS-01, KAN-OPS-02 и отчасти KAN-POL-02 | **исправлено**: производители названы порознь, со ссылками на §7 пп. 7 и 8. Правка была вынужденной — числа §3 менялись этой же правкой |
| 2 | последнее «И» KAN-VER-01 истинно by construction | **исправлено**: названо свойством построения и сказано, чем оно наблюдается у оператора |
| 3 | §1.6 стоит усилить находкой рецензии | **не сделано намеренно**: §1 перемерен рецензией и правки не требует (§12.7), а усиление §1.6 — правка §1. Оставлено следующему кругу как **необязательное**; наблюдение рецензии при этом использовано — оно стоит доводом в KAN-VER-09 |

### 13.5 Числа круга

| величина | было | стало |
|---|---:|---:|
| сценариев | 26 | **31** |
| ждут производителя | 17 | **22** |
| производитель есть | 9 | **9** |
| строк в таблице §5 | 26 | **31** |
| обязательных проверок покрыто сценариями | 7 из 11 | **11 из 11** |
| причин в закрытом перечне KAN-DENY-01 | 9 | **13** |
| объявленных отступлений у читателя | не назван | **0, осознанно** |

### 13.6 Что осталось неустановленным

- **исполнимость новых сценариев на стенде не проверялась** — читателя ещё нет, и это ожидаемо:
  кода по приёмке до APPROVED не пишется. Все пять помечены `ждёт производителя` и в зачёт
  покрытия держателем не идут;
- **§1 не перемерялся заново** — рецензия перемерила его на двух ревизиях, и правка §1 в предмет
  этого круга не входила. Перепроверены только величины §1.5, которые понадобились доводу
  §13.2, — они совпали;
- **соседние задачи линии** (#2086, #2090…#2096) и замысел линии не разбирались — чужой предмет;
- **выбор между «исполнить» и «объявить отступление»** сделан автором по существу и подлежит
  вердикту рецензента: довод — отсутствие причины, которая делала бы проверку неприменимой
  **на поверхности предъявления**, а не очередь работ.
