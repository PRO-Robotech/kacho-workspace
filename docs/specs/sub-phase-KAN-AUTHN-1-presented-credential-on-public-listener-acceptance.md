# Sub-phase KAN-AUTHN-1 (публичный слушатель принимает предъявленное удостоверение) — Acceptance

> **Статус:** DRAFT — **редакция 4, ждёт круга 7**. Кодировать **нельзя**: вердикт APPROVED круга 4 относится к отпечатку **`f77d062d`** (коммит, который круг 4 и рассматривал) и на эту редакцию **не переносится** — правка одобренного документа вердикт не переносит (`change-graph.md` §2). Прежняя редакция шапки объявляла APPROVED на отпечатке, которого **не рассматривал ни один круг**; сам факт круга 5 это опровергает, и утверждение снято тем же порядком, каким редакция 2 переводилась в черновик.
> **Прежнее объявление шапки, сохранённое дословно:** APPROVED — `acceptance-reviewer`, **круг 4**, 2026-09-07 (ban #1, единственный approve-gate). Кодировать **разрешено**: объём §7 в порядке §8. Блокирующее круга 3 снято по существу; три замечания круга 4 внесены тем же коммитом и отдельным кругом не гонялись.
> **Записи ревью §12–§17 — не правлены ни байтом.** В §18 правлена **одна строка**: ссылка на шаг фазы, который редакция 3 переиндексировала (§19.7). История ревью добавляется, а не переписывается; ссылка, ставшая ложной от переиндексации, — не переписывание истории, а приведение её к факту, и названа она отдельно, а не спрятана под «не тронуто». Что пересматривает редакция 2 — §15; круг 3 — §16; круг 4 — §17; разбор классов риска — §18; круг 5 — §19; круг 6 — §20.
> **Область одобрения названа узко, и границ ДВЕ.** Предмет кругов 3 и 4 — **внутренняя согласованность документа**, а не дерево; стенд не поднимался, о поведении на кластере вердикта нет (§17.5). И: **исполнима сейчас только Ф0** (её остаток — три пункта, §5), а Ф1 и Ф2 ждут ответа владельца на вопрос 1 §11 — форму взаимоисключения полос. Санкция §7 их покрывает, вход — ещё нет.
> **Прежнее объявление шапки, сохранённое дословно:** APPROVED — `acceptance-reviewer`, круг 2, 2026-09-06 (ban #1, единственный approve-gate). Кодировать **разрешено**: объём §7 в порядке §8. Оба блокирующих замечания круга 1 закрыты и проверены прогоном, а не прочтением. Одно не блокирующее — §14.4, правится в посадочном коммите. Прежние объявления шапки сохранены: круга 1 — в §12, ответа автора — в §13, круга 2 — в §14.
> **Дата:** 2026-09-06 · **Ревьюер:** `acceptance-reviewer` (вердикт) · **Автор правки круга 2:** `acceptance-author`.
> **Задача:** продукт #2077 (редакция 1) · **#2191, P0** (редакция 2). **Отзывает решение по:** #2100. **Наблюдалось в:** #2188. **Разблокирует:** #1109 (свой мультиплексор REST) → #1111. **Линия:** `release:standalone-iam`.
> **Ревизия, на которой сняты ВСЕ числа §15:** монорепо `release/kaname` @ `fe0e5a60f3`; воркспейс `main` @ `98e0a01a`. Замер 2026-09-07. Числа §1 остаются числами **своей** ревизии (`caa48b770d`) и здесь не переписываются: замер верен на дату, а не бессрочно.
> **Ревизии, на которых сняты ВСЕ числа §1:** монорепо `github.com/PRO-Robotech/kacho`, ветка `release/kaname` @ `caa48b770d`; воркспейс `lane/acc-2077` от `issue-382-change-package`. Замер 2026-09-06.
> **Замысел линии:** `docs/changes/standalone-iam/design.md` — этот документ на него **опирается** и его не пересказывает и не правит.
> **Род документа:** **ПРИЁМКА, САНКЦИОНИРУЮЩАЯ РАБОТУ** — несёт сценарии Given-When-Then и после APPROVED разрешает кодировать объём §7 в порядке §8. Кода не содержит.
> **Публичный репозиторий:** документ написан так, чтобы выдерживать публикацию (`security.md` §«Публичные артефакты»). Предмет — **отсутствующая возможность**, а не обход: отсечка анонима отвергает всех без личности, то есть дефект даёт лишний отказ, а не лишнее разрешение. Сборка «координата + условие, при котором защита не действовала + следствие для постороннего» здесь не пишется; требования сформулированы как требования, а не как описание того, что сегодня проходит. Отдельно см. §10.

## 0. Обзор

Служба доступа выносится самостоятельным продуктом **Kaname**, устанавливаемым в чужом облаке.
Личность на её публичном слушателе производится ровно двумя способами, и оба предполагают
**нашу** инфраструктуру рядом: клиентский сертификат проверенного пира и личность,
**переданная** разрешённым отправителем. В чужом облаке нет ни нашего края, чтобы передать, ни
модульного сертификата у человека — арендатору **нечем назваться**.

При этом служба чеканит токены сама, публикует набор ключей проверки и несёт авторитет отзыва.
Больше того: **полная проверка нашего собственного токена в дереве уже написана** — она стоит на
поверхности, которую служба **предоставляет другим**, и не стоит на пути её **собственного**
публичного вызова (§1.4). То есть предмет — не «написать проверяющего с нуля», а **поставить
читателя предъявленного на публичную цепочку** и снабдить его тем, чего у выданной наружу
проверки нет по построению: адресатом, типом токена и коротким кешем.

Эта приёмка закрепляет наблюдаемое поведение такого читателя: что он принимает, что отвергает,
чем отказы **неразличимы между собой**, почему отзыв обязан читаться **на предъявлении**, и
почему прежний путь при этом не отзывается.

---

## 1. Что верно в дереве сегодня

Каждое число снято названным предикатом на ревизии `caa48b770d`. Единица счёта названа у
каждого. Предикаты прогнаны **с контролем в обе стороны**: предикат, который умеет находить,
обязан это показать на заведомом примере, иначе «ноль находок» неотличимо от «ноль
прочитанного».

### 1.1 Читателя предъявленного удостоверения на публичной цепочке нет — и предикат задачи считает НЕ ТО

Задача #2077 приводит два предиката и оба дают ноль. **Оба воспроизведены, оба дали ноль.**
Единица счёта — не-тестовый файл Go, читающий метаданные `authorization`.

```sh
git grep -c '"authorization"' -- 'services/iam/**/*.go' ':!*_test.go'   # вывод пуст → 0 файлов
git grep -c '"authorization"' -- 'pkg/grpcsrv/**/*.go'  ':!*_test.go'   # вывод пуст → 0 файлов
```

**Второй предикат при этом негоден, и это надо сказать прямо.** Шаблон `pkg/grpcsrv/**/*.go`
требует промежуточного каталога, поэтому файлы, лежащие в `pkg/grpcsrv` **непосредственно**,
в предмет не попадают вовсе. Контроль:

```sh
git grep -l '"authorization"' -- 'pkg/grpcsrv/**/*.go' ':!*_test.go' | wc -l   # 0
git grep -l '"authorization"' -- 'pkg/grpcsrv/*.go'    ':!*_test.go' | wc -l   # 1
```

Единственное найденное вхождение — **перечень ключей, чьи значения не пишутся в журнал**, то
есть маркер «не логировать», а не читатель. Вывод задачи от этого не меняется, **её предикат —
меняется**: он под-читывает на один файл, и под-читанный файл читателем тоже не является.
Полоса, по которой предикат должен ходить, — каталог целиком (`-- pkg/grpcsrv`), а не шаблон
с двумя звёздами. Это тот же класс, что `testing.md` §«Твой СОБСТВЕННЫЙ разборщик — тоже
распознаватель»: форма записи, о которой выражение не знает, даёт не красное и не зелёное, а
молчание.

Тем же предикатом по **всему** дереву читателей ровно два, и оба вне Kaname:
`gateway/internal/middleware/auth.go` (край платформы) и упомянутый перечень в `pkg/grpcsrv`.

### 1.2 Звенья публичной цепочки — восемь, по порядку

Каталог композиционного корня **переехал**: задача называет `services/iam/cmd/kacho-iam/serve.go`,
в дереве это `services/iam/cmd/kaname/serve.go` (сборка `publicUnary`). Предикат:
`git grep -n 'publicUnary' -- services/iam`.

| # | звено | что производит |
|---|---|---|
| 1 | измеритель задержки | ряд длительности и кода **на весь** вызов, включая отказ по правам |
| 2 | перехват паники | паника ниже по цепочке → `Internal` на **один** запрос вместо падения процесса |
| 3 | разметка отказа | машиночитаемая причина на `PermissionDenied`; **иные коды не трогает** |
| 4 | личность из проверенного клиентского сертификата | личность модуля из SAN сертификата пира |
| 5 | личность, **переданная** разрешённым отправителем | личность конечного пользователя — **только** если (4) доказало проверенного пира из круга |
| 6 | политика вызывающего | кому вообще позволено звать этот RPC на этом слушателе |
| 7 | отсечка анонима | отказ мутации без личности; **чтение и белый список пропускаются** |
| 8 | дверь | пообъектный вопрос к модели доступа |

Звенья 4–5 — пара `PrincipalExtract*` общего транспортного пакета. **Читателя предъявленного
арендатором удостоверения среди восьми нет ни одного.**

### 1.3 Поверхность, которую это делает недосягаемой, — тринадцать публичных служб

Единица счёта — объявление `service` в контракте домена.

```sh
git grep -h '^service ' -- 'proto/kacho/cloud/iam/v1/*.proto' | wc -l              # 22
git grep -h '^service ' -- 'proto/kacho/cloud/iam/v1/*.proto' | grep -c '^service Internal'  # 9
```

**13** публичных служб против 9 внутренних. Ни одну из тринадцати арендатор чужого облака
позвать не может: назваться нечем, и отказ приходит честный и бесполезный.

### 1.4 Проверка нашего токена в дереве ЕСТЬ — но не на пути собственного запроса

Это главное уточнение к постановке задачи, и оно удешевляет работу.

| механизм | где стоит | что проверяет | достижим ли арендатором |
|---|---|---|---|
| авторитет отзыва (интроспекция) | **внутренний** слушатель, требует клиентского сертификата | подпись своим набором ключей, издателя, срок, критические заголовки, **отзыв** | нет |
| проверка на пути запроса | **край платформы**, не Kaname | оба издателя, с кешем набора ключей | нет — края в чужом облаке нет |
| проверка входящего **утверждения клиента** | полоса выдачи Kaname | подпись клиента перед выдачей ему токена | да, но это **вход в выдачу**, а не предъявление уже выданного |

Предикаты: `git grep -n 'func (h \*Handler) judge' -- services/iam/internal/handler/tokenintrospecthttp`;
`git grep -n 'func (v \*JWTVerifier) Verify' -- gateway/internal/middleware`.

Пакет авторитета отзыва объявляет в своей шапке ровно ту норму, ради которой заведена ось 4
решения (§2): контроль, действующий на выдаче и не действующий на предъявлении, отзывом не
является. **Авторитет построен; потребителя у него на публичной цепочке нет.**

**Чего у авторитета нет по построению** — и почему он не годится «как есть»: он объявляет
**отступления** от единого перечня проверок, и отступления эти суть адресат и тип токена,
названные свойствами **поверхности предъявления**, а не издателя. Публичный слушатель и есть та
поверхность. Кеша у авторитета тоже нет — на каждый вопрос идёт обращение к хранилищу.

### 1.5 Единый перечень проверок токена существует; носителей четыре, проб согласия — две

Перечень — `pkg/tokenpolicy`, одиннадцать проверок: подпись · допустимость подписи · связь
заголовка с ключом · издатель · адресат · тип токена · срок · момент начала действия ·
идентификатор ключа · критические заголовки · отзыв.

```sh
git grep -ln 'func (.*) DeclaredChecks()' -- '*.go' ':!*_test.go' | wc -l   # 4 носителя
git grep -ln 'MissingChecksExcept'        -- '*_test.go' | grep -v pkg/tokenpolicy | wc -l  # 2 пробы
```

Из четырёх носителей два принадлежат Kaname и **оба несут пробу согласия** с перечнем; два
принадлежат платформе и пробы согласия в своих пакетах **не несут**. Читатель, заводимый этой
приёмкой, — третий носитель Kaname, и он обязан нести пробу согласия по образцу двух уже
существующих. Расхождение двух платформенных носителей — **не предмет этой приёмки**, оно
названо в §11.

### 1.6 Возможность объявлена оператору и читателя не имеет

Посадка `own` уже объявляет оператору две величины, чьё назначение в её же документации названо
приёмом **на входе**: издателя, признаваемого своим, и перечень принимаемых подписей. Обе
величины судятся стражем старта. Читателя у перечня подписей в дереве **нет**:

```sh
git grep -n 'AllowedAlgorithmList' -- '*.go' ':!*_test.go'   # 3 строки, все в ОДНОМ файле
git grep -l 'AllowedAlgorithmList' -- '*.go' ':!*_test.go' | wc -l   # 1 — сам объявляющий файл
```

То есть величина объявлена, задокументирована, провалидирована при старте — и **не читается ни
одной проверкой**. Это `api-conventions.md` §«Неисполнимая возможность»: два правила об одном
поле, из-за которых объявленное не исполняется ни при каком входе. Заводимый читатель закрывает
это **по построению**: он и есть тот, кто обе величины читает.

### 1.7 Посадка `own` в дереве развёртывания не выбрана ни разу

```sh
git grep -n 'identityProvider:' -- deploy          # 1 вхождение, значение external
git grep -c 'identityProvider: own' -- deploy      # 0
```

Умолчания у ручки нет намеренно; базовый профиль чарта объявляет `external`, и **ни один**
профиль его не переопределяет. Следствие для этой приёмки: сценарии посадки `own` наблюдаются на
профиле, которого сегодня в дереве нет, и он заводится вместе с работой по §7. Это назван**о**, а
не подразумевается.

---

## 2. Решение о форме проверки — ПРИНЯТО, здесь оно не переоткрывается

Задача #2077 прямо говорит, что оси 2 и 3 её предиката требуют отдельного решения о **форме**.
Решение принято владельцем линии и приводится здесь как **вход** приёмки. Сценарии §4 описывают
его наблюдаемым поведением; спор о самой форме предметом этого документа не является.

**Оси 1–7 редакцией 2 не тронуты.** Ось 8 **переписана**, оси 9–11 **заведены**: помечены `⟲` и
`＋`. Что именно отзывается, каким замером и почему прежняя формулировка была миной, а не
требованием, — §15; здесь стоит только принятое, чтобы решение читалось в одном месте.

| ось | решение | почему это несущее |
|---|---|---|
| **1. что читается** | предъявитель в метаданных запроса, стандартной формой | иной формы у арендатора нет — он приходит обычным клиентом |
| **2. чем проверяется подпись** | собственным реестром ключей службы | служба сама издатель ⇒ **внешней зависимости у проверки нет by construction**; для установки в чужом облаке это и есть условие исполнимости |
| **3. что ещё проверяется** | срок · момент начала действия · издатель (обязан быть собственным) · адресат | адресат и тип — свойства **поверхности предъявления**; у выданного наружу авторитета они объявлены отступлением (§1.4), и закрыть их обязан именно этот читатель |
| **4. отзыв читается НА ПРЕДЪЯВЛЕНИИ** | читателем служит полоса интроспекции, с коротким кешем; недоступность авторитета — **fail-closed** | контроль на выдаче отзывом не является: предъявленное проходило бы до истечения срока, и это состояние **не сходится само**. Окно тогда задаёт срок кеша, а не срок удостоверения |
| **5. отказы неразличимы** | все отказы аутентификации **побайтово** одинаковы | различимый отказ здесь — оракул: он сообщает предъявителю, какая половина неверна |
| **6. страж старта** | отказ в пуске, когда проверка включена, а материал не задан | величина, которую построение подставляет молча, предметом стража быть не может: он зелен при любом входе |
| **7. прежний путь не отзывается** | переданная личность от разрешённого отправителя остаётся; пустой круг по-прежнему означает «не сужаем», а не «запрещаем» | за нашим краем этот путь законен, и отзыв его сломал бы платформу ради установки, которой края не нужно |
| **8. полосы взаимоисключающи ПОСТРОЕНИЕМ, а не проверкой** (⟲ редакция 2) | край **снимает** арендаторское удостоверение перед пересылкой за себя, установив личность сам. За краем действует переданная личность; напрямую — предъявленная. Сочетания не бывает | неоднозначность «кто звонит» не разрешается ни догадкой, ни проверкой: проверку можно обойти, построение — нет. Прежняя редакция оси требовала **отвергать** сочетание, и это отвергало бы **весь** платформенный трафик — §15.2 |
| **9. посадка ОБЪЯВЛЯЕТ ручку читателя** (＋ редакция 2) | ручка приёма предъявленного объявлена в значениях чарта и в шаблоне настроек — там же, где объявлена соседняя ручка того же блока | механизм, которого нет в объявлениях чарта, не поднимается ни на одной посадке: он присутствует в дереве и не исполняется никогда. Проверка, не отказавшая ни разу за свою жизнь, отличима от исправной только замером |
| **10. страж СВЯЗЫВАЕТ фронт и читателя** (＋ редакция 2) | поднят собственный **публичный** фронт ⇒ читатель предъявленного включён. Иначе — отказ пуска | половина пары хуже отсутствия обеих: она выглядит настроенной. Антецедент — **поднятый фронт**, а не объявленная посадка: фронт поднимается на любой посадке, и связывание с посадкой оставляет ровно ту дыру, ради которой страж заводится (§15.4) |
| **11. путь установления личности на поверхности — ОДИН** (＋ редакция 2) | на каждой поверхности личность производит ровно один механизм; второго пути не заводится ни для запаса, ни для совместимости | два механизма об одном предмете расходятся молча, и расходятся там, где расхождение не видно. Ось 8 делает это выразимым построением, ось 11 — требуемым |

**Что из этого НЕ является решением о реализации.** Приёмка утверждает наблюдаемое: какой исход
видит арендатор, оператор и соседняя служба. Достигается ли чтение отзыва обращением по полосе
интроспекции или тем же авторитетом внутри процесса — предмет замысла реализации, а не этого
документа; сценарии §4 верны при обоих и в обоих случаях требуют одного: **fail-closed при
недоступности авторитета** и **окно, равное сроку кеша**.

---

## 3. Сценарий → производитель

**Правило раздела.** Каждое «Тогда» требует текста, кода возврата, строки или значения, которое
**что-то в дереве производит**. «Производитель» здесь — **механизм**, эмитирующий наблюдаемое **на
той поверхности, которую сценарий наблюдает**, а не конкретная строка. Где производителя нет, он
назван, сценарий помечен `ждёт производителя` и **покрытым не считается**: исполнить его нельзя,
и в зачёт покрытия он не идёт.

| Семейство | Что наблюдает | Производитель сегодня | Состояние |
|---|---|---|---|
| **VER** — верификация предъявленного | арендатор чужого облака | **нет на этой поверхности.** Механизм проверки нашего токена в дереве есть (§1.4), перечень проверок есть (§1.5), харнесс публичной цепочки есть — читателя на публичной цепочке нет | **ждёт производителя** — заводится этим переходом |
| **STRIP** — край не пересылает удостоверение за себя (＋ ред. 2) | соседняя служба, арендатор | **есть**: механизм снятия заголовков перед пересылкой на крае существует и работает (`gateway/internal/middleware/auth.go`, предикат `principalmeta.IsClientForgeableKey`); прецедент той же формы для **удостоверения** — `services/registry/internal/dataplane/proxy.go:73`, и его комментарий несёт ровно этот довод | производитель есть, **ось заводится этим переходом** |
| **LAND** — посадка объявляет ручку (＋ ред. 2) | оператор | **есть**: соседняя ручка того же блока объявлена ровно этой формой — значение в `values.yaml` и подстановка в шаблоне настроек | производитель есть, **ось заводится этим переходом** |
| **BIND** — страж связывает фронт и читателя (＋ ред. 2) | оператор | **есть по форме**: таблица требований посадки и проверка полноты провязки, в которой уже живёт образец «включено, а материала нет». **Антецедента «поднят фронт» у неё нет**: поле адреса собственного фронта не читает ни одна из трёх проверок стража | форма есть, **антецедент заводится этим переходом** |
| **SOLE** — путь установления личности на поверхности один (＋ ред. 2) | инженер | **наполовину**: страж размещения, читающий композиционный корень, заводится этой же приёмкой (KAN-WIRE-01); **переписи производителей личности по поверхностям в дереве нет** | **ждёт производителя** |
| **REG** — платформенный трафик не деградирует (＋ ред. 2) | арендатор платформы | **есть**: набор шарда, сегодня зелёный, и свод, отличающий «не выполнилось» от красного | производитель есть |
| **REV** — отзыв на предъявлении | арендатор + оператор | **нет на этой поверхности.** Авторитет отзыва построен и объявляет ровно эту норму (§1.4); потребителя на публичном пути нет, кеша нет | **ждёт производителя** |
| **DENY** — неразличимость отказов **для предъявителя** и различимость причины **для владельца** | арендатор (неразличимость) · оператор (причина) | **нет**: отказов верификатора не существует. Базовый отказ по правам сегодня побайтово одинаков у трёх производителей публичной цепочки — образец есть, предмет отсутствует. Причину владельцу производят измерители, они заводятся §7 п. 7 | **ждёт производителя** |
| **BOOT** — отказ старта | оператор | **есть**: страж настройки отвергает пуск списком, и в нём уже живёт образец формы «контроль включён, материал не задан»; таблица требований посадки `own` — тот же механизм; перечень обязательных величин **порождается** и сверяется гейтом | производитель есть, **ось заводится этим переходом** |
| **FWD** — прежний путь не отозван | соседняя служба, наш край | **есть и держится**: поведенческие замки публичной цепочки и страж размещения (§5) | производитель есть |
| **WIRE** — провязка читателя | инженер | **есть**: страж, читающий композиционный корень, и харнесс, собирающий цепочку **боевым** сборщиком | производитель есть, **новая проверка заводится этим переходом** |
| **POL** — согласие с единым перечнем | инженер | **POL-01: есть** — единый перечень и форма пробы согласия (§1.5). **POL-02: нет** — величину сегодня не читает ни одна проверка (§1.6) | смешанное, помечено по сценариям |
| **OPS** — что видит оператор чужого облака | оператор | **нет**: шаг проверки подъёма в руководстве установки предписывает проверять **через ваш край**, а в чужом облаке края нет | **ждёт производителя** |

**Сценариев всего 41** (было 31; KAN-DUP-01 **снят вместе со своим предметом** — §15.2, ban #11
исход 2; заведено одиннадцать). **Ждут производителя — 23** (`VER` 12 · `REV` 4 · `DENY` 3 ·
`POL-02` 1 · `OPS` 2 · `SOLE` 1). Двадцать два из них заводят производителя **этим же переходом**,
и производитель у них **не один**: у девятнадцати это читатель предъявленного на публичной
цепочке, у KAN-OPS-01 — шаг руководства установки (§7 п. 6), у KAN-OPS-02 и KAN-DENY-03 —
измерители (§7 п. 7), а KAN-POL-02 закрывается читателем и наблюдается переписью. Двадцать
третий — KAN-SOLE-01: его производителем служит **перепись производителей личности по
поверхностям**, которой в дереве нет и которая заводится §7 п. 10.
**Производитель есть у 18** (`BOOT` 3 · `FWD` 3 · `WIRE` 2 · `POL-01` 1 · `STRIP` 3 · `LAND` 2 ·
`BIND` 3 · `REG` 1).

> **Проверь этот счёт, а не поверь ему — и проверь СНАЧАЛА свой разборщик.** Единица — строка
> объявления сценария в **§4**. Предикат обязан быть ограничен областью:
>
> ```sh
> s4=$(grep -n '^## 4\. Сценарии' "$f" | cut -d: -f1); s5=$(grep -n '^## 5\. Состояние' "$f" | cut -d: -f1)
> sed -n "${s4},${s5}p" "$f" | grep -cE '^\*\*KAN-[A-Z]+-[0-9]+ '        # 41
> ```
>
> **Тот же предикат без ограничения области даёт 45, и это не находка, а его слепота:** записи
> ревью §13 и §14 начинают абзацы с идентификатора сценария (`**KAN-VER-12 заведён не для
> симметрии.**`), и разборщик засчитывает их как объявления. Расхождение поймано не глазом, а
> несовпадением с собственным ожиданием — тем же способом, каким такое ловится всегда.

**Механизм каждой проверки в дереве уже есть — недостаёт его на этой поверхности.** Единый
перечень (§1.5) закрыт и содержит одиннадцать проверок; два платформенных носителя исполняют все
одиннадцать, авторитет отзыва — девять из одиннадцати, объявив адресат и тип **отступлением с
причиной** (§1.4). Поэтому ни одна из проверок не изобретается этой приёмкой: заводится
**потребитель** на публичной цепочке. Предикат состава перечня:

```sh
git grep -n -A14 'func MandatoryChecks' -- pkg/tokenpolicy/policy.go   # 11 проверок
```

Отдельно: **состояние держателя** (кто эти сценарии закрепит пробой) — другая ось, и она в §5.
Сегодня держатся два сценария, наполовину — один; остальные 23 не начаты, и это ожидаемо: кода по
приёмке до APPROVED не пишется.

---

## 4. Сценарии

Единица наблюдения — **арендатор чужого облака**, у которого нет ни нашего края, ни нашего
стенда, ни модульного сертификата. Всё, что он не может проверить у себя, наблюдаемым поведением
не является.

### VER — верификация предъявленного удостоверения

**KAN-VER-01 — годный токен собственной чеканки называет вызывающего** · E · *ждёт производителя*

**Given** установка Kaname в чужом облаке, посадка `own`: внешнего поставщика удостоверений нет вовсе
**And** арендатор получил токен у самой службы её штатной полосой выдачи
**And** токен не истёк, момент начала его действия наступил, он не отозван
**When** арендатор зовёт публичный RPC, предъявив этот токен обычной формой предъявителя
**Then** вызов доходит до обработчика
**And** вызывающий назван — это тот субъект, которому токен выдан, а не подставная системная личность
**And** дальше действуют обычные правила доступа: **право звать** этот RPC вопросом аутентификации не решается
**And** ни один шаг проверки не обращался к внешнему поставщику удостоверений — в посадке `own` это **свойство построения**, а не наблюдение: поставщика в установке нет вовсе. У оператора оно наблюдается тем, что профиль его установки не объявляет ни одного внешнего источника ключей

> **Положительный контроль всего семейства.** Без него шесть отрицаний ниже зеленели бы на
> установке, которая отвергает вообще всё.

**KAN-VER-02 — истёкший токен отвергнут** · E · *ждёт производителя*

**Given** те же условия, что в KAN-VER-01
**And** тот же токен, отличающийся от годного **ровно одним фактом**: срок его действия истёк
**When** арендатор предъявляет его на том же RPC
**Then** вызов до обработчика не доходит
**And** отказ побайтово равен отказам семейства — тем же тринадцати, что перечисляет KAN-DENY-01

**KAN-VER-03 — токен, чья подпись не сходится, отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: его идентификатор ключа резолвится в ключ собственного реестра, но подпись этим ключом **не сходится**
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** решение принято **без** обращения к какому-либо внешнему авторитету: реестр ключей — собственный

> **Почему «подпись не сходится», а не «подписан не нашим ключом».** Прежняя формулировка меняла
> **два** факта разом — и разрешение идентификатора ключа, и сходимость подписи, — а какой из
> двух дал отказ, такой сценарий не различает. Разрешение идентификатора вынесено своим
> односфактным отрицанием: KAN-VER-10.

**KAN-VER-04 — токен с чужим адресатом отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен нашей чеканки, нашим издателем, годный по сроку и подписи, но выданный **для другого адресата** — той полосы, которая публичным слушателем не является
**When** арендатор предъявляет его на публичном RPC
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства

> Это тот сценарий, ради которого адресат назван осью 3 решения: у авторитета отзыва он объявлен
> **отступлением** и по построению не проверяется (§1.4). Токен, годный для одной поверхности,
> не обязан быть годен для другой.

**KAN-VER-05 — токен, чьё время ещё не наступило, отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: момент начала его действия в будущем
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** допуск на расхождение часов применён **тот же**, что применяют остальные проверяющие продукта — второго значения этой величины не заводится

**KAN-VER-06 — подпись вне перечня принимаемых отвергнута** · E · *ждёт производителя*

**Given** те же условия
**And** установка объявила перечень принимаемых подписей (§1.6)
**And** токен, отличающийся от годного ровно одним фактом: его подпись в объявленный перечень не входит
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** решение принято по **объявленному установкой** перечню, а не по тому, что заявлено в самом токене

**KAN-VER-07 — издатель, не являющийся собственным, отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: его издателем назван не тот, которым установка объявила себя
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** незаданный издатель установки означает **не** «любой наш», а отказ старта (KAN-BOOT-01), поэтому состояния «принимаем издателя, которого никто не выбирал» не существует

**KAN-VER-08 — токен, выпущенный не для этой полосы, отвергнут по типу** · E · *ждёт производителя*

**Given** те же условия
**And** токен нашей чеканки, нашим издателем, годный по сроку, подписи и адресату, но объявляющий **тип**, который для публичного слушателя не предназначен
**When** арендатор предъявляет его на публичном RPC
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** **отсутствие** типа и **несовпадение** типа дают один и тот же исход — отказ: «тип не назван» не означает «любой»

> **Второй из двух предметов оси 3 решения** (§2), и без него ось закрыта наполовину. У авторитета
> отзыва тип объявлен **отступлением с причиной**: тип говорит, для какой поверхности токен
> выпущен, и сверяет его та поверхность, а не издатель (§1.4). Публичный слушатель и есть эта
> поверхность — здесь отступления быть не может.

**KAN-VER-09 — алгоритм заголовка, не закреплённый за найденным ключом, отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: объявленный в его заголовке алгоритм входит в перечень принимаемых (§1.6), но **не равен** тому, который закреплён за ключом, найденным по идентификатору этого же токена
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** способ проверки подписи выбирает **ключ**, а не заголовок: значение из заголовка служит только сверке и никогда — выбором

> **Это та проверка, чьё отсутствие §1.6 предъявляет как «неисполнимую возможность»**, и §7 п. 1
> санкционирует её прямо. Односфактность здесь несущая: «алгоритм допустим» (KAN-VER-06) и
> «алгоритм закреплён за ключом» — **разные** отказы, и сценарий меняет ровно второй. Слить их в
> один значило бы вернуть ту же двухфактность, из-за которой правится KAN-VER-03.

**KAN-VER-10 — идентификатор ключа, не резолвящийся в собственном реестре, отвергнут** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: его идентификатор ключа называет ключ, которого в собственном реестре службы нет
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** форма идентификатора ограничена **до** того, как он попадёт в поиск по реестру, в повод обновления снимка ключей и в журнал
**And** отказ по **негодной форме** идентификатора побайтово равен отказу по **неизвестному** идентификатору: различимость «мусор» и «не наш» сама была бы оракулом

> Положительный близнец — KAN-VER-01, где тот же идентификатор резолвится. С KAN-VER-03 сценарий
> разведён намеренно: там идентификатор резолвится и не сходится подпись, здесь до подписи дело
> не доходит вовсе.

**KAN-VER-11 — параметр, помеченный обязательным к пониманию и не исполняемый, отвергает токен целиком** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: его заголовок помечает **обязательным к пониманию** параметр, которого читатель не исполняет
**When** арендатор предъявляет его
**Then** вызов отвергнут, и отказ побайтово равен отказам семейства
**And** отвергается **весь** токен, а не игнорируется помеченный параметр: принять его значило бы исполнить условие, которого читатель не понял
**And** перечень параметров, которые читатель умеет исполнять и потому вправе принять помеченными, **объявлен**; пустой перечень означает «любой помеченный отвергается» и является решением, а не заготовкой

**KAN-VER-12 — НЕ помеченный обязательным неизвестный параметр игнорируется** · E · *ждёт производителя*

**Given** те же условия
**And** токен, отличающийся от годного ровно одним фактом: его заголовок несёт неизвестный читателю параметр, **не** помеченный обязательным к пониманию
**When** арендатор предъявляет его
**Then** вызов доходит до обработчика, и вызывающий назван — исход тот же, что у KAN-VER-01

> **Положительный близнец KAN-VER-11, и он обязателен, а не для симметрии.** Требование
> двустороннее: помеченное обязательным — исполнить либо отвергнуть токен целиком; **не**
> помеченное неизвестное — игнорировать. Ужесточение первой половины без второй ломает
> совместимость, а без этого сценария читатель, отвергающий **любой** незнакомый параметр,
> проходил бы KAN-VER-11 как правильный.

### DUP — СНЯТО вместе с предметом (редакция 2)

Семейство несло один сценарий — **KAN-DUP-01**, «предъявленный токен и переданная личность разом
отвергаются», с утверждением «ни одна из двух личностей не выбрана — ни „предъявленная главнее",
ни „переданная главнее"». Оно **снято вместе со своим предметом**, а не ослаблено и не оставлено
без производителя: после оси 8 редакции 2 сочетание двух форм в одном запросе **непредставимо**,
и сценарий, чей вход построением невозможен, — не «Дано», а описание несуществующего мира.

Это второй из трёх исходов ban #11 («снять вместе с кодом, который маркер подпирал»), и он
выбран, потому что первый — «отвергать сочетание» — был **миной**: он отвергал бы весь
платформенный трафик. Разбор, замер и отзыв прежнего решения — §15.2. Требование, ради которого
семейство существовало, не пропало: неоднозначность «кто звонит» закрывает **ось 11** (путь
установления личности на поверхности один), и наблюдаема она семейством SOLE.

### STRIP — край не пересылает арендаторское удостоверение за себя

**KAN-STRIP-01 — за краем удостоверения нет** · I

**Given** установка платформы, где край стоит перед службой и назван в круге разрешённых отправителей
**And** запрос арендатора, несущий годное удостоверение, через край
**When** край установил личность вызывающего и пересылает запрос на публичный слушатель службы
**Then** в пересланном запросе **удостоверения нет** — ни в какой форме и ни в каком регистре имени
**And** переданная личность в нём **есть** и не изменилась
**And** снятие происходит **после** того, как край сам прочитал удостоверение: собственные проверки края не деградируют

> **Почему это не «ослабление транспорта».** Край не перестаёт **требовать** удостоверение — он
> перестаёт **пересылать** его дальше, установив личность сам. Требование к вызывающему не
> меняется ни на байт; меняется то, что уезжает за край.

**KAN-STRIP-02 — прямой вызов удостоверение доносит** · I

**Given** та же служба и то же годное удостоверение
**And** запрос, отличающийся от KAN-STRIP-01 **ровно одним фактом**: он идёт **прямо** на публичный слушатель, минуя край
**When** запрос приходит на публичный слушатель
**Then** удостоверение доезжает и читается — исход тот же, что у KAN-VER-01

> **Положительный близнец KAN-STRIP-01.** Без него «за краем удостоверения нет» зеленело бы на
> сборке, где удостоверение не доезжает **никуда** и читателя нет вовсе, — то есть на сегодняшнем
> состоянии.

**KAN-STRIP-03 — снятие не трогает поверхностей, которых край не проксирует** · P

**Given** перепись потребителей входящего удостоверения в не-тестовом дереве, с **названной единицей счёта**
**When** перепись разделяет их на «поверхность, до которой край проксирует запрос» и «собственная поверхность службы»
**Then** во второй группе изменений нет: каждая собственная поверхность продолжает читать удостоверение сама
**And** перепись печатает **обе** величины — сколько потребителей осмотрено и сколько отнесено к каждой группе
**And** пустой обход роняет прогон: «ноль находок» обязано быть отличимо от «ноль прочитанного»

> **Почему перепись, а не «мы посмотрели».** Радиус снятия берётся **по свойству** («кто читает
> входящее удостоверение»), а не по каталогу, в котором правку заметили. Разборщик обязан знать
> **обе** формы записи имени — строчную и заглавную: предикат, знающий одну, недобирает молча, и
> при первом же замере этой редакции недобрал (§15.3).


### REV — отзыв читается на предъявлении

**KAN-REV-01 — сквозь обе стороны: записали отзыв → предъявили → отказ** · E · *ждёт производителя*

**Given** арендатор, у которого есть годный токен, и этим токеном он **только что успешно** звал публичный RPC (KAN-VER-01)
**And** срок этого токена **не истёк** и до его истечения остаётся заметное время
**When** отзыв этого удостоверения записан штатным путём
**And** по истечении объявленного срока кеша арендатор предъявляет **тот же самый** токен на том же RPC
**Then** вызов отвергнут
**And** отказ побайтово равен отказам семейства VER

> **Сценарий обязан пройти СКВОЗЬ обе стороны одним прогоном.** Две пробы по половине — «отзыв
> записался» и «отвергнутое отвергается» — этого класса не ловят: каждая зелена о своей половине,
> а вопрос в том, **про один ли предмет** они говорят (`security.md` §«Разрыв, невидимый ни с
> одной стороны по отдельности»). Проба, не предъявляющая удостоверение после записи отзыва,
> сценария не закрывает.

**KAN-REV-02 — тот же токен без отзыва продолжает приниматься** · E · *ждёт производителя*

**Given** те же условия, что в KAN-REV-01, и тот же токен
**And** отличие **ровно одно**: отзыв не записан
**When** арендатор предъявляет токен после того же интервала ожидания
**Then** вызов доходит до обработчика, и вызывающий назван

> Положительный близнец KAN-REV-01. Без него утверждение «после отзыва отвергается» зеленело бы
> на читателе, отвергающем **всё** по истечении кеша.

**KAN-REV-03 — недоступный авторитет отзыва даёт отказ, а не проход** · E · *ждёт производителя*

**Given** арендатор с годным, неотозванным токеном
**And** авторитет, у которого спрашивают про отзыв, недоступен
**And** положительного вердикта по этому токену в кеше нет
**When** арендатор предъявляет токен
**Then** вызов отвергнут
**And** неполученный ответ **не** засчитан как «не отозван»
**And** отказ отличим **в журнале и в измерителях** — но не в том, что видит предъявитель

**KAN-REV-04 — окно отзыва задаёт срок кеша, а не срок удостоверения** · E · *ждёт производителя*

**Given** арендатор с годным токеном, чей срок жизни **заметно длиннее** объявленного срока кеша
**And** он только что успешно звал RPC — положительный вердикт закеширован
**When** отзыв записан
**And** арендатор предъявляет тот же токен **сразу**, не дожидаясь истечения кеша
**Then** вызов **доходит** до обработчика — положительный вердикт по этому токену закеширован, и объявленный срок кеша не истёк; это и есть объявленное окно, а не дефект
**And** после истечения объявленного срока кеша тот же токен отвергается (KAN-REV-01)
**And** **отрицательный** вердикт не кешируется: восстановленный доступ не ждёт истечения записи
**And** часы прогона **управляемы**: обе половины окна — до истечения кеша и после — наблюдаются детерминированно, а не выжидаются

> **Утверждение решительное намеренно, и это несущее.** Формулировка «вызов **ещё может** дойти»
> истинна при любом исходе — в том числе на читателе, у которого кеша нет вовсе и отзыв
> спрашивается на каждом предъявлении. Такой читатель проходил бы **все четыре** сценария REV,
> при том что короткий кеш объявлен осью 4 решения (§2) и стоит в объёме §7 п. 1. Утверждение,
> принимающее оба исхода, утверждением не является — это тот самый класс, который документ сам
> цитирует в KAN-DENY-02 и §6.
>
> Довод «окно недетерминированно» здесь не проходит: §5 назначает держателем пробу **с
> управляемыми часами**, а при управляемых часах исход внутри окна определён. Сценарий обязан
> **краснеть** на читателе без кеша — и краснеет: сразу после записи отзыва такой читатель
> отвергнет вызов, которого этот сценарий требует пропустить.

### DENY — отказы аутентификации неразличимы между собой

**KAN-DENY-01 — все отказы аутентификации побайтово одинаковы** · E · *ждёт производителя*

**Given** набор предъявлений, каждое негодное **своей** причиной: истёк · подпись не сходится · чужой адресат · время не наступило · подпись вне перечня · чужой издатель · чужой тип · алгоритм не закреплён за ключом · идентификатор ключа не резолвится · помеченный обязательным параметр не исполняется · отозван · авторитет отзыва недоступен
**When** каждое предъявляется на одном и том же RPC
**Then** ответы **побайтово равны между собой** — совпадают код возврата, текст **и** приложенные к ответу подробности
**And** равенство утверждается **сравнением байтов**, а не совпадением кодов: два отказа с одним кодом и разными подробностями различимы
**And** ни один ответ не называет, **какая** половина предъявленного неверна
**And** перечень причин **закрыт и сходится с семейством отрицаний**: **двенадцать** причин отвечают KAN-VER-02…11, KAN-REV-01 и KAN-REV-03 — отказ, не попавший в перечень, неразличимости ничем не обязан, и это дыра, а не умолчание
**And** тринадцатой причины — «обе формы разом» — в перечне **нет намеренно**: после оси 8 такой вход непредставим, и держать для него строку значило бы описывать несуществующий отказ (редакция 2, §15.2)

> **Почему подробности названы отдельно.** На публичной цепочке стоит звено, приписывающее
> машиночитаемую причину отказам **по правам** (§1.2, звено 3). Отказ аутентификации, оформленный
> тем же кодом, получил бы такое приложение автоматически, и два отказа, равные по тексту, стали
> бы различимы по приложенному. Утверждение о равенстве обязано покрывать **весь** ответ.

**KAN-DENY-02 — годное предъявление даёт ДРУГОЙ исход** · E · *ждёт производителя*

**Given** тот же RPC и годный токен KAN-VER-01
**When** он предъявляется
**Then** исход **отличается** от общего отказа KAN-DENY-01

> Положительный близнец. Без него «все отказы одинаковы» — тождественная истина на установке,
> которая отвечает одним и тем же на **всё**.

> **Что это утверждение НЕ требует.** Отказ аутентификации не обязан быть побайтово равен отказу
> **авторизации**: это ответы на разные вопросы, и ни один из них не сообщает, существует ли
> названный субъект. Предмет требования — неразличимость **внутри** аутентификации.

**KAN-DENY-03 — причина различима ВЛАДЕЛЬЦУ и неразличима предъявителю** · E · *ждёт производителя*

**Given** те же двенадцать негодных предъявлений KAN-DENY-01
**When** каждое предъявляется
**Then** у оператора службы **причина каждого отказа названа** — своим значением измерителя либо своей записью журнала
**And** причины **различимы между собой**: два отказа разных причин не сливаются в один счётчик
**And** в **ответе** предъявителю причины по-прежнему нет, и ответы остаются побайтово равны (KAN-DENY-01 не смягчается)
**And** ни измеритель, ни запись журнала не несут **самого удостоверения** и его содержимого

> **Две оси, которые легко перепутать, и потому они разведены здесь явно.**
> **Различимость ДЛЯ ВЛАДЕЛЬЦА обязательна**: без неё отказ, не случившийся ни разу, неотличим
> от отказа, случающегося постоянно, и мёртвая проверка невидима.
> **Неразличимость ДЛЯ ПРЕДЪЯВИТЕЛЯ обязательна**: различимый ответ сообщает постороннему, какая
> половина предъявленного неверна, а на некоторых причинах — и то, существует ли названный
> субъект. Это не компромисс между двумя требованиями: у них **разные адресаты**, и каждое
> действует целиком на своём.
>
> **Положительный близнец — KAN-DENY-02** (годное даёт другой исход) плюс собственный: годное
> предъявление **не** увеличивает ни один счётчик отказа. Без второго «причины различимы»
> зеленело бы на измерителе, растущем на любом запросе.

### BOOT — страж старта

**KAN-BOOT-01 — проверка включена, материал не задан → служба не поднимается**

**Given** профиль установки, объявивший посадку `own` и включивший проверку предъявленного
**And** величина, без которой проверка не имеет предмета, не задана
**When** оператор поднимает службу
**Then** служба **не стартует**
**And** отказ называет **ключ настройки** и причину, по которой без него не пускаемся
**And** отказ приходит **вместе с остальными** найденными проблемами настройки, а не по одной за перезапуск
**And** значения самой величины в отказе нет — служба называет имя ручки, но никогда её содержимое

> Формулировка отказа — часть контракта оператора, и образец этой формы в страже уже живёт
> (§3, семейство BOOT). Текст отказа при отказе старта под запрет §10 **не подпадает**: без него
> стенд не поднять.

**KAN-BOOT-02 — материал задан → служба поднимается**

**Given** тот же профиль, отличающийся **ровно одним фактом**: величина задана
**When** оператор поднимает службу
**Then** служба стартует
**And** её собственное заявление о посадке называет, что проверка предъявленного включена

> Положительный близнец KAN-BOOT-01: без него «отказывает в старте» зеленело бы на страже,
> отвергающем любой профиль.

**KAN-BOOT-03 — перечень обязательных величин порождается и содержит новые ручки**

**Given** руководство установки, чей перечень обязательных величин **порождён** из таблицы стража, а не выписан
**When** к стражу добавлены величины проверки предъявленного
**Then** порождённый перечень содержит их строки
**And** сверка порождённого с таблицей стража сходится, и расхождение роняет прогон

### LAND — посадка объявляет ручку читателя

**KAN-LAND-01 — ручка читателя объявлена в чарте** · P

**Given** объявления чарта службы: значения и шаблон настроек
**When** перепись ищет в них ручку приёма предъявленного удостоверения
**Then** ручка объявлена **обоими** способами, которыми объявлена соседняя ручка того же блока: значением по умолчанию и подстановкой в шаблон настроек
**And** перепись печатает **обе** величины — вхождений искомой ручки и вхождений соседней, — чтобы «ноль» читалось как находка, а не как «предикат не нашёл ничего вообще»
**And** значение по умолчанию **не включает** читателя молча: умолчание остаётся тем, каким было, а включение — выбор профиля

> **Почему сверка с соседней ручкой, а не с константой.** Форма объявления — свойство чарта, а не
> платформы; она менялась и будет меняться. Предикат, сверяющий с **живой** соседкой того же
> блока, переживает смену формы, а предикат, сверяющий с выписанным образцом, тихо устареет.

**KAN-LAND-02 — профиль установки без края включает читателя, и это видно в рендере** · I

**Given** профиль установки, предназначенный для чужого облака, где нашего края нет
**When** чарт рендерится этим профилем
**Then** в отрендеренной настройке службы читатель предъявленного **включён**
**And** величины, без которых он не имеет предмета, в ней заданы
**And** тот же рендер профилем платформы, где край есть, даёт **другое** значение — читатель включается решением профиля, а не сборкой

> **Положительный близнец у KAN-LAND-02 — второй рендер.** Без него «профиль включает» зеленело
> бы на чарте, включающем читателя **всегда**, — то есть на том, что этот документ включением по
> умолчанию как раз и запрещает (KAN-LAND-01).
>
> **Умолчание объявлено здесь, а не оставлено читателю на догадку.** Редакция 2 принимает:
> читатель включается **только** профилем чужого облака; профиль платформы его **не включает**, и
> именно на этом различии близнец и держится.
>
> **Предикат пересмотра назван вместе с умолчанием.** Если владелец ответит на вопрос 2 §11
> («включать ли читателя и на посадке платформы») утвердительно, оба профиля дадут одно значение,
> близнец станет **неисполнимым by construction**, а утверждение «включается решением профиля, а
> не сборкой» перестанет им доказываться. Тогда KAN-LAND-02 обязан быть **переписан** — под
> другую пару, которая различие ещё различает, — а не оставлен зеленеть на вырожденной. Это тот же
> исход, что применён к снятому семейству: сценарий живёт ровно столько, сколько живёт его
> предмет.

### BIND — страж связывает поднятый фронт и наличие читателя

**KAN-BIND-01 — публичный фронт поднят, читателя нет → служба не стартует** · I

**Given** профиль, объявивший адрес **собственного публичного** фронта службы
**And** читатель предъявленного в нём **не включён**
**When** оператор поднимает службу
**Then** служба **не стартует**
**And** отказ называет **обе** ручки — ту, что подняла фронт, и ту, что включает читателя, — и говорит, каким значением любой из них требование снимается
**And** отказ приходит **вместе с остальными** найденными проблемами настройки, а не по одной за перезапуск

> **Антецедент — поднятый ФРОНТ, а не объявленная посадка.** Это несущее различие, и цена ошибки
> в нём измерена: собственный публичный фронт поднимается **на любой** посадке, поэтому страж,
> связанный с посадкой, оставляет ровно ту дыру, ради которой заводится, — и выглядит при этом
> исполненным (§15.4).

**KAN-BIND-02 — тот же профиль с включённым читателем стартует** · I

**Given** тот же профиль, отличающийся **ровно одним фактом**: читатель включён и его величины заданы
**When** оператор поднимает службу
**Then** служба стартует
**And** её собственное заявление о посадке называет, что читатель предъявленного включён

> Положительный близнец KAN-BIND-01: без него «отказывает в старте» зеленело бы на страже,
> отвергающем любой профиль.

**KAN-BIND-03 — фронт не поднят → требования нет** · I

**Given** профиль, **не** объявивший адреса собственного публичного фронта
**And** читатель предъявленного в нём не включён
**When** оператор поднимает службу
**Then** служба стартует, и этого требования ей не предъявляется

> **Вторая половина связывания, и без неё оно неверно.** Страж, требующий читателя там, где
> предъявлять его некому, отвергал бы законную посадку — то есть требовал бы того, чем не
> пользуются. Предмет стража — **противоречие**, а не наличие ручки.

### FWD — прежний путь не отзывается

**KAN-FWD-01 — переданная личность от разрешённого отправителя по-прежнему принимается**

**Given** установка Kaname за нашим краем, круг разрешённых отправителей непуст
**And** проверенный пир, названный в круге, передаёт личность конечного пользователя
**And** предъявленного токена в запросе **нет** — и это не условие пробы, а **следствие оси 8**: край снял его перед пересылкой (KAN-STRIP-01)
**When** запрос приходит на публичный слушатель
**Then** вызывающим назван **конечный пользователь**, а не системная личность

> **Редакция 2 сменила этому сценарию роль, а не текст.** В редакции 1 он был положительным
> близнецом KAN-DUP-01 — «тот же запрос без предъявленного токена принят». Сценарий снят вместе
> с предметом (§4, семейство DUP), поэтому близнецом быть больше не для чего. Утверждение
> осталось прежним и продолжает держаться: **прежний путь не отзывается** (ось 7). Его
> положительная пара теперь — KAN-STRIP-01, которая утверждает, что удостоверения за краем нет;
> вместе они говорят, что за краем действует ровно одна форма личности, и она переданная.

**KAN-FWD-02 — переданная личность от непроверенного пира по-прежнему отбрасывается**

**Given** та же установка
**And** пир, чей сертификат не проверен либо в круге не назван, передаёт личность конечного пользователя
**When** запрос приходит на публичный слушатель
**Then** переданная личность **отброшена**, и вызывающий остаётся системной личностью
**And** введение читателя предъявленного этого не изменило

**KAN-FWD-03 — семантика пустого круга не изменилась**

**Given** установка, чей круг разрешённых отправителей пуст
**When** служба поднимается и принимает запрос с переданной личностью от проверенного пира
**Then** поведение **то же, что до введения читателя предъявленного**: пустой круг означает «не сужаем», а не «запрещаем»
**And** отдельный страж старта, требующий явно объявить круг либо явно от сужения отказаться, продолжает действовать без изменений

> **Что этот сценарий охраняет.** Соблазн «раз появился предъявитель, переданную личность можно
> и запретить» ломает платформу ради установки, которой края не нужно. Ось 7 решения (§2) это
> запрещает, и здесь она превращается в замок.

### WIRE — провязка читателя

**KAN-WIRE-01 — читатель стоит в публичной цепочке, и это утверждает дерево**

**Given** композиционный корень, собирающий цепочку публичного слушателя
**When** прогоняется страж размещения, читающий этот корень
**Then** страж требует присутствия читателя предъявленного в публичной цепочке
**And** снятие читателя из цепочки роняет прогон **с именем виновника**
**And** страж печатает объём осмотренного и падает на пустом обходе

**KAN-WIRE-02 — харнесс цепочки видит именно ту цепочку, что собирает боевой корень**

**Given** харнесс, которым проверяются поведенческие сценарии VER, REV и DENY
**When** читатель предъявленного добавлен в публичную цепочку
**Then** харнесс собирает цепочку **боевым** сборщиком и потому видит читателя
**And** публичная и внутренняя цепочки в харнессе перестают быть тождественными, если читатель стоит только на публичной

> **Ловушка, ради которой сценарий заведён.** Сегодня харнессы обеих цепочек собираются одним и
> тем же вызовом и потому **тождественны по телу**. Читатель, добавленный в публичную цепочку
> мимо общего сборщика, окажется вне харнесса — и все пробы VER, REV и DENY станут **вакуумными**,
> оставаясь зелёными. Это `testing.md` §«Гейт на класс», п. 6: тест, чей заголовок шире тела.
>
> Прежняя редакция называла здесь семейство DUP, снятое вместе с предметом (§4). Соседний раздел
> об этом говорил, но **действующий сценарий продолжал утверждать обратное** — утверждение,
> пережившее свой предмет, в живом «Дано». Найдено переписью круга 3 (§16.1).

### POL — согласие с единым перечнем проверок

**KAN-POL-01 — состав проверок читателя сходится с единым перечнем**

**Given** единый перечень проверок токена (§1.5) и два уже существующих носителя Kaname, каждый со своей пробой согласия
**When** заводится третий носитель — читатель предъявленного
**Then** он объявляет свой состав проверок, и проба сверяет его с перечнем **в обе стороны**
**And** проверка, которую он не исполняет, обязана быть объявлена **отступлением с причиной** — иначе отступление неотличимо от пропуска
**And** перечень объявленных отступлений у этого читателя **пуст, и пуст осознанно**: он исполняет все одиннадцать проверок перечня — включая адресат и тип токена, объявленные отступлением у авторитета отзыва (§1.4), и включая связь заголовка с ключом, идентификатор ключа и критические заголовки
**And** каждой из одиннадцати проверок отвечает **своё односфактное отрицание** в §4, а каждому отрицанию — строка в закрытом перечне причин KAN-DENY-01: покрытие перечня и покрытие неразличимости расходиться не вправе

**KAN-POL-02 — объявленная оператору величина обрела читателя** · *ждёт производителя*

**Given** посадка `own`, объявившая перечень принимаемых подписей (§1.6)
**When** арендатор предъявляет токен, чья подпись в перечень не входит
**Then** он отвергнут **по этому перечню** (KAN-VER-06)
**And** величина, до сих пор не читавшаяся ни одной проверкой, имеет читателя в не-тестовом дереве

> Это закрытие `api-conventions.md` §«Неисполнимая возможность» на конкретном поле: исход
> «реализовать» из трёх законных выбран, потому что читатель заводится этим же переходом.

### SOLE — путь установления личности на поверхности один

**KAN-SOLE-01 — производителей личности на поверхности ровно один** · P · *ждёт производителя*

**Given** перепись механизмов, производящих личность вызывающего, по **поверхностям** службы: публичный слушатель · внутренний слушатель · собственные фронты · собственные HTTP-поверхности
**When** перепись сопоставляет каждой поверхности её механизмы
**Then** на каждой поверхности механизм **один**
**And** перепись печатает объём осмотренного — поверхностей и механизмов, — и **падает на пустом обходе**
**And** взаимоисключение полос держится **построением** (ось 8), а не проверкой во время запроса: на публичном слушателе за краем удостоверения нет, а напрямую нет переданной личности

> **Чего этот сценарий НЕ утверждает.** Он не запрещает службе иметь **разные** механизмы на
> **разных** поверхностях: собственная HTTP-поверхность хуков и публичный слушатель отвечают на
> разные вопросы разным вызывающим. Запрещено **два механизма на одной** — они расходятся молча,
> и расходятся там, где расхождение не видно.

### REG — платформенный трафик не деградирует

**KAN-REG-01 — то, что было зелёным, остаётся зелёным** · E

**Given** набор проб шарда службы, дающий сегодня **ноль** упавших утверждений на своих коллекциях
**When** прогон повторяется после снятия удостоверения на крае (ось 8)
**Then** упавших утверждений — **ноль**, и упавших скриптов — **ноль**
**And** вердикт прогона читается **по числам**: коллекций исполнено из скольких, запросов, утверждений, упавших утверждений, **неотвеченных запросов**
**And** «не выполнилось» **не вычитается** из вердикта и не зачитывается в успех: набор без отчётов — провален, а не зелен

> **Почему это отдельный сценарий, а не «ну проверим».** Часть 1 работы трогает **край**, то есть
> общий путь всего платформенного трафика. Утверждение «ничего не сломалось» без числа есть
> впечатление; с числом — проверяемое требование. Величину берёт прогон, а не этот документ:
> сегодняшнее её значение названо в §15.5 вместе с тем, чем оно получено и чем **не** является.

### OPS — что видит оператор и арендатор чужого облака

**KAN-OPS-01 — проверка подъёма исполнима без нашего края** · *ждёт производителя*

**Given** оператор чужого облака, поднявший Kaname по руководству установки
**And** ни нашего края, ни нашего стенда у него нет
**When** он выполняет шаг руководства «служба отвечает по своему публичному слушателю»
**Then** шаг исполним **средствами, которые у него есть**: он получает токен у самой службы и зовёт им публичный вызов
**And** руководство не предписывает ему проверять через край, которого в его установке не существует

> **Что здесь наблюдается.** Сегодня этот шаг руководства адресован установке, у которой край
> есть. Для посадки `own` он **не исполним** — и это первое, обо что арендатор чужого облака
> спотыкается. Предикат готовности выноса — «отдельный клон поднимается в боевой посадке» —
> этим шагом и проверяется.

**KAN-OPS-02 — отказ не рассказывает предъявителю, чем токен негоден** · *ждёт производителя*

**Given** любое из негодных предъявлений KAN-DENY-01
**When** оно отвергнуто
**Then** **наружу** уходит суждение, и только оно
**And** подробность — какая именно проверка не сошлась — доступна оператору **в журнале и измерителях** его установки
**And** различение исходов в измерителях считает **и выданные, и отвергнутые**: ноль в одном ряду иначе отвечает сразу на два вопроса — «отказов не было» и «сюда никто не приходил»

---

## 5. Состояние сценариев

Таблица заведена **сейчас**, а не после реализации: сценарий без строки уходит из счёта молча.
Состояние здесь — про **держателя** (что закрепит сценарий пробой), и это **другая ось**, чем
производитель в §3. Два сценария держатся уже сегодня, потому что утверждают поведение, которое
эта работа обязана **не сломать**.

**Редакция 2:** строка KAN-DUP-01 снята вместе со своим сценарием (§4, семейство DUP) — держатель
у снятого предмета не заводится; заведено одиннадцать строк. Строк в таблице **41**, сценариев в
§4 **41**; предикат обеих величин — в §3, и он ограничен областью.

> [!important] ФАЗА Ф0 УЖЕ ПОСАЖЕНА В ЛИНИИ — колонка «Состояние» ниже про это НЕ говорит
> Числа §1 и колонка состояния сняты на своей ревизии и верны на неё; но читаются они как
> утверждение о **сегодня**, и исполнитель, идущий по фазам буквально, начнёт переписывать
> существующее. Поэтому сказано прямо и с предикатом.
>
> **Что уже есть** (линия `release/kaname` @ `fe0e5a60f3`, замер 2026-09-07; единица — файл
> механизма в дереве): файлов **11**, из них не-тестовых **5** — читатель, его настройка, носитель
> происхождения вызывающего, измеритель исходов и правило отзыва; плюс провязка в композиционном
> корне (**5** вхождений), харнесс и пробы цепочки и провязки.
>
> ```sh
> git ls-tree -r origin/release/kaname --name-only | grep -icE 'presentedcred|presented_credential|callerorigin|tokenrevocation'   # 11
> git grep -c 'presentedcred\.' origin/release/kaname -- services/iam/cmd/kaname/serve.go   # 5
> ```
>
> **Остаток Ф0 — три пункта объёма, и каждый измерен, а не предположен:**
>
> | пункт §7 | чего недостаёт | ось | предикат |
> |---|---|---|---|
> | 4 — профиль посадки `own` | **производителя**: не выбран ни одним профилем | производитель | `git grep -c 'identityProvider: own' -- deploy` → **0** |
> | 5 — замок семантики пустого круга | **держателя**: проба KAN-FWD-03 закрепляет происхождение круга из настройки, а не семантику пустого | держатель | состояние строки KAN-FWD-03 — «наполовину», и это не оговорка |
> | 7 — различимость причины владельцу | **держателя, а НЕ производителя** | держатель | `git grep -c 'slog.String("reason"' origin/release/kaname -- services/iam/internal/presentedcred/reader.go` → **1**; разбор ниже |
>
> **Пункт 7 разобран отдельно, потому что я приписал отсутствие не той оси.** Обе половины
> предиката верны — измеритель различает **исход** (принято · отвергнуто · ответить не смогли,
> закрытый набор из **3** значений) и меток причины не несёт, — но **вывод из них не следует**:
> KAN-DENY-03 требует, чтобы причина была названа своим значением измерителя **либо** своей
> записью журнала. **Журнал в дереве есть и причину несёт** — у отказа читателя параметр так и
> подписан, и он уходит в запись отдельным полем. Больше того, автор измерителя выбрал ось исхода
> **сознательно** и объяснил выбор годдоком: ряды отвечают на разные вопросы, чинящиеся в разных
> местах, и подробность отказа туда не кладётся намеренно.
>
> Значит производитель **существует**, и работа — **проба**, а не метки. Это тот же зазор трёх
> осей, что закрыт §17.1 для KAN-FWD-03, **повторённый мной в этой же врезке**: я направил
> исполнителя на производителя, который есть и которого автор кода решил не заводить.
>
> **Что эта врезка НЕ утверждает — и это её граница, а не оговорка.** Первый предикат врезки
> меряет **файлы механизма** и о **держателях** сценариев не говорит ничего. Поэтому держатели
> меряются **вторым** предикатом, и редакцией 4 он стоит **здесь**, а не в «работе перед
> исполнением»: он стоит одной команды, а отложенный замер к исполнителю не приезжает.
>
> **ПЕРЕМЕР КОЛОНКИ «Состояние» (редакция 4).** Единица — **идентификатор сценария §5, названный
> в любом отслеживаемом файле дерева**. Форм записи в этом дереве **две**, и предикат обязан
> знать обе: дефисная (`KAN-VER-01` — так пишут документы) и с подчёркиванием (`KAN_VER_02` — так
> пишут имена проб Go, где латиница обязательна, ban #17). Перечень форм **выведен из дерева**,
> а не из памяти:
>
> ```sh
> # смешанных форм нет — 0; значит разделителей ровно два, и оба ниже
> git grep -hoE 'KAN-[A-Z]+_[0-9]+|KAN_[A-Z]+-[0-9]+' origin/release/kaname | wc -l
> # обе формы, приведённые к одной; пересечь с идентификаторами колонки §5
> git grep -hoE 'KAN[-_][A-Z]+[-_][0-9]+' origin/release/kaname | tr '_' '-' | sort -u
> ```
>
> | что считаем | величина |
> |---|---:|
> | строк в колонке §5 | **41** |
> | из них имеют держателя, называющего сценарий по номеру (**любая** форма) | **25** |
> | — видит **дефисная** форма | 9 |
> | — видит форма с **подчёркиванием** | 23 |
> | **строк «не начат», у которых держатель уже есть** | **24** |
>
> Замер 2026-09-07, вершина линии `release/kaname` @ `e9c498f928`; на `fe0e5a60f3` — **те же
> числа**, то есть вершина их не сдвинула. Считать под `LC_ALL=C`: `comm` на несогласованной
> сортировке молча врёт.
>
> **Здесь стояло «минимум восемь», и это была не другая единица, а слепой разборщик.** Прежний
> предикат знал **только дефисную форму** и потому не видел **16** строк из 25 — тех, чьи пробы
> названы по-Go. Дефисная половина воспроизводится в точности: те же **9** сценариев тем же
> списком (`VER-01`, `VER-09`, `VER-10`, `REV-04`, `BOOT-01`, `BOOT-02`, `FWD-01`, `WIRE-01`,
> `WIRE-02`), из них **8** «не начат». То есть расхождение с кругом 5 было **не о ширине
> единицы**: его число (24) моей единицей теперь **воспроизводится** — расходились мы тем, что
> мой разборщик не знал одной из двух законных форм записи. Второе его число (11 строк) моей
> единицей по-прежнему не воспроизводится и остаётся неустановленным (§19.8).
>
> **24 — это ТОЖЕ нижняя граница, и по другой причине.** Она считает строки, чей держатель
> **называет сценарий по номеру**; проба, закрепляющая сценарий без упоминания номера, ей не
> видна, и сколько таких — **не измерено**. Отдельно и важнее: «держатель существует» ≠ «сценарий
> закреплён» — предикат доказывает, что **имя названо**, а не что утверждение проверяет именно
> этот сценарий. Это те же три оси (производитель · держатель · проба), что §17.1 и §19.2,
> поэтому **ячейки колонки редакцией 4 не переписаны**: перевод строки из «не начат» требует
> прочтения её пробы, а это уже не одна команда.
>
> **Следствие для исполнителя, названное ровно настолько, насколько мерил предикат:** по объёму
> §7 недостают три пункта выше; колонка «Состояние» устарела **минимум на 24 строки из 41**, и
> первый шаг внутри Ф0 — не писать пробы по колонке, а сверить её построчно с **пятью**
> файлами-носителями (`git grep -lE 'KAN[-_][A-Z]+[-_][0-9]+' origin/release/kaname` даёт 82 файла по
> всему дереву; идентификаторы **этого** документа несут пять, все под `services/iam/`). Порядок
> §8 внутри Ф0 читать как порядок **добора**, а не постройки. Ф1 и Ф2 не посажены вовсе.

| Сценарий | Уровень | Состояние | Чем держится |
|---|---|---|---|
| KAN-VER-01 | E | не начат | поведенческая проба публичной цепочки: годный токен даёт названного вызывающего |
| KAN-VER-02 | E | не начат | та же проба, вход отличается ровно сроком |
| KAN-VER-03 | E | не начат | та же проба, вход отличается ровно ключом подписи |
| KAN-VER-04 | E | не начат | та же проба, вход отличается ровно адресатом |
| KAN-VER-05 | E | не начат | та же проба, вход отличается ровно моментом начала действия |
| KAN-VER-06 | E | не начат | та же проба, вход отличается ровно подписью; перечень принимаемых задан установкой |
| KAN-VER-07 | E | не начат | та же проба, вход отличается ровно издателем |
| KAN-VER-08 | E | не начат | та же проба, вход отличается ровно типом токена; отсутствие типа — отдельный вход того же прогона |
| KAN-VER-09 | E | не начат | та же проба, вход отличается ровно алгоритмом заголовка при том же ключе |
| KAN-VER-10 | E | не начат | та же проба, вход отличается ровно идентификатором ключа; негодная форма и неизвестный идентификатор сверяются на побайтовое равенство |
| KAN-VER-11 | E | не начат | та же проба, вход отличается ровно помеченным обязательным параметром |
| KAN-VER-12 | E | не начат | та же проба, вход отличается ровно НЕ помеченным неизвестным параметром; исход положительный |
| KAN-REV-01 | E | не начат | сквозная проба одним прогоном: запись отзыва, затем предъявление того же токена |
| KAN-REV-02 | E | не начат | та же проба, отличается ровно отсутствием записи отзыва |
| KAN-REV-03 | E | не начат | проба с недоступным авторитетом: отказ, а не проход |
| KAN-REV-04 | E | не начат | проба с управляемыми часами: до истечения кеша и после |
| KAN-DENY-01 | E | не начат | проба, сравнивающая ответы **побайтово**, включая приложенные подробности |
| KAN-DENY-02 | E | не начат | тот же прогон: годное предъявление даёт другой исход |
| KAN-DENY-03 | E | не начат | тот же прогон + измерители: причина названа владельцу, ответ предъявителю не изменился; годное не растит ни один счётчик отказа |
| KAN-BOOT-01 | I | не начат | проба стража настройки: включённая проверка без материала роняет старт и называет ключ |
| KAN-BOOT-02 | I | не начат | та же проба, профиль отличается ровно заданной величиной |
| KAN-BOOT-03 | P | не начат | гейт сверки порождённого перечня обязательных величин с таблицей стража |
| KAN-LAND-01 | P | не начат | перепись объявлений чарта: искомая ручка и соседняя того же блока, обе величины печатаются |
| KAN-LAND-02 | I | не начат | проба рендера: два профиля, отличающиеся ровно предназначением, дают разные значения ручки |
| KAN-BIND-01 | I | не начат | проба стража: фронт объявлен, читатель выключен → отказ пуска, названы обе ручки |
| KAN-BIND-02 | I | не начат | та же проба, профиль отличается ровно включённым читателем |
| KAN-BIND-03 | I | не начат | та же проба, профиль отличается ровно необъявленным фронтом; исход положительный |
| KAN-STRIP-01 | I | не начат | проба края: пересланный запрос осматривается на присутствие удостоверения и переданной личности |
| KAN-STRIP-02 | I | не начат | та же проба, вход отличается ровно тем, что идёт мимо края |
| KAN-STRIP-03 | P | не начат | перепись потребителей входящего удостоверения с разделением по поверхностям; знает обе формы записи имени |
| KAN-SOLE-01 | P | не начат | перепись производителей личности по поверхностям; падает на пустом обходе |
| KAN-REG-01 | E | не начат | прогон набора шарда до и после части 1; вердикт читается по числам, «не выполнилось» считается отдельно |
| KAN-FWD-01 | I | держится | `services/iam/cmd/kacho-iam/serve_internal_principal_trust_test.go` · `TestPublicChain_HonorsVerifiedConsumerForwarder` |
| KAN-FWD-02 | I | держится | `services/iam/cmd/kacho-iam/serve_internal_principal_trust_test.go` · `TestPublicChain_DropsForgedPrincipal_HonorsVerified` |
| KAN-FWD-03 | P | наполовину | `services/iam/cmd/kacho-iam/trusted_forwarders_wiring_test.go` · `TestServe_ForwarderAllowListComesFromConfig` — держится происхождение круга из конфигурации; **семантика пустого круга своего замка на публичной цепочке не имеет**, он заводится этим переходом |
| KAN-WIRE-01 | P | не начат | страж размещения, читающий композиционный корень; доказан инъекцией в обе стороны |
| KAN-WIRE-02 | P | не начат | проба тождества: харнесс собирается боевым сборщиком, снятие читателя из него роняет прогон |
| KAN-POL-01 | I | не начат | проба согласия с единым перечнем по образцу двух существующих носителей |
| KAN-POL-02 | P | не начат | перепись: величина имеет читателя в не-тестовом дереве |
| KAN-OPS-01 | E | не начат | сквозной прогон по руководству установки в посадке `own`, без края |
| KAN-OPS-02 | E | не начат | проба измерителей: выданные и отвергнутые считаются раздельно |

Уровни: **E** — наблюдаемо арендатором либо оператором; **I** — проба компонента или стража;
**P** — свойство дерева, закрепляемое гейтом.

> [!note] Координаты трёх держащихся строк названы ПО СТВОЛУ продукта, а не по линии
> Каталог композиционного корня переименован **линией** `release/kaname`, и в её дереве
> те же файлы лежат под `services/iam/cmd/kaname/` (§1.2). В стволе продукта этого
> каталога нет: там `services/iam/cmd/kacho-iam/`, и **имена всех трёх проверок в нём
> те же самые** — перемер: `git ls-tree -r origin/main --name-only | grep
> serve_internal_principal_trust_test` и `git grep -c 'func TestServe_ForwarderAllowListComesFromConfig(' origin/main`.
>
> Свидетельство обязано резолвиться у **читателя**, а читатель этого документа берёт
> дерево продукта со ствола: гейт `scripts/docs-gate/check-03-holding-claim-resolves.py`
> сверяет координату с `git ls-files` того клона, который стоит в `project/kacho`, и
> координата линии давала ему шесть претензий без проверяемого адреса. Координата линии
> здесь **не** возвращается вместе с переименованием: она вернётся тогда, когда
> переименование окажется в стволе, и гейт скажет об этом сам — прежний путь исчезнет
> из индекса.

---

## 6. Классы риска, которые предмет задевает

Каждый назван вместе со сценарием, который его закрывает, и с признаком, по которому нарушение
видно **в момент написания кода**, а не после инцидента.

| Класс | Признак в момент написания | Закрывает |
|---|---|---|
| контроль на выдаче без энфорсмента на предъявлении | у записи отзыва нет читателя на пути запроса | KAN-REV-01 |
| разрыв, невидимый ни с одной стороны по отдельности | две пробы по половине: «отзыв записался» и «отвергнутое отвергается» | KAN-REV-01 |
| мягкий проход при отказе зависимости | недоступный авторитет читается как «не отозван» | KAN-REV-03 |
| оракул различимого отказа | два отказа аутентификации, различимые кодом, текстом **или** подробностями | KAN-DENY-01 |
| отрицание без положительного близнеца | утверждение «все отказы одинаковы» без годного предъявления рядом | KAN-DENY-02, KAN-REV-02, KAN-BOOT-02 |
| принято-и-проигнорировано / неисполнимая возможность | величина объявлена оператору и не читается ни одной проверкой | KAN-POL-02 |
| вакуумная проба: заголовок шире тела | харнесс собирает цепочку **не** боевым сборщиком | KAN-WIRE-02 |
| два места об одном предмете | второй перечень проверок токена рядом с единым | KAN-POL-01 |
| величина, подставляемая построением молча | у стража нет предмета: незаданного состояния не бывает | KAN-BOOT-01 |
| пустой список как «не сужаем» | попытка переопределить семантику пустого круга заодно | KAN-FWD-03 |
| внешняя зависимость у решения о доступе | проверка подписи спрашивает кого-то за пределами установки | KAN-VER-01, KAN-VER-03 |
| отзыв прежнего пути | переданная личность перестала приниматься | KAN-FWD-01, KAN-FWD-02 |

---

## 7. Объём работы, который APPROVED санкционирует

1. **Читатель предъявленного** на публичной цепочке: разбор предъявителя, проверка подписи
   собственным реестром ключей, срока, момента начала действия, издателя, адресата, типа,
   допустимости подписи, связи заголовка с ключом, формы идентификатора ключа и
   понятости критических заголовков; чтение отзыва **на пути запроса** с коротким
   кешем положительного вердикта; fail-closed при недоступности авторитета.
2. **Отказ единой формы** — побайтово одинаковый для всех причин, включая приложенные к ответу
   подробности.
3. **Ось стража старта**: проверка включена, материал не задан → отказ в пуске списком, с именем
   ключа; новые строки в порождаемом перечне обязательных величин.
4. **Профиль посадки `own`**, объявляющий это поле, — сегодня посадка `own` в дереве
   развёртывания не выбрана ни разу (§1.7).
5. **Провязка и её стражи**: присутствие читателя в публичной цепочке; харнесс, собирающий цепочку
   боевым сборщиком; проба согласия с единым перечнем проверок; **замок семантики пустого круга
   разрешённых отправителей на публичной цепочке** — держателя у неё сегодня нет, и он заводится
   этим переходом (KAN-FWD-03).

> [!note] Последняя оговорка пункта 5 дописана кругом 4 — и НЕ перенумеровывает список
> KAN-FWD-03 требовал работы, которой объём не санкционировал: его состояние «наполовину», а хвост
> строки прямо говорит «заводится этим переходом». Санкция дописана **внутрь** существующего
> пункта, а не заведена двенадцатым: перенумерация обесценила бы таблицу соответствия §16.2 и
> сделала бы тихо ложными ссылки записей ревью, которые правке не подлежат. Пункт 5 — «провязка
> и её стражи», и замок поведения публичной цепочки принадлежит ему по существу, а не по удобству.
>
> **Почему зазор не поймал ни один критерий, и это важнее самой правки.** Осей **три**, а не две:
> производитель · **держатель** · санкция. Критерий 1 связывает сценарий с производителем,
> критерий 8 — пункт объёма со сценарием; связи «сценарий, чей **держатель** заводится этим
> переходом → пункт объёма» не устанавливал ни один, и KAN-FWD-03 проваливался между ними
> **by construction**. Критерий 8 дополнен вторым предложением; строка предшествует редакции 2 и
> была одобрена прошлым кругом — это зазор, а не регрессия правки.
6. **Шаг руководства установки**, исполнимый без нашего края.
7. **Измерители**: выданные и отвергнутые считаются раздельно, и **причина отказа различима
   владельцу** при неизменной неразличимости для предъявителя (KAN-DENY-03).

> [!note] Третьим пунктом здесь стоял «отказ обеих форм разом» — СНЯТ вместе с предметом (круг 3)
> Пункт дословно санкционировал реализацию отвергнутой оси 8: «предъявленный токен вместе с
> переданной личностью». По разбиению фаз он попадал в **Ф0**, то есть исполнитель, получив
> одобрение, был бы обязан построить ровно то, что §15.2 измеряет как отказ каждому запросу через
> край. Сверх того у пункта не осталось сценария: соответствующий снят вместе с предметом.
>
> Снят тем же исходом ban #11, что и сценарий, — **вместе с предметом**, а не ослаблен. Пункты
> перенумерованы; соответствие прежней и нынешней нумерации названо в §16.2, потому что записи
> ревью §12–14 ссылаются на прежнюю и правке не подлежат.
>
> **Класс, ради которого врезка оставлена, а не строка просто удалена:** редакция 2 прошла по
> документу по слову «DUP» и закрыла одиннадцать мест из тринадцати. Уцелело ровно то, что этого
> слова **не содержит**: пункт объёма говорил «обеих форм разом». Сущность живёт в документе
> графой таблицы, строкой перечня, пунктом объёма и прозой врезки — и в каждом месте называется
> по-своему. Перемеряй по **разделам**, а не по слову (§16.1).

**Заведено редакцией 2 (задача #2191):**

8. **Снятие арендаторского удостоверения на крае** перед пересылкой за себя — после того, как
   край установил личность сам. Требование к вызывающему не меняется; меняется то, что уезжает за
   край. Плюс **перепись потребителей входящего удостоверения** с разделением по поверхностям и с
   разборщиком, знающим **обе** формы записи имени (KAN-STRIP-01…03). Сверх того — два требования,
   выведенные разбором классов и без которых пункт исполним неверно:

   **8а. Снятие стоит в ОБЩЕМ УЗЛЕ, а по каждой конструкции вне его принято явное решение.** Край
   строит исходящие метаданные не в одном месте: общий узел есть, и его зовут **4** раза, но
   **3** конструкции живут помимо него. Снятие, положенное только в общий узел, три конструкции
   не покрывает; положенное в каждую — расходится молча при следующей правке. Поэтому: снятие — в
   узле, а по каждой из трёх конструкций **записано решение** (покрыта узлом · снимает сама ·
   удостоверения не несёт by construction). **Перечень называется в коде рядом со снятием**, а не
   в этом документе: документ читают раз, код правят постоянно, и следующий, кто заведёт четвёртую
   конструкцию, обязан увидеть перечень там, где пишет.

   Числа сняты на `release/kaname` @ `fe0e5a60f3`, 2026-09-07; единица — **вхождение в прод-коде
   края**. Предикаты печатают величину, а не счёт по файлам: `grep -c` дал бы три строки там, где
   обещано одно число.

   > **Второй предикат я сперва написал неверно, и записываю это здесь.** Форма
   > `git grep -h … | grep -vc principalmeta` даёт **4** вместо 3: `-h` снимает имена файлов, и
   > отрицание начинает фильтровать **содержимое строки**, а строка внутри общего узла слова
   > `principalmeta` не содержит — сам узел в счёт и попадает. Фильтр по **пути** требует имён,
   > поэтому `-h` здесь несовместим с замыслом. Поймано прогоном собственного предиката сразу
   > после того, как он был вписан, — а не чтением.

   ```sh
   # вызовов общего узла: объявление вычтено — объявление вызовом не является
   git grep -h 'OutgoingFromIncoming(' -- 'gateway/**/*.go' ':!*_test.go' | grep -vc '^func '   # 4
   # конструкций помимо узла: фильтр по ПУТИ, поэтому имена файлов НЕ снимаются
   git grep -n 'metadata.NewOutgoingContext' -- 'gateway/**/*.go' ':!*_test.go' \
     | grep -vc 'principalmeta/forward.go'   # 3
   ```

   **8б. Живая ветка отказа «обе формы разом» СНИМАЕТСЯ, и снимается ВМЕСТЕ со снятием на крае.**
   Ось 8 её отозвала, сценарий снят вместе с предметом (§4), пункт объёма снят кругом 3 — а **код
   в дереве жив**: читатель отвергает запрос, несущий обе формы. Это тот же зазор трёх осей,
   который круг 4 закрыл для KAN-FWD-03, только по оси «предмет, подлежащий **снятию**»: снять
   сценарий и не дать санкции на снятие кода значит оставить в дереве реализацию отозванного
   решения.

   **Держателей у ветки ДВА, и они снимаются тем же изменением.** Пробы, закрепляющие отозванное
   решение, — утверждения о том, чего контракт больше не требует: оставить их значит получить
   зелёный вердикт о снятом предмете. Обе названы по снятому сценарию и живут на **разных**
   уровнях — цепочка и читатель, — поэтому снятие одной вторую не закрывает.

   ```sh
   git grep -c 'func TestKAN_DUP_01' -- 'services/iam/**/*_test.go'   # 2 файла, по одной пробе
   ```
9. **Объявление ручки читателя в чарте** — обоими способами, которыми объявлена соседняя ручка
   того же блока, **без включения по умолчанию**; и **профиль установки для чужого облака**, её
   включающий (KAN-LAND-01, KAN-LAND-02).
10. **Связывание стража старта**: поднят собственный **публичный фронт** ⇒ читатель предъявленного
    включён; фронт не поднят — требования нет. Плюс **перепись производителей личности по
    поверхностям** (KAN-BIND-01…03, KAN-SOLE-01).

    **10а. ПРЕЖДЕ связывания у поля адреса фронта заводится ЧИТАТЕЛЬ в страже.** Сегодня это поле
    не читает **ни одна** из проверок посадки: связывание, написанное на невидимом страж
    поле, зелено при любом входе — то есть повторило бы ровно тот дефект, который заменяет.
    Существующее связывание мертво **по антецеденту**: оно требует читателя при посадке, которую
    не выбирает ни один профиль (§15.4). Порядок обязателен: читатель поля → связывание → проба.

    Предикат **в обе стороны** (`release/kaname` @ `fe0e5a60f3`; единица — вхождение имени поля в
    трёх прод-файлах проверок посадки): поле адреса фронта — **0**, поле читателя предъявленного —
    **не ноль**. Односторонний здесь негоден: «0» без второй величины неотличимо от «предикат не
    нашёл ничего вообще».

    ```sh
    G=services/iam/internal/apps/kaname/config
    for fld in RESTEndpoint PresentedCredential; do
      printf '%s %s\n' "$fld" "$(git grep -h "$fld" -- "$G/validate.go" \
        "$G/required_settings.go" "$G/lane_requirements.go" | wc -l)"
    done
    ```
11. **Регрессия платформенного трафика**: набор шарда, зелёный сегодня, остаётся зелёным после
    пункта 8 (KAN-REG-01).

### Чего в объём НЕ входит

- **Свой мультиплексор REST** (#1109) — он этой приёмкой **разблокируется**, а не выполняется.
- **Отзыв или изменение прежнего пути** переданной личности — ось 7 решения запрещает прямо.
- **Пообъектная авторизация** — другой предмет, закрыт своей задачей.
- **Расписание ротации ключей подписи**: механизм в дереве есть, живого расписания нет. Это
  **отдельный предмет**, названный в §11, а не отсрочка внутри этого.
- **Проба согласия у двух платформенных носителей перечня проверок** (§1.5) — предмет чужой
  линии, чинится своей задачей (§11).
- **Пересмотр формы проверки** — решение §2 принято; переоткрытие требует своего решения владельца.
- **Ослабление требования транспорта на крае** — пункт **8** снимает **пересылку**, а не **требование**.
  Край продолжает требовать удостоверение и проверять его сам; вызывающий не замечает разницы.
- **Второй путь установления личности** — ни для запаса, ни для совместимости, ни «пока не
  доедет первый» (ось 11).
- **Приведение посадки `own` к живому профилю платформы** — пункт 10 заводит профиль для **чужого
  облака**; посадка платформы остаётся какой была, и пункт 11 обязан её пропускать (KAN-BIND-03).

---

## 8. Порядок исполнения после APPROVED

Строгий TDD (ban #12): красная проба до кода, по каждому пункту.

**Фаз ТРИ, и порядок между ними несущий, а не удобный.** Каждая **сама production-complete** в
своих границах (ban #14): полностью реализована, покрыта, безопасна, и отложенное вынесено в
`Out-of-scope` следующей — а не «пока сделаем минимально».

#### Ф0 — читатель на публичной цепочке (объём §7 пп. 1–7)

Порядок внутри фазы прежний и редакцией 2 не тронут, кроме одного вычёркивания:

1. `class-exposure-analyst` по этому документу — до первой строки кода.
2. Стражи провязки (KAN-WIRE-01, KAN-WIRE-02) — **первыми**: без них поведенческие пробы
   рискуют оказаться вакуумными, и цена этого выясняется только на сведённом дереве.
3. Поведенческие пробы VER и DENY на харнессе публичной цепочки — красные. **Проб семейства DUP
   больше нет**: предмет снят (§4).
4. Читатель предъявленного; проба согласия с перечнем (KAN-POL-01).
5. Чтение отзыва на пути запроса и сквозная проба REV — **одним** прогоном, не двумя по половине.
6. Ось стража старта (BOOT) и измерители, включая различимость причины владельцу (KAN-DENY-03).
7. Шаг руководства (KAN-OPS-01).

**Production-complete по Ф0:** механизм существует, покрыт, безопасен и **выключен по
умолчанию**. Читателя на развёрнутой посадке ещё нет — и это не отсрочка, а граница: включать его
до Ф1 **запрещено**, потому что включение отвергало бы платформенный трафик (§15.2).
**Out-of-scope Ф0:** снятие удостоверения на крае · ручка в чарте · связывание стража.

#### Ф1 — край перестаёт пересылать удостоверение за себя (объём §7 п. 8)

1. Перепись потребителей входящего удостоверения по поверхностям (KAN-STRIP-03) — **первой**:
   она устанавливает радиус, и без неё снятие делается наугад.

   **1а. Перепись мест сборки исходящих метаданных на крае** (§7 п. 8а): общий узел, его вызовы и
   конструкции помимо него. Решение по каждой конструкции вне узла принимается **здесь**, до
   правки, и записывается **в код рядом со снятием**.
2. Красные пробы KAN-STRIP-01 и KAN-STRIP-02 — **парой**, не по одной: без второй первая зеленеет
   на сборке, где удостоверение не доезжает никуда.
3. Снятие на крае — **в общем узле**, плюс исполнение решений по конструкциям вне его.

   **3а. Снятие живой ветки отказа «обе формы разом» у читателя вместе с её держателями**
   (§7 п. 8б) — **тем же изменением**, что шаг 3, и не раньше него.
4. Прогон KAN-REG-01 — вердикт **по числам**, с отдельным счётом «не выполнилось».

> [!important] Шаги 3 и 3а НЕ ВЫПОЛНЯЮТСЯ ПОРОЗНЬ — и это парадокс, а не предпочтение
> Ф0 запрещает включать читателя до Ф1, потому что включение отвергало бы платформенный трафик.
> **Держит этот запрет сегодня именно снимаемая ветка:** она даёт **громкий** отказ там, где иначе
> был бы **молчаливый** выбор одной из двух личностей. То есть ветка, отменённая осью 8 по
> замыслу, исполняет по факту роль страховки на время перехода.
>
> Отсюда порядок несущий, и оба обратных порядка неверны **по-разному**:
>
> | что сделано порознь | что получится |
> |---|---|
> | ветка снята **раньше** снятия на крае | запрос через край несёт обе формы, и читатель выбирает одну из них **молча** — неоднозначность разрешается догадкой, ровно то, что ось 8 запрещает |
> | снятие на крае сделано, ветка **оставлена** | реализация отозванного решения живёт в дереве без сценария, без пункта объёма и без производителя входа — она никогда не сработает и никогда не покраснеет |
>
> Первое хуже: оно не отказ, а тихий выбор. Поэтому шаги 3 и 3а — **одно изменение**, и проба
> KAN-STRIP-01 (за краем удостоверения нет) есть одновременно доказательство того, что снятие
> ветки безопасно.

**Production-complete по Ф1:** сочетание двух форм личности стало непредставимым **и в контракте,
и в коде**, мина #2100 разряжена, платформенный трафик не деградировал (число — в §15.5).
Читатель по-прежнему выключен на всех посадках, и служба ведёт себя ровно как до Ф1.
**Out-of-scope Ф1:** ручка в чарте · профиль установки · связывание стража.

#### Ф2 — посадка поднимает читателя, страж связывает (объём §7 пп. 9–11)

1. KAN-LAND-01 — перепись объявлений чарта, красная.
2. Ручка в значениях и в шаблоне настроек; **умолчание не меняется**.

   **2а. Читатель поля адреса публичного фронта в страже** (§7 п. 10а) — до связывания:
   связывание, написанное на поле, которого страж не читает, зелено при любом входе.
3. KAN-BIND-01…03 — три красные пробы стража **сразу**, включая KAN-BIND-03: без неё связывание
   отвергало бы законную посадку без фронта.
4. Связывание в таблице требований.
5. Профиль установки для чужого облака (KAN-LAND-02) и перепись производителей личности
   (KAN-SOLE-01).
6. Прогон на **развёрнутой** посадке: годное принимается, негодные отвергаются неразличимо между
   собой. **Рендер вердиктом не является** — предикат снятия #2191 требует прогона.

**Production-complete по Ф2:** собственный публичный фронт различает предъявителя на развёрнутой
посадке, и половины пары «фронт без читателя» не существует — страж её не пускает.
**Out-of-scope Ф2:** ничего из объёма §7 не остаётся; остатки — §11, у каждого своя задача.

#### Общее

- `proto-api-reviewer` не требуется: контракт не меняется. `go-style-reviewer`,
  `system-design-reviewer` и `security-surface` — обязательны на **каждой** фазе.
- **Ф1 обязана предшествовать Ф2.** Обратный порядок даёт отказ каждому запросу через край;
  это не риск, а следствие, и оно измерено (§15.2). Фазы не сливаются в одну: у них разные
  предметы и разные прогоны, а слитая фаза не даёт сказать, чем вызвана краснота.

---

## 9. Определение готовности приёмки

1. каждый сценарий несёт производителя либо помечен `ждёт производителя` — §3; помеченных
   **23**, из них двадцать два заводят производителя этим же переходом, а двадцать третий
   (KAN-SOLE-01) — переписью §7 п. 10;
2. каждый сценарий имеет строку в таблице состояния — §5, строк **41**, сценариев **41**;
   предикат обеих величин назван и **ограничен областью** (§3), потому что без ограничения он
   даёт 45 и его слепота названа прямо;
3. у каждого отрицания назван положительный близнец, отличающийся **ровно одним** названным
   фактом — VER-01 для семейства VER, REV-02 для REV, DENY-02 (и собственный) для DENY,
   BOOT-02 для BOOT, BIND-02 и BIND-03 для BIND, STRIP-02 для STRIP, LAND-02 несёт свой второй
   рендер; у KAN-VER-11 близнец **свой** (VER-12), потому что требование двустороннее;
4. состав проверок читателя сходится с единым перечнем **в обе стороны**: одиннадцати
   обязательным проверкам отвечают одиннадцать односфактных отрицаний §4, отступлений не
   объявлено, и закрытый перечень причин KAN-DENY-01 несёт **двенадцать** причин — по одной на
   каждое отрицание семейств VER и REV. Тринадцатая («обе формы разом») снята **вместе со своим
   предметом**, а не выпала;
5. каждое число §1 снято названным предикатом на названной ревизии, и негодность одного из
   предикатов задачи названа прямо, а не обойдена — §1.1. Числа редакции 2 сняты на **своей**
   ревизии и стоят отдельно — §15, чтобы замер одной даты не выдавался за замер другой;
6. решение о форме (§2) приведено как **вход**; оси 8–11 редакции 2 названы вместе с тем, **какое
   прежнее решение отзывается и чем это измерено** (§15), а не переписаны молча;
7. каждая фаза §8 **сама production-complete**, и порядок между ними обоснован следствием, а не
   удобством;
8. **у каждого пункта объёма §7 есть сценарий, И у каждого сценария, чей ДЕРЖАТЕЛЬ заводится
   этим переходом, есть пункт объёма.** Обратная сторона критерия 1, и проверяется она отдельно,
   потому что нарушается отдельно — причём **в обе стороны, и стороны разные**. Пунктов **11**, и
   каждому отвечает хотя бы один сценарий §4:

   | пункт | сценарии | | пункт | сценарии |
   |---:|---|---|---:|---|
   | 1 | VER, REV, **POL-02** | | 7 | OPS-02, DENY-03 |
   | 2 | DENY-01, **DENY-02** | | 8 | STRIP |
   | 3 | BOOT | | 9 | LAND |
   | 4 | OPS | | 10 | BIND, SOLE |
   | 5 | WIRE, POL-01, **FWD-03** | | 11 | REG |
   | 6 | OPS-01 | | | |

   Отображение сверено **в обе стороны**: пустых пунктов нет, и ни один сценарий с заводимым
   держателем не остался без пункта. Три имени выделены потому, что их не было в сокращённом
   отображении круга 3: DENY-02 — положительный близнец, покрытый пунктами 1 и 2; POL-02 —
   закрывается читателем (пункт 1) и наблюдается переписью; FWD-03 — предмет правки круга 4.
   **Пункт без сценария** санкционирует работу, наблюдаемого исхода у которой не объявлено, —
   именно так пережил редакцию 2 снятый предмет (§16.1). **Сценарий без пункта** требует работы,
   которой одобрение не разрешает, — именно так провалился между осями KAN-FWD-03 (§7, оговорка
   к пункту 5);
9. документ выдерживает публикацию — §10.

---

## 10. Публичность

Оба репозитория публичны. Предмет этого документа — **отсутствующая возможность**, и это делает
его безопаснее обычного: дефект даёт **лишний отказ**, а не лишнее разрешение, и раскрываемого
состояния у него нет.

Что здесь **написано**: предмет, принятое решение, следствие для контракта, требования к
поведению читателя, координаты уже существующих механизмов, которые он переиспользует.

Чего здесь **нет** и не будет: сборки «точная координата + условие, при котором защита не
действовала + следствие для постороннего»; описания того, какой именно вход сегодня проходит
какую именно поверхность; порядка действий, которым этим можно воспользоваться. Требования
сформулированы как требования — «отказы обязаны быть неразличимы», — а не как описание того,
что сегодня различимо.

**Три места из-под запрета выведены** и выхолащивать их не следует: текст отказа оператору при
отказе старта (KAN-BOOT-01) — без него стенд не поднять; имя и текст падения пробы — это
описание защищаемого свойства; комментарий у стража — без него страж снимут как непонятный.

---

## 11. Что остаётся за пределами этого документа

Названо предметом, а не обещанием: у каждого пункта есть признак и предикат, и каждый заводится
**своей** задачей — чужой предмет в чужом изменении не чинится.

| Остаток | Признак | Предикат снятия |
|---|---|---|
| два платформенных носителя единого перечня проверок токена не несут пробы согласия | носителей 4, проб согласия 2 (§1.5) | у каждого носителя есть проба, сверяющая состав с перечнем в обе стороны |
| живого расписания ротации ключей подписи нет | механизм ротации вызывается однократно при подъёме, если активного ключа нет вовсе | ротация исполняется по расписанию, и её исполнение наблюдаемо |
| посадка `own` не выбрана ни одним профилем развёртывания | `git grep -c 'identityProvider: own' -- deploy` → 0 | профиль, на котором сценарии OPS исполнимы, существует |
| предикат задачи #2077 негоден по единице счёта | шаблон с двумя звёздами не видит файлов верхнего уровня каталога (§1.1) | предикат в теле задачи заменён на обход каталога целиком |
| **предикат задачи #2191 знает одну форму записи имени** (＋ ред. 2) | по строчной форме потребителей 3, по обеим — 11; разница не край, а вторая законная форма (§15.3) | предикат в теле задачи назван вместе с **единицей счёта** и знает обе формы |

**Ни один из пяти не является отсрочкой внутри этого предмета** (ban #11): у каждого свой
предмет, своя причина и свой предикат снятия. Первый и второй — чужих линий; третий заводится
объёмом §7 п. 4 и §7 п. 9; четвёртый и пятый — правка тел задач.

### Требует РЕШЕНИЯ ВЛАДЕЛЬЦА, а не работы (＋ редакция 2)

Три вопроса. Ни один не решается этим документом, и ни один не блокирует Ф0 — но **Ф1 и Ф2 без
первого не начинаются**, потому что §2 оставляет форму проверки владельцу линии.

| Вопрос | Что уже установлено замером | Чего не хватает |
|---|---|---|
| **1. взаимоисключение полос — снятием на крае или сужением по доверию к пиру?** | #2100 предлагал сужение и отверг снятие доводом «удостоверение на крае читают другие потребители». Довод **измерен и оказался о другом предмете**: на крае — да, читают, и снятие их не трогает; **за краем** читатель ровно один (§15.3). То есть основание отказа не подтвердилось | выбор владельца между двумя формами. Редакция 2 предлагает снятие как **более узкое по радиусу решения** и **более широкое по радиусу правки**; обратный размен тоже защитим |
| **2. включается ли читатель на посадке ПЛАТФОРМЫ, а не только чужого облака** | после Ф1 включение перестаёт отвергать трафик, то есть становится **возможным**. Нужно ли оно — вопрос продукта, а не безопасности: за краем предъявленного всё равно не будет | решение «одна посадка на обе установки» против «две посадки, каждая со своей полосой» |
| **3. что считать ПОВЕРХНОСТЬЮ в переписи KAN-SOLE-01** | поверхностей у службы сегодня шесть по числу слушателей; собственные HTTP-поверхности читают удостоверение **сами**, и это законно | граница переписи: слушатель, порт или адресат. От неё зависит, что перепись назовёт нарушением |

> [!warning] Санкция объёма и ВЫБОР ЕГО ВХОДА — разные вещи; читать это надо так
> **Сегодня шапка говорит «кодировать нельзя»**: вердикт круга 4 остался на отпечатке
> `f77d062d` и на эту редакцию не переносится. Здесь стояло обратное — «шапка говорит
> „кодировать разрешено"». Утверждение было верно при круге 4 и стало ложным **от дельты**,
> переведшей шапку в черновик: врезка, специально написанная о том, что можно начинать, выдавала
> разрешение, которого документ не даёт (ban #1). Правит та же дельта, что его отняла; найдено
> кругом 6.
>
> **Само предупреждение не отпадает, а откладывается до возврата одобрения.** Когда APPROVED
> вернётся, §7 п. 8 будет санкционировать снятие на крае, а вопрос 1 выше — объявлять форму
> взаимоисключения нерешённой. Противоречия внутри документа нет — §2 всегда оставлял форму
> владельцу, — но исполнитель, прочитавший одну шапку, получил бы санкцию на работу, чей вход
> не назван.
>
> **Поэтому граница проводится по фазам, а не по документу целиком: у Ф0 вход назван, Ф1 и Ф2
> ждут ответа на вопрос 1.** Ответ «сужение по доверию к пиру» переписывает ось 8, §7 п. 8 и всё
> семейство STRIP; ответ «снятие на крае» оставляет их как есть. Остаток Ф0 (три пункта, §5) от
> ответа не зависит и может делаться параллельно.

---

## 12. Ревью, круг 1 — ❌ CHANGES REQUESTED

> **Ревьюер:** `acceptance-reviewer` · **дата:** 2026-09-06 · **вердикт:** ❌ CHANGES REQUESTED.
> **Ревизии рецензии:** монорепо `release/kaname` @ `da18ac9929` (голова на день рецензии;
> документ снимал числа на `caa48b770d`, между ними **9** коммитов); воркспейс `lane/acc-2077`
> @ `f3305ffb`, содержимое побайтово равно `issue-382-change-package`.
> **Прежнее объявление шапки, замещённое этим кругом** (история добавляется, не переписывается):
> `**Статус:** DRAFT — вердикт выносит acceptance-reviewer (единственный approve-gate; заказчик
> подключается на шаге 7, к финальному smoke/e2e). До вердикта запрет #1 действует: кода по
> этому документу не пишется.`

### 12.1 Исход проверки посылок — числа §1 ПЕРЕМЕРЕНЫ, устаревших нет

Каждый предикат §1 прогнан **дословно** и на **обеих** ревизиях — `caa48b770d` (названа
документом) и `da18ac9929` (голова на день рецензии). Обе стороны названы вслух: «перемерил и
сошлось» иначе неотличимо от «не перемерял».

| § | величина документа | `caa48b770d` | `da18ac9929` | исход |
|---|---|---|---|---|
| 1.1 | оба предиката задачи → 0 | 0 · 0 | 0 · 0 | сошлось |
| 1.1 | контроль: `**/*.go` → 0, `*.go` → 1 | 0 · 1 | 0 · 1 | сошлось |
| 1.2 | звеньев публичной цепочки — 8 | 8 | 8 | сошлось |
| 1.3 | служб 22, из них `Internal` — 9 ⇒ 13 публичных | 22 · 9 | 22 · 9 | сошлось |
| 1.5 | носителей 4, проб согласия 2 | 4 · 2 | 4 · 2 | сошлось |
| 1.6 | `AllowedAlgorithmList` — 3 строки, 1 файл | 3 · 1 | 3 · 1 | сошлось |
| 1.7 | `identityProvider:` — 1 вхождение, `own` — 0 | 1 · 0 | 1 · 0 | сошлось |

**Устаревших чисел нет ни одного**, и это не совпадение: девять коммитов дрейфа трогают
конвейер, пробы посадки и умолчания чарта, но ни одной величины, на которую документ опирается.
Ручка посадки (§1.7) была под наибольшим риском — файлы значений в дрейфе изменены — и **не**
изменилась.

**Качественные утверждения проверены отдельно и подтвердились, а два — сильнее заявленного:**

- **§1.1 верно и о своём предмете, и о предикате задачи.** Единственное вхождение —
  перечень ключей, чьи значения не пишутся в журнал; читателем оно не является. Негодность
  предиката задачи (шаблон с двумя звёздами не видит файлов верхнего уровня каталога)
  воспроизведена контролем в обе стороны.
- **§1.2 сошлось поэлементно**: 3 звена + пара извлечения личности + 3 звена = 8; разметка
  отказа действительно не трогает коды, кроме `PermissionDenied`.
- **§1.4 подтверждено самим деревом, и это сильнейшее свидетельство документа**: авторитет
  отзыва объявляет отступлениями ровно адресат и тип токена, и **причина отступления сама
  требует** того читателя, который заводится этим переходом. Шапка того же пакета объявляет
  норму оси 4 дословно. Кеша у него нет.
- **§1.6 сильнее, чем сказано в документе.** Величина не только не читается ни одной
  проверкой — **текст отказа стража старта утверждает, что на этом перечне держится проверка
  связи заголовка с ключом**, а такой проверки на этой поверхности нет. То есть это
  «неисполнимая возможность» плюс комментарий, противоречащий коду. Это усиливает KAN-POL-02
  и прямо связано с блокирующим замечанием 12.2.
- **§5: все три названных существующих держателя резолвятся** — файлы и имена проб совпадают
  дословно.

### 12.2 БЛОКИРУЮЩЕЕ 1 — решение §2 называет ДВА предмета, сценарий получил ОДИН

**Координата.** §2 ось 3 (приведена документом как принятый **вход**): «адресат и тип —
свойства поверхности предъявления; у выданного наружу авторитета они объявлены отступлением
(§1.4), и **закрыть их обязан именно этот читатель**». §7 п. 1 санкционирует сверх того «типа,
допустимости подписи и связи заголовка с ключом». KAN-POL-01 требует согласия состава проверок
читателя с единым перечнем **в обе стороны**.

**Предикат.** Единый перечень обязателен целиком — его состав в дереве:

```sh
git grep -n -A14 'func MandatoryChecks' -- pkg/tokenpolicy/policy.go   # 11 проверок
```

Сопоставление одиннадцати с семейством VER + REV даёт: подпись → KAN-VER-03 · допустимость
подписи → KAN-VER-06 · издатель → KAN-VER-07 · адресат → KAN-VER-04 · срок → KAN-VER-02 ·
момент начала → KAN-VER-05 · отзыв → KAN-REV-01. **Покрыто 7 из 11.** Без сценария остаются
**тип токена**, **связь заголовка с ключом**, **критические заголовки** и **идентификатор
ключа** (последний в лучшем случае слит с KAN-VER-03).

**Почему это блокирует, а не является придиркой.** Дело не в арифметике покрытия, а в двух
следствиях:

1. **документ противоречит собственному принятому решению.** Ось 3 называет ровно две вещи,
   которые этот читатель существует закрыть, — адресат **и тип**. Адресат получил KAN-VER-04
   с отдельной врезкой, объясняющей, почему он несущий. Тип не получил ничего. Из двух
   названных предметов наблюдаемым поведением закреплён один;
2. **перечень причин KAN-DENY-01 закрыт девятью пунктами** («истёк · чужой ключ · чужой
   адресат · время не наступило · подпись вне перечня · чужой издатель · отозван · авторитет
   отзыва недоступен · обе формы разом») и четырёх названных причин **не содержит**. То есть
   отказ по типу токена, по связи заголовка с ключом и по критическим заголовкам **ничем не
   обязан** быть неразличим с остальными — при том что неразличимость отказов есть
   центральная гарантия документа (§2 ось 5). Сценарий, который должен был бы поймать оракул,
   этих отказов не наблюдает.

Отдельно и в пользу автора: **связь заголовка с ключом** — это ровно та проверка, чьё
отсутствие §1.6 предъявляет как «неисполнимую возможность». Она санкционирована §7, объявлена
стражем старта, названа в тексте его отказа — и не имеет ни сценария, ни строки в перечне
неразличимости.

**Применимый исход — один из трёх, выбор за автором:**

- **переписать «Тогда» под производимое** — завести недостающие односфактные отрицания по
  образцу KAN-VER-02…07 (производитель у них тот же читатель, он заводится этим переходом,
  поэтому нового производителя заказывать не нужно) **и** расширить перечень причин
  KAN-DENY-01 до полного;
- **сузить обещание явно** — объявить недостающие проверки **отступлениями с причиной** в §7
  и в KAN-POL-01, ровно тем механизмом, которым авторитет отзыва объявляет свои. Тогда
  отсутствие сценария законно, потому что отсутствует и сама проверка;
- **снять задачей-преемником** — вынести их своей задачей с признаком и предикатом, добавив
  строку в §11.

«Оставить как есть» исходом не является: сегодня §7 их санкционирует, KAN-POL-01 их требует, а
§4 их не наблюдает.

### 12.3 БЛОКИРУЮЩЕЕ 2 — KAN-REV-04 не может упасть на отсутствии кеша

**Координата.** KAN-REV-04, первое «Тогда»: «вызов **ещё может дойти** до обработчика — это
объявленное окно, а не дефект».

**Предикат — рассуждением о представимом мире, проверяется чтением четырёх сценариев REV.**
Возьмём реализацию, у которой кеша нет вовсе и отзыв спрашивается на каждом запросе:

| сценарий | исход без кеша | почему проходит |
|---|---|---|
| KAN-REV-01 | проходит | после ожидания токен отвергнут — да |
| KAN-REV-02 | проходит | отзыва нет, токен принят — да |
| KAN-REV-03 | проходит | положительного вердикта в кеше нет **by construction** |
| KAN-REV-04 | проходит | «может дойти» верно и когда не доходит |

**Ни один сценарий документа не краснеет на реализации без кеша** — при том что короткий кеш
объявлен осью 4 решения (§2) и стоит в объёме §7 п. 1 («с коротким кешем положительного
вердикта»). Утверждение, принимающее оба исхода, утверждением не является — это тот самый
класс, который документ сам цитирует в KAN-DENY-02 и в §6.

Довод «окно недетерминированно» здесь не проходит: §5 назначает KAN-REV-04 держателем «пробу
**с управляемыми часами**», а при управляемых часах исход внутри окна детерминирован.

**Применимый исход:** **переписать «Тогда» под производимое** — сделать первое утверждение
решительным («вызов доходит до обработчика, потому что положительный вердикт закеширован и
срок кеша не истёк»), чтобы отсутствие кеша роняло сценарий. Заказывать производителя не
нужно: кеш уже в объёме §7 п. 1.

### 12.4 Не блокирующее — учесть при правке

1. **§3, сводная фраза** «производитель у них один — читатель предъявленного» точна для 14 из
   17 помеченных и неточна для KAN-OPS-01 (шаг руководства), KAN-OPS-02 (измерители) и
   отчасти KAN-POL-02: их производители — §7 пп. 7 и 8. Сам объём §7 их перечисляет верно,
   расходится только обобщение.
2. **KAN-VER-01, последнее «И»** («ни один шаг проверки не обращался к внешнему поставщику»)
   в посадке `own` истинно **by construction** — поставщика в этой установке нет. Это не
   ошибка, но и не наблюдение: стоит либо назвать его свойством построения, либо сказать, чем
   оно наблюдается у оператора.
3. **§1.6 стоит усилить** тем, что нашла рецензия: текст отказа стража утверждает наличие
   проверки, которой нет. Это укрепляет KAN-POL-02 и связывает его с 12.2.

### 12.5 Ось публикуемости — ПРОЙДЕНА

Проверено, а не принято на веру: документ прочитан как посторонним, и отдельно проверено
**само основание** его довода — что дефект даёт лишний отказ, а не лишнее разрешение.

- **Основание истинно.** Последнее звено публичной цепочки — пообъектная дверь — строго
  fail-closed и стоит на **каждом** вызове обеих полос, а не только на мутациях. Поэтому
  «назваться нечем» оканчивается отказом, и состояния, которым можно воспользоваться, из
  документа не восстанавливается.
- **Сборки «координата + условие, при котором защита не действовала + следствие для
  постороннего» в документе нет.** Требования сформулированы как требования; какой вход
  сегодня какую поверхность проходит, документ не описывает.
- **Ближайшее к границе место** — строка звена 7 в §1.2, называющая, что отсечка анонима
  пропускает чтение. Границу она не переходит: следствия из неё не выводится, а соседняя
  строка той же таблицы называет дверь, которая на чтении и стоит. Оставить как есть.
- **Три выведенных из-под запрета места** (§10) названы верно и выхолащиванию не подлежат.

Тем же требованием связан и **текст этой рецензии**: он называет предметы и требования и не
собирает восстановимого состояния.

### 12.6 Что осталось неустановленным

Названо, чтобы «не проверено» не читалось как «проверено и сошлось»:

- **истинность объявленного вердикта машинно не проверяется** — распознаватель набора
  `docs-gate` судит **объявление** в шапке, а не одобрение. Это его известный предел, а не
  свойство этого документа;
- **§1.2 «звеньев восемь»** проверено чтением композиционного корня, а не прогоном: у числа
  звеньев цепочки исполняемого предиката в дереве нет;
- **замысел линии** (`docs/changes/standalone-iam/design.md`, файл на ветке присутствует)
  **не рецензировался** — он вне предмета этой рецензии, документ на него опирается законно;
- **соседние задачи линии** (#2086, #2090…#2096) не разбирались — чужой предмет.

### 12.7 Что делать дальше

`acceptance-author` закрывает 12.2 и 12.3 — обе правки в существующей структуре документа
(семейство VER уже устроено ровно под односфактные отрицания, KAN-DENY-01 — под перечень
причин), после чего круг 2. **Объём §7, решение §2 и порядок §8 под сомнение не ставятся**;
§1 перемерен и правки не требует.

---

## 13. Ответ автора на круг 1 — правка внесена, круг 2

> **Автор:** `acceptance-author` · **дата:** 2026-09-06 · **предмет:** ровно два блокирующих
> замечания §12.2 и §12.3. Запись **добавлена**, а не заменяет §12: прежний вердикт относится к
> прежней редакции и остаётся её вердиктом дословно.
> **Ревизия дерева продукта, на которой проверялись производители:** `2d2b508076`. Обе ревизии
> рецензии (`caa48b770d`, `da18ac9929`) в этом дереве резолвятся; величины §1.5 на всех трёх
> совпадают — носителей **4**, проб согласия **2**.
>
> **Прежнее объявление шапки, замещённое этой правкой** (история добавляется, не переписывается):
> `**Статус:** CHANGES REQUESTED — acceptance-reviewer, круг 1, 2026-09-06 (ban #1). Кодировать
> по этому документу **нельзя**: два блокирующих замечания в §12. Оба структурные и дешёвые —
> объём §7 и замысел §2 под сомнение не ставятся, правится покрытие сценариями. Прежнее
> объявление шапки сохранено в §12.`
>
> **Вердикта эта запись не выносит.** Автор приёмки не является approve-gate: круг 2 выносит
> `acceptance-reviewer`. До него запрет #1 действует.

### 13.1 Исход проверки посылок круга 1 — обе стороны названы

| посылка §12 | исход |
|---|---|
| единый перечень содержит **11** проверок | **сошлось** — `git grep -n -A14 'func MandatoryChecks' -- pkg/tokenpolicy/policy.go` |
| покрыто сценариями **7** из 11 | **сошлось** поэлементно: подпись · допустимость подписи · издатель · адресат · срок · момент начала · отзыв |
| без сценария — тип, связь заголовка с ключом, критические заголовки, идентификатор ключа | **сошлось**; идентификатор ключа был **слит** с KAN-VER-03, как и предполагала рецензия |
| §2 ось 3 называет **два** предмета — адресат и тип | **сошлось** дословно |
| перечень причин KAN-DENY-01 закрыт **девятью** и четырёх не содержит | **сошлось** |
| KAN-REV-04 истинен при любом исходе; читатель без кеша проходит все четыре REV | **сошлось** — разобрано в §13.3 |
| правка помещается **внутрь существующей структуры** | **сошлось**: новых разделов §4 не заведено, семейство VER продолжено, перечень причин расширен на месте |
| «нового производителя заказывать не нужно» | **сошлось, и сильнее заявленного** — см. §13.2: механизм **каждой** из четырёх проверок в дереве уже есть, причём три из четырёх объявлены **всеми четырьмя** носителями перечня; недостаёт потребителя на этой поверхности |

### 13.2 Ответ на §12.2 — выбран исход «переписать под производимое», по всем четырём

**Почему не «сузить обещание отступлением».** Отступление обязано называть, **что делает
проверку неприменимой здесь**, а не когда её собираются добавить. Такой причины нет ни у одной из
четырёх, и это измерено по всем четырём носителям перечня (§1.5), а не выведено из одного:

| проверка | сколько носителей из 4 её **объявляют** | кто и почему **отступает** |
|---|---:|---|
| связь заголовка с ключом | **4** | никто |
| идентификатор ключа | **4** | никто |
| критические заголовки | **4** | никто |
| тип токена | **3** | авторитет отзыва — с причиной: тип сверяет **поверхность предъявления**, а не издатель |

Публичный слушатель и есть та поверхность, на которую причина единственного отступления и
указывает. Объявить отступление здесь значило бы сказать, что тип не сверяет **никто**.

Отдельно, чтобы число не читалось шире, чем оно есть: у двух платформенных носителей объявление
несёт **десять** записей статически, а одиннадцатая — отзыв — попадает в него тогда и только
тогда, когда его объявила хотя бы одна запись издателя. Для довода выше это ничего не меняет:
все четыре спорные проверки стоят в статической части.

**Почему не «снять задачей-преемником».** Преемник законен там, где предмет чужой (§11 — четыре
таких). Здесь предмет свой: §2 ось 3 называет тип прямо, §7 п. 1 санкционирует тип, допустимость
подписи и связь заголовка с ключом, а §7 п. 6 санкционирует пробу согласия с единым перечнем —
и согласие это **двустороннее**. То есть объём уже обязывает читателя ко всем одиннадцати:
исполнить либо объявить отступление. Третьего состояния нет, и вынести часть перечня в преемника
значило бы оставить читателя, который своей же пробе согласия не удовлетворяет.

**Найденные производители — по одному на проверку.** «Производитель» здесь тот же, что в §3:
механизм, эмитирующий наблюдаемое. Ни одна из четырёх проверок этой приёмкой **не изобретается**
— заводится её потребитель на публичной цепочке.

| проверка | сценарий | механизм в дереве | чем проверено |
|---|---|---|---|
| тип токена | KAN-VER-08 | объявляют три носителя из четырёх; четвёртый — авторитет отзыва — объявляет отступлением **с причиной**, называющей поверхность предъявления | `git grep -n 'CheckTokenType' -- '*.go' ':!*_test.go'` |
| связь заголовка с ключом | KAN-VER-09 | объявляют **все четыре** носителя | `git grep -n 'CheckKeyBoundAlgorithm' -- '*.go' ':!*_test.go'` |
| идентификатор ключа | KAN-VER-10 | объявляют **все четыре**; форма идентификатора ограничивается **до** использования, разрешение — по собственному реестру записи | `git grep -n 'CheckKeyID' -- '*.go' ':!*_test.go'` |
| критические заголовки | KAN-VER-11 + KAN-VER-12 | объявляют **все четыре**; исполняются **общей** функцией платформы, несущей обе стороны требования сразу; перечень исполняемых параметров объявлен и сегодня пуст — решением, а не заготовкой | `git grep -n 'CriticalHeadersUnderstood' -- '*.go' ':!*_test.go'` |

**Что изменено в §4.** Пять новых односфактных сценариев по образцу KAN-VER-02…07 — KAN-VER-08
(тип) · KAN-VER-09 (связь заголовка с ключом) · KAN-VER-10 (идентификатор ключа) · KAN-VER-11
(помеченный обязательным параметр) · KAN-VER-12 (**положительный** близнец к KAN-VER-11).
KAN-VER-03 **сужен** до одного факта: прежняя редакция меняла и разрешение идентификатора, и
сходимость подписи разом.

**KAN-VER-12 заведён не для симметрии.** Требование о помеченных обязательными параметрах
двустороннее: ужесточение одной половины без другой ломает совместимость. Без положительного
близнеца читатель, отвергающий **любой** незнакомый параметр, проходил бы KAN-VER-11 как
правильный — то есть отрицание зеленело бы на слишком строгой реализации.

**Перечень причин KAN-DENY-01 расширен вместе со сценариями — с 9 до 13**, и к нему добавлено
утверждение о том, что перечень **сходится с семейством отрицаний**. Иначе повторилась бы ровно
та дыра, которую нашла рецензия: покрытие перечня проверок шире покрытия неразличимости, и
отказ, не попавший в перечень, неразличимости ничем не обязан.

**KAN-POL-01 уточнён**: перечень отступлений этого читателя **пуст осознанно** — все одиннадцать
исполняются; и каждой проверке отвечает своё отрицание, а каждому отрицанию — строка перечня
причин.

**§7, §2 и §8 не тронуты.** Объём уже санкционировал всё внесённое: п. 1 называет тип,
допустимость подписи и связь заголовка с ключом прямо, разрешение идентификатора входит в
проверку подписи собственным реестром ключей, а п. 6 требует согласия с перечнем в обе стороны.

### 13.3 Ответ на §12.3 — выбран исход «переписать под производимое»

Первое утверждение KAN-REV-04 сделано **решительным**: вызов **доходит** до обработчика, потому
что положительный вердикт закеширован и объявленный срок кеша не истёк. Добавлено требование
**управляемых часов** — оно уже стояло в §5 как держатель, но в самом сценарии названо не было,
а именно оно делает исход внутри окна определённым.

**Проверка, что сценарий теперь способен упасть.** Читатель без кеша спрашивает авторитет на
каждом предъявлении, поэтому сразу после записи отзыва он вызов **отвергнет** — а сценарий
требует пропустить. KAN-REV-04 краснеет, и блокирующее «ни один сценарий не краснеет на
реализации без кеша» снято.

**Что осталось верным и не правилось.** KAN-REV-03 по-прежнему выполним на читателе без кеша:
его предусловие «положительного вердикта в кеше нет» такой читатель удовлетворяет by
construction. Это не делает его вакуумным **относительно собственного предмета** — он утверждает
fail-closed при недоступности авторитета, и на читателе, засчитывающем неполученный ответ за
«не отозван», он краснеет. Отличать кеш от его отсутствия — работа KAN-REV-04, и теперь она
делается.

### 13.4 Замечания §12.4 — что сделано и что нет

| # | замечание | исход |
|---|---|---|
| 1 | сводная фраза §3 «производитель у них один» неточна для KAN-OPS-01, KAN-OPS-02 и отчасти KAN-POL-02 | **исправлено**: производители названы порознь, со ссылками на §7 пп. 7 и 8. Правка была вынужденной — числа §3 менялись этой же правкой |
| 2 | последнее «И» KAN-VER-01 истинно by construction | **исправлено**: названо свойством построения и сказано, чем оно наблюдается у оператора |
| 3 | §1.6 стоит усилить находкой рецензии | **не сделано намеренно**: §1 перемерен рецензией и правки не требует (§12.7), а усиление §1.6 — правка §1. Оставлено следующему кругу как **необязательное**; наблюдение рецензии при этом использовано — оно стоит доводом в KAN-VER-09 |

### 13.5 Числа круга

| величина | было | стало |
|---|---:|---:|
| сценариев | 26 | **31** |
| ждут производителя | 17 | **22** |
| производитель есть | 9 | **9** |
| строк в таблице §5 | 26 | **31** |
| обязательных проверок покрыто сценариями | 7 из 11 | **11 из 11** |
| причин в закрытом перечне KAN-DENY-01 | 9 | **13** |
| объявленных отступлений у читателя | не назван | **0, осознанно** |

### 13.6 Что осталось неустановленным

- **исполнимость новых сценариев на стенде не проверялась** — читателя ещё нет, и это ожидаемо:
  кода по приёмке до APPROVED не пишется. Все пять помечены `ждёт производителя` и в зачёт
  покрытия держателем не идут;
- **§1 не перемерялся заново** — рецензия перемерила его на двух ревизиях, и правка §1 в предмет
  этого круга не входила. Перепроверены только величины §1.5, которые понадобились доводу
  §13.2, — они совпали;
- **соседние задачи линии** (#2086, #2090…#2096) и замысел линии не разбирались — чужой предмет;
- **выбор между «исполнить» и «объявить отступление»** сделан автором по существу и подлежит
  вердикту рецензента: довод — отсутствие причины, которая делала бы проверку неприменимой
  **на поверхности предъявления**, а не очередь работ.

---

## 14. Ревью, круг 2 — ✅ APPROVED

> **Ревьюер:** `acceptance-reviewer` · **дата:** 2026-09-06 · **вердикт:** ✅ APPROVED.
> **Предмет круга:** ровно два блокирующих замечания §12.2 и §12.3 и три остатка, названных
> автором в §13. Объём §7, замысел §2 и порядок §8 не пересматривались — они и не менялись.
> **Ревизии рецензии:** воркспейс `lane/acc-2077-r2` @ `52bfc61a`; дерево продукта
> `release/kaname` @ `2d2b508076` (голова линии на день круга 2; документ пинит числа §1 на
> `caa48b770d`, между ними **13** коммитов).
> **Прежнее объявление шапки, замещённое этим кругом** (история добавляется, не переписывается):
> `**Статус:** CHANGES REQUESTED — acceptance-reviewer, круг 1, 2026-09-06 (ban #1). Вердикт
> круга 1 остаётся действующим: нового не выносилось, поэтому кодировать по этому документу
> по-прежнему нельзя. Оба блокирующих замечания §12 закрыты правкой — ответ автора и числа круга
> в §13; ожидается круг 2. Объём §7, замысел §2 и порядок §8 не менялись. Прежние объявления
> шапки сохранены: круга 1 — в §12, круга 2 — в §13.`

### 14.1 §12 — проверено на байтовую нетронутость, а не принято на слово

Раздел круга 1 извлечён из обеих ревизий и сличён:

```sh
for R in d06a3dc2 52bfc61a; do git show $R:<этот файл> \
  | awk '/^## 12\./{f=1} /^## 13\./{f=0} f' > /tmp/s12_$R.txt; done
diff /tmp/s12_d06a3dc2.txt /tmp/s12_52bfc61a.txt
```

Различие — **три строки границы приписки** (пустая · `---` · пустая) в самом конце; проза §12
не изменена ни в одном знаке. Вердикт круга 1 остался вердиктом о своей редакции.

**Объявление шапки автор заместил, но не присвоил:** прежний текст сохранён дословно в §13,
токен вердикта остался `CHANGES REQUESTED`, и запись прямо говорит, что вердикта она не выносит.
Это верное обращение с шапкой: приёмка одобряет себя не сама.

### 14.2 Исход проверки посылок — обе стороны названы, и МОЙ предикат ошибался дважды

**Числа автора перемерены своим предикатом, а не прочитаны из его таблицы.** Существенно, что
первые два моих предиката были неверны, и оба раза прав оказался автор:

| что мерил | мой первый ответ | почему он был неверен | итог |
|---|---|---|---|
| сценариев и строк §5 | 33 и 36 | предикат брал `^**KAN-` и `^| KAN-` по **всему** файлу; в разбор попали ссылки в прозе §12–§13 и **таблица моего же §12.3** | **31 и 31** после сужения к §4 и §5 |
| носителей, объявляющих спорные проверки | 4/4 по всем четырём | предикат читал **весь файл** носителя и считал объявлением отступление `DeclaredDeviations()` | **3/4** для типа, **4/4** для трёх остальных |

Оба раза виновата была форма моего выражения, а не дерево, — тот самый класс, который документ
цитирует в KAN-VER-09 и который `testing.md` называет «твой СОБСТВЕННЫЙ разборщик — тоже
распознаватель». Названо здесь затем, чтобы «перемерил и сошлось» не читалось как «поверил».

**Что сошлось после исправления предиката** (все — мои прогоны, не таблица автора):

| величина | заявлено | мой замер | исход |
|---|---|---|---|
| сценариев в §4 · строк в §5 | 31 · 31 | 31 · 31, **множества ID тождественны**, дублей 0 | сошлось |
| помечено `ждёт производителя` | 22 | 22 (`VER` 12 · `DUP` 1 · `REV` 4 · `DENY` 2 · `POL` 1 · `OPS` 2) | сошлось |
| причин в перечне KAN-DENY-01 | 13 | 13 | сошлось |
| покрытие единого перечня | 11 из 11 | 11 из 11 **по собственному сопоставлению** | сошлось |
| носители: тип · связь заголовка с ключом · идентификатор ключа · критические заголовки | 3 · 4 · 4 · 4 | 3 · 4 · 4 · 4 | сошлось |
| `MandatoryChecks` | 11 | 11 на `2d2b508076` | сошлось |

**Числа §1 перемерены на голове линии `2d2b508076`** — тринадцать коммитов сверх ревизии, на
которой они снимались. **Устаревших нет ни одного:** 1.1 → 0 и контроль 0/1 · 1.3 → 22/9 ·
1.5 → 4/2 · 1.6 → 1 файл · 1.7 → 0. Работа по приёмке в дереве **не начата**, что и требуется:
приёмка санкционирует её, а не описывает.

**Собственное сопоставление одиннадцати проверок со сценариями** (не таблица автора): подпись →
VER-03 · допустимость подписи → VER-06 · связь заголовка с ключом → VER-09 · издатель → VER-07 ·
адресат → VER-04 · тип → VER-08 · срок → VER-02 · момент начала → VER-05 · идентификатор ключа →
VER-10 · критические заголовки → VER-11 (+ VER-12 близнец) · отзыв → REV-01. **Одиннадцать из
одиннадцати, по одному сценарию на проверку, ни одного слияния двух проверок в один вход.**

Отдельно: тринадцать причин KAN-DENY-01 — это **ровно** множество отказных сценариев документа
(десять отказов семейства VER + отозван + недоступный авторитет + обе формы разом). Перечень
причин перестал быть короче перечня отказов; в круге 1 он был короче на четыре.

### 14.3 Пять новых сценариев способны краснеть на НЕВЕРНОЙ работе, а не только на её отсутствии

Это проверялось отдельно, потому что «ждёт производителя» у всех пяти и соблазн зачесть их
формально велик. По каждому назван мир, в котором работа **сделана, но сделана неверно**:

| сценарий | на чём краснеет, когда читатель уже написан |
|---|---|
| KAN-VER-08 | читатель сверяет тип, но **отсутствие** типа читает как «любой» — второе «И» этого не пропускает |
| KAN-VER-09 | способ проверки подписи выбирается **по заголовку**, а не по найденному ключу (классическая подмена алгоритма) |
| KAN-VER-10 | форма идентификатора не ограничена до поиска, обновления снимка и журнала; либо «мусор» и «не наш» различимы между собой |
| KAN-VER-11 | помеченный обязательным параметр **игнорируется** вместо отказа целиком; либо перечень исполняемых не объявлен |
| KAN-VER-12 | читатель отвергает **любой** незнакомый параметр — то есть ужесточение KAN-VER-11 без его второй половины |

Ни один из пяти не проходит тождественно на мире, где работа сделана: у каждого назван вход,
отличающийся от годного **ровно одним** фактом, и исход, который неверная реализация даёт другим.
KAN-VER-12 — положительный близнец, без которого KAN-VER-11 зеленел бы на чрезмерно строгом
читателе.

**KAN-VER-03 сужен верно:** прежняя редакция меняла два факта разом (разрешение идентификатора и
сходимость подписи) и потому перекрывалась с новым KAN-VER-10. Теперь идентификатор резолвится, а
не сходится подпись — один факт, и два сценария разведены.

**KAN-REV-04 закрывает §12.3 полностью.** Утверждение стало решительным («вызов **доходит**»),
добавлено требование управляемых часов. Проверено рассуждением, ради которого замечание и
писалось: читатель без кеша отвергнет вызов сразу после записи отзыва — то есть **краснеет** там,
где прежняя формулировка была истинна при любом исходе.

### 14.4 Не блокирующее — одно, правится в посадочном коммите

**§7 п. 1 перечисляет девять проверок, а §4 покрывает одиннадцать.** Идентификатор ключа
(KAN-VER-10) и критические заголовки (KAN-VER-11/12) в перечне объёма поимённо не названы.

**Почему это НЕ блокирует, и различие принципиальное.** Блокирующие круга 1 были дефектами
**необнаружимости**: ни один сценарий не мог покраснеть — ни на непокрытых проверках, ни на
отсутствующем кеше. Здесь наоборот: реализация, построенная буквально по §7 п. 1, роняет
KAN-VER-11, KAN-VER-12 и KAN-POL-01 — то есть ошибка **обнаруживается до посадки** собственными
средствами документа. Сверх того §7 п. 6 санкционирует пробу согласия с единым перечнем, а
согласие двустороннее, поэтому объём обязывает ко всем одиннадцати и по существу. Довод автора
(§13.2) проверен и принят.

Остаётся расхождение **перечислений**, а не требований: два места об одном предмете, из которых
одно короче. Дописать два имени в §7 п. 1 — одна строка, и делать это отдельным кругом дороже,
чем сделать при посадке.

### 14.5 Три остатка, названных автором, — судимы прямо

1. **KAN-REV-03 выполним на читателе без кеша — ПРИНЯТО.** Довод автора верен: предмет
   KAN-REV-03 — fail-closed при недостижимом авторитете, а не наличие кеша. Читатель,
   засчитывающий неполученный ответ за «не отозван», этот сценарий **роняет**, значит
   относительно своего предмета он не вакуумен. Требовать от него ещё и падения на отсутствии
   кеша значило бы вернуть двухфактность, из-за которой правился KAN-VER-03. Моё замечание
   круга 1 гласило, что **ни один** сценарий не ловит отсутствие кеша; теперь его ловит
   KAN-REV-04, и этого довольно — детектор обязан существовать, а не быть у каждого.
2. **«Исполнить» против «объявить отступление» — ПРИНЯТО, и это единственный верный исход.**
   Проверено замером, а не согласием: три из четырёх спорных проверок объявляют **все четыре**
   носителя перечня и не отступает никто; по типу токена отступает ровно один — авторитет
   отзыва, — и его причина **указывает на поверхность предъявления**, то есть ровно на
   заводимого читателя. Объявить отступление здесь значило бы, что тип не сверяет **никто**:
   издатель отказался от него в пользу поверхности, а поверхность — в пользу издателя.
3. **§1.6 намеренно не правился — ПРИНЯТО.** Замечание было не блокирующим (§12.4 п. 3), правка
   §1 заданием круга запрещена, и это верно по существу: числа §1 пришпилены к названной
   ревизии, и правка прозы рядом с ними рассинхронизировала бы запись замера. Наблюдение
   использовано там, где оно работает, — доводом внутри KAN-VER-09. Это лучше, чем было
   предложено.

### 14.6 Ось публикуемости — ПРОЙДЕНА повторно по изменившимся разделам

Прогнана заново, а не унаследована: документ прибавил пять сценариев и четыре причины отказа, а
перечень причин неразличимого отказа — самое опасное место этого текста.

- **Перечень тринадцати причин — требование, а не разглашение.** Он предписывает
  **неразличимость**, то есть просит систему сообщать меньше, а не больше. Состав проверок
  токена и без того объявлен в общем перечне платформы, публичном по построению.
- **Основание довода перепроверено на новой ревизии и держится:** работа не начата, читателя на
  публичной цепочке нет, назваться нечем — предмет остаётся **отсутствующей возможностью**, и
  дефект даёт лишний отказ, а не лишнее разрешение.
- **Отдельно проверено самое рискованное из нового** — требование KAN-VER-10 ограничивать форму
  идентификатора **до** поиска, обновления снимка и журнала. Проверено, что оно **не указывает на
  живой пробел**: обе поверхности продукта, читающие токен сегодня, такие ограничители несут, а
  их величины живут в общем перечне платформы. Требование адресовано коду, которого ещё нет, и
  переиспользует уже существующий механизм — что и заявляет §13.2.
- **Сборки «координата + условие, при котором защита не действовала + следствие для
  постороннего» в новых разделах нет.** Все пять сценариев сформулированы как требования к
  поведению, ни один не описывает, что сегодня проходит какую поверхность.

Тем же требованием связан текст этой записи.

### 14.7 Что остаётся неустановленным

- **истинность объявленного вердикта машинно не проверяется** — распознаватель `docs-gate` судит
  объявление в шапке, а не одобрение; это его известный предел;
- **«звеньев восемь» (§1.2)** по-прежнему установлено чтением композиционного корня: исполнимого
  предиката у числа звеньев цепочки в дереве нет;
- **замысел линии** (`docs/changes/standalone-iam/design.md`) не рецензировался — вне предмета;
- **соседние задачи линии** (#2086, #2090…#2096) не разбирались — чужой предмет;
- **способность пяти новых сценариев краснеть** установлена разбором их формулировок, а не
  прогоном: производителя у них ещё нет by construction. Прогон — предмет шага 3 §8.

### 14.8 Что дальше

Запрет #1 снят: `class-exposure-analyst` по этому документу, далее порядок §8 без изменений —
стражи провязки (KAN-WIRE-01/02) **первыми**, затем поведенческие пробы VER/DUP/DENY красными.
При посадке дописать два имени в §7 п. 1 (§14.4). Разблокируются #1109 → #1111.

---

## 15. Редакция 2 — что пересматривается, чем это измерено (задача #2191, P0)

Раздел добавлен, а не вписан поверх: записи ревью §12, §13 и §14 **не тронуты ни байтом** —
история ревью добавляется (`change-graph.md` §2). Здесь стоит только то, что редакция 2
**отзывает, заводит и измеряет**; сами требования живут в §2, §4, §7, §8 — второго места об одном
предмете не заводится.

### 15.0 Почему это РЕДАКЦИЯ, а не вторая приёмка

Предмет #2191 — тот же механизм, что и #2077: чтение предъявленного удостоверения на собственной
публичной поверхности. Вторая приёмка о нём была бы **двумя местами об одном предмете**, и
разошлись бы они молча — прежде всего на оси 8, которую #2100 требует переписать именно здесь
(его предикат: «ось 8 в §2 приёмки переписана; сценарий „обе формы разом" **снят вместе с
предметом**, а не осиротел; приёмка получает **новый круг рецензии**»).

Поэтому: правится этот документ, вердикт круга 2 **не переносится** на новую редакцию (он о
прежнем отпечатке, названном в шапке), и документ уходит на круг 3.

### 15.1 Ревизия и единицы счёта

Все числа этого раздела сняты на монорепо `release/kaname` @ `fe0e5a60f3` и воркспейсе `main` @
`98e0a01a`, замер 2026-09-07. **Числа §1 не перемерялись и не переписывались**: они верны на
своей ревизии (`caa48b770d`), и замена их сегодняшними стёрла бы основание, из которого выведена
редакция 1. Где сегодняшнее число расходится с числом задачи, названы **оба** и названа единица.

### 15.2 ОТЗЫВАЕТСЯ ось 8 редакции 1 — она была миной, а не требованием

**Что было записано** (редакция 1, отпечаток `e1e6d504…`, §2 ось 8 и §4 семейство DUP, сценарий
KAN-DUP-01) — цитата дословная:

> **8. обе формы разом — отказ** | предъявленный токен **и** переданная личность в одном запросе →
> fail-closed | неоднозначность о том, **кто звонит**, догадкой не разрешается

> **And** ни одна из двух личностей не выбрана — ни «предъявленная главнее», ни «переданная
> главнее»

**Что с этим не так.** Требование верно как принцип и неисполнимо как проверка: редакция 1 §1 не
измеряла, **бывает ли** такое сочетание в нашей же сборке. Бывает, и всегда — край, установив
личность вызывающего, **добавляет** её к входящим метаданным, а удостоверение в них уцелевает.
Значит всякий запрос, проксированный краем, несёт обе формы, и ось 8 отвергала бы его.

**Гипотеза «достаточно объявить ручку и включить» — ПРОВЕРЕНА И ОТВЕРГНУТА.** Это не рассуждение:
включение читателя при действующей оси 8 даёт отказ **каждому** запросу через край. Величина
сегодня зелёного, что при этом покраснело бы, — в §15.5.

**Что записано вместо** — ось 8 редакции 2: полосы делаются **взаимоисключающими построением**.
Край снимает арендаторское удостоверение перед пересылкой за себя, установив личность сам; за
краем действует переданная личность, напрямую — предъявленная. Сочетания не бывает, поэтому
сценарий о нём **снят вместе с предметом** (ban #11, исход 2), а не оставлен без производителя и
не ослаблён.

**Довод в пользу построения, а не проверки** — корпусный и не изобретён здесь: проверку можно
обойти, построение — нет. Побочно закрывается ещё одна ось: доверенному отправителю нельзя
подменить переданную личность подсунутым удостоверением, потому что подсунутое до слушателя не
доезжает.

**Что редакция 2 отзывает у #2100.** #2100 предлагал другую форму — сужение полосы по признаку
«пир не вправе передавать личность» — и отверг снятие на крае доводом: «удостоверение на крае
читают другие потребители». Довод **измерен** (§15.3) и оказался **о другом предмете**: на самом
крае удостоверение читают, и снятие их не трогает — оно происходит **после** чтения, перед
пересылкой. За краем читатель ровно один. Основание отказа не подтвердилось.

**Обе формы остаются защитимыми, и выбор между ними — владельца** (§11): §2 оставляет форму
проверки владельцу линии, и редакция 2 этого не отменяет. Она предъявляет замер, которого у
#2100 не было, и предлагает решение; она его не присваивает.

### 15.3 Радиус части 1 — и слепота первого предиката, названная прямо

**Вопрос:** кто прочитает удостоверение, если край перестанет его пересылать.

**Первый предикат был слеп.** Поиск по строчной форме имени в не-тестовом Go дал **3** файла.
Поиск, знающий **обе** законные формы записи — строчную и заглавную, — дал **11**. Разница не
край и не редкость: заглавная форма столь же законна и в этом дереве обычна. Слепой предикат не
дал бы ни красного, ни зелёного — он **промолчал** бы о восьми файлах, и радиус снятия был бы
взят наугад.

**Адъюдикация одиннадцати — по одному, потому что число само по себе ответа не даёт:**

| группа | сколько | относится ли к предмету |
|---|---:|---|
| **пишут исходящее** (ставят заголовок в свой запрос к соседу) | 3 | нет — не читатели |
| **читают на самом крае** | 3 | нет — читают **до** пересылки; снятие их не трогает |
| **перечень «не писать в журнал»** в общем транспорте | 1 | нет — маркер, а не читатель |
| **читают на СОБСТВЕННОЙ поверхности, которую край не проксирует** | 3 | нет — продолжают читать сами |
| **читают на поверхности, до которой край проксирует запрос** | **1** | **да — механизм этой приёмки** |

**Замер задачи #2191 подтверждён**, но её формулировка «остальным семи службам оно не нужно»
требует уточнения, и без него снятие выглядело бы ломающим: у двух служб есть **собственные**
поверхности, читающие удостоверение, — они просто не за краем. Соседние службы платформы
(сеть, вычисление, хранение, балансировка, размещение) не читают его **вовсе**: по обеим формам
имени — **0**.

**Прецедент формы в дереве есть, и он не наш придуманный.** Другая служба продукта **уже** снимает
удостоверение перед проксированием за себя, и её собственный комментарий несёт ровно тот довод,
который редакция 2 применяет здесь: доступ уже разрешён выше по пути, за проксированием
удостоверение не нужно, а осевшее в чужих журналах — расширяет поверхность. Механизм снятия
заголовков перед пересылкой на самом крае тоже существует и работает. Значит Ф1 — **применение
живущей в дереве формы к ещё одному заголовку**, а не новая конструкция.

### 15.4 Антецедент стража — ПОДНЯТЫЙ ФРОНТ, а не объявленная посадка

Страж старта уже связывает «посадка объявлена своей ⇒ читатель включён», и требование выражено
таблицей, а не цепочкой условий. **Дыру это не закрывает**, и различие здесь несущее:

- посадку «своя» **не объявляет ни один** профиль развёртывания — соответствующее значение в
  объявлениях чарта встречается **0** раз;
- собственный **публичный фронт** при этом поднимается **на любой** посадке: его поднимает
  объявленный адрес, а не выбор посадки;
- поля адреса фронта **не читает ни одна** из трёх проверок стража — сегодня в них **0**
  вхождений против одного вхождения ручки читателя.

Итог: связывание существует, и его антецедент никогда не наступает; следствие никогда не
требуется; фронт поднят, читателя нет — и всё выглядит настроенным. Поэтому редакция 2 меняет
**антецедент**, а не заводит второй страж: два стража об одном предмете разошлись бы молча.

**И заводит вторую половину** — KAN-BIND-03: фронт не поднят ⇒ требования нет. Без неё связывание
отвергало бы законную посадку, то есть требовало бы того, чем не пользуются; предмет стража —
**противоречие**, а не наличие ручки.

**Ручка читателя в объявлениях чарта отсутствует, и предикат прогнан в обе стороны:** искомой
ручки — **0** вхождений, соседней ручки того же блока — **2** (значение по умолчанию и подстановка
в шаблон настроек). Односторонний предикат здесь не годится: «0» без второй величины неотличимо
от «предикат не нашёл ничего вообще».

### 15.5 Числа задачи, НЕ подтвердившиеся при перемере

| Утверждение #2191 | Перемер | Вердикт |
|---|---|---|
| ручки читателя в объявлениях чарта **0**, соседней **2** | 0 и 2 | **подтверждено** тем же предикатом и моей единицей счёта |
| за краем удостоверение читает **ровно один** потребитель | 1 | **подтверждено**, но единица счёта требует уточнения: без неё наивный предикат даёт 3 либо 6 (§15.3) |
| «остальным **семи** службам оно не нужно» | у пяти — не нужно (0 вхождений), у двух есть **свои** поверхности, читающие его сами | **уточнено**: «не нужно за краем» ≠ «не читают нигде» |
| включение отвергло бы «около **4700**» сегодня зелёных утверждений одного шарда | #2188 называет **4658** | **разошлось на округление.** И то и другое — **счёт событий прогона**, а не замер дерева: своим предикатом я его не воспроизвожу и здесь не подтверждаю. Величину даёт KAN-REG-01 прогоном |
| «34 упавших утверждения и 17 упавших скриптов» | из #2188; там же сказано, что одна запись из 51 — **дефект пробы**, а не продукта | **уточнено**: корень A даёт 50 записей из 51, не все 51 |

**Ни одно расхождение не меняет решения** — все три уточнения делают его уже и точнее. Но
названы они прямо: число, переданное дальше без перемера, становится «фактом», за который никто
не отвечает.

### 15.6 Что осталось НЕУСТАНОВЛЕННЫМ

- **стенд не поднимался.** О поведении на кластере вердикта здесь нет — это «не выполнилось», а
  не зелёное. Предикат снятия #2191 требует **прогона на развёрнутой посадке**, и рендер его не
  заменяет (§8, Ф2 шаг 6);
- **величина регрессии не воспроизведена мной** — см. §15.5, строка про 4658;
- **«звеньев восемь» (§1.2)** остаётся установленным чтением, как и в круге 2: исполнимого
  предиката у числа звеньев цепочки в дереве нет, и редакция 2 его не завела;
- **число поверхностей службы (шесть)** установлено чтением композиционного корня, а не
  предикатом; от него зависит граница переписи KAN-SOLE-01, и потому оно вынесено вопросом
  владельцу (§11, вопрос 3);
- **способность одиннадцати новых сценариев краснеть** установлена разбором формулировок, а не
  прогоном: производителя у большинства ещё нет by construction. Прогон — предмет §8.

---

## 16. Круг 3 — ❌ CHANGES REQUESTED и ответ автора

Запись добавлена соседней: §12, §13, §14 и §15 не тронуты. Вердикт круга 3 —
**CHANGES REQUESTED**, блокирующее **одно**; все пять посылок задания рецензент подтвердил
собственным замером, трижды ошибившись своей единицей счёта и каждый раз назвав это сам.

### 16.1 БЛОКИРУЮЩЕЕ — раздел объёма санкционировал снятое. Закрыто

**Замечание.** Пункт 3 §7 дословно требовал «отказ обеих форм разом — предъявленный токен вместе
с переданной личностью», тогда как ось 8 редакции 2 говорит: сочетания не бывает. Три довода, и
каждый самостоятельно достаточен: (а) §7 — это то, что **одобрение разрешает делать**, и по
разбиению фаз пункт попадал в **Ф0**; (б) два места об одном предмете, и неверное стояло там, где
определяется объём; (в) у пункта не осталось сценария — обратная сторона правила «у каждого
сценария есть производитель».

**Исход — снят вместе с предметом** (ban #11, исход 2), тем же способом, что и сценарий. Пункты
перенумерованы, ссылки поправлены переписью (§16.2). Обратная сторона заведена критерием
готовности: §9 п. 8.

**Класс, который редакция 2 закрыла не до конца, и метод, которым он ловится.** Правка редакции 2
шла **по слову «DUP»** и закрыла **11 мест из 13** — все верно. Уцелело ровно то, что этого слова
**не содержит**: пункт объёма говорил «обеих форм разом». Первый предикат рецензента был слеп так
же и остатка не увидел.

Нашла **перепись по разделам, обеими формулировками**. Мой прогон её, единица — **строка-попадание**
(не вхождение и не документ; у рецензента единица была своя, и числа поэтому разные — оба верны):

| формулировка | что ищется |
|---|---|
| `DUP` | имя семейства |
| `обе/обеих/двух форм(ы)` | предмет, названный по существу |
| `разом` | предмет, названный наречием |
| `две личности` · `главнее` | предмет, названный через исход |

**Число неотделимо от набора форм, которым оно получено, и потому называется вместе с ним.** Этими
четырьмя формами строк-попаданий **32**; они разбираются **по одному**, потому что число само по
себе ответа не даёт:

| разряд | сколько | что это |
|---|---:|---|
| законные надгробия | **13** | говорят о снятии прямо, в §3, §4, §5, §8, §9 |
| ложные попадания моего разборщика | **3** | о другом предмете: «два факта разом» у KAN-VER-03 и «обе формы **записи имени**» ×2 (STRIP-03, §11) |
| записи ревью §12–14 | **6** | правке не подлежат |
| §15 — запись редакции 2 | **7** | разбор отзыва, законен целиком |
| **остатки** | **3** | пункт объёма §7 + **две строки живого сценария** KAN-WIRE-02 |

Остатки закрыты все три. Две из них — «Дано» и примечание KAN-WIRE-02, называвшие снятое
семейство при том, что соседний раздел о снятии говорил.

**Рецензент круга 4 насчитал в §15 восемь, а не семь, — и он прав.** Расхождение разрешено не
выбором правдоподобного, а проверкой **своего** разборщика: единица счёта у нас одна (строка-
попадание) и арифметика сходится у обоих; расходится **набор форм**. Предмет назван в документе и
**пятой** формулировкой — «неоднозначность о том, кто звонит», — которой мои четыре не знали. Она
стоит второй строкой дословной цитаты оси 8 в §15, и построчный разбор видит её, только если эта
форма в наборе есть.

С пятой формой перепись даёт **35**, и §15 становится **8**. Прибавившиеся три попадания — все
законные и ложатся в три **разных** разряда: надгробие в §4 (13 → **14**), вторая строка цитаты в
§15 (7 → **8**) и **живая ось 8 редакции 2** в §2 — ей разряда в таблице выше нет вовсе, потому
что она не надгробие и не остаток, а действующее требование, называющее предмет по-новому. Это
**шестой** разряд, и он равен **1**. **Остатков не прибавилось: их по-прежнему три, и все три
закрыты.** Шире набор — точнее перепись, и вывод от неё не изменился.

> **Мой разборщик был слеп ЗДЕСЬ ЖЕ, и это стоит записать, а не спрятать.** Первое разбиение,
> которое я вписал в этот раздел, называло «4 ложных» и «12 записей ревью» — оба числа неверны;
> верные — 3 и 6, а седьмой разряд (§15) я не выделил вовсе. Перепроверка нашла и вторую слепоту:
> набор форм **без** «обе формы» даёт **29** вместо 32 — недобор ровно на той форме, которой
> разборщик не знал, и недобор **молчаливый**.
>
> **Третье срабатывание — уже от рецензента круга 4**, и оно записано здесь же: разряд §15
> недосчитан, потому что набор форм был у́же предмета.
>
> То есть класс, о котором этот раздел написан, сработал на самом разделе **трижды**: пропущен
> предмет, названный чужой формулировкой (пункт объёма); перепутаны разряды собственной переписи
> (4 и 12 вместо 3 и 6); недосчитан разряд из-за узкого набора форм. **Ни одно из трёх не нашлось
> перечитыванием — каждое нашлось несовпадением с чужим числом.** Это и есть рабочий детектор.
>
> Противоядие то же и другого нет: **прогнать предикат ещё раз, более широким набором форм, и
> назвать набор рядом с числом** — иначе следующий сверщик получит другое число и будет прав.
> Суммы сходятся под обоими наборами: четырьмя формами 13 + 3 + 6 + 7 + 3 = **32**, пятью —
> 14 + 3 + 6 + 8 + 3 + **1** = **35**, где шестое слагаемое — живая ось 8 в §2, которой в
> четырёхформенной таблице разряда не было.
>
> **Четвёртое срабатывание — моё собственное, и оно поймано сложением.** Первую редакцию этого
> абзаца я написал как «14 + 3 + 6 + 8 + 3 = 35», не заметив, что сумма даёт **34**: разряд под
> третье прибавившееся попадание я не завёл. Сложить пять чисел — секунда, и она отделяет
> «перепись сошлась» от «перепись объявлена сошедшейся». **Сверяй сумму разрядов с итогом
> руками**, а не глазами — особенно в абзаце, который учит именно этому.

**Урок в одну строку, и он о методе, а не об этом документе:** сущность живёт в документе графой
таблицы, строкой перечня, пунктом объёма и прозой врезки — и **в каждом месте называется
по-своему**. Поиск по имени сущности находит её там, где она названа именем. Перемеряй по
**разделам**, а не по слову.

### 16.2 Нумерация пунктов §7 — соответствие названо, а не оставлено на догадку

Пунктов было 12, стало **11**. Соответствие для читателя, который придёт с прежним номером:

| было | стало | | было | стало |
|---:|---:|---|---:|---:|
| 1 | 1 | | 7 | 6 |
| 2 | 2 | | 8 | 7 |
| **3** | **снят** | | 9 | 8 |
| 4 | 3 | | 10 | 9 |
| 5 | 4 | | 11 | 10 |
| 6 | 5 | | 12 | 11 |

**Ссылки правлены переписью, а не поиском по фразе.** Единица — вхождение вида `§7 п(п). N`,
включая диапазоны. Живых ссылок **12**, поправлено **9** (три указывали на пункт 1 и на диапазон,
начинающийся с него, и не сдвинулись). Контроль после правки: ни одна живая ссылка не выходит за
диапазон 1…11.

**Записи ревью §12–14 ссылаются на ПРЕЖНЮЮ нумерацию и не правятся** — история ревью добавляется,
а не переписывается (`change-graph.md` §2). В них встречаются номера **1, 6, 7**; по таблице выше
это нынешние **1, 5, 6**. Сказано здесь прямо, потому что молчание сделало бы их тихо ложными —
ровно тот класс, который этот документ ловит в других местах.

### 16.3 Не блокирующее из того же круга — закрыто в этом же заходе

- **два места живого сценария называли снятое семейство** — закрыто (§16.1, остатки);
- **умолчание включения читателя оставалось на догадку** — зафиксировано строкой в примечании
  KAN-LAND-02: читатель включается **только** профилем чужого облака, профиль платформы его не
  включает. Вместе с умолчанием назван **предикат пересмотра**: утвердительный ответ владельца на
  вопрос 2 §11 делает положительного близнеца неисполнимым by construction, и тогда KAN-LAND-02
  **переписывается**, а не оставляется зеленеть на вырожденной паре.

### 16.4 Что рецензент признал не подлежащим переделке — и я не трогал

Отзыв решения (прежнее процитировано дословно, названо чем опровергнуто, опровергнута гипотеза, а
не человек); раздел не подтвердившихся чисел, назвавший три штуки, включая моё собственное
расхождение; разведение отказов **по адресатам** — различимость владельцу и неразличимость
предъявителю как два требования, а не компромисс; порядок фаз, записанный требованием с
обоснованием следствием; три вопроса владельцу §11 — форма выноса вопроса, а не откладывания:
у каждого назван уже сделанный замер и чего именно не хватает.

### 16.5 Что осталось неустановленным после круга 3

Остатки §15.6 в силе и не сократились: стенд не поднимался, величина регрессии мной не
воспроизведена, число звеньев цепочки и число поверхностей установлены чтением, способность новых
сценариев краснеть — разбором формулировок. Круг 3 к ним ничего не добавил и ничего не снял: его
предмет был **внутренняя согласованность документа**, а не дерево.

---

## 17. Круг 4 — ✅ APPROVED, три правки внесены в тот же коммит

Запись добавлена соседней. **§12–§15 не тронуты — 669 строк побайтово, сверено.** §16 правлен
намеренно и по прямому указанию круга 4 (правка 2 требовала исправить число **в нём**): изменено
45 строк, все в §16.1, разряды и суммы переписаны. Это не нарушает «историю ревью не
переписывают»: §16 — запись **моего** ответа автора, а не вердикта рецензента, и правил её тот же
автор по замечанию, названному вслух. Записи вердиктов §12 и §14 не тронуты ни байтом.

Вердикт круга 4 — **APPROVED**: блокирующее круга 3 снято по существу. Три замечания внесены без
отдельного круга.

> **В первой редакции этого абзаца стояло «§12–§16 не тронуты» — утверждение было ложным**, и
> ложным ровно в том разряде, который сам же документ учит проверять: §16 я правил десятью
> минутами раньше. Поймано не перечитыванием, а **сравнением с предыдущим коммитом** — тем же
> способом, каким ловились три предыдущих срабатывания класса (§16.1). Пятое за две редакции;
> записано, потому что «сверено» без вывода сверки есть заявление, а не факт.

**Что рецензент подтвердил замером, а не прочтением:** пунктов объёма ровно 11 и отображение
«пункт → сценарий» сверено **в обе стороны** поимённо, пустых нет; живых ссылок на номера
двенадцать, и сверен **смысл** каждой против новой нумерации, а не попадание в диапазон; записи
прежних кругов побайтово нетронуты — сравнение даёт чистое добавление трёх строк-разделителей при
667 идентичных на его ревизии. Перепись §16.1 он воспроизвёл на дофиксовой ревизии **более широким** набором
форм: 48 попаданий, 22 в живых разделах, разобраны по одному — **остатков ноль**, а три ложных
попадания оказались ровно теми, что названы мной.

### 17.1 Зазор между тремя осями — закрыт, и он важнее самой правки

**Замечание.** KAN-FWD-03 требовал работы, которой объём не санкционировал: состояние
«наполовину», хвост строки прямо говорит «заводится этим переходом», а пункта объёма нет и шага в
фазах нет. Критерий 8 круга 3 его не ловил **by construction**.

**Причина — осей три, а не две:** производитель · **держатель** · санкция. Критерий 1 связывает
сценарий с производителем, критерий 8 — пункт объёма со сценарием; связи «сценарий, чей
**держатель** заводится этим переходом → пункт объёма» не устанавливал **ни один**, и сценарий
проваливался между ними.

**Закрыто двумя правками сразу**, потому что одной мало: критерий 8 получил **второе
предложение** (обратная связь по держателю), а пункт 5 §7 — недостающую санкцию. Санкция дописана
**внутрь** пункта, а не заведена двенадцатым: перенумерация обесценила бы таблицу §16.2 и сделала
бы тихо ложными ссылки записей ревью, правке не подлежащих.

**Это зазор, а не регрессия правки:** строка KAN-FWD-03 предшествует редакции 2 и была одобрена
кругом 2. Рецензент назвал это прямо и потому не блокировал.

### 17.2 Разряд переписи недосчитан — исправлено НАБОРОМ ФОРМ, а не цифрой

Рецензент насчитал в §15 восемь вместо семи. **Он прав, и причина не в арифметике, а в наборе
форм:** предмет назван в документе пятой формулировкой, которой мои четыре не знали. Правка
внесена в §16.1 вместе с обеими суммами и с шестым разрядом, которого в четырёхформенной таблице
не было.

**По дороге я поймал себя четвёртый раз тем же классом:** первая редакция исправляющего абзаца
объявляла сумму сошедшейся, а пять слагаемых давали **34** при итоге 35. Записано там же —
сложить пять чисел руками стоит секунды и отделяет «перепись сошлась» от «перепись объявлена
сошедшейся».

### 17.3 Отображение «пункт → сценарий» — названы все, кого не хватало

В сокращённом отображении круга 3 отсутствовали **три** имени, а не одно: **FWD-03** (предмет
§17.1), **DENY-02** (положительный близнец, покрытый пунктами 1 и 2) и **POL-02** (закрывается
читателем, пункт 1, и наблюдается переписью). Отображение переписано таблицей и сверено в обе
стороны; три имени выделены, чтобы следующий сверщик видел, что они добавлены осознанно.

### 17.4 Что рецензент проверил сверх заданного

Закрытый перечень причин отказа — двенадцать, и арифметика сходится с объявленным; сценарии и
строки состояния тождественны побайтово; ось публикуемости пройдена — сборки «координата плюс
условие плюс следствие» в новых разделах нет, предмет остаётся **отсутствующей возможностью**.

### 17.5 Что остаётся неустановленным — и не выдавать это за большее

**Стенд не поднимался**, поэтому о поведении на кластере вердикта нет: это «не выполнилось», а не
зелёное. Два числа — звенья публичной цепочки и поверхности службы — установлены **чтением**, а
не предикатом. Предмет кругов 3 и 4 был **внутренняя согласованность документа**, а не дерево
целиком: APPROVED говорит, что документ можно исполнять, и не говорит, что дерево уже таково.

---

## 18. Разбор классов риска по одобренному документу — четыре внесения

Записи §12–§17 не тронуты. Разбор шёл **по одобренному** документу, до первой строки исполнения,
и вернул три правки текста и один вопрос владельцу. Правки внесены; вопрос задаёт координатор
линии, не автор.

### 18.1 Ф0 уже посажена — документ объявлял её ожидающей производителя

**Замечание.** Читатель существует, провязан в публичной цепочке, несёт настройку, стража,
измеритель, харнесс и пробы. Оговорка «числа сняты на своей ревизии» формально защищает, но
колонка «Состояние» читается как утверждение о **сегодня**, и исполнитель, идущий по фазам
буквально, начал бы переписывать существующее.

**Внесено** врезкой в §5, с предикатами и с **измеренным остатком**: файлов механизма 11 (из них
не-тестовых 5), провязка — 5 вхождений; недостают три пункта — профиль посадки `own` (**0**
профилей), замок семантики пустого круга (держатель KAN-FWD-03 — проба происхождения круга, не
замок) и различимость причины владельцу (измеритель различает **исход**, а не причину: закрытый
набор — три значения, меток причины нет). Порядок §8 внутри Ф0 читается как порядок **добора**.

### 18.2 Живая ветка отзванного решения — санкция и ПОРЯДОК

**Замечание.** Ось 8 отозвала отказ «обе формы разом», сценарий снят вместе с предметом, пункт
объёма снят кругом 3 — а **код в дереве жив**. Тот же зазор трёх осей, что круг 4 закрыл для
KAN-FWD-03, только по оси «предмет, подлежащий **снятию**».

**Внесено пунктом 8б §7 и шагом 3а фазы Ф1 — вместе с порядком, а не просто разрешением.**
Парадокс назван в документе: временный запрет «читателя не включать до Ф1» держится **сегодня
именно этой веткой** — она даёт громкий отказ вместо молчаливого выбора одной из двух личностей.
Оба обратных порядка неверны, и по-разному; худший — снять ветку раньше снятия на крае: это не
отказ, а тихая догадка. Шаги 4 и 5 Ф1 — **одно изменение**.

### 18.3 Off-by-one от перенумерации круга 3

«Пункт 9 снимает пересылку» — пересылку снимает пункт **8**, девятый это ручка. Исправлено.
Мелочь по объёму правки и **не мелочь по существу**: строка стоит в разделе «чего в объём не
входит», то есть читается как граница санкции.

### 18.4 Два требования к Ф1 и Ф2, которых в документе не было

- **место снятия** (§7 п. 8а): край строит исходящие метаданные не в одном месте — общий узел
  зовётся **4** раза, и **3** конструкции живут помимо него. Снятие — в узле; по каждой
  конструкции вне его принимается явное решение, а **перечень называется в коде рядом со
  снятием**, потому что документ читают раз, а код правят постоянно;
- **читатель поля адреса фронта** (§7 п. 10а): существующее связывание мертво **по антецеденту**
  — требует читателя при посадке, которую не выбирает ни один профиль. Смена антецедента на
  «поднят публичный фронт» верна, но поле адреса фронта сегодня не читает ни одна проверка
  посадки: связывание, написанное на невидимом страж поле, зелено при любом входе. Порядок —
  читатель поля, затем связывание, затем проба.

### 18.5 Что разбор подтвердил, а не поправил

Числа по потребителям удостоверения (11 против 3 при одной форме записи имени), отсутствие ручки
в объявлениях чарта (0 против 2 у соседней), невыбранность посадки `own` (0). Разбор назвал и
**собственную** ошибку того же рода: его поиск по слову дал два файла, и оба оказались прозой в
комментарии, а не ручкой. Класс, которым этот документ занят с круга 3, сработал ещё раз — и
снова был пойман не перечитыванием, а сверкой с чужим числом.

### 18.6 Вопрос владельцу — и граница санкции, которая из него следует

Форма взаимоисключения (§11, вопрос 1) остаётся за владельцем; вопрос задаёт координатор линии.
Из этого следует граница, внесённая в §11 врезкой: **Ф0 исполнима сейчас, Ф1 и Ф2 ждут ответа**.
Ответ «сужение по доверию к пиру» переписывает ось 8, §7 п. 8 и всё семейство STRIP; ответ
«снятие на крае» оставляет их как есть. Остаток Ф0 от ответа не зависит.

---

## 19. Круг 5 — ❌ CHANGES REQUESTED, редакция 3

Записи §12–§17 не тронуты; в §18 правлена **одна строка** — координата шага, переиндексованного
этим же кругом (§19.7). Все замечания круга — **в дельте** редакции 2→3; одобренного кругом 4
ни одно не касается, и переоценке оно не подвергалось.

> **Здесь стояло «§12–§18 не тронуты» — шире факта ровно на ту строку, которую §19.7 и называет.**
> Два места об одном предмете в одном разделе, и верным было дальнее. Поправлено кругом 6.

> **База ревью бралась из записи вердикта, а не из памяти.** Координатор назвал рецензенту
> `d205d342` — это круг **3**; кругом 4 одобрен `f77d062d`. Рецензент поймал подмену сам и
> ревьюил истинную дельту (170 вставок вместо 323), иначе завёл бы блокирующие на уже одобренном.
> Норма отсюда: база берётся из **записи вердикта**, и она есть отпечаток, а не воспоминание.

### 19.1 Б1 — врезка утверждала об остатке шире, чем мерил её предикат

Врезка §5 кончалась словами «писать заново нечего, **кроме этих трёх**», а её предикат мерит
**файлы механизма** и о держателях сценариев не говорит ничего. Следствие двустороннее и оба
конца плохи: идущий по **колонке** пишет существующие пробы, идущий по **врезке** не пишет
недостающие.

**Внесено:** граница врезки названа прямо («о держателях не утверждает ничего»); сказано, что
**колонка §5 редакцией 3 не перемерялась**; добавлен отдельный замер держателей с **названной
единицей** — 9 сценариев названы пробами, из них **8** стоят «не начат», и это объявлено **нижней
границей**. Числа рецензента (24 пробы, 11 строк) моей единицей **не воспроизводятся**; названы
обе, расхождение не заглажено выбором правдоподобного.

### 19.2 Б2 — отсутствие приписано ПРОИЗВОДИТЕЛЮ, а нет ДЕРЖАТЕЛЯ

Обе половины предиката верны — измеритель знает исход, а не причину, — **а вывод из них не
следует**: сценарий требует причину «своим значением измерителя **либо** своей записью журнала», и
журнал в дереве есть, причину несёт, поле подписано. Автор измерителя выбрал ось исхода
**сознательно** и объяснил это годдоком.

То есть врезка направляла работу на производителя, который **существует**. Недостаёт **пробы**.
Это тот же зазор трёх осей, что §17.1 закрыл для KAN-FWD-03, — **повторённый мной в разделе,
написанном сразу после**. Строка таблицы переписана: ось названа явно, и у каждой строки она
теперь стоит отдельной колонкой.

### 19.3 Б3 — врезка стояла ВНУТРИ таблицы

Пустая строка закрывает таблицу, поэтому вопросы 2 и 3 владельцу переставали быть её строками и
затягивались в цитату: колонок «что установлено» и «чего не хватает» у них не оставалось ни при
каком прочтении. Врезка вынесена **за** таблицу; строк в таблице снова три.

### 19.4 Б4 — перенумерация фазы сделала ссылку ложной

Новый шаг сдвинул прогон на развёрнутой посадке с шестого на седьмой, и ссылка §15.6 стала
указывать на соседний шаг — **тот самый off-by-one, который дельта чинила**. Исправлено не
правкой ссылки (§15 — запись, правке не подлежащая), а **возвратом нумерации**: новые шаги
получили буквенный индекс — Ф1 `1а` и `3а`, Ф2 `2а`, — ровно как пункты объёма 8а, 8б и 10а, и
ровно затем же. Ссылка снова верна, защищённый раздел не тронут.

### 19.5 Б5 — шапка объявляла одобрение на нерассмотренном отпечатке

Круг 4 одобрил `f77d062d`; редакция 3 его изменила, и сам факт круга 5 доказывает, что вердикт не
переносится. Шапка переведена в **DRAFT, редакция 3**, прежнее объявление сохранено дословно —
тем же порядком, каким в черновик переводилась редакция 2. Перечень нетронутых записей дополнен.

**Число в этой строке было неверным, и поправил его круг 6.** Здесь стояло «их **семь**» — счёт
брал §12–§18 целиком, тогда как §18 несёт правленую строку (§19.7). Нетронутых записей **шесть**
(§12–§17), плюс §18 с одной правленой координатой. Прежняя редакция называла шесть — и была
права по числу, а не по составу: она ещё не знала про §18.

### 19.6 Четыре не блокирующих

- **числа §7 п. 8а без ревизии** — ревизия и единица названы;
- **у требования §7 п. 10а не было предиката вовсе** — заведён, и **в обе стороны**: поле адреса
  фронта 0, поле читателя не ноль, потому что «0» без второй величины неотличимо от «предикат не
  нашёл ничего»;
- **держатели снимаемой ветки не названы** — их **два**, на разных уровнях, снятие одной вторую не
  закрывает; названы предикатом и включены в шаг 3а Ф1;
- **предикат печатал счёт по файлам** там, где обещано число — переписан.

**И пятое, моё собственное, поймано прогоном сразу после написания:** второй предикат пункта 8а в
форме `git grep -h … | grep -vc principalmeta` давал **4** вместо 3 — `-h` снимает имена файлов, и
отрицание начинало фильтровать содержимое строки, а строка общего узла этого слова не содержит.
Фильтр по пути требует имён. Записано рядом с предикатом: класс ловится **прогоном своего же
предиката**, а не перечитыванием.

### 19.7 Одна строка §18 правлена — и это названо, а не спрятано

Заявление шапки «§12–§18 не тронуты» я написал **до** того, как проверил, и оно оказалось шире
факта на одну строку: §18.2 ссылалась на «шаг 5 фазы Ф1», а переиндексация круга 5 сделала этот
шаг **3а**. Оставить ссылку значило бы сохранить в записи ложную координату ради буквального
«не тронуто»; поправить и промолчать — то же самое с другой стороны.

**Поправлено и объявлено.** Правка — одна строка, предмет — координата, а не суждение; вердикты
§12 и §14 не тронуты ни байтом.

**Это уже второй раз, когда «сверено» я пишу раньше сверки** (первый — круг 4, §17). Оба раза
ловило **сравнение с предыдущим коммитом**, оба раза не перечитывание. Норма для себя, а не для
документа: строка «не тронуто» пишется **после** вывода `diff`, а не до.

### 19.8 Что круг подтвердил и что осталось неустановленным

Подтверждено: четыре правки редакции 2→3 верны **по существу**; записи прежних кругов нетронуты —
889 строк, сравнение чисто, и моё заявление было **скромнее факта**; граница санкции названа в обе
стороны; пункты объёма, сценарии и строки состояния целы. Рецензент назвал и **свою** ложную
находку: заподозрил мёртвые координаты у одного семейства и снял подозрение, найдя в документе
объявленное соглашение о координатах по стволу.

Неустановленным остаётся прежнее (§15.6, §17.5) плюс одно новое: **сколько строк колонки §5
устарело на самом деле**. Моя единица даёт нижнюю границу 8; единица рецензента даёт 11 и мной не
воспроизводится. Перемер колонки — работа **перед исполнением**, и она названа, а не сделана.

---

## 20. Круг 6 — ❌ CHANGES REQUESTED, редакция 4

Записи §12–§18 не тронуты — и это написано **после** вывода `diff`, а не до. Единица: от строки
`## 12.` до строки перед `## 19.`, **958** строк; сравнение с `9729e26c` пусто, отпечаток один.
Числа §19.8 (889) и рецензента (892) сняты **другими** границами того же куска и здесь не
переписываются: расхождение — в единице, а не в дереве.

**§19 правлен в двух местах, и это названо, а не спрятано.** Оба — координаты, ставшие ложными от
той же дельты, которая их и породила; оба найдены кругом 6 и разобраны в §20.2 (г) и (д). Правка
координаты внутри записи — приведение её к факту, а не переписывание истории; тот же порядок, каким
§19.7 объявил свою правку §18.

### 20.1 Б1 — врезка §5 занижала величину ВТРОЕ, потому что её предикат знал одну форму из двух

Врезка объявляла остаток колонки «минимум восемь строк». Замер обеими формами даёт **минимум 24
из 41**.

**Предмет — не ширина единицы, а слепота разборчика.** Идентификатор сценария записывается в этом
дереве **двумя** законными формами: дефисной (`KAN-VER-01`) и с подчёркиванием (`KAN_VER_02` — так
устроены имена проб Go, где латиница обязательна, ban #17). Прежний предикат знал только первую и
потому не видел **16** строк из 25. Дефисная половина при этом воспроизводится **в точности** — те
же 9 сценариев тем же списком, из них 8 «не начат», — то есть прежнее число было верным ответом на
**более узкий** вопрос, а не другим счётом того же.

| величина | значение | форма предиката |
|---|---:|---|
| строк колонки §5 | 41 | — |
| держатель называет сценарий по номеру | **25** | обе формы |
| — видит дефисная | 9 | прежняя |
| — видит подчёркивание | 23 | недостававшая |
| строк «не начат» с держателем | **24** | обе формы |

Замер 2026-09-07 на `release/kaname` @ `e9c498f928` и на `fe0e5a60f3` — **числа совпали**, вершина
линии их не сдвинула. Перечень форм **выведен из дерева**, а не из памяти: смешанных форм
(`KAN-VER_01`) в дереве **0**, значит разделителей ровно два.

**Внесено:** предикат, знающий обе формы, вместе с командой его вывода · величина «минимум 24 из
41» вместо восьми · сам перемер перенесён **из «работы перед исполнением» в тело врезки** — он
стоит одной команды, а отложенный замер к исполнителю не приезжает.

**Чего перемер НЕ даёт, и это сказано во врезке отдельно.** Он доказывает, что **имя названо**, а
не что проба закрепляет именно этот сценарий: «держатель существует» ≠ «сценарий закреплён» — те же
три оси, что §17.1 и §19.2. Поэтому **ячейки колонки не переписаны**: перевод строки из «не начат»
требует прочтения её пробы, а это уже не одна команда. Число 24 остаётся **нижней** границей и по
второй причине — проба, закрепляющая сценарий без упоминания номера, предикату не видна.

### 20.2 Б2 — пять координат дельты пережили правку; одна из них разрешала кодировать

| # | где | стояло | верно |
|---|---|---|---|
| а | §11, врезка | «Шапка говорит „кодировать разрешено"» | шапка говорит «кодировать **нельзя**» |
| б | шапка | «редакция 3, ждёт круга 5» | круг 5 записан в §19; ждёт круга **7** |
| в | шапка | «(§19.8)» о правке строки §18 | правка описана в **§19.7** |
| г | §19, преамбула | «Записи §12–§18 не тронуты» | §18 правлен, что §19.7 и говорит |
| д | §19.5 | «нетронутых записей… их семь» | **шесть** (§12–§17) + одна правленая строка §18 |

**(а) — единственное с последствием за пределами текста, и оно про ban #1.** Текст врезки был верен
при круге 4 и стал ложным **от дельты**, переведшей шапку в черновик. Цена именно в адресате:
исполнитель, открывший §11 — раздел, специально написанный о том, что можно начинать, — получал
разрешение, которого документ не даёт. Врезка переписана так, что предупреждение **не отпадает, а
откладывается** до возврата одобрения: её предмет (санкция объёма ≠ выбор его входа) от статуса
шапки не зависит.

**(г) и (д) — два места об одном предмете внутри одного раздела**, и верным было дальнее: §19.7
честно объявляла правку §18, пока преамбула и §19.5 её отрицали.

**Класс общий у всех пяти: утверждение о документе, пережившее правку этого документа.** Он
отверг круг 5 (перенумерация фаз сделала ложной ссылку §15.6) и повторился здесь пятью
экземплярами. Правится он не вниманием, а порядком: **каждая ссылка дельты прогоняется своим
предикатом после того, как правки внесены**, — по номеру раздела, по номеру шага, по статусу шапки.

### 20.3 Два не блокирующих — закрыты, и второе проверено ПРОГОНОМ, а не цитатой

**Н1 — у пункта 7 таблицы стояло «ниже» в колонке «предикат».** Утверждение верно, но команды при
нём не было, тогда как две соседние строки её несут. Внесена:
`git grep -c 'slog.String("reason"' origin/release/kaname -- services/iam/internal/presentedcred/reader.go`
→ **1**. Журнал причину несёт, поле подписано.

**Н2 — буквенные шаги `1а.` / `3а.` / `2а.` маркерами списка не являются.** Рецензент назвал это
цитатой правила, оговорив, что рендер не выполнял. **Рендер выполнен** (`markdown_it`, профиль
`commonmark`), и цитата подтвердилась наблюдением:

```
было:  <ol> с ЧЕТЫРЬМЯ <li> — «1а.» втянут в абзац пункта 1, «3а.» в пункт 3
стало: <ol> с четырьмя <li>, где 1а и 3а — отдельные абзацы ВНУТРИ своих пунктов
```

То есть шаг 3а — тот самый, о котором врезка ниже говорит «не выполняется порознь с шагом 3», — не
отображался шагом **вовсе**, а читался хвостом прозы шага 3. Все три приведены к форме §7 (жирная
подпись внутри пункта, как у 8а/8б/10а): она безопасна при любом рендере, потому что не опирается
на распознавание маркера.

**И третье, моё собственное, поймано прогоном опубликованной команды, а не чтением.** Первый
предикат врезки §5 — тот, что считает файлы механизма, — был опубликован в виде
`… --name-only >   | grep -iE …`: потерянный перенос строки оставил `>` от разметки цитаты внутри
команды. Это **синтаксическая ошибка** шелла, то есть предикат не исполнялся **ни разу** ни у
одного читателя, а число 11 при нём выглядело измеренным. Прогнан, починен, число подтверждено:
интактная форма даёт **11**. Рядом проверены и два соседних — `presentedcred\.` в композиционном
корне даёт **5**, `identityProvider: own` по `deploy` даёт **0**, оба как заявлено.

Тот же класс, что Н1, и он ровно про это: колонка «предикат» обязана нести **исполнимую** команду,
а не её вид. Отличается только тем, что Н1 команды не имел вовсе, а здесь она была и не работала —
второе хуже, потому что выглядит проверенным.

**Ссылка на ветку приведена к `origin/release/kaname` во всех командах врезки.** Локальной ветки
`release/kaname` в свежем клоне может не быть; ветка удалённого отслеживания есть у всякого, кто
склонировал. Обе на момент замера указывают на один коммит — проверено, поэтому числа не сдвинулись.

### 20.4 Исход проверки посылок круга 6 — обе стороны названы

**Подтвердились замером:**

- формы записи идентификатора — ровно **две**, смешанных 0 (выведено из дерева, не из задания);
- числа рецензента **41 · 25 · 9 · 23 · 24** — воспроизведены **полностью**, моим предикатом,
  на двух ревизиях;
- «дефисная половина воспроизводит прежние 9 и 8 в точности, тем же списком» — воспроизведена;
- все пять координат Б2 — ложны, как названо;
- «на текущей вершине линии результат тот же» — проверено: `e9c498f928` даёт те же числа, что
  `fe0e5a60f3`;
- Н2 — подтверждена **сильнее**, чем заявлена: рецензент дал цитату правила, рендер дал факт.

**Опровергнуто либо уточнено:**

- **мой собственный разборщик колонки был слеп дважды, и оба раза поймал прогон, а не чтение.**
  Первый заход извлёк из таблицы §5 **одну** строку вместо 41: присваивание полю в `awk`
  пересобирает запись через `OFS`, и следующее правило, проверявшее «строка не начинается с `|`»,
  срабатывало на **пересобранной** строке. Второй — `comm` на несогласованной сортировке: под
  локалью по умолчанию он молча дал предупреждение вместо ответа. Оба лечатся одним: **считать под
  `LC_ALL=C` и сверять число строк с ожидаемым**, а не доверять форме вывода;
- **«перемер колонки стоит одной команды» — верно ровно наполовину.** Одной командой измеряется,
  **сколько** строк устарело; перевести конкретную ячейку из «не начат» одна команда не может —
  нужно прочесть пробу. Поэтому перемер внесён как **число**, а ячейки оставлены; иначе врезка
  утверждала бы про держателя то, что предикат меряет про имя, — ровно зазор §19.2;
- **число «5 файлов-носителей» требует точного предиката.** Семейственный образец
  (`KAN[-_](VER|REV|…)[-_][0-9]+`) даёт **24** файла, потому что ловит `KAN-WIRE-1` — чужой
  идентификатор с одной цифрой. Идентификаторы **этого** документа несут **пять** файлов, все под
  `services/iam/`.

### 20.5 Что осталось неустановленным

Прежнее (§15.6, §17.5, §19.8) в силе, и к нему добавляется уточнение по одному пункту:

- **«сколько строк колонки §5 устарело на самом деле» — сужено, но не закрыто.** Нижняя граница
  поднята с 8 до **24 из 41** и теперь опирается на предикат, знающий обе формы. Верхней границы
  по-прежнему нет: пробы, не называющие номер сценария, предикату не видны, и сколько их — не
  измерено. Второе число круга 5 (**11 строк**) моей единицей так и не воспроизведено; я не
  разрешаю расхождение выбором правдоподобного и называю обе величины;
- **закрепляет ли найденная проба именно свой сценарий — не проверено ни по одной из 25 строк.**
  Это чтение 25 проб, а не команда, и оно отнесено к первому шагу Ф0;
- **стенд не поднимался ни одним кругом**, включая этот. О поведении на кластере вердикта нет —
  ни одна правка редакции 4 его и не касается: все шесть в тексте документа.
