# Sub-phase KAN-REPO-1 — имя репозитория вынесенной службы и судьба истории её домена

> **Статус:** DRAFT
> **Статическая форма вердикта:** DRAFT. Действующее одобрение выводится только из
> внешнего события ревью и добавляемой записи; строка статуса после ревью не меняется.
> **Дата:** 2026-09-08
> **Ревьюер:** `acceptance-reviewer`
> **Предмет:** [PRO-Robotech/kacho#1763](https://github.com/PRO-Robotech/kacho/issues/1763) —
> решение: имя репозитория для standalone-iam и судьба его истории
> **Эпик:** [#2119](https://github.com/PRO-Robotech/kacho/issues/2119) — Kaname становится
> самостоятельным продуктом
> **Смежные:** [#1111](https://github.com/PRO-Robotech/kacho/issues/1111) (веха M11 — разрез
> и переименование одним переключением), [#2120](https://github.com/PRO-Robotech/kacho/issues/2120)
> (видимость опубликованного артефакта, ЗАКРЫТА), приёмка
> `sub-phase-KAN-RELEASE-1-platform-version-and-pin-order-acceptance.md` (порядок выпуска —
> она задаёт условие, закрывающее окно §3.4 этой приёмки)
> **Ревизии замера:** продукт `origin/main` @ `a10bfd208d4dc556c2a4c75ec813edb60f350b5d`
> (2026-09-08T11:15:27+03:00), воркспейс `origin/main` @ `f534c9ae264a1adadcf327ddd77693b821fbc42d`,
> `PRO-Robotech/kaname` `main` @ `3c21a59f8c3a36a070aadec2b1e62716578632f5`,
> `PRO-Robotech/kacho-iam-polyrepo-archive` `main` @ `c744f9561847ef3079c29d2afb36dc308621b2b7`,
> ствол продукта **на день заведения задачи** @ `1b8f467fb496edce6e6a9763194816409f7c3654`
> **История ревью** (добавляется, не переписывается):
> - 2026-09-08 · `acceptance-reviewer` · ❌ CHANGES REQUESTED · отпечаток
>   `f9ccd1f249a00d37fb9066411fb6c30854ce61898d7a542fb0405f6f21a87a58` (blob `f6e7592a`,
>   коммит `8c6cda55`) · блокирующих 3, важных 3 · замер воспроизведён независимо: 30 из 30.
>   Запись **не переносит** вердикт на эту редакцию: вердикт привязан к отпечатку, а эта
>   редакция его меняет. Круг 2 закрыл все три блокирующих в операционной части §3.5 и §4:
>   шагу материализации названо средство и заведён свой сценарий; шагу отправки названа
>   ссылка дословно и запрещены формы, снимающие чужие ветки; сценарий 08 приведён к тому,
>   что его «Дано» действительно производит, а вторая полоса выведена отдельным сценарием.
> - 2026-09-08 · `acceptance-reviewer` · ❌ CHANGES REQUESTED · отпечаток
>   `3eb02e3133b97a112a9b0275ba58c65716d0c1eba1792ab4169bbf7e11d548e4` (коммит `d0ffb245`) ·
>   три блокирующих круга 1 закрыты, проверено исполнением. Остаётся блокирующим 1: шаг 6
>   без команды, условие §9 №8 (3 поля) уже того, что сносит шаг 4 (10 настраиваемых групп),
>   и `required_pull_request_reviews` теряется молча. Важное 1: перепись снимает подписи —
>   45 из 67 подписаны, в результате 0, слово в документе не встречается.
>   Круг 3 закрыл оба: шаг 4 получил снимок, шаг 6 — команду полной замены, условие §9 №8
>   сверяет объект целиком (предикат доказан инъекцией в обе стороны, сценарий 32), а путь
>   снятия защиты выбран замером и назван эскалацией с ценой каждой ступени (§5.8).
> - 2026-09-08 · `acceptance-reviewer` · ✅ APPROVED · отпечаток
>   `68ed6bc04905fdbb04c738fb469ad66d73ddaf3b5b349d9f90ded30928d0f03e` (коммит `95907b59`) ·
>   блокирующий круга 2 закрыт, проверено исполнением: посадка защиты 11/10/3 дословно,
>   преобразование §3.6 прогнано на живом снимке (тело валидно, `url` снят,
>   `required_signatures` исключён), инъекция сценария 32 сошлась в обе стороны — старое
>   условие даёт `[true,false,false]` на обоих, новое называет 7 потерянных групп,
>   положительный контроль пуст. Связка подписей подтверждена: неподписанный префикс от
>   корня равен 2 и совпадает с множеством уцелевших хешей. Перепись сверена своим обходом:
>   33 сценария, 01–33 сплошные, 28 строк §6, покрытие 33 из 33. Эскалация §5.8 признана
>   годной заменой опыта: отказ каждой ступени безвреден и наблюдаем, обратим одним снимком,
>   а ступень 3 отвергнута замером — `allow_deletions: false` единственное, что держит ствол
>   (`ci-bootstrap` protected=false, правил ветвей 0). Непроверенным остаётся поведение `PUT`
>   защиты и сценарии 26, 27 — до посадки. Незакрытых блокирующих нет; два замечания
>   к исполнению: §9 заявляет нумерацию 1:1 при неотображённых шагах 4а/4б, §5.8 не называет
>   вид отказа ступени 1.
>   Обе правки внесены той же редакцией, что несёт эту запись: §9 получил строки на 4а и 4б
>   и точную формулировку соответствия вместо «1:1», §5.8 — вид отказа ступени 1 с
>   различителем, измеренным на месте.

## Обзор

Задача просит **решения**, а не работы: как назвать отдельный репозиторий вынесенной
службы и едет ли туда история домена. Приёмка отвечает на оба вопроса, называя **все**
исходы с ценой и выбранный, и делает это перемером — потому что за неделю, прошедшую с
заведения задачи, мир сдвинулся по обоим предметам сразу.

Перемер подтвердил принятое решение и внёс в него **две поправки, обе с числами**:

1. **Решение об имени ИСПОЛНЕНО** — и механизмом, которого в перечне исходов задачи не
   было. Прежний полирепо **переименован** в `kacho-iam-polyrepo-archive` и заархивирован,
   а имя `kacho-iam` отдано новому репозиторию, ныне `PRO-Robotech/kaname`. Это **четвёртый**
   исход, и он **сильнее** выбранного третьего ровно по той оси, ради которой третий и
   выбирался: старое имя не мертво — оно **перенаправляет на победившее**, поэтому внешний
   потребитель, склонировавший `kacho-iam`, получает сегодняшнюю службу, а не состояние
   полуторамесячной давности (§2, сценарии 01–04).
2. **Решение об истории НЕ исполнено, и класс, о котором предупреждала задача, УЖЕ
   реализовался**: `kaname` заведён снимком. `git rev-list --count HEAD -- .` в нём даёт
   **2** — ровно тот исход, который задача называла проигрышным. Значит предмет решения
   сместился с **предотвращения** на **починку**, и цена изменилась: переписывать придётся
   **опубликованную** историю (§3).

Сверх этого перемер разделил второе решение надвое, чего задача не делала, и это
разделение несущее: **раскол** даёт 894 коммита, **прививка** добавляет 67. То есть 93 %
ценности решения лежит в расколе, а прививка — семипроцентная добавка, чья польза измерена
(§3.2) и чья стена **не исчезает**: корень архива сам снимок (883 файла одним коммитом).
Оба шага при этом обязаны идти **одной переписью**: вторая перепись стоит столько же, что
первая, а окно, в котором перепись бесплатна, сегодня открыто и закроется первым же
опубликованным тегом (§3.4).

---

## 1. Перемер посылки

Правило простое: посылка задачи проверяется первой, и каждое её число — своей командой.
Из четырёх чисел тела **воспроизвелись все четыре** — но два из них только на **своей**
ревизии, и это надо сказать прямо, иначе следующий читатель решит, что автор ошибся.

### 1.1. Числа тела задачи — верны для ревизии дня заведения, не для сегодня

Задача заведена 2026-08-31T12:33:02Z. Вершина ствола продукта на тот момент —
`1b8f467fb496edce6e6a9763194816409f7c3654`.

```sh
r=$(git rev-list -1 --before='2026-08-31T12:33:02Z' origin/main)   # 1b8f467fb4
git rev-list --count "$r" -- services/iam   # → 866
git rev-list --count "$r"                   # → 2834
```

Оба числа тела воспроизводятся **дословно**. На сегодняшней вершине
`a10bfd208d4dc556c2a4c75ec813edb60f350b5d` те же команды дают **880** и **2848**.

| величина | тело задачи | сегодня | предикат |
|---|---:|---:|---|
| коммитов, тронувших `services/iam` | 866 | **880** | `git rev-list --count origin/main -- services/iam` |
| коммитов в стволе | 2834 | **2848** | `git rev-list --count origin/main` |
| веток у прежнего полирепо | 15 | **15** | `gh api repos/PRO-Robotech/kacho-iam-polyrepo-archive/branches --jq length` |
| незаархивированных зеркал | 12 | **12** | `gh repo list PRO-Robotech --limit 100 --json name,isArchived` + отбор `kacho-*` без `kacho-workspace` |

**Замечание об адресе, из-за которого число веток едва не разошлось.** Имя `kacho-iam`
сегодня **перенаправляет**, поэтому `gh api repos/PRO-Robotech/kacho-iam/branches --jq length`
отвечает **2** — это ветки `kaname`, а не архива. Пятнадцать веток живут у архива под его
новым именем. Спрашивать надо адрес, а не привычное имя: перенаправление отвечает молча и
правдоподобно.

### 1.2. Единица счёта: 866 ≠ 880 ≠ 894, и все три верны

Задача называет предикат снятия «866 при переносе». Он **недостижим** ни при каком верном
исполнении, и не потому, что перенос неверен, а потому, что три числа мерят разное:

| число | что считает | предикат |
|---:|---|---|
| **866** | коммиты, тронувшие путь, **на ревизии дня заведения** | `git rev-list --count 1b8f467fb4 -- services/iam` |
| **880** | то же **сегодня** | `git rev-list --count origin/main -- services/iam` |
| **894** | коммиты **расколотой истории** | `git subtree split -P services/iam -b iamhist && git rev-list --count iamhist` |

Разница 894 − 880 = **14**, и она **не одного вида** — это надо сказать, иначе следующий
объяснит её одними слияниями и ошибётся:

| | раскол | по пути | разница |
|---|---:|---:|---:|
| всего | 894 | 880 | **14** |
| слияний | 153 | 147 | 6 |
| без слияний | 741 | 733 | 8 |

Обе части даёт одно и то же: упрощение истории по пути отбрасывает коммит, чей результат на
этом пути совпал с результатом одного из родителей, а раскол его сохраняет. Предикаты —
`git rev-list --count [--merges|--no-merges] iamhist` и та же пара с
`origin/main -- services/iam`.

**Следствие для предиката задачи:** число сверки обязано браться **из раскола**, а не из
`rev-list` по пути, и обязано называть ревизию, на которой раскол сделан. Форма предиката
дана в §9.

### 1.3. Решение об имени — исполнено; исполнено четвёртым исходом

```sh
gh api repos/PRO-Robotech/kacho-iam --jq .full_name
#   → PRO-Robotech/kaname                       ← имя ПЕРЕНАПРАВЛЯЕТ

gh repo view PRO-Robotech/kacho-iam-polyrepo-archive \
  --json name,isArchived,visibility,pushedAt,createdAt
#   → archived=true, PUBLIC, push=2026-07-15T18:50:36Z, created=2026-05-17T02:25:38Z

gh repo view PRO-Robotech/kaname --json name,isArchived,visibility,createdAt
#   → archived=false, PUBLIC, created=2026-09-05T02:22:07Z
gh api repos/PRO-Robotech/kaname --jq .delete_branch_on_merge         # → true
gh api repos/PRO-Robotech/kaname/branches/main/protection --jq \
  '{enforce_admins:.enforce_admins.enabled, force:.allow_force_pushes.enabled,
    del:.allow_deletions.enabled, checks:(.required_status_checks//null)}'
#   → {"enforce_admins":true,"force":false,"del":false,"checks":null}
```

Второй пункт предиката задачи («у победившего имени настройки заведены, у проигравшего
`isArchived: true`») **исполнен целиком**, включая защиту ствола, которая на 2026-09-06
отвечала `403` из-за приватности и была открыта закрытием
[#2120](https://github.com/PRO-Robotech/kacho/issues/2120).

> [!note] Что здесь НАБЛЮДЕНИЕ, а что ВЫВОД — разделено намеренно
> Наблюдается: имя `kacho-iam` перенаправляет на `kaname`; репозиторий
> `kacho-iam-polyrepo-archive` создан **2026-05-17**, заархивирован, несёт 67 коммитов
> полирепо-эры и 15 веток, последняя запись — 2026-07-15T18:50:36Z, то есть ровно те
> величины, которые тело задачи назвало у `kacho-iam`.
>
> Выводится: архив и есть прежний `kacho-iam`, переименованный. Иначе не сходится дата
> создания — она **предшествует** первому коммиту полирепо (2026-06-28), значит это тот же
> объект, а не копия. Самого события переименования API не отдаёт, и утверждать его как
> наблюдение было бы неверно; вывод же держится на трёх независимых величинах — дате
> создания, числе веток и отметке последней записи.

### 1.4. Решение об истории — НЕ исполнено; снимок уже публичен

```sh
git clone --bare https://github.com/PRO-Robotech/kaname.git kaname-probe
git -C kaname-probe rev-list --count main                    # → 2
git -C kaname-probe log --format='%h %ad %s' --date=short main
#   3c21a59 2026-09-06 Дерево службы приведено к монорепо
#   5618c88 2026-09-05 Kachō IAM — начальный импорт
git -C kaname-probe show --stat --format= 5618c88 | tail -1
#   → 2188 files changed, 764531 insertions(+)
```

Снимок — **2188 файлов и 764 531 строка одним коммитом**. Задача предсказывала, что при
таком исходе `git blame` ответит одной датой; предсказание сбылось, и величина оказалась
крупнее названной. Сегодняшний корпус Go службы: **436 916** строк в 2051 файле, из них
не-тестовых **139 100** в 651 файле
(`git grep -c '' origin/main -- 'services/iam/**/*.go'`, второй — с исключением `*_test.go`).

### 1.5. Что перемер нашёл СВЕРХ посылки — четыре факта, и каждый меняет цену

**Факт А. Окно бесплатной переписи открыто, и оно узкое.** У `kaname` сегодня **0** тегов,
**0** заметок о выпусках, **0** форков, **0** звёзд, **0** наблюдателей, и **ни один модуль
его не требует**: объявление `services/iam/go.mod` называет путь
`github.com/PRO-Robotech/kaname`, но `require` на него нет ни в одном `go.mod` дерева
(объявлений модулей два — корневой `github.com/PRO-Robotech/kacho` и модуль службы).
Значит перепись истории сегодня не рвёт **ничего измеримого**. Первый же опубликованный
тег либо первый потребитель, пиннувший псевдоверсию, закрывает окно необратимо.

**Факт Б. Ветка `ci-bootstrap` — работа в единственном экземпляре, и она РАЗОШЛАСЬ, а не
отстала.** «Впереди на 2» было бы неверно и мягче правды:

```sh
git rev-list --count main..ci-bootstrap   # → 2
git rev-list --count ci-bootstrap..main   # → 1
git merge-base main ci-bootstrap          # → 5618c88 — КОРЕНЬ снимка
```

Общий предок — корневой снимок, то есть ветка отделилась от самого начала и своей стороной
`main` не поглощена. Перепись путей разделяет её содержимое честно:

| величина | значение |
|---|---:|
| путей у `ci-bootstrap` | 2194 |
| из них вне расколотой истории | 1216 |
| из этих 1216 — **были в корневом снимке** (прежняя раскладка, не работа) | **1210** |
| **уникальны для ветки** | **6** |

Уникальны ровно шесть, и все они — оснастка конвейера службы:
`.github/golangci.yml`, `.github/scripts/classify-integration-outcome.sh`,
`.github/scripts/go-test-verdict.py`, `.github/scripts/gosec-gate.sh`,
`.github/scripts/run-integration.sh`, `.github/workflows/ci.yml`. Каталога `.github` нет ни
у `kaname/main` (0 путей), ни у `services/iam` (0 путей), и все шесть отсутствуют у
`kaname/main` — проверено поимённо, 6 из 6. Всякий план, трогающий `kaname`, обязан эту
ветку **не тронуть**; преемник для этих шести назван остатком §8.

**Факт В. Корень архива сам снимок.** `1a4dbb2a7` «chore: initial public release»,
2026-06-28, **883 файла, +269 449** одним коммитом. Прививка **не убирает стену** — она
сдвигает её на **17 дней** назад.

**Факт Г. Охват прививки ограничен переработкой домена.** Из **918** путей вершины архива
до сегодняшнего дерева дожили **228** (из них `.go` — **149**). Остальные 690 переименованы
или сняты.

### 1.6. Побочная находка: два утверждения корпуса пережили свой предмет

`.claude/rulebook/polyrepo.md` (строки 23–26) утверждает, что предшествующие полирепо
«**существуют и не заархивированы**» и что «последний push в каждом — **середина июля
2026**». Оба замера сегодня иные: заархивирован **один** (`kacho-iam-polyrepo-archive`), а
`kacho-vpc` и `kacho-compute` двигались **2026-08-19** и **2026-08-17**. Правка корпуса —
не предмет этой приёмки; она заведена остатком §8.

---

## 2. РЕШЕНИЕ 1 — имя репозитория

### 2.1. Исходы и цена каждого

| # | исход | цена |
|---:|---|---|
| 1 | **оживить `kacho-iam`** | имя противоречит собственному решению продукта о наименовании (#2076): репозиторий несёт прежнее имя домена, а не имя продукта. Наследуется 15 чужих веток и описание, пережившее свой предмет. Настройки (защита ствола, снятие головы при вливании) всё равно заводятся заново |
| 2 | **завести новое имя, прежнее оставить живым** | **два живых претендента**: поиск по прежнему имени отдаёт его первым, потребитель клонирует состояние 2026-07-15 и об этом не узнает. Расхождение не сходится само и ничем не наблюдаемо |
| 3 | **заархивировать прежнее имя на месте, завести новое** | второго живого претендента нет. Но прежнее имя остаётся **занятым архивом**: ссылка на `kacho-iam` продолжает вести на состояние 2026-07-15 — читаемое, доступное и устаревшее |
| 4 | **переименовать прежнее в архивное имя, отдать имя новому, заархивировать прежнее** | ссылка на `kacho-iam` **перенаправляет на победителя**: устаревшего состояния по прежнему адресу больше нет. Цена — архив находится только по новому имени, и всякий, у кого была ссылка на полирепо, попадает не туда, куда шёл |

### 2.2. Выбранный исход — **4**, и он уже исполнен

Исход 4 в перечне задачи отсутствовал; перемер (§1.3) показал, что дерево пошло именно им.
Он **доминирует** исход 3 ровно по оси, ради которой тот выбирался: задача называла риском
то, что «внешний потребитель склонирует и не узнает, что взял состояние на полтора месяца
старше ствола». Исход 3 этот риск **не снимает** — заархивированный репозиторий читается и
клонируется, — а исход 4 снимает его **построением**: по прежнему адресу устаревшего
состояния больше нет.

**Цена исхода 4 названа честно и она не нулевая.** Всякая внешняя ссылка на полирепо —
в статье, в закладке, в чужом README — ведёт теперь на `kaname`, то есть **не туда, куда
вела**. Внутри корпуса такие ссылки есть: **652** файла воркспейса упоминают `kacho-iam`
при 1532 отслеживаемых `.md` (предикат: `git grep -l 'kacho-iam' origin/main -- '*.md'`).
Переписывать их **нельзя**: записки свидетельствуют о сделанном, а не предписывают, и
переименование сделало бы ложными утверждения, которые были верны. Это осознанный остаток,
а не недоделка.

### 2.3. Что осталось незакрытым по решению 1

Описание архива сегодня гласит: «Архив прежнего полирепо домена IAM (до 2026-07-15).
Разработка ведётся в PRO-Robotech/kacho; **продукт для отдельной поставки —
PRO-Robotech/kacho-iam**». Названное имя перенаправляет на `kaname`, то есть строка
называет адрес, который больше не обозначает то, что обозначал. Исход — правка описания
(§7, действие наружу).

---

## 3. РЕШЕНИЕ 2 — судьба истории домена

### 3.1. Исходы и цена каждого

| # | исход | что получаем | цена |
|---:|---|---|---|
| 1 | **снимок** (исполнено 2026-09-05) | 2 коммита; дерево верно | `git blame` отвечает одной из двух дат на 764 531 строку; история домена теряется **второй раз** — первый был сборкой монорепо |
| 2 | **раскол** `git subtree split -P services/iam` | **894** коммита, 2026-07-15…сегодня | хеши иные, чем в монорепо (проекция, не тождество); перепись опубликованной истории `kaname`; `ci-bootstrap` остаётся при своём общем предке — корне снимка, — и после переписи `main` перестаёт делить с ней хоть один коммит |
| 3 | **раскол + прививка** к вершине архива | **961** коммит, 2026-06-28…сегодня | то же плюс шов, где 539 из 916 файлов приписываются коммиту импорта; стена **остаётся**, сдвинувшись на 17 дней; **и хеши эры архива тоже переписываются** — см. ниже |

> [!warning] Цена, которую легко не заметить: прививка ПЕРЕПИСЫВАЕТ и эру архива
> Материализация замены рождает новые коммиты **по всей** цепочке, а не только у раскола.
> Замер (сценарий 30): из **67** коммитов архива уцелело **2** — корень `1a4dbb2a7` и его
> прямой потомок `0ec2b0fd7`; у раскола уцелело **0** из 894 (эти коммиты и так были
> синтетическими).
>
> **Следствие, которое надо сказать прямо:** после переписи сверка «этот коммит архива и
> есть этот коммит службы» становится **невозможной для 65 из 67**. Архив остаётся
> единственным местом, где живут исходные идентификаторы полирепо-эры, и всякая ссылка на
> них — в записке, в задаче, в чужой статье — продолжает указывать **в архив**, а не в
> службу. Это не поломка, а свойство любой переписи; неназванным оно превращается в
> ловушку для того, кто попробует сверить два дерева по хешу.
>
> **И та же перепись снимает ПОДПИСИ — это не следует из «хеши меняются».** Читатель вправе
> решить, что подпись едет вместе с коммитом; она не едет и **не может**: подпись покрывает
> объект вместе с его родителями, а перепись меняет родителей. Замер (сценарий 33): в архиве
> подписано **45** коммитов из 67, в привитой материализованной истории — **0** из 961.
>
> **Тем же объясняются и «уцелевшие 2».** Первые два коммита архива не подписаны, третий
> (`490895355`, 2026-06-29) подписан — его объект меняется, и вся цепочка за ним тоже. То
> есть «45 подписей» и «уцелело 2 хеша» — один факт, а не два.
>
> **Смягчение называется в той же фразе, что и потеря:** исходные подписанные коммиты
> **остаются в архиве**, он публичен и от чтения не закрыт, — проверить подпись есть где,
> просто не в службе. Восстановить подписи в переписанной истории нельзя ничем: ключей
> подписантов у нас нет, а подписать заново значило бы удостоверить чужую работу своей
> подписью.
| 4 | **оживить прежний репозиторий и вести службу там** | история непрерывна без прививки | отвергается решением 1: имя противоречит имени продукта; и это возврат к исходу 1 §2.1 |

### 3.2. Цена и польза раскола и прививки — ПОРОЗНЬ, потому что они разного веса

Задача считала раскол и прививку одним решением. Замер их разделяет:

| ось | раскол | прививка |
|---|---:|---:|
| коммитов добавляет | **894** | **67** |
| доля в итоговых 961 | **93 %** | **7 %** |
| охватывает период | 2026-07-15 → сегодня | 2026-06-28 → 2026-07-15 (**17 дней**) |
| убирает ли стену `blame` | да, для своего периода | **нет** — корень архива сам снимок (883 файла, +269 449) |
| файлов, до которых дотягивается | все 2394 сегодняшних | **228** доживших путей из 918 |

**Польза прививки измерена, а не предположена.** Из 149 доживших файлов Go (23 395 строк
сегодня) после прививки **14 321 строка** приписывается коммитам **до 2026-07-15**:

```sh
git blame -M --line-porcelain iamhist -- <файл> | grep '^author-time '
#   счёт строк с меткой времени раньше 2026-07-15
#   → файлов осмотрено 149, строк всего 23395, строк эры до 2026-07-15 — 14321
```

Это 61 % строк **в тех файлах, которые дожили**, и 3,3 % корпуса Go службы. Число названо в
обеих единицах намеренно: одна показывает, что прививка не пустая, вторая — что она не
несущая.

### 3.3. Выбранный исход — **3**, обоими шагами и ОДНОЙ переписью

Решение задачи подтверждается. Две поправки к его обоснованию:

- **несущее — раскол**, а не прививка. Довод задачи («история воссоединяется») верен, но
  93 % истории даёт раскол, и если бы прививка оказалась невозможна, решение не изменилось
  бы;
- **прививка берётся потому, что она почти бесплатна ВНУТРИ той же переписи**: отдельная
  вторая перепись стоила бы столько же, сколько первая, и второй раз потребовала бы снятия
  защиты ствола. Делать её позже — значит платить дважды за 7 %.

**Раскол проверен исполнением, а не объявлен.** Ключевые свойства (сценарии 09–11):

```sh
git subtree split -P services/iam -b iamhist
git rev-list --count iamhist                                    # → 894
git rev-parse iamhist^{tree}                                    # caa65753…
git rev-parse origin/main:services/iam                          # caa65753…  ← СОВПАЛИ
git rev-parse 95bdde5711^{tree}                                 # d8773438…
git rev-parse 5dbdbf6907:services/iam                           # d8773438…  ← СОВПАЛИ
```

Корень раскола в этом замере — `95bdde57111b141f5b66bfa31e16b150e629168b`. **Опираться надо
на дерево, а не на этот идентификатор**: он воспроизводится, только пока раскол делают той же
командой от той же ревизии, тогда как равенство деревьев верно при любом способе раскола и
именно оно доказывает, что содержимое не потеряно.

То есть раскол — **точная проекция**: дерево корня равно дереву службы на импорте, дерево
вершины равно дереву службы на сегодняшнем стволе. Содержимое не меняется, меняется только
форма истории.

**Прививка проверена исполнением тоже** (сценарии 13–15):

```sh
git replace --graft 95bdde571 c744f9561      # корень раскола ← вершина архива
git rev-list --count iamhist                 # → 961
git --no-replace-objects rev-list --count iamhist   # → 894  ← замена есть ВИД, не история
git rev-parse iamhist^{tree}                 # caa65753…  ← дерево не изменилось
```

### 3.4. Окно: почему сейчас, и что его закрывает

Перепись опубликованной истории `kaname` сегодня не рвёт ничего измеримого (§1.5, факт А).
Окно закрывают **три** события, любое достаточно:

| событие | почему закрывает |
|---|---|
| опубликован тег версии | версия раздана модуль-прокси Go; отозвать розданное нельзя, а перепись сделает её коммит недостижимым |
| посторонний модуль пиннул псевдоверсию | псевдоверсия называет коммит; после переписи `go mod download` у него откажет |
| появился форк или клон, ведущий свою работу | у него история, которой в стволе больше нет; сведение потребует ручного переноса |

Первое из трёх приходит **из соседней приёмки** — `KAN-RELEASE-1` описывает порядок, в
котором версия платформы публикуется, а пин службы переводится. Порядок между двумя
приёмками поэтому обязателен и назван: **перепись истории `kaname` идёт ДО первого
опубликованного тега службы**.

### 3.5. Порядок шагов

| # | шаг | команда | обратим? |
|---:|---|---|---|
| 0 | снять **страховочный** клон `kaname` целиком и убедиться, что `ci-bootstrap` в нём есть | `git clone --mirror <адрес> kaname-backup.git` · `git -C kaname-backup.git rev-parse --verify ci-bootstrap` | да |
| 1 | из чистой копии ствола продукта **с рабочей выкладкой** сделать раскол; сверить **оба** дерева | `git subtree split -P services/iam -b iamhist` (сценарии 08–11) | да |
| 2 | привить эру архива | `git replace --graft <корень раскола> <вершина архива>` (сценарии 13, 14) | да |
| 3 | **материализовать** замену | `FILTER_BRANCH_SQUELCH_WARNING=1 git filter-branch -f -- iamhist` — и сразу сверка: `git --no-replace-objects rev-list --count iamhist` → **961** (сценарий 30) | да |
| 4а | **снять СНИМОК посадки** — до всякого изменения | `gh api repos/PRO-Robotech/kaname/branches/main/protection > protection-backup.json` | да |
| 4б | ослабить защиту **полной заменой**, ступень 1 (§5.8) | `PUT …/branches/main/protection` телом из §3.6, где `allow_force_pushes: true` | да |
| 5 | **отправить переписанную историю — ОДНОЙ названной ссылкой** | `git push --force <адрес> iamhist:refs/heads/main` | **НЕТ** |
| 6 | вернуть защиту **полной заменой из снимка** | `PUT …/branches/main/protection` телом из §3.6, построенным из `protection-backup.json`; затем сверка §9 №8 — **объект целиком** | да |
| 7 | сверить в **свежем** клоне | `git rev-list --count HEAD -- .` → **961**; дерево вершины равно `services/iam` ствола | да |
| 8 | `ci-bootstrap` **не трогать** — работа в единственном экземпляре (§1.5, факт Б) | — | — |

> [!important] Шаг 5 отправляет ОДНУ ссылку — и это запрет, а не стиль
> **Запрещены `--mirror`, `--all` и `--delete`.** Копия раскола ветки `ci-bootstrap` не
> содержит **by construction** (она сделана из монорепо продукта, где этой ветки нет), а
> `--mirror` приводит удалённый набор ссылок к локальному — то есть **снимает** её вместе с
> шестью файлами, которых больше нигде нет. `--all` толкает все локальные ветки, включая
> служебные `base`/`archmain`/`iamhist-mat` из рабочей копии замера.
>
> Порядок обязан **закрывать** класс, а не только компенсировать его в §7: для необратимой
> операции компенсация — это уже ущерб. Поэтому ссылка называется дословно
> `iamhist:refs/heads/main`, а страховочный клон шага 0 существует **только** как
> восстановление и источником отправки не служит никогда.

Необратим **шаг 5**. Всё до него — правка; всё после — только новая перепись, которая
дороже первой ровно на то, что окно к тому времени может быть закрыто.

### 3.6. Тело замены строится ИЗ снимка, а не повторяет его

**Снимок не является годным телом замены**, и это надо сказать до того, как оператор
попробует его отправить. Форма ответа и форма запроса у этого ресурса **разные**:

| ось | что отдаёт чтение | что принимает замена |
|---|---|---|
| `enforce_admins` | объект `{url, enabled}` | **логическое** значение |
| `required_pull_request_reviews` | объект с `url` | объект **без** `url` |
| `required_status_checks` | отсутствует (значит «нет») | обязателен, допускается `null` |
| `restrictions` | отсутствует | обязателен, допускается `null` |
| `required_signatures` | объект `{url, enabled}` | **не принимается вовсе** — только свой под-ресурс |

Отсюда преобразование снимка в тело — механическое и обязано быть записано, а не
припомнено:

```sh
jq '{
  required_status_checks: null,
  enforce_admins: .enforce_admins.enabled,
  required_pull_request_reviews:
    (if .required_pull_request_reviews == null then null
     else (.required_pull_request_reviews | del(.url)) end),
  restrictions: null,
  required_linear_history: .required_linear_history.enabled,
  allow_force_pushes: .allow_force_pushes.enabled,
  allow_deletions: .allow_deletions.enabled,
  block_creations: .block_creations.enabled,
  required_conversation_resolution: .required_conversation_resolution.enabled,
  lock_branch: .lock_branch.enabled,
  allow_fork_syncing: .allow_fork_syncing.enabled
}' protection-backup.json > protection-restore.json
```

`required_signatures` в теле отсутствует **намеренно**: замена его не принимает. Сегодня он
`enabled: false`, поэтому восстанавливать нечего; будь он включён — потребовался бы
отдельный вызов его под-ресурса, и это надо проверить снимком, а не памятью.

**Под-ресурс есть не у всякой группы, и это решает выбор пути.** Из десяти настраиваемых
групп собственный адрес имеют **три** — `enforce_admins`, `required_pull_request_reviews`,
`required_signatures` (у них в ответе есть поле `url`). У `allow_force_pushes` его **нет**,
значит тронуть эту группу иначе, чем полной заменой, невозможно **by construction**, а для
полной замены тело неоткуда взять, кроме снимка. Предикат:

```sh
gh api repos/PRO-Robotech/kaname/branches/main/protection \
  | jq -r 'to_entries[] | select(.key!="url")
           | "\(.key)\t\(if (.value|type=="object") and (.value|has("url")) then "под-ресурс" else "только полная замена" end)"'
# → 10 строк: 3 «под-ресурс», 7 «только полная замена»
```

---

## 4. Сценарии

Идентификатор — `KAN-REPO-1-NN`. Производитель каждого «Тогда» назван в §6.

**Номер — идентичность, а не позиция.** Сценарии 29 и 30 заведены кругом 2 и стоят по
смыслу — рядом с теми, к которым относятся, — а не в конце. Переномеровать существующие
значило бы сделать ложными ссылки на них в §3, §6 и §9.

### S1. Имя: один живой претендент

#### KAN-REPO-1-01 (отрицательный) — прежнее имя не разрешается в самостоятельный репозиторий

**Дано** организация `PRO-Robotech`

**Когда** спрошено `gh api repos/PRO-Robotech/kacho-iam --jq .full_name`

**Тогда** ответ — `PRO-Robotech/kaname`
**И** это перенаправление, а не совпадение имён: `gh repo view PRO-Robotech/kacho-iam --json name`
отдаёт `kaname`

#### KAN-REPO-1-02 (положительный) — у победившего имени настройки заведены

**Дано** `PRO-Robotech/kaname`

**Когда** спрошены его настройки

**Тогда** `gh api repos/PRO-Robotech/kaname --jq .delete_branch_on_merge` → `true`
**И** `gh repo view PRO-Robotech/kaname --json isArchived` → `false`
**И** `gh api repos/PRO-Robotech/kaname/branches/main/protection` отвечает **не 404 и не 403**
**И** в ответе `enforce_admins.enabled=true`, `allow_force_pushes.enabled=false`,
`allow_deletions.enabled=false`

#### KAN-REPO-1-03 (положительный) — у проигравшего имени стоит признак архива

**Дано** прежний полирепо домена

**Когда** спрошен `gh repo view PRO-Robotech/kacho-iam-polyrepo-archive --json isArchived,visibility,pushedAt`

**Тогда** `isArchived=true`
**И** `visibility=PUBLIC` — архив читается, но не принимает записи
**И** `pushedAt=2026-07-15T18:50:36Z`

#### KAN-REPO-1-04 (положительный) — клон по прежнему адресу отдаёт СЕГОДНЯШНЮЮ службу

**Дано** прежний публичный адрес `https://github.com/PRO-Robotech/kacho-iam.git`

**Когда** спрошены его ссылки — `git ls-remote --heads <прежний адрес>`

**Тогда** ответ побайтово равен ответу `git ls-remote --heads https://github.com/PRO-Robotech/kaname.git`
**И** среди ссылок — `main` со значением `3c21a59f8c3a36a070aadec2b1e62716578632f5`
**И** состояния 2026-07-15 по прежнему адресу больше нет

#### KAN-REPO-1-05 (отрицательный) — описание архива называет адрес, который больше не обозначает его предмет

**Дано** `PRO-Robotech/kacho-iam-polyrepo-archive`

**Когда** прочитано его описание

**Тогда** в нём стоит «продукт для отдельной поставки — PRO-Robotech/kacho-iam»
**И** этот адрес перенаправляет на `kaname` (сценарий 01), то есть строка утверждает о
дереве неправду
**И** это находка, а не стиль: исход — правка описания (§7)

#### KAN-REPO-1-06 (отрицательный) — прежнее имя нельзя спрашивать о свойствах архива

**Дано** привычка обращаться к архиву прежним именем

**Когда** спрошено `gh api repos/PRO-Robotech/kacho-iam/branches --jq length`

**Тогда** ответ **2** — это ветки `kaname`, а не 15 веток архива
**И** правильный вопрос — `gh api repos/PRO-Robotech/kacho-iam-polyrepo-archive/branches --jq length` → **15**
**И** перенаправление отвечает молча и правдоподобно, поэтому различать обязательно

### S2. История: сегодняшнее состояние — снимок

#### KAN-REPO-1-07 (отрицательный) — история домена в опубликованном репозитории отсутствует

**Дано** свежий клон `PRO-Robotech/kaname`

**Когда** спрошено `git rev-list --count main`

**Тогда** ответ **2**
**И** корневой коммит `5618c88` «Kachō IAM — начальный импорт» несёт **2188 файлов, +764 531**
**И** второй коммит `3c21a59` «Дерево службы приведено к монорепо» — 2054 файла, +51 097 −12 998
**И** предикат задачи («1 при снимке») не воспроизводится буквально: коммитов **два** —
снимок и последовавшая за ним синхронизация с монорепо, а не один

### S3. Раскол — точная проекция, а не пересказ

#### KAN-REPO-1-08 (отрицательный) — раскол не открывается на копии без выкладки

**Дано** копия ствола продукта **без рабочей выкладки** (голое хранилище либо клон
`--no-checkout`)

**Когда** позван `git subtree split -P services/iam -b iamhist`

**Тогда** код возврата **1**
**И** сообщение — `fatal: <путь>/git-subtree cannot be used without a working tree.`
**И** ни одной ветки не создано (`git rev-parse --verify -q iamhist` пусто)

> Полоса названа отдельно, потому что именно в неё упирается шаг 1 порядка: копия ствола
> заводится дёшево без выкладки, а раскол её требует. Сообщение здесь **верное** и называет
> верное лекарство — сделать выкладку; ловушка не в нём, а в соседней полосе (сценарий 29).
>
> **Текст локализован.** Под русской локалью то же сообщение приходит как «критическая
> ошибка: … нельзя использовать не имея рабочего каталога». Утверждать надо под
> `LC_ALL=C`, иначе кейс краснеет от настроек машины, а не от предмета.

#### KAN-REPO-1-29 (отрицательный) — выкладка есть, а пути нет: сообщение зовёт НЕ туда

**Дано** копия ствола продукта **с рабочей выкладкой**
**И** путь, которого в ней нет

**Когда** позван `git subtree split -P services/nosuch -b probe`

**Тогда** код возврата **1**
**И** сообщение — `fatal: 'services/nosuch' does not exist; use 'git subtree add'`
**И** ни одной ветки не создано

> **Вот здесь ловушка.** `git subtree add` **вносит чужое поддерево в текущее дерево** — то
> есть делает противоположное расколу. Исполнивший подсказку получит коммит, которого не
> хотел, в копии, из которой собирался только читать. Верное лекарство — проверить путь
> (`git ls-tree -r HEAD --name-only | grep '^services/'`), а не менять команду.
>
> Две полосы разведены намеренно: у них **разные** «Дано», **разные** сообщения и **разные**
> лекарства, а код возврата и «ветки не создано» у обеих одинаковы — то есть по коду их не
> различить, и кейс, утверждающий только код, сошёл бы за оба сразу.

#### KAN-REPO-1-09 (положительный) — раскол даёт 894 коммита

**Дано** копия ствола продукта на `a10bfd208d4dc556c2a4c75ec813edb60f350b5d` с выкладкой

**Когда** позван `git subtree split -P services/iam -b iamhist`

**Тогда** код возврата **0**
**И** `git rev-list --count iamhist` → **894**
**И** `git rev-list --count --no-merges iamhist` → **741**, `--merges` → **153**

#### KAN-REPO-1-10 (положительный) — дерево корня раскола равно дереву службы на импорте

**Дано** расколотая история

**Когда** сверены деревья корня раскола и первого коммита монорепо, тронувшего путь

**Тогда** `git rev-parse <корень раскола>^{tree}` = `git rev-parse 5dbdbf6907:services/iam`
= `d8773438a5150d4f6e934b851fd65c2146672be6`

#### KAN-REPO-1-11 (положительный) — дерево вершины раскола равно дереву службы на стволе

**Дано** та же расколотая история

**Когда** сверены деревья вершины раскола и `services/iam` на `origin/main`

**Тогда** оба равны `caa65753ca250a8ac1e17140c771b50e6ef35125`
**И** это и есть доказательство, что раскол ничего не теряет и ничего не добавляет:
меняется форма истории, не содержимое

#### KAN-REPO-1-12 (отрицательный) — предикат «должно быть 866» красен на верном расколе

**Дано** верно исполненный раскол

**Когда** его результат сверен с числом, названным телом задачи

**Тогда** `git rev-list --count iamhist` → 894, а ожидалось 866
**И** ни одно из трёх чисел не ошибочно: 866 верно для `1b8f467fb4`, 880 — для сегодняшнего
ствола той же командой, 894 — для раскола
**И** предикат сверки обязан называть **и число, и ревизию, и команду** (§9)

### S4. Прививка — что она даёт и чего не даёт

#### KAN-REPO-1-13 (положительный) — прививка даёт 961 коммит

**Дано** расколотая история и клон архива

**Когда** корень раскола получает родителем вершину архива —
`git replace --graft <корень раскола> c744f9561`

**Тогда** `git rev-list --count iamhist` → **961**
**И** самый ранний коммит — `1a4dbb2a7` от **2026-06-28**
**И** 961 = 894 + 67, где 67 = `git rev-list --count main` в клоне архива
**И** состав сходится по обеим частям: слияний 158 = 153 + 5, без слияний 803 = 741 + 62

#### KAN-REPO-1-14 (положительный) — прививка не меняет содержимого

**Дано** привитая история

**Когда** спрошено дерево вершины

**Тогда** `git rev-parse iamhist^{tree}` → `caa65753ca250a8ac1e17140c771b50e6ef35125`
**И** это то же значение, что до прививки (сценарий 11)

#### KAN-REPO-1-15 (отрицательный) — замена есть ВИД, а не история

**Дано** привитая через `git replace` история

**Когда** тот же счёт спрошен без учёта замен

**Тогда** `git --no-replace-objects rev-list --count iamhist` → **894**, не 961
**И** отправка такой истории в чужой репозиторий даст **894**, потому что ссылки замен не
уезжают вместе с коммитами
**И** значит шаг материализации (§3.5, шаг 3) обязателен и не является формальностью

#### KAN-REPO-1-16 (положительный) — шов когерентен по раскладке

**Дано** вершина архива и дерево службы на импорте

**Когда** сопоставлены их пути

**Тогда** путей в архиве **918**, на импорте **916**
**И** все **916** путей импорта присутствуют в архиве
**И** лишних в архиве ровно **2**: `go.mod` и `go.sum` — сняты при слиянии в единый модуль
**И** путей, которых нет в архиве, — **0**

#### KAN-REPO-1-17 (отрицательный) — шов не бесплатен

**Дано** те же два дерева

**Когда** сопоставлены пары «путь + объект», а не только пути

**Тогда** совпало **377** из 916
**И** значит 539 файлов приписываются коммиту импорта, и `blame` пересекает шов только для
строк, доживших без изменения

#### KAN-REPO-1-18 (отрицательный) — прививка НЕ убирает стену

**Дано** привитая история

**Когда** осмотрен её самый ранний коммит

**Тогда** `git show --stat --format= 1a4dbb2a7 | tail -1` → `883 files changed, 269449 insertions(+)`
**И** стена `blame` не исчезает, а сдвигается с 2026-07-15 на 2026-06-28 — на **17 дней**

#### KAN-REPO-1-19 (положительный) — прививка окупается измеримо

**Дано** привитая история и перечень путей, доживших от эры архива

**Когда** для каждого дожившего файла `.go` посчитаны строки с меткой времени раньше 2026-07-15

**Тогда** файлов осмотрено **149**, строк всего **23 395**, приписано эре до 2026-07-15 —
**14 321**
**И** это 61 % строк доживших файлов и 3,3 % корпуса Go службы (436 916 строк)

#### KAN-REPO-1-20 (отрицательный) — охват прививки ограничен переработкой домена

**Дано** 918 путей вершины архива и 2394 пути сегодняшнего дерева

**Когда** взято их пересечение

**Тогда** дожило **228** путей, из них `.go` — **149**
**И** 690 путей эры архива до сегодняшнего дерева не дожили

### S5. Необратимое: окно, защита, работа в единственном экземпляре

#### KAN-REPO-1-21 (положительный) — окно переписи открыто

**Дано** `PRO-Robotech/kaname` сегодня

**Когда** спрошены теги, выпуски, форки и потребители модуля

**Тогда** `git ls-remote --tags https://github.com/PRO-Robotech/kaname.git | wc -l` → **0**
**И** `gh api repos/PRO-Robotech/kaname/releases --jq length` → **0**
**И** `gh api repos/PRO-Robotech/kaname --jq '[.forks_count,.stargazers_count,.subscribers_count]'`
→ `[0,0,0]`
**И** в дереве продукта объявлений модулей два, и ни одно не **требует**
`github.com/PRO-Robotech/kaname` — оно только объявляется путём модуля службы

#### KAN-REPO-1-30 (положительный) — материализация замены ИСПОЛНИМА, и вот чем

**Дано** привитая через `git replace` история (сценарий 13)
**И** средства `git-filter-repo` в окружении **нет** (`command -v git-filter-repo` пусто)

**Когда** позвана материализация —
`FILTER_BRANCH_SQUELCH_WARNING=1 git filter-branch -f -- iamhist`

**Тогда** код возврата **0**
**И** `git rev-list --count iamhist` → **961**
**И** `git --no-replace-objects rev-list --count iamhist` → **961** тоже — то есть история
стала историей, а не видом (контроль: до материализации та же команда давала **894**)
**И** дерево вершины по-прежнему `caa65753ca250a8ac1e17140c771b50e6ef35125`
**И** самый ранний коммит по-прежнему датирован 2026-06-28
**И** из 67 коммитов эры архива уцелело **2** (`1a4dbb2a7`, `0ec2b0fd7`), из 894 коммитов
раскола — **0**

> Сценарий заведён кругом 2 и закрывает разрыв, который был виден только вместе: 13–15
> доказывали, что материализация **обязательна**, и ни один не доказывал, что она
> **выполнима**. Шаг 3 порядка без названного средства был неисполним, а §5.5 при этом сама
> требует называть средство, которое есть.
>
> Замер этого прогона — **28 с** на дереве из 961 коммита; величина названа не как норматив,
> а чтобы шаг не считали мгновенным и не бросали на полпути. Она зависит от машины: тот же
> опыт у рецензента дал 13,6 с.

#### KAN-REPO-1-31 (отрицательный) — посадка защиты шире трёх полей, и три из десяти групп имеют свой адрес

**Дано** защита `main` у `PRO-Robotech/kaname`

**Когда** прочитан её объект целиком

**Тогда** верхнеуровневых ключей **11**, из них настраиваемых групп — **10** (`url` группой
не является)
**И** собственный адрес (поле `url` внутри группы) имеют ровно **3**: `enforce_admins`,
`required_pull_request_reviews`, `required_signatures`
**И** у `allow_force_pushes` его **нет** — значит тронуть эту группу иначе, чем полной
заменой, невозможно by construction
**И** `required_pull_request_reviews` **присутствует** сегодня, а `required_signatures`
выключен (`enabled: false`)
**И** `ci-bootstrap` не защищена вовсе (`protected: false`), правил ветвей у репозитория
**0** — то есть `allow_deletions: false` держит только `main`

#### KAN-REPO-1-32 (отрицательный) — условие из трёх полей НЕ КРАСНЕЕТ на потере семи групп

**Дано** снимок посадки и восстановление, в котором выставлены только три поля —
`enforce_admins`, `allow_force_pushes`, `allow_deletions`

**Когда** оба состояния сверены **старым** условием из трёх полей и **новым** — по объекту
целиком

**Тогда** старое условие даёт `[true,false,false]` на **обоих** — то есть проходит
**И** новое даёт непустое расхождение и называет потерянные группы: их **7** из 10, среди
них `required_pull_request_reviews` и `required_signatures`
**И** положительный контроль: то же новое условие на снимке против живого объекта молчит

```sh
canon() { jq -S 'walk(if type=="object" then del(.url) else . end)' "$1"; }
diff <(canon protection-backup.json) <(canon <(gh api …/branches/main/protection))
```

> Это и есть предмет блокирующего круга 2: предикат готовности **не способен покраснеть**
> на дефекте, ради которого стоит. Потеря требования пул-реквеста означает, что прямая
> отправка в `main` службы становится возможной для всех, а условие при этом проходит.
> Заметить такое можно только отдельным вопросом к API, который никто не задаст.

#### KAN-REPO-1-33 (отрицательный) — перепись снимает подписи, и восстановить их нечем

**Дано** архив полирепо-эры и привитая материализованная история

**Когда** в обоих посчитаны коммиты, несущие заголовок подписи

**Тогда** в архиве подписано **45** из 67
**И** в результате переписи — **0** из 961
**И** два уцелевших хеша (`1a4dbb2a7`, `0ec2b0fd7`) — ровно те два первых коммита, что
подписи **не несут**; третий (`490895355`, 2026-06-29) подписан, и с него цепочка
расходится
**И** восстановить подписи нечем: ключей подписантов у нас нет

```sh
git rev-list main | while read c; do git cat-file commit $c | head -20 | grep -q '^gpgsig' && echo s; done | wc -l
```

#### KAN-REPO-1-22 (отрицательный) — отправка переписанной истории отвергается защитой

**Дано** защита `main` у `kaname` в сегодняшнем виде

**Когда** спрошено `gh api repos/PRO-Robotech/kaname/branches/main/protection --jq .allow_force_pushes.enabled`

**Тогда** ответ **false**
**И** значит шаг 5 порядка (§3.5) неисполним без временного снятия защиты, а шаг 6 —
восстановления — обязателен и не является уборкой

#### KAN-REPO-1-23 (отрицательный) — `ci-bootstrap` несёт работу, которой больше нигде нет

**Дано** ветки `kaname`

**Когда** сопоставлены их пути с расколотой историей

**Тогда** ветка **разошлась**, а не отстала: `git rev-list --count main..ci-bootstrap` → **2**
**И** `git rev-list --count ci-bootstrap..main` → **1**
**И** `git merge-base main ci-bootstrap` → `5618c88` — **корень снимка**, то есть общее у неё
с `main` только начало
**И** путей у ветки 2194, вне расколотой истории — **1216**, но из них **1210** были в
корневом снимке (прежняя раскладка, не работа), а уникальны — **6**
**И** все шесть отсутствуют у `kaname/main` (проверено поимённо, 6 из 6) и у `services/iam`
(0 путей `.github/`)
**И** значит ветку трогать нельзя ни при каком шаге порядка, а «1216» само по себе работой
единственного экземпляра **не является** — это число прежней раскладки

#### KAN-REPO-1-24 (положительный) — работа `kaname/main` не пропадает при переписи

**Дано** `kaname/main` и расколотая история

**Когда** сопоставлены их пути

**Тогда** путей, которые есть у `kaname/main` и отсутствуют в расколе, — **0**
**И** обратно раскол несёт **44** пути, которых у `kaname/main` нет: он новее на два дня
**И** значит перепись `main` не уничтожает содержимого, а заменяет его более поздним из
ствола

#### KAN-REPO-1-25 (отрицательный) — окно закрывается публикацией версии, и это необратимо

**Дано** опубликованный тег версии службы

**Когда** после него выполнена перепись истории

**Тогда** коммит, названный псевдоверсией у потребителя, становится недостижимым
**И** розданное модуль-прокси не отзывается
**И** производителя у этого «Тогда» в дереве **нет** — это свойство внешнего прокси (§6)

### S6. Сверка после исполнения

#### KAN-REPO-1-26 (положительный) — счёт в новом репозитории сходится с объявленным

**Дано** перепись посажена в `kaname/main`

**Когда** сделан свежий клон и спрошен счёт

**Тогда** `git rev-list --count HEAD -- .` → **961**
**И** `git --no-replace-objects rev-list --count HEAD` даёт то же число: замены
материализованы, а не оставлены видом
**И** самый ранний коммит — от 2026-06-28

#### KAN-REPO-1-27 (положительный) — содержимое не изменилось переписью

**Дано** тот же клон

**Когда** сверено дерево вершины с деревом службы на стволе продукта той ревизии, из которой
сделан раскол

**Тогда** оба равны
**И** это единственная сверка, которая отличает перепись от подмены содержимого

#### KAN-REPO-1-28 (отрицательный) — предмет соседней задачи шире этой

**Дано** организация `PRO-Robotech`

**Когда** посчитаны зеркала прежнего полирепо

**Тогда** их **13**, заархивировано **1**, незаархивировано **12**
**И** это подтверждает число тела задачи
**И** это **не** предмет данной приёмки: она решает судьбу одного имени, а не двенадцати
(§8)

---

## 5. Что отвергнуто и по какой цене

### 5.1. Оставить снимок как есть — отвергнуто

Довод «дерево верно, история не нужна» защитим ровно до первой отладки. Цена: `git blame`
отвечает одной из двух дат на 764 531 строку; «когда и зачем это появилось» становится
вопросом к монорепо, у которого другой путь и другие хеши, то есть ответ приходится
восстанавливать вручную. Отдельно: исход **не истекает сам** — чем дольше он живёт, тем
дороже перепись (§3.4).

### 5.2. Перепись «когда-нибудь потом» — отвергнуто по цене окна

Сегодня перепись не рвёт ничего измеримого (сценарий 21). После первого опубликованного
тега она рвёт розданное, а розданное не отзывается. Значит «потом» — это не отсрочка, а
**другой** исход, и его цена не равна цене сегодняшнего.

### 5.3. Прививка отдельной, второй переписью — отвергнуто

Довод «сперва раскол, прививку добавим позже» стоит **двух** снятий защиты ствола и двух
переписей опубликованной истории вместо одной, ради 7 % коммитов. Вторая перепись при этом
попадает в то же окно риска, что первая, но уже с меньшим запасом.

### 5.4. Прививка ВМЕСТО раскола — отвергнуто

67 коммитов эры архива без 894 коммитов эры монорепо дают историю, обрывающуюся 2026-07-15,
и стену ровно там, где сегодня, только с другой стороны. Ценность решения — в расколе.

### 5.5. `filter-repo` как обязательное средство — отвергнуто, инструмента нет

`git-filter-repo` в этом окружении отсутствует (`command -v git-filter-repo` → пусто);
`git subtree` и `git filter-branch` присутствуют. Приёмка называет средство, которое
**есть**, а не то, которое принято рекомендовать.

Материализация замены выполняется парой «`git replace --graft` →
`FILTER_BRANCH_SQUELCH_WARNING=1 git filter-branch -f -- <ветка>`», и она **проверена
исполнением** (сценарий 30): код 0, 28 с, 961 и с учётом замен, и без него. Предупреждение
`filter-branch` о том, что средство устарело, гасится переменной осознанно — альтернативы в
окружении нет, а молчаливое предупреждение в выводе необратимого шага хуже явного глушителя.

**Круг 1 этого средства не называл, и это было блокирующим:** шаг 3 порядка требовал
«материализовать», а команды не было ни в одном разделе. Сценарии 13–15 доказывали, что
материализация обязательна, и ни один — что она выполнима. Правило §5.5 существовало и к
собственному единственному шагу без средства применено не было.

### 5.6. Оживить прежнее имя ради непрерывности истории — отвергнуто решением 1

Это исход 4 §3.1, и он возвращает исход 1 §2.1: имя домена вместо имени продукта. Прививка
даёт ту же непрерывность, не требуя сохранять имя, — то есть решение об имени и решение об
истории **разделимы**, и разделять их надо, иначе одно тянет за собой другое без довода.

### 5.7. Переписать 652 упоминания `kacho-iam` в корпусе — отвергнуто

Записки свидетельствуют о сделанном. Переименование сделало бы ложными утверждения, которые
были верны (репозиторий с таким именем существовал, и по этому адресу лежало то, о чём
записка говорит). Правится только то, что **предписывает** и адресует вперёд (§8, п. 2).

### 5.8. Снятие защиты удалением объекта — отвергнуто как НАЧАЛЬНАЯ ступень

Круг 1 писал шаг 4 как удаление объекта защиты. Замер показывает, чем это платится, и даёт
более дешёвый путь. Ось цены одна и измерима: **сколько настраиваемых групп из десяти
открыто на время окна**.

| ступень | открыто групп из 10 | что становится возможно | вызовов | обратима |
|---|---:|---|---:|---|
| **1** — полная замена, `allow_force_pushes: true` | **1** | принудительная отправка | 1 `PUT` | да, тем же `PUT` из снимка |
| **2** — то же плюс `required_pull_request_reviews: null` | **2** | ещё и отправка без пул-реквеста | +1 `PUT` | да |
| **3** — удаление объекта (`DELETE`) | **10** | ещё и **удаление ветки** `main`, и снятие `enforce_admins` | 1 `DELETE` | да, но объект собирается заново целиком |

**Выбрана ЭСКАЛАЦИЯ, начиная со ступени 1.** Довод не вкусовой: ступень 3 снимает среди
прочего `allow_deletions: false`, а это единственное, что сегодня держит саму ветку `main`
службы (`ci-bootstrap` не защищена вовсе — `gh api …/branches/ci-bootstrap --jq .protected`
→ `false`, правил ветвей у репозитория **0**). То есть широкая ступень открывает не только
то, ради чего её берут.

**Что здесь НЕ измерено, и это названо прямо.** Пройдёт ли принудительная отправка на
ступени 1 — то есть блокирует ли присутствующий `required_pull_request_reviews` прямую
отправку в ветку, — **я не проверял**: проверка требует тронуть защиту, а это действие
наружу. Это гипотеза о чужом механизме, взятая из его назначения, а не из его поведения, —
ровно тот класс, на котором ошибаются чаще всего. Поэтому порядок **решает исходом, а не
верой**: ступень 1 применяется, отправка пробуется, и только на наблюдённом отказе
берётся ступень 2, затем 3. Каждое расширение — один вызов, обратимый тем же снимком;
снимок снимается **однажды**, шагом 4а.

**Как выглядит отказ ступени 1 — и чем он отличается от опечатки.** Оператор обязан
различить их **до** того, как расширит окно: расширять окно из-за собственной опечатки
значит платить за неё периметром.

| источник отказа | где происходит | как выглядит | что делать |
|---|---|---|---|
| **защита ветки** | на сервере, после приёма ссылок | строка с префиксом `GH006` («protected branch update failed») и причиной; ветка названа | ступень эскалации расширяется |
| **опечатка в ссылке-источнике** | **локально, до сети** | `error: src refspec <имя> does not match any` (код 1) | чинится ссылка, окно не трогается |

Различитель измерен на месте, обе стороны:

```sh
git push <адрес> nosuchref:refs/heads/main
#   error: src refspec nosuchref does not match any        ← локально, сервер не спрошен
git push <адрес> iamhist-mat:refs/heads/main
#   код 0; в приёмнике rev-list --count = 961              ← форма ссылки верна
```

Первая строка **никогда** не содержит `GH006`: до сервера дело не доходит. Значит правило
простое — нет `GH006`, значит защита ни при чём.

**Вид отказа самой защиты здесь НЕ измерен**, и это сказано прямо: измерить его можно только
попыткой отправки в защищённую ветку, то есть действием наружу. Форма `GH006` названа как
**ожидаемая**, а решающий признак — от кого пришёл отказ (сервер против локального git), — и
он измерен.

Цена эскалации названа честно: лишний круг стоит одного вызова API и одной отвергнутой
отправки. Цена обратного порядка — окно, в котором ветку `main` можно удалить, и оно
берётся ради экономии этого круга.

---

## 6. Перечень «сценарий → производитель»

| сценарий | производитель | что именно производит |
|---|---|---|
| 01, 06 | внешний: GitHub API, `gh api repos/PRO-Robotech/kacho-iam --jq .full_name` | строку `PRO-Robotech/kaname` — разрешение перенаправления |
| 02 | внешний: `gh api repos/PRO-Robotech/kaname`, `.../branches/main/protection` | `delete_branch_on_merge`, `isArchived`, четыре поля защиты ствола |
| 03 | внешний: `gh repo view PRO-Robotech/kacho-iam-polyrepo-archive --json isArchived,visibility,pushedAt` | `true`, `PUBLIC`, `2026-07-15T18:50:36Z` |
| 04 | внешний: `git ls-remote --heads <адрес>` по обоим адресам | два побайтово равных перечня ссылок с `main = 3c21a59f…` |
| 05 | внешний: `gh repo view … --json description` | строку описания, называющую `PRO-Robotech/kacho-iam` |
| 07 | `git` в клоне `kaname`: `rev-list --count`, `show --stat` | `2`; `2188 files changed, 764531 insertions(+)`; `2054 files changed, 51097 insertions(+), 12998 deletions(-)` |
| 08 | `git subtree split` в копии **без выкладки**, под `LC_ALL=C` | `fatal: <путь>/git-subtree cannot be used without a working tree.`, код 1, ветки нет |
| 29 | `git subtree split -P <несуществующий путь>` в копии **с выкладкой**, под `LC_ALL=C` | `fatal: '<путь>' does not exist; use 'git subtree add'`, код 1, ветки нет |
| 30 | `git filter-branch -f -- <ветка>` после `git replace --graft`; плюс `git rev-list --count` в обеих формах и `comm` по спискам хешей | код 0; `961` и с заменами, и без них; дерево `caa65753ca…`; уцелевших хешей архива **2** из 67, раскола **0** из 894 |
| 09 | `git subtree split -P services/iam -b iamhist` + `git rev-list --count [--merges\|--no-merges]` | `894` / `153` / `741` |
| 10, 11, 27 | `git rev-parse <ревизия>^{tree}` и `git rev-parse <коммит>:<путь>` | `d8773438a5…` и `caa65753ca…` — по два равных значения |
| 12 | `git rev-list --count` на трёх ревизиях/единицах | `866` (`1b8f467fb4`), `880` (`origin/main`), `894` (раскол) |
| 13 | `git replace --graft` + `git rev-list --count`, `git log … \| tail` | `961`; `1a4dbb2a7 2026-06-28 chore: initial public release` |
| 14 | `git rev-parse iamhist^{tree}` до и после прививки | одно и то же `caa65753ca…` |
| 15 | `git --no-replace-objects rev-list --count` | `894` против `961` — вид против истории |
| 16, 20, 23, 24 | `git ls-tree -r … --name-only` + `comm` (сортировка `LC_ALL=C`) | `918`/`916`/`916`/`0`/`2`; `228`/`149`; `1216`; `0`/`44` |
| 17 | тот же `comm` по парам «объект + путь» | `377` из 916 |
| 18 | `git show --stat --format= 1a4dbb2a7 \| tail -1` | `883 files changed, 269449 insertions(+)` |
| 19 | `git blame -M --line-porcelain iamhist -- <файл>` + счёт `author-time` | `файлов 149, строк 23395, эры до 2026-07-15 — 14321` |
| 21 | внешние: `git ls-remote --tags`, `gh api …/releases`, `gh api repos/PRO-Robotech/kaname`; плюс `git ls-tree -r origin/main --name-only \| grep 'go\.mod$'` и чтение первой строки каждого | `0`; `0`; `[0,0,0]`; два объявления модулей, ни одного `require` на путь службы |
| 22 | внешний: `gh api …/branches/main/protection --jq .allow_force_pushes.enabled` | `false` |
| 31 | внешний: `gh api …/branches/main/protection`, `…/branches/ci-bootstrap`, `…/rulesets` + `jq to_entries` | `11` ключей / `10` групп / `3` с полем `url`; `protected:false`; `0` правил |
| 32 | `jq` + `diff` над снимком и его усечённой копией (инъекция в обе стороны) | старое условие `[true,false,false]` на обоих; новое — непустой `diff`, **7** потерянных групп; на неусечённом — пусто |
| 33 | `git cat-file commit` + счёт заголовка подписи по обоим деревьям | `45` из 67; `0` из 961; оба уцелевших хеша не подписаны |
| 23 | `git rev-list --count` в обе стороны, `git merge-base`, `git ls-tree -r` + `comm` против корневого снимка | `2` и `1`; `5618c88`; `2194` / `1216` / `1210` / `6` |
| 26 | `git rev-list --count HEAD -- .` и `git --no-replace-objects rev-list --count HEAD` в свежем клоне | два раза `961` |
| 28 | внешний: `gh repo list PRO-Robotech --limit 100 --json name,isArchived` + отбор | `13` / `1` / `12` |
| **25** | **производителя НЕТ** | «розданное модуль-прокси не отзывается» — свойство внешнего прокси Go; проверяется только наблюдением человека. Исход выбран явно: **заказать производителя** — §8, п. 4 |

**Перепись производителей — ДВЕ единицы счёта, и обе названы**, потому что одной здесь
недостаточно: строка таблицы покрывает группу сценариев, и «строк 23» читалось бы как
«пять сценариев без производителя».

| единица | значение | предикат |
|---|---:|---|
| сценариев (заголовков `#### KAN-REPO-1-NN`) | **33** | `grep -c '^#### KAN-REPO-1-'` |
| **воспроизведены исполнением** — производитель позван, вывод сверен | **30** | все, кроме 25, 26, 27 |
| производитель назван, **исполнение отложено** до посадки | **2** | 26, 27 — они утверждают о состоянии ПОСЛЕ необратимого шага 5 |
| **объявлен без производителя** | **1** | 25 |
| **строк** таблицы, без шапки и разделителя | **28** | часть строк покрывает группу: 01/06, 10/11/27, «16, 20, 23, 24» и другие |

**Круг 1 объявлял эту перепись двумя состояниями — «27 с производителем, 1 без», — и это
было неверно в обе стороны.** Оно скрывало, что два сценария производителя имеют, но
исполнены быть ещё не могут, и что один сценарий (08) производителя называл, а производил
тот **не то**, что кейс утверждал: у названного «Дано» приходит другое сообщение
(сценарий 29 — вторая полоса, заведённая тем же кругом). Три состояния вместо двух — не
педантизм: «объявлен без производителя» чинится заказом производителя, «производит не то» —
правкой кейса, а «исполнение отложено» не чинится вовсе и ждёт посадки.

**Перепись строк по виду производителя** (сумма — те же 28): внешний GitHub API — **7**;
внешний `git ls-remote` — **1**; смешанный (внешние вместе с локальным `git`) — **1**;
локальный `git` — **17**; локальный `jq`/`diff` над снимком — **1**; нет производителя — **1**.

**Ни один сценарий не называет гейта дерева продукта, и это не пропуск.** Предмет приёмки —
состояние **чужих репозиториев** и форма истории; свойством дерева ни то, ни другое не
является, поэтому производители здесь внешние by construction. Обратная сторона названа
честно: **ни один** сценарий этой приёмки не удержан механизмом, живущим в дереве, и держат
их порядок работ (§3.5) и сверка §9.

---

## 7. Что делать, если порядок нарушен

Наружу ведут четыре действия: архивация, заведение имени, отправка переписанной истории,
правка описания. Первые два уже исполнены (§1.3). По каждому нарушению назван не «откат», а
компенсация и её цена.

| нарушение | компенсация | цена |
|---|---|---|
| имя победителя занято **до** архивации проигравшего | архивация проигравшего под новым именем; перенаправление со старого адреса **не возникнет** — имя уже занято | ссылка на прежний адрес ведёт на победителя, но архив теряет перенаправление навсегда: его находят только по новому имени |
| проигравший заархивирован **без** переименования (исход 3 §2.1) | переименование архива и передача имени победителю | архив меняет адрес; все внешние ссылки на него ломаются — цена ровно та, что названа в §2.2 |
| **шаг 3 пропущен: отправлена непривитая история** | вторая перепись и вторая отправка | посажено **894** вместо 961 молча — код возврата нулевой, вершина верная, а эры архива нет; заметить можно только счётом (сценарий 15) |
| **отправка сделана `--mirror` либо `--all`** | восстановление `ci-bootstrap` из страховочного клона шага 0 | если клона нет — **шесть файлов оснастки конвейера утрачены**: их нет ни у `main` службы, ни в `services/iam`. Плюс `--all` толкает служебные ветки замера (`base`, `archmain`) в чужой репозиторий |
| **перепись отправлена до снятия защиты** | ничего: отправка не состоится, `main` не изменится | потерян заход; ущерба нет — это единственное безобидное нарушение |
| **защита не восстановлена после отправки** | полная замена из снимка (§3.6) | окно, в котором `main` принимает переписи, — и оно не наблюдаемо ничем, кроме вопроса к API |
| **посадка восстановлена ЧАСТИЧНО** (например, тремя полями) | повторная полная замена из снимка | потеряно **7** групп из 10, среди них требование пул-реквеста: прямая отправка в `main` открыта для всех, а условие из трёх полей это **проходит** (сценарий 32). Без снимка шага 4а восстановить нечем — прежние значения не хранятся нигде |
| **перепись отправлена после публикации версии** | только выпуск следующей версии; прежняя **не отзывается** | коммит, названный псевдоверсией, недостижим: у потребителя `go mod download` отказывает, и починить это может лишь он сам, сменив пин |
| **`ci-bootstrap` снята или перезаписана** | восстановление из клона `--mirror`, снятого шагом 0 | если клона нет — работа шести файлов CI утрачена: её нет ни в `kaname/main`, ни в `services/iam` |
| раскол сделан из копии, отставшей от ствола | повторить раскол на вершине; сверить оба дерева (сценарии 10, 11) | заход; необратимого нет, пока не сделан шаг 5 |
| прививка забыта, перепись отправлена | вторая перепись | второе снятие защиты и второе окно риска ради 7 % коммитов (§5.3) |
| описание архива не поправлено | правка описания | пока не поправлено, документ организации утверждает о дереве неправду; цена — чужое время на переход по адресу, который ведёт не туда |

**Практическое следствие.** Необратим ровно один шаг — отправка переписанной истории.
Шаги 0–4 и 7 повторяемы сколько угодно и ничего не стоят; шаг 5 не бывает бесплатным
никогда, а после публикации первой версии службы становится вредным.

---

## 8. Остатки: чего эта приёмка НЕ решает

Каждый пункт — задачей, а не обещанием.

1. **Двенадцать незаархивированных зеркал прежнего полирепо** (сценарий 28). Класс тот же,
   что решён здесь для одного имени, и шире его: у каждого зеркала свой живой адрес и своё
   описание. Предмет назван телом задачи #1763 и там же оставлен нерешённым; заводить
   его отдельной задачей.
2. **Два утверждения `.claude/rulebook/polyrepo.md` пережили свой предмет** (§1.6):
   «существуют и не заархивированы» — заархивирован один; «последний push — середина июля
   2026» — два зеркала двигались в августе. Правка корпуса — работа воркспейса, не этой
   приёмки.
3. **652 файла воркспейса упоминают `kacho-iam`.** Переписывать их запрещено (§5.7). Но
   отделить **свидетельство** от **предписания** никто не пробовал: строка раскладки
   каталогов в `README.md` и путь в `tests/authz-fixtures/README.md` адресуют вперёд, а не
   свидетельствуют. Разбор — отдельная задача.
4. **У сценария 25 нет производителя, и половину его предмета уже кто-то держит.**
   «Розданное прокси не отзывается» проверяется только наблюдением. Ближайший предмет —
   задача продукта [#2230](https://github.com/PRO-Robotech/kacho/issues/2230) («выпуск:
   псевдоверсия платформы в `go.mod` может указывать на коммит вне всех ссылок», OPEN):
   она заказывает гейт достижимости коммита, названного псевдоверсией. Это **половина**
   сценария 25 — та, где коммит становится недостижимым от переписи. Вторая половина —
   необратимость самой раздачи прокси — предмета не имеет и иметь не может: у неё нет
   производителя ни в каком дереве. Круг 1 ссылался на «`KAN-RELEASE-1`, остаток 1» вместо
   номера, и по такой ссылке проверить покрытие было нельзя.
5. **Шести уникальным файлам `ci-bootstrap` преемник не назначен.** Оснастка конвейера
   службы (`.github/workflows/ci.yml` и пять файлов рядом) существует **в одной ветке
   одного репозитория** и не поглощена ни `main` службы, ни `services/iam`. Порядок §3.5
   велит ветку не трогать — это защита, а не решение: пока эти шесть файлов не переехали
   куда-то, где их видит перепись, всякая уборка веток `kaname` их уничтожит. Заводить
   задачей.
6. **Номер и форма первой версии службы** — предмет `KAN-RELEASE-1`, не этой приёмки. Здесь
   от неё нужен один факт: перепись истории идёт **до** первого тега (§3.4).
7. **Регистрация в ведомости приёмок продукта не требуется**: миграций приёмка не заводит.
   Названо, чтобы отсутствие записи не приняли за пропуск.

---

## 9. Определение готовности стадии

**Каждому шагу §3.5 отвечает строка, но соответствие не взаимно-однозначно, и «1:1» здесь
стояло ошибочно.** Строки с «—» шага не имеют вовсе: они о состоянии, заведённом **до**
порядка (решение об имени) либо **после** него (задачи-преемники). Круг 3 добавил строки на
подшаги 4а и 4б, которых в перечне не было: без снимка условие №10 неисполнимо, а без него
самого — необнаружимо, какую ступень эскалации оператор взял.

| # | шаг §3.5 | условие | предикат |
|---:|:---:|---|---|
| 1 | — | решение об имени исполнено | `gh api repos/PRO-Robotech/kacho-iam --jq .full_name` → `PRO-Robotech/kaname` **и** `gh repo view PRO-Robotech/kacho-iam-polyrepo-archive --json isArchived` → `true` |
| 2 | — | настройки победившего имени заведены | `gh api repos/PRO-Robotech/kaname --jq .delete_branch_on_merge` → `true`; `.../branches/main/protection` — не 404 и не 403 |
| 3 | 0 | клон `--mirror` снят, `ci-bootstrap` в нём есть | `git -C <клон> rev-parse --verify ci-bootstrap` резолвится |
| 4 | 1 | раскол сделан и **сверен по обоим деревьям** | `git rev-parse iamhist^{tree}` = `git rev-parse <ревизия>:services/iam` **и** `git rev-parse <корень>^{tree}` = `git rev-parse 5dbdbf6907:services/iam` |
| 5 | 2 | прививка сделана | `git rev-list --count iamhist` → **961**; самый ранний коммит датирован 2026-06-28 |
| 6 | 3 | замена **материализована** названным средством | `FILTER_BRANCH_SQUELCH_WARNING=1 git filter-branch -f -- <ветка>` → код 0, и `git --no-replace-objects rev-list --count <ветка>` даёт то же число, что и с заменами (**961**) |
| 7 | **4а** | снимок посадки снят **до** всякого изменения | `test -s protection-backup.json` **и** `jq -e 'has("enforce_admins") and has("allow_force_pushes")' protection-backup.json` |
| 8 | **4б** | взята **названная** ступень эскалации, и это записано | в журнале исполнения назван номер ступени (1, 2 или 3) и вид отказа, на котором она расширена (§5.8) |
| 9 | 5 | перепись посажена **одной названной ссылкой** | отправка сделана формой `<ветка>:refs/heads/main`; в журнале отправки нет ни `--mirror`, ни `--all`, ни `--delete`; в свежем клоне `git rev-list --count HEAD -- .` → **961** |
| 10 | 6 | защита ствола восстановлена **целиком**, а не по трём полям | `diff` канонизованного живого объекта против `protection-backup.json` **пуст** (§3.6, сценарий 32). Условие из трёх полей отвергнуто: оно проходит при потере семи групп |
| 11 | 7 | содержимое не изменилось | дерево вершины в свежем клоне равно `services/iam` ствола той ревизии, из которой сделан раскол |
| 12 | 8 | `ci-bootstrap` цела | `git ls-remote --heads https://github.com/PRO-Robotech/kaname.git` называет `ci-bootstrap`; её значение равно снятому шагом 0, и шесть уникальных путей на месте |
| 13 | — | описание архива приведено к факту | в описании нет адреса `PRO-Robotech/kacho-iam` как имени живого продукта |
| 14 | — | задачи-преемники §8 (пп. 1, 2, 3, 5) заведены | номера названы в комментарии к #1763 |

**Число сверки объявляется вместе с ревизией и командой, а не одним числом.** Предикат
задачи требовал «866 при переносе»; он недостижим (§1.2, сценарий 12). Действующая форма:
**961** при расколе с прививкой от ревизии `a10bfd208d4dc556c2a4c75ec813edb60f350b5d`,
командой `git rev-list --count HEAD -- .` в свежем клоне; при расколе без прививки — **894**;
всякая другая ревизия ствола даёт другое число, и оно обязано быть названо вместе с ней.

**Что эта приёмка НЕ доказывает.** Она не исполняет ни одного действия наружу: репозиториев
не заводит, ничего не архивирует, истории не расщепляет и не отправляет. Раскол и прививка
проверены **исполнением в одноразовой копии**, поэтому числа §3 и §4 — замер, а не расчёт;
но посадка (шаг 5) не производилась, и сценарии 26, 27 остаются **непроверенными** до неё.
