# Пересверка классов на отпечаток ЗАМЫСЛА: LINE-A-1, редакция 12 — второй проход, предмет kacho-workspace#723

| поле | значение |
|---|---|
| **Issue** | PRO-Robotech/kacho#2564 (эпик); задача PRO-Robotech/kacho-workspace#723 (волна-1 #772) — «18 условий §2а адресуют дерево вне всех трёх стволов» |
| **Предмет** | `docs/specs/sub-phase-LINE-A-1-own-authorization-endpoint-and-code-design.md` (редакция 12, DRAFT) |
| **SHA-256 предмета** | `48cb2d72f29b8813a28f50589b06d526d2a2a682396e142b706fffc1577896b9` |
| **Приёмка** | `docs/specs/sub-phase-LINE-A-1-own-authorization-endpoint-and-code-acceptance.md`, SHA-256 `b755886fd3a9cbf4070598ad3d921bb92d53d7f4186832a2c34f1d8ca197a7ec` (APPROVED; перемерен, совпал) |
| **Роль** | class-exposure-analyst |
| **Режим** | ПЕРЕСВЕРКА на отпечаток замысла ДО его одобрения (`change-graph.md` §4) |
| **Actor** | pointpu (`docs/changes/policy.yaml` → `review_authority.class-exposure-analyst: [pointpu]`) |
| **Ревизии** | воркспейс — ветка `acceptance/line-a-1`, HEAD `8a123bba`, рабочее дерево чистое до этой записи. **kacho**: `origin/main` = `4382f05c0a3`; ветка эпика `2564` = `82afff04433` (пустой коммит поверх `b32ca653083`; **на origin её нет** — `gh api …/branches/2564` → 404, PR нет); объявленная замыслом база — `f445aaaa554`. **kaname**: `main` = `cbbac984`; ветка эпика `357` = `2f2415ec` (пустой коммит, содержимое = `main`; PR #359 открыт). **corelib**: `main` = `34bc8104`; ветка эпика `26` = `b35f9d25` (пустой коммит, содержимое = `main`; PR #28 открыт). Эталон источника для сверки справочных координат — исходный модуль `github.com/ory/fosite@v0.49.0` из кэша модулей (283 файла, 237 `.go` — совпало с §1.1 замысла) |
| **Снимок** | `sha256sum` обоих документов — первым действием; `git diff --name-only` между базой и веткой эпика: kaname **0**, corelib **0**, kacho **0** (ветка эпика против своей базы `b32ca653083`) |
| **Итог** | **unmapped: 22 из 22 пунктов первичного разбора отображены, но пересверка вскрыла ОДИН новый пункт — `RV-1`, порождённый решением З6, и в замысле ему не отвечает ни одно решение.** Маркеров отсрочки нет (предикат §9.2 дословно: **3 / 2**, все разобраны в §9.2). Нерешённых вопросов нет. **#723 этой редакцией не закрыт:** все 18 условий неадресуемы на всех шести ревизиях — это ожидаемо до посадки кода, — но из пяти пунктов предиката снятия #723 выполнен **один** (п. 4), частично — два (п. 2, п. 3), не выполнены — два (п. 1, п. 5); у исполнимой формы условий пять дефектов области и один дефект входа ратификации. Правок к замыслу по #723 — **девять** (Ф1–Ф9), по решениям диспетчера от 2026-09-22 — **три** (Д1–Д3), по core-no-prod снять — **два** пассажа |

---

## 0. Что эта запись делает и чем не является

На этот же отпечаток уже есть запись `48cb2d72….md` (пересверка ред. 12, итог «22 из 22, блокера
нет»). Журнал ревью только дополняется (`change-graph.md` §3), поэтому та запись **не правится**, а
эта ложится рядом под суффиксом задачи. Суффикс выводит имя из пространства `^[0-9a-f]{64}\.md$`
намеренно: прежняя запись остаётся свидетельством того, что видел её проход. **На отпечаток
`48cb2d72…` теперь две записи, и итог этой строже:** она вскрыла пункт `RV-1`, которого прежний
проход не видел. Читатель, выбирающий запись по имени `<отпечаток>.md`, найдёт прежнюю — и это
сказано здесь, чтобы расхождение не прошло молча.

Первичный разбор `initial/b755886f….md` (`LAX-1…LAX-20` и два сквозных) остаётся в силе:
приёмка не менялась. `SUPERSEDED.yaml` не тронут. Вердикта замыслу здесь нет. Решение несёт
строка «Итог».

---

## 1. Отображение: 22 из 22 — перемерено, а не унаследовано

Предикат полноты, объявленный самим замыслом (§3), прогнан дословно:
`for n in $(seq 1 20); do grep -c "LAX-$n\b" <файл>; done` → **ни одного нуля** (минимум 1 у
`LAX-1`, `LAX-5`, `LAX-9`, `LAX-17`, `LAX-19` — у них имя встречается ровно в своей строке §3).
Строже: перепись **строк** таблицы §3 по началу строки (`awk '/^## §3 /,/^## §4 /' | grep -cE
'^\| \*\*(LAX-[0-9]+|сквозное-[12])\*\*'`) → **22**, имена `LAX-1…LAX-20`, `сквозное-1`,
`сквозное-2`; строк с пустой ячейкой решения — **0**. Перечень первичного разбора — **20** имён
`LAX-*` (`grep -oE 'LAX-[0-9]+' initial/b755886f….md | sort -u | wc -l`), все присутствуют.

**Неотображённых из первичного перечня — ноль.** Неотображённый пункт в этой записи есть, но он
**новый** — раздел 5.

## 2. Маркеры отсрочки и нерешённые вопросы — их нет

Предикат §9.2 прогнан дословно, обе ступени: двухступенчатая очистка → `2 × «в следующей
редакции» · 1 × «позже»` (**3**), построчно → **2**. Совпадает с числом, объявленным ред. 12; все
три совпадения разобраны в §9.2 (две цитаты снятого маркера ред. 3, наречие порядка). Контроль
расширенным словарём (TBD, XXX, «на потом», «отложен*», «временно», «открытый вопрос»,
«нерешённ*») по тому же телу без §9.2: **14 попаданий, маркеров среди них 0** — «временно» дают
слова «одновременно/одновременных», остальные попадания говорят «вопроса нет» или «не отложено».
Вопросов со знаком `?` вне блоков кода — **3**, все в шапках таблиц либо риторические.

## 3. Применимые роли ревью — названы не все

| роль | в реестре | агент | в замысле | что не так |
|---|---|---|---|---|
| `design-reviewer` — событие DESIGN_APPROVED | да | нет, и это верно: событие человека, агентом не подменяется (`dispatcher.md`) | **только в перечне реестра** (§2д `SS-X1`) | держатель одобрения замысла **не назван** держателем; вердикты по замыслу выносил `system-design-reviewer`, а он в реестре — пост-дифф специалист распределённых свойств |
| `system-design-reviewer` | да | да | 36 вхождений: ратификатор §2а, §9.3 | — |
| `db-architect-reviewer` | да | да | 11 | — |
| `go-style-reviewer` | да | да | **только в перечне реестра** | применим к каждой Go-полосе (L0a, L0b, L1–L8, L11, L13), в колонке held-by §8 не назван ни разу |
| `proto-api-reviewer` | да | да | **только в перечне реестра** | неприменимость сказана прозой (З1: «нового proto-контракта не заводит»), а законна **только** по id `no-proto-in-change` с evidence (SDD-1: «свободный текст N/A не является evidence») |

По оси одобрения это **не блокер**: отсутствующего применимого **ревью** нет, недоназвана его
**применимость**. К замыслу: таблица «роль → полосы → основание или id неприменимости».

---

## 4. #723: восемнадцать условий, перемеренные поимённо

**Единица счёта** — условие §2а.0; **область осмотра** — шесть ревизий из шапки плюс эталон
источника для проверки области предиката. Предикат присутствия поддерева на каждой ревизии:
`git grep -l 'ory/fosite' <rev>` (весь ствол) · `git grep -l '^package fosite' <rev> -- '*.go'` ·
перепись путей по именам файлов движка (`flow_authorize_code_token`, `config_default`,
`client_authentication`, `authorize_helper`, `access_request_handler`,
`authorize_request_handler`, `hmacsha`) · пути с `oauthceremony` · файлы `PROVENANCE`.
**Все пять — 0 на каждой из шести ревизий** (kacho 6692/6691 путей, kaname 3493/3493,
corelib 473/473). **Положительный контроль прибора:** те же имена файлов на эталоне источника
находятся все семь, а `package fosite` — в корневом пакете. Прибор умеет сказать «да».

| усл. | что обходит предикат | дом и полоса, приносящая предмет | 6 ревизий сегодня | после одной L0a — вход ратификации по §2а.0 п. 1 | дефект формы предиката |
|---|---|---|---|---|---|
| **H1** | граф вызовов от нашего писателя ответа; поля подсказки/отладки источника | писатель — corelib, **L0b**; поля — поддерево, L0a | **нет** | половина: поля есть, корня обхода (писателя) нет | — |
| **H2** | отображения «отказ хранилища → исход обмена» против таксономии | таксономия — corelib **L0b**; отображение SQLSTATE → вид исхода — kaname **L2** (§2б) | **нет** | нет | у одного предиката два дома; прогон по corelib недобирает половину kaname |
| **H3** | сигнатуры чтения нашего storage-порта | corelib **L0b** | **нет** | нет | — |
| **H5** | инициализация полей нашей структуры настроек литералом; «загрузчик» | структура — corelib **L0b**; загрузчик — конфигурация kaname (§2б) | **нет** | нет | «загрузчик» живёт не в corelib |
| **H6** | ручка секрета сигнатур ≠ материалу подписи JWT | ручка — конфигурация kaname; материал — `kaname:internal/apps/kaname/config/token_signing.go` (`key-set-path`) + `internal/tokensigner` | **половина**: материал подписи есть @ `cbbac984` (= `357`); ручки секрета сигнатур церемонии нет ни в одном стволе (`git grep -il 'hmac\|global-secret' -- internal/apps/kaname/config/` → 1 непробный файл, `mtls.go`, и там только комментарии о секрете hook-ребра) | та же половина | см. Д3 |
| **M1** | узел сверки `redirect_uri` на обеих полосах; ветвь «пусто ⇒ пропустить» | corelib **L0b** | **нет** | нет; ветвь «пусто ⇒ пропустить» есть **в самом поддереве** (полоса обмена кода, сравнение с пустой строкой) | область не названа: «обе полосы» — наши или поддерева |
| **M2** | наш конструктор; «идентификатор нестрогого проверяющего в непробном дереве не встречается» | corelib **L0b** | **нет**; вторая половина сегодня истинна **вакуумно** — 0, потому что поддерева нет | **красный по построению**: поддерево само называет нестрогий проверяющий в **4** непробных файлах (объявление, умолчание геттера конфигурации, две полосы-обработчика) | область «непробное дерево» включает ввозимое поддерево |
| **M3** | ветвь «публичный ⇒ пропустить сверку» «в нашем пакете» | corelib **L0b** | **нет** (вакуумно) | **зелёный по построению** при живой ветви в поддереве (аутентификация клиента поддерева) | область слепа к месту, где ветвь живёт; условие A-1 («посев заводит клиента конфиденциальным») предикатом не выражено вовсе |
| **M4** | сверка проверочного значения на каждом пути аутентификации клиента | corelib **L0b** | **нет** | нет | — |
| **M5** | A-1: операций `jti` в порте — 0 | corelib **L0b** | **нет** (вакуумно 0 — это и есть пустой обход) | нет | — |
| **M6** | определений производителя сигнатуры предъявителя — 1 | corelib **L0b** | **нет** | нет | — |
| **M7** | sentinel'ы поддерева ⊆ видам исхода нашего порта | поддерево L0a + порт **L0b** | **нет** | половина | **признак «sentinel поддерева» синтаксически не определён**: разбор эталона даёт **2** (`errors.New` / `stderr.New` уровня пакета) либо **36** (с **34** значениями `&RFC6749Error`), а не **4** из §2а.2 |
| **M8** | определений полосы отзыва семейства — 1, вызывающих — 2 | corelib **L0b** | **нет** | нет | вызовы отзыва стоят в поддереве на **3** непробных путях (обмен кода, refresh, эндпоинт отзыва): по области «поддерево» число 1 ложно, по области «наш пакет» предикат этих путей не видит |
| **M10** | обёртка тела — предок вызова обработчика в композиционном корне | обёртка — corelib **L0b**; корень — `kaname:cmd/kaname/serve.go`, монтаж **L3/L4** | **половина**: величина `body-ceiling` есть (`kaname:internal/apps/kaname/config/client_token.go:60`) | нет | дом предиката — kaname, и ратификация «по поддереву» его не достаёт; координата `client_token.go:60` в замысле без каталога **двусмысленна** — в kaname два файла с этим именем |
| **M11** | «идентификатор величины источника в непробном дереве не читается; читается наша» | corelib **L0b** | **нет**; первая половина вакуумно истинна | **красный по построению**: поддерево читает эту величину в **4** непробных файлах (обработчик авторизации и три полосы OpenID) | как у M2 |
| **M13** | пометка = `PROVENANCE` = дифф против апстримного коммита | **L0a** | **нет** | два множества из трёх; третье — дифф против `653c812` — требует дерева **вне всех трёх стволов** и после L0a | **класс #723 переживает посадку L0a**: предикат исполним только из сети и по чужому дереву; пин содержимого (хэш модуля) и способ получения не названы |
| **S1** | геттеры `*Config` поддерева ↔ обязательное поле либо явная запись | L0a + **L0b** | **нет** | половина | — (числа 46 / 46 / 47 сверены на эталоне и сошлись) |

**Счёт.** Неадресуемы сегодня на всех шести ревизиях — **18 из 18**. Из них одна половина в наших
стволах есть у **2** (H6, M10), а **4** сегодня удовлетворены **вакуумно** (M2 и M11 — по половине,
M3, M5): их реализация без стража пустого обхода позеленела бы на дереве, где предмета нет.
**После одной L0a** — то есть на входе, который §2а.0 п. 1 назначает ратификации, — полностью
адресуемо **0** условий, частично **4** (H1, M7, M13, S1), **2** красные по построению (M2, M11).
Предмет **17 из 18** приносит **L0b**, а у **4** из них (H2, H5, H6, M10) вторая половина лежит в
kaname и садится полосами L2–L4. Только в L0a предмет у одного условия — M13.

**Корня обхода нет ни у одного условия.** Каталог поддерева в corelib и импорт-путь пакета
церемонии в замысле **не названы ни разу**: `grep -oE 'corelib/[A-Za-z0-9_./-]+'` находит только
`tokenpolicy`, `treehygiene` и `grpcsrv`, а `oauthceremony` встречается **2** раза и оба раза без
пути. «Обход под нашим путём пакета» (#723 п. 1) без названного пути исполнитель начинает с
догадки.

---

## 5. НЕОТОБРАЖЁННЫЙ ПУНКТ `RV-1` — вскрыт решением З6, решения под ним нет

**Решение замысла.** З6: движок вносится поддеревом исходников в модуль фундамента. Код остаётся
чужим по происхождению, но получает **наше имя модуля**.

**Класс.** `code-authoring` §«Инвариант и оснастка — это тоже прод-код»: оснастка обязана судить
по **идентичности**, а не по **имени**. Прибор уязвимостей сопоставляет записи базы с **путём
модуля** в графе сборки. Поддерево меняет имя и сохраняет идентичность, и прибор слепнет к коду
движка **по построению**. Признак виден уже в формулировке З6: «строки `require
github.com/ory/fosite` нет».

**Замер (контроль в обе стороны).** Индекс базы уязвимостей Go (`vuln.go.dev/index/modules`)
ведёт записи движка под путём `github.com/ory/fosite`: сегодня их **2** (`GO-2021-0109`,
`GO-2021-0110`, обе исправлены до `v0.49.0`). Граф сборки двух форм, собранных автором замысла для
§1.2 (`go list -deps -f '{{.Module.Path}}'`, без сети): у **формы А** в графе есть
`github.com/ory/fosite`, у **формы В** (поддерево) его **нет** — код движка числится за модулем
пробы. Значит строка §1.2 «вызываемых уязвимостей **0 / 0**» и ячейка «безопасность: да — 0
вызываемых уязвимостей» в §10 для формы В сравнивают видящий прибор со **слепым**. Сегодня числа
совпадают только потому, что действующих записей о `v0.49.0` нет. Первая будущая запись о движке
форме В не покажется **никогда**. Решение диспетчера 2026-09-22 называет это прямо: «наблюдение за
уязвимостями upstream обязано быть названо (govulncheck поддерево не видит)».

**Чем это не закрыто в замысле.** Замысел держит три других свойства поддерева: атрибуцию
(`NOTICE`, `PROVENANCE`), дрейф (883 пробы апстрима) и границу импорта (гейт §2в). Обнаружение
уязвимостей не держит **ни одно** его решение: слов «наблюдение», «OSV», «GHSA», «advisory» в теле
нет. §9.1 п. 2–3 отвечает на вопрос «как обновимся», а не «как узнаем, что пора».

**Что должно быть верно в коде и конвейере фундамента (условие `RV-1`):**
1. `PROVENANCE` несёт **машинно читаемо** путь модуля апстрима и его версию. Проверка
   уязвимостей поддерева читает их **оттуда**, а не из `go.mod`, и в конвейере фундамента краснеет
   на любой записи базы, покрывающей эту версию.
2. **Положительный контроль обязателен:** та же проверка, получившая пин версии с известной
   записью (например, версию младше исправления `GO-2021-0110`), даёт **красный**. Без него
   зелёный неотличим от слепоты. Это `gate-authoring`: проверка обязана уметь упасть на том, ради
   чего заведена.
3. Проверка **периодическая**, а не только на изменении поддерева. Поддерево не меняется
   (0 релизов в год — §1.1), а база уязвимостей меняется; проверка на коммит в таком режиме
   не исполнилась бы ни разу.
4. Строки §1.2 и §10 о формах сравнения называют слепоту прибора: у формы В число покрывает
   **только транзитивные модули**, код движка оно не покрывает. Иначе ячейка «безопасность: да» —
   текст, вводящий в заблуждение о безопасности (ban19 — блокирующий род).

**Чем будет держаться:** нужен гейт, его нет. Заказ — в разделе «Заказы».

---

## 6. Решения диспетчера 2026-09-22 против текста ред. 12

| # | решение | что в замысле | исход |
|---|---|---|---|
| **Д1** | «граф `ory/x` в фундамент не входит» | §1.2, §2в, §9.1 пп. 4–5: `ory/x` — **первый** из 8 модулей, доходящих до **производственного** графа фундамента | **расходится, несущее.** Радиус перемерен на эталоне: в 8 апстримных пакетах графа сборки (шесть импортируемых из §1.2 — корневой, `handler/oauth2`, `handler/openid`, `handler/pkce`, `token/hmac`, `token/jwt` — плюс `i18n` и `storage`) модуль `ory/x` импортируют **39** непробных файлов. Пакет `errorsx` — **39** файлов, `otelx` — **9**. `otelx` снимается нашей обёрткой телеметрии, `errorsx` — **нет**. Контроль: граф сборки **обеих** собранных форм несёт `github.com/ory/x`. Выполнение решения меняет перечень правок `PROVENANCE` (сегодня 14 файлов), повторяемую цену обновления (§9.1 п. 2) и предмет гейта §2в |
| **Д2** | «go-jose — только внутри движка» | гейт §2в прощает прямой импорт всех 8 модулей и из каталога поддерева, **и из пакета церемонии** | к замыслу: для `go-jose` послабление — только каталог поддерева. Сегодня `go-jose` — **0** в `go.mod` и **0** непробных импортёров во всех трёх стволах |
| **Д3** | «наши токены — JWT, подписывает только tokensigner» | H6 заводит **второй** подписывающий материал — HMAC-секрет сигнатур кода и refresh, отдельный от материала JWT | **вопрос диспетчеру:** распространяется ли решение на refresh и код. Если да — H6, стратегия HMAC источника и две величины S1 (глобальный секрет и его ротация) к A-1 не годятся, и refresh выпускается иначе. Если нет — так и сказать в §2а H6 |
| — | `tokenpolicy/policy.go:14–16` «общей делается ПОЛИТИКА, а не функция» | приём предъявителя и family-энфорсмент — на крае (§2б, L5/L7); в `tokenpolicy` уходит только константа (L0c) | **согласовано** |
| — | «стенд с консолью без входа не остаётся» | приёмка: поверхность провайдера снимается **последней** под-фазой Линии A, отдельным изменением; замысел садит `own` | **противоречия нет** |

## 7. core-no-prod — «к замыслу: снять», не блокирующие

1. **§9.1 п. 8** «Поведенческого паритета с развёрнутой Hydra `v26.2.0` НЕТ… следствие для
   мигрирующих клиентов». Прода нет — нет и мигрирующих клиентов. Контракт задаёт приёмка, пункт
   снимается целиком.
2. **§6, строка «тернарная посадка»** — «замысел принимает тернар как есть» (`Unset ≡ External`).
   Переходное значение сохраняется как допустимая посадка. К замыслу: A-1 опирается **только** на
   `own`, совместимости с `external` / `Unset` не держит, а снятие этих значений принадлежит эпику.

## 8. Смежное с #723: координаты `файл:строка` о НАШЕМ дереве уехали на базе эпика

Класс тот же, что у #723, только дерево наше. Замысел объявляет базой kacho `f445aaaa554`. Полосы
волн ответвляются от `2564` (`82afff04433`), а между ними сел #2787 — 87 файлов. Перемерено:
`git grep -l 'NewLoginLaneRelay'` → **4 → 5** (добавилась проба
`gateway/internal/handler/logout_exits_census_test.go`); `login_lane_paths.go` — **147 → 157**
строк; `tryHydraJWT` — `auth.go:1245 → 1392`; `AuthInterceptor.HTTP` — `:999 → :1023`. Вывод
«Ноги 1» §5.1б (полоса нашей сессии стоит первой и терминальна) **устоял**. Координаты под ним — нет.
Ингресс, обе таблицы маршрутов края и валидация полосы входа **не менялись**. kaname и corelib
на ветках эпика содержимым равны `main`. Для полос это не блокер: база дома L13 прежняя. К
замыслу: перебазировать ведомость §7а на `82afff04433`, у кода называть символ, а не номер строки.

---

## Экспозиция и условия на код — сводка

| решение | класс (раздел скилла) | что должно быть верно | чем держится |
|---|---|---|---|
| З6 — поддерево под нашим именем модуля | `code-authoring` §«Инвариант и оснастка — это тоже прод-код» (идентичность, а не имя) | `RV-1` пп. 1–4 | **нужен гейт, его нет** |
| §2а.0 — 18 предикатов «над нашим поддеревом» | `gate-authoring` (обход, названный корнем; пустой обход — красный; область, способная и упасть, и промолчать) | Ф1–Ф6 ниже | внимание автора до правки; после правки — предикаты сами |
| §2а.0 п. 1 и §8 L0b — порядок ратификации | `code-authoring` §«Решение и его следствие разнесены» | Ф2 | строка «зависит от» §8 плюс условие §2а.0 — после их сведения |

## Числа, полученные до кода

| утверждение | как измерено (единица · ревизия · объём) | что оказалось |
|---|---|---|
| «каталога поддерева нет ни в одном стволе» (§2а.0) | 5 предикатов присутствия · 6 ревизий · все пути ствола; положительный контроль на эталоне | **0** на каждой; контроль находит все 7 имён |
| «кодинг-полосы по 18 условиям адресуют наше поддерево» | классификация предмета каждого предиката по полосам §8 | 17 из 18 приносит **L0b**, 4 — ещё и kaname; только M13 — L0a |
| «§2а без формы «путь клона:строка»» (#723 п. 1) | `grep -oE '[A-Za-z0-9_/]+\.go:[0-9]+'` + продолжения `` `:N` `` по §2а (426 строк) | **51 + 33 = 84** о клоне, **4** о нашем дереве; требуемое #723 значение — 0 |
| H4 на `dd66b5be` против базы `cbbac984` (#723 п. 5) | `git diff dd66b5be cbbac984` по трём файлам H4; чтение `handler.go:55`, `:206–211` @ `cbbac984` | дифф **пуст**; координаты держатся @ `cbbac984`; в замысле всё ещё `dd66b5be` |
| «0 вызываемых уязвимостей у формы В» (§1.2, §10) | индекс базы уязвимостей по пути модуля; граф сборки двух форм без сети | у формы В пути `github.com/ory/fosite` в графе **нет** — прибор слеп к движку по построению |
| `ory/x` в производственном графе | импорты непробных файлов 8 пакетов графа на эталоне | **39** файлов (`errorsx` 39, `otelx` 9) |

## Чего измерить нельзя

- **Исход ратификации по дереву.** Её вход — поддерево и пакет церемонии, а их нет ни на одной
  ревизии (§4, 0 на шести). До посадки L0a + L0b оценка «со слов» — единственная честная.
- **Воспроизведение чисел §1.2 прибором замысла.** Прибор — предмет L0a, в дереве его нет
  (`ls scripts/` — 0 скриптов замера). Собранные формы я использовал только для одного
  качественного факта — какие модули стоят в графе сборки. Чисел таблицы §1.2 я не перемерял.

## Итог

**unmapped** — `RV-1`. Плюс к-замыслу: Ф1–Ф9 (#723), Д1–Д2, снятие двух пассажей core-no-prod,
таблица применимости ролей, перебазирование координат kacho. Вопрос диспетчеру — Д3. Вернуть в
приёмку — **ничего**. Одобрение замысла по моей оси **запрещено**, пока `RV-1` не отображён.

## Вернуть в приёмку

— (приёмка перемерена, отпечаток совпал; ни одно утверждение этой записи Given приёмки не
касается)

## Неотображённые пункты (пересверка)

- **`RV-1`** — обнаружение уязвимостей кода движка в поддереве; прибор, названный замыслом,
  слеп к нему по построению (раздел 5).

## Что поправить в замысле по #723 — поимённо

- **Ф1** (п. 1 #723). Назвать каталог поддерева в corelib и импорт-путь пакета церемонии. У
  каждого из 18 предикатов назвать **корень обхода** и **область**: поддерево, наш пакет или
  композиционный корень kaname.
- **Ф2** (п. 2 #723, несущее). Вход ратификации — не «поддерево, внесённое L0a», а **дерево corelib
  после L0a и L0b до выпуска тега**: предмет 17 из 18 приносит L0b. Для H2, H5, H6 и M10 вторая
  половина судится на сдаче полос kaname (L2–L4). Строку «зависит от» у L0b в §8 свести с §2а.0
  п. 3: старт L0b — после L0a, **сдача и выпуск тега** — после вердикта. Сегодня два места говорят о
  разном (старт против сдачи), и прочтение §8 даёт круг: ратификации нужен предмет L0b, а L0b
  ждёт ратификации.
- **Ф3**. M2 и M11: исключить каталог поддерева из области «непробное дерево» либо внести правку
  поддерева в `PROVENANCE`. Иначе оба предиката красны по построению с первой минуты после L0a
  (по **4** непробных файла у каждого).
- **Ф4**. M1, M3, M8: область обязана включать исполняемые пути поддерева — ветвь пустого
  `redirect_uri`, ветвь пропуска публичного клиента, три вызова отзыва. Для M3 выразить условие A-1
  предикатом: посевной клиент конфиденциален.
- **Ф5**. M7: определить синтаксический признак «sentinel хранилища». Без него разбор даёт 2 или
  36, а не 4.
- **Ф6**. M13: назвать пин **содержимого** апстрима (хэш модуля в `PROVENANCE`) и способ его
  получения. Иначе третье множество предиката остаётся вне трёх стволов и после L0a.
- **Ф7** (п. 1 #723, перепись → 0). Снять 84 справочные координаты клона из §2а.1–2а.3 либо
  вынести их отдельным приложением с пином коммита и пометкой «не держатель» у **каждой**.
  Объявления «справочные» в §2а.0 предикату снятия #723 недостаточно.
- **Ф8** (п. 3 #723). Заголовок §2а «…ПЕРЕМЕРЕННЫЕ на `fosite@v0.49.0`» и 17 меток «Воспроизвелась:
  ДА» противоречат §2а.0 п. 2 («перемеренной не считается») в одном разделе. Переименовать в
  «прочитанные автором замысла на клоне; статус — со слов до L0a + L0b».
- **Ф9** (п. 5 #723). H4: `kaname dd66b5be` → `cbbac984` вместе с замером «три файла H4 между
  ними не менялись». Координату `client_token.go:60` у M10 и в §6 N2 писать с каталогом.

## Заказы (адресат · предмет · основание)

- автор замысла (`acceptance-author`) · ред. 13: отображение `RV-1`, Ф1–Ф9, Д1–Д2, снятие §9.1 п. 8
  и тернара §6, таблица применимости ролей, перебазирование координат kacho на `82afff04433` ·
  `change-graph.md` §4; #723 «предикат снятия» пп. 1–5.
- `go-implementer` (L0a, дом corelib) · проверка уязвимостей поддерева по пину `PROVENANCE` с
  положительным контролем и периодическим прогоном · `RV-1`; `gate-authoring` (проверка обязана
  уметь упасть).
- диспетчер · ответ на Д3 · решение диспетчера 2026-09-22.

## Новый класс и кандидат в однофайловый предикат

Класса не завожу. «Предикат без названной области обхода»: в одном месте он красный по
построению (M2, M11), в другом слепой (M3, M8). Но это **один** документ, то есть один экземпляр,
а не два независимых. Это **находка**, и она уходит в Ф3–Ф4. `RV-1` ложится в существующий раздел
`code-authoring` (идентичность, а не имя), нового класса не требует. Однофайлового признака нет.
