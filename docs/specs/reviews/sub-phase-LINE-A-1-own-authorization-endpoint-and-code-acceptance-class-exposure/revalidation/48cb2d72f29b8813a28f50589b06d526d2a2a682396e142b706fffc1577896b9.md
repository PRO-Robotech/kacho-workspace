# Пересверка классов на отпечаток ЗАМЫСЛА: LINE-A-1, редакция 12

| поле | значение |
|---|---|
| **Issue** | PRO-Robotech/kacho#2564 (Линия A; свой вход человека + полная церемония OAuth 2.1 `authorization_code`) |
| **Предмет** | `docs/specs/sub-phase-LINE-A-1-own-authorization-endpoint-and-code-design.md` (**редакция 12**, DRAFT) |
| **SHA-256 предмета** | `48cb2d72f29b8813a28f50589b06d526d2a2a682396e142b706fffc1577896b9` |
| **Роль** | class-exposure-analyst |
| **Режим** | ПЕРЕСВЕРКА на отпечаток замысла ДО его одобрения (`change-graph.md` §4) |
| **Actor** | pointpu (`docs/changes/policy.yaml:43` → `class-exposure-analyst: [pointpu]`) |
| **Ревизии** | воркспейс: ветка `acceptance/line-a-1`, worktree `tmp/wt-line-a-1`, HEAD `86a040f134883b44fd28210ed9dd3d53d5ab9a9b`. Деревья продукта: **kacho `f445aaaa554`**, **kaname `cbbac984`**, **corelib `34bc8104`** — все три перемерены `git rev-parse origin/main` и равны названным ведомостью радиуса |
| **Снимок** | оба отпечатка **перемерены первым действием**. `sha256sum <замысел>` → `48cb2d72f…` (совпал с заданием). `sha256sum <приёмка>` → `b755886fd…` (совпал; **приёмка не сдвинулась, одобрение в силе**). `wc -l` замысла → **4593**. Пакета `docs/changes/<id>/` для LINE-A-1 нет — запись вне пакета, подкаталог `revalidation/` |
| **Итог** | **отображены 22 из 22; неотображённых — ноль** (§3 **побайтово идентичен** ред. 11 и ред. 10). **Все пять печатей выписанного числа приведены к замеру, и число получается прогоном — проверено моим прогоном на восьми ревизиях.** Обход §7а.2 объявлен командой, прогоняется дословно и даёт **39 → 4 (инв. 30, 34, 37, 38)**; инв. 30 внесён. Граница правила названа прямо и не читается как гарантия. Оба расширения сверх задания **подтверждены прогонами**. **Но:** ведомость §7а.2 недобирает **инв. 29** молча — её команда различает ствол по буквальному имени, а инв. 29 называет его описанием; наивный предикат цепи даёт **4**, а не 5, и пятая находка автора — артефакт разборщика, а не проза; «Рост в **497** строк» и «оба прогона сделаны ПОСЛЕ последней правки ред. 11» стоят **строкой ниже** собственной поправки, объявившей их неверными. К коду — **четыре** условия. Блокера по моей оси нет |

---

## 0. Что эта запись делает и чем не является

Действующей пересверкой была `85b85436….md` (ред. 11). Ред. 12 сменила отпечаток и тем обесценила
её (`change-graph.md` §4). Прежняя запись **не правится и не переносится** — она свидетельствует о
своей редакции.

Первичный разбор (`initial/b755886f….md`, **20** классов `LAX-1…LAX-20`, отображённые в §3 вместе с
двумя сквозными = **22** строки) **остаётся в силе**: он о приёмке, а приёмка не менялась.

`SUPERSEDED.yaml` — **правлена ровно перепись каталога, три числа**, и сделано это потому, что
ведомость требует этого от **посадки записи**, а не от чеканки редакции (§11). Номер редакции,
предикат снятия, строки `superseded` и чьи-либо вердикты — **не тронуты**: владение ими названо в
самой ведомости и принадлежит автору замысла (`change-graph.md` §1).

Вердикта замыслу здесь нет. Решение несёт раздел «Итог».

---

## 1. ОТОБРАЖЕНИЕ: 22 из 22, раздел решений не тронут

Основание сильнее пересчёта: раздел §3 ред. 12 **побайтово равен** своему виду в ред. 11 —
отпечаток выделенного раздела совпал (`569925b667cce459…`, 44 строки в обоих телах), а `git diff`
между `88465f8a` и `HEAD` по файлу **не содержит ни одного ханка** в координатах §3 (ханки идут по
старым строкам 1–9, 29, 58, 75, затем 2222 — §3 ред. 11 лежит на строках ~1874–1917).

Имена в таблице: `LAX-1 … LAX-20`, `сквозное-1`, `сквозное-2` — **22** строки. Перечень классов
первичного разбора: `LAX-1 … LAX-20` — **20** имён, все присутствуют.

**Неотображённых — ноль.** Ред. 12 новых внешних вызовов, асинхронных путей и sentinel-значений не
вносит: её решения — приведение чисел к замеру, объявление обхода командой, называние границы
правила, внесение строки в ведомость и внесение L9 в колонку «зависит от». Перечень классов не
растёт, и по `change-graph.md` §4 одобрение замысла по моей оси не запрещено.

---

## 2. ПУНКТ 1 ЗАДАНИЯ: пять печатей выписанного числа — ВСЕ ПРИВЕДЕНЫ, число получается прогоном

**Моя находка расширена автором верно, и пятая печать существует.** Ред. 11 печатала у L13 число
**3** в четырёх местах, которые я назвал; автор нашёл **пятую** — §7 инв. 37, таблица держателей,
половина (а), — то есть в собственной ведомости, написанной против этого класса.

**Перепись печатей на теле ред. 12 (единица — вхождение имени предиката в тело; ревизия —
`48cb2d72…`):** `grep -c 'NewLoginLaneRelay'` → **7**. Из них **пять** — печати числа (строки
130 врезка «чем снят Б2», 3479 §7 инв. 37, 3684 §7а.1 строка L13, 3732 §7а.2 строка инв. 37,
3788 §8 строка L13 «ЗАМЕР ДОМА»); две — не числовые (строка 2810 — цитата кода пробы, строка 3583 —
сама врезка п. 5, где выписанное число разбирается). **Шестой печати нет.**

**Все пять печатают 4.** Проверено чтением каждой: «замер `NewLoginLaneRelay` → **4** файла»
(130) · «→ **4** файла» (3479) · «@ kacho **4**, по всему стволу kaname **0** | **4 · 0**» (3684) ·
«→ **4**» (3732) · «→ **4** файла @ kacho `f445aaaa554`, в kaname — **0**» (3788).

**Число получается прогоном — прогнал сам, дословно (`testing-verdict` → `foreign-predicate-run-verbatim`):**

```
git grep -l 'NewLoginLaneRelay' origin/main -- gateway/     @ kacho f445aaaa554 → 4
git grep -l 'NewLoginLaneRelay' origin/main                 @ kaname cbbac984  → 0
```

Четыре файла названы поимённо и совпали с телом врезки: `gateway/cmd/api-gateway/main.go`,
`gateway/cmd/api-gateway/own_lane_readers_wiring_test.go`,
`gateway/internal/handler/login_lane_relay.go`, `gateway/internal/handler/login_lane_relay_test.go`.

**Контрольный прогон автора воспроизведён.** Тот же предикат на первопредковых коммитах от
`f445aaaa554` вниз: `f445aaaa554 → 4`, `ac33f733962 → 4`, `b5fa093341f → 4`, `15e4031e3c5 → 4`,
`26206c80f9d → 4`, `f50a33d6295 → 4`, `275240efdc1 → 4`, `972bdc342d1 → 0`. Восемь коммитов, четыре
на каждом вплоть до `275240efdc1` («край: читатель нашей сессии, ретрансляция четырёх глаголов
формы»), ноль до него. **Числа «три» не было ни на одной ревизии** — вывод автора верен дословно.

**Пункт 5 правила вписан и сформулирован на наблюдаемом различии** («перенос и прогон наблюдаемо
неотличимы, оба печатают число рядом с командой») — это верная формулировка: она называет то, чем
два состояния различаются, а не призывает к аккуратности.

### 2.1 НО: три строки из восемнадцати п. 5 не удовлетворяют — и это не придирка, а его собственная область

Правило п. 3 требует: «Каждый дом и каждый держатель несёт **рядом** строку замера: команда · число ·
ревизия. **Без неё имя не называется вовсе**». П. 5 добавляет: число обязано быть **получено
прогоном названной рядом команды**. Разобрал все 18 строк §7а.1 своим разборщиком (единица — строка
таблицы; форма ячейки учтена вместе с экранированным `\|`):

| строка | что стоит вместо команды | годность по п. 3 / п. 5 |
|---|---|---|
| **L0b** | «дом — тот же модуль, что и L0a»; число — «—» | **нет**: дом назван, замера рядом нет, числа нет вовсе |
| **L4** | `… grep -c 'internal/handler/clienttokenhttp'` @ kaname → **3** | **нет**: команда начинается с многоточия и **дословно не исполнима**; голова взята у соседней строки читателем, а не автором |
| **L6** | «тот же ствол и тот же обработчик, что L4» → «см. L4: **3**» | **нет**: ссылочная форма, правилом не объявленная |

Число L4 я **достроил и перемерил**:
`git ls-tree -r --name-only origin/main | grep -c 'internal/handler/clienttokenhttp'` @ kaname
`cbbac984` → **3**. Число верно. Негодна **форма**: п. 5 существует ровно затем, чтобы читатель мог
отличить полученное число от перенесённого, а многоточие эту возможность отнимает — читатель обязан
угадать голову команды. Это **то же наблюдаемое различие**, ради которого п. 5 и вписан, только с
другой стороны: не «прогон против переноса», а «исполнимая команда против её изложения».

**Это мои условия 1.2/1.3 на ред. 11, и они НЕ закрыты:** строки L0b и L6 в ред. 12 **побайтово
те же**, что в ред. 11 (сверено `git show 88465f8a:<файл>` против рабочего тела).

---

## 3. ПУНКТ 2 ЗАДАНИЯ: второе перенесённое число и решение «не печатать вовсе» — СУЖУ

**Что произошло.** Ред. 11 напечатала «строк файла **4226** (+497)» и строкой ниже —
«**Оба прогона сделаны ПОСЛЕ последней правки ред. 11, а не до.**» Замер тела ред. 11 из линии
(`git show 88465f8a:<файл> | wc -l`, отпечаток сверен → `85b85436…`) даёт **4282**. Разница —
**56** строк, и при счёте после последней правки ей взяться неоткуда. Автор нашёл это сам и записал
оба следствия, включая то, что соседнее утверждение о порядке прогонов **неверно**.

**Решение ред. 12: своё число строк в теле не печатать вовсе, отдать прогону.** Проверил: в теле
ред. 12 числа `4593` нет; ячейка говорит «берётся `wc -l` этого файла».

### Суждение: решение ВЕРНО, и вот по какому признаку

Это **не** потеря сведения, и вот почему — признак проверяемый, а не вкусовой:

1. **Сведение не теряется, а переадресуется исполнимой командой.** `wc -l <файл>` — одна команда,
   без аргументов-угадываний, дающая число за секунду. Утрачен не факт, а его **копия**.
2. **Копия здесь заведомо негодна по построению.** Число строк файла, напечатанное **внутри этого
   же файла**, изменяет предмет, о котором говорит: всякая правка после его печати делает его
   ложным, а печать самого числа — тоже правка. Это не «может устареть», а «ложно в момент, когда
   станет окончательным». Ровно поэтому же в теле не печатается отпечаток редакции — и автор
   приводит этот довод сам, то есть решение выведено, а не принято по вкусу.
3. **Читателю оно нужно не как число, а как сравнение,** и сравнение сохранено: строка «ред. 11
   **4282** → ред. 12 `wc -l`» оставляет обе стороны, одну числом (чужое тело, неизменяемое,
   поднятое из линии и сверенное отпечатком), другую командой (своё тело, изменяемое). **Единица
   счёта у сторон одна** — это и есть условие сравнимости.
4. **Проверяемость выросла, а не упала.** Ред. 11 утверждала «прогоны сделаны после последней
   правки» и проверить это было нечем. Ред. 12 утверждает то же и **называет проверку**: `wc -l`
   над телом обязан совпасть с числом в возврате исполнителя. Утверждение перестало быть словом.

**Что этим НЕ закрыто и что я обязан назвать (находка).** Поправка стоит в ячейке таблицы, а
**строкой ниже** — строка 4141 тела ред. 12, дословно:

> **Оба прогона сделаны ПОСЛЕ последней правки ред. 11, а не до.** Рост в **497** строк несут: §7а с
> двумя ведомостями, …

Оба утверждения этой строки — те самые, которые ячейка выше объявила **неверными** (рост **553**, а
не 497; порядок прогонов не тот). Они стоят **в изъявительном наклонении, без пометки**, в одном
экране с собственной поправкой. Это **два места об одном предмете, одно из которых ложно** —
`doc-truthfulness`, и класс тот же, против которого написана вся редакция.

Довод «это отчёт ред. 11 о своей работе, и ред. 12 его не правит» здесь **не действует по критерию
самого автора**: он объявил исключение для утверждений **о предмете**, а не о своей работе, и по
этому критерию исправил **4226 → 4282**. Число **497** и порядок прогонов — тот же предмет
(**файл**), проверяемый той же командой. Критерий применён к ячейке и не применён к строке под ней.

---

## 4. ПУНКТ 3 ЗАДАНИЯ: ведомость второго рода — ОБХОД ПРОГНАЛ САМ

**Команда исполнена дословно, без пересборки под свой шелл** (`testing-verdict` →
`foreign-predicate-run-verbatim`):

```
awk '/^## §7 /,/^## §7а /' <файл> | awk '
  /^[0-9]+\. \*\*/ { if (n) ev(); n=$0; sub(/\..*/,"",n); buf="" }
  { buf = buf " " $0 }
  function ev() { if (buf ~ /kaname/ && (buf ~ /kacho/ || buf ~ /gateway\// || buf ~ /край/)) print n }
  END { if (n) ev() }'
```

**Прогон на теле `48cb2d72…`: блоков осмотрено 39, отобрано 4 — `30`, `34`, `37`, `38`.**
Воспроизводится дословно. **Инв. 30 внесён** — строка в таблице есть, с замером достижимости и
держателем **по каждой половине**: (1) парный близнец L13, дом `kacho/gateway`; (2) двухдревесный
гейт L13а, дом воркспейс.

**Признак даёт ОДИН перечень — по построению, а не по договорённости.** Прежняя неоднозначность
(широкое прочтение → 4, узкое → 3) снята тем, что признак перестал быть прозой: у команды прочтение
одно. Счёт по популяциям разделён явно (инвариантов §7 — четыре, записей вне §7 — одна, строк
таблицы — пять), и предикат переписи строк объявлен **по строению строки**, а не по именам ячеек —
что верно: перечень имён недобрал бы строку `на-реплику` молча.

**Замеры строк ведомости перепроверил сам, на названных ревизиях:**
`login_lane_paths.go` → **147** строк @ kacho · `repohygiene` → kacho **879**, kaname **0** ·
`clienttokenhttp` @ kacho → **0** · `LoginLaneRoutes` @ kaname → **0** ·
`crossrepo-gate/run-all.sh` → «стволов 3 (kacho=**6664**, kaname=**3493**, corelib=**473**);
путей **10575**; ПАР **51**», код выхода **0**. Все сошлись дословно.

### 4.1 НАХОДКА: ведомость недобирает ИНВ. 29 — и недобирает МОЛЧА

Обход стал командой, и это правильно. Но у команды появился **свой** словарь, и он `уже` узок:
различение ствола сделано по **буквальному имени** `kaname`. Прогнал контроль в обе стороны (свой
разборщик, нормализация переносов строк — блоки переносятся, и наивное выражение молча теряет
словосочетание, разорванное концом строки):

| контроль | что дал |
|---|---|
| блоки с `kaname` **и** второй стороной (объявленный признак) | **30, 34, 37, 38** — 4 |
| блоки с `kaname`, но **без** второй стороны | 31, 32 — 2 |
| блоки со второй стороной, но **без** `kaname` | 10, 29, 33, 35, 36, 39 — 6 |
| блоки, называющие ствол kaname **описанием** (`служб\w+ доступа`) и **не** словом `kaname` | **29** — 1 |

**Инв. 29 (`SS-H7`, «второго внешнего входа не заводится») — двухствольный своим же текстом**:
одна половина — «число **манифестов входа в поставке службы доступа** остаётся **0**» (это
`kaname`, и там действительно 0: `git grep -l 'kind: Ingress' origin/main -- deploy/` @ `cbbac984`
→ **0**), вторая — «публикуется через существующий единственный внешний вход — **край**» (это
`kacho`). Слова `kaname` в блоке нет, и предикат его **не берёт**.

Это не формальность, а **прямое следствие расширения, которое автор внёс сам** (§8, ред. 12):
`SS-H7` «сегодня истинен **по величине посадки**, а не по построению», и условие на код требует от
него называть **обе** стороны. Инвариант, которому только что предписано быть двухствольным, обязан
стоять в ведомости двухствольных **пятой строкой инвариантов**. Его там нет.

Остальные шесть проверил и **находками не считаю**: 10, 33, 35 называют пути вида
`internal/handler/…`, но это пути **края** в `kacho` (`gateway/internal/handler/login_lane_relay.go`
и соседи) — совпадение по форме пути, а не по стволу; 31, 32, 36, 39 односторонни по существу.

**Класс назван точно:** `testing` → «Предикат по ДВУЯЗЫЧНОМУ корпусу, ищущий на одном языке,
недобирает МОЛЧА». Здесь языков тоже два, только это не русский и английский, а **имя ствола** и
**его описание**. Вывод, который редакция записала о прозе, применим к её собственной команде:
перечень, заведённый против рода, наследует слабость перечня — а команда наследует слабость своего
словаря.

---

## 5. ПУНКТ 4 ЗАДАНИЯ: граница правила названа — и названа так, что оговоркой не читается

**Сказано в самом правиле**, врезкой внутри §7а, заголовком без смягчения: «**ЧЕМ ЭТО ПРАВИЛО
ДЕРЖИТСЯ: ВНИМАНИЕМ. МАШИННОГО ДЕРЖАТЕЛЯ У НЕГО НЕТ**». Довод назван тем же, каким я его называл:
«правило, не называющее своей границы, читается как гарантия».

**Граница — замер, а не осторожность.** Таблица из пяти приборов; проверил каждую строку сам, в
рабочей копии ветки:

| утверждение правила | мой прогон |
|---|---|
| читателей документа среди приборов **0** | `grep -rl 'sub-phase-LINE-A-1' scripts/ .claude/hooks/` → **0** |
| `docs-gate` — **8** проверок, файл замысла открывает одна и считает его **отпечаток** | в ветке 8 файлов `check-*.py`; `check-06` печатает «замысел …-design.md, sha256 `48cb2d72…` (повторить: `sha256sum …`)» и ничего из содержания не разбирает — проверено чтением исходника (`sha256_of`, `design_path`) |
| пакета `docs/changes/<id>` для LINE-A-1 нет | `ls docs/changes/` → `census`, `policy.*`, `standalone-iam` — пакета нет |
| `class-guard`: предикатов этого рода **0** | каталог хука есть, `text_predicates.py` рода не несёт |

**Оговоркой-гарантией не стало, и вот проверяемый признак этого.** Текст **сам** отказывается от
вывода, который читатель мог бы сделать:

- «девятнадцатую полосу без строки … не поймает ничто, **и число, разошедшееся с деревом, — тоже
  ничто** (так и вышло: «3» у L13 прожило пять печатей и целую редакцию)» — граница
  проиллюстрирована **собственным** дефектом, а не гипотетическим;
- «**«Сошлось однажды» и «держится» — разные вещи, и вторую эта строка не утверждает**» — прямой
  отказ от превращения зелёного прогона в держателя;
- «Правило связывает **автора**, а не дерево, и ровно в этом его предел».

**Предикат, который дал бы держателя, назван исполнимым и прогнан мной дословно:**

```
awk '/^### 7а\.1 /,/^### 7а\.2 /' <файл> | grep -oE '^\| \*\*L[0-9а-яa-z]*\*\*' | sort -u > /tmp/a
awk '/^## §8 /,/^## §9 /'          <файл> | grep -oE '^\| \*\*L[0-9а-яa-z]*\*\*' | sort -u > /tmp/b
diff /tmp/a /tmp/b && test -s /tmp/a
```

→ **18 = 18**, `diff` пуст, обход непуст. Воспроизводится.

### 5.1 НАХОДКА: предикат назван «тем, который дал бы держателя» — а держит он ОДНУ из двух названных потерь

Граница сформулирована **двумя** потерями: (1) девятнадцатая полоса без строки, (2) число,
разошедшееся с деревом. Названный предикат сверяет **множества имён** двух таблиц — то есть ловит
(1) и **не касается** (2) вовсе: он не читает ни команд, ни чисел, ни ревизий. Ровно тот дефект,
который прожил пять печатей и целую редакцию, этот предикат **не покраснел бы**.

Текст этого не говорит. Он называет предикат единственным числом — «предикат, который дал бы
машинного держателя», — и заказ уйдёт `tooling-maintainer`'у в этой формулировке. Исполнитель
построит его, набор позеленеет, и §7а будет прочитан как имеющий держателя — при том что его
**наиболее дорогая** половина останется на внимании. Это не отменяет честности врезки: это
**граница внутри границы**, которую надо назвать словом, как назвали первую.

---

## 6. ПУНКТ 5 ЗАДАНИЯ: отрицательный результат автора — ВЫВОД ВЕРЕН, ПРИЧИНА НАЗВАНА НЕ ТА

**Проверил вывод своим разбором.** Цепь критичности: `L0a→L0c→L0b→L1→L2→L9→L13→L12→L12б→L3→L4→L5`
(12 имён). Колонка «зависит от» — 18 строк §8.

**Прогон наивной формы («взять из клетки все имена вида `L<N>`») моим разборщиком, делящим строку
по сырому `|`: пять находок** — `L9→L13`, `L12→L3`, `L13→L6`, `L13→L3`, `L13→L11`. Совпало с числом
автора.

**Но я проверил СВОЙ разборщик первым** (`testing-verdict` → `divergence-check-own-parser-first`,
`own-parser-knows-all-forms`), и он знал не все законные формы ячейки. В строке L9 стоит
**экранированная вертикальная черта** внутри ячейки «ЗАМЕР ДОМА»:

```
… ls-tree -r --name-only origin/main \| grep -c '^\.claude/rules/'` → **0** …
```

Деление по сырому `|` даёт у этой строки **9** полей вместо **8** и сдвигает колонки на одну:
разборщик читает **колонку «предмет»** вместо «зависит от» и находит в ней `L13`.
**Настоящая клетка «зависит от» у L9 — «—», она пуста.**

**Прогон той же наивной формы разборщиком, знающим форму `\|`: находок ЧЕТЫРЕ** — `L12→L3`,
`L13→L6`, `L13→L3`, `L13→L11`.

**Вывод автора «все ложные» — ВЕРЕН, и я его подтверждаю.** Разобрал каждую по телу:

| находка | текст клетки | почему ложная |
|---|---|---|
| `L12 → L3` | «— (стоит на цепи критичности перед L3)» | клетка объявляет **«—»**; имя в скобках — пояснение о месте в цепи |
| `L13 → L6` | «та же асимметрия, которую ред. 11 свела у **L11 ↔ L6** строкой левее» | цитата чужой строки в доводе |
| `L13 → L11` | там же | то же |
| `L13 → L3` | «её имя объявляло себя предусловием «до посадки **L3** и L13»» · «строкой «зависит от» у **L3**, которая называет L13 явно» | обе — о **встречном** ребре L3←L13 |
| `L9 → L13` (пятая) | клетки не существует: имя взято из **соседней колонки** | **не ложная находка, а артефакт разборщика** |

**Объявленные зависимости нарушений не дают — проверил обходом всех 12 полос цепи:** L0b←L0a ·
L1←L0b · L2←{L1,L0c} · L9←∅ · L13←L9 · L12←∅ · L12б←∅ · L3←{L1,L2,L12,L12б,L13,L9} · L4←{L2,L3} ·
L5←L4. У каждой всякая объявленная зависимость стоит раньше. **Ноль нарушений** — совпадает.

### 6.1 Годность ужесточённого предиката: закрывает ЧЕТЫРЕ из пяти, пятую — НЕТ

Ужесточение, предложенное текстом: «имена берутся из **головы** клетки до первого знака конца
перечисления, а проза отделяется разделителем, объявленным здесь же».

- **На `L12`, `L13→L6`, `L13→L11`, `L13→L3` — годится.** Все четыре имени стоят в прозе **после**
  объявленного перечня; отсечение по голове клетки их снимает, а объявленные зависимости (`L9` у
  L13) сохраняет. Форма названа исполнимо, а не благим пожеланием — это верно.
- **На пятой — не годится, и не могла бы.** Причина пятой находки лежит **до** разбора ячейки: это
  неверный **номер колонки**. Какую бы голову клетки предикат ни брал, он возьмёт её у **не той**
  клетки. Лечится не разделителем, а тем, что разборщик знает форму `\|`.

**То есть диагноз автора («клетки содержат прозу, называющую соседние полосы») верен для четырёх
находок из пяти и неверен для пятой.** Пятая — экземпляр `own-parser-knows-all-forms`: число
**5** получено разборщиком, не знающим одной законной формы корпуса, и в тело оно попало **числом
без названной команды** — при том что п. 5 правила радиуса, вписанный этой же редакцией, требует
обратного. Класс тот же, что чинился; предмет — не ведомость, а абзац рядом с ней.

**Заказ от этого не снимается, а получает второе требование**, и оно проверяемо: разборщик колонок
обязан знать экранированную черту; инъекция — ячейка с `\|`, близнец — та же ячейка без неё.
**Оба близнеца берутся из дерева:** строка `| **L9** …` несёт `\|`, строка `| **L11** …` — нет.

---

## 7. РАСШИРЕНИЕ 1 (не было в задании автору): оговорка обхода с другой стороны — ПОДТВЕРЖДЕНО ПОЛНОСТЬЮ

Моя находка на ред. 11: оговорка про «в kaname манифестов входа ноль» проверяла границу там, где
вывод от неё не зависит, а несущее лежит в `kacho`. Автор говорит, что прогнал и нашёл второй
манифест входа, объявленный зависимостью **без условия** и выключенный **только значением**.
**Перемерил всё сам, на `f445aaaa554` / `cbbac984`:**

| утверждение ред. 12 | мой прогон | сошлось |
|---|---|---|
| kacho `-- deploy/` → **1** | `git grep -l 'kind: Ingress' origin/main -- deploy/` @ kacho → **1** | да |
| kacho весь ствол → **12**, под `templates/` — **11** | то же без pathspec → **12**; из них с `templates/` в пути → **11** | да |
| kaname весь ствол → **1**, и это чарт сайта документации | @ kaname → **1** (`docs/deploy/templates/ingress.yaml`); `docs/deploy/Chart.yaml` → `name: kaname-docs`, «Docusaurus … nginx-served» | да |
| `api-gateway` объявлен зависимостью **без `condition`** | `deploy/helm/umbrella/Chart.yaml`: `- name: api-gateway` · `repository: file://../../../gateway/deploy` — **условия у записи нет**; это **единственная** служебная зависимость умбреллы без условия (у `vpc`, `compute`, `kacho-nlb`, `uif`, `registry`, `storage` условие есть) | да |
| шаблон огорожен одной строкой | `gateway/deploy/templates/ingress.yaml:4` → `{{- if .Values.ingress.enabled }}` | да |
| выключен **величиной** `ingress.enabled: false` | `deploy/helm/umbrella/values.yaml` → `api-gateway.ingress = {enabled: False}` | да |
| **ни один профиль не переопределяет** | разбор YAML всех четырёх: `values.yaml` → `{enabled: False}`; `values.dev.yaml`, `values.prod.yaml`, `values.dev-prod.yaml` → ключа **нет** | да |

**Условие на код внесено и сформулировано верно по существу:** предикат `SS-H7` называет обе
стороны, второе число берётся счётом **включённых**, а не файлов, и выключенность объявляется
«величиной посадки со своим стражем, а не свойством дерева». Это ровно то противоядие, которого
класс требует (`security-hardening` → `hard-missing-knob-vs-unseeded`).

### 7.1 НАХОДКА внутри расширения: про `uif` сказано о ЧАРТЕ, а измерено по ОДНОМУ профилю

Текст: «Второй сабчарт с манифестом входа — `uif` — выключен **условием чарта**
(`condition: uif.enabled`, `uif.enabled: false`), и это **другой** род выключенности: его шаблоны
не рендерятся вовсе».

**Перемерил по всем профилям, как перемерил api-gateway:**

| профиль умбреллы | `uif.enabled` | `uif.ingress` |
|---|---|---|
| `values.yaml` | **false** | — |
| `values.dev.yaml` | **true** | не задан ⇒ берётся собственное умолчание сабчарта |
| `values.prod.yaml` | не задан | — |
| `values.dev-prod.yaml` | не задан | — |

Собственное умолчание сабчарта: `ui-future/deploy/values.yaml` → `ingress: {enabled: true, …}`,
шаблон огорожен `{{- if .Values.ingress.enabled }}`. **Значит на профиле `values.dev.yaml` шаблоны
`uif` рендерятся, и его манифест входа — включён.** Утверждение «не рендерятся **вовсе**» верно для
базовых величин и **неверно** для `values.dev.yaml`.

Это **то же самое**, что автор снял абзацем ниже про слово «единственный»: утверждение о
**популяции** (профилей), выведенное из одного её члена. И оно лежит **внутри** нового условия на
код: «включённых манифестов входа в рендере умбреллы — **1**» есть число **на профиль**, а не на
чарт; по базовым величинам их 1 (умбреллин `api-gateway-ingress.yaml`, ничем не огороженный), по
`values.dev.yaml` — 2. Условие обязано называть профиль, иначе оно не исполнимо: исполнитель не
знает, какой рендер обходить.

---

## 8. РАСШИРЕНИЕ 2 (не было в задании автору): «единственный дом» — ПОДТВЕРЖДЕНО, домов действительно два

**Утверждение ред. 11 было неверно в момент написания, и ред. 12 это говорит.** Перемерил:

- `grep -rln 'kaname' scripts/` → обе оснастки: `scripts/crossrepo-gate/*` **и** `scripts/docs-gate/*`
  (`check-03-holding-claim-resolves.py`, `_lib.py`, `check-04-…`, `inject.sh`, `README.md`).
- Прогон `docs-gate` с созданным условием печатает резолв второго ствола своими словами:
  `[CENSUS] check-03-holding-claim-resolves: дом PRO-Robotech/kaname — …/project/kaname @ cbbac984 (main); судится ствол origin/main cbbac984`
  и `[PASS] … координата резолвится у всех 81 строк в 3 таблицах`.

**Форма снятия верна и по доводу, а не только по факту:** «перечень из одного элемента, названный
«единственным», ослепнет ровно так же, как перечень из восемнадцати: **на следующей строке**.
Поэтому здесь названы оба, а не выбран лучший». Это правильный ход — образец при этом оставлен
`crossrepo-gate`, и вывод ред. 11 не отменён, а сужен.

---

## 9. Числа, полученные до кода

| утверждение замысла | как измерено (единица · ревизия · объём) | что оказалось |
|---|---|---|
| у L13 — **4** файла, kaname **0** | файлы; kacho `f445aaaa554` / kaname `cbbac984`; обход всего ствола | **4 · 0** — сошлось, имена файлов совпали |
| числа «3» не было ни на одной ревизии | файлы; 8 первопредковых коммитов от `f445aaaa554`; каждый прогнан | 4·7, затем 0 — **сошлось дословно** |
| печатей выписанного числа — **пять** | вхождение имени предиката; тело `48cb2d72…`; `grep -c` → 7, разобраны все | **5** числовых, 2 не числовые; шестой нет |
| обход §7а.2 даёт 39 → 4 | блок инварианта; тело `48cb2d72…`; команда исполнена дословно | **39 → 30, 34, 37, 38** — сошлось |
| §7а.1 = §8 по именам полос | имя полосы; тело `48cb2d72…`; обе таблицы | **18 = 18**, `diff` пуст, обход непуст |
| читателей документа среди приборов — **0** | файл; ветка `acceptance/line-a-1`; `scripts/` + `.claude/hooks/` | **0** |
| `docs-gate` — 8 проверок, с условием 8/8 | проверка; рабочая копия ветки; `KACHO_MONOREPO` + `KACHO_HOME_KANAME` | **8 рассмотрено, 8 пройдено, 0 провалено, 0 без предмета**; без условия — 4 пройдено, **4 без предмета** (не зелёное) |
| `crossrepo-gate` — 3 ствола, 10575 путей, 51 пара | путь · пара; три ствола на названных ревизиях | **kacho 6664 · kaname 3493 · corelib 473 · 10575 · 51**, код **0** |
| kacho: ingress `-- deploy/` **1**, весь ствол **12**, `templates/` **11** | файл; kacho `f445aaaa554` | **1 · 12 · 11** — сошлось |
| `api-gateway` — зависимость без `condition`, выключен величиной | запись зависимости · ключ профиля; kacho `f445aaaa554`; 4 профиля разобраны YAML | подтверждено; **ни один профиль не переопределяет** |
| §3 не тронут | строка раздела; тела `85b85436…` и `48cb2d72…` | **побайтово равен**, 22 строки, 44 строки раздела |
| тело ред. 11 — **4282** строки | строка; `git show 88465f8a:<файл>`, отпечаток сверен | **4282** (не 4226) — поправка автора верна |
| L4 — **3** | файл; kaname `cbbac984`; команда достроена из соседней строки | **3** — число верно, **команда не исполнима дословно** |
| **инв. 29 не отобран обходом** | блок инварианта; тело `48cb2d72…`; контроль в обе стороны + нормализация переносов | **недобор**: ствол назван описанием, не именем |
| **наивный предикат цепи даёт 4, а не 5** | находка; тело `48cb2d72…`; два разборщика — сырой `\|` и знающий его | **5 = 4 + артефакт колонки**; вывод «все ложные» верен |
| **`uif` рендерится на `values.dev.yaml`** | ключ профиля; kacho `f445aaaa554`; 4 профиля + умолчание сабчарта | `uif.enabled: true` на dev, `ingress.enabled: true` по умолчанию сабчарта |

---

## 10. Чего измерить нельзя

- **Что правило удержит девятнадцатую полосу.** Держателя нет — это установлено замером
  (читателей 0), и сам документ это говорит. Проверяемого признака «удержит» не существует; есть
  только признак «сошлось на этом теле».
- **Что перечень двухствольных предметов полон.** Он полон ровно до следующей строки — и инв. 29
  это показывает на ред. 12 так же, как инв. 30 показывал на ред. 11. Доказательство на пункт:
  оба раза недобор нашёлся не проверкой перечня, а **сменой способа смотреть**.
- **Что пятый круг рода A — последний.** Два круга подряд перечень, заведённый против рода, ронял
  по одному элементу. Утверждать «теперь полон» нечем; утверждать «команда лучше прозы» — можно, и
  это подтверждено: прозу нельзя было прогнать, команду я прогнал.

---

## Итог

- **к-замыслу:** отображены **22 из 22**; неотображённых — **ноль**. §3 побайтово равен ред. 11 и
  ред. 10. Ред. 12 не вносит ни нового внешнего вызова, ни асинхронного пути, ни sentinel-значения;
  перечень классов не растёт. **Все пять пунктов задания закрыты по существу**, два расширения
  подтверждены прогонами.
- **к-коду:** **четыре** условия, каждое с ответом «сделал / не сделал» —
  **4.1** строка **инв. 29** внесена в §7а.2 (он двухствольный своим же текстом, и новое условие на
  код этой же редакции требует от него обеих сторон), а различение ствола в команде обхода
  перестаёт опираться на буквальное имя ·
  **3** строка 4141 приведена к собственной поправке: «497» и «оба прогона сделаны ПОСЛЕ последней
  правки ред. 11» либо исправлены, либо помечены как снятые — два места об одном предмете, одно
  ложно ·
  **2.1** строки **L0b**, **L4**, **L6** ведомости §7а.1 приведены к п. 5: команда исполнима
  дословно (без многоточия), либо ссылочная форма объявлена правилом явно ·
  **7.1** условие на код у `SS-H7` называет **профиль**, по которому считается включённый набор
  (`uif` на `values.dev.yaml` рендерится, и «не рендерятся вовсе» верно лишь для базовых величин).
  Плюс две правки формулировок, смысла не меняющие: §5.1 — предикат «§7а.1 = §8» держит **одну** из
  двух названных потерь, и вторая остаётся на внимании; §6.1 — причина пятой находки названа
  разборщиком, а не прозой.
- **вернуть-в-приёмку:** **1 пункт**, не блокирующий, унаследован и неизменен — `SS-M5` (пара
  сценариев об уровне самой сессии при шаге вверх; дом — приёмка LINE-A-1, исполнитель
  `acceptance-author` → `acceptance-reviewer`, предикат снятия назван замыслом в §9.2). Ред. 12
  наблюдаемых величин приёмки не меняла — §3 побайтово тождествен, отпечаток приёмки перемерен.
- **блокер:** **по моей оси — нет.** Ни один класс не остался без отображения; ни один `Given`
  приёмки не стал неисполнимым; приёмка не сдвинулась. Одобрение замысла по признаку
  `change-graph.md` §4 не запрещено. `docs-gate` на ветке с созданным условием — **8 из 8**,
  `crossrepo-gate` — **1 из 1**, красного нет **после** приведения переписи ведомости к дереву
  (§11): посадка этой записи делает `check-06` красным по построению, и ведомость сама называет
  посадку тем изменением, которое её числа правит.

**Ответ на главный вопрос задания.** Ред. 12 сделала то, чего не делали одиннадцать предыдущих:
**перевела свой собственный контроль из прозы в команду и прогнала его**. Пять печатей выписанного
числа приведены к замеру, число получается прогоном, граница правила названа своим словом и
проиллюстрирована собственным дефектом. Оба расширения, которых в задании не было, автор нашёл сам
и измерил верно.

И ровно поэтому находки этого круга **другого рода, чем прежние**: они уже не о непроизведённом
замере, а о **словаре произведённого**. Обход стал командой — и команда недобирает инв. 29, потому
что различает ствол по буквальному имени. Число стало прогоном — и остался абзац, где число
получено разборщиком, не знающим одной формы ячейки. Поправка о строках сделана — и утверждение,
ею снятое, стоит строкой ниже в изъявительном наклонении. **Перечень перестал быть слабым местом;
слабым местом стал словарь предиката.** Это движение вперёд, а не топтание: следующий круг спорит
уже не с тем, померено ли, а с тем, что именно считает прибор.

---

## 11. ПОСАДКА ЭТОЙ ЗАПИСИ КРАСИТ `check-06` ПО ПОСТРОЕНИЮ — и ведомость сама называет, кто это правит

**Замер до правки.** Прогон `docs-gate` из рабочей копии ветки с созданным условием, сразу после
того как файл этой записи лёг в каталог: **8 проверок, пройдено 7, провалено 1** — `check-06`
тремя находками, все три арифметические:

| поле ведомости | объявлено | замер гейта |
|---|---|---|
| `binding_as_of.records_bound_to_it` | 0 | **1** |
| `completeness.census.bound_to_design` | 0 | **1** |
| `completeness.census.records_total` | 12 | **13** |

**Это не дефект ред. 12.** Ред. 12 объявила ноль привязанных записей, и **на её ревизии это было
верно**: пересверки на ред. 12 ещё не существовало. Она же объявила предикат снятия — «в каталоге
появился файл `48cb2d72f29b8813a28f50589b06d526d2a2a682396e142b706fffc1577896b9.md`» — с полностью
выписанным отпечатком, чтобы предикат был исполним. **Предикат исполнен этой записью.**

**Правку делает посадка, и ведомость говорит это своими словами.** Строка над самим полем:
«`records_bound_to_it` … Число приведено к дереву **ТЕМ ЖЕ изменением, которым запись садится**».
Прецедентов в файле два, оба дословны: «ПЕРЕСВЕРКА НА РЕД. 8 … `records_total` 8 -> 9,
`bound_to_design` 0 -> 1» и «ПЕРЕСВЕРКА НА РЕД. 9 САДИТСЯ … 9 -> 10, 0 -> 1. `superseded_rows` НЕ
меняется: обесцененных записей не прибавилось — прибавилась действующая».

**Что я правил и чего не правил.** Правлены **три числа переписи каталога** и рядом поставлены
комментарии в форме, уже принятой в файле. **Не тронуты:** номер редакции и её форма, предикат
снятия, все строки `superseded`, блок `resolved_by_design_owner`, блок
`_superseded_unresolved_kept_for_history`, любые вердикты записей. Их владение названо в самой
ведомости — автор замысла, `change-graph.md` §1, — и перепись каталога к этому владению не
относится: она измеряет `ls` каталога, а не чьё-либо решение.

**Замер после правки, из рабочей копии ветки:**
`docs-gate: рассмотрено проверок 8; пройдено 8, провалено 0, без предмета 0` ·
`crossrepo-gate: рассмотрено проверок 1; пройдено 1, провалено 0, без предмета 0`.

**Без созданного условия** (клонов продукта в рабочей копии ветки нет) тот же набор даёт
**8 проверок, пройдено 4, провалено 0, без предмета 4** — и это **не зелёное, а воздержание
четырёх**. Число проверок названо в обоих прогонах намеренно: ред. 11 отчиталась зелёным о семи
проверках при восьми на ветке, и именно так красное стало невидимым.

---

## Вернуть в приёмку

**Блокирующих — нет.** Единственный пункт — `SS-M5`, унаследованный, названный замыслом третьим
законным исходом ban #11 с домом, исполнителем и предикатом снятия. Одобрения замысла он не
запрещает.

**Новых `Given`, которых схема не допускает, ред. 12 не создала** — перемерено: §3 побайтово
тождествен ред. 11 и ред. 10; решения ред. 12 касаются чисел, обхода, границы правила, строки
ведомости и порядка полос; наблюдаемых величин приёмки не трогают. Отпечаток приёмки
`b755886fd3a9cbf4070598ad3d921bb92d53d7f4186832a2c34f1d8ca197a7ec` перемерен первым действием и
совпал.

**Неизмеренных утверждений приёмки о дереве этой редакцией не прибавилось.**

---

## Неотображённые пункты (пересверка)

**Ноль.**

Основание: таблица §3 ред. 12 **побайтово равна** таблице §3 ред. 11 (отпечаток раздела совпал,
`git diff` не несёт ханка в её координатах), а по ред. 11 отображение 22 из 22 было установлено и
записано; ред. 11 была тождественна ред. 10 по тому же разделу, ред. 10 — ред. 9. Отображение не
могло измениться, потому что не изменился его носитель.

По `change-graph.md` §4 одобрение замысла запрещает **неотображённый** пункт; таких нет. Четыре
условия раздела «Итог» — дефекты **словаря предиката, полноты перечня и двух мест об одном
предмете**, не отображения.

---

## Заказы (адресат · предмет · основание)

- **автор замысла** · внести **инв. 29** в ведомость §7а.2 строкой инварианта со своим замером и
  держателем по каждой половине; различение ствола в команде обхода не опирать на буквальное имя
  (`kaname` · «служба доступа» · путь ствола — один предмет, три написания) · основание: 4.1;
  `testing` §«Предикат по ДВУЯЗЫЧНОМУ корпусу, ищущий на одном языке, недобирает МОЛЧА»;
  `measurement-discipline` §«Предикат — только с контролем».
- **автор замысла** · привести строку 4141 к собственной поправке: «рост **497**» и «оба прогона
  сделаны ПОСЛЕ последней правки ред. 11» либо исправить, либо пометить снятыми — сейчас они стоят
  в изъявительном наклонении строкой ниже ячейки, объявившей их неверными · основание: 3;
  `doc-truthfulness` (два места об одном предмете, одно верно); критерий самого автора об
  утверждениях о предмете.
- **автор замысла** · привести строки **L0b**, **L4**, **L6** §7а.1 к п. 5: команда исполнима
  дословно, без многоточия; ссылочная форма («см. L4», «тот же модуль, что L0a») объявлена правилом
  явно либо заменена своим замером · основание: 2.1; п. 3 и п. 5 самого правила;
  `testing-verdict` → `foreign-predicate-run-verbatim`.
- **автор замысла** · в условии на код у `SS-H7` назвать **профиль**, по которому считается
  включённый набор манифестов входа; поправить утверждение об `uif` («не рендерятся вовсе» верно
  для базовых величин, на `values.dev.yaml` `uif.enabled: true` и собственное умолчание сабчарта
  `ingress.enabled: true`) · основание: 7.1; `measurement-discipline` §«Форма числа» (единица —
  профиль, не чарт); тот же довод о популяции, которым автор снял слово «единственный».
- **автор замысла** · в §7а сказать словом, что названный однофайловый предикат держит **одну** из
  двух названных потерь (девятнадцатую полосу), а вторую — число, разошедшееся с деревом — не
  держит · основание: 5.1; собственная норма §7а о третьем исходе ban #11.
- **автор замысла** · в §8 назвать причину пятой находки предиката цепи точно: это не проза в
  клетке, а **колонка, прочитанная не та** (экранированная черта в строке L9) · основание: 6.1;
  `testing-verdict` → `own-parser-knows-all-forms`.
- **`tooling-maintainer` / `gate-author`** · предикат «имена §7а.1 = имена §8» (заказ открыт
  замыслом, дом — воркспейс); разборщик колонок обязан знать экранированную черту, инъекция —
  ячейка с `\|`, близнец — без; **оба близнеца из дерева**: строки `| **L9** …` и `| **L11** …`
  того же файла · основание: 5, 6.1; `testing` §«Гейт на класс…» п. 7.
- **`gate-author` / `tooling-maintainer`** · гейт сверки полос носителя: перепись «полос гашения N ·
  несущих ключ `Domain` M», инъекция в обе стороны; дом `kacho/gateway` · основание: заказ открыт с
  ред. 10 и подтверждён ред. 12 словом (инв. 38); `architecture` → `arch-lanes-gate-census`.
- **`gate-author` / `tooling-maintainer`** · `check-02-…` в `scripts/crossrepo-gate` (половина (б)
  инв. 37 и половина (2) инв. 30) — дом существует и измерен (3 ствола, 10575 путей), прибора нет ·
  основание: инв. 30, инв. 37.
- **`deploy-engineer`** · страж на выключенность сабчартовых манифестов входа умбреллы: сегодня
  `api-gateway.ingress.enabled: false` держится **величиной без стража**, и рост включённого набора
  до 2 не краснит ничего · основание: 7, 7.1; `security-hardening` → `hard-missing-knob-vs-unseeded`.
- **`acceptance-author` → `acceptance-reviewer`** · пара сценариев `SS-M5` · основание: §9.2 замысла.

Конструкцию каждой пробы и каждого гейта строит её автор — здесь назван только заказ.

---

## Петля накопления

### Предложение записи нового класса — ПОРОГ ВЗЯТ, владелец `measurement-discipline`

**Имя.** Перечень, переведённый из прозы в команду, наследует слабость **словаря** команды: он
недобирает молча ровно те предметы, что названы **описанием**, а не тем написанием, которое
различает предикат.

**Признак, наблюдаемый до кода.** Обход объявлен командой; различение оси (ствол, домен, роль,
владелец) сделано по **буквальному имени**; в корпусе тот же предмет законно называется ещё и
описанием или путём.

**Противоядие.** Перечислить написания оси **до** написания команды и прогнать контроль в обе
стороны: сколько блоков несут имя без описания, сколько — описание без имени. Непустое второе
множество есть находка **предиката**, а не корпуса.

**Два независимых вхождения — оба в дереве, оба измерены:**
1. **ред. 11 → ред. 12**: обход-проза допускал два прочтения и недобрал **инв. 30**; чинилось
   переводом в команду (§7а.2 замысла).
2. **ред. 12 (эта запись, 4.1)**: обход-команда различает ствол по имени `kaname` и недобирает
   **инв. 29**, называющий тот же ствол как «поставку службы доступа».

**Измеренная цена** (сверх порога): инв. 29 — это `SS-H7`, решение, объявленное замыслом **несущим**,
и недобор случился **в той же редакции**, которая вписала ему условие быть двухствольным.

**Предикат снятия — на внешнем факте, умеет ответить «нет»:** «в `scripts/` лежит прибор, который
перед прогоном обхода печатает перепись написаний своей оси и краснеет на непустом множестве
«описание без имени»». Пока такого прибора нет — запись в силе.

**Чем будет держаться:** сегодня — вниманием и этой записью; заказ на прибор назван выше.
**Вписывает не я:** правила и скиллы — владение `tooling-maintainer` (`ai-tooling.md`).
**Владелец по противоядию** (`code-authoring` §9.2): противоядие предписывает **произвести число**
(перепись написаний, контроль в обе стороны) → `measurement-discipline`.

### Кандидат в однофайловый предикат `class-guard` — ЕСТЬ, оба близнеца из дерева

**Предикат.** В markdown-таблице документа разборщик, делящий строку по сырому `|`, обязан быть
признан негодным: строка, содержащая `\|`, даёт на одно поле больше и сдвигает колонки.

**Живой отрицательный близнец** (строка с `\|`, на которой наивный разборщик ошибается):
`docs/specs/sub-phase-LINE-A-1-own-authorization-endpoint-and-code-design.md`, строка таблицы §8
`| **L9** (вне кода, до посадки L3 и **L13**) | …` — одна экранированная черта, 9 полей вместо 8.
**Живой положительный близнец:** строка `| **L11** …` того же файла и той же таблицы — 8 полей.

Требование `.claude/hooks/class-guard/README.md` §«Как добавить предикат» выполнено: оба близнеца
взяты из дерева, живой отрицательный существует.

---

## Посылка задания

**Посылка «редакция 12 отвечала на мою запись на редакцию 11» — ПОДТВЕРЖДЕНА** командой:
`git show --stat HEAD` называет предметом правки шесть условий и четыре точечных предмета записи
`revalidation/85b854367….md`; сверка тел (`git diff 88465f8a HEAD -- <замысел>`, 596 строк дельты)
показала правки ровно в §7а, §7а.1, §7а.2, §7, §8, §9.1, §9.2 и в шапке — и **ни одного ханка** в
§3. Из шести моих условий закрыты **четыре** (1.5, 2.2, 3.1, 5.2), **не закрыты два** (1.2, 1.3 —
строки L0b/L6 побайтово те же).
