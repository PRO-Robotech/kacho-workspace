# Sub-phase F4d (посадка личности разводится полем — свой вход человека, стражи и компонент уходят) — Acceptance

> **Статус:** DRAFT — ожидает `acceptance-reviewer` (ban #1: до APPROVED не пишется ни строки кода).
> **Дата:** 2026-08-24 · **Ревьюер:** `acceptance-reviewer` (единственный approve-gate; заказчик подключается на шаге 7, к финальному smoke/e2e).
> **Задача:** продукт #1125 (Ф4д) · **Родитель:** #1107 · **Эпик:** #896 · **Веха:** воркспейс #382 (M7) · **Метки:** `area:iam`, `area:deploy`, `P1`, `size:XL`, `release:identity`.
> **Объём расширен решением оркестратора:** сюда перенесён остаток закрытой #1122 — **свой вход человека и его сессия**. Обоснование в комментарии #1122: у оставшихся половин предмет один, а вести раздельно значило бы требовать двух приёмок на один предмет и дважды трогать проверку настройки при старте.
> **Предшественники по контракту:** `docs/specs/sub-phase-F1-token-signer-acceptance.md` (своя чеканка), `docs/specs/sub-phase-F2-client-assertion-acceptance.md` (проверка утверждения клиента), `docs/specs/sub-phase-IAM-INT-1-interactive-login-capability-acceptance.md` (интерактивный вход через поставщика — эта приёмка её **наследует**, см. §6).
> **Монорепо:** `github.com/PRO-Robotech/kacho`. **База сверки — накопительная ветка `release/identity-3` @ `143ace3e5`** (2026-08-24, +38 коммитов к стволу). Все числа §1 сняты на ней и снабжены предикатом, чтобы их можно было повторить и опровергнуть, а не поверить. Ствол отстаёт на всю работу Ф4б — числа, снятые на нём, к этому документу не относятся.
> **Род документа:** **ПРИЁМКА, САНКЦИОНИРУЮЩАЯ РАБОТУ** — несёт сценарии Given-When-Then и после APPROVED разрешает кодировать объём §9 в порядке §10. Кода не содержит.
> **Форма выбрана под публичность репозитория.** Документ читается как перечень **требований**, а не как разбор состояния: сборка «точная координата + условие, при котором защита не действовала + следствие для постороннего» здесь не производится (`security.md` §«Публичные артефакты», признак восстановимости). Три места из-под запрета выведены и написаны прямо: текст отказа оператору при отказе в старте, комментарий у самого гейта, имя и текст падения пробы.
>
> **Нормативка — по названию раздела, без пересказа в теле:**
> - `.claude/rules/security.md` — §«AuthN+AuthZ ВЕЗДЕ», §«Production-mode обязателен ВЕЗДЕ», §«Контроль, действующий на ВЫДАЧЕ, но не на ПРЕДЪЯВЛЕНИИ», §«Контроль, у которого нет МЕХАНИЗМА исполниться», §«Публичные артефакты»
> - `.claude/rules/architecture.md` — §«Параллельные полосы одного механизма обязаны сверяться МЕЖДУ СОБОЙ», §«Конфигурация, впервые ставшая ЧИТАЕМОЙ, перебивает умолчание поставщика»
> - `.claude/rules/api-conventions.md` — §«Принято-и-проигнорировано», §«Неисполнимая возможность: ДВА ПРАВИЛА ОБ ОДНОМ ПОЛЕ», §«Поле контракта не выходит наружу, пока КРАЙ не пересобран»
> - `.claude/rules/data-integrity.md` — §«Within-service инварианты — ТОЛЬКО на DB-уровне», §«Данные СТЕНДА заводятся посевом, а не миграцией»
> - `.claude/rules/testing.md` — §«Гейт на класс», §«E2E НИКОГДА не пропускаются»
> - `.claude/rules/e2e-flow.md` — §11 «Проба, чей предмет исчез, ЗАМЕНЯЕТСЯ, а не ослабляется»
> - `.claude/rules/00-kacho-core.md` — ban #1, #5, #6, #9, #10, #11, #12, #14, #15, #16

---

## §0 Обзор

Служба прав не поднимается в боевой посадке без **трёх** адресов внешнего поставщика личности,
а край — ещё без **двух** своих. Пока это так, отдельный клон продукта в боевой посадке не
поднимается, сколько бы кода ни переехало: единственный доступный выход — режим разработки, то
есть ровно та посадка, которую все прочие стражи запрещают.

Подфаза делает три вещи, и они **неделимы**:

1. **заводит вход человека и его сессию как предмет продукта** — со своими учётными данными,
   своим носителем сессии, своей ступенью доверия и своей обратимостью отказа. Это остаток
   #1122, и он не «перенос вызовов»: у нас сегодня **ноль** колонок учётных данных человека и
   **ноль** таблиц его сессии (§1.4, §1.5);
2. **разводит посадку явным полем** `authn.identity-provider` со значениями `external` и `own`,
   после чего стражи становятся **полосными**: провайдерские действуют на одной полосе, свои —
   на другой, и **ни при одном значении поля посадки без проверки личности не возникает**;
3. **снимает компонент** из развёртывания и из дерева — пять подчартов, десять шаблонных
   артефактов, код обвязки и записи ведомости поверхностей.

**Почему именно вместе.** Снятие сессии у поставщика выданный им токен **не гасит** — это
измерено пробой с контролями в обе стороны, и пока полоса церемонии жива, тот рычаг остаётся
единственным механизмом обратимости: отказ по отсечке без него даёт **стоящее** состояние, в
котором ничто не запросит повторной аутентификации. Снять рычаг, не сняв церемонию, значит
поменять работающий выход на тупик. Обратно: снять церемонию, не заведя свой вход, значит снять
у человека возможность войти. Половины держат друг друга.

**Что этот документ НЕ решает** — §7 (границы с #1123 и соседями) и §15 (открытые вопросы
владельцу). Догадкой они не заполняются.

---

## §1 Что верно в дереве — измерено, а не предположено

Все команды — на `origin/release/identity-3` @ `143ace3e5`. Единица счёта названа у каждого числа.

### 1.1 Стражей провайдерских адресов ПЯТЬ, а не три: три в службе прав и два на крае

Задача называет три. Их действительно три — **в службе прав**, и живут они в двух функциях
проверки настройки, обе внутри ветки боевой посадки:

| адрес (имя настройки) | переменная окружения | что стережёт |
|---|---|---|
| `authn.hydra-admin-url` | `KACHO_IAM_HYDRA_ADMIN_URL` | административная дорога к поставщику |
| `authn.hydra-jwks-url` | `KACHO_IAM_HYDRA_JWKS_URL` | набор проверочных ключей, который процесс зеркалит на внутреннем слушателе |
| `authn.hydra-token-url` | `KACHO_IAM_HYDRA_TOKEN_URL` | адрес обмена подписанного утверждения на токен |

Опыт (боевая посадка, своя чеканка включена, адресов нет ни одного) даёт **ровно три** отказа:
первые два накапливаются, третий возвращается первым в своей функции; процесс завершается с
ненулевым кодом. Отказы называют настройку, переменную окружения и то, зачем полоса нужна.

**Но у края есть свои два, и задача их не называет.** Предикат — перечислить функции проверки
настройки края, отказывающие в старте, и посмотреть, какие адреса они требуют:

| страж края | требует | предмет |
|---|---|---|
| проверка настройки проверяющего токен | адрес набора ключей поставщика + ожидаемый издатель | приём токена, выданного поставщиком |
| проверка настройки отзыва | адрес интроспекции **и** административный адрес поставщика | живость чужого токена + снятие сессии входа на его стороне |

Итого **пять** стражей об одном предмете в двух процессах. Различать их обязательно, потому что
владельцы у них разные: административный адрес края нужен **выходу** и уходит с этой задачей;
адрес интроспекции и ожидаемый издатель — предмет #1123 (§7).

> **Следствие для объёма:** формулировка «три стража уходят» верна для службы прав и **неполна**
> для посадки. Приёмка требует полосности у **всех пяти** — иначе стенд в посадке `own` не
> поднимется по причине, к его работе отношения не имеющей, и это будет тот же класс, что чинила
> #1118.

### 1.2 Полей поставщика в настройке службы прав — семь, и восьмая ручка вне надзора

```
git grep -n 'mapstructure:"[^"]*hydra' origin/release/identity-3 \
  -- 'services/iam/internal/apps/kacho/config/*.go' | grep -v _test | wc -l     # → 7
```
Единица счёта — **поле структуры настройки**. Семь полей: издатель, административный адрес и его
якорь доверия, адрес обмена и его якорь, адрес набора ключей и его якорь.

**Восьмая ручка существует и полем настройки НЕ является** — административный предъявитель
поставщика читается прямым обращением к окружению в корне сборки. Предикат:
`git grep -n 'mapstructure:"[^"]*admin-token' -- 'services/iam/**/*.go'` → **пусто**.

> Следствие названо как **требование**, а не как находка о состоянии: всякая ручка, от которой
> зависит разговор с поставщиком, обязана быть видна проверке настройки при старте — иначе
> полосность посадки окажется неполной ровно на ту ручку, которую проверка не видит.

### 1.3 Пол аутентификации в каталоге прав — 342 записи, уровня «3» не требует ни одна

```
git show origin/release/identity-3:gateway/internal/middleware/embed/permission_catalog.json \
  | python3 -c "import json,sys,collections; d=json.load(sys.stdin); \
print(len(d), dict(collections.Counter(str(e.get('required_acr_min','нет')) for e in d)))"
```

| требуемый пол | записей | что это значит |
|---|---:|---|
| `"1"` — пароль (AAL1) | **285** | рутина: обычный CRUD ресурсов |
| `"2"` — второй фактор / устойчивость к фишингу (AAL2) | **32** | чувствительные: выдача и снятие удостоверений, запись привилегий, снос тенантности |
| поля нет — освобождённые | **25** | глобальный справочник и прочее |
| **всего** | **342** | |
| **`"3"` — привязанный к устройству ключ (AAL3)** | **0** | **ни один глагол его не требует** |

Ранжирование — единственная функция платформы, её зовут **обе** точки применения (край и
внутренний слушатель службы прав); ни одна не держит своей таблицы. Освобождает от пола **только**
машинный принципал; человек не освобождён никогда.

**Это число определяет объём полосы `own`.** Полоса, дающая только пароль, закрывает человеку
**32** записи из 342 — и это ровно те, где меняется посадка безопасности. Полоса, дающая пароль
и второй фактор, закрывает **ноль**: уровня «3» не требует никто.

> Прежний замер той же оси — 26/210/64 из 300 — снят в приёмке IAM-INT-1 на другой ревизии. Числа
> расходятся, потому что каталог вырос; действителен тот, у которого назван предикат и ревизия.

### 1.4 Своих учётных данных человека — ноль колонок

Предикат по нашим миграциям (единица счёта — **объявление колонки** в отслеживаемых файлах
миграций службы прав):

```
git grep -hoiE '^\s+[a-z_]*(password|credential|passwd|secret_hash)[a-z_]*\s+(text|bytea|varchar)' \
  -- 'services/iam/internal/migrations/*.sql'                                   # → пусто
```
`totp` → 0, `webauthn` → 0, `identity_schema` → 0. Все попадания слов `password`/`credential` —
комментарии (проверено построчно).

Зеркало человека несёт **внешний идентификатор**, почту, отображаемое имя, аккаунт и состояние
приглашения — и ничего, чем можно проверить, что перед нами он. Проверку пароля, второй фактор,
ключ устройства, восстановление и подтверждение почты исполняет **поставщик**, в своей базе.

**Способы входа, объявленные его настройкой сегодня** (перечень нужен §2 Р4 и §11 как предмет
переписи): ключ устройства без пароля · пароль (Argon2id, память 64 МБ, три итерации, четыре
потока, плюс проверка на утечку по k-анонимности) · одноразовый код по времени как второй
фактор · резервные коды. Плюс потоки: регистрация, вход, настройки (с окном привилегированной
сессии), восстановление кодом, подтверждение почты кодом, выход. Федерация в его настройке
**выключена**.

### 1.5 Своей сессии нет; своя отсечка есть и уже читается на предъявлении

Своей таблицы сессии человека у нас нет (57 таблиц службы прав, со словом `session` — две, и ни
одна сессией человека не является: одна ведёт отзывы по идентификатору удостоверения, вторая —
состояние протокола федерации).

Носитель сессии сегодня — печенье поставщика; его срок объявлен его настройкой равным **24 часам**,
уровень, требуемый для ответа «кто я», — **первый**.

Что **уже наше** и работает: отсечка субъекта (одна строка на человека, монотонная вперёд), её
пишут выход, принудительный выход и хук завершения восстановления; читают — хуки выдачи **и**
обе полосы личности человека на крае через внутренний глагол, введённый работой #1122. То есть
свойство «наш отзыв действует на предъявлении, а не только на выдаче» **достигнуто** и
переписыванию не подлежит (§6).

Чего у отсечки нет: она делает отказ, но **не** делает повторную аутентификацию. Сегодня её
делает административный рычаг поставщика — и это единственная причина, по которой две записи
ведомости (§1.6) ещё живы.

### 1.6 Ведомость поверхностей — шесть записей, две из них ждут ИМЕННО эту задачу

Гейт `internal/repohygiene` `TestProviderSurfaceIsBoundedByTheLedger`: словарь путей поставщика —
**5**, записей ведомости — **6**, послаблений — **1** (сам словарь). На пустой ведомости гейт
**проходит** и говорит, что это цель фазы, а не поломка; падает он на непрочитанном дереве.
Способность падать доказана инъекцией — **11** утверждений.

Из шести записей **две** относятся к человеку, обе про снятие сессий входа у поставщика (одна на
крае, одна в службе прав), и у обеих предикат снятия записан дословно:

> `Until: "вход человека перестал заводить сессию у поставщика"`

**Это и есть предикат снятия S1 настоящей приёмки**, записанный не нами и раньше нас. Остальные
четыре — машинные полосы: две про интроспекцию (#1123), одна про адрес обмена, одна про жизненный
цикл клиента у поставщика.

### 1.7 Компонент в посадке — пять подчартов и десять шаблонных артефактов

Число «пять» из тела задачи проверено и **верно**: два процесса (личность и выдача токенов), две
их базы и консоль самообслуживания. Четыре объявлены зависимостями зонтичного чарта с прибитыми
версиями; пятый лежит каталогом и намеренно зависимостью не объявлен.

**Граница числа названа**, чтобы «пять» не читалось как исчерпывающее: подчартами тир не
исчерпывается — ещё **10** артефактов живут шаблонами (шесть у зонта, четыре у чарта службы
прав): якорь и служба административного хопа, задание регистрации доверия издателям, страж
учётных данных обратного вызова, объявление настройки личности, её схема и её хуки.

Отдельно: **консоль самообслуживания снять простым выключением нельзя** — наш веб-сервер
безусловно проксирует к ней пути входа, регистрации, восстановления, подтверждения, настроек,
согласия и выхода; при выключенном подчарте службы не существует и веб-сервер отказывает при
старте. Это делает снятие консоли **связанной** правкой, а не отдельной (сценарии S3).

### 1.8 Профили, стенды — и три стенда, до которых объявленное не доезжает

Профилей зонтичного чарта — **10**; цепочки объявлены в единственном месте дерева и держатся
двумя гейтами (вторая копия цепочки — находка; профиль, не названный ни одной цепочкой, —
находка). Стендов — **6**.

**Раскатывающее место есть не у всех.** По замеру задачи #1161 на этой же ветке: у трёх стендов
из шести цепочку не раскатывает **ни одно** место дерева — `helm upgrade` для них собирается
рукой. Следствие для этой подфазы названо прямо: **новое поле посадки до такого кластера само не
доезжает**, и снятие компонента там не наступит от того, что мы правим профиль.

Механизм, которым это оформляется, в дереве **уже есть** и служит образцом: записанное
исключение с причиной **и** предикатом истечения, читающим дерево, а не память автора; сегодня
таких записей две, и обе истекают тремя разными способами.

### 1.9 Проба про двух издателей — что она утверждает и чего у неё нет

`TestF1b_OurIssuerIsADDEDToTheListNotSubstitutedForIt` (`gateway/deploy/f1b_token_acceptance_declared_test.go`)
утверждает: профиль, назвавший **нашего** издателя, обязан нести в перечне приёма **хотя бы одну
не нашу** запись. Вердикт выносит **тот же разборщик**, что исполняет процесс при старте, — свой
разбор строки разошёлся бы с ним на вырожденном значении.

Перепись, которую она печатает на этой ревизии: **профилей 10, объявляют нашего издателя 3,
перечень разобран у 3**. Профилей, где наш издатель — единственная запись, — **0**; у каждого из
трёх ровно **1** не-наша.

Два свойства этой пробы существенны для §2 Р9:

- **на исчезнувшем предмете она зеленеет**: при нуле профилей, объявивших нашего издателя, она
  печатает строку и проходит. Два её соседа по файлу на том же условии **роняют** прогон. Из
  шести проб файла эта — единственная с таким исходом;
- **инъекции у неё нет** (предикат: файлов с инъекцией в каталоге проб развёртывания края — 0),
  то есть её способность упасть не доказана.

Файл сам называет, чего у него нет: сквозного доказательства, что край **и сегодня принимает**
предъявителя прежней полосы, у машинных наборов больше не осталось — машинного пути к такому
предъявителю нет by construction после #1120 и #1121, а на шарде края церемония не гоняется
(остаток заведён #1175).


---

## §2 Решения — ратифицировать явно, ревьюер проверяет каждое

Вопрос, получивший «зависит от», считается неотвеченным.

### Р1. Поле — закрытый словарь из двух значений, БЕЗ умолчания в коде

`authn.identity-provider ∈ {external, own}`. Незаданное значение — **отказ старта**, называющий
поле и оба законных значения. Неизвестное значение — тоже отказ, а **не** откат к «безопасному».

**Почему умолчания в коде нет — обе альтернативы разобраны, а не отброшены:**

- умолчание `external` заставило бы каждый профиль, поля не объявивший, требовать адресов,
  которых у него нет. Поле, которое ничего не разводит, — это поле без читателя;
- умолчание `own` **молча сняло бы** провайдерские требования у профиля, который просто забыли
  обновить. Это и есть состояние «продукт поднялся, а посадку никто не выбирал» — ровно то, что
  директива владельца по релизу запрещает.

**Умолчание живёт в ПРОФИЛЕ, а не в коде.** Базовый профиль зонтичного чарта объявляет значение
явно, поэтому ни один существующий стенд не ломается; отдельный клон, пишущий свои значения,
обязан выбрать сам — и это ровно то, ради чего предикат готовности вехи M7 сформулирован как
«отдельный клон поднимается в боевой посадке».

**Пустая строка значением не является** (`api-conventions.md` §«Пустое значение обязано означать
„пусто“»; `code-authoring` §1.3 — представление «не задано» не пересекается с областью значений).
Перечень законных значений **выводится из типа**, а не выписывается в проверках: выписанный
разошёлся бы с типом на первом же новом значении.

### Р2. Стражи полосные, и ни при одном значении поля посадки без проверки личности не бывает

| полоса | что применяется | что НЕ применяется |
|---|---|---|
| `external` | все пять стражей §1.1 — **дословно как сегодня**, включая тексты отказов | требования полосы `own` |
| `own` | требования полосы `own` (ниже) | пять стражей §1.1 |

**Требования полосы `own`** (каждое — отказ старта, не предупреждение):

1. своя чеканка включена и её настройка валидна;
2. издатель объявлен, и он наш; набор проверочных ключей публикуется;
3. свои учётные данные человека провязаны — хранилище способов входа доступно;
4. своя сессия провязана — хранилище сессий доступно, носитель объявлен;
5. **посадка умеет предъявить каждый уровень доверия, которого требует каталог прав.** Полоса, у
   которой не провязан второй фактор, при каталоге с непустым множеством записей уровня «2»
   старт **не проходит**. Величина берётся из каталога, а не из константы.

**Инвариант, ради которого приёмка написана:** множество значений поля × множество обязательных
элементов полосы обязано быть покрыто пробой отказа старта **целиком**, и **не существует
значения поля, при котором множество обязательных элементов пусто**. Пятое требование — не
украшение: без него полоса `own` объявляет пол, которого не может предъявить, и посадка
получается ослабленной молча.

**Тексты отказов провайдерской полосы не меняются** — они часть контракта оператора
(`api-conventions.md` §Error-format, тон сообщений). Полосность добавляет к ним **одну** строку:
чем это требование обусловлено и каким значением поля снимается.

### Р3. Страж своей чеканки: посадочная половина переезжает, половина полноты провязки остаётся

Задача говорит, что страж, поставленный подфазой #1119 «в сборке», переезжает к остальным. Он
делится на две половины, и переезжает **одна**:

| половина | предмет | где живёт после |
|---|---|---|
| **посадочная** — «в посадке `own` своя чеканка обязана быть включена» | значение настройки | **проверка настройки при старте**, вместе с прочими требованиями полосы |
| **полнота провязки** — «подписант и состав утверждений провязаны» | объекты композиционного корня | **остаётся в сборке**: настройка объектов не видит и выразить их отсутствие не может |

**Ни одна из половин не смягчается**, и разойтись во мнении они не могут: включённость контура
обе читают **одним** аксессором — это свойство уже заведено #1119 и сохраняется дословно.
Требование к пробам: обе точки отказывают, и проба называет **обе**; проба, доказавшая одну,
о второй не утверждает ничего.

### Р4. Свой вход человека — что продукт ОБЕЩАЕТ и что НЕ обещает

**Модель заводится сразу шире, чем исполнение**, и это осознанное решение о представлении, а не
задел «на будущее»: способ входа — **строка отдельной таблицы** (человек, вид способа, материал
проверки, уровень доверия, состояние), а не колонки в зеркале человека. Причина измерена
классом, а не вкусом: добавить второй вид способа потом в колоночной модели значит мигрировать
каждую строку, а представление выбирается **в первой миграции** (`code-authoring` §1.3, §5.1).

**Обещается арендатору:**

| обещание | форма |
|---|---|
| вход по паре «почта + пароль» | пароль хранится **только** проверочным материалом; параметры устойчивости — не слабее объявленных сегодня (§1.4) |
| второй фактор — одноразовый код по времени | уровень доверия «2»; заводится и снимается самим человеком |
| резервные коды | одноразовые, показываются один раз, невосстановимы |
| смена пароля | требует подтверждения текущим способом |
| выход | снимает сессию **на предъявлении**, не только на выдаче |
| принудительный выход и блокировка | администратором; действует так же, на предъявлении |
| **восстановление доступа — сбросом распорядителем** | распорядитель аккаунта сбрасывает способ входа человека; свой аккаунт распорядителя — надзор облака; надзор облака — бутстрап-удостоверение. Цепочка **завершается** и не требует почтового тракта |

**НЕ обещается — названо, потому что необъявленное потом дорого вводить:**

| не обещается | почему | условие возврата |
|---|---|---|
| ключ устройства (passkey/WebAuthn) | уровня «3» не требует **ни одна** из 342 записей каталога (§1.3), поэтому ни один глагол не становится недостижим. Устойчивость к фишингу при этом теряется, и это **цена**, а не ноль | отдельная подфаза; модель способов её уже допускает — миграции данных не потребуется |
| самостоятельное восстановление по почте | требует своего почтового тракта: отправитель, шаблоны, ограничение темпа, одноразовый код с коротким сроком. Это отдельный домен со своей приёмкой, а не поле в чужом потоке | почтовый тракт заведён продуктом |
| подтверждение владения почтой | тот же тракт; сегодня его исполняет поставщик | вместе со строкой выше |
| вход через внешнего поставщика (федерация) | в настройке поставщика она **выключена** и сегодня; заводить её при переезде значило бы переносить то, чего нет | требование арендатора |
| самостоятельная регистрация человека с улицы | вход и регистрация — разные церемонии; сегодня человек заводится приглашением либо посевом | продуктовое требование |

**Правило, связывающее обе колонки:** способность, которую полоса `external` арендатору **даёт**,
а полоса `own` не даёт, — **находка, а не умолчание**. Перечень таких способностей производится
**переписью** (сценарий S3), а не мнением, и пока он непуст, значение `external` живо и это
записано. Иначе снятие компонента стало бы тихим понижением того, что арендатору уже обещано.

### Р5. Своя сессия — срок, носитель, отзыв, обратимость

| ось | решение | почему так |
|---|---|---|
| носитель | наше печенье, имя **наше**, флаги «только по протоколу», «только по защищённому соединению», ограничение межсайтовой отправки, путь — корень | имя обязано отличаться: обработчик выхода чистит носители по **перечню имён**, и перечень должен называть наш, иначе снятое и наше неотличимы |
| срок | **абсолютный**, не длиннее объявленных сегодня 24 часов | паритет с §1.5. Понижение или повышение — отдельное решение, а не побочный эффект переезда |
| окно бездействия | **не вводится** | два срока об одном предмете дают два места, из которых верно одно; ввод — отдельное решение владельца |
| состав записи | субъект · **момент аутентификации** · **уровень доверия** · вид способа, которым вошли · срок | момент нужен отсечке (она спрашивает по паре «субъект, момент»), уровень — ступени. Нулевой момент остаётся **отдельным** исходом: не «давно» и не «только что» — свойство уже закреплено и сохраняется |
| отзыв | авторитет — **та же** отсечка, что сегодня; новых источников об одном предмете не заводится | два источника об одной отсечке разошлись бы молча |
| **обратимость** | отказ по отсечке **заканчивает носитель** и снимает запись сессии — повторную аутентификацию запрашивает **наше** действие | это и есть замена административного рычага поставщика; без неё отказ остаётся стоящим (§0) |
| недоступность авторитета | **fail-closed** на отказе, носитель при этом **не** гасится | заминка своего же соседа — не повод выкидывать тех, кого никто не отзывал. Свойство введено #1122 и сохраняется дословно |

### Р6. Ступень доверия — СВОЙСТВО СПОСОБА входа, и второй ранжировки не заводится

Уровень доверия приписан **виду способа**, а не константе полосы: пароль → «1», второй фактор →
«2», ключ устройства (когда появится) → «3». Тогда введение нового способа не меняет ни одной
строки и ни одной проверки.

Решение о ступени остаётся в **единственной** функции платформы, которую уже зовут обе точки
применения. Полоса `own` подаёт в неё свои значения и **не заводит** ни своей таблицы рангов, ни
своего освобождения. Освобождение остаётся ровно одно — машинный принципал.

**Ни один пол не понижается** (наследуется из IAM-INT-1, Р4 той приёмки): 285/32/25 — измерение,
а не предмет правки. Если после переезда какой-то пол окажется недостижим, чинится **полоса**, а
не пол.

### Р7. Порядок снятия компонента обратен интуиции

Снимать начинают с самого заметного — с объявления. Здесь правильный порядок обратный
(`code-authoring` §4.2):

1. **полоса `own` существует и исполнима** (S1);
2. **профили переводятся** на `own` — по стенду, каждый со своей проверкой посадки **живого
   процесса**, а не карты настроек (S2);
3. **подчарты и шаблонные артефакты снимаются** — только со стендов, где значение уже `own` (S3);
4. **код обвязки и записи ведомости** снимаются (S3);
5. **значение `external` снимается последним** — и только когда его не объявляет ни один профиль.

Каждое утверждение о снятии парно с **живым контролем**: «имени нет» иначе неотличимо от
«словарь пуст» и от «обход ничего не прочитал».

### Р8. Поле идёт ВМЕСТЕ со своей полосой, а не после неё — уточнение порядка задачи

Задача ставит поле «последним, а не первым», и обоснование #1107 верное: значение `own`,
введённое до переезда контуров, даёт стенд, который поднимается и **не может выдать ни одного
токена**.

Машинные контуры переехали (Ф4б закрыта). Осталась полоса человека. Значит поле, введённое
сегодня **без** неё, даёт стенд, на который **не может войти человек**, — тот же класс, другая
половина.

**Решение:** поле и полоса `own` садятся **одной стадией** (S1). Порознь получается либо значение
без исполнимого входа, либо полоса, которую нечем включить и потому нечем проверить.

> Ревьюеру: это **уточнение текста задачи**, и оно требует явного подтверждения (§14 п. 7).

### Р9. Проба про двух издателей ПЕРЕЕЗЖАЕТ вместе с предметом — не отключается и не ослабляется

Свойство «наш издатель **добавлен** к перечню, а не подставлен вместо» верно ровно там, где
перечень обязан содержать чужую запись, — то есть на полосе `external`. Снятие второго издателя
делает утверждение ложным о **верном** мире, и это ровно тот случай, для которого норма
предписывает **замену**, а не ослабление (`e2e-flow.md` §11).

Проба становится **функцией полосы**:

| профиль объявляет | проба требует |
|---|---|
| `external` | перечень приёма несёт **хотя бы одну не нашу** запись — формулировка и текст падения сохраняются **дословно** |
| `own` | перечень приёма несёт **ровно одну** запись, и она наша |
| поля нет | **находка** — профиль не выбрал посадку (Р1) |

И три вещи, которых у неё сегодня нет, заводятся тем же изменением:

- **отказ на исчезнувшем предмете**: ноль профилей, попавших под суд, роняет прогон — как это
  делают два её соседа по файлу;
- **перепись тремя величинами**: профилей всего · внешних · своих. Одно число не отличает
  согласие от того, что вторую полосу не спросили;
- **инъекция в обе стороны** на синтетических профилях: подставить нашего вместо чужого на
  `external` → красное с именем профиля; оставить второго на `own` → красное; законные формы
  обеих полос → молчит. Без синтетики доказательство исчезает вместе с последним внешним
  профилем.

---

## §3 Асимметрия цены: где отказ при сомнении ОБЯЗАТЕЛЕН

Цена ошибки в две стороны здесь **не** симметрична, и потому по каждой оси названо направление.

| ось | ошибка в сторону «пропустить» | ошибка в сторону «отказать» | направление |
|---|---|---|---|
| значение поля не распознано | посадка выбрана не человеком, а умолчанием | стенд не поднялся, оператор увидел текст с именем поля | **отказать** |
| элемент полосы `own` не провязан | продукт работает с дырой в проверке личности | стенд не поднялся | **отказать** |
| каталог требует пола, которого полоса не даёт | человек либо получает отказ на 32 глаголах, либо — хуже — пол объявлен и не применён | стенд не поднялся | **отказать** |
| авторитет отсечки не ответил | отозванный продолжает работать | неотозванные получают отказ на время заминки | **отказать**, но носитель не гасить (Р5) |
| версия соседа старше и глагола не предлагает | то же, что строкой выше | **консоль ложится на всё окно раската** | **пропустить**, громко и со счётчиком — раскат не атомарен, и это состояние сходится само |
| профиль не объявил поле | посадка неизвестна | профиль правится одной строкой | **отказать** |

Последние две строки — **разные** исходы, и различать их обязательно: расхождение версий исчезает
от раската, неверная настройка — никогда (`security.md` §Hardening п. 8). Мягкий проход законен
только там, где отличим от неисправности **предикатом**, а не намерением.

---

## §4 Классы, невидимые на положительном пути

Каждый — наблюдался в этом дереве; здесь они названы как **требования**, которые сценарии §11
обязаны закрыть.

1. **Возможность, объявленная и неисполнимая ни при каком входе.** Значение поля, ручка, глагол
   — всё, что можно объявить и нельзя применить. Закрывается требованием: **каждый** сценарий
   §11 вызываем, минимально-законный вход существует (§14 п. 2).
2. **Два правила об одном поле.** Величину, которую назначает запись, проверка входа обязана
   принимать незаданной. Для сессии это момент аутентификации и уровень: их назначает вход, а не
   вызывающий.
3. **Контроль, действующий на выдаче и не действующий на предъявлении.** Половина уже закрыта
   #1122; переписывание обязано её сохранить, а не воспроизвести (§6).
4. **Контроль, у которого нет механизма исполниться.** Ручка объявлена, а удостоверения к соседу
   нет ни в одном профиле — контроль отказывает всегда и по одной причине. Проверяется перепи́сью
   в **две** колонки: профилей, называющих зависимость, и профилей, объявляющих к ней доступ.
5. **Настройка, впервые ставшая читаемой, перебивает умолчание поставщика целиком.** Класс уже
   стоил этому дереву выдачи сессии на одной из полос регистрации. Всякий раздел, который наш
   источник объявляет, **замещает** одноимённый раздел умолчания — значит при снятии компонента
   проверять надо не только то, что мы объявили, но и то, что переставало приезжать даром.
6. **Проба, зеленеющая на исчезнувшем предмете.** §1.9 — живой экземпляр; Р9 — его закрытие.
7. **Положительный контроль, выполняющийся на пустом наборе.** Отрицание вида «второго издателя
   нет», «способа входа нет», «подчарта нет» обязано стоять рядом с утверждением, что предмет
   вообще существует.
8. **Запись послабления, пережившая свой предмет.** Записи ведомости §1.6 и записанные исключения
   стендов обязаны истекать **сами**: запись, которой нечего исключать, — находка.

---

## §5 Параллельные полосы — что с чем сверяется

`architecture.md` §«Параллельные полосы одного механизма»: свойство, обязательное для одной
полосы, проверяется **сравнением полос**, а не по каждой отдельно. Вопрос, на который сравнение
отвечает, — «решал ли кто-нибудь, что они различаются».

| пара полос | что обязано совпасть | чем держится |
|---|---|---|
| **носители личности человека на крае** (подписанный предъявитель ↔ сессия) | пол аутентификации вычисляется для **каждого**; форма отказа по полу одна; ответ про одну сессию одинаков | наследуется из IAM-INT-1 (Р3, Р9) и из работы #1122; перепись «носителей N · несут свойство M», N≠M — находка |
| **полосы посадки** (`external` ↔ `own`) | у обеих непустое множество обязательных элементов; обе фиксируют вход, выход, отсечку, ступень; способность, данная одной и не данной другой, названа | перепись способностей (S3) + табличная проба отказа старта |
| **точки применения ступени** (край ↔ внутренний слушатель) | обе зовут единственную функцию решения; ни одна не держит своей таблицы рангов и своего освобождения | свойство уже держится парными пробами и сохраняется дословно |
| **полосы регистрации** в объявлении настройки личности | набор действий после регистрации у полос сходится (сегодня 2 · 2) | гейт уже есть; при переезде **переезжает вместе с предметом**, а не снимается |
| **стражи одного предмета в двух процессах** (служба прав ↔ край) | полосность у обоих; посадка `own` поднимает **оба** | сценарии S1 |

**Требование к переписи во всех пяти строках одинаковое: печатаются ДВЕ величины** — «полос
осмотрено N · несут свойство M». Одно число скрывает ровно тот случай, ради которого сравнение
заведено.

---

## §6 Свойства, которые нельзя потерять при переписывании

Строка без исхода — потерянное свойство, и потеряно оно будет тихо. «Истекает» — такой же исход,
как «сохраняется», но требует обоснования.

| свойство | откуда | исход после F4d |
|---|---|---|
| наш отзыв действует **на предъявлении**, а не только на выдаче | #1122 | **сохраняется**; после переезда перестаёт зависеть от чужого рычага |
| отказ **заканчивает носитель** — иначе отказ стоячий | #1122 | **сохраняется**, и исполняет его наша запись сессии |
| молчащий авторитет: отказ **без** окончания носителя | #1122 | **сохраняется** дословно |
| глагола не предлагает старый сосед: проход, громкий, со счётчиком | #1122 | **сохраняется** (окно раската) |
| сессия **без момента аутентификации** при живой отсечке — отказ | #1122 | **сохраняется**; момент теперь наш и назначается входом |
| граница отсечки **включающая** | #1122 | **сохраняется** |
| обе полосы личности отвечают про одну сессию одинаково | #1122 | **сохраняется**, и перепись печатает обе величины |
| пол аутентификации считается для **каждого** носителя | IAM-INT-1 Р3 | **сохраняется** |
| ни один пол не понижается | IAM-INT-1 Р4 | **сохраняется**; числа перемерены (§1.3) |
| одна форма отказа по полу для обоих носителей | IAM-INT-1 Р9 | **сохраняется** |
| аудитория — не поле запроса, штампуется и эхается | IAM-INT-1 Р2 | **сохраняется** |
| второй фактор заводится **без браузера** | IAM-INT-1 Р5 | **сохраняется** — иначе 32 записи каталога не проверяются автоматически никогда |
| «не настроено» ≠ «недоступно» — два исхода с разными именами | F1 | **сохраняется**, и полосность добавляет третий: «не относится к этой посадке» |
| выдача сессии есть у **каждой** полосы регистрации | гейт объявления настройки личности | **переезжает** вместе с предметом: у нашей регистрации свойство обязано держаться своим гейтом |
| проверка пароля исполняется **проверяющим**, а не сценарием посева | шапка посева церемонии | **сохраняется**: посев играет роль консоли входа, а не роль проверяющего |
| адрес зависимости, от которой зависит решение о доступе, **не выводится** из чужого адреса | `security.md` п. 9 | **сохраняется** для обеих полос |
| административный рычаг снятия сессии у поставщика | сегодня | **истекает** вместе с церемонией — предикат записан в самой ведомости (§1.6) |
| требование трёх адресов поставщика в боевой посадке | сегодня | **истекает** на полосе `own`, **сохраняется** на `external` |
| поток восстановления кодом по почте | поставщик | **истекает** на полосе `own`; замещается сбросом распорядителем (Р4) и вынесен в §12 с условием возврата |

---

## §7 Границы с соседними задачами — что делает ЭТА, что другие

| задача | её предмет | граница |
|---|---|---|
| **#1123** (Ф4в) — край перестаёт принимать издателя-поставщика, полоса интроспекции уходит | второй **издатель** в перечне приёма края; **адрес интроспекции** и его якорь; две записи ведомости про интроспекцию | Снимает их **она**. Здесь снимается **административный** адрес края — он нужен выходу, а выход переезжает сюда. **Порядковая связь названа:** полосную форму пробы §1.9 заводит **эта** задача, потому что без поля полосу выразить нечем; поэтому #1123 **не переводит ни один профиль в состояние «один издатель», пока S1 не сел** — иначе проба краснеет на каждом таком профиле, и починить её будет нечем. Обратной зависимости нет: S1 от #1123 не зависит |
| **#1161** — у трёх стендов цепочка объявлена, а раскатывать её нечем | доставка объявленного до кластера | **Предусловие S2** для этих трёх стендов. Здесь не чинится. Стенд без раскатывающего места либо не переводится в `own`, либо получает **записанное исключение** с причиной и предикатом истечения — по образцу, который в дереве уже есть (§1.8) |
| **#1164** — посев доверия издателям идёт поставщику | задание регистрации доверия и его ручка в профилях | Один из **десяти** шаблонных артефактов §1.7. Снимает его **она**; S3 обязана это учесть и **не** считать его своим — иначе два изменения об одном файле |
| **#1175** — кейс сравнивал полосы издателей, ставшие одной | сквозной кейс набора края | Уже закрыт переездом свойства к профилю. Эта задача **продолжает** переезд: делает пробу полосной и доказывает её инъекцией (Р9) |
| **#1179** — мёртвая полоса обмена в оснастке проб и проза, зовущая её живой | оснастка проб | Отдельно. Здесь не трогается |
| **#1134** — профили не объявляют издателей | объявление перечня приёма в профилях | Отдельно и раньше: пока перечень не объявлен, полосность объявлять не над чем |
| **#1142** — базового токена доступа не существует | форма машинного удостоверения | Отдельный предмет. Здесь **не** решается, и полоса `own` его форму не меняет |

**Что этой задачей НЕ снимается, хотя выглядит смежным:** зеркало человека и его внешний
идентификатор, хуки обогащения утверждений, персональный токен человека, ключ служебной учётки,
модель прав. Ни один из них не меняется — иначе вердикт по подфазе стал бы непрослеживаемым.

---

## §8 Стадии — каждая production-complete в своих границах (ban #14)

| стадия | предмет | посадку меняет | предикат готовности |
|---|---|---|---|
| **S1** | свой вход человека, своя сессия, ступень доверия, поле и полосные стражи | **нет** ни на одном существующем стенде: поле объявлено, значения у всех прежние | стенд, поднятый в посадке `own`, впускает человека и отвергает отозванного; ни при одном значении поля посадка без проверки личности не поднимается |
| **S2** | перепись способностей и перевод стендов | **да**, по стенду | посадка **живого процесса** каждого переведённого стенда объявлена `own` и подтверждена независимо; способность, оставшаяся без пары, названа |
| **S3** | компонент уходит из посадки и из дерева | нет (уже переведены) | подчартов поставщика ноль, записи ведомости истекли сами, значение `external` снято последним |

**Почему S1 не меняет посадку и это не «наполовину».** Возможность вводится **добавлением**;
существующие стенды продолжают работать через `external` без единой правки поведения. Полоса
`own` при этом не «объявлена на будущее»: она поднимается, впускает человека и проверяется
сквозным набором — то есть исполнима по-настоящему, а не по типам.

**Почему S2 отдельно от S3.** Перевод стенда обратим правкой одной строки профиля; снятие
подчарта — нет. Необратимый шаг не делается в том же изменении, что и проверяемый (§3).

---

## §9 Область фазы

### 9.1 В области

1. Поле `authn.identity-provider` — закрытый словарь `external | own`, без умолчания в коде, с
   объявлением в базовом профиле; отказ старта на незаданном и на неизвестном значении.
2. Полосность **всех пяти** стражей провайдерских адресов (§1.1) — трёх в службе прав и двух на
   крае; из края переезжает **только** административный адрес (§7).
3. Требования полосы `own` как отказы старта, включая требование «посадка умеет предъявить
   каждый уровень, которого требует каталог» (Р2, п. 5).
4. Разделение стража своей чеканки на посадочную половину и половину полноты провязки (Р3).
5. Хранилище **способов входа** человека: вид способа, проверочный материал, уровень доверия,
   состояние; инварианты — на уровне базы, не проверкой перед вставкой (ban #10).
6. Способы: пароль и одноразовый код по времени; резервные коды; смена пароля; заведение и
   снятие второго фактора; сброс распорядителем.
7. Хранилище **сессии**: субъект, момент аутентификации, уровень доверия, вид способа, срок;
   носитель — наше печенье с объявленными флагами.
8. Глаголы входа, выхода, повторной аутентификации и сброса — на своих слушателях, с
   объектной проверкой прав и полом аутентификации по каталогу.
9. Чтение отсечки **на предъявлении** нашей сессии и окончание носителя при отказе.
10. Ступень доверия как свойство способа; подача в **единственную** функцию решения.
11. Экран входа продукта и снятие безусловного проксирования путей самообслуживания к консоли
    поставщика.
12. Полосная форма пробы про издателей, её отказ на исчезнувшем предмете, перепись тремя
    величинами и инъекция в обе стороны (Р9).
13. Перепись способностей полосы `external`, не имеющих пары в `own`; перевод стендов; проверка
    посадки **живого процесса**.
14. Снятие пяти подчартов и десяти шаблонных артефактов, кода обвязки и записей ведомости; снятие
    значения `external` последним.
15. Записанные исключения для стендов без раскатывающего места — с причиной и предикатом
    истечения, читающим дерево.

### 9.2 Явно НЕ в области — переносится в соседние задачи и в §12

Второй издатель в перечне приёма края, адрес интроспекции и его якорь (#1123). Доставка
объявленного до трёх стендов (#1161). Задание регистрации доверия издателям (#1164). Оснастка
проб прежней полосы обмена (#1179). Объявление перечня приёма в профилях, которые его не
объявили (#1134). Форма машинного удостоверения (#1142). Ключ устройства, самостоятельное
восстановление по почте, подтверждение владения почтой, федерация, самостоятельная регистрация с
улицы (§12). Модель прав, зеркало человека, персональный токен, ключ служебной учётки — не
трогаются вовсе.

### 9.3 Почему фаза при этом production-complete (ban #14)

- **Полоса `own` полна в своих границах:** человек входит, повторно аутентифицируется, выходит,
  меняет пароль, заводит и снимает второй фактор, восстанавливает доступ сбросом; отозванный
  получает отказ **на предъявлении**; каждый уровень, которого требует каталог, предъявим.
- **Ни одна возможность не «допиливается потом»:** отложенное (§12) вынесено с числом и с
  условием возврата, и **ни одна** отложенная строка не делает существующий глагол
  недостижимым — это проверено по каталогу (уровня «3» не требует никто, §1.3).
- **Посадка `external` не деградирует ни на шаг:** стражи, тексты отказов и поведение сохраняются
  дословно; полосность только добавляет к ним строку о том, чем требование обусловлено.
- **Урезание есть, и оно названо ценой, а не нулём:** устойчивость к фишингу и самостоятельное
  восстановление — реальные способности, и пока их пара не заведена, значение `external` живо, а
  снятие компонента на таком стенде не наступает (Р4, последнее правило).

### 9.4 Обязательства перед документами и оснасткой — тем же изменением

1. **Записи ведомости поверхностей**, чей предикат выполнен, снимаются в том же изменении;
   гейт обязан краснеть, если их оставить.
2. **Перечень рёбер** (`.claude/rules/polyrepo.md`) — ребро «служба прав → поставщик» в части
   человеческого потока снимается; ребро края «→ административный API поставщика» снимается.
3. **Шапка объявления настройки личности** содержит утверждение о том, что её карту не монтирует
   ни один профиль; на этой ревизии это **неверно** (предикат даёт по 12 попаданий в двух
   профилях, оба несут и довод `--config`, и тома, и подстановку). Утверждение пережило свой
   предмет и правится **до** снятия файла — иначе оно уедет вместе с ним неисправленным и его
   унаследует следующий читатель истории.
4. **Страница документации края об аутентификации** и **страницы службы прав** о входе, сессии и
   восстановлении переписываются под полосность: посадка перестаёт быть единственной.
5. **Приёмка IAM-INT-1** надписывается: её §5 вынес «собственную консоль входа» и «ротацию и отзыв
   сессии браузера» из объёма **с условием возврата**; оба условия наступили, и наследование
   называется явно (§6) — иначе два документа об одном предмете разойдутся.
6. **Trail задачи** в vault (`obsidian/kacho/KAC/`, файл по номеру задачи) заводится и обновляется;
   затрагиваются записки ресурсов, рёбер и пакетов.
7. **Спека-книга** дополняется подфазой: дорожная карта об этом переезде молчит, и без записи
   следующий читатель сочтёт работу самоволкой.

---

## §10 Порядок работ: первой красной ложится ОБРАТИМОСТЬ

Порядок выведен из того, что дороже всего чинится задним числом.

1. **F4d-24 — обратимость отказа.** Первая красная проба фазы: отозванный человек, получив отказ,
   обязан быть **запрошен на повторную аутентификацию** нашим действием. Это единственное
   свойство, ради которого две записи ведомости ещё живы (§1.6); всё остальное можно написать и
   потом обнаружить, что выход стал тупиком.
2. **F4d-21…F4d-23** — запись сессии и её состав: без момента аутентификации и уровня отсечка и
   ступень не выразимы.
3. **F4d-13…F4d-20** — способы входа: модель прежде исполнения.
4. **F4d-31…F4d-36** — ступень и сверка полос.
5. **F4d-01…F4d-12** — поле и полосные стражи: они судят уже существующую полосу.
6. **F4d-37…F4d-42** — перепись способностей и перевод стендов.
7. **F4d-43…F4d-50** — снятие компонента.

**Почему не наоборот.** Поле, написанное первым, судит полосу, которой нет, и его требования
пришлось бы писать «на вырост» — то есть по догадке о том, что полоса будет уметь.

---

## §11 Сценарии

Обозначения уровня пробы: **U** — модульная · **I** — интеграционная (Postgres в контейнере) ·
**E** — сквозная через край на посаженном стенде · **G** — гейт по дереву · **B** — проба стража
старта. Каждый идентификатор трассируется в имя теста.

**Каждое отрицание несёт положительный контроль.** Сценарий, все утверждения которого ждут
отказа, зеленеет на проверяющем, отвергающем всё, — и это не гипотеза, а способ, которым такие
пробы ломаются чаще всего.

**Множитель читается по оси, и ось называется.** `B ×2` — по значениям поля (третьего не
существует); `E ×2` — по носителям личности человека.

### S1 — группа A. Поле и полосные стражи

**ID: F4d-01** · B + U
**Given** боевая посадка и профиль, не объявивший `authn.identity-provider`
**When** процесс службы прав стартует
**Then** старт **отвергается**, и текст называет поле и **оба** законных значения
**And** тот же вход с любым из двух законных значений старт проходит — парный положительный контроль
**And** отказ производится **до** любых полосных требований: посадка неизвестна, и требовать по ней нечего

**ID: F4d-02** · B + U
**Given** значение поля вне словаря — соседняя раскладка регистра, лишний символ, пустая строка
**When** процесс стартует
**Then** старт **отвергается**; отката к «безопасному» значению не происходит
**And** ни одно из трёх негодных значений не читается как `own` и ни одно — как `external`
**And** законное значение принимается — положительный контроль

**ID: F4d-03** · G
**Given** перечень законных значений поля
**When** гейт обходит дерево
**Then** перечень **выводится из типа** и объявлен ровно один раз; второе объявление — находка
**And** гейт печатает объём осмотренного и падает на пустом обходе
**And** законный близнец — иное перечисление, к этому полю не относящееся, — гейт молчит

**ID: F4d-04** · B ×2
**Given** посадка `external` и отсутствующий административный адрес поставщика
**When** служба прав стартует
**Then** старт отвергается **тем же текстом, что сегодня**, с добавленной строкой о том, каким
значением поля требование снимается
**And** та же настройка при `own` старт **проходит** — положительный контроль полосности
**And** проба утверждает **текст**, а не только исход: тон отказа — часть контракта оператора

**ID: F4d-05** · B ×2
**Given** посадка `external`, отсутствуют адрес набора проверочных ключей и адрес обмена
**When** служба прав стартует
**Then** отказы **накапливаются** — называются **оба**, а не первый
**And** при `own` те же пустые значения старт не блокируют
**And** при `external` с обоими заданными адресами старт проходит — положительный контроль

**ID: F4d-06** · B ×2
**Given** посадка `external` и незаданные адреса поставщика **на крае**
**When** край стартует
**Then** старт отвергается, и текст называет ручку и предмет
**And** при `own` край стартует без них
**And** при `own` край **не** перестаёт требовать своего: перечень приёма и авторитет отзыва
по-прежнему обязательны — положительный контроль того, что полосность не сняла защиту заодно

**ID: F4d-07** · B
**Given** посадка `own`, своя чеканка **выключена** настройкой
**When** служба прав стартует
**Then** старт отвергается, и текст называет настройку своей чеканки
**And** с включённой и валидной чеканкой старт проходит — положительный контроль

**ID: F4d-08** · B + U
**Given** посадка `own`, своя чеканка включена настройкой, но подписант **не провязан** в сборке
**When** процесс собирается
**Then** отказ наступает **в сборке**, отдельным текстом, и он **не** заменяется посадочной проверкой
**And** проверка настройки и сборка читают **один** аксессор включённости — разойтись во мнении
о включённости контура они не могут
**And** полностью провязанный контур собирается — положительный контроль

**ID: F4d-09** · B
**Given** посадка `own`, каталог прав, требующий уровня «2» хотя бы одной записью; второй фактор
в полосе **не провязан**
**When** служба прав стартует
**Then** старт отвергается, и текст называет **число** записей каталога, которые остались бы
недостижимыми
**And** тот же вход при провязанном втором факторе старт проходит — положительный контроль
**And** величина берётся **из каталога**, а не из константы: подмена каталога на набор без
записей уровня «2» отказ снимает

**ID: F4d-10** · G
**Given** множество значений поля × множество обязательных элементов каждой полосы
**When** гейт обходит объявление требований
**Then** **не существует** значения поля, при котором множество обязательных элементов пусто
**And** каждая клетка произведения покрыта пробой отказа старта; непокрытая клетка — находка
**And** гейт печатает обе величины — «клеток N · покрыто M» — и падает на пустом обходе
**And** законный близнец: требование, объявленное для обеих полос сразу, клеткой не считается и
гейт на нём молчит

**ID: F4d-11** · G
**Given** ручки, от которых зависит разговор с поставщиком
**When** гейт сверяет их с тем, что видит проверка настройки при старте
**Then** ручка, которой проверка не видит, — **находка** (§1.2, восьмая ручка)
**And** гейт печатает две величины: ручек всего · видимых проверке
**And** ручка, к разговору с поставщиком не относящаяся, находкой не считается — положительный контроль

**ID: F4d-12** · B
**Given** посадка `own` и **все** обязательные элементы полосы на месте
**When** служба прав и край стартуют
**Then** оба стартуют, и каждый **объявляет свою посадку в самоотчёте при старте**
**And** посадка, объявленная процессом, читается гейтом посадки — не карта настроек, а живой процесс
**And** тот же стенд при `external` объявляет `external` — положительный контроль различимости

### S1 — группа B. Свои способы входа

**ID: F4d-13** · I
**Given** человек без единого способа входа
**When** распорядитель заводит ему способ «пароль»
**Then** в хранилище появляется **строка способа**, а не колонка в зеркале человека
**And** строка несёт вид способа, проверочный материал, уровень доверия и состояние
**And** исходный пароль в хранилище **не выражается** — у строки нет такого поля; это проверяется
попыткой положить его туда, а не чтением

**ID: F4d-14** · I
**Given** человек с заведённым паролем
**When** заводится второй способ — одноразовый код по времени
**Then** оба способа сосуществуют как **две строки**, и добавление второго не изменило первую
**And** заведение третьего вида способа не требует правки ни одной существующей строки —
положительный контроль модели (Р4)
**And** попытка завести второй способ того же вида отвергается **уровнем базы**, а не проверкой
перед вставкой

**ID: F4d-15** · I
**Given** два одновременных запроса на заведение способа одного вида одному человеку
**When** оба доходят до записи
**Then** проходит **ровно один**; второй получает ожидаемый отказ
**And** отказ производится ограничением базы, а не сравнением прочитанного
**And** последовательные заведения разных видов проходят оба — положительный контроль

**ID: F4d-16** · E
**Given** человек с паролем
**When** он предъявляет верную пару «почта + пароль»
**Then** вход состоялся, сессия выдана, уровень доверия записан «1»
**And** неверный пароль даёт отказ, **неотличимый по тексту и по времени** от отказа
несуществующему человеку
**And** верная пара после неверной по-прежнему проходит — положительный контроль

**ID: F4d-17** · E
**Given** человек с паролем и вторым фактором
**When** он предъявляет верную пару и затем верный одноразовый код
**Then** вход состоялся, уровень доверия записан «2»
**And** вход, остановившийся после пароля, даёт сессию уровня «1», а не «2» — положительный
контроль того, что уровень производится **фактом**, а не намерением
**And** повторное предъявление того же одноразового кода отвергается

**ID: F4d-18** · E + I
**Given** человек с заведённым вторым фактором и резервными кодами
**When** он предъявляет резервный код
**Then** вход состоялся уровнем «2»; **этот** код становится непригодным
**And** остальные коды остаются пригодными — положительный контроль
**And** коды показываются **один раз** при выдаче и в хранилище проверочным материалом, а не
значением; их повторный показ не выразим

**ID: F4d-19** · E
**Given** человек, помнящий текущий пароль
**When** он меняет пароль
**Then** смена требует подтверждения текущим способом и проходит
**And** без подтверждения смена отвергается — и это **не** тот же отказ, что «пароль не подошёл»
**And** после смены прежний пароль не подходит, а новый подходит — положительный контроль обеих сторон

**ID: F4d-20** · E
**Given** человек, утративший все способы входа, и распорядитель его аккаунта
**When** распорядитель сбрасывает способы этого человека
**Then** прежние способы становятся непригодны, человек заводит новый и входит
**And** распорядитель **чужого** аккаунта того же не может — положительный контроль области
**And** глагол сброса несёт пол аутентификации по каталогу и объектную проверку прав; вызов без
пола отвергается той же формой отказа, что и на соседнем носителе

### S1 — группа C. Своя сессия, отзыв, обратимость

**ID: F4d-21** · I
**Given** состоявшийся вход
**When** записывается сессия
**Then** запись несёт субъект, **момент аутентификации**, **уровень доверия**, вид способа и срок
**And** момент и уровень назначает **запись**, а не вызывающий: присланные им значения отвергаются
явно, с именем поля
**And** сессия с незаданными моментом и уровнем **не создаётся** — состояние не выразимо

**ID: F4d-22** · E ×2
**Given** живая сессия и живая отсечка позднее её момента аутентификации
**When** человек предъявляет сессию
**Then** предъявление **отвергается**
**And** сессия с моментом позднее отсечки проходит — положительный контроль
**And** сессия, аутентифицировавшаяся **ровно** в момент отсечки, отвергается: граница включающая

**ID: F4d-23** · E
**Given** живая сессия и авторитет отсечки, **не** ответивший на вопрос
**When** человек предъявляет сессию
**Then** предъявление отвергается, и носитель при этом **не** гасится
**And** тот же вход при ответившем авторитете проходит — положительный контроль
**And** отказ отличим по тексту от отказа по самой отсечке

**ID: F4d-24** · E — **первая красная проба фазы (§10)**
**Given** человек с живой сессией, которому администратор выполнил принудительный выход
**When** человек продолжает работать в консоли
**Then** его предъявление отвергается, носитель **заканчивается**, и продукт **запрашивает
повторную аутентификацию**
**And** после повторного входа он работает снова — положительный контроль обратимости
**And** ни одно из двух действий не производится обращением к внешнему поставщику: отказ и
окончание носителя исполняются **нашим** действием

**ID: F4d-25** · E
**Given** человек, вышедший своим действием
**When** он предъявляет прежний носитель
**Then** предъявление отвергается
**And** прежний носитель не оживает и после того, как окно кэшей истекло
**And** второй человек, не выходивший, продолжает работать — положительный контроль

**ID: F4d-26** · U + G
**Given** обработчик выхода и перечень имён носителей, которые он гасит
**When** гейт читает перечень
**Then** перечень называет **наш** носитель
**And** имя нашего носителя **отличается** от имени носителя поставщика: иначе снятое и наше
неотличимы
**And** перечень, назвавший имя, которого не производит ни один наш путь, — находка
**And** гейт печатает две величины: имён в перечне · имеющих производителя

**ID: F4d-27** · I
**Given** сессия со сроком
**When** срок истёк
**Then** предъявление отвергается, и запись перестаёт быть пригодной
**And** сессия в пределах срока проходит — положительный контроль
**And** второго срока (окна бездействия) в хранилище **нет**: поле не заведено, и гейт находит
второе объявление срока как находку

**ID: F4d-28** · G
**Given** авторитет отсечки
**When** гейт перечисляет его читателей
**Then** читатель есть **на пути предъявления**, а не только на пути выдачи
**And** гейт печатает две величины: точек предъявления · несущих читателя
**And** законный близнец — точка выдачи с читателем — находкой не считается

**ID: F4d-29** · E
**Given** посадка `own` и версия соседа, глагола отсечки **не предлагающая**
**When** человек предъявляет сессию
**Then** предъявление **проходит**, громко, со своим счётчиком
**And** тот же вход при соседе, отвечающем отказом сети, предъявление **отвергает** — положительный
контроль различимости «окно раската» и «неисправность»
**And** оба исхода отличимы в наблюдаемости, а не только в коде

**ID: F4d-30** · E
**Given** человек, чья запись человека заблокирована администратором
**When** он предъявляет живую сессию
**Then** предъявление отвергается
**And** незаблокированный проходит — положительный контроль
**And** блокировка действует **на предъявлении**, а не только на следующей выдаче

### S1 — группа D. Ступень доверия и сверка полос

**ID: F4d-31** · U + G
**Given** виды способов входа
**When** читается уровень доверия
**Then** уровень — **свойство вида способа**, объявленное один раз
**And** второй таблицы рангов в дереве нет; второе объявление — находка гейта
**And** законный близнец: **единственная** платформенная функция решения, которая ранги
сравнивает, находкой не считается

**ID: F4d-32** · E ×2
**Given** глагол, которому каталог требует уровня «2», и человек, вошедший только паролем
**When** он зовёт глагол
**Then** он получает отказ по полу аутентификации — **одинаковой формы на обоих носителях**
**And** тот же человек после второго фактора глагол зовёт успешно — положительный контроль
**And** машинный принципал тот же глагол зовёт без второго фактора: освобождение остаётся ровно одно

**ID: F4d-33** · G
**Given** носители личности человека
**When** гейт сравнивает их между собой
**Then** пол аутентификации вычисляется **для каждого**
**And** перепись печатает две величины: носителей N · несут свойство M; **N ≠ M — находка**
**And** гейт падает на пустом обходе: «ноль находок» обязано быть отличимо от «ноль прочитанного»

**ID: F4d-34** · G
**Given** полосы посадки `external` и `own`
**When** гейт сравнивает их между собой по перечню свойств §5
**Then** свойство, объявленное для одной и не объявленное для другой, — **находка**, если различие
не записано **решением**
**And** перепись печатает две величины: свойств осмотрено · сошлись
**And** записанное решение о различии гейт принимает и печатает отдельной строкой — иначе
законное различие пришлось бы прятать

**ID: F4d-35** · G
**Given** объявление полос регистрации нашего входа
**When** гейт читает **объявление**, а не результат сборки
**Then** каждая полоса заканчивается выдачей сессии
**And** перепись печатает «полос N · выдают сессию M»
**And** гейт падает, если полос разобрано ноль
**And** свойство переезжает вместе с предметом: снятие прежнего объявления не снимает требования

**ID: F4d-36** · E
**Given** посадка `own` и человек, вошедший вторым фактором
**When** он зовёт глагол, требующий свежести аутентификации
**Then** свежесть считается от **нашего** момента аутентификации
**And** тот же вызов при истёкшем окне свежести отвергается — положительный контроль
**And** сессия без момента аутентификации при живой отсечке отвергается всегда

### S2 — группа E. Перепись способностей и перевод стендов

**ID: F4d-37** · G
**Given** способности, которые полоса `external` даёт арендатору (§1.4)
**When** производится перепись их пар на полосе `own`
**Then** каждая способность имеет исход: **есть пара** · **вынесена в §12 с условием возврата** ·
**снята решением**; четвёртого исхода нет
**And** перепись печатает три величины: способностей осмотрено · с парой · вынесено
**And** способность без исхода — **находка**, а не умолчание
**And** пустой перечень способностей роняет прогон: он означает, что перепись не читала объявления

**ID: F4d-38** · G
**Given** стенд, переводимый в посадку `own`
**When** гейт сверяет его цепочку профилей
**Then** стенд, у которого нет раскатывающего места, в `own` **не переводится**
**And** законный обход — **записанное исключение** с причиной и предикатом истечения, читающим
дерево; исключение без предиката — находка
**And** исключение, чей предикат выполнился, — тоже находка: запись, которой нечего исключать,
разрешает то, чего нет
**And** стенд с раскатывающим местом переводится без исключения — положительный контроль

**ID: F4d-39** · E
**Given** переведённый стенд
**When** гейт посадки спрашивает **живой процесс**
**Then** процесс объявляет посадку `own` при старте, и гейт читает это объявление, а не карту настроек
**And** стенд, оставшийся на `external`, объявляет `external` — положительный контроль
**And** «под готов» доказательством посадки **не** является: гейт обязан покраснеть на стенде,
где карта настроек говорит одно, а процесс — другое

**ID: F4d-40** · G
**Given** профили и поле посадки
**When** гейт читает **объявления** профилей, а не результат сборки
**Then** профиль, объявивший `own`, **не вправе** объявлять издателя поставщика; профиль,
объявивший `external`, обязан его объявлять
**And** профиль без поля — находка (Р1)
**And** перепись печатает три величины: профилей всего · внешних · своих
**And** гейт падает, если под суд не попал ни один профиль

**ID: F4d-41** · G — полосная форма пробы про издателей (Р9)
**Given** профиль, объявивший нашего издателя
**When** проба читает перечень приёма тем же разборщиком, что исполняет процесс при старте
**Then** на полосе `external` перечень несёт **хотя бы одну не нашу** запись — формулировка
сохраняется дословно
**And** на полосе `own` перечень несёт **ровно одну**, и она наша
**And** ноль профилей, попавших под суд, **роняет** прогон — как это делают её соседи по файлу
**And** инъекция доказана в обе стороны на синтетических профилях: наш вместо чужого на `external`
— красное с именем профиля; второй издатель при `own` — красное; законные формы обеих полос — молчит

**ID: F4d-42** · E
**Given** переведённый стенд в посадке `own`
**When** прогоняется сквозной набор
**Then** человек входит, зовёт глагол уровня «1» и глагол уровня «2», выходит и получает отказ
**And** машинный принципал на том же стенде работает без изменений — положительный контроль того,
что перевод не тронул машинные контуры
**And** ни один кейс набора не пропущен, не ослаблен и не замаскирован; вердикт читается числами:
исполнено из скольких, утверждений, упавших, безответных

### S3 — группа F. Компонент уходит

**ID: F4d-43** · G
**Given** записи ведомости поверхностей, чей предикат снятия — «вход человека перестал заводить
сессию у поставщика»
**When** предикат выполнен
**Then** записи сняты, и гейт **краснеет**, если их оставить
**And** записи, чей предикат ещё не выполнен, остаются, и гейт на них молчит — положительный контроль
**And** перепись гейта печатает объём осмотренного и на пустой ведомости **проходит**: пустая
ведомость — цель фазы, а не поломка

**ID: F4d-44** · G
**Given** подчарты компонента
**When** гейт обходит объявление зонтичного чарта и каталог подчартов
**Then** подчартов поставщика личности — **ноль**
**And** гейт называет **обе** формы дома подчарта: объявленную зависимостью и лежащую каталогом
**And** живой подчарт другого назначения находкой не считается — положительный контроль
**And** гейт падает, если объявление не разобралось: «ноль подчартов» и «ноль прочитанного» —
разные вердикты

**ID: F4d-45** · E
**Given** консоль продукта и снятый подчарт консоли самообслуживания
**When** веб-сервер консоли стартует
**Then** он стартует: безусловного проксирования путей самообслуживания к снятой службе больше нет
**And** пути входа, выхода, настроек и смены пароля обслуживает **наш** экран
**And** до снятия проксирования веб-сервер отказывал при старте — это и есть связанность правки,
а не отдельность (положительный контроль на предыдущей ревизии)

**ID: F4d-46** · G
**Given** шаблонные артефакты поставщика (§1.7)
**When** гейт обходит шаблоны зонта и чарта службы прав
**Then** каждый артефакт имеет исход: **снят здесь** · **снимает соседняя задача** (с её номером)
· **остаётся с причиной**
**And** артефакт без исхода — находка
**And** перепись печатает две величины: артефактов осмотрено · с исходом

**ID: F4d-47** · G
**Given** объявление профилей
**When** ни один из них не объявляет `external`
**Then** значение `external` снимается из словаря, и это **последний** шаг
**And** пока хотя бы один профиль его объявляет, снятие **отвергается** гейтом — положительный
контроль порядка (Р7)
**And** после снятия поле остаётся закрытым словарём: незаданное значение по-прежнему роняет старт

**ID: F4d-48** · G
**Given** дерево после снятия
**When** прогоняется гейт поверхностей и перепись упоминаний
**Then** мест разговора с поставщиком — ноль; ведомость пуста; гейт **проходит** и говорит, что
это исход, к которому вела фаза
**And** перепись упоминаний печатается **отдельно** от мест разговора: упоминание в прозе местом
разговора не является, и смешение этих единиц уже однажды давало неверное число
**And** гейт падает на непрочитанном дереве

**ID: F4d-49** · B
**Given** отдельный клон продукта без монорепо, в боевой посадке, с посадкой `own`
**When** он поднимается
**Then** он **поднимается** — установка чарта проходит и нагрузки готовы, а не только шаблон рендерится
**And** тот же клон с посадкой `own` и незаполненным обязательным элементом полосы **не**
поднимается — положительный контроль того, что готовность не куплена снятием требований
**And** это и есть предикат вехи M7, и он проверяется установкой, а не рендером

**ID: F4d-50** · E
**Given** стенд после снятия компонента
**When** прогоняются наборы, которым нужен человек
**Then** волна, создающая условие «предъявитель принадлежит человеку», работает на **нашей** полосе
**And** ни один кейс не переведён в пропуск и ни один не ослаблен: условие создаётся, а не
объявляется недостижимым
**And** машинные наборы проходят без изменений — положительный контроль

---

## §12 Out-of-scope — что отложено, с числом и условием возврата

| Вынесено | Число | Почему вне объёма | Условие возврата |
|---|---:|---|---|
| Ключ устройства (passkey / WebAuthn) как способ входа | **0** записей каталога требуют уровня «3» (§1.3) — ни один глагол не становится недостижим | это отдельный протокол с собственной приёмкой и собственным набором соответствия; модель способов (Р4) его уже допускает, поэтому возврат **не потребует миграции данных** | продуктовое требование устойчивости к фишингу **либо** появление записи каталога с уровнем «3» |
| Самостоятельное восстановление доступа по почте | **1** способность полосы `external` без пары | требует своего почтового тракта: отправитель, шаблоны, ограничение темпа, одноразовый код с коротким сроком. Домен, а не поле в чужом потоке | почтовый тракт заведён продуктом |
| Подтверждение владения почтой | **1** способность без пары | тот же тракт | вместе со строкой выше |
| Вход через внешнего поставщика (федерация человека) | **0** — в объявлении настройки личности она **выключена** и сегодня | переносить то, чего нет, значит вводить непроверяемую возможность | требование арендатора |
| Самостоятельная регистрация человека с улицы | **0** — сегодня человек заводится приглашением либо посевом | вход и регистрация — разные церемонии | продуктовое требование |
| Окно бездействия сессии | **0** — сегодня объявлен один срок | два срока об одном предмете дают два места, из которых верно одно | отдельное решение владельца |
| Второй издатель на крае, полоса интроспекции | предмет **#1123** | другая задача, идёт параллельно | — (границы §7) |
| Доставка объявленного до трёх стендов | предмет **#1161** | другая задача | предусловие S2 для этих трёх |
| Форма машинного удостоверения | предмет **#1142** | другой предмет: машина, а не человек | — |

Каждая строка — **осознанное сужение объёма**, а не понижение качества: подфаза
production-complete в своих границах (§9.3), а отложенное названо, посчитано и снабжено условием
возврата. Три первые строки — **цена**, а не ноль: способность, которую арендатор сегодня имеет,
на полосе `own` до возврата отсутствует, и именно поэтому Р4 запрещает считать `external`
снятым, пока перепись §11 F4d-37 непуста.

---

## §13 Definition of Done

1. **Приёмка APPROVED** от `acceptance-reviewer` **до первой строки кода** (ban #1).
2. Все сценарии §11 реализованы пробами названного уровня; для каждого показана пара
   **RED → GREEN** фактическим выводом (ban #12).
3. **Порядок §10 соблюдён**: первой красной пробой была F4d-24 (обратимость отказа).
4. **Ни при одном значении поля** посадка без проверки личности не поднимается — доказано
   табличной пробой по произведению «значения × обязательные элементы» (F4d-10), и произведение
   покрыто **целиком**.
5. Все **пять** стражей §1.1 полосные; тексты отказов провайдерской полосы сохранены **дословно**
   и утверждаются пробой как текст, а не как исход.
6. Страж своей чеканки разделён по Р3; **обе** точки отказывают, и проба называет обе.
7. Модель способов входа заведена **строками**, а не колонками; заведение третьего вида способа
   не требует правки существующих строк — доказано, а не заявлено.
8. Отсечка читается **на предъявлении** нашей сессии; обратимость исполняется **нашим** действием;
   ни одно из двух не производится обращением к внешнему поставщику.
9. **Свойства §6 перечислены построчно**, и у каждой строки проставлен исход; «истекает»
   проставлено **решением**, а не умолчанием.
10. **Перепись способностей F4d-37 непуста и у каждой способности назван исход**; способность без
    исхода — блокирующая находка, а не стилистическая.
11. Проба про издателей **полосная**, роняет прогон на исчезнувшем предмете, печатает три
    величины и **доказана инъекцией в обе стороны** (Р9).
12. **Каждое отрицание §11 несёт положительный контроль.** Проверяется переписью по документу, а
    не впечатлением: сценарий, все утверждения которого ждут отказа, — находка.
13. Каждый гейт печатает **объём осмотренного** и падает на пустом обходе; «ноль находок»
    отличимо от «ноль прочитанного».
14. Записи ведомости поверхностей, чей предикат выполнен, **сняты**; гейт краснеет, если их
    оставить. Записанные исключения стендов несут предикат истечения, читающий дерево.
15. **Обязательства §9.4 исполнены все семь**, включая правку утверждения в шапке объявления
    настройки личности **до** снятия файла и надпись на приёмке IAM-INT-1.
16. Отдельный клон в боевой посадке `own` **поднимается**: установка чарта плюс готовность
    нагрузок, а не только рендер шаблона (F4d-49).
17. Trail задачи в vault заведён и обновлён; затронуты записки ресурсов, рёбер и пакетов.
18. Прогон **перед отправкой**, локально: типы, линт, сборка, короткие пробы, гейты дерева,
    сквозные наборы затронутых контуров. Конвейер — подтверждение, а не первый читатель.
19. **Ни одна проба не пропущена, не ослаблена и не замаскирована** (`testing.md` §E2E). Красное
    закрывается фиксом, а не сужением вопроса. Проба, чей предмет исчез, **заменяется**.
20. Публичный текст изменения прогнан через признак восстановимости: читатель без доступа к коду
    не должен восстановить по нему уязвимое состояние. Три места из-под запрета выведены: текст
    отказа оператору при отказе в старте, комментарий у самого гейта, имя и текст падения пробы.

---

## §14 Критерии для `acceptance-reviewer`

Помимо обычных (покрытие, полнота, трассируемость, область) прошу проверить именно это:

1. **§2 отвечает на все девять вопросов решениями**, а не перечислением вариантов. Решение,
   получившее «зависит от», считается непринятым.
2. **§11 не содержит сценария, состояние которого схема не допускает.** Неконструируемое Given —
   не Given; такие возвращаются автору, а не обходятся в реализации. Особое внимание — F4d-09
   (каталог, требующий уровня, которого полоса не даёт) и F4d-29 (версия соседа без глагола):
   оба требуют, чтобы состояние было **создаваемо**, а не описываемо.
3. **Каждое отрицание имеет положительный контроль.** Сценарий, где все утверждения ждут отказа,
   зеленеет на проверяющем, отвергающем всё.
4. **§6 полон.** Свойство, не названное там, будет потеряно при переписывании — и потеряно тихо.
   Особенно прошу сверить его с приёмкой IAM-INT-1 и с работой #1122: обе вводили свойства,
   которые эта подфаза переписывает.
5. **§7 разводит эту задачу и #1123 без остатка.** Место, о котором обе задачи могли бы решить,
   что оно чужое, — блокирующее замечание. Отдельно прошу проверить порядковую связь: полосную
   форму пробы заводит эта задача, и #1123 на неё опирается.
6. **§12 обосновывает полноту фазы по каждому пункту**, а не одним общим доводом; и три первые
   строки названы **ценой**, а не нулём.
7. **Р8 — уточнение текста задачи, и оно требует подтверждения.** Задача ставит поле «последним»;
   приёмка ставит его **вместе с полосой**. Прошу либо подтвердить, либо назвать другой порядок:
   единственный неприемлемый исход — поле, объявленное раньше исполнимой полосы, потому что это
   ровно тот класс, из-за которого #1107 и просила поставить его последним.
8. **Р2 п. 5 — самое сильное требование документа**, и оно новое: посадка обязана уметь предъявить
   каждый уровень, которого требует каталог. Прошу проверить, что его величина берётся **из
   каталога**, а не из константы, — иначе оно превратится в ещё одно число, которое устареет молча.
9. **§1.1 расширяет объём задачи с трёх стражей до пяти.** Прошу подтвердить расширение либо
   назвать, кто снимает два оставшихся: стенд, у которого один процесс перевели, а другой нет, не
   поднимается по причине, к его работе отношения не имеющей.
10. **Документ выдерживает публикацию**: он читается как перечень требований, а не как разбор
    состояния. Место, по которому посторонний восстановит уязвимое состояние, — блокирующее
    замечание, а не стилистическое.

---

## §15 Открытые вопросы владельцу — догадкой не заполняются

По каждому — два взаимоисключающих исхода; выбор за владельцем. Ни один **не блокирует** S1.

**Вопрос 1. Остаётся ли посадка `external` осью продукта или это переходное состояние?**
Тело задачи требует «ноль упоминаний поставщика» — то есть полного снятия. Рабочее решение эпика
воркспейса #382 (вопрос владельца № 4 — «поставщик: обязательная часть поставки или опция») до
сих пор **не принято**, и поле `authn.identity-provider` — ровно его механизм. Исходы:
**(а)** `external` — переходное значение с предикатом истечения, и после перевода всех стендов
оно снимается (так написана S3); **(б)** `external` остаётся поддерживаемой посадкой, и тогда
снятие подчартов из дерева **не наступает**, а задача закрывается на S2 с записью о том, что
предикат #1125 изменён владельцем. Приёмка **написана по (а)** и на (б) переписывается одной
правкой §9.1 п. 14.

**Вопрос 2. Пароль как единственный первый фактор — или сразу обязательный второй?**
Исходы: **(а)** второй фактор добровольный, уровень «1» законен для 285 записей каталога — это
паритет с сегодняшним поведением; **(б)** второй фактор обязателен каждому человеку, и тогда
уровень «1» перестаёт существовать как состояние живого человека. (б) строже и дороже во входе;
приёмка написана по **(а)**, потому что (б) — понижение доступности, а не повышение
защищённости, и его цена измеряется отдельно.

**Вопрос 3. Срок сессии — 24 часа паритетом или своя величина?**
Приёмка фиксирует паритет (Р5), потому что менять срок в том же изменении, где меняется
владелец сессии, значит смешать два предмета. Если владелец назовёт другую величину, она
записывается решением с обоснованием, а не выводится из удобства.

**Вопрос 4. Что делает продукт с людьми, у которых на момент перевода стенда нет ни одного
нашего способа входа?** Исходы: **(а)** распорядитель заводит им способ (глагол сброса, F4d-20)
— переход ручной и наблюдаемый; **(б)** заводится одноразовая ссылка приглашения — это уже
почтовый тракт (§12) и потому недоступно. Приёмка написана по **(а)** и требует, чтобы число
таких людей на каждом переводимом стенде было **названо перед переводом**, а не обнаружено после.

**Вопрос 5. Устойчивость к фишингу.** Снятие ключа устройства — единственная строка §12, которая
понижает уже обещанную защищённость. Владелец вправе потребовать, чтобы S1 её несла; тогда объём
растёт на протокол и набор соответствия, и это **осознанный** размен, а не пропуск.
