# Sub-phase ID-PW-1 (проверяющий пароля) — переехала в дом службы

> **Статус:** SUPERSEDED — документ переехал в дом службы, и вердикт по нему выносится там: `PRO-Robotech/kaname:docs/engineering/acceptance/password-verifier-follows-the-stored-value.md` (`PRO-Robotech/kaname#94`). **Одобрения здесь не было:** прежняя редакция — DRAFT, кругов ревью ноль, запрет #1 по ней не снимался ни разу.
> **Дата переезда:** 2026-09-15 · **Задача:** `PRO-Robotech/kacho#1268` (Ф2), эпик `#1266`.
> **Прежняя редакция цела в истории, а не здесь:** `git show 132f8d48:docs/specs/sub-phase-ID-PW-1-password-hash-verifier-acceptance.md` (567 строк). Этот файл — надгробие: он нужен затем, чтобы документы, называющие прежний адрес, не стали висячими, и истекает вместе с ними (ниже).

## Почему переехала

- **Дом приёмки — решение** (`.claude/rules/polyrepo.md` §«У приёмки домов ДВА»): предмет
  целиком лежит внутри одной службы, а служба вынесена в свой репозиторий.
- **Координаты прежней редакции указывали в дерево, откуда служба вынесена:** каталог чарта
  переименован вместе со службой, каталога службы в платформе нет вовсе.

## Что изменилось — не здесь

Надгробие **не пересказывает** содержание документа: у черновика одно место описания, и оно в
новом доме. Второе место отстало бы на первом же круге ревью. Что сохранено ради ссылок и что
изменено против прежней редакции — §12 нового документа; ход ревью — записи по отпечатку в
каталоге `docs/specs/reviews/password-verifier-follows-the-stored-value/` дома службы.

## Кто называет прежний адрес — перепись

Дом — `PRO-Robotech/kacho-workspace`; единица — документ, в котором стоит прежний путь;
предикат:

```sh
git grep -l 'sub-phase-ID-PW-1-password-hash-verifier-acceptance' -- docs/ obsidian/ .claude/
```

| документ | строк с прежним путём | что сделано |
|---|---:|---|
| `docs/specs/sub-phase-F4d-identity-provider-posture-acceptance.md` (APPROVED) | 2 | не тронут: его ссылки резолвятся сюда, а отсюда — в новый дом |
| `docs/specs/sub-phase-ID-MAIL-1-mail-delivery-acceptance.md` (APPROVED) | 2 | не тронут: то же |

Одобренные документы не правятся ради адреса: правка после вердикта переводит их на
редакцию, которую никто не рассматривал, а ссылки и без неё целы — ровно для этого надгробие и
стоит. В продуктовых деревьях (`kacho`, `kaname`, `corelib`) прежний адрес не называет никто:
тот же предикат по их стволам на 2026-09-15 даёт ноль.

## Когда надгробие снимается

Когда предикат выше называет **только этот файл** — то есть ни F4d, ни ID-MAIL-1 прежний путь
больше не называют (каждый — своим изменением, с правкой после вердикта, объявленной в его
шапке). До тех пор надгробие — адрес, а не мусор: снятое раньше, оно сделало бы четыре ссылки
одобренных документов висячими.

Новый дом называет этот путь как прежний адрес с ревизией `132f8d48` (история, от надгробия не
зависит) и фразой о надгробии в шапке. Снимая надгробие, найди ту фразу
(`grep -n 'надгробие' <новый документ>`) и поправь её тем же изменением: иначе она переживёт
свой предмет.
