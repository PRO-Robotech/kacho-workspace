# Sub-phase ID-PW-1 (проверяющий пароля) — переехала в дом службы

> **Статус:** SUPERSEDED — документ переехал в дом службы, и первый вердикт по нему будет вынесен там: `PRO-Robotech/kaname:docs/engineering/acceptance/password-verifier-follows-the-stored-value.md` (`PRO-Robotech/kaname#94`). **Одобрения здесь не было:** прежняя редакция — DRAFT, кругов ревью ноль, запрет #1 по ней не снимался ни разу.
> **Дата переезда:** 2026-09-15 · **Задача:** `PRO-Robotech/kacho#1268` (Ф2), эпик `#1266`.
> **Прежняя редакция цела в истории, а не здесь:** `git show 132f8d48:docs/specs/sub-phase-ID-PW-1-password-hash-verifier-acceptance.md` (567 строк). Этот файл — надгробие: он нужен затем, чтобы документы, называющие прежний адрес, не стали висячими, и истекает вместе с ними (ниже).

## Почему переехала

- **Дом приёмки — решение** (`.claude/rules/polyrepo.md` §«У приёмки домов ДВА»): предмет —
  проверяющий, перечень форматов, перепись хранилища — целиком лежит внутри одной службы, а
  служба вынесена в свой репозиторий.
- **Координаты прежней редакции указывали в дерево, откуда служба вынесена:** каталог чарта
  переименован вместе со службой, каталога службы в платформе нет вовсе.
- **Одобренная Ф1 её не поглощает.** Из 47 требований прежней редакции Ф1 покрывает целиком 4,
  наполовину 2, не покрывает 41. В новом доме решённое Ф1 названо её номером, а непокрытое
  осталось предметом документа.

## Что сохранено ради ссылок — и что изменилось

**Сохранено намеренно, чтобы ссылки по имени продолжали резолвиться в новый дом:**
идентификатор `ID-PW-1`, номера сценариев `PWV-01…13`, имена полос, нумерация §1.1, §1.3,
§4.2; заголовок и предикат §1.1 — дословно те, что воспроизводит F4d §1.4а.

**Добавлено:** §0.1–§0.3 (что решила Ф1, что разрешает одобрение, почему дом), §1.6 (стоимость
проверки двух форматов), §4.3 (ведомость производителей), §4.4 (перепись источников нормы),
§10.1 (замер, потребованный F4d, — «не выполнилось»), §12 (что изменено при переезде);
решения Р6 (вторая половина предиката снятия) и Р7 (ёмкость проверки); сценарии PWV-14 и
PWV-15.

**Одна ссылка резолвится иначе, чем читается.** F4d в замечаниях рецензента круга 3 называет
«§15 ID-PW-1» местом правки второй половины предиката снятия. Такого раздела у ID-PW-1 не было
и нет; предмет решён в новом доме — §3 Р6 и PWV-13, замер — §10.1.

## Кто называет прежний адрес — перепись

Дом — `PRO-Robotech/kacho-workspace`; единица — документ, в котором стоит прежний путь;
предикат:

```sh
git grep -l 'sub-phase-ID-PW-1-password-hash-verifier-acceptance' -- docs/ obsidian/ .claude/
```

| документ | строк с прежним путём до этого изменения | что сделано |
|---|---:|---|
| `docs/specs/sub-phase-F4d-identity-provider-posture-acceptance.md` (APPROVED) | 2 (ссылок по имени ещё 25) | путь заменён новым тем же изменением, что завело надгробие; правка объявлена в шапке F4d как правка после вердикта без изменения содержания |
| `docs/specs/sub-phase-ID-MAIL-1-mail-delivery-acceptance.md` (APPROVED) | 2 | не тронут: его ссылки резолвятся сюда, а отсюда — в новый дом |

В продуктовых деревьях (`kacho`, `kaname`, `corelib`) прежний адрес не называл никто — тот же
предикат по их стволам на 2026-09-15 давал ноль.

## Когда надгробие снимается

Когда предикат выше называет **только этот файл** — то есть ID-MAIL-1 перестанет называть
прежний путь (правкой координаты, объявленной в её шапке). До тех пор надгробие — адрес, а не
мусор; снятое раньше, оно сделало бы две ссылки одобренного документа висячими.

Новый дом называет этот путь дважды — как прежний адрес с ревизией `132f8d48` (история, от
надгробия не зависит) и фразой шапки «там оставлено надгробие». Снимая надгробие, поправь ту
фразу тем же изменением: иначе она переживёт свой предмет.
