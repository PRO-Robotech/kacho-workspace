# Sub-phase KAN-HOME-1 — удаление `services/iam` из монорепо: приёмка

> **Статус: APPROVED** — `acceptance-reviewer`, круг 1, 2026-09-08 (`00-kacho-core.md`
> ban #1, единственный approve-gate). Вердикт относится к ОТПЕЧАТКУ предмета
> `sha256:eb834301b529aff3e4e6628f4cc26524a49b7e0ad7cc678996506a3021d18e44`
> (blob `b4d7c09f`, коммит `6ef1090e`, 910 непустых строк), а не к имени файла:
> любая правка делает эту запись вердиктом о ПРЕЖНЕЙ редакции и требует новой,
> соседней (`.claude/rulebook/change-graph.md` §2). Блокирующих — **ноль**; два не
> блокирующих — запись круга 1, §9.1. **Разрешена фаза A целиком.** Фаза B (первое
> удаление хотя бы одного файла `services/iam/`) остаётся закрытой до закрытия
> пяти заказанных задач `kacho#2326`…`#2330`: до них шесть сценариев в покрытие не
> засчитываются, и §7 п. 1 это объявляет сам.

**Прежнее объявление шапки сохранено и не переписано:** `DRAFT — на review не
подавался`. Статическая форма после review не меняется: действующее одобрение
выводится из внешнего события и добавляемой записи, а не из слова в строке
(`.claude/rulebook/change-graph.md` §2).
**Ревьюер:** `acceptance-reviewer` — единственный gate APPROVED (не заказчик).
**Задача-владелец:** `PRO-Robotech/kacho#1111` (веха M11 — «разрез репозитория и
переименование, одним переключением»). **Эпик линии:** `kacho#2119`.
**Изменение, чьи решения здесь исполняются:** `docs/changes/standalone-iam/`
(`design.md` §0 — три модуля; §7.1–§7.4 — Р10…Р14). Решения не пересматриваются
и не пересказываются.
**Дерево продукта, на котором сняты ВСЕ числа:** `PRO-Robotech/kacho` @
`21f89f6302a2ac89a842943afaeda520d99a31e5` (`origin/main`).
**Дерево воркспейса:** `PRO-Robotech/kacho-workspace` @ `f534c9ae`, ветка
`docs/kan-extract-1` от `main`.

---

## О чём этот документ и о чём он НЕ

**Предмет — ПОЛОВИНА разреза, обращённая к монорепо:** что перестаёт работать,
что начинает молчать и в каком порядке это чинится, когда 2394 файла
`services/iam/` перестают существовать в дереве платформы.

**Вторую половину — что происходит у самой службы — этот документ не покрывает.**
Её несут предикат `kacho#1111` («отдельный клон поднимается в боевой посадке») и
приёмка перехода `docs/changes/standalone-iam/acceptance.md`, семейства `BOOT`,
`DEP`, `MOD`, `REST`. Здесь они не воспроизводятся: два места об одном предмете
расходятся молча, и расходится обычно то, которое читают чаще.

**Документ НЕ утверждает:**

- **что удаление исполнено** — на названной ревизии каталог существует целиком
  (`git ls-tree -r origin/main --name-only -- services/iam | wc -l` → **2394**);
- **что производитель каждого сценария уже РАБОТАЕТ.** Перепись производителей
  идёт **§2, до единого сценария**. После сведения безымянного «производителя нет»
  не осталось: у **21** сценария производитель в дереве, у **6** он **заказан
  задачей** — номер и предикат снятия названы у каждого. Заказанный в покрытие
  **не засчитывается** до своего закрытия;
- **что судьба контракта решена.** Она не решена, и от неё зависит, что именно
  наблюдается по трём сценариям — §3;
- **что числа шести замеров, из которых документ собран, верны.** Пять из них
  расходились с моим собственным замером, и каждое расхождение разобрано
  уточнением единицы, а не выбором правдоподобного — §1.

**Документ НЕ является планом работ.** Порядок вех живёт в `design.md`; здесь —
порядок **фаз одного изменения** (§4) и наблюдаемое поведение каждой (§5 и далее).

---

## 1. Свод шести замеров — расхождения разбираются уточнением единицы

Разведка велась шестью независимыми осями: **импорты · конвейер · развёртывание ·
сквозные пробы · гейты дерева · тексты**. Ниже — их числа против моего замера.
Единица счёта названа у каждой строки: две переписи одного предмета расходятся
ровно тогда, когда единицы разные, и различить это можно только назвав обе.
Ревизия одна на всю таблицу и стоит в шапке. `И<n>` — идентификатор замера этого
документа.

| И | величина | единица | ось | мой замер | предикат | сошлось? |
|---|---|---|---|---:|---|---|
| **И1** | файлов дерева продукта | файл | 9521 | **9521** | `git ls-tree -r origin/main --name-only \| wc -l` | да |
| **И2** | из них под `services/iam` | файл | 2394 | **2394** | тот же с `-- services/iam` | да |
| **И3** | объявлений модуля Go | файл `go.mod` | 2 | **2** (корень + `services/iam`) | `git ls-tree -r origin/main --name-only \| grep 'go\.mod$'` | да |
| **И4** | импортёров стабов службы **вне** её каталога | файл `.go` | 99 (43 прод / 56 проб) | **99** — см. разбор ниже | `git grep -l 'PRO-Robotech/kacho/pkg/api/kaname/cloud/iam/v1' origin/main -- '*.go'` + отсев формы | **да, после уточнения** |
| **И5** | импортов пакетов службы (`github.com/PRO-Robotech/kaname/…`) вне её каталога | import-объявление | 0 | **0** | тот же грep + адъюдикация формы | **да, после уточнения** |
| **И6** | осколков имени продукта вне каталога службы | путь | 151 | **151** (`pkg/api` 83 · `proto` 40 · чарт умбреллы 16 · `repohygiene` 10 · `deploy` 2) | `git ls-tree -r origin/main --name-only \| grep kaname \| grep -v '^services/iam/'` | да |
| **И7** | чартов с именем `kaname` | файл `Chart.yaml` | 2 | **2** — `distribution: platform` (ставится) и `standalone` (не ставится) | `git ls-tree -r origin/main --name-only \| grep Chart.yaml` + чтение `name:` | да |
| **И8** | `file://`-зависимостей умбреллы | строка `repository:` | — | **7**, и `services/iam/deploy` среди них **нет** | `git show origin/main:deploy/helm/umbrella/Chart.yaml \| grep -c 'file://'` | — |
| **И9** | `Dockerfile` служб | файл | — | **8**; у службы доступа единственный контекст сборки, равный своему каталогу | `git ls-tree -r origin/main --name-only \| grep -E '^(services/[^/]+\|gateway)/Dockerfile$'` | — |
| **И10** | манифестов модулей | файл `manifest.yaml` | 6 → 5 | **6** | `git ls-tree -r origin/main --name-only \| grep -E '^services/[^/]+/manifest\.ya?ml$'` | да |
| **И11** | целей корневого Makefile, зовущих модуль службы | цель | 7 | **7** (12 строк) | `git show origin/main:Makefile \| awk '/^[a-zA-Z0-9_.-]+:/{t=$1} /IAM_MODULE_DIR/{if(t)print t}' \| sort -u` | да |
| **И12** | наборов сквозных проб / коллекций | набор · коллекция | 8 · 98 | **8** · 98 | `git ls-tree -r origin/main --name-only \| grep -oE '^[a-z/]*tests/newman' \| sort -u` | да |
| **И13** | вердиктный гейт всех наборов | файл-экземпляр | 1 | **1**, в службе; файлов, называющих его, — **47** | `git ls-tree -r origin/main --name-only \| grep assert-suites-green` | да |
| **И14** | перепись исполненности и покрытия | файл-экземпляр | 1 · 1 | **1** · **1**, оба в службе | `git ls-tree … \| grep -E 'newman/scripts/(exec-coverage\|coverage)\.py$'` | да |
| **И15** | проб оснастки сквозных наборов | файл `*_test.py` | 24 из 24 в службе | **24 из 24** | `git ls-tree … \| grep -E '^[a-z/]*tests/newman/scripts/.*_test\.py$'` | да |
| **И16** | ядро шардов | имя образа | iam в `core_images` всех 5 | **iam** в `core_images`, в `gates` — **нет** | `git show origin/main:deploy/e2e-shards.json \| grep -n 'core_images\|"gates"'` | да |
| **И17** | гейтов корпуса, обходящих дерево и называющих службу | файл `_test.go` | 83 / из них исполняемо 65 | **83** / исполняемо **64** | обход `internal/repohygiene`, отбор по `repoRoot(`, снятие хвостового комментария | **да, в пределах единицы** |
| **И18** | записей ведомости остатка имени | строка `Path:` | 28 / 28 в службе | **28 / 28** | `git show origin/main:internal/repohygiene/kanamenameresidue.go \| grep -cE '^\s*Path: *"'` | да |
| **И19** | записей ведомости границ фундамента с источником в службе | строка | 4 | **4**; плюс **1** приставка в карте путей | `git show origin/main:internal/repohygiene/foundationboundary.go \| grep -n 'services/iam'` | да |
| **И20** | гейтовых утверждений, уезжающих со службой | `func Test` | — | **4908** в **1400** файлах (в корпусе `repohygiene` — 2636) | обход `services/iam`, счёт `^func Test` | — |
| **И21** | координат службы в ЖИВОМ корпусе правил воркспейса | уникальная координата | 115 «живого» шире | **13**, из них **6 мертвы уже сегодня** | `git grep -o 'services/iam/[^ ]*' origin/main -- '.claude/rulebook/*' '.claude/agents/*' '.claude/skills/*'` + резолв по дереву продукта | **разные единицы, обе верны** |
| **И22** | приёмок дерева продукта | файл под `acceptance/` | 29, все в службе | **29**, все в `services/iam/docs/engineering/acceptance` | `git ls-tree -r origin/main --name-only \| grep -E '(^\|/)acceptance/.*\.md$'` | да |
| **И23** | открытых задач линии | issue | 63 (замер 2026-09-07 замысла) | **30** | `gh issue list -R PRO-Robotech/kacho --label release:standalone-iam --state open --limit 300 --json number -q length` | **разные даты, обе верны** |

### Пять расхождений — и каждое разобрано, а не заглушено

**И4 (99 против моих 141, затем 102).** Три величины, три единицы, все три верны.
**141** — файлы, знающие путь стабов **где угодно**, включая комментарий; из них
**39** — сами порождённые стабы, называющие собственный путь. **102** — мой грубый
прокси «строка выглядит как импорт»; три лишних оказались **raw-строками фикстур
инъекций** (`src := \`package …\``) в `internal/repohygiene`, и лексер оси их не
считает. **99** — импорт как узел. Решающее свидетельство, что ни одна сторона не
слепа: **прод-число 43 сошлось у двух РАЗНЫХ выражений**, а совпадение чисел,
полученных одним способом, свидетельством не является.

**И5 (0 против моих 10).** Мой предикат был слеп: все десять — **строковые
литералы и raw-строки внутри гейтов**, а не импорты. Ось права. Но адъюдикация
дала находку, которой у оси не было: **три места держат путь модуля службы
константой** — `internal/repohygiene/subscriptionkindvocabulary.go`
(`iamModulePathPrefix`), `…/withdrawalproducerarriveswiththeapplier.go`
(`applierImportPath`) и `…/iamcontourdecisionbasis_test.go` (`authzguardPkgPath`).
Первые два — **не-тестовые**. Они судят по пути модуля, а не по каталогу, и
потому переживут удаление молча.

**И17 (65 против моих 64).** Единица воспроизведена **точно** на первом шаге —
83 файла с обходом настоящего дерева, — и разошлась на один при отсеве
хвостового комментария. Поимённой адъюдикации 83 файлов не проводилось ни одной
стороной; расхождение в единицу здесь есть предел обоих предикатов, а не спор.

**И21 (115 против моих 13).** Единицы разные и обе названы: у оси «живое»
включает хранилище записок, README продукта, `docs/architecture/` и страницы
сайтов документации; моя — **только** `.claude/rulebook|agents|skills` воркспейса,
то есть текст, `@import`-ящийся в каждую сессию. Мой замер добавляет то, чего у
оси нет: **6 из 13 координат живого корпуса мертвы уже сегодня**, до всякого
удаления (служба переименовала свои внутренние каталоги `kacho` → `kaname`).

**И23 (63 против моих 30).** Разные даты одного предиката: 63 снято 2026-09-07
(`design.md` §0, П6), 30 — 2026-09-08. Линия схлопнулась вдвое за сутки; ни одно
число не неверно, и подставлять одно вместо другого без даты значило бы стереть
основание.

### Расхождение, которое разрешено ОПЫТОМ, а не разбором единиц

**Ось «конвейер» и ось «гейты дерева» противоречили друг другу прямо.** Первая
утверждала: беспредметные шаги «молча зелёными не станут — три из них называют
вынос дословно». Вторая: два судьи-цели прочтут отсутствие каталога как
«проверять нечего» и **пройдут**.

Разрешено **опытом** (не чтением). Вызов сборщика с ключом каталога, указывающим на
НЕСУЩЕСТВУЮЩИЙ каталог, выходит кодом **2**; то же на существующем каталоге без
объявления сборки — тоже **2**. Команда здесь намеренно не приводится цитатой: гейт
цитируемых команд судит такую цитату как живую координату и был бы прав — каталога
у неё нет by construction, и отличить демонстрацию от опечатки он не может. Оба вызывающих в
`.github/workflows/ci.yaml` (шаги «манифесты модулей» и «модель доступов»)
несут `case "$rc" in … 2) echo "::notice::…"` **без `exit 1`**.

**Обе оси правы о РАЗНЫХ целях**, и спора между ними нет: первая перечисляла
`permission-catalog-check`, `test-pg-outside-selection`, `test-integration` —
они отказывают громко; вторая — `module-manifest-check` и `model-canon-check`.
Неверен был только **вывод** первой оси, распространённый на все беспредметные
шаги: он шире сделанного.

**И вторая её половина неверна по существу:** эти два шага отнесены к
«предмет уезжает целиком». Предмет **остаётся** — манифестов в дереве после
удаления **пять**, а канон модели лежит в `proto/kaname/cloud/iam/v1/fga_model.fga`,
то есть **вне** уезжающего каталога. Уезжает **исполнитель**. Это и делает
находку дорогой: конвейер напечатает «манифестов в дереве нет ни одного» при
пяти живых манифестах.

> [!important] Вывод ВТОРОЙ оси тоже был шире сделанного — перемерено при сведении
> Она заключила, что снятие каталога «пройдёт» — и это верно про **шаг**, но не про
> **дерево**. Обе цели-судьи объявлены ровно в одном `services/*/Makefile`, и
> провязку каждой судит свой гейт корпуса; снятие объявителя роняет его дважды —
> предпосылкой обхода и обратным направлением («шаг позеленеет ни на чём»). То есть
> посадка молчаливого состояния невозможна, а сам шаг остаётся немым.
>
> **Общее у обеих ошибок — одно и стоит того, чтобы назвать его вслух:** каждая ось
> читала место, где дефект **проявляется**, и делала вывод о том, что дерево
> **производит**. Первая распространила «отказывают громко» на все беспредметные
> шаги; вторая — «шаг молчит» на весь прогон. Разбор и предикат — §2.3.

---

## 2. Перепись производителей — ДО единого сценария

Каждое «Тогда» требует того, что **что-то в дереве производит**. Таблица идёт
первой именно поэтому: три приёмки этой линии подряд получили `CHANGES REQUESTED`
по одному классу — «Тогда» без производителя
(`.claude/rulebook/multi-agent-flow.md` §14а, «класс, отвергший работу дважды»).

**Колонка называется «производитель», а не «чем держится».** Различие не
косметическое: `scripts/docs-gate/check-03-holding-claim-resolves.py` судит
**таблицу состояния кейса** закрытым набором (`держится` · `наполовину` ·
`не начат`). Здесь предмет другой — производитель у большинства сценариев
существует и **сегодня отвечает о дереве, в котором служба ещё есть**, а
состояние сценария при этом «не исполнен». Уложить это в тот набор нельзя, не
соврав в одной из колонок.

| сценарий | что требует «Тогда» | производитель в дереве | есть? |
|---|---|---|---|
| HOME-01 | образ службы, который умбрелла требует, имеет производителя | `deploy/Makefile` (`build-services`, `IMAGES`), `internal/productnaming`; наблюдение — цель `assert-rollout-ready` в `deploy/Makefile` | есть |
| HOME-02 | посадка судится живым процессом, а не объявлением | `deploy/scripts/assert-production-posture.sh` (жёсткий перечень компонентов; объявленный выход — `POSTURE_SKIP`) | есть |
| HOME-03 | у чарта-тёзки не остаётся односторонней взаимности | `deploy/chart_name_addresses_exactly_one_chart_test.go` — обе пробы: `TestChartNameAddressesExactlyOneChart` и `TestStandalonePredicateSelectsExactlyOneChart` | есть |
| HOME-04 | у каждой исполняемой суиты есть шаг-гейт и путь артефактов | `.github/scripts/check-newman-suite-gates.py` | есть |
| HOME-05 | метка третьего исхода читается у своего производителя | `internal/repohygiene/newmanpreconditionmark_test.go` | есть |
| HOME-06 | пробы оснастки исполняются, пустой обход — отказ | `.github/scripts/run-python-probes.py` (ноль файлов по образцу → `ОТКАЗ`) | есть |
| HOME-07 | вердиктный гейт не пропускается молча, когда его файла нет | **заказан `kacho#2326`** — сегодня `deploy/scripts/newman-parallel.sh` и `deploy/scripts/newman-e2e.sh` несут ветку «файла нет ⇒ пропущено», и она молчалива by construction | **производитель заказан** |
| HOME-08 | снятие исполнителя судьи-цели не проходит молча | `internal/repohygiene/modulemanifestcheckwiring_test.go` и `modelcanoncheckwiring_test.go` (механизм `gatetargetwiring.go`, `findJudgeTargetWiringFaults` — обратное направление); доказательство — ось 4 инъекции | **есть — перемерено, посылка была неверна** |
| HOME-09 | судья с живым исполнителем и живым предметом отдаёт 0 | те же два шага `ci.yaml` + `internal/repohygiene/modulemanifestcheckwiring_test.go` (провязка в обе стороны) | есть |
| HOME-10 | приставка карты путей без путей в дереве — находка | `internal/repohygiene/foundationboundary.go`, `judgeFoundationPrefixes` | есть |
| HOME-11 | запись ведомости границ фундамента без предмета — находка | тот же файл, `judgeBoundaryEdges` (ведомость несёт **точный** счёт, а не потолок) | есть |
| HOME-12 | запись ведомости остатка имени без предмета — находка | `internal/repohygiene/kanamenameresidue.go` + `TestKanameNameResidueStayLedgerExpiresWithItsSubject` | есть |
| HOME-13 | запись ведомости самопроверок без предмета — находка | `deploy/scripts/run-gate-self-tests.sh` (перечень `DECLARED`, сверка двусторонняя) | есть |
| HOME-14 | уровень лицензии, объявивший свой файл, роняет прогон без него | `internal/repohygiene/license_test.go` `TestLicenseTierRootsCarryTheirLicenseFile` | есть |
| HOME-15 | образец рабочего пространства не называет несуществующий модуль | **заказан `kacho#2327`** — сегодня сверка в `internal/repohygiene/crossmoduleworkspace_test.go` стоит за `len(modules) > 1`, а модулей станет **один** | **производитель заказан** |
| HOME-16 | при двух модулях образец сверяется в обе стороны | `internal/repohygiene/crossmoduleworkspace_test.go` `TestCrossModuleWorkspaceExampleNamesEveryModule` | есть |
| HOME-17 | отображение путь→лицензия не судит несуществующий каталог | **заказан `kacho#2328`** — сегодня `licenseTierFor` (`internal/repohygiene/licensemap.go`) разрешает по самой длинной приставке и о существовании путей под ней не спрашивает | **производитель заказан** |
| HOME-18 | проверка, знающая путь модуля константой, не переживает свой предмет | **заказан `kacho#2329`** — три константы (`iamModulePathPrefix`, `applierImportPath`, `authzguardPkgPath`), из них две в **не-тестовых** файлах | **производитель заказан** |
| HOME-19 | обязательные контексты защиты ствола сходятся с манифестом шардов | `.github/scripts/assert-required-contexts-match-jobs.py`; расхождение ловит `branch-protection-drift.yml` | есть (по расписанию, не на PR — оговорено в сценарии) |
| HOME-20 | каждая суита дерева принадлежит ровно одному шарду | `deploy/scripts/assert-shard-coverage.py` | есть |
| HOME-21 | проба запрета #6 не измеряет неразвёрнутый домен | `deploy/scripts/assert-ban6-external-isolation.py` (объявленный исход — код 2) | есть |
| HOME-22 | живой корпус не утверждает о топологии того, что перестало быть верным | `.claude/hooks/docfresh/docfresh.py` (судит по стволу продукта, отставание копии называет числом) | есть |
| HOME-23 | свидетельство «чем держится» называет резолвимую координату | `scripts/docs-gate/check-03-holding-claim-resolves.py` | есть |
| HOME-24 | запрет #1 на дереве продукта читается как «читать нечего», а не как успех | `scripts/docs-gate/check-04-product-acceptance-verdict.py` (исход `VOID`, код 2) | есть |
| HOME-25 | каждый отсутствующий путь объяснён снятием, необъяснённых ноль | **заказан `kacho#2330`** — переписи путей, снятых относительно состояния до разреза, в дереве нет: `diff-filter=D` встречается в одном файле, и его обход сужен константой `gateCorpusDir` | **производитель заказан** |
| HOME-26 | носитель гейта корпуса не исчезает молча | `internal/repohygiene/gatecarrierremoval.go`, `TestGateCarrierIsNotRemovedSilently` + надгробие, чьё имя объявляет константа `gateCarrierLedgerName` | есть |
| HOME-27 | носитель гейта **внутри службы** не исчезает молча | **заказан `kacho#2330`** (тот же, что HOME-25) — корпус объявлен константой `gateCorpusDir`, и 4908 утверждений в 1400 файлах службы он не видит by construction; вторая половина «Тогда» машинного предиката не имеет — §2.2 | **производитель заказан наполовину** |

**Итог переписи после сведения, названный точно:** сценариев **27**; производитель
**есть в дереве** у **21**, **заказан задачей** у **6** — `kacho#2326` (HOME-07),
`#2327` (HOME-15), `#2328` (HOME-17), `#2329` (HOME-18), `#2330` (HOME-25 и
HOME-27). Безымянных «производителя нет» не осталось ни одного: третьего исхода —
«оставить как есть» — у этой колонки не существует.

**Первая редакция переписи насчитала семь, и одна из семи была ошибкой ЕЁ, а не
дерева.** HOME-08 объявлялся без производителя по прочтении `case "$rc"` двух
шагов конвейера — то есть посылка бралась у того места, где дефект **проявился**,
а не у того, что **производит** наблюдаемое. Перемер нашёл производителя (§2.3), и
это стоило одной команды. Число исправлено здесь, а не заглушено: перепись,
называющая недостачу шире фактической, обесценивает себя ровно так же, как
называющая уже.

**Шесть — не «почти двадцать семь».** Пять из шести описывают **молчание**, а не
отказ: проверка остаётся на месте, перепись печатает объём, вердикт зелёный, и
отличить это от исправной работы нельзя ничем. Именно они и составляют цену
удаления, потому что громкое красное чинится по тексту отказа, а молчание —
только тем, что кто-то заранее спросил.

---

### 2.1. Молчание нельзя утверждать ОТСУТСТВИЕМ — форма заказанного производителя

**Требование, из-за которого шесть заказов ниже описаны парами, а не строкой
«завести гейт».** «Тогда» вида «шаг не покраснеет», «проверка не сработает
вхолостую», «прогон останется зелёным» **не производится ничем**: оно выполняется
и на исправном дереве, и на мёртвой проверке, и различить эти два состояния такое
утверждение не даёт по построению. Приёмка, принявшая такое «Тогда», описывает
наблюдаемое, которого не бывает.

Годный производитель сценария о молчании — **прогон с внесённым фактом**, и он
всегда пара:

| половина | что подаётся | что обязано произойти |
|---|---|---|
| **внесённый факт** | предмет снят (модуля нет · путей под приставкой ноль · гейта по пути нет) | судья **отказывает либо объявляет третий исход**, называя предмет; зелёного не бывает |
| **законный близнец** | предмет на месте, всё остальное то же | судья **молчит** либо выносит обычный вердикт |

Без второй половины отрицание зеленело бы на дереве, где сломано всё сразу; без
первой — судья мог бы вовсе потерять способность падать, и прогон этого не
показал бы.

**Отсюда — почему все шесть заказов принадлежат фазе A, а не B и не C.**
Положительный близнец у каждого из них **сегодня в дереве и есть**: модулей два,
пути под приставкой лицензии существуют, гейт по пути резолвится, константы
называют объявленный модуль. После удаления близнец исчезает **вместе с
предметом**, и доказать способность нового судьи падать будет нечем — останется
синтетика, у которой «краснеет» перестаёт означать «краснеет на живом входе»
(§6.1). Это и делает A5 несущим пунктом фазы, а не её оформлением.

**Предикат снятия у каждой из шести задач записан в её теле** и здесь не
пересказывается: два места об одном предмете расходятся молча, и расходится
обычно то, которое читают чаще. Здесь — только пара «факт · близнец», потому что
именно она есть требование **приёмки** к производителю, а не деталь его
устройства.

---

### 2.2. Что из шести заказано ЦЕЛИКОМ, а что наполовину

Пять заказов покрывают своё «Тогда» полностью. Шестой — нет, и это сказано здесь,
а не спрятано в «отложено».

**HOME-27 несёт два утверждения, и они разной природы:**

| половина «Тогда» | выразимо машинно? | чем закрывается |
|---|---|---|
| «сказано, **сколько** утверждений уехало» | да | перепись `kacho#2330`: снятых путей · объяснённых · необъяснённых · из них носителей гейтов |
| «**ни одно** из них не стерегло предмет, оставшийся в дереве платформы» | **нет** | ограниченный прокси в той же задаче + поимённая адъюдикация человеком |

**Почему вторая половина невыразима by construction.** «Стерёг ли этот гейт
предмет, оставшийся в платформе» — вопрос о **смысле** утверждения, а не о путях:
гейт службы мог судить общее свойство, не называя ни одной координаты платформы, и
мог называть координату платформы, ничего о ней не утверждая. Предиката, который
различает эти два случая, не существует; выдать за него любой поиск по образцу
значило бы завести проверку, чьё «ноль находок» неотличимо от «ноль понятого».

**Ограниченный прокси назван числом, и его единица названа тоже** (замер по
`origin/main` @ `21f89f6302`):

```sh
git grep -lE '"(pkg|proto|gateway)/' -- 'services/iam/**/*_test.go'   # → 19
```

**19** файлов проб службы называют координату платформы **однозначной** приставкой
(`pkg/` 4 · `proto/` 14 · `gateway/` 3; сумма 21 > 19 — файл вправе назвать более
одной). Приставки `internal/`, `deploy/`, `docs/`, `cmd/`, `tools/` в прокси **не
входят намеренно**: у службы есть свои каталоги с теми же именами (`tools` — 63
файла), и совпадение по ним было бы шумом, а не находкой.

**19 — верхняя граница кандидатов, не число дефектов.** Поимённая адъюдикация не
проводилась ни одной стороной и входит в `kacho#2330`. До неё сказать «ни одно не
стерегло оставшийся предмет» нельзя, и приёмка этого не говорит.

**Остаток, который эта веха НЕ закрывает, с предикатом:** полная адъюдикация
**4908** утверждений службы. Предикат её отсутствия — не число и не прогон, а
свойство: у неё нет машинного судьи, и заводить его не следует, потому что он
судил бы форму записи вместо предмета. Закрывается она человеком в `kacho#2330`
на **19** кандидатах прокси, и объём закрытого обязан быть назван числом — иначе
«адъюдицировано» неотличимо от «прочитано по диагонали».

---

### 2.3. HOME-08: посылка была неверна, и перемер это показал

Строка таблицы правлена **замером**, а не суждением, поэтому предикат назван
здесь целиком.

**Что утверждала первая редакция:** производителя нет, потому что `case "$rc"`
обоих шагов `.github/workflows/ci.yaml` не отличает «манифестов нет» от
«исполнителя нет».

**Что верно.** Само наблюдение о шагах **верно и остаётся в силе** — код возврата
`2` у них действительно значит и то, и другое (опыт §1). Неверен **вывод**: из
того, что шаг молчит, не следует, что молчит **дерево**. Обе цели-судьи объявлены
ровно в одном месте:

```sh
git grep -ln '^module-manifest-check:' -- 'services/*/Makefile'   # → services/iam/Makefile
git grep -ln '^model-canon-check:'     -- 'services/*/Makefile'   # → services/iam/Makefile
```

а провязку каждой судит свой гейт корпуса —
`internal/repohygiene/modulemanifestcheckwiring_test.go` и
`internal/repohygiene/modelcanoncheckwiring_test.go`, механизм общий
(`internal/repohygiene/gatetargetwiring.go`). Он несёт **обе** половины пары:

| половина | место | что делает при удалении каталога |
|---|---|---|
| предпосылка обхода | `len(w.Declaring) == 0` | **отказ**: «ни один из N прочитанных Makefile не объявляет цель … — распознавание сломано либо цель сняли» |
| обратное направление | `findJudgeTargetWiringFaults` | находка «зовёт цель для services/iam, но такой цели там не объявлено — **шаг позеленеет ни на чём**» |

Вторая формулировка есть дословно то наблюдаемое, которого требует HOME-08, и она
**уже доказана инъекцией**: `modulemanifestcheckwiring_injection_test.go`, ось 4
«обратное направление» (внесённый факт → находка), контроль «всё цело — гейт
молчит» (законный близнец) и предпосылка «пустое дерево → отказ». Три прогона, а
не два.

**«Тогда» сценария поэтому переписано под производимое** (§F3): производитель
делает молчаливое состояние **непосадочным**, а не делает шаг громким. Разница
названа прямо, потому что она есть предел этого производителя: сам шаг
по-прежнему не отличает своих двух смыслов кода `2`, и правится он пунктом **B8**
как работа, а не как проверка.

---

## 3. Развилка, меняющая ПРЕДМЕТ наблюдения: судьба контракта

Приёмка описывает удаление при любом исходе — кроме одного места, где исход
меняет **то, что наблюдается**. Оно названо здесь, чтобы разница не всплыла при
исполнении.

### 3.1. Что уже решено, и чем

Р14 замысла (`design.md` §7.4) решило **направление**: имя пакета контракта
следует за продуктом. Веха KAN-PKG-1 решила **имя** и исполнила переход —
пакет сегодня `kaname.cloud.iam.v1`, контракт лежит в `proto/kaname/cloud/iam/v1/`
(**40** файлов), стабы — в `pkg/api/kaname/cloud/iam/v1/` (**83** файла).

### 3.2. Что НЕ решено

**Физический дом контракта после разреза.** Оба каталога лежат **вне**
`services/iam/`, то есть удаление каталога службы их не касается вовсе. Уедут ли
они следом — отдельное решение, и его нет ни в замысле, ни в задаче `kacho#1111`,
чей предикат говорит только об отдельном клоне.

### 3.3. Почему приёмка исходит из «контракт ОСТАЁТСЯ в этой вехе»

Не как из предпочтения, а как из замера — и обратный исход делает веху двумя:

| если контракт уезжает вместе с каталогом | замер |
|---|---:|
| файлов вне службы перестают собираться | **99** (43 прод / 56 проб) |
| из них в крае | 27 |
| из них у пяти служб платформы | 51 |
| из них в провайдере инфраструктуры | 7 |
| файлов вне службы произносят имя пакета **строковым литералом** — они не покраснеют, а **замолчат** | **152** (стабы 83 · край 51 · корпус гейтов 15, адъюдикация поимённо не проводилась) |
| опубликованных выпусков службы, на которые их можно перевести | **0** (`gh api repos/PRO-Robotech/kaname/releases --jq length`, замер `design.md` §7.4) |

Перевод 99 файлов на внешний модуль требует опубликованной версии, а её нет:
значит «контракт уезжает» **невыполнимо by construction** до закрытия `kacho#1766`
и `kacho#2131`. Приёмка это фиксирует не как рекомендацию, а как условие
исполнимости: **сценарии §5 описывают дерево, в котором `proto/kaname/**` и
`pkg/api/kaname/**` остались на месте.** Другой исход — другая веха и другая
приёмка.

**Следствие для сценария HOME-16, названное прямо.** Приёмка перехода
(`acceptance.md`, `IAM-SEV-HOME-03`) ожидает, что гейт запрета подмены модуля
«получает предмет впервые и проходит». На этой развилке верно **обратное**:
модулей станет **один**, и предмет у него **исчезнет**, а не появится. Предмет
вернётся только тогда, когда платформа начнёт пинить опубликованную службу, —
то есть после `kacho#2131`, а не этой вехой. Расхождение названо здесь, а не
исправлено в чужом документе: приёмка перехода — его владелец.

---

## 4. Порядок трёх фаз

Удаление 2394 файлов **необратимо в том смысле, что возврат — новая работа, а не
откат**: восстановленное дерево не станет тем, что было, потому что за время
разреза платформа уйдёт вперёд по тем же файлам, а служба — по своим. Поэтому
порядок здесь не оформление, а предмет: **фаза A целиком состоит из работы,
которую после удаления сделать уже нельзя**.

### Фаза A — ДО удаления. Платформа перестаёт зависеть от каталога службы

Признак завершения фазы: **удаление каталога перестаёт менять поведение
платформы, кроме отсутствия самой службы**.

| # | что переносится или заводится | почему до, а не вместе | замер предмета |
|---|---|---|---|
| **A1** | опубликованный артефакт службы (образ и его пин) | умбрелла требует образ **безусловно**: `image.repository: kaname` в четырёх профилях; производитель образа — единственная строка `deploy/Makefile`, чей контекст сборки равен каталогу службы | выпусков службы **0**; `services/iam` — единственный из 8 `Dockerfile` со своим контекстом |
| **A2** | вердиктный слой сквозных наборов из службы в общее место | без него **98 коллекций из 98** теряют вердикт, третью категорию и перепись исполненности — то есть теряют их **все** наборы, а не один | `assert-suites-green.sh` · `exec-coverage.py` · `coverage.py` — по **1** экземпляру, все в службе; проб оснастки **24 из 24** там же |
| **A3** | производитель метки третьего исхода | гейт читает её **из каталога скриптов службы**, а не проверяемой суиты, и отказывает первой же строкой | метку объявляют все 8 наборов, согласие держит `newmanpreconditionmark_test.go` |
| **A4** | решение о доме контракта — записанное, а не подразумеваемое | §3: обратный исход делает веху двумя, и его исполнимость сегодня равна нулю | 99 импортёров · 0 выпусков |
| **A5** | производители шести заказанных сценариев §2 | пять из шести описывают **молчание**; заводить их после удаления некому — **положительный близнец** снимается вместе с предметом | `kacho#2326` (HOME-07) · `#2327` (15) · `#2328` (17) · `#2329` (18) · `#2330` (25 и 27). HOME-08 из перечня выведен: производитель нашёлся перемером, §2.3 |
| **A6** | перепись путей, снятых относительно состояния до разреза | она отличает потерю от осознанного снятия, и построить её можно только пока обе стороны существуют | заказана `kacho#2330` (HOME-25 · HOME-27); механизм в дереве есть и сужен константой `gateCorpusDir` |

**A5 — несущий пункт фазы, и он контринтуитивен.** Соблазн — «заведём гейт, когда
появится, на что ему краснеть». Здесь наоборот: гейт на молчание обязан быть
**красным на внесённом факте** (предмет снят) и **молчащим на законном близнеце**
(предмет на месте) — а близнец существует **только до удаления**. Форма пары и
причина, по которой все шесть заказов принадлежат этой фазе, а не следующим, —
§2.1.

### Фаза B — ВМЕСТЕ с удалением, одним изменением

Всё перечисленное правится **тем же коммитом**, что снимает каталог. Разнесение
по разным изменениям даёт окно, в котором дерево красно или, хуже, молчаливо
зелено.

| # | что | замер | почему нельзя отложить |
|---|---|---|---|
| **B1** | 28 записей ведомости остатка имени | 28 из 28 в службе | ведомость точная: истечёт вся сразу |
| **B2** | 4 записи ведомости границ фундамента + 1 приставка карты путей | `foundationboundary.go` | обе оси падают громко с текстом «снимите её вместе с предметом» |
| **B3** | запись отображения лицензий + `services/iam/LICENSE` | `licensemap.go:99`, `OwnLicenseFile: true` | снятие файла без записи — красное; записи без файла — **молчание** (HOME-17) |
| **B4** | образец рабочего пространства | `go.work.example`, директива `use ./services/iam` | после удаления его не судит **никто** (HOME-15) |
| **B5** | 7 целей корневого Makefile, зовущих модуль службы | `IAM_MODULE_DIR`, 12 строк | иначе `make test-unit` отказывает у всякого |
| **B6** | перечни служб в двух Makefile | корневой `SERVICES ?= iam …`, `deploy/Makefile` `SERVICES := iam …` | первый же `dev-up` обрывается на сборке образа |
| **B7** | шард `iam` манифеста, `core_images`, `core_ban6_domains` **и** обязательные контексты защиты ствола | `deploy/e2e-shards.json` | имена контекстов **выводятся** из манифеста: снятие шарда без правки защиты оставляет контекст, который не начнётся, не позеленеет и не покраснеет — слияние блокируется бессрочно |
| **B8** | два шага-судьи конвейера | `module-manifest-check`, `model-canon-check` | предмет остаётся (5 манифестов + канон в `proto/`), уезжает исполнитель — иначе `::notice::` при живом предмете |
| **B9** | 3 записи ведомости самопроверок | `run-gate-self-tests.sh`, `DECLARED` | двусторонняя сверка: запись без предмета — провал |
| **B10** | 5 файлов `deploy/*.go` / 6 проб | жёсткие координаты внутрь службы | `t.Fatalf` на чтении |
| **B11** | ~65 гейтов корпуса с исполняемой координатой службы | 83 файла обходят дерево, исполняемо 64–65 | предпосылка «прочитано ноль» роняет прогон |
| **B12** | 3 константы пути модуля службы | 2 из них в не-тестовых файлах | судят по пути модуля, не по каталогу — переживут молча (HOME-18) |
| **B13** | 13 координат живого корпуса правил + строка топологии и число модулей | `.claude/rulebook/polyrepo.md` §Раскладка, §Build-граф | корпус `@import`-ится в каждую сессию: ложное утверждение читается чаще любого другого |
| **B14** | надгробие снятых носителей гейтов корпуса | имя объявляет константа `gateCarrierLedgerName` в `internal/repohygiene/gatecarrierremoval.go`; **файла в дереве сегодня нет, и это норма** — дерево, из которого носителей не снимали, надгробия не несёт (`readGateCarrierLedger` → `nil, nil`) | снятие носителя и объявление снятия правятся **одним** изменением — так объявляет сам гейт. Фаза B снимает носителей десятками, поэтому надгробие заводится **этим** изменением, а не «когда понадобится» |

### Фаза C — ПОСЛЕ удаления. Доказательство, а не уборка

| # | что доказывается | чем |
|---|---|---|
| **C1** | ни один отсутствующий путь не остался необъяснённым | перепись A6 (её производитель заводится в фазе A) |
| **C2** | стенд поднимается на опубликованном артефакте, а не на соседнем каталоге | `make -C deploy dev-prod-up` + `assert-rollout-ready` + `assert-production-posture` |
| **C3** | восемь наборов сквозных проб дают вердикт с тремя исходами | перепись «исполнено N из M» на каждом |
| **C4** | ни одна ведомость не прощает того, чего нет | прогон корпуса гейтов, ноль находок при непустой переписи |
| **C5** | запрет #1 на дереве продукта читается как «читать нечего» | `check-04` → код 2, а не 0 |

**C5 назван отдельно, потому что это потеря, а не поломка.** Все **29** приёмок
дерева продукта лежат внутри службы; после разреза второй дом приёмки исчезает
целиком, и запрет #1 на дереве продукта перестаёт измеряться. Проверка честно
объявит `VOID` — но не отличит «уехали вместе с продуктом» от «не завели ни
одной». Это остаток, а не обещание: он назван здесь и не закрыт этой вехой.

### Почему фазы нельзя переставить

- **A перед B** — потому что после удаления производителя для шести заказанных
  сценариев заводить не на чем: положительный близнец исчезает вместе с предметом,
  и «краснеет» перестаёт означать «краснеет на живом входе»;
- **B одним изменением** — потому что 14 его пунктов дают либо красное, либо
  молчание, и разнесение по коммитам оставляет окно, в котором ствол красен для
  всех, кто в это время работает;
- **C после** — потому что доказывать поглощение содержимым можно только на
  сведённом дереве (`.claude/rulebook/change-graph.md` §8).

---

## 5. Scope — восемь поверхностей

| F | поверхность | сценарии |
|---|---|---|
| F1 | образ службы и подъём стенда | HOME-01, HOME-02, HOME-03 |
| F2 | вердиктный слой сквозных наборов | HOME-04, HOME-05, HOME-06, HOME-07 |
| F3 | судьи, читающие код возврата | HOME-08, HOME-09 |
| F4 | ведомости и карты путей: самоистечение | HOME-10 … HOME-14 |
| F5 | проверки, которые после удаления замолчат | HOME-15, HOME-16, HOME-17, HOME-18 |
| F6 | защита ствола и шарды | HOME-19, HOME-20, HOME-21 |
| F7 | корпус правил и координаты | HOME-22, HOME-23, HOME-24 |
| F8 | перепись потерь и носители гейтов | HOME-25, HOME-26, HOME-27 |

---

## F1 — образ службы и подъём стенда

### HOME-01 — образ, который умбрелла требует, имеет производителя (положительный)

**Дано** дерево платформы после удаления каталога службы; профиль умбреллы
по-прежнему объявляет компонент службы (подчарт `distribution: platform`
остаётся, он **не** лежит в удаляемом каталоге).
**Когда** поднимается стенд.
**Тогда** образ службы берётся у объявленного производителя — из реестра по
опубликованному пину — и под доходит до `Ready`.

*Отличие от сегодняшнего дня — один факт: производитель образа сменился со сборки
на пин. Сегодня образ собирает единственная строка, чей контекст равен каталогу
службы; после удаления той строке нечего собирать, а требование образа
остаётся — умбрелла его не теряет.*

### HOME-02 — посадка судится живым процессом, а не объявлением (положительный)

**Дано** поднятый стенд в боевой посадке.
**Когда** прогоняется проверка посадки.
**Тогда** она читает посадку, **объявленную процессом при старте**, и шифрование
со стороны базы, а не содержимое карты настроек.

*Сценарий остаётся в силе и после разреза, и это его предмет: перечень
компонентов у проверки жёсткий, служба в нём названа поимённо. Объявленный выход —
сужение перечня, а не молчаливый пропуск.*

### HOME-03 — у чарта-тёзки не остаётся односторонней взаимности (отрицательный)

**Дано** два чарта с одним именем: один в умбрелле (`distribution: platform`),
второй в каталоге службы (`distribution: standalone`); каждый называет другого.
**Когда** второй снимается вместе с каталогом, а взаимная запись первого
остаётся.
**Тогда** прогон роняется: запись называет координату, которой нет.

**Положительный близнец, отличающийся одним фактом:** взаимная запись снята тем
же изменением — прогон проходит, и перепись называет **один** чарт этого имени.

---

## F2 — вердиктный слой сквозных наборов

### HOME-04 — у каждой исполняемой суиты есть шаг-гейт и путь артефактов (положительный)

**Дано** дерево после разреза: суита службы снята, семь остальных исполняются.
**Когда** проверяется связность конвейера.
**Тогда** у каждой исполняемой суиты есть шаг, зовущий вердиктный гейт, и путь
её отчётов в артефактах.

*Сегодня семь из восьми таких шагов зовут гейт **абсолютным путём внутрь
удаляемого каталога**. Сценарий исполним только после того, как гейт переехал
(A2): иначе проверка связности честно найдёт шаги, чей адресат не существует.*

### HOME-05 — метка третьего исхода читается у своего производителя (положительный)

**Дано** прогон любой суиты.
**Когда** вердиктный гейт определяет, чем является отсутствие отчёта.
**Тогда** метку третьего исхода он читает **у производителя проверяемой суиты**,
а не у производителя суиты службы.

*Отличие от сегодняшнего дня — один факт: адресат чтения. Сегодня гейт читает
метку из каталога, где сам лежит, и это работает ровно пока он лежит в службе.*

### HOME-06 — пробы оснастки исполняются, пустой обход — отказ (положительный)

**Дано** прогон проб оснастки сквозных наборов.
**Когда** обход по объявленному образцу не находит ни одного файла.
**Тогда** прогон **отказывает**, а не отчитывается чистотой.

*Это уже норма дерева и уже производитель: обход даёт `ОТКАЗ` на нуле файлов.
Сценарий назван потому, что сегодня **24 пробы из 24** лежат в удаляемом
каталоге, и без переноса (A2) отказ наступит на первом же прогоне — верно, но
дорого: он не отличит «пробы уехали» от «образец неверен».*

### HOME-07 — вердиктный гейт не пропускается молча (отрицательный) · производитель заказан: `kacho#2326`

**Дано** прогонщик наборов, у которого путь вердиктного гейта не резолвится.
**Когда** прогон завершается.
**Тогда** он **отказывает**, называя отсутствие гейта, и вердикт не падает до
сырого счёта прогонщика.

*Сегодня обе ветви обоих прогонщиков делают обратное: «файла нет ⇒ гейт
пропущен», и это молчаливо by construction — исход прогона выглядит так же, как
у прогона с исполненным гейтом.*

**Положительный близнец:** путь резолвится — гейт исполняется, вердикт несёт
четыре исхода запроса раздельно. **Он существует сегодня**, и потому пара
доказуема сейчас, а после переезда гейта — уже нет.

*Отдельно, сверх переписи: сообщение `newman-e2e.sh` о пропуске **не называет
пути**, поэтому «гейт выключен ручкой» и «файла гейта нет» неразличимы для
читателя журнала. Это третья ось той же пары, и она в заказе названа.*

**Третий исход (`ban #11`) заказан `kacho#2326`:** ветвь пропуска обязана быть
либо снята, либо превращена в отказ **тем же изменением, что переносит гейт**
(A2). Оставить её «на случай» значит завести послабление, которое не истечёт
никогда: после переезда гейта его условие станет истинным навсегда.

---

## F3 — судьи, читающие код возврата

### HOME-08 — снятие исполнителя судьи-цели не проходит молча (отрицательный)

**Дано** цель-судья, объявленная ровно в одном `services/*/Makefile` — том, что
уезжает, — при **живом предмете** в дереве: манифестов модулей пять, канон модели
доступов лежит вне каталога службы.
**Когда** каталог снят, а шаг конвейера продолжает звать цель.
**Тогда** прогон корпуса гейтов **роняется**, называя цель и отсутствие её
объявителя, — и посадка такого дерева невозможна.

*«Тогда» переписано под производимое, и прежняя его редакция названа в §2.3
вместе с предикатом, которым она опровергнута. Требовалось наблюдаемое «шаг
отказывает»; в дереве его не производит ничто, а производится другое — снятие
объявителя роняет `internal/repohygiene/modulemanifestcheckwiring_test.go` и
`modelcanoncheckwiring_test.go` дважды: предпосылкой обхода (объявителей ноль) и
обратным направлением («шаг позеленеет ни на чём»).*

**Положительный близнец — HOME-09** (объявитель на месте, гейт молчит, шаг отдаёт
0). Различающий факт ровно один: существование объявителя цели.

**Предел этого производителя назван прямо.** Он делает молчаливое состояние
**непосадочным**, но не делает шаг **громким**: `case "$rc"` обоих шагов
по-прежнему читает код `2` и как «манифестов нет», и как «исполнителя нет»
(опыт §1). Пока гейт корпуса цел, второй смысл недостижим — но он недостижим
**чужой** проверкой, и это надо знать тому, кто однажды тронет её корпус.

**Третий исход остаётся работой, а не проверкой:** шаг переводится на
исполнителя, оставшегося в дереве, либо снимается **вместе с исполнителем** —
пункт **B8**. «Оставить с кодом 2» исходом не является: он даёт зелёный конвейер
при пяти живых манифестах.

### HOME-09 — судья с живым исполнителем и живым предметом отдаёт 0 (положительный)

**Дано** шаг, чей исполнитель существует, и предмет в дереве непуст.
**Когда** шаг исполняется.
**Тогда** он отдаёт 0 и печатает перепись: сколько манифестов прочитано, сколько
блоков сверено, сколько байт.

*Отличается от HOME-08 **одним** фактом — существованием исполнителя. Оба
исхода сегодня различимы только этим, и потому близнец обязателен: без него
отрицание зеленело бы на любом дереве.*

---

## F4 — ведомости и карты путей: самоистечение

### HOME-10 — приставка карты путей без путей — находка (отрицательный)

**Дано** карта путей корпуса гейтов, объявляющая приставку каталога службы.
**Когда** под этой приставкой в дереве не остаётся ни одного пути.
**Тогда** прогон роняется с текстом, называющим приставку и предписывающим снять
её **вместе с предметом**.

**Положительный близнец:** приставка, под которой пути есть, — молчит, и перепись
называет число рассмотренных приставок.

### HOME-11 — запись ведомости границ фундамента без предмета — находка (отрицательный)

**Дано** ведомость известных рёбер границы, где источник четырёх записей лежит в
каталоге службы.
**Когда** каталог снят, а записи остались.
**Тогда** прогон роняется: ведомость несёт **точный** счёт, а не потолок, и
потому истекает сама.

**Положительный близнец:** записи сняты тем же изменением — прогон проходит, а
перепись называет оставшееся число рёбер.

### HOME-12 — запись ведомости остатка имени без предмета — находка (отрицательный)

**Дано** ведомость остатка чужого имени: **28 записей из 28** указывают внутрь
каталога службы.
**Когда** каталог снят.
**Тогда** прогон роняется по каждой записи, которой больше нечего прощать.

**Положительный близнец:** ведомость опустела и снята целиком — прогон проходит,
и это **цель**, а не поломка: проверка на пустой ведомости обязана проходить,
объявляя «записей 0».

### HOME-13 — запись ведомости самопроверок без предмета — находка (отрицательный)

**Дано** перечень объявленных самопроверок гейтов, где три записи указывают в
каталог службы.
**Когда** каталог снят.
**Тогда** прогон роняется: сверка двусторонняя — «в списке есть запись, для
которой самопроверки больше нет».

**Положительный близнец:** записи сняты — перечень и дерево сходятся.

### HOME-14 — уровень лицензии, объявивший свой файл, роняет прогон без него (отрицательный)

**Дано** отображение путь→лицензия, где уровень каталога службы объявляет
собственный файл лицензии.
**Когда** каталог и его файл сняты, а запись осталась.
**Тогда** прогон роняется, называя уровень и отсутствующий путь.

**Положительный близнец:** запись снята вместе с файлом — прогон проходит и
объявляет число уровней со своим файлом; **ноль** таких уровней — тоже отказ, и
это отдельное свойство того же производителя.

---

## F5 — проверки, которые после удаления ЗАМОЛЧАТ

Семейство собрано по одному признаку: после удаления проверка остаётся на месте,
исполняется, печатает перепись и **не может покраснеть**. Отличить это от
исправной работы нельзя ничем, кроме вопроса, заданного заранее.

### HOME-15 — образец рабочего пространства не называет несуществующий модуль (отрицательный) · производитель заказан: `kacho#2327`

**Дано** дерево, где объявление модуля осталось **одно**, а отслеживаемый образец
рабочего пространства по-прежнему объявляет директиву на каталог службы.
**Когда** прогоняется корпус гейтов.
**Тогда** прогон роняется: образец называет модуль, которого в дереве нет.

*Сегодня обе пробы образца стоят за условием «модулей больше одного». При одном
модуле проверка не зовётся вовсе: находок ноль, перепись печатает «модулей 1»,
вердикт зелёный. Комментарий самого гейта называет это состояние **законным** —
и он прав про кросс-модульную разработку, но не про **остающийся образец**.*

**Положительный близнец — HOME-16.**

**Третий исход заказан `kacho#2327`:** образец приводится к составу дерева либо
снимается **тем же изменением**, что и каталог. «Оставить до появления второго
модуля» — послабление без предиката снятия: второй модуль вернётся не раньше, чем
платформа начнёт пинить опубликованную службу (§3.3).

*Сверх переписи: шапка самого `go.work.example` утверждает, что гейт «сверяет
обе стороны». При одном модуле это перестаёт быть верным — утверждение переживает
свой предмет в файле, который читают ровно тогда, когда собираются им
воспользоваться. Правится тем же заказом.*

**Пара, доказуемая сегодня:** внесённый факт — модуль один, образец называет
директиву на несуществующий каталог → находка; законный близнец — модуль один,
образец называет только корень → молчание. Различающих фактов ровно один, и
условие `len(modules) > 1` его сегодня скрывает.

### HOME-16 — при двух модулях образец сверяется в обе стороны (положительный)

**Дано** дерево с двумя объявлениями модуля.
**Когда** прогоняется тот же гейт.
**Тогда** он находит **и** модуль без директивы, **и** директиву без модуля.

*Это сегодняшнее состояние дерева, и оно служит положительным близнецом
HOME-15: различие ровно в одном факте — числе модулей.*

### HOME-17 — отображение путь→лицензия не судит несуществующий каталог (отрицательный) · производитель заказан: `kacho#2328`

**Дано** отображение путь→лицензия, где приставка каталога службы снята вместе с
файлом лицензии (HOME-14 закрыт), **но** соседняя проверка совместимости
по-прежнему строит своё ожидание, разрешая путь службы через то же отображение.
**Когда** путь больше не существует.
**Тогда** проверка **называет** это, а не выносит вердикт о несуществующем.

*Разрешение идёт по самой длинной совпавшей приставке и о существовании каталога
не спрашивает. Пока запись жива, красное придёт от соседа (HOME-14). Как только
запись снимут вместе с файлом, пара исчезнет из наблюдения **тихо**: ожидание
станет вычисляться по приставке уровня по умолчанию, и вердикт останется
зелёным.*

**Положительный близнец:** приставка, чей каталог существует, — вердикт о ней
верен и проверяем.

**Третий исход заказан `kacho#2328`:** карта уровней лицензий участвует в той же
четвёртой оси, что и две карты путей `internal/repohygiene/foundationboundary.go`
(`judgeFoundationPrefixes` — «запись, чьей приставке не соответствует ни один
путь, никогда не совпадёт и потому не истечёт сама»). Ось уже написана, доказана
инъекцией и покрывает **две** карты из трёх; закрыть надо симметрию собственного
принципа, а не изобретать механизм.

*Уровень по умолчанию (пустая приставка) из оси исключён **by construction**:
пустая приставка совпадает со всем, и судить её нечем. Это обязано быть сказано в
коде, а не подразумеваться, — иначе следующий читатель примет исключение за
пропуск.*

### HOME-18 — проверка, знающая путь модуля константой, не переживает свой предмет (отрицательный) · производитель заказан: `kacho#2329`

**Дано** три места корпуса гейтов, держащие путь модуля службы **константой**, а
не координатой каталога; два из них — не-тестовые файлы.
**Когда** каталог службы снят, а модуль перестал существовать в дереве.
**Тогда** каждое из трёх объявляет, что его предмет исчез.

*Сегодня они судят по **пути модуля**, а не по каталогу: удаление каталога их
условие не нарушает, совпадений становится ноль, и перепись печатает ноль
находок. Это тот же класс, что «негативная проверка замолкает при снятом
предмете» (`.claude/rulebook/testing.md` §«Гейт на класс», п. 9), только предмет
здесь снимается **не правкой самой проверки**, и потому автор снятия о ней не
узнает.*

**Положительный близнец:** константа, чей модуль в дереве есть, — проверка
находит свои совпадения, и их число печатается.

**Третий исход заказан `kacho#2329`:** каждое из трёх мест либо переводится на
признак, который дерево **производит** (координата каталога либо объявление
модуля), либо снимается вместе с предметом; «оставить как есть» исходом не
является.

*Смежная задача `kacho#2251` («путь нашего модуля, не резолвящийся ни в один
пакет, держателя не имеет») этого класса **не закрывает и после разреза теряет ту
же популяцию тем же механизмом**: её гейт судит токены, начинающиеся с
**объявленного в дереве** пути модуля, а объявление модуля службы уезжает вместе
с каталогом. Две задачи смежны и не заменяют друг друга — там предмет популяция
без держателя, здесь константа, пережившая свой модуль.*

---

## F6 — защита ствола и шарды

### HOME-19 — обязательные контексты защиты сходятся с манифестом шардов (отрицательный)

**Дано** манифест шардов, из которого **выводятся** имена обязательных контекстов
защиты ствола.
**Когда** шард суиты службы снят из манифеста, а имя его контекста осталось в
обязательном наборе защиты.
**Тогда** расхождение названо, и слияние не блокируется бессрочно.

*Цена ошибки здесь качественно иная, чем у прочих находок: контекст, который не
начнётся, **не позеленеет и не покраснеет** — он останется «ожидается», и ствол
закроется навсегда, без единого красного.*

**Положительный близнец:** манифест и защита сходятся — перечень контекстов
выводится и совпадает поимённо.

**Оговорка, названная прямо:** производитель существует, но встречает расхождение
**по расписанию**, а не на запросе слияния. Значит окно между правкой манифеста и
прогоном по расписанию остаётся, и в фазе B оба места правятся **одним заходом**,
а не полагаются на проверку.

### HOME-20 — каждая суита дерева принадлежит ровно одному шарду (отрицательный)

**Дано** манифест шардов после снятия шарда службы.
**Когда** сверяются суиты дерева и суиты, названные шардами.
**Тогда** ни одна суита дерева не осталась без шарда и ни один шард не называет
суиту, которой нет.

*Сегодня шард службы называет её суиту, а ядро всех пяти шардов требует её образ
**безусловно**: ручки выключения у ядра нет by construction.*

**Положительный близнец:** сумма суит шардов равна числу наборов дерева.

### HOME-21 — проба запрета #6 не измеряет неразвёрнутый домен (отрицательный)

**Дано** проба внешней изоляции внутренних методов, чей перечень доменов ядра
называет службу.
**Когда** домен на шарде не развёрнут.
**Тогда** проба объявляет **третий исход** — «условие не создано», — а не отказ и
не успех.

*Это её сегодняшнее объявленное поведение, и оно уже наблюдалось. Сценарий назван
затем, чтобы третий исход не был прочитан как вердикт: домен, которого нет,
ответить не может, и это не находка о продукте.*

**Положительный близнец:** домен развёрнут — проба спрашивает его внутренний
метод и получает отказ на внешнем периметре.

---

## F7 — корпус правил и координаты

### HOME-22 — живой корпус не утверждает о топологии того, что перестало быть верным (отрицательный)

**Дано** корпус правил воркспейса, `@import`-ящийся в каждую сессию, где строка
раскладки называет каталог службы частью монорепо, а раздел о графе сборки
объявляет **два** объявления модуля.
**Когда** каталог снят и объявление модуля осталось одно.
**Тогда** оба утверждения приведены к факту тем же изменением, и проверка
свежести не находит несуществующих координат.

*Класс уже срабатывал на этом самом разделе: полтора месяца там стояло «модуль
один» после того, как модулей стало два, и ложное утверждение читалось чаще
любого другого — потому что правило загружается в каждую сессию.*

**Положительный близнец:** координата, чей путь в стволе продукта резолвится, —
проверка молчит, а перепись называет объём осмотренного и отставание копии
числом.

### HOME-23 — свидетельство «чем держится» называет резолвимую координату (отрицательный)

**Дано** приёмки воркспейса, чьи таблицы состояния называют координаты внутри
каталога службы.
**Когда** каталог снят.
**Тогда** каждая такая строка названа: свидетельство, чью координату нельзя
разрешить, свидетельством не является.

*Замер, названный числом: **6 из 13** координат службы в живом корпусе мертвы
**уже сегодня** — служба переименовала свои внутренние каталоги, а корпус остался
с прежними. Удаление доводит это до 13 из 13.*

**И это уже наблюдается, а не предсказывается.** Прогон набора проверок
документов на стволе воркспейса даёт **6 находок** этого самого гейта, и все
шесть — в приёмке `KAN-AUTHN-1`: три кейса называют координаты внутри
`services/iam/cmd/kacho-iam/`, тогда как служба переименовала этот каталог в
`cmd/kaname`. Контроль проведён в обе стороны: те же шесть находок и тот же код
возврата **1** получаются **без** этого документа, то есть красное
предсуществующее, а не привнесённое им. Заведения задачи это требует отдельно —
предмет чужой, и чинить его в этом изменении значило бы смешать линии.

**Положительный близнец:** координата резолвится вместе с именем проверки внутри
файла — строка засчитывается.

### HOME-24 — запрет #1 на дереве продукта читается как «читать нечего» (положительный)

**Дано** дерево продукта после разреза, где приёмок не осталось ни одной.
**Когда** прогоняется чтение вердиктов второго дома приёмок.
**Тогда** исход — **«читать нечего»**, отдельной строкой переписи, и он не
засчитывается в успех.

*Все **29** приёмок дерева продукта лежат внутри службы. Проверка объявит третий
исход честно — но она **не отличает** «уехали вместе с продуктом» от «не завели
ни одной». Это назван остаток, а не закрытый вопрос: см. §7, п. 6.*

---

## F8 — перепись потерь и носители гейтов

### HOME-25 — каждый отсутствующий путь объяснён снятием (отрицательный) · производитель заказан: `kacho#2330`

**Дано** дерево платформы после разреза.
**Когда** перечисляются пути, отсутствующие относительно состояния до него.
**Тогда** **каждый** объяснён снятием, и необъяснённых **ноль**.

*Перепись обязательна и тогда, когда «и так всё видно»: она отличает потерю от
осознанного снятия. Производителя у неё в дереве нет — ни один инструмент не
сверяет состав дерева до и после по путям. Построить её можно только пока обе
стороны существуют (фаза A, пункт A6).*

**Положительный близнец:** путь, снятый и объявленный, в перечень необъяснённых
не попадает.

**Третий исход заказан `kacho#2330`** — задачей фазы A (пункт A6). Второй исход,
«разрез без доказательства сохранности», остаётся выразимым, но тогда он обязан
быть **сказан вслух** в отчёте о разрезе, а не подразумеваться.

*Механизм для переписи в дереве **есть** и сужен константой:
`internal/repohygiene/gatecarrierremoval.go` считает снятое между базой и HEAD
(`diff-filter=D`) по каталогу `gateCorpusDir`. Расширять надо охват, а не
изобретать способ.*

**Предпосылка, отличающая эту перепись от предыдущих пяти:** пустая дельта (ветка
ничего не снимает) — **законный зелёный**, а не отказ. Отказом остаётся
непрочитанное: база не резолвится либо состав дерева не прочитан. Проба не имеет
права падать на достижении своей цели.

### HOME-26 — носитель гейта корпуса не исчезает молча (отрицательный)

**Дано** корпус гейтов и надгробие снятых носителей, лежащее внутри него.
**Когда** файл-носитель снят, а записи в надгробии нет.
**Тогда** прогон роняется, называя носителя.

*Гейт существует и объявляет собственный предел: единица счёта — **файл**, а не
имя утверждения, и база сравнения — точка ответвления линии, а не ствол.
Сценарий назван потому, что фаза B снимает носителей корпуса десятками, и каждый
обязан попасть в надгробие **тем же изменением**.*

**Положительный близнец:** носитель снят и объявлен — прогон проходит, перепись
называет число объявленных снятий.

### HOME-27 — носитель гейта ВНУТРИ службы не исчезает молча (отрицательный) · производитель заказан наполовину: `kacho#2330`

**Дано** каталог службы, несущий **4908** гейтовых утверждений в **1400** файлах,
включая **78** доказательств инъекцией.
**Когда** каталог снят целиком.
**Тогда** сказано, сколько утверждений уехало и что ни одно из них не стерегло
предмет, оставшийся в дереве платформы.

*Существующий гейт этого не покроет: его корпус объявлен как каталог проверок
дерева, и утверждения службы он не видит **by construction**. Прогон после
удаления даст зелёное — и это верно о корпусе и ничего не говорит об уехавшем.*

**Положительный близнец:** носитель корпуса — виден и требует объявления
(HOME-26). Различие ровно в одном факте: **каталог**, в котором лежит носитель.

**Третий исход заказан `kacho#2330` — тем же, что HOME-25, и НАПОЛОВИНУ.**
Разбор в §2.2; здесь — только то, что связывает исполнителя.

**Первый из двух названных исходов отвергнут замером, а не вкусом.** Расширить
корпус надгробия на `services/**` значило бы требовать записи на снятие **любого**
файла проб **любого** сервиса — послабление краснело бы на обычном рефакторинге, а
краснеющее на верной работе отключают первым. Реализуемая форма — перепись,
которая **считает и называет**, а не ведомость, которая требует записи по файлу.

**Вторая половина «Тогда» машинного предиката не имеет** — «стерёг ли этот гейт
предмет, оставшийся в платформе» есть вопрос о смысле, а не о путях. Ограниченный
прокси назван числом в §2.2 (**19** файлов проб службы называют координату
платформы однозначной приставкой); поимённая адъюдикация **19** кандидатов входит
в задачу и выполняется человеком. До неё утверждение «ни одно не стерегло
оставшийся предмет» не сделано, и приёмка его не делает.

---

## 6. Компенсации по каждому виду нарушения порядка

**Отката у этого изменения нет.** Возврат каталога — новая работа, а не откат:
за время разреза платформа уйдёт вперёд по общим файлам, а служба — по своим, и
восстановленное дерево не станет прежним. Поэтому ниже названа не «отмена», а
**компенсация**: что делать, если порядок уже нарушен, и чем это стоит дороже,
чем соблюдение.

### 6.1. Удаление опередило фазу A (B без A) — самый дорогой случай

| признак | сквозные наборы дают вердикт без гейта; конвейер печатает «манифестов нет» при пяти живых; образ службы не собирается и не тянется |
|---|---|
| **что уже потеряно** | положительные близнецы шести заказанных сценариев §2: предмет, на котором они молчали бы законно, снят вместе с каталогом |
| **компенсация** | восстановить **вердиктный слой** из истории (`git show <ревизия до удаления>:<путь>`), а не написать заново: переписанный гейт не равен тому, чьи пробы уехали, и доказать это нечем |
| **чего компенсация НЕ вернёт** | доказательства способности гейта падать. Инъекции строились на настоящем дереве; после удаления их близнец синтетический, и «краснеет» перестаёт означать «краснеет на живом входе» |
| **цена** | восемь наборов идут без вердикта столько времени, сколько занимает восстановление; каждый их зелёный за это окно **недействителен**, а не зелёный |

### 6.2. Фаза A сделана, удаление не доведено — два экземпляра одного гейта

| признак | вердиктный гейт есть и в общем месте, и в каталоге службы |
|---|---|
| **цена** | два места об одном предмете расходятся молча; расходится обычно то, которое читают чаще — то есть общее |
| **компенсация** | экземпляр в каталоге службы **заменяется на переходник**, зовущий общий, либо снимается; ведомость самопроверок правится тем же заходом (три записи) |
| **предикат снятия** | `git ls-tree -r origin/main --name-only \| grep -c assert-suites-green` → **1** |

### 6.3. Фаза B разнесена по нескольким изменениям

| признак | ствол красен дольше одного прогона; либо, хуже, зелен при снятом предмете |
|---|---|
| **цена** | 14 пунктов B дают либо громкое красное (ведомости, карты путей, координаты), либо **молчание** (образец рабочего пространства, отображение лицензий, константы пути). Смешение видов делает вердикт непрослеживаемым: красное объясняют одним пунктом, а молчащие пункты не объясняет никто |
| **компенсация** | свести незакрытые пункты в **один** доводящий коммит и прогнать корпус гейтов целиком; частичное закрытие не засчитывать — перепись «находок 0» на неполном B означает, что молчащие пункты просто не измерялись |
| **предикат** | ноль находок при **непустой** переписи по каждому набору проверок |

### 6.4. Шард снят, защита ствола не правлена (B7 наполовину)

| признак | запрос слияния показывает контекст в состоянии «ожидается» и не двигается |
|---|---|
| **цена** | ствол закрыт **бессрочно и без единого красного**: контекст, который не начался, не зеленеет и не краснеет. Проверка расхождения существует, но приходит по расписанию |
| **компенсация** | привести обязательный набор защиты к именам, выводимым из манифеста, и **перепроверить сверкой имён**, а не наличием зелёного: «влит» и «его проверки были зелёными» — разные факты |
| **чего компенсация НЕ вернёт** | ничего необратимого здесь нет — это единственный пункт, чья цена измеряется только временем |

### 6.5. Контракт уехал вместе с каталогом (нарушение §3)

| признак | **99** файлов вне службы не собираются; **152** файла произносят имя пакета литералом и **не краснеют** |
|---|---|
| **цена** | вторая половина хуже первой: несобирающееся видно сразу, а маршруты и записи каталога решений, ключуемые именем, **замолкают** — край перестаёт резолвить методы, не сказав об этом |
| **компенсация** | вернуть `proto/kaname/**` и `pkg/api/kaname/**` в дерево платформы (они не лежат в удаляемом каталоге, поэтому возврат здесь дёшев) и **записать решение** о доме контракта отдельной вехой |
| **предикат исполнимости обратного** | опубликованных выпусков службы **0**: перевод 99 файлов на внешний модуль невозможен by construction до закрытия задач публикации |

### 6.6. Фаза C не сделана

| признак | в отчёте о разрезе нет ни числа объяснённых снятий, ни исхода подъёма на пине |
|---|---|
| **цена** | «удалили и всё зелено» не отличимо от «удалили, и часть проверок замолчала». Пять из шести заказанных сценариев §2 — именно про это |
| **компенсация** | перепись C1 строится **из истории** (`git diff --diff-filter=D <ревизия до>..HEAD --name-only`), и это дороже, чем построить её заранее: список придётся адъюдицировать целиком, а не сверять с объявленным |
| **предикат** | необъяснённых путей **ноль** при непустом перечне рассмотренных |

### 6.7. Носители гейтов сняты без объявления (B14 пропущен)

| признак | прогон корпуса зелёный, число утверждений упало |
|---|---|
| **цена** | удаление сторожа делает прогон **зеленее** и не может быть замечено прогоном by construction |
| **компенсация** | надгробие достраивается из истории по тому же разностному признаку, что использует сам гейт (перепись носителей сейчас против переписи на базе линии) |
| **чего компенсация НЕ вернёт** | утверждения, уехавшие **внутри** каталога службы: их корпус гейта не видит, и восстановить их перечень можно только вручную из истории (HOME-27) |

---

## 7. Определение готовности

1. **у каждого сценария назван производитель — координатой в дереве либо номером
   заказавшей его задачи** — выполнено: сценариев **27**, производитель в дереве у
   **21**, заказан задачей у **6** (`kacho#2326` · `#2327` · `#2328` · `#2329` ·
   `#2330` дважды). Безымянного «производителя нет» не осталось ни одного. У пяти
   заказов из шести производитель обязан быть **парой** «внесённый факт · законный
   близнец» (§2.1); у шестого (HOME-27) машинно выразима одна половина «Тогда», и
   вторая названа остатком с прокси на **19** кандидатов (§2.2);
2. **у каждого отрицания назван положительный близнец, отличающийся одним
   фактом** — выполнено; у HOME-08/HOME-09 и HOME-15/HOME-16 близнец вынесен
   отдельным сценарием, потому что различающий факт — состояние дерева, а не
   входа;
3. **третья категория присутствует как отдельная, не вычитаемая** — выполнено:
   HOME-21 (условие не создано) и HOME-24 (читать нечего);
4. **место, где исход решения меняет предмет наблюдения, названо** — выполнено,
   §3, и там же названо расхождение с приёмкой перехода по `IAM-SEV-HOME-03`;
5. **порядок фаз назван, и по каждому виду его нарушения названа компенсация** —
   выполнено, §4 и §6;
6. **остаток, который эта веха НЕ закрывает, назван числом** — выполнено, и
   остатков **два**, а не один:
   - запрет #1 на дереве продукта после разреза перестаёт измеряться (**29**
     приёмок уезжают, второй дом исчезает целиком), и производителя у его замены
     нет;
   - вторая половина «Тогда» HOME-27 — «ни одно уехавшее утверждение не стерегло
     предмет, оставшийся в платформе» — **машинного предиката не имеет by
     construction** (§2.2). Заказано ограниченным прокси в `kacho#2330`:
     **19** кандидатов из **1400** файлов проб службы, адъюдикация поимённая и
     человеком. Предикат закрытия — не прогон, а **названное число**
     адъюдицированных: без него «адъюдицировано» неотличимо от «прочитано по
     диагонали»;
7. **вердикт выпущен внешним событием и записан** — **не выполнено**: документ на
   review не подавался.

**Пункт 7 — единственный невыполненный, и он не «почти выполнен».** Пока вердикта
нет, `ban #1` этой вехой не удовлетворён, и кодить по ней нельзя — включая
удаление хотя бы одного файла каталога службы.

---

## 8. Чего эта приёмка НЕ покрывает

- **вторую половину разреза** — что происходит у самой службы: её несут предикат
  `kacho#1111` и приёмка перехода, семейства `BOOT`, `DEP`, `MOD`, `REST`;
- **решения замысла** — они приняты владельцем и оркестратором (`design.md` §0,
  §7.1–§7.4) и здесь не пересматриваются;
- **судьбу контракта** — §3 называет развилку и условие исполнимости, но решения
  не принимает: у него другой владелец;
- **выпиливание модуля квотирования** (`kacho#2117`, П8 предиката эпика) — оно
  идёт своей вехой и своим порядком, обратным привычному: сперва потребители
  перестают спрашивать, затем снимается отвечающий;
- **адъюдикацию 83 файлов корпуса гейтов поимённо** — ни одна сторона её не
  делала; в §1 названо расхождение в единицу и назван его предел;
- **поведение на поднятом стенде** — ни один сценарий не наблюдался: документ
  собран чтением дерева и одним опытом (код возврата вызова цели в
  несуществующем каталоге).

### Слепые зоны сведения — называю сам

1. **Ни один сценарий не прогонялся.** «Покраснеет» и «замолчит» — выводы из
   чтения кода производителей, а не наблюдения. Исполненных измерений **два**, оба
   названы: код возврата `make -C` на несуществующем каталоге и на существующем
   каталоге без Makefile (**2** в обоих случаях, §1) — и перепись объявителей двух
   целей-судей (§2.3), опровергшая посылку HOME-08.
2. **Числа шести осей воспроизведены выборочно.** Перемерено **девять** величин
   из двадцати трёх (И2, И3, И4, И5, И6, И7, И10, И13, И17, И22); остальные взяты
   у осей с их предикатами и здесь не перепроверялись.
3. **Единица «исполняемое употребление» груба.** Она снимает хвостовой
   комментарий текстом, а не разбором, и потому считает координатой всякую
   строку, где путь стоит вне `//` — включая **синтетические фикстуры**, строящие
   поддельное дерево. Именно на этом мой первый предикат разошёлся с осью
   «гейты» в **девять раз** (676 против 65), и починка была сменой единицы, а не
   уточнением образца.
4. **Не мерилось обратное направление** — сколько текстов **внутри** службы
   называют координаты вне её. Это ось того, что сломается у переехавшей службы,
   и она принадлежит другой приёмке.
5. **Не адъюдицированы 152 файла**, произносящих имя пакета литералом (§3.3):
   разделение «маршрут · запись каталога · проза» не проводилось ни одной
   стороной, и число названо как верхняя граница кандидатов.
6. **Судьба вспомогательных каталогов не разобрана**: `tests/authz-fixtures`
   (16 файлов) остаётся в платформе, но его исполнитель — чеканка — идёт через
   службу; чем это станет после разреза, зависит от A1 и здесь не решено.
7. **Способность шести заказанных производителей падать НЕ доказана** — они
   заказаны, а не написаны. Приёмка утверждает только то, что у каждого сценария
   назван адрес работы и форма её приёмки (пара «внесённый факт · законный
   близнец», §2.1); доказательство инъекцией остаётся за самими задачами. До их
   закрытия шесть сценариев в покрытие **не засчитываются**, и предикат готовности
   вехи это учитывает.
8. **Прокси HOME-27 не адъюдицирован** (§2.2). **19** — верхняя граница
   кандидатов, снятая одним выражением по трём однозначным приставкам; ни один
   кандидат не прочитан. Единица счёта — **файл**, а не утверждение, поэтому с
   числом **4908** она несравнима напрямую и сводить их в одну строку нельзя.
9. **Перемер шести осей §1 не расширялся.** К девяти воспроизведённым величинам
   добавлены три новых замера (объявители двух целей-судей · прокси HOME-27 · три
   константы пути модуля); остальные четырнадцать по-прежнему взяты у осей с их
   предикатами и здесь не перепроверялись.

---

## 9. История ревью

История добавляется, не переписывается: вердикт относится к отпечатку
содержимого, а не к имени файла (`.claude/rulebook/change-graph.md` §2). Прежнее
состояние — «записей нет, subject на review не подавался» — было верно до круга 1
и здесь сохранено как предшествующее, а не стёрто.

### 9.1. Круг 1 — 2026-09-08, `acceptance-reviewer`: ✅ APPROVED

**Предмет вердикта:** `sha256:eb834301b529aff3e4e6628f4cc26524a49b7e0ad7cc678996506a3021d18e44`
(blob `b4d7c09f`, коммит `6ef1090e`, ветка `docs/kan-extract-1`, 910 непустых
строк). **Дерево продукта, на котором перемерялось:** `PRO-Robotech/kacho` @
`origin/main` `21f89f6302` — та же ревизия, на которой собран документ.

**Блокирующих — ноль.** Проверялось **опытом и перемером**, не прочтением.

**Несущая находка воспроизведена независимо.** `make -C ./нет-такого-каталога
цель` → **2**; контроль «каталог есть, цели нет» → **2**; контроль «каталог и цель
есть» → **0**. Оба вызывающих в `.github/workflows/ci.yaml` (строки **756** и
**818**) несут ветвь `2) echo "::notice::…"` **без `exit 1`** — шаг остаётся
зелёным. Отдельно отмечено: текст самой метки говорит «это НЕ успех», а
исполняемая часть выходит успехом; проза и поведение расходятся, и верна проза.
Документ описывает это верно.

**Довод о порядке фаз (A перед B) — верен, и это тоже замер, а не согласие.**
Модулей в стволе **2** (`go.mod`, `services/iam/go.mod`); сверка образца рабочего
пространства стоит за `len(modules) > 1`
(`internal/repohygiene/crossmoduleworkspace_test.go:212`), и ветвь при одном
модуле — **законный молчаливый пропуск по замыслу**, что сказано комментарием в
самом коде. После удаления `found` остаётся пуст, проба печатает перепись и
**проходит**. Положительный близнец действительно снимается вместе с предметом,
поэтому A5 несущий, а не оформительский.

**§2.1 соблюдён самой приёмкой во всех 27 сценариях, а не только в шести
заказанных.** Обход всех «Тогда»: каждое — положительное производимое
утверждение (`роняется` · `отказывает` · `объявляет третий исход` · `называет` ·
`отдаёт 0 и печатает перепись`). Формы «шаг не покраснеет» нет ни одной. У
HOME-07 и HOME-24 отрицательная половина стоит **следствием** положительного
факта, а не вместо него.

**Перепись рецензента сошлась с переписью автора.** Заголовков `### HOME-NN` —
**27**, номера сплошные 01…27, дублей ноль, пометку «производитель заказан» несут
**6**. Пять заказанных задач существуют и открыты, заголовок каждой отвечает
своему сценарию (`#2326` HOME-07 · `#2327` HOME-15 · `#2328` HOME-17 · `#2329`
HOME-18 · `#2330` HOME-25/27). **Сценариев без названного производителя — ноль.**

**Перемерено выборочно, воспроизвелось точно:** удаляемых файлов **2394**;
записей ведомости остатка имени **28**, из них внутрь службы **28**; строк с
`IAM_MODULE_DIR` в корневом Makefile **12**; констант пути модуля **3**, из них в
не-тестовых файлах **2**; прокси HOME-27 → **19** (`pkg` 4 · `proto` 14 ·
`gateway` 3); экземпляров вердиктного слоя по **1**, все в службе; приёмок дерева
продукта **29**, вне службы **0**; предикат §6.2 → **1**; контракт вне удаляемого
каталога (`proto/kaname` 40 · `pkg/api/kaname` 83, внутри `services/iam/` — **0**).
Координаты производителей резолвятся в стволе у **22 из 23**.

**Расхождение было одно, и слепым оказался предикат РЕЦЕНЗЕНТА.** По И4 я получил
**141** против **99** документа — ровно то число, которое §1 уже назвал и
разобрал: моя приставка (`pkg/api/kaname`) шире авторской
(`pkg/api/kaname/cloud/iam/v1`) и считает комментарии и сами порождённые стабы.
Документ предсказал мою ошибку раньше, чем я её сделал; довод §1 «три величины,
три единицы, все три верны» подтверждён.

**Три названные автором слепые зоны рассмотрены и блокирующими НЕ признаны:**

| зона | почему не блокирует |
|---|---|
| ни один сценарий не прогонялся | приёмка пишется **до** работы (ban #1); от неё требуется **исполнимость**, а не исполненность. Прогнать сценарии значило бы удалить каталог — тот самый необратимый шаг, который она и гейтит. Один опыт, разрешавший противоречие двух осей, автор поставил, и он верен |
| 14 величин из 23 без перемера | предикат закрытия фазы B — **не число**, а «ноль находок при непустой переписи» (§6.3): состав работы перечисляют сами гейты. Пять перемеренных мною сошлись точно, шестое расхождение оказалось моим — основание считать остальные того же качества |
| обратное направление не мерилось | явно вынесено §8 в `kacho#1111` и приёмку перехода (`BOOT`/`DEP`/`MOD`/`REST`). Названное исключение объёма с названным владельцем — законно |

**Компенсации §6 исполнимы, а не объявлены.** У каждой названо действие и
предикат; два прогнаны дословно (§6.2 → **1**; §6.5 «контракт вне каталога» →
**0** внутри). §6.1 и §6.7 честно называют, чего компенсация **не** вернёт, —
это и делает их компенсациями, а не обещаниями отката.

#### Не блокирующие — учесть в посадочном коммите

1. **[КООРДИНАТА]** §2, строка производителя HOME-01 называет
   `deploy/scripts/assert-rollout-ready.sh` — такого файла в стволе **нет**
   (предикат: `git cat-file -e origin/main:deploy/scripts/assert-rollout-ready.sh`
   → отказ; в `deploy/scripts/` его нет среди 63 файлов). Механизм существует, но
   как **цель** `assert-rollout-ready` в `deploy/Makefile:792`, и сам документ
   пишет её верно в §4 C2 — то есть два места об одном предмете, из которых верно
   дальнее. Сценарий от этого исполнимым быть не перестаёт: производители HOME-01
   (`deploy/Makefile`, `internal/productnaming`) резолвятся оба. Правится заменой
   на `deploy/Makefile` (цель `assert-rollout-ready`). Отмечено отдельно потому,
   что машинного судьи у этой колонки **нет**: `check-03-holding-claim-resolves`
   судит «таблицу состояния кейса», которой здесь намеренно нет (§2), — то есть
   ровно тот класс, который стережёт HOME-23, в самой приёмке не удержан ничем.
2. **[СРЕДА, не предмет приёмки]** `./scripts/docs-gate/run-all.sh` при доступном
   дереве продукта отдаёт **1**: `check-03-holding-claim-resolves` даёт 6 находок
   в `sub-phase-KAN-AUTHN-1-…-acceptance.md` (координаты внутри `services/iam/`,
   уже переименованные службой). Это **предсуществует** на `main` и данной веткой
   не введено: ветка меняет **один** файл (`git diff --name-only
   origin/main...origin/docs/kan-extract-1`). К вердикту не относится, но слияние
   PR #586 в зелёный ствол потребует закрытия того документа отдельно.

**Что вердикт разрешает и чего НЕ разрешает.** Разрешена **фаза A целиком**,
включая A5 — написание шести заказанных производителей с парой «внесённый факт ·
законный близнец». **Фаза B закрыта** до закрытия `#2326`…`#2330`: пока они
открыты, шесть сценариев в покрытие не засчитываются (§7 п. 1, §8 п. 7), и первое
же удаление файла `services/iam/` уничтожит положительные близнецы, на которых
эти producers доказываются. Это не оговорка ревью, а предикат самой приёмки —
здесь он лишь назван вслух, чтобы APPROVED не прочитали как «можно удалять».
