# Sub-phase IAM-ID-1 (отрыв аккаунта от пользователя) — Acceptance

> **Статус: ✅ APPROVED — круг 12, `acceptance-reviewer`, 2026-08-24.** Санкция выдана;
> её объём назван ниже и он **уже** документа. Оба блокирующих круга 10 закрыты, семь важных —
> тоже. Блокирующих в этом круге **ноль**; всё найденное сверх — неблокирующее и вынесено
> отдельным перечнем, чтобы его не приняли за условие санкции.
>
> **Ревизии замера круга 12** (числа относятся к ним и ни к чему больше): воркспейс —
> `acc/iam-id-1-b3` @ `e917367`; продукт — `release/identity-4` @ **`76c12ec78`**. Это
> **сегодняшняя вершина**, на **5** коммитов дальше ревизии круга 11 (`f58089a44`): §1
> перемерен мной на ней, а не принят с чужого замера.
>
> ### Б10.2 — посылка МОЕГО ЖЕ предшественника опровергнута, и круг 11 прав
>
> Это главный исход круга. Круг 10 (то есть я в предыдущем заходе) блокировал переносом `65`
> на том основании, что «второе членство на одну строку личности неконструируемо до S3.2».
> **Основание ложно, и было ложным уже тогда.** Проверено не прочтением:
>
> | Что | Мой замер на `76c12ec78` | Контроль (обязан различать) |
> |---|---|---|
> | `c83eb6bc1` и `5ce4e89e8` — предки ревизий кругов 9 (`475f8527`) и 10 (`ec133b94e`) | **предки все четыре раза** | обратная проверка (вершина против старой ревизии) отвечает «не предок» |
> | глобальный ключ почты — второй СТРОКИ человеку не завести | **1** | — |
> | приглашение пишет членство тем же стейтментом | **1** | — |
> | вызывающий — публичный глагол приглашения | **1** вызов в `invite.go` | **0** во всех прочих use-case; голое имя даёт **3** — единица «вызов», и она названа |
> | ветвь зеркала, снимавшая чужие членства, снята при S2 | **0** в новой миграции | **1** в первой редакции `470001` — предикат находит |
>
> **Решает не арифметика предков, а посев.** Проба
> `TestUserInvite_ExistingActiveEmail_SecondAccount` (посажена, 285 строк) строит ровно то,
> чего требует `Given` сценария `65`: строк с этой почтой — **одна**, членств — **два**, оба
> `ACTIVE`, плюс собственный контроль третьим аккаунтом, где то же чтение обязано получить
> `ErrNotFound`. То есть конструируемость **доказана посевом**, как и требует
> `gate-authoring`, а не выведена из замысла.
>
> **Отсюда `65` конструируем на S3.1, переносить его некуда, и мой предыдущий блокирующий
> был выставлен по непроверенной посылке.** Записываю это прямо: посылка выглядела
> следствием замысла, и ни автор, ни я её не перемеряли — ровно тот класс, который
> `doc-truthfulness` называет «утверждение пережило свой предмет». Радиус круг 11 взял
> честно: правлено **одиннадцать** мест плюс одно добавление, и в действующей части я не
> нашёл ни одного уцелевшего вхождения опровергнутого довода.
>
> ### Б10.1 — закрыт; перепись круга 11 воспроизводится, и она точнее моей
>
> §0.2 п. 1 больше не заказывает посаженный глагол: формулировка приведена к §1.9. Прежняя
> фраза («глаголы записи **и снятия** членства … строятся здесь и целиком») в действующей
> части **отсутствует**; два её вхождения — цитата внутри моего вердикта круга 10 и отчётная
> таблица §16.3, то есть история. Контроль: на ревизии круга 10 (`4fca3bc`) фраза в §0.2
> **есть** — предикат различает. *(Моя первая форма контроля промахнулась по разметке —
> искала выделение, которого там не было; это моя ошибка предиката, названная затем, чтобы
> её не повторили.)*
>
> **Счёт мест — восемь, а не шесть, и круг 11 прав.** Его предикат воспроизвёлся у меня
> дословно: кандидатов **12**, несут канон **8**, говорят о другом **4**. Названы обе
> величины и способ отсева — это и делает восьмёрку проверяемой, а не заявленной.
>
> ### Семь важных — исход по каждому, проверено замером
>
> | # | Что | Чем проверил |
> |---|---|---|
> | 1 | парное утверждение о кортеже у `65` | пара «до/после» стоит, утверждается **предковость**, а не написание кортежа; форма названа по концу стадии; `65` внесён в DoD п. 5 |
> | 2 | исполнимость починенного предиката §1.5 | **прогнал**: даёт `permission)     = "iam.users.list"`. Прежняя форма не исполняется вовсе (движок отвергает escape) — диагноз круга 11 подтверждён |
> | 3 | естественный ключ и предикат истечения | `UNIQUE (user_id, account_id)` — **полный**, `WHERE` нет; контроль на заведомо частичном ключе `WHERE` находит. Предикат истечения назван |
> | 4 | родовые перечни S3.1 | сужены — см. Б10.1 |
> | 5 | отступление записано в дереве продукта | DoD п. **17** заведён; его предикат сегодня **0** (работа исполнителя), контроль по `services/iam/internal` — **25**; сопутствующие числа сошлись все: `user.mdx` **8**, пронумерованных расхождений **17**, файлов каталога **26**, образец nlb существует |
> | 6 | предикат каталога сужен | без исключения **4**, с исключением порождённой коллекции **3**; четвёртый файл действительно коллекция при исходнике `cases/iam-user.py` |
> | 7 | метка стадии у `31` | стоит **S3, шаг S3.1**, с записью о том, что устарела ровно метка |
>
> **Замечание соседу** переписано с условного на факт: сосед `Delete` не заказывает (**0**),
> снятие судит тиром аккаунта со ссылкой на посаженный глагол (**8**), состояние — `DRAFT`.
> Все три предиката воспроизвелись.
>
> ### Что перемерено мной независимо
>
> | Величина | Мой замер | Контроль |
> |---|---|---|
> | арифметика `71 / 9 / 1 / 61` | **сходится целиком** | замыкание незачётной переписью: **10** анкеров состояния + **61** сценарий без анкера = **71** заголовок — число действующих получено вторым, не вычитающим способом |
> | §1 на сегодняшней вершине | значения прежние: `ListAccountsForUser` 29·8 · `membership_id` 0 · `rpc RemoveFromAccount` 1 · `string account_id = 6` 0 · `reserved` 2 · `"mbr"` 0 | — |
> | публикуемость | **сборки нет**: адъюдицировано 14 кандидатов, ни в одном не сходятся координата + условие отказавшей защиты + следствие для постороннего | части (имена отношений, коды, пути) на месте — документ не выхолощен |
> | ban #14 | маркеров отложенной работы **0** в действующей части и **0** в истории | тот же предикат по соседним приёмкам даёт **143** файла из 196 — он исполним и различает |
>
> ### Неблокирующее — перечень; санкции НЕ обусловливает
>
> 1. **Круг 11 заявил «все восемь несут одну формулировку» шире, чем показывает дерево.**
>    Несущее сужение («сверх посаженного исключения» либо «снятие из этого перечня») стоит
>    во **всех восьми** — это и есть то, что закрывает Б10.1. Но перечисление в скобках
>    различается: «создание и два чтения» несут **4** из 8, «снятие уже есть» — **6** из 8.
>    Противоречия нет ни в одном (нигде не сказано «и снятия»), поэтому это точность, а не
>    предмет; но заявление стоит привести к замеру.
> 2. **§2.3а сохранил абсолютную форму довода о дельте** («пока членство у каждого одно,
>    дельта пуста»), тогда как §4 ту же мысль уже уточнил до «пусто для преобладающего
>    случая». Утверждение как условное истинно, а несущий вывод (S3.1 неделим) верен и не
>    затронут — но два места об одном предмете разной точности.
> 3. **Отрицательная половина предиката замечания соседу вырожденна**: `rpc Delete` даёт 0 в
>    документе, где объявлений формы `rpc X` нет **вовсе**. Вывод спасает положительная
>    половина (**8**), и по существу факт верен — я проверил сам; но пара стоила бы того,
>    чтобы её негативная сторона была способна найти.
>
> ### ОБЪЁМ САНКЦИИ — что разрешено кодировать
>
> **Разрешено:** стадии **S1 → S2 → S3 → S4** в порядке и границах §4, шаг S3 — строго
> `S3.1 → S3.2 → S3.3`, и **61 действующий сценарий** как источник проб. S3.1 приземляется
> **одним изменением** (модель, префикс `mbr`, глаголы ресурса членства сверх посаженного
> исключения, записи каталога, строка форм ответа края). Действует строгий TDD (ban #12) и
> production-completeness каждой стадии в её границах (ban #14).
>
> **ВНЕ САНКЦИИ — не кодировать:**
>
> | Что | Почему |
> |---|---|
> | **второй глагол снятия членства** | `UserService.RemoveFromAccount` посажен; §1.9 — решение, адресация парой, у ресурса членства `Delete` не заводится |
> | **девять сценариев с отозванной санкцией** (43-48, 63, 66, 67) | позволение реализовывать отозвано; текст сохранён свидетельством |
> | **сценарий 49** | перенесён, производитель — IAM-ID-2-22 |
> | **плоская поверхность чтения членств** и форма «в каких аккаунтах состоит НАЗВАННЫЙ человек» | отменены решением владельца; запрет исполняется **отсутствием параметра**, а не проверкой прав |
> | **закрытие стадии S3** | внешнее предусловие: у IAM-ID-2 сегодня `DRAFT`; четыре действующих сценария (01, 55, 68, 73) производителя здесь не имеют. Объявлять S3 production-complete до `APPROVED` соседа нельзя (DoD п. 16) |
> | **контракт двух форм чтения** | принадлежит IAM-ID-2 §2.1; здесь он предусловие, а не определение — второго контракта не заводить |
>
> **Условия, не блокирующие старт, но входящие в DoD:** запись отступления в
> `services/iam/docs/engineering/architecture/` (**DoD п. 17**, предикат сегодня даёт `0` —
> это работа исполнителя тем же изменением, что и S3.1).
>
> **§1 — замер, а не лицензия.** Норма §1.0а не истекает: дерево ушло на 5 коммитов за время
> этого ревью, и перед первым изменением §1 перемеряется заново
> (`git rev-list --count 76c12ec78..<вершина>` не ноль).
>
> ---
>
> **Круг 10 (история, `acceptance-reviewer`, 2026-08-24) — ЗАКРЫТ кругом 12, вердиктом больше
> НЕ является.** На момент записи действующей
> санкции не было; оба его блокирующих закрыты кругом 11 и перепроверены кругом 12. Блокирующих **два**, оба —
> непроброшенное следствие правок круга 9, а не новый предмет; важных — семь. Область правки
> **сужена** и названа ниже: круги 7 и 9 закрыли одиннадцать блокирующих, и переписывать
> закрытое запрещено.
>
> **Ревизии замера круга 10** (числа относятся к ним и ни к чему больше): продукт —
> `release/identity-4` @ **`ec133b94e`** (2026-08-24 17:21 +0300); воркспейс —
> `acc/iam-id-1-b3` @ `4fca3bc`.
>
> **Дерево двигалось ВО ВРЕМЯ ревью, и это названо числом, а не оговоркой.** На первой моей
> команде вершина была `1a8d01994`, на второй — `ec133b94e`: **4** коммита за минуты (волну
> вливает соседняя сессия). От ревизии круга 9 (`475f8527`) вершина ушла на **10**, от его
> пересъёма (`03d8ce07a`) — на **4**. Поэтому все предикаты ниже прогнаны через `git show
> "$R":…` на пиннутом хеше, а не по рабочей копии.
>
> ### §1 перебазирован верно — сплошной прогон на СЕГОДНЯШНЕЙ вершине не нашёл ни одного расхождения дерева
>
> Я не поверил перечню §15.1, а прогнал предикаты §1.1-§1.9 **сплошь** — 87 команд на
> `ec133b94e`. Совпало **всё**, включая те двенадцать, что круг 9 объявил разошедшимися и
> исправил. Выборочно, чтобы замер можно было повторить:
>
> | Что | Объявлено кругом 9 | Мой замер на `ec133b94e` |
> |---|---:|---:|
> | `blk List \| grep -c 'scope_filtered'` · контроль `blk Invite` | 2 · 0 | **2 · 0** |
> | `blk List \| grep -c '"<exempt>"'` | 0 | **0** |
> | `string account_id = 6` · `reserved` | 0 · 2 | **0 · 2** |
> | `from_request_field: "account_id"` | 2 | **2** |
> | писатели кортежа области на личности · зеркало по членству | 5 · 0 | **5 · 0** |
> | `ListAccountsForUser` все · непробные | 29 · 8 | **29 · 8** |
> | пол типизированных потребителей | 15 | **15** |
> | `kacho_account_id` в кейсе · утверждающих исходников | 5 · 2 | **5 · 2** |
> | `parentAccountExpr: "o.account_id"` · типы | 3 · project/group/serviceAccount | **3 · те же** |
> | `fkText`: имён · `users_account_fk` среди них · ловушка `case "` | 9 · 0 · 41 | **9 · 0 · 41** |
> | §1.9: глагол · `member_remover` · `membership_id` в контракте iam | 1 · 4 · 0 | **1 · 4 · 0** |
> | §2.1а: `"mbr"` в каталоге · контроль `PrefixLimitHyphen` | 0 · ≥1 | **0 · 3** |
> | сосед IAM-ID-2 (предикат DoD п. 16) | `DRAFT` | **`DRAFT`** |
>
> Норму §1.0а это не отменяет и срок годности главы не продлевает: совпадение на десяти
> коммитах есть свидетельство, а не гарантия. Но вывод для вердикта прямой — **на перебазировке
> §1 претензий нет**, и открывать её кругу 11 нечем.
>
> ### Четыре блокирующих круга 8 — исход по каждому, проверено замером, а не прочтением
>
> | # | Закрыт | Чем я это проверил |
> |---|---|---|
> | **Б8.1** | **да** | предикат стал различающим: `scope_filtered` у `List` — **2**, у `Invite` — **0**; прежняя пара по `"<exempt>"` даёт **0 · 0**, то есть действительно перестала различать. Полоса в контракте — `permission = "iam.users.list"` + `scope_filtered = true`, сужение на данных; §1.5 п. 3 и выноска `21` говорят это же, и утверждения `21` остались об **исходе** |
> | **Б8.2** | **да, исходом** | скрипта нет: `git grep -ln 'fga_ensure'` → **0**; единственное вхождение `heal-authz` в дереве — надгробие в прозе (`ui-future/proxies.md`), файла нет. Коммит `c78901a33` резолвится и принадлежит этому же переходу. Взамен класс 3 ключуется на пару «отношение + тип объекта» и даёт **5** писателей при контроле **15** и зеркале **0** — предикат различает, а не находит слово |
> | **Б8.3** | **да, решением — и решение верно** | разбор ниже отдельным блоком |
> | **Б8.4** | **да** | `string account_id = 6` → **0**, `reserved 6` + `reserved "account_id"` → **2**; §1.5 строка 1 и §4 строка S4 говорят теперь одно |
>
> Пять важных круга 8 закрыты — проверил каждое: класс 5 (**5** вхождений, **два** исходника,
> порождённая коллекция исключена с объяснением), `ListAccountsForUser` (**29** и **8** с
> прежними значениями рядом), положительный контроль §1.1 (**3**, с выноской о том, что ложным
> было объяснение числа), `1..73` в §9.2 и §9.3 сведены, атрибуция контроля в `64` названа
> двумя предикатами (по токену — пять сервисов, по конструктору полосы — четыре; несущий ноль
> у `iam` верен у обоих).
>
> ### РЕШЕНИЕ Б8.3 — ПРИНЯТО, и оно сильнее, чем документ его обосновывает
>
> Вопрос стоял так: называет ли пара «человек + аккаунт» область решения полностью и не
> теряется ли то, ради чего заводился идентификатор членства. Ответ — **называет, и не
> теряется**, но решающий довод в §1.9 не назван.
>
> Документ стоит на области выдачи (`member_remover` @ `account`, пара уже несёт аккаунт,
> гейту нечего резолвить). Это верно и достаточно для анти-BOLA. **Но опровергнуть решение
> можно было бы только одним способом — показав, что пара не определяет членство однозначно**,
> и вот на этот вопрос §1.9 не отвечает. Отвечает схема, которую посадил **этот же** документ:
>
> ```sh
> git show "$R":services/iam/internal/migrations/470001_memberships_expand.sql \
>   | grep -A1 'CREATE UNIQUE INDEX memberships_user_account_unique'
> #   → ON kacho_iam.memberships (user_id, account_id);
> ```
>
> Ключ **полный, а не частичный**, и миграция объясняет, почему частичным он быть не может:
> состояний у членства два, снятие есть удаление строки, предиката для исключения из ключа не
> существует. Значит пара — **естественный ключ**, и `mbr-…` для снятия не несёт ничего, чего
> не несёт пара. Ban #15 не задет: обе половины пары — неизменяемые идентификаторы.
>
> Отсюда же берётся **предикат истечения решения**, которого в §1.9 нет: решение живёт, пока
> ключ полон. Заведут третье состояние членства («приостановлено») и сделают ключ частичным —
> пара перестанет быть ключом, и адресация по паре станет неоднозначной **молча**. Внести
> (важное В10.3).
>
> Второй глагол снятия заводить действительно нельзя, и сужение перечня S3.1 верно — но
> **проброшено не всюду**, см. Б10.1.
>
> ### Блокирующие круга 10 — два, оба одного класса: правка круга 9 не доехала
>
> **Б10.1. §0.2 по-прежнему ЗАКАЗЫВАЕТ посаженный глагол — в том самом абзаце, который
> документ велит читать первым.** Внешнее предусловие, п. 1: «шаги S3.1 и S3.2 от вердикта
> соседа НЕ зависят — модель, префикс, глаголы записи **и снятия** членства, каталог, край,
> переезд глагола приглашения строятся здесь и целиком».
>
> Это дословно то, чем блокировал круг 8: исполнитель, прочитавший «глаголы снятия строятся
> здесь», заводит **второй** глагол снятия членства. §1.9 решил обратное, §2.3а и §4 сужены,
> §0.2 — нет.
>
> **Перепись мест, называющих работы S3.1** (действующая часть, строки 657-4573; шапка не
> считается — там история вердикта круга 6, сохранённая намеренно): **шесть**. Сужены и верны
> **три** (§1.9, таблица §2.3а, таблица §4); **противоречит один** (§0.2); остаются родовыми
> **два** (проза §2.3а «глаголы ресурса членства», §9.1 «глаголы членства»).
>
> **Круг 9 назвал это «сужено в обоих местах»** (§15.2, Б8.3). Мест не два, и ошибка здесь не в
> аккуратности, а в **единице счёта** — ровно тот класс, который сам круг 9 разобрал у строки
> 22 своей таблицы. Радиус берётся по имени механизма, а не по диффу, в котором правку делали.
>
> **Почему это блокирует, а не «мелочь на одну фразу».** §0.2 несёт собственное объяснение,
> почему стоит первым: «исполнитель, дошедший до §5, уже несёт в голове форму… Указание,
> отменённое в конце, из головы не выгружается — оно оттуда исполняется». Ложное указание в
> этом месте дороже такого же в §9.1.
>
> **Это НЕ пересмотр принятого.** Круг 8 принял §0.2 как **форму внешнего предусловия** и
> проверил, что предмет реален (у соседа `DRAFT`). Фраза про глаголы снятия стала ложной
> **позже** — решением круга 9. Предусловие как таковое не трогается.
>
> **Исход:** §0.2 п. 1 привести к §1.9 (снятие уже есть; здесь строятся создание, два чтения
> §5.0 п. 0, модель, префикс, каталог, край, переезд приглашения). Родовые перечни §2.3а
> (проза) и §9.1 — тоже, одним заходом (важное В10.4).
>
> **Б10.2. Перенацеленный `65` требует ЧЕЛОВЕКА В ДВУХ АККАУНТАХ — а карта стадий об этом не
> знает.** Круг 9 добавил `65` утверждение «тот же человек продолжает работать в `B`, где его
> членство осталось». Проверено сравнением: в редакции круга 8 (`b472689`) этого утверждения
> **не было** — сценарий обходился одним членством.
>
> Второе членство **на одну строку личности** сегодня неконструируемо: зеркало S1 заводит ровно
> одно членство на строку пользователя, а человек, приглашённый в два аккаунта, существует как
> две строки — это и есть предмет перехода. Документ сам называет, когда это меняется:
> «**S3.2** — переезд глагола приглашения… с этого момента второе членство конструируемо, и
> IAM-ID-1-31/-57 исполнимы **без синтетики**» (§2.3а, §4, §9.1). `65` в этом перечне **не
> назван** ни в одном из трёх мест.
>
> **Почему это блокирует.** §9.1 объявляет себя ответом на вопрос «относительно какого
> множества стадия зелена» (DoD п. 10) и распределяет сценарии **по конструируемости, а не по
> теме**. Сценарий, стоящий в остатке S3.1 и неисполнимый до S3.2, делает «S3.1 зелена»
> недоказуемой — то есть стадию нельзя объявить production-complete в своих границах (ban #14).
> Исполнитель же получит выбор между остановкой и синтетикой, которую документ запрещает
> прямым текстом.
>
> **Исход:** назвать `65` рядом с `31`/`57` в трёх местах (§2.3а, §4, §9.1) либо — если автор
> считает иначе — **доказать конструируемость** двух членств на одну личность до S3.2 посевом,
> а не рассуждением (`gate-authoring`: конструируемость доказывается посевом, и «не строится»
> означает, что неверен сценарий, а не продукт).
>
> ### Важное — не блокирует, чинится тем же кругом
>
> 1. **`65`: новое утверждение о кортеже — голое отрицание, и оно пиннит ДО-S3-форму.**
>    «Кортеж `account:A` → `account` → `iam_user:<id>` после вызова отсутствует» не имеет
>    положительного близнеца (до вызова присутствует), поэтому зеленеет на системе, которая
>    такого кортежа не пишет вовсе. Хуже: сам сценарий стоит на **S3**, а после S3 этот кортеж
>    ключуется на членство (§1.5а класс 3, зеркало `31`) — то есть утверждение станет
>    тождественно истинным по посторонней причине. Дать пару «до/после» и назвать форму,
>    ожидаемую **на конце стадии**. DoD п. 5 перечисляет сценарии с парными отрицаниями — `65`
>    туда добавить.
> 2. **§1.5: предикат извлечения полосы НЕ ИСПОЛНЯЕТСЯ** — `blk List | grep -oE 'permission +=
>    +"[^"]+"'` объявлен с выводом `"iam.users.list"`, а печатает **пустоту**: между
>    `permission` и `=` стоит `)`, которого в выражении нет. Заведён кругом 9 в том самом блоке,
>    которым закрывался Б8.1. Это §1.0 п. 1 дословно («команда возвращает значение, а не
>    пустоту»), то есть предикатом эта строка не является. Несущая пара рядом (`scope_filtered`
>    2 · 0) работает, поэтому вывод не меняется. Рабочая форма: `grep -oE 'permission\)[[:space:]]*=
>    *"[^"]+"'` → `permission)     = "iam.users.list"`.
> 3. **§1.9: назвать естественный ключ и предикат истечения решения** — полный
>    `UNIQUE (user_id, account_id)` из `470001` есть решающий довод за адресацию парой, и он же
>    даёт условие, при котором решение перестаёт быть верным (ключ стал частичным). Без него
>    решение стоит на области выдачи и истекает молча.
> 4. **Родовые перечни S3.1** — проза §2.3а и §9.1 говорят «глаголы ресурса членства» /
>    «глаголы членства» там, где две таблицы уже говорят «сверх посаженного исключения из
>    аккаунта». Пока §0.2 не исправлен, это часть Б10.1; после — самостоятельный класс «два
>    места об одном предмете разной точности».
> 5. **Отступление от стандартных методов ресурса объявлено ТОЛЬКО в приёмке.** §1.9 честно
>    называет ценой отсутствие `Delete` у ресурса членства и объявляет это решением — но
>    `api-conventions.md` требует, чтобы осознанное дизайн-решение легло в
>    `services/iam/docs/engineering/architecture/` соответствующего сервиса. Документ сам
>    требует ровно этого от отступления полосы (выноска `64`), и не требует от своего.
>    Приёмка закрывается и уходит из поля зрения, а страница сервиса остаётся — иначе следующий
>    заведёт `Delete` «по стандарту». Внести пунктом DoD.
> 6. **§1.9: запись каталога названа тремя местами, предикат находит четыре.** «Обе вшитые
>    копии каталога + кейс newman», а `git grep -l 'iam.users.removeFromAccount' -- gateway
>    services/iam` даёт **4** файла — четвёртый есть порождённая коллекция. Либо назвать её,
>    либо исключить с объяснением, как это уже сделано у класса 5.
> 7. **`31`: «правка модели делается на S2» против двенадцати мест, где она на S3.** Строка
>    пережила решение о переносе (проверено: стоит и на `42af441`, и на `0cb8ad2`, и на
>    `b472689`, и сегодня), при том что §2.3а объявляет перенос **своим заголовком**. Рассуждение
>    вокруг («до того как членств станет больше») остаётся верным — устарела ровно метка стадии.
>
> ### Что ОСТАЁТСЯ ПРИНЯТЫМ — не переписывать
>
> | Что | Чем проверено вердиктом круга 10 |
> |---|---|
> | **семь блокирующих круга 6 и три важных** | закрыты кругом 7, подтверждены кругом 8; повторно не открываются |
> | **четыре блокирующих круга 8 и пять важных** | закрыты кругом 9 — каждое перемерено мной на `ec133b94e`, таблица выше |
> | **перебазировка §1 целиком** | сплошной прогон 87 команд: расхождений дерева **ноль**; единственная находка главы — негодный предикат (важное 2) |
> | **решение Б8.3 (адресация парой, второго глагола нет)** | принято по существу; естественный ключ его подтверждает, а не опровергает |
> | **арифметика `71 / 9 / 1 / 61`** | перемерена своим предикатом: заголовков **71**, дублей **0**, пропуски ровно `9` и `10`, анкер «санкция отозвана» строкой ниже заголовка — **9** (43-48, 63, 66, 67), «перенесён» — **1** (`49`), пересечение пусто. Контроль различает: сырой греп даёт **12** и **4**, позиционный — 9 и 1 |
> | **§2.1а как норма**, внешнее предусловие (§0.2 как предусловие, DoD п. 16), порядок `S3.1 → S3.2 → S3.3` | предметы реальны: `"mbr"` в каталоге **0** при контроле **3**; у соседа **`DRAFT`** |
> | **§5.0, §0.3, §3, §6, §9.2, §11, §12, §13, §15** | приняты прежними кругами, кругом 10 не пересматриваются |
> | **коды и тон отказов** | `412`/`422` в утверждениях — **0** (два вхождения в документе оба внутри вердиктов); четыре явные пары код+HTTP верны по таблице края (`INVALID_ARGUMENT`/400 ×2, `NOT_FOUND`/404 ×2); взаимоисключающих `oneOf` нет |
> | **публикуемость** | признак восстановимости прогнан по тексту круга 9. Разбор полосы `<exempt>` остался на уровне механизма («второй смысл; решение принимается не по личности вызывающего») — без координаты живого места, без условия отказавшей защиты и без следствия для постороннего; описываемое состояние к тому же не действующее. Сборки нет. Ban #14 тоже: маркеров отложенной работы в действующей части — **0** |
>
> ### Что делать
>
> `acceptance-author` закрывает **Б10.1** и **Б10.2** и семь важных. Правки §1 (кроме одного
> предиката), §1.9 (кроме двух дополнений), §2.1а, §5.0, §9.2, §9.3, §13, §15 и всех
> одиннадцати закрытых блокирующих вердиктом **не заказаны**. Ре-ревью — круг 11; перед
> подачей §1 перемерить по §1.0а (вершина уходит за смену, предикат тот же:
> `git rev-list --count ec133b94e..<вершина>` не ноль).
>
> ### Замечание соседу (§15.5 п. 4) — годится, но переписать: условие УЖЕ разрешимо, и владелец у решения ОДИН
>
> Круг 9 адресовал IAM-ID-2 условное: «если IAM-ID-2 заказывает ресурс членства с полным
> набором стандартных методов, отсутствие `Delete` обязано быть названо **там же**». Как
> замечание соседу оно законно — набор методов ресурса членства есть контракт той приёмки.
> Но в двух отношениях оно негодно, и оба чинятся здесь:
>
> 1. **условие уже разрешимо замером, и я его разрешил.** IAM-ID-2 `Delete` **не заказывает**:
>    `grep -c 'RemoveFromAccount\|member_remover'` по соседней приёмке → **8**, и снятие членства
>    там прямо названо судимым **тиром аккаунта** со ссылкой на посаженный глагол. Условное «если»
>    надо заменить фактом с этим предикатом — иначе замечание не связывает никого;
> 2. **«названо там же» приглашает ВТОРОЕ изложение одного предмета** — тот самый класс, ради
>    которого круг 9 и перенацеливал `65`. У решения один владелец — §1.9 этого документа;
>    сосед обязан **ссылаться**, а не пересказывать.
>
> **Предметом здесь оно становиться не должно** — с одной поправкой: место, где отступление
> обязано быть записано для исполнителя iam, находится в дереве продукта, а не в соседней
> приёмке (важное 5). Тогда сосед ссылается на страницу сервиса, а не на приёмку, которая
> закроется.
>
> ---
>
> ### Круг 11 (автор, 2026-08-24) — правки внесены, ВЕРДИКТА НЕТ
>
> *(Круг 12: действующее состояние документа — вердикт круга 12 в начале шапки; фраза ниже
> описывала положение на момент своей записи и вердиктом не является.)* Вердикт круга 10
> оставался действующим состоянием документа, пока
> `acceptance-reviewer` не поставил новый: автор вердикта не ставит. Что сделано — **§16**;
> коротко: **Б10.1** закрыт — восемь мест, называющих состав работ S3.1, приведены к одному
> утверждению (вердикт называл шесть; расхождение — в единице счёта, обе величины и способ
> отсева названы в §16.3). **Б10.2 закрыт вторым исходом, который вердикт назвал
> предпочтительным: конструируемость двух членств на одну строку личности ДОКАЗАНА посевом** —
> её строит обычное приглашение известной почты во второй аккаунт, и это утверждает посаженная
> проба `TestUserInvite_ExistingActiveEmail_SecondAccount`. Отсюда `65` конструируем на S3.1 и
> переносить его некуда, а утверждение «с S3.2 второе членство конструируемо», стоявшее в
> четырёх местах, опровергнуто замером и исправлено (§16.2). Семь важных закрыты; замечание
> соседу переписано фактом с предикатом (§16.5). §1 перемерен по норме §1.0а на
> `release/identity-4` @ `f58089a44` — расхождений с деревом **ноль** (§16.1).
>
> ---
>
> **Ниже — вердикт круга 8.** Его четыре блокирующих и пять важных закрыты кругом 9 и
> перепроверены выше; блок сохранён как история решений. Вердиктом он больше **не является**.
>
> **Круг 8 (история, `acceptance-reviewer`, 2026-08-24).** Действующей санкции нет; область
> правки сужена — см. «что остаётся принятым» круга 8: круг 7 закрыл все семь блокирующих
> круга 6. Ниже — вердикт круга 8; вердикт круга 6 сохранён следом как история, вместе с
> записью автора о судьбе санкции круга 5.
>
> **Ревизии замера круга 8** (числа ниже относятся к ним и ни к чему больше): воркспейс —
> `acc/iam-id-1-b3` @ `0cb8ad2`; продукт — `release/identity-4` @ `e4da590cf`.
>
> ---
>
> *(Круг 10: действующее состояние документа — вердикт круга 10 в начале шапки; абзац ниже
> описывает положение на момент своей записи и вердиктом не является.)*
>
> **Круг 9 (автор, 2026-08-24) — правки внесены, вердикта НЕТ.** Вердикт круга 8 остаётся
> действующим состоянием документа, пока `acceptance-reviewer` не поставит новый: автор
> вердикта не ставит. Что сделано — **§15**; коротко: четыре блокирующих закрыты **исходом**
> (Б8.1 полоса `List` приведена к `scope_filtered`; Б8.2 предмет класса 3 назван исчезнувшим
> и переписан на пять типизированных писателей; Б8.3 посаженный глагол
> `UserService.RemoveFromAccount` назван в новом §1.9, адресация парой **признана**, `65`
> перенацелен; Б8.4 поле объявлено снятым), пять важных закрыты, **§1 перебазирован целиком**
> на `release/identity-4` @ `475f8527` — перемерены **все 70** объявленных чисел, разошлось
> **12**, из них **3** найдены сверх перечня вердикта. Перечень всех 70 с обеими величинами —
> §15.1.
>
> ### Семь блокирующих круга 6 — ЗАКРЫТЫ, все семь, проверено замером
>
> | # | Чем закрыто | Чем я это проверил (не прочтением) |
> |---|---|---|
> | **Б1** | внешнее предусловие объявлено в §0.2 (таблица «сценарий → производитель»), §4 строка S3, §4 «что делает стадию production-complete», **DoD п. 16**; §9.1 строка S3 больше не обещает формы чтения здесь, четыре потребителя помечены `*` | предусловие реально: `sed -n '1,4p'` соседней приёмки → `Статус: DRAFT — круг 2`; гейт `check-01` читает у неё `DRAFT`. Зависимость существует и названа там, где её спросят |
> | **Б2** | заведён §2.1а: норма · признак · предикат с контролем · прецедент; предусловием назван в `Given` **64** и **65**; регистрация стоит шагом S3.1 | `git show e4da590cf:pkg/ids/ids.go \| grep -c '"mbr"'` → **0**; контроль `PrefixLimitHyphen` → **3**; миграция `470001` чеканит `mbr-…` и несёт `CHECK (id ~ '^mbr-…')`. Предмет реален, контроль различает |
> | **Б3** | требование заменено на действующее: `make -C deploy fga-model-embed` + две пробы дрейфа вшитой копии; в §2.3а — таблица «было объявлено → предмет → взамен» | конфигмапов модели в `deploy/` — **0**; `go:embed fga_model.fga` — **1**; обе пробы и цель сборки существуют (`deploy/Makefile:1241`). Заменённое исполнимо каждой командой |
> | **Б4** | `64` приведён к решению дерева: `NOT_FOUND` / `RESOURCE_NOT_FOUND`, отрицательный контроль полосы, выноска называет решение на **пути записи** | `PEER_*` в непробном Go `services/iam` — **0** (единственное вхождение под каталогом — кейс newman); по дереву полосу эмитят потребители чужих сервисов. Контракт-тон и код совпали с тем, что сервис уже отвечает у соседнего ресурса |
> | **Б5** | предупреждение §2.3а снято **как исполненное**, назначенный порядок S3.1 → S3.2 → S3.3 внесён таблицей; §4 и §9.1 приведены | текст порядка в §2.3а, §4 и §9.1 совпадает с назначенным вердиктом дословно; расхождения между тремя местами нет |
> | **Б6** | состояния разведены: остаток `66` вынесен в **действующий** IAM-ID-1-21 своими утверждениями; остаток `67` снят со ссылкой на держателя (IAM-ID-2-03/-13/-15 и -08/-21) | `grep -n 'IAM-ID-1-67'` → 4 вхождения, и **ни одно** не называет его живым производителем; группа F ссылается на IAM-ID-2-08/-21. В `21` требование стоит утверждением, а не долгом |
> | **Б7** | «все четыре глагола» → **три**; «4 типа выводят предка из колонки» → **три**, `iam.user` ушёл; звено цепи областей **названо** | блок `iam_user` в `fga_model.fga`: глаголов с `super_admin` — **3**, всего глаголов — **3** (контроль на `43d332ac` → **4**, предикат различает); `parentAccountExpr: "o.account_id"` — **3** типа, `iam.user` выражен `parentAccountsExpr` над членствами; `resource_scope_edge` в документе — **10** (было **1**), миграций — **4** файла |
>
> **Три важных круга 6 — тоже закрыты:** `55` утверждает наблюдаемое (проба браузером, отрицание
> в паре с положительным в том же прогоне, `// verifies` внутри пробы, `retries: 0`), а прежний
> предикат оставлен **сопутствующим**; §0.2 сужено до **S3 и S4**; строка S5 снята во **всех
> четырёх** таблицах §4.
>
> ### Арифметика — ПЕРЕМЕРЕНА МНОЙ, `71 / 9 / 1 / 61` верна, и третье состояние обязательно
>
> Перемерено своими предикатами с контролями в обе стороны:
>
> | Утверждение | Мой замер | Контроль (обязан НАХОДИТЬ) |
> |---|---:|---|
> | заголовков сценариев | **71** | дублей номеров — **0** |
> | множество = `1..73` без `9,10` | пусто | `seq 1 73 \| grep -vxE '9'` → находит пропуск `10` |
> | анкер «санкция отозвана» | **9** | все девять стоят **строкой ниже своего заголовка** (дельта 1), уникальных — 9 |
> | анкер «перенесён» | **1** | `49`, дельта 1 |
> | пересечение множеств | пусто | `comm -12 <(t) <(t)` → `IAM-ID-1-49` — команда различает, а не молчит всегда |
> | `71 − 9 − 1` | **61** | сходится с DoD п. 1 |
>
> **Записать `49` десятым «отозванным» было бы неверно, и это проверяемо, а не вкус.** У
> отозванного санкция снята потому, что реализовывать **нельзя**; у `49` предмет верен и
> реализуется — в другом документе, другим номером. Слив состояния, документ утверждал бы, что
> требование `ui.md` правила 12 к этой линии не предъявляется; оно предъявляется и здесь его
> несёт `55`. Снять `49` как номер (по образцу `09`/`10`) тоже нельзя: §9.3 сам называет
> причину — номер уезжает в документ, где у предмета **свои** номера, и освобождение здесь
> дало бы два предмета под одним идентификатором. Третье состояние — единственный исход,
> согласный с обоими правилами документа.
>
> ### Расхождение переписей `PEER_*` — принято, вывод от него действительно НЕ зависит
>
> Проверил обе стороны. Круг 6 назвал один набор сервисов, круг 7 — другой; мой третий
> предикат даёт третий. Причина в единице счёта: перепись **по токену** и перепись **по
> конструктору полосы** мерят разное, и обе истинны. Существенное совпадает у всех трёх:
> у `iam` — **ноль** в непробном Go, у потребителей чужих сервисов — не ноль. Подгонять было
> нечего, и круг 7 правильно не стал.
>
> ### §1 НЕ ПЕРЕБАЗИРОВАН — и круг 8 постановляет, что теперь это блокирует
>
> Круг 6 полной перебазировки не требовал, круг 7 честно сказал это вслух (§14.7 п. 1) и
> тронул только три места Б7. Претензии к кругу 7 в этом нет. Но вопрос «блокирует ли»
> адресован **вердикту**, и я на него отвечаю замером, а не осторожностью.
>
> **Дрейф измерен:** `git rev-list --count 43d332ac..e4da590cf` → **403** коммита, из них путей
> §1 касаются **218**. Я перемерил **40** предикатов §1 на `e4da590cf`: **11** изменили
> значение, ещё **1** не совпадал с документом **на обеих** ревизиях. То есть **12 из 40**
> утверждений главы «что уже верно в дереве» сегодня о дереве неверны.
>
> **Четыре из них меняют работу исполнителя** — это и есть блокирующие круга 8. Остальные
> восемь названы ниже как важные.
>
> ### Блокирующие круга 8 — четыре, все внутри одной причины
>
> **Б8.1. ДЕЙСТВУЮЩИЙ сценарий IAM-ID-1-21 утверждает о крае то, что перестало быть верным, и
> ошибка направлена в сторону снятой защиты.** §1.5 п. 3 и выноска `21` объявляют
> `UserService.List` освобождённым от пообъектной проверки края, а предикатом называют
> `blk List | grep -c '"<exempt>"'` → **1** с контролем `blk Invite` → **0**. Сегодня оба дают
> **0**: глагол несёт `permission = "iam.users.list"` и `scope_filtered = true` — сужение **на
> данных**, а не освобождение. Смена намеренная и объяснена в самом контракте; коммит —
> `91a04dc16 fix(security): шесть списков iam ушли из освобождения в сужение на данных`.
> **Почему это блокирует:** предикат перестал различать (оба конца дают ноль, §1.0 п. 2), а
> сценарий `21` — тот самый, в который круг 7 внёс остаток `66`, и он приземляется шагом S3.3.
> Исполнитель, читающий его, будет искать освобождение, не найдёт и либо вернёт его, либо
> остановится. Первое — откат чужой защитной работы.
> **Исход:** привести §1.5 п. 3 и выноску `21` к действующей полосе, предикат — к тому, что
> различает (`scope_filtered` у `List` против его отсутствия у соседнего глагола), и сказать,
> что перевыражение фильтра идёт **внутри этой полосы**, а не восстанавливает прежнюю.
>
> **Б8.2. §4 требует от стадии S3 работы, предмета у которой НЕТ — тот же класс, что Б3.**
> Строка «что делает стадию production-complete» требует: «оперативный скрипт восстановления
> приведён к новому смыслу отношения (§1.5а класс 3)». Скрипта в дереве нет: он **удалён**
> коммитом `c78901a33 [#944] Аккаунт человека читается из членства, а не из колонки строки`,
> то есть работой **этого же перехода**. Предикат класса 3 даёт **0**, и его контроль на
> соседнем объекте — тоже **0**: он больше не различает.
> **Почему это блокирует:** критерий завершённости, который нечем исполнить, — не критерий
> (дословная формулировка круга 6 в Б3), а стадия с таким критерием не может быть объявлена
> production-complete (ban #14). Ирония в том, что круг 4 записал этот класс с **обратной**
> стороны — «ложный ноль на живом потребителе»; сегодня ноль честный, а требование пережило
> свой предмет.
> **Исход:** класс 3 переписать как **исполненный** (с предикатом и коммитом), строку §4 снять
> либо заменить тем, что осталось предметом; §2.3а «сопутствующее» — привести (там сегодня
> «остаётся предметом `kacho#471`»).
>
> **Б8.3. Глагол снятия человека из аккаунта УЖЕ ПОСАЖЕН, и документ не знает о нём ни разу.**
> В дереве есть `UserService.RemoveFromAccount` (`#1127`): отношение `member_remover`, запись
> каталога с областью «аккаунт», `required_acr_min = "2"`, реализация
> `services/iam/internal/apps/kacho/api/user/remove_from_account.go`. Его собственный godoc
> называет предметом ровно то, что этот документ заказывает: человек перестаёт состоять в
> названном аккаунте, строка личности не трогается, порядок «сперва права, потом участие»
> держит отложенный триггер миграции **472002** — миграции стадии **S2 этого документа**.
> Предикат: `grep -c 'RemoveFromAccount\|member_remover'` по этой приёмке → **0**; по соседней
> (IAM-ID-2) → **4**, и она о нём рассуждает.
> **Почему это блокирует:** S3.1 заказывает «глаголы ресурса членства», а действующий
> IAM-ID-1-65 «Снятие членства» пиннит контракт снятия — при живом посаженном глаголе того же
> предмета. Исполнитель либо заведёт второй глагол снятия членства (два места об одном
> предмете), либо решит, что документ описывает другое дерево. Плюс `Given` у `65` требует
> предусловия §2.1а (префикс `mbr`), то есть предполагает адресацию **по идентификатору
> членства**, тогда как посаженный глагол адресуется парой «человек + аккаунт». Что из этого
> заказывается, а что уже сделано, обязан назвать документ, а не исполнитель в момент прогона.
> **Исход:** назвать посаженный глагол; вынести решение — остаётся ли id-адресуемое снятие
> отдельным предметом или `65` перенацеливается на посаженный; привести список работ S3.1 и
> `Given` у `65` к этому решению. Если id-адресуемое снятие остаётся — сказать, чем два глагола
> об одном предмете оправданы.
>
> **Б8.4. §1.5 строка 1 заказывает уже сделанное, и §1.5 расходится с §4 внутри документа.**
> `git grep -c 'string account_id = 6' -- proto/…/user.proto` → **0**: поле снято с контракта,
> `reserved 6` и `reserved "account_id"` объявлены. §4 это **знает** (таблица состояния, строка
> S4: `reserved` → 2), а §1.5 по-прежнему числит поле живым со «снять» в столбце исхода. Два
> места об одном предмете, из которых верно одно, — тот же класс, что круг 6 закрывал у строк
> «S3 НЕ НАЧАТА».
> **Исход:** §1.5 строку 1 привести; исход назвать исполненным с предикатом и ревизией.
>
> **И общий исход по четырём: §1 перебазировать ЦЕЛИКОМ**, как требует его же §1.0а. Довод не
> в педантизме, а в том, что адресный ремонт уже дважды оставлял класс: круг 3 назвал два
> места — круг 4 нашёл восемь; круг 6 назвал три — я нашёл ещё четыре. Перечень обязан
> называть **все** предикаты главы с обеими величинами (§12.1), а не только разошедшиеся:
> перечень одних находок неотличим от перечня, в котором остального не смотрели.
>
> ### Важное — не блокирует, но чинится тем же кругом
>
> 1. **§1.5а класс 5: было 2, стало 5**, и утверждающих мест теперь **два**, а не одно
>    (`iam-token-facade-conformance.py`). Радиус планового красного на S4 вырос вдвое — число
>    в документе занижает работу.
> 2. **§1.4 `ListAccountsForUser`: 20 → 29** (все вхождения) и **6 → 8** (непробные). Обе
>    единицы названы в документе честно, но обе устарели.
> 3. **§1.1 положительный контроль неверен НА ОБЕИХ ревизиях:**
>    `grep -c 'externalId is immutable after User.Create'` → **3**, а не 2 (третье вхождение —
>    комментарий шапки). Контроль остаётся годным по существу (ненулевой), но число ложно с
>    круга 4.
> 4. **§9.2, выноска про сироту, называет предикат полноты как «множество = 1..72»**, тогда как
>    §9.3 — `1..73`. Два места об одном предикате; §9.2 вердиктом круга 6 принят, поэтому это
>    названо важным, а не блокирующим.
> 5. **Отрицательный контроль в `64` приписан не той команде.** Скобка перечисляет
>    `compute · nlb · registry · vpc` — это результат переписи **по конструктору полосы**, а
>    «та же команда» (греп по токену) по дереву даёт другой набор. Либо назвать предикат,
>    которым получен перечень, либо написать «среди них». Вывод сценария от этого не меняется.
>
> ### Что ОСТАЁТСЯ ПРИНЯТЫМ — не переписывать
>
> Область сужена намеренно: ниже то, что круг 8 **не заказывает** трогать.
>
> | Что | Чем проверено вердиктом круга 8 |
> |---|---|
> | **все семь блокирующих круга 6 и три важных** | закрыты, каждое перемерено — таблица выше. Повторно их не открывать |
> | **арифметика `71 / 9 / 1 / 61` и третье состояние** | перемерена своими предикатами с контролями в обе стороны; анкеры не пересекаются, контроль умеет находить пересечение |
> | **§2.1а целиком** (префикс `mbr`) | норма, признак, контроль и прецедент верны на дереве |
> | **§0.2, DoD п. 16, §4 строка S3, §9.1 — внешнее предусловие** | форма верна, предмет реален: у соседа `DRAFT` |
> | **порядок S3.1 → S3.2 → S3.3** | назначен вердиктом круга 6, внесён дословно в трёх местах |
> | **§5.0, §0.3, §9.2, §3, §6, §11, §12, §13** | приняты кругом 6, кругом 8 не пересматриваются |
> | **коды и тон отказов** | пар код+HTTP — шесть площадок, все верны (`INVALID_ARGUMENT`/400, `NOT_FOUND`/404); `412`/`422` в утверждениях — **0**; взаимоисключающих `oneOf` нет |
> | **публикуемость** | признак восстановимости прогнан по тексту круга 7: сборок «координата + условие, при котором защита не действовала + следствие для постороннего» **не заведено**; §2.1а, выноска `64` и DoD п. 16 говорят о собственных решениях и каталогах |
>
> **Что делать:** `acceptance-author` закрывает Б8.1-Б8.4 и пять важных, перебазирует §1
> целиком по норме §1.0а (перечень **всех** предикатов с обеими величинами — в §12.1 либо в
> своём разделе круга 8), ре-ревью — круг 9. Правки §2.1а, §5.0, §9.3, §13 и семи закрытых
> блокирующих вердиктом **не** заказаны.
>
> ---
>
> **Ниже — вердикт круга 6.** Все семь его блокирующих закрыты кругом 7 и перепроверены выше;
> блок сохранён как история решений (порядок S3, судьба S5, перенос `49`) — на него ссылаются
> §2.3а, §4 и §9.1. Вердиктом он больше **не является**.
>
> ### Пять вопросов круга 6 — исход по каждому
>
> | # | Вопрос | Решение вердикта |
> |---|---|---|
> | 1 | вердикт круга 6 | **CHANGES REQUESTED**, семь блокирующих; область правки сужена — см. «что остаётся принятым» |
> | 2 | порядок внутри S3 (`kacho-workspace#357`) | **назначен вердиктом** (ниже). Гипотеза документа принята **частью**: её последний элемент верен, средний называет отозванное |
> | 3 | судьба стадии S5 | **снимается как стадия этого документа.** Сценарии 43-48 остаются текстом с записью об отзыве, номера в обращении |
> | 4 | IAM-ID-1-49 | **переносится в IAM-ID-2 вслед за фиксом** (к IAM-ID-2-22); здесь снимается с записью о переезде. Взамен наблюдаемое утверждает IAM-ID-1-55 |
> | 5 | §1 не перебазирован | **не согласен, что это не влияет.** Полной перебазировки не требую; блокируют **три** места, описывающие радиус S3 |
>
> ### Решение по вопросу 2 — порядок внутри S3, ЗАМЕНЯЕТ объявленный §2.3а
>
> Порядок назначается вердиктом, а не автором: смена порядка стадии меняет санкционируемую
> работу. Он выведен из трёх ограничений, каждое **измерено**, а не принято на веру:
>
> 1. **звено `iam_user → account` уже берётся из членства** — миграция
>    `944001_identity_scope_comes_from_membership.sql` посажена и переписала ветвь (4a)
>    представления цепи областей. Следствие: `super_admin: admin from account` резолвится
>    через **каждое** членство человека, и колонка тут больше ни при чём;
> 2. **поэтому снятие `admin from account` в одиночку отнимает доступ** у владельца аккаунта и
>    делегированного распорядителя — путь-замена ещё не существует. Это и есть дельта, которую
>    `#357` измерил прогоном (я её сам не прогонял и на этом не настаиваю: вывод держится
>    п. 1, а не чужим числом);
> 3. **запись каталога прав на тип «членство» требует RPC, чей `scope_extractor` на него
>    указывает** — обратный гейт `internal/repohygiene` `TestEmptyPageNeverPrecedes…`-соседа
>    `hideexistence_test.go:59` объявляет находкой строку таблицы форм ответа края, до которой
>    не дотягивается ни одна запись каталога. Значит «сменить `object_type` первым шагом»
>    неисполнимо by construction.
>
> **Назначенный порядок:**
>
> | шаг | что приземляется | почему не раньше и не позже |
> |---|---|---|
> | **S3.1** — **одним изменением** | тип членства в модели прав · перевыраженный источник каскада для личности · **префикс `mbr` в каталоге дефис-префиксов платформы** · глаголы ресурса членства · записи каталога прав на новый тип · строка таблицы форм ответа края | модель и её замена обязаны приземлиться **вместе** (огр. 2); запись каталога и строка края возможны только при живом RPC (огр. 3). Контроль шага — дельта доступа до/после **пуста**, пока у каждого одно членство: это **положительный контроль**, а не признак завершённости |
> | **S3.2** | переезд глагола приглашения на ресурс членства | с этого момента второе членство конструируемо, и IAM-ID-1-31/-57 исполнимы **без синтетики**. Раньше S3.1 — запрещено: окно, в котором членств больше одного, а доступ к личности разрешается через любое из них |
> | **S3.3** | читатели: фильтр `ListUsers` **внутри сервиса**, консоль, провайдер инфраструктуры; `User.account_id` продолжает заполняться | читатель, переведённый до существования источника, читает пустоту; колонка жива до S4 (откат S3 полон, IAM-ID-1-51) |
>
> **Что из гипотезы документа отвергнуто и почему.** «читатели → публичная поверхность
> членства (`kacho#470`) → объект прав и глагол»: последний элемент верен и вошёл в S3.1;
> средний — **публичная поверхность чтения** — этим документом больше не заказывается (санкция
> отозвана 2026-08-24), поэтому шагом стадии быть не может. Первый элемент исполним лишь
> частью: перевыражение фильтра `ListUsers` внутри сервиса — да, консоль — нет, она ждёт
> источника.
>
> **Этим закрывается предикат снятия `kacho-workspace#357`** в части порядка. Сопутствующее,
> названное там же, закрывается блокирующими Б3 и Б7 ниже.
>
> ### Блокирующие — семь, каждое с предикатом
>
> **Б1. Четыре ДЕЙСТВУЮЩИХ сценария неисполнимы средствами, которые этот документ
> санкционирует.** `01`, `55`, `68`, `73` требуют форм чтения членства, чей контракт и
> производитель принадлежат **IAM-ID-2**, а он сам в состоянии `DRAFT`: `73` — «свой список»
> (производитель назван в его же тексте: IAM-ID-2-07); `55` — «список членств названного
> аккаунта»; `68` — записи каталога **на обе** формы; `01` — оба чтения. DoD п. 1 требует
> исполнимого производителя для всех 62 — для этих четырёх его нет, и ban #14 нарушен:
> стадия не production-complete **в своих границах**, её завершение зависит от вердикта
> другого документа. Зеркало того же расхождения: §9.1, строка S3, по-прежнему утверждает
> «**обе** формы чтения членства появляются здесь же» — прямо против §0.2 и шапки.
> **Исход:** объявить внешнее предусловие явно — S3 не закрывается раньше APPROVED IAM-ID-2 —
> в §4 (строка S3), §0.2 и отдельным пунктом DoD; §9.1 привести к нему. Перевыражать сценарии
> «в обход» IAM-ID-2 **не** требую: их предмет и есть публичная форма.
> Предикат: перечень действующих сценариев, чей текст опирается на формы чтения, — **4** из 8
> кандидатов после адъюдикации (`08`, `64`, `42`, `49` — упоминание, не зависимость).
>
> **Б2. Префикс `mbr` не стоит в каталоге дефис-префиксов платформы — и условие названо только
> в ОТОЗВАННОМ сценарии.** `grep -c '"mbr"' pkg/ids/ids.go` → **0**, при том что миграция S1
> уже чеканит такие идентификаторы (`CHECK (id ~ '^mbr-…')`, `470001`). `corevalidate.ResourceID`
> судит по членству первого сегмента в этом каталоге, поэтому **корректный** идентификатор
> членства отвергается `INVALID_ARGUMENT` — ровно тот класс, который каталог оговаривает у
> себя для предела. Действующие `64` («чтение собственного несуществующего членства даёт
> `NOT_FOUND`») и `65` («снятие несуществующего членства даёт `NOT_FOUND`, а не `INTERNAL`»)
> неконструируемы. В документе условие названо **один раз** (`grep -c 'mbr'` → 1) — внутри
> отозванного `63` и адресовано IAM-ID-2-04.
> **Исход:** внести регистрацию префикса шагом S3.1 (уже сделано назначенным порядком) и
> назвать её предусловием `64`/`65`, а не соседнего документа.
>
> **Б3. DoD п. 13 и §2.3а требуют гейта, предмета у которого нет.** «гейт дрейфа конфигмапа
> чарта из модели прав» и «применение мятит новый идентификатор версии модели и перекатывает
> сервисы»: конфигмапов модели в чартах — `git ls-files deploy/ | grep -ci -E 'fga|authz.*model'`
> → **0**; посадка внешнего движка снята целиком (`deploy/r7_3_32_engine_landing_retired_test.go`);
> модель **вшита** (`//go:embed fga_model.fga`), а дрейф держат `make -C deploy fga-model-embed`
> и пробы `authzmap/fga_model_drift_test.go` · `authzmodel/identity_test.go`.
> **Исход:** назвать действующий механизм. Пункт DoD, который нечем исполнить, — не критерий.
>
> **Б4. IAM-ID-1-64 назначает внутрисервисной ссылке ЧУЖУЮ полосу — и дерево уже решило этот
> вопрос ЯВНО, в обратную сторону.** Сценарий требует `FAILED_PRECONDITION` + `reason`-токен
> полосы «чужой ресурс отсутствует», обосновывая это тем, что «аккаунт резолвится у своего
> владельца». Владелец аккаунта — **сам iam**: аккаунт и членство лежат в одной базе одного
> сервиса, никакого peer-вызова здесь нет.
>
> Решение в дереве принято, прокомментировано и стоит на **пути записи** —
> `services/iam/internal/repo/kacho/pg/limit_repo.go`, ветвь 23503 у `mapLimitErr`, дословно:
> «общий маппер читает 23503 как `FAILED_PRECONDITION`, что верно для настоящего внешнего
> ключа (предусловие соседа). **Здесь соседа нет: аккаунты и проекты — собственные строки
> iam, значит полоса прямого чтения и ответ — `NOT_FOUND`**». Тот же выбор повторён на пути
> чтения (`api/limit/usecases.go`, «Direct-read lane: accounts and projects are iam's OWN
> rows»). То есть у формы «ресурс iam называет несуществующий аккаунт» **уже** есть
> контрактный ответ, и он — `NOT_FOUND` / `RESOURCE_NOT_FOUND`.
>
> Замер, отделяющий полосу от помощника-носителя координаты: `PEER_*`-токенов в непробном Go
> `services/iam` — **0** (единственное вхождение под каталогом — комментарий в кейсе newman);
> по дереву `PEER_*` эмитят только потребители **чужих** сервисов (`nlb`, `registry`,
> `storage` → `geo`). `kerrors.PeerRef` полосы не объявляет — его godoc называет его
> «координатой ресурса, о котором отказ», и iam пользуется им **вместе с**
> `ReasonResourceNotFound`.
> **Исход:** привести `64` к решению дерева либо, если членство обосновывает отступление,
> объявить это отступление решением со ссылкой отсюда. Сценарий в нынешней редакции пиннит
> контракт, противоречащий действующему у того же сервиса, — и проба его закрепит.
>
> **Б5. Порядок S3 внесён не автором, а вердиктом** — §2.3а, §4 (строка S3) и §9.1 привести к
> решению по вопросу 2 выше; предупреждение §2.3а снять как исполненное.
>
> **Б6. Счёт смешивает «санкция отозвана» с «частью остаётся предметом здесь».** `66` объявлен
> отозванным, а его тело говорит: «Часть про `ListUsers` … **остаётся предметом этого
> документа**»; `67` объявлен отозванным, а его тело говорит: «Здесь остаётся требование к их
> **паре**, которого ни один из производителей по отдельности не несёт» — и группа F ссылается
> на `67` как на **живого** производителя («свой список остаётся … IAM-ID-1-67»). При этом
> `66` — **единственный** производитель требования о порядке «формат раньше замыкания» для
> `ListUsers` (замер: сценариев, называющих `ListUsers`, — 2, из них по существу один), а
> `ListUsers` этот документ перевыражает в S3. Два места об одном предмете, из которых верно
> одно; исполнителю нечем выбрать.
> **Исход:** развести состояния. Остаточное требование либо выносится в действующий сценарий
> (своим номером), либо снимается явно со ссылкой на держателя. Арифметика 71/9/62 при этом
> **верна и не переписывается** — переписывается принадлежность остатка.
>
> **Б7. Три места §1/§2.3а, описывающие радиус S3, разошлись с деревом.** Полной перебазировки
> §1 не требую — требую эти три, потому что по ним исполнитель считает объём работы:
> · §2.3а «с `super_admin` объединены **все четыре** глагола типа личности» — сегодня их
> **три** (`v_update` снят `#1128`); замер по трём ссылкам: `43d332ac` → 4, `origin/main` → 3,
> `release/identity-4` → 3;
> · §1.5а класс 6 «**4** типа выводят предка из колонки» — сегодня **3**
> (`iam.project`, `iam.group`, `iam.serviceAccount`); `iam.user` **уже переведён** миграцией
> `944001`, то есть S3 заказывает **уже сделанное** — тот же класс, что круг 6 чинил у строк
> «S3 НЕ НАЧАТА»;
> · **звено цепи областей не названо в документе ни разу** (`grep -c 'resource_scope_edge'`
> → **0**), хотя это и есть решающий производитель факта «личность принадлежит аккаунту» и
> предмет посаженной миграции S3.
>
> ### Важное — не блокирует, учесть тем же кругом
>
> 1. **IAM-ID-1-55 обязан утверждать наблюдаемое.** После решения по вопросу 4 это
>    **единственная** консольная правка, которую заказывает этот документ; её `Then` сегодня
>    держится предикатом `listUsers({ account_id }) → 0`, то есть отсутствием вызова, а не тем,
>    что видит человек (`ui.md` правило 12, `e2e-flow.md` §9 D4).
> 2. **§0.2 «Разрешает: S3, S4, S5»** — после решений 1 и 3 читается как позволение на снятое.
>    Автор оставил абзац намеренно, ожидая вердикта; вердикт есть: **S3 и S4**.
> 3. **§4, соответствие шагам документа решения** — строка S5 снимается вместе со стадией.
>
> ### Что ОСТАЁТСЯ ПРИНЯТЫМ — не переписывать
>
> Область правки сужена намеренно: ниже перечислено то, что круг 7 **не трогает**, иначе
> преемник перепишет верное.
>
> | Что | Чем проверено вердиктом |
> |---|---|
> | **арифметика §9.3 — 71 объявлено / 9 с отозванной санкцией / 62 действующих** | перемерено своим предикатом с контролями в обе стороны: заголовков **71**, дублей **0**, множество = `1..73` без `9,10` (диффом — пусто; контроль находит пропуск `10`), меток отзыва **9** поимённо, разбивка по группам обходом файла — `12·7·5·13·15·7·7·5 = 71` |
> | **§9.2 — сироты и призраки** | `71` объявлено = `71` назначено; обе стороны пусты; контроль находит сироту `59` |
> | **§5.0 целиком** — две формы чтения как предусловие главы, страница+пообъектная проверка вместо перечисления вселенной, окно утверждения (повтор законен на появлении доступа, запрещён на его исчезновении) | принято без замечаний; п. 0 и п. B — сильнейшая часть документа |
> | **§0.3 — признание влитого задним числом** и четыре довода | принято; сужение предиката до двух файлов (значение **3**) верно, дрейф 8 → 16 назван честно |
> | **решение по `kacho#1085` и его отражение** — шапка, группа F, §13.1 | принято, включая **отказ подгонять** 34 → 43 и три названных числа с единицей у каждого |
> | **§4, таблица фактического состояния стадий** (S2/S3/S4 — «исполнена частью») | перемерена кругом 6, предикаты воспроизводимы |
> | **коды и тон отказов** | `412`/`422` в документе — **0**; три явные пары код+HTTP верны (`INVALID_ARGUMENT`/400 ×3, `NOT_FOUND`/404); взаимоисключающих `oneOf` нет |
> | **публикуемость** | признак восстановимости прогнан по тексту: сборок «координата + условие, при котором защита не действовала + следствие для постороннего» — **нет**. Единственный адъюдицированный кандидат — §1.4 (слитое условие запасного пути): следствие там — привязка, выданная не на каноническую строку, то есть свойство целостности, а не путь, которым посторонний чем-то пользуется. Оговорка остаётся |
>
> **Что делать:** `acceptance-author` закрывает Б1-Б7 и три важных, ре-ревью — круг 7. Правки
> §1 сверх трёх мест Б7, §3, §6, §11, §12 вердиктом **не** заказаны.
>
> ---
>
> **Ниже — запись АВТОРА о судьбе санкции круга 5.** Она сохранена как свидетельство о том,
> что произошло, и вердиктом не является: вердикт — блок выше.

> [!important] ДВА СОБЫТИЯ, И ОНИ РАЗНЫЕ — одно другого не заменяет
> | когда | что произошло | где это видно |
> |---|---|---|
> | **2026-08-23** | ре-ревью круга 6 вернуло документ автору с **суженной областью**; правки внесены автором в тот же день — ими переписаны §0.2, §4, §5 и §13 | §13 этого документа |
> | **2026-08-24** | решением по `PRO-Robotech/kacho#1085` **отозвана часть санкции круга 5**: перечень членств сужается до аккаунта, о котором спрашивают | ведомость продукта (ниже) · `docs/specs/sub-phase-IAM-ID-2-membership-read-acceptance.md` §2.0 |
>
> **Действующего вердикта нет ни у одной части, и это не формальность.** Вердикт круга 5
> выносился документу **целиком**; носитель, которым он переносится в продукт, несёт **один**
> вердикт на документ и сегодня несёт `DRAFT` (предикат ниже). Поэтому формулировка «у данных
> APPROVED сохраняется» неисполнима: раздельного вердикта не существует ни в одном носителе,
> и гейту нечем его прочитать.
>
> **Это НЕ отменяет второго — верного — утверждения:** директива владельца касается **чтения**,
> а не **хранения**, поэтому предмет S1, S2 и данные-частей S3/S4 круг 6 по существу не
> пересматривает: строка человека, таблица членств, глобальный ключ идентичности и звено
> области из членства ею не затронуты. «Что пересматривается по существу» и «что сегодня
> разрешено» — разные вопросы, и смешение их и породило формулировку выше.
>
> [!important] ЧТО ИМЕННО ОТОЗВАНО, ЧЬИМ РЕШЕНИЕМ И ПОЧЕМУ
> **Решение:** `PRO-Robotech/kacho#1085`, комментарий оркестратора 2026-08-23. Две приёмки
> об одном предмете санкционировали противоположное, и обе стояли APPROVED. Принято
> **сужение**: перечень членств, отдаваемый по аккаунт-скоупному пути, содержит членство
> **только в том аккаунте, о котором спрашивают**. Надзор облака видит всё; распорядитель
> аккаунта — своё. Полное изложение решения и его последствия для контракта —
> `docs/specs/sub-phase-IAM-ID-2-membership-read-acceptance.md` §2.0; здесь оно не
> пересказывается, иначе заведутся два места об одном предмете — ровно тот класс, из-за
> которого отзыв и понадобился.
>
> **Отозвано** (санкция снята, предмет переходит в круг 6):
>
> | что | где в этом документе | почему |
> |---|---|---|
> | перечень **всех** членств человека на его ресурсе, отдаваемый **по умолчанию** обоими чтениями | IAM-ID-1-43 | человек — глобальная личность и может состоять в аккаунтах разных организаций; полный перечень, отданный распорядителю одного из них, сообщает **о существовании остальных** |
> | столбец «аккаунты человека» на странице пользователей и полный перечень на его карточке | IAM-ID-1-44, -45, -46, -47, -48 | та же величина, показанная оператору: карточка сотрудника моего аккаунта перечисляла бы его связи с чужими организациями |
> | плоская поверхность **чтения** членства (`GET /iam/v1/memberships`, `?userId=`, `/{id}`) | IAM-ID-1-01 (последнее **And**), -63, -66, -67 | вопрос «в каких аккаунтах состоит этот человек» задаётся такой формой напрямую, а по идентификатору — ещё и арифметикой; заменяется двумя чтениями IAM-ID-2 §2.1 |
>
> **IAM-ID-1-49 НЕ отозван**, хотя стоит в той же группе F: его предмет — **порядок работ**
> (проба браузером написана до фикса и падает на дефекте, `ui.md` правило 12), а не величина,
> которую продукт отдаёт. Так это и записано в IAM-ID-2 §2.6, таблица Б; здесь номер назван
> явно, чтобы «снята группа» не прочиталось как «снято семь номеров».
>
> *Круг 7: отзыва у него по-прежнему нет — он **ПЕРЕНЕСЁН** в IAM-ID-2 (к IAM-ID-2-22)
> вердиктом круга 6, вопрос 4. Состояние третье и считается отдельно (§9.3); производителя
> этот документ по нему не заказывает, а требование `ui.md` правила 12 несёт IAM-ID-1-55.*
>
> **Почему это отзыв, а не уточнение.** Речь не о формулировке, а о величине, которую
> продукт отдаёт: перечень всех членств против членства в названном аккаунте. Разница
> наблюдаема арендатором и невыразима правкой текста сценария.
>
> **Четыре довода, по которым решён спор** (изложены в IAM-ID-2 §2.0, здесь — только их
> имена, чтобы отзыв читался без второго документа): глобальность личности · тот же класс,
> что запрет инфраструктурных данных на публичной поверхности, но картируется состав
> арендаторов · анти-оракул «состоит ли человек в конкретном чужом аккаунте» ·
> несимметричная цена (безопасность выигрывает много, удобство теряет мало — спрашивающему
> нужен ответ про **свой** аккаунт).
>
> **НЕ отозвано и остаётся предметом круга 6 в прежнем виде:** глобальность строки человека
> (одна строка на все аккаунты) · сужение распоряжения личностью (круг = сам человек и
> надзор облака) · отложенный триггер «сперва права, потом участие» · стадии S1-S4 и всё,
> что уже посажено (применённую миграцию править нельзя, ban #5).
>
> **Указание владельца, из-за которого круг 6 состоялся** (2026-08-23), дословно:
>
> > «человек может состоять в n аккаунтах и членство определено инвайтом. При этом отображать
> > всем в каких аккаунтах он приглашён неправильно, при этом приглашённый в своём списке
> > может увидеть где он приглашён».
>
> **Форм чтения членства ДВЕ, и третьей нет** (контракт обеих — IAM-ID-2 §2.1; здесь они
> названы затем, что служат предусловием сценариев §5, а не переизлагаются):
>
> 1. **свой список** — субъект спрашивает про себя; параметра пользователя у запроса **нет
>    вовсе**, субъект берётся из контекста вызова. Приглашённый видит здесь, куда он приглашён;
> 2. **членство в НАЗВАННОМ аккаунте** — спрашивают про аккаунт, права проверяются **на нём**.
>
> **Форма «в каких аккаунтах состоит НАЗВАННЫЙ человек» — запрещена.** Именно её документ
> строил до круга 6. Ре-ревью назвало **34** места; перепись по названной единице даёт **43** —
> **15** сценариев (сходится с вердиктом точно) и **28** не-сценарных мест. Перечень с исходом
> по каждому и разбор расхождения — §13.1; расхождение **не подогнано**.
>
> **Это ровно тот класс, который документ ловит сам** (§7, В-8): «правка решения не закончена,
> пока не убрано умолчание, которое оно отменяет». Отменённое указание, оставленное в тексте,
> читается подряд и исполняется — тем вернее, чем аккуратнее выписано вокруг него всё
> остальное.
>
> [!warning] Парная запись в ДРУГОМ дереве: она СДЕЛАНА в релизной линии и НЕ ДОЕХАЛА до
> ствола продукта — перемерено 2026-08-24
> Вердикт переносится в продукт ведомостью `docs/acceptance-ledger.yaml`, и её состояние
> сегодня **разное на двух ссылках**:
>
> | ссылка продукта | что несёт запись `IAM-ID-1` |
> |---|---|
> | `release/identity-4` (`e315e43f5`) | `verdict: DRAFT`, `verdict_withdrawn: true`, `debt_issue: 1085`, `workspace_revision: 3d47901` — приведена **отдельной работой** |
> | `origin/main` (`7bb0a6e14`) | `verdict: APPROVED`, `verdict_dated: "2026-08-21"`, `workspace_revision: 5d6a2d6` |
>
> Предикат (повторяемый, по любой ссылке):
> `git show <ссылка>:docs/acceptance-ledger.yaml | grep -A4 'prefix: IAM-ID-1$'`.
>
> **Что из этого следует.** Пока запись ствола печатает `APPROVED`, гейт продукта
> `TestNewMigrationCitesAnApprovedAcceptance` считает вердикт действующим — то есть отзыв
> станет фактом для гейта ровно тогда, когда релизная линия сядет в ствол, и не раньше.
> Прежняя редакция этой врезки требовала работы, которая уже сделана; здесь она **не
> удалена**, а перемерена: «сделано в линии» и «действует в стволе» — разные состояния, и
> первое второго не заменяет.
>
> **Запрет кодирования без вердикта держится ГЕЙТОМ, а не вниманием** (заведено и
> закрыто `PRO-Robotech/kacho#938`). Приёмки живут в этом репозитории, миграции — в
> продукте, поэтому гейт продукта прочитать вердикт не может по построению: он
> переносится ведомостью `docs/acceptance-ledger.yaml` (документ · префикс сценариев ·
> вердикт · дата · ревизия воркспейса, на которой прочитан), а `internal/repohygiene`
> требует APPROVED от приёмки, которую цитирует миграция, **добавленная изменением**.
> Уже стоящие в стволе не судятся: момент, который проверяет ban #1, для них прошёл,
> а применённую миграцию править нельзя (ban #5).
>
> Именно на этом документе класс и проявился: S0, S1 и часть S2 сели в ствол **до**
> вердикта и признаны §0.3 задним числом — поймать это было нечему.
>
> **Разрешено было (круг 5) и сужается кругом 6 до:** стадии **S3 и S4** в объявленном §4
> порядке, строгим TDD (ban #12), по сценариям §5 — то есть ровно §0.2 и ничего сверх.
> **С S5 санкция отозвана**: её предмет — консольная поверхность членств — принадлежит приёмке
> `docs/specs/sub-phase-IAM-ID-2-membership-read-acceptance.md` (§2.6 таблица Б, §5 группа F5).
> Разрешением этот абзац сегодня не является: действующего вердикта нет (шапка).
> S0 и S1 исполнены; S2, S3 и S4 исполнены **частью** — фактическое состояние каждой с
> предикатом даёт таблица §4, перемеренная кругом 6.
>
> **Ревизии вердикта — две, потому что §1 сверялся на обеих:** якорная `43d332ac` (на ней
> написан круг 4) и вершина ствола `f7a494478` на момент вердикта. **Все 47 предикатов §1
> дают на них одинаковое значение — расхождений ноль**, то есть §1 не устарел повторно
> (между ними влит один коммит, `#933`, не задевающий ни одного пути главы). Норма §1.0а
> исполнена по существу.
>
> **Замер вердикта:** исполнено **76** различных предикатов документа (около 170 запусков —
> каждый прогонялся на нескольких из четырёх названных ревизий). **Сошлось 73, разошлось 3.**
> Замер вёлся на полном клоне (`git rev-parse --is-shallow-repository` → `false`).
>
> **Три блокирующих замечания круга 4 закрыты и проверены независимым замером:** Б1 — новый
> предикат класса 3 даёт 1 на всех четырёх ревизиях, прежний 1/1/1/0; Б2 — обе половины
> подтверждены порознь (Go-функция без единого прод-вызывающего · действующий производитель
> `parentAccountExpr` над таблицей людей, четыре записи плана); Б3 — §0.2 переформулирован,
> таблица §4 описывает дерево по всем шести стадиям, §0.3 называет нарушение ban #1 прямо.
> Семь важных (В1-В7) — закрыты.
>
> **Шесть замечаний круга 5, ВСЕ не блокирующие** (закрываются попутно, отдельного круга не
> требуют; полный текст — в вердикте круга 5):
> 1. §1.1, положительный контроль неизменяемости: заявлено **2**, дерево даёт **3** на всех
>    четырёх ревизиях — число никогда не было верным. Закрывает: `3` и «две формы ключа маски
>    + строка godoc».
> 2. §12.1 объявлен перечнем **ВСЕХ** предикатов §1 «целиком, а не по находкам», фактически
>    не несёт **14** из них — включая `parentAccountExpr`, на котором стоит весь довод Б2, и
>    включая единственный предикат с неверным числом (зам. 1). Закрывает: дописать строки.
> 3. Признак нарушения §1.0а меряет **расстояние в коммитах**, а не дрейф предикатов:
>    сработал и сейчас, при нулевом расхождении. Закрывает: признаком объявить сравнение
>    вывода предикатов на двух ревизиях — механизм существует и дёшев.
> 4. §6 п. 9: контрольные числа (**2** и **8**) верны для базовой ревизии и дрейфнули на
>    якорной (**3** и **9**). Норма §1.0а покрывает только §1, тогда как измеренные числа
>    документ несёт также в §2.3а, §6 и §9. Закрывает: расширить охват нормы.
> 5. §0.3 «Предмет заведён отдельно» о гейте ban #1 — **не подтверждается**: задачи нет ни в
>    продукте, ни в воркспейсе (просмотрено 454). §12.4 говорит по существу обратное.
>    Закрывает: завести задачу и назвать номер **либо** привести формулировку к §12.4.
> 6. §12.2: у трёх классов есть признак и противоядие, нет «чем держится»; класс «неполный
>    клон» не имеет дома в корпусе вовсе. Закрывает: перенести классы в корпус.
>
> **Отсутствие гейта на ban #1 APPROVED не блокирует** — довод автора принят: заводить его
> этим документом значило бы смешивать предметы, а §12.4 называет пропуск прямо, не обещая
> его. Обязательным остаётся зам. 5: у названного предмета должен быть владелец.
>
> Круг 4 — ❌ CHANGES REQUESTED, 3 блокирующих + 7 важных, закрыто **10 из 10**; разбор — §12.
> APPROVED круга 2 (2026-08-16, `acceptance-reviewer`, ревизия `bc25699`) **снят автором**:
> из документа сняты два сценария, одна стадия сменила предмет, и это не текстовая правка.
> Вердикт ставит `acceptance-reviewer` и только он (ban #1).
> Круг 1 — ❌ CHANGES REQUESTED, 20 замечаний (8 блокирующих), закрыто **20 из 20**; разбор — §10.
> **Дата:** написан 2026-08-16 · круги 1 и 2 — 2026-08-16 · круг 3 — 2026-08-18 ·
> круг 4 — 2026-08-21 · **круг 5 (вердикт APPROVED) — 2026-08-21**.
>
> **Что изменил круг 4 — §1 перебазирован на текущий ствол.** Круг 3 осознанно этого не делал
> (§11.4), назвав отдельной работой. Допущение было верным на момент записи и перестало быть
> верным **через четырнадцать часов**: за три дня, что документ пролежал непрочитанным, четыре
> его предиката разошлись с деревом, и один из них давал **ложный ноль на живом потребителе**
> безопасности. Отсюда норма §1.0а: §1 перемеряется перед подачей на вердикт, и ревизия замера
> называется. Полный разбор — §12; перечень расхождений — §12.1.
>
> **Работа, которую документ санкционирует, ЧАСТЬЮ УЖЕ В СТВОЛЕ.** S1 реализована полностью,
> S2 частью — и посажены **до** вердикта, то есть гейт ban #1 был обойдён. Круг 4 признаёт их
> явно и задним числом, не откатывая; причина, цена и предикат каждой стадии — **§0.3**.
>
> **Круг 6: то же верно теперь и для S3, и для S4** — обе исполнены **частью**, и это
> перемерено, а не предположено: `944001_identity_scope_comes_from_membership.sql` (S3),
> `20260823050000_users_identity_uniqueness_goes_global.sql` и
> `20260823053000_first_login_activates_every_membership.sql` (обе объявляют себя «Стадия
> S4-expand»), плюс снятие `User.account_id` с контракта с резервированием номера и имени.
> До круга 6 §4 утверждал про обе «**НЕ НАЧАТА**» — утверждение пережило свой предмет за двое
> суток и читалось как указание начать заново то, что стоит в стволе. Предикаты — §4.
>
> **Что изменил круг 3 и почему — `PRO-Robotech/kacho#616`.** Два утверждения документа о
> дереве оказались ложными, и **каждое стоило исполнителю прогона**. Разбор — §11; здесь —
> перечень:
> 1. **сняты IAM-ID-1-09 и IAM-ID-1-10** (инвариант «владелец состоит в своём аккаунте»):
>    производителя у них нет, и решение владельца по `PRO-Robotech/kacho#610` — **владеть ≠
>    состоять**. Номера **выведены из обращения и не переиспользуются** (§9.3);
> 2. **правка модели прав перенесена из S2 в S3** (§2.3а, §4): вопрос о доступе к объекту
>    личности нельзя сделать аккаунт-относительным, **не сменив объект**, — аккаунт-скоупным
>    объектом становится членство, а это `object_type` записей каталога прав и таблица края.
>    Это предмет контрактной стадии (`PRO-Robotech/kacho#471`), а не стадии прав;
> 3. **каждое утверждение о дереве несёт предикат** — команду, которой его перепроверяют.
>    Правило чтения предикатов — §1.0. Найдено и починено **9 утверждений сверх двух
>    названных**, из них **три уже разошлись с деревом** (§11.2).
>
> **Что APPROVED санкционирует — §0.2 и §4, не документ целиком.**
>
> **После APPROVED внесены три текстовые правки, ре-ревью не требующие** (указаны
> вердиктом круга 2; ни одна не меняет ни сценария, ни вывода):
> 1. §2.3а — радиус модели **6 и 6**, а не «6 и 5»: прежний предикат был префиксным грепом и
>    мерил **порядок дизъюнктов**, а не свойство; скобка «все, кроме `iam_access_binding`»
>    была прямо неверна — этот тип уровень выводит. Разбор оставлен на месте ошибки;
> 2. §1.5а — четыре предиката, принадлежащие §6, убраны из её таблицы (попали туда правкой,
>    задевшей обе таблицы разом);
> 3. §6 — два предиката заменены: про приостановку **лгал** (греп по подстроке даёт 3, а не
>    0, и все три — проза комментариев), про псевдонимы был **неисполним** (`grep -c … | grep`
>    ищет слово в числах и пуст всегда). Оба заменены на анкерованные, с контролем в обе
>    стороны.
>
> Плюс два вопроса владельцу, заведённых тем же вердиктом: **В-7** (утверждения токена) и
> **В-8** (блокировка становится глобальной).
>
> **Блокер П2 снят фактом:** на живом стенде дублей почты между аккаунтами **ноль**
> (89 пользователей, 89 уникальных почт — замер координатора, не мой). Процедура слияния не
> требуется, переход стартует.
>
> **Эпик:** `PRO-Robotech/kacho-workspace#211`.
> **Задачи:** `PRO-Robotech/kacho#470` (данные) · `#471` (контракт) · `#472` (права) · `#473` (консоль).
>
> [!warning] Круг 6 §1 НЕ перебазировал — и §1.0а требует сказать это ВСЛУХ, а не молча
> Норма §1.0а: «Документ, чей §1 не перемерен, подаётся на вердикт **с этим сказанным
> вслух**». Исполняю дословно. Круг 6 сужен ответом владельца по В-3 и мерил **только то, что
> сам утверждает**: поверхность чтения членств, состояние стадий §4, класс 9 §1.5а и предикат
> §0.3. Остальные предикаты §1 стоят на `43d332ac` и с тех пор **не перепроверялись**.
>
> **Признак нарушения §1.0а исполняется прямо сейчас:**
> `git rev-list --count 43d332ac..origin/main` в продукте → не ноль. Значит §1 описывает
> дерево, которого больше нет, и подавать документ на **полный** вердикт по нему нельзя.
>
> **Что круг 6 перемерил — на `5533d21e` (ствол продукта 2026-08-23):** столбец консоли
> (`spec` → **0**, §1.7) · класс 9 (`ListAccountsForUser`, три источника — §1.5а) · пять
> строк состояния стадий §4 · предикат §0.3. Перечень с обеими величинами — §13.2.
>
> **Обратное прочтение запрещено** тем же §1.0а: «раз §1 стареет, его можно не мерить» —
> вывод обратный верному. Полная перебазировка §1 — работа **следующего** круга, и она
> названа здесь предметом, а не обещанием.

> **Ревизии сверки — по одной на дерево, потому что деревьев два:**
> продукт `project/kacho` @ **`43d332ac`** (ствол на день круга 4, 2026-08-21) · воркспейс
> `kacho-workspace` @ **`origin/main`**. Прежние базы названы, потому что на них стоят числа
> прошлых кругов: **`554a8d49`** (база APPROVED круга 2), **`90a54650`** (ствол круга 3),
> **`78d062bb`** (ветка работы круга 3 — **снята с origin**, живёт только как объект коммита,
> см. §1.0). Круг 4 перемерил **весь §1** на `43d332ac`; расхождения названы в §12.1, числа в
> §1 приведены к стволу. Все координаты — от корня монорепо.
>
> **Замер круга 4 выполнен на ПОЛНОМ клоне.** Клоны с усечённой историей (`--depth`) отвечают
> на `git log --diff-filter=A` и `git merge-base` **молча и неверно**: предок, отрезанный
> глубиной, неотличим от несуществующего. Предикат пригодности клона —
> `git rev-parse --is-shallow-repository` → `false`; на усечённом любой вывод о происхождении
> строки недействителен (§12.2, класс 3).
>
> **Что прочитано и чего не читалось.** Документ решения
> `docs/specs/identity-account-coupling-design.md` (DRAFT, коммит `d8fd079`) прочитан
> целиком; его факты **перемерены** на `554a8d49`, а не приняты — он писался на `7ebb670d`.
> Три его утверждения устарели, одно уточнено — §1.8. Живой стенд мной **не опрашивался**:
> числа стенда взяты с атрибуцией (владелец — 74/74/74 на момент решения; координатор —
> 89/89 на круге 2) и мной не перемерялись; §3 называет предикаты, которыми их обязан
> перепроверить исполнитель.
>
> **Род документа:** ПРИЁМКА, САНКЦИОНИРУЮЩАЯ РАБОТУ. После APPROVED разрешается
> кодировать ровно то, что перечислено в §0.2 и §4.
>
> **Нормативка — ссылками по названию раздела, не пересказом:**
> `00-kacho-core.md` ban #1 · #5 · #9 · #10 · #11 · #12 · #14 · #15 ·
> `api-conventions.md` §Error-format · §By-lane code-split · §«Принято-и-проигнорировано» ·
> §update_mask discipline · §«Пустое значение обязано означать „пусто“» ·
> §«Поле контракта не выходит наружу, пока КРАЙ не пересобран» ·
> `data-integrity.md` §Within-service инварианты · §Cross-domain ссылки ·
> §«Authz-материализация owner-доступа» (в т.ч. B15 pending-intent) ·
> `security.md` §«Три уровня супер-доступа» · §«Авторизация живёт в МОДЕЛИ» ·
> §Hardening #2/#6/#7/#8 · §«Отношение, выполнимое подстановкой» ·
> `testing.md` §«Гейт на класс» · §Regression-lock · §E2E · `e2e-flow.md` §1-§5 ·
> `ui.md` правила 2, 3, 6, 9, 11, 12.

---

## §0 Предмет

### §0.1 Указание владельца — три пункта, и третий не подлежит пересмотру

> «Нужна миграция и отрыв аккаунта от пользователя, убрать из методов пользователя
> упоминание аккаунта и как есть оставить материализацию аккаунта при появлении
> пользователя в системе».

| # | Пункт | Где живёт в этом документе |
|---|---|---|
| 1 | миграция и **отрыв** принадлежности от строки пользователя | §2.1, стадии S1/S2/S4, группа A |
| 2 | **поле аккаунта уходит** из методов пользователя | §2.3, стадия S3, группа C |
| 3 | **материализация личного аккаунта СОХРАНЯЕТСЯ** «как есть» | §2.4, группа B |

> [!note] Здесь стоял предикат про приостановку членства — он принадлежит §6 п. 3 и об этой
> строке не говорит ничего (снято кругом 3)
> Он попал сюда той же правкой, что задела таблицу §1.5а (см. шапку, правка 2): предикат
> считает объявления значения перечисления `SUSPEND` в контракте, то есть проверяет **границу
> охвата**, а не сохранность директивы материализации. Предикат, стоящий не под своим
> утверждением, хуже отсутствующего: он создаёт вид проверенности у соседа. На месте — §6 п. 3.
>
> **Предикат самой директивы** (ворота материализации остаются «собственных аккаунтов ноль»,
> а не «членств ноль») — §1.6.

**Третий пункт — ограничение, а не пожелание.** Он назван владельцем явно и стоит здесь
отдельной группой сценариев (B) именно потому, что отрыв создаёт соблазн его «заодно
поправить»: ворота материализации сегодня спрашивают «сколько у человека **собственных
аккаунтов**», и при отрыве естественно захотеть спросить «сколько у него **членств**».
Это разные вопросы, и вторая формулировка **отменяет** директиву для приглашённого
человека — он членство имеет, личного аккаунта не получит. Сценарий IAM-ID-1-16
существует ровно затем, чтобы такая подмена покраснела.

### §0.2 Что APPROVED этого документа разрешает — и чего не разрешает

> [!important] ПЕРВЫМ ДЕЛОМ — область сужена, и читать документ подряд без этого абзаца нельзя
> Указание владельца по В-3 (2026-08-23) и решение по `PRO-Robotech/kacho#1085` (2026-08-24)
> сняли санкцию с части предмета. Отозванное и не пересматриваемое разделены так:
>
> | | Область | Что с ней |
> |---|---|---|
> | **по существу НЕ пересматривается** | данные: **S1**, **S2**, данные-части **S3** и **S4** | директива — про **чтение**, а не про хранение: строка человека, таблица членств, глобальный ключ идентичности, звено области из членства ею не затронуты. Но **разрешением это не является**: действующего вердикта нет ни у одной части (шапка) |
> | **САНКЦИЯ ОТОЗВАНА** | **публичная поверхность чтения членств** · стадия **S5** (группа F, сценарии 43-48) | поверхность перевыражена под **две** формы чтения; консольный предмет S5 принадлежит приёмке IAM-ID-2 (§2.6 таблица Б, §5 группа F5). **Круг 7:** вердиктом круга 6 S5 **снята как стадия** — не отложена (§4) |
> | **ПЕРЕНЕСЕНО** (круг 7, третье состояние) | **IAM-ID-1-49** — требование порядка работ по консольной пробе | предмет верен и исполняется в IAM-ID-2 (IAM-ID-2-22) вслед за фиксом, который проба стережёт; здесь производителя не заказывает (§9.3, §14.4) |
>
> **Форм чтения членства две, и третьей нет** (§5.0 п. 0, контракт обеих — IAM-ID-2 §2.1):
> **свой список** — субъект спрашивает про себя, параметра пользователя у запроса нет вовсе;
> **членство в НАЗВАННОМ аккаунте** — спрашивают про аккаунт, права проверяются на нём. Форма
> «в каких аккаунтах состоит НАЗВАННЫЙ человек» **запрещена**: она показывает всякому, кто её
> позовёт, куда человек приглашён.
>
> **Почему этот абзац стоит первым, а не в §13.** Документ читают подряд, и исполнитель,
> дошедший до §5, уже несёт в голове форму, которую построил по §1 и §2. Указание,
> отменённое в конце, из головы не выгружается — оно оттуда исполняется. Ровно это
> зафиксировано в §7, В-8 как норма: «правка решения не закончена, пока не убрано умолчание,
> которое оно отменяет».

> [!warning] Абзац ниже описывает вердикт круга 5, а он ОТОЗВАН ЧАСТЬЮ — читать вместе с шапкой
> Действующего вердикта у документа нет (шапка, запись от 2026-08-24). Ниже сохранён текст,
> которым круг 5 очерчивал разрешённое, — он нужен, чтобы круг 6 видел, от чего отсчитывать,
> и **не является позволением сегодня**. Из очерченного снята санкция на стадию **S5**
> (сценарии IAM-ID-1-43…-48) и на плоскую поверхность **чтения** членства в S3 (IAM-ID-1-01
> последнее **And**, -63, -66, -67); стадии S3 и S4 в остальном предмета не меняли, но и они
> ждут вердикта круга 6, потому что вердикт выносился документу целиком.

**Разрешает — стадии `S3` и `S4`** из §4 в назначенном порядке (§2.3а: S3.1 → S3.2 → S3.3),
строгим TDD (ban #12), по сценариям §5. S0 исполнена, S1 исполнена полностью, S2 исполнена
частью: они посажены до вердикта и признаны §0.3, а не разрешены им. Фактическое состояние
каждой стадии с предикатом — таблица §4.

> [!note] Здесь стояло «S3, S4, S5», и абзац НАМЕРЕННО не переписывался до вердикта
> Круг 6 предлагал сузить, но оставил редакцию круга 5: сужение санкции — предмет **вердикта**,
> а не авторской правки (ban #1). Вердикт круга 6 состоялся и назвал область прямо: **S3 и S4**
> (шапка, «Важное», п. 2). Стадия **S5 снята как стадия** — не «отложена»: её консольный
> предмет принадлежит IAM-ID-2, а сценарии 43-48 остаются здесь текстом с записью об отзыве
> (§9.3 — состояния «снят», «санкция отозвана» и «перенесён» считаются раздельно).

> [!important] ВНЕШНЕЕ ПРЕДУСЛОВИЕ: S3 не закрывается раньше APPROVED у IAM-ID-2 (заведено кругом 7, Б1)
> **Четыре ДЕЙСТВУЮЩИХ сценария этого документа неисполнимы средствами, которые он
> санкционирует** — их производитель принадлежит соседней приёмке
> `docs/specs/sub-phase-IAM-ID-2-membership-read-acceptance.md`, а она сама в состоянии
> `DRAFT` (круг 2, вердикта нет):
>
> | сценарий | что требует | производитель |
> |---|---|---|
> | **IAM-ID-1-01** | **оба** чтения — членства названного аккаунта и свой список | IAM-ID-2-01/-03 · IAM-ID-2-07 |
> | **IAM-ID-1-55** | список членств **названного аккаунта** (консоль спрашивает про аккаунт) | IAM-ID-2-01/-03 |
> | **IAM-ID-1-68** | записи каталога прав **на обе** формы чтения | IAM-ID-2-06 |
> | **IAM-ID-1-73** | свой список — как канал наблюдения для снимка прав | IAM-ID-2-07 |
>
> **Почему это названо предусловием, а не переписано «в обход».** Предмет этих четырёх и есть
> **публичная форма чтения**; перевыразить их средствами данного документа значило бы завести
> здесь второй контракт чтения — два места об одном предмете, из которых верно одно. Вердикт
> круга 6 этого прямо не требует («Перевыражать сценарии в обход IAM-ID-2 **не** требую»).
>
> **Что из этого следует для исполнителя, дословно:**
> 1. **шаги S3.1 и S3.2 от вердикта соседа НЕ зависят** — модель, префикс, глаголы ресурса
>    членства **сверх посаженного исключения из аккаунта** (создание и два чтения §5.0 п. 0;
>    **снятие уже посажено**, второго глагола не заводится — §1.9), каталог, край, переезд
>    глагола приглашения строятся здесь и целиком;
> 2. **шаг S3.3 и закрытие стадии — зависят**: пока у IAM-ID-2 нет APPROVED, четыре сценария
>    выше производителя не имеют, и объявлять S3 production-complete нельзя (ban #14: стадия
>    обязана быть завершена **в своих границах**, а не по чужому вердикту);
> 3. **предикат снятия предусловия** — вердикт `APPROVED` у IAM-ID-2, читаемый машинно тем же
>    способом, что и здесь: `./scripts/docs-gate/run-all.sh`, строка переписи вердиктов.
>
> Это записано в §4 (строка S3), §9.1 и **отдельным пунктом DoD** — иначе зависимость
> существует, а спросить её негде.

> [!important] Формулировка «разрешает начать» здесь была бы ложной, и это не педантизм
> Прежняя редакция разрешала «стадии S0-S5». К моменту круга 4 две с половиной из шести
> **уже стоят в стволе**. Приёмка, разрешающая сделанное, не санкционирует ничего — она
> сообщает исполнителю, что предмет открыт, тогда как предмет закрыт, и тот начинает работу
> заново либо ищет, чего не хватает, по документу, который об этом молчит.

**Не разрешает** (и не потому, что «потом», а потому, что это другой предмет со своей
приёмкой): федеративную идентичность · передачу владения аккаунтом · приостановку членства
как отдельное состояние · слияние дублей почты · перенос ресурсов между арендаторами.
Полный перечень с предикатом каждого пункта — §6.

**Не разрешает и внутри своего предмета:** пропускать S2 ради скорости. Порядок стадий —
часть контракта этого документа, и §4 называет, что именно ломается при перестановке.

### §0.3 Признание влитого: S1 и часть S2 приняты задним числом, гейт был обойдён

**Факт, названный прямо.** Миграции `470001_memberships_expand.sql` (S1) и
`472002_membership_carries_rights.sql` (S2, частью) посажены в ствол схлопнутым мёржем
`97544aefc` **2026-08-18**, то есть **до** вердикта по этому документу. Обе цитируют его
поимённо — **вдвоём они ссылаются на три сценария: IAM-ID-1-08, 13, 50**; предикат ключуется
на **сами эти два файла**, а не на каталог:

```sh
git grep -hoE 'IAM-ID-1-[0-9]+' "$R" \
  -- services/iam/internal/migrations/470001_memberships_expand.sql \
     services/iam/internal/migrations/472002_membership_carries_rights.sql | sort -u
#   → 08, 13, 50 — и на `43d332ac`, и на `5533d21e`: утверждение о ДВУХ файлах не дрейфует
```

> [!warning] Здесь стояло «восемь сценариев» с предикатом по ВСЕМУ каталогу — предикат был
> шире собственного утверждения, и число дрейфовало от чужой работы (перемерено кругом 6)
> Утверждение говорит о **двух** миграциях, а команда считала весь каталог. На `43d332ac` она
> давала 8, на `5533d21e` даёт **16** — и вырос не предмет утверждения, а число миграций
> вокруг него. То есть число здесь было функцией чужой работы, а не своей: тот же класс, что
> §9.3 ловит у неанкерованного предиката («его величина есть функция того, сколько раз о нём
> написали»), только источник роста внешний.
>
> Контроль в обратную сторону, показывающий, что суженный предикат исполним и что-то находит:
> та же команда по каталогу целиком даёт **16** на `5533d21e`.

Значит работа велась **по этому документу**, находившемуся в состоянии DRAFT, при снятом
APPROVED. **Ban #1 был нарушен**, и умолчать об этом нельзя: приёмка, не заметившая, что её
предмет реализован в обход её же гейта, перестаёт быть гейтом для следующего.

**РЕШЕНИЕ ВЛАДЕЛЬЦА: влитое принимается явно и задним числом, без отката.** Причина названа, а
не подразумевается:

| Довод | Почему он решающий |
|---|---|
| **миграции применены** | править применённую миграцию запрещено (ban #5) — отката «на месте» не существует |
| **откат стоил бы второй миграции** | она отменяла бы первую, и в истории осталось бы два взаимно погашающих изменения схемы вместо одного полезного |
| **предмет не расходится с приёмкой** | §12.3 сверяет влитое против сценариев поимённо: расхождений по существу нет, есть недоделанное |
| **цена признания меньше цены формальности** | откат ради процедуры уничтожил бы верную работу, ничего не исправив в самом процессе |

**Чего это признание НЕ делает.** Оно не объявляет S1 и S2 закрытыми: S2 исполнена **частью**
(§4 называет предикатом, чего в ней нет), и остаток идёт обычным порядком. Оно не
распространяется вперёд: **S3 и S4 кодируются только после APPROVED** (с S5 санкция отозвана,
§0.2), и повторное
обхождение гейта признанием не станет — сегодняшнее решение принято по обстоятельствам,
которых у будущей стадии не будет (применённая миграция).

> [!warning] Что этот случай говорит о самом гейте — и это дороже одного признания
> Гейт ban #1 не имеет **механизма**: он держится тем, что исполнитель прочитает статус
> документа перед работой. Здесь исполнитель документ прочитал (миграции цитируют сценарии
> поимённо) и всё равно начал — потому что читать статус и читать содержание суть **разные
> действия**, а из второго первое не следует. Правило, держащееся вниманием, отличается от
> невыполняемого только тем, что его неисполнение незаметно (`multi-agent-flow.md` §11).
>
> Предикат, которым это ловится дешевле всего и который здесь **сработал бы**: миграция,
> цитирующая идентификатор сценария приёмки, обязана иметь эту приёмку в состоянии APPROVED
> на момент своего коммита. Обе величины машинно доступны — идентификатор стоит в самой
> миграции, состояние в шапке документа. Предмет заведён отдельно; здесь он **назван, а не
> обещан**: этот документ его не заказывает и своим APPROVED не покрывает.

---

## §1 Что уже верно в дереве — ПЕРЕБАЗИРОВАНО кругом 9 на `475f8527`

Единица счёта названа у каждого числа. Предикаты приведены, чтобы их можно было повторить,
а не поверить.

**Круг 9 перебазировал главу целиком** — не по перечню вердикта, а прогнав **каждый** её
предикат на действующей ревизии продукта (`release/identity-4` @ `475f8527`, накопительная
ветка волны линии). Единица — **объявленное число**: одна исполняемая команда с приведённым
рядом значением. Перемерено **70**, значение изменилось у **десяти**, и ещё **два** не
совпадали с документом **на обеих** ревизиях — итого разошлось **12**. Перечень **всех 70**
строк с обеими величинами — §15.1; там же названы четыре, которые меняли работу исполнителя,
и одно место, где негодным оказался предикат, а не дерево.

Где число изменилось, в строке стоит **новое**, а прежнее названо рядом с ревизией, на
которой оно было верно: снятое без следа число делает прошлые круги непроверяемыми.

> [!warning] Число «63» больше названных вердиктом круга 8 «40» — расходится ЕДИНИЦА, а не дерево
> Вердикт круга 8 перемерил **40** предикатов и нашёл **11** расхождений плюс одно
> несовпадение на обеих ревизиях — **12** всего. Я считаю **объявленные числа**, а вердикт
> считал **предикаты**: блок вида «перепись, разбивка по трём действиям» у него одна строка,
> у меня четыре. Обе величины истинны, и подгонять было нечего.
>
> **Итог разошедшихся сошёлся на числе 12**, а разбивка внутри него у нас разная: у меня
> **10** изменили значение и **2** были неверны на обеих ревизиях, у вердикта — 11 и 1.
> Причина та же самая: предикат класса 3 и его контроль у меня две строки, у вердикта одна.
>
> Существенное **сошлось**: тот же набор мест разошёлся, и ни одно расхождение вердикта я не
> отменил. Сверх его перечня круг 9 нашёл **три** (§15.1, помечены `+`): предикат §1.5 строки
> 5 даёт **2** вместо 1 — и второе вхождение есть тот самый посаженный глагол Б8.3, то есть
> глава находила его **своей же командой** и не заметила; контроль §1.8 даёт **2** файла
> вместо 1; пол типизированных потребителей — **15** вместо 16.
>
> Ревизия названа явно, потому что дерево движется прямо сейчас: между замером вердикта
> (`e4da590cf`) и моим — **4** коммита, между ревизией §1 круга 4 (`43d332ac`) и моим —
> **407** (`git rev-list --count 43d332ac..475f8527`). Вердикт называл **403** на своей
> ревизии; расхождение есть движение ветки за сутки, а не спор.

### 1.0 Как читать предикаты этого документа (заведено кругом 3)

**Каждое утверждение о дереве ниже несёт предикат — команду, которой оно проверяется.** Это
не оформление: утверждение без предиката живёт до первого исполнителя, который на него
обопрётся, и обнаруживается **ценой прогона**. По этому документу так обнаружились два
(§11.1) — потому и заведена эта глава.

**Ревизия задаётся один раз и подставляется в каждый предикат.** Прежняя редакция писала в
предикатах `origin/main`, а числа относила к `554a8d49`, — то есть **команда читала
движущуюся ссылку, а число стояло на неподвижной**. Разойтись они обязаны были, и разошлись
(§11.2). Форма, действующая с круга 3:

```sh
# ОДНА строка на весь документ. Меняешь её — перемеряй ВСЕ числа: они относятся к ней.
R=475f8527                                # продукт, release/identity-4 на день круга 9 — ДЕЙСТВУЮЩАЯ
# R=43d332ac                              # продукт, ствол на день круга 4
# R=554a8d49                              # продукт, база APPROVED круга 2
# R=90a54650                              # продукт, ствол на день круга 3
# R=78d062bb                              # ветка работы круга 3 — СНЯТА С ORIGIN, см. ниже
W=origin/main                             # воркспейс (правила), отдельное дерево
```

> [!warning] `78d062bb` пиннится ХЕШЕМ, а не именем ветки — имя пережило свою ветку
> Прежняя редакция писала в предикатах `B=origin/release/identity-decoupling`. Ветка **снята
> с origin** (предикат: `git ls-remote --heads origin 'refs/heads/release/identity-decoupling'`
> → пусто), а локальная отслеживающая ссылка, если она у читателя ещё есть, указывает на
> **другой** коммит — у меня `244f6500`, а не на названный `78d062bb`. То есть предикат,
> ключующийся на имя ветки, после её снятия либо не резолвится вовсе, либо **молча отвечает
> про другое дерево**; вторая форма опаснее, потому что даёт число.
>
> Хеш коммита переживает снятие ветки: `git rev-parse 78d062bb` резолвится и сегодня. Это тот
> же класс, что §1.0 п. 1 запрещает для `origin/main`, — движущаяся ссылка при неподвижном
> числе, — только здесь ссылка не сдвинулась, а **исчезла**. Предикаты приведены к хешу.

Дальше предикаты пишутся как `git grep … "$R" -- <путь>` и **никогда** как `git grep …
origin/main`. Число, приведённое рядом, — значение **на действующей `R`** (`475f8527`), если
явно не сказано иное; где прежняя ревизия давала другое, прежнее значение названо в самой
строке вместе с ревизией.

> [!note] Круг 9 перевернул умолчание: было «число относится к `554a8d49`»
> Прежняя редакция объявляла умолчанием **базу APPROVED круга 2**, а действующей ревизией в
> `R` ставила ствол круга 4 — то есть команда читала одно дерево, а число по умолчанию
> относилось к другому, отстоящему на сотни коммитов. Пока глава перемерялась целиком, это
> было безобидно; на пятом круге без перебазировки оно стало ровно тем классом, который
> §1.0 ловит первым пунктом. Умолчание и `R` теперь одна ревизия **by construction**.

**Три требования к предикату, без которых он не считается предикатом:**

1. **исполним** — команда возвращает значение, а не пустоту. `grep -c` на файле без совпадений
   не печатает `0`, он не печатает **ничего**; такой предикат читается как «ноль» и им не
   является. Форма, которая печатает число всегда: `… | wc -l` либо `grep -c … || true`;
2. **контроль в обратную сторону** — рядом стоит та же команда на предмете, где ответ заведомо
   ненулевой. Без него «ноль» неотличим от «предикат не находит ничего никогда»;
3. **единица счёта названа** — строки, файлы, ключи, записи каталога, сообщения. Одно и то же
   утверждение даёт разные числа в разных единицах, и обе величины бывают истинны.

**Что предикат НЕ доказывает.** Он доказывает состояние дерева на названной ревизии — и
только. Утверждения о **стенде** (§3) проверяются на стенде, каждый раз заново; утверждения о
**будущем** (§2, §5) предикатов не имеют by construction и обязаны быть отличимы от
утверждений о дереве. Смешение этих трёх родов — отдельный дефект, и §5 отделяет их прямо:
сценарий описывает целевое поведение, а всё, что он утверждает **о сегодняшнем дереве**, стоит
в его выноске с предикатом.

### 1.0а Приёмка на гейте — не архив: §1 живёт до следующего мёржа в предмет (заведено кругом 4)

**НОРМА. Перед подачей документа на вердикт §1 перемеряется против ТЕКУЩЕГО ствола, и ревизия
замера называется в шапке.** Не выборочно по замечаниям прошлого вердикта — **целиком**: тот
предикат, который разойдётся, заранее неизвестен, иначе он был бы уже починен.

**Признак нарушения — механический.** Ревизия в §1.0 старше вершины ствола, а в шапке нет
строки «перемерено на ‹ревизия›» с сегодняшней датой. Проверяется одной командой:
`git merge-base --is-ancestor "$R" origin/main && git rev-list --count "$R"..origin/main` —
не ноль означает, что документ описывает дерево, которого больше нет.

**Почему это норма, а не пожелание — цена измерена.** Круг 3 осознанно не перебазировал §1
(§11.4) и назвал это отдельной работой. Довод был защитим: смешивать две правки в одну — само
по себе дефект. **Допущение перестало быть верным через четырнадцать часов после записи** — в
ствол влилась работа по этому же документу. Документ пролежал на гейте **три дня**, и за это
время из **38** предикатов §1 значение изменилось у восьми, и четыре из них глава утверждала
как действующие:

| Что разошлось | Было | Стало | Чем это грозило исполнителю |
|---|---|---|---|
| §1.5а класс 3 (скрипт восстановления) | 1 | **0** | **ложный ноль на живом потребителе**: «переводить нечего» — и скрипт восстановления выкатывается по прежнему смыслу отношения |
| §1.5а класс 6 (каскад) | 7 | **4** | координата, на которой стоит определение готовности S3, указывает на символ **без единого вызывающего** |
| §1.3 (перепись ссылок) | 13 | **14** | вердикт круга 3 этого не назвал; 14-я ссылка — **сама влитая S1** |
| §1.8 (`case "` по файлу) | 32 | **35** | число-ловушка, приведённое как контрпример, само стало неверным |

**Три из четырёх не были и не могли быть названы вердиктом круга 3** — он нашёл два. Значит
перемер обязан быть **сплошным**, а не адресным: адресный чинит названное и оставляет класс.

**Чем держится.** Вниманием — предиката у «перемерил ли автор всё» не существует, и объявлять
его было бы ровно тем, что этот документ ловит. Проверяемый признак для автора: **в §12.1
стоит перечень ВСЕХ предикатов главы с обеими величинами**, а не только разошедшихся. Перечень,
называющий одни находки, неотличим от перечня, в котором остального не смотрели
(`testing.md` §«Гейт на класс», п. 3: «ноль находок» обязано быть отличимо от «ноль
прочитанного»).

**Обратное прочтение запрещено.** «Раз §1 стареет, его можно не мерить» — вывод обратный
верному: стареет он именно потому, что его читают как факт. Документ, чей §1 не перемерен,
подаётся на вердикт **с этим сказанным вслух**, а не молча.

### 1.1 Принадлежность обязательна, уникальность — парная

`kacho_iam.users` несёт `account_id text NOT NULL` (`services/iam/internal/migrations/0001_initial.sql:840`).
Уникальность объявлена **парой**, обе ведут с `account_id`:

| Индекс | Форма | Координата |
|---|---|---|
| `users_account_email_unique` | `UNIQUE (account_id, lower(email))` | `0001_initial.sql:1639` |
| `users_account_external_id_unique` | `UNIQUE (account_id, external_id) WHERE external_id <> ''` | `0001_initial.sql:1646` |
| `users_active_external_id_uniq` | `UNIQUE (external_id) WHERE invite_status='ACTIVE' AND external_id <> ''` — **глобальный** | `0011_users_drop_global_email_uniqueness.sql:55` |

Третий введён осознанно: он сериализует гонку конкурентного первого входа, которую
пер-аккаунтные ключи сериализовать не могут (два одновременных входа выбирают **разные**
новые `account_id`). Это свойство обязано быть заменено, а не потеряно — IAM-ID-1-07.

`account_id` hard-immutable: `services/iam/internal/apps/kacho/api/user/update.go`, ветка
неизменяемости в `Update`.

**Предикаты** (единица — объявление; номера строк намеренно не воспроизводятся: они
устаревают от любого коммита выше по файлу, и проверить их нечем):

```sh
# колонка принадлежности объявлена и NOT NULL
git grep -c 'account_id text NOT NULL' "$R" -- services/iam/internal/migrations/0001_initial.sql
#   → 4 (единица — строка; колонка `users` среди них, соседние — другие таблицы)

# три ключа уникальности существуют именно как объявления индекса, а не как упоминания
git grep -nE 'users_account_email_unique|users_account_external_id_unique|users_active_external_id_uniq' \
  "$R" -- services/iam/internal/migrations/ | grep -c 'CREATE UNIQUE INDEX'
#   → 3 — по одному объявлению на ключ

# принадлежность объявлена hard-immutable ИМЕННО ЕЮ, а не «где-то рядом»
git grep -c 'accountId is immutable after User.Create' "$R" \
  -- services/iam/internal/apps/kacho/api/user/update.go
#   → 2 (две формы ключа маски: `account_id` и `accountId`)

# контроль в обратную сторону — КОМАНДОЙ, а не прозой: тот же АНКЕР (текст отказа
# неизменяемости) с подставленным другим полем. Состояние приглашения меняется ДЕЙСТВИЕМ,
# а не маской, поэтому «immutable» о нём было бы ложью — и анкер его не находит:
git grep -c 'inviteStatus is immutable after User.Create' "$R" \
  -- services/iam/internal/apps/kacho/api/user/update.go || echo 0
#   → 0

# и ПОЛОЖИТЕЛЬНЫЙ контроль тем же анкером на ТРЕТЬЕМ поле — иначе ноль выше читался бы
# как «анкер находит только account и ничего больше»:
git grep -c 'externalId is immutable after User.Create' "$R" \
  -- services/iam/internal/apps/kacho/api/user/update.go
#   → 3 — ДВЕ формы ключа маски плюс третье вхождение в комментарии шапки.
#     Круг 9: здесь стояло «2 (те же две формы ключа маски)», и это было неверно
#     НА ОБЕИХ ревизиях — см. выноску ниже
```

> [!warning] Круг 9: положительный контроль давал ЛОЖНОЕ ЧИСЛО с круга 4 — и оно ложно на
> обеих ревизиях, то есть никогда не было верным
> Здесь стояло **2** с объяснением «те же две формы ключа маски». Дерево даёт **3**:
> ```sh
> git grep -c 'externalId is immutable after User.Create' 475f8527 \
>   -- services/iam/internal/apps/kacho/api/user/update.go   # 3
> git grep -c 'externalId is immutable after User.Create' 43d332ac \
>   -- services/iam/internal/apps/kacho/api/user/update.go   # 3 — то же на ревизии круга 4
> ```
> Третье вхождение — комментарий шапки, перечисляющий неизменяемые поля; двух форм ключа
> маски там нет, есть третье упоминание того же анкера.
>
> **По существу контроль остаётся годным, и это надо сказать прямо**: его работа —
> доказать, что анкер находит не только принадлежность, то есть что ноль строкой выше
> получен от дерева, а не от неумения предиката. Ненулевое значение эту работу делает
> при любом числе. Ложным было **объяснение** числа, а объяснение и есть то, по чему
> следующий читатель решает, сошлось ли.
>
> **Почему это важнее, чем «единица на единицу».** Число с объяснением читается как
> проверенное дважды; здесь объяснение было выведено из формы соседнего предиката, а не
> из вывода команды. Тот же класс, что «контроль, записанный прозой» выше по разделу, —
> только тише: там проза расходилась с командой, здесь расходится **комментарий к
> команде**, а его никто не исполняет.

Различие не выведено мной из формы предиката, а **записано в самом коде** рядом с таблицей
неизменяемых полей: `invite_status` назван там изменяемым **действиями**, а не маской, с
объяснением, почему обещать о нём неизменяемость было бы ложью. Предикат и комментарий
согласны — это и есть тот случай, когда контроль подтверждает намерение автора кода, а не
только поведение грепа.

> [!warning] Контроль здесь стоял ПРОЗОЙ — и, прочитанный буквально, был неверен
> Прежняя редакция писала: «тот же файл на `invite_status` даёт 0». Буквально —
> `git grep -c 'invite_status' … update.go` → **2**, а не 0. Ноль получается только при
> подстановке имени поля **в сообщение о неизменяемости**, то есть контроль имел в виду
> другой предикат, чем тот, который читатель наберёт по его словам. Проверено на обеих
> ревизиях: 2 и на `554a8d49`, и на `43d332ac`.
>
> Класс общий и стоит того, чтобы назвать его один раз: **контроль, записанный прозой, не
> является контролем** (§1.0 п. 2). Он не исполним, значит не перепроверяем, и первое же его
> буквальное прочтение даёт число, опровергающее утверждение, которое он должен был
> подпирать. Контроль пишется командой — той самой, целиком.

> Третий ключ **глобален среди активных** — это и есть опора утверждения В-8 §7 о том, что
> блокировка уже сегодня действует на всю платформу: авторизуемая строка у человека одна на
> кластер. Предикат тот же, что и выше; отдельного у В-8 нет намеренно, чтобы два места об
> одном предмете не разошлись.

### 1.2 Цикл держит СХЕМА, и проверка не откладывается

```
accounts_owner_fk  accounts.owner_user_id → users(id)     ON DELETE RESTRICT DEFERRABLE INITIALLY DEFERRED
users_account_fk   users.account_id       → accounts(id)  ON DELETE RESTRICT DEFERRABLE INITIALLY DEFERRED
```

**Предикат** (единица — объявление ключа; ищется по имени ключа, не по номеру строки):

```sh
for k in accounts_owner_fk users_account_fk; do
  printf '%s: RESTRICT=%s\n' "$k" \
    "$(git grep -h "$k" "$R" -- services/iam/internal/migrations/ | grep -c 'RESTRICT')"
done
#   → по 1 на ключ. Контроль в обратную сторону: тот же счётчик на `CASCADE` даёт 0 —
#     то есть предикат различает действие ключа, а не просто находит его имя
```

Оговорка `DEFERRABLE` управляет проверкой вида `NO ACTION`; `RESTRICT` — другой вид, он
исполняется немедленно на самом `DELETE` независимо от неё. Это документированное свойство
PostgreSQL. **Следствие: исполнимого порядка снятия пары не существует, и никакой код
транзакции этого не меняет.**

Гипотеза «ключ объявлен отложенным ⇒ одна транзакция пройдёт» была **опровергнута пробой на
живой базе** (документ решения §3): отказ пришёл на стейтменте, повтор с
`SET CONSTRAINTS ALL DEFERRED` исхода не изменил. Записано здесь, а не только там, потому
что предикат `grep "DEFERRABLE INITIALLY DEFERRED" … | grep -E "accounts_owner_fk|users_account_fk"`
возвращает обе строки — то есть **правду, которая ничего не значит**, и следующий читатель
повторит вывод, если не предупредить.

**Что из этого следует для нас.** Отрыв снимает `users.account_id` вместе с ключом, поэтому
второе ребро цикла **исчезает**, а не размыкается. После стадии S4 удаление аккаунта
перестаёт упираться в строки людей by construction — IAM-ID-1-12.

### 1.3 Перепись всех ссылок на `users(id)` — 14, живых `RESTRICT` четыре

Радиус взят по **механизму** (ссылка на `users(id)` во всём каталоге миграций), а не по
файлу, где предмет заметили. Единица счёта — **ключ**, не таблица.

```sh
# перепись ключей (единица — КЛЮЧ, не таблица)
git grep -n "REFERENCES kacho_iam\.users *(" "$R" -- services/iam/internal/migrations/ | wc -l   # 14
# разбивка по действию — то, чем получены числа таблицы ниже
for a in RESTRICT CASCADE 'SET NULL'; do
  printf '%-10s %s\n' "$a" \
    "$(git grep -n "REFERENCES kacho_iam\.users *(" "$R" -- services/iam/internal/migrations/ | grep -c "$a")"
done                                                                        # 8 / 5 / 1
# живость держателя — таблица могла быть дропнута последующей миграцией
git grep -n "DROP TABLE.*<таблица>"          "$R" -- services/iam/internal/migrations/
```

| Действие | Всего | Живых | Мёртвых (таблица дропнута) |
|---|---:|---:|---|
| `RESTRICT` | 8 | **4** | 4 × `break_glass_grants_*` на `cluster_break_glass_grants` (дропнута `0006_drop_scim_saml_break_glass.sql:25`) |
| `CASCADE` | **5** | **4** | `scim_user_mappings_user_fk` (`0006:20`) |
| `SET NULL` | 1 | 1 | — |

> [!warning] Было **13**, стало **14** — и четырнадцатая ссылка есть САМА ВЛИТАЯ S1
> Круг 3 записал здесь: «Число 13 проверено и на стволе круга 3 (`90a54650`) — не изменилось».
> На день круга 4 оно **14**, и вердикт круга 3 этого расхождения не называл — он нашёл два
> других (§12.1). Разница ровно одна строка, и предикат называет её сам:
>
> ```sh
> diff <(git grep -n "REFERENCES kacho_iam\.users *(" 554a8d49 -- services/iam/internal/migrations/ | sed 's/^554a8d49://') \
>      <(git grep -n "REFERENCES kacho_iam\.users *(" "$R"      -- services/iam/internal/migrations/ | sed "s|^$R:||")
> # > services/iam/internal/migrations/470001_memberships_expand.sql:122:
> #       REFERENCES kacho_iam.users (id) ON DELETE CASCADE
> ```
>
> Это `memberships_user_fk` — ключ, заведённый миграцией стадии **S1 этого же документа**
> (§0.3). То есть глава «что уже верно в дереве» теперь описывает **в том числе работу,
> которую сам документ и санкционирует**, и это не курьёз: перепись, ведённая как снимок
> «до», незаметно стала снимком «в процессе». Число оставлено **фактическим** (14), а не
> «13 плюс наше» — иначе следующий читатель, прогнав предикат, получит 14 и не поймёт, чему
> верить. Разделение по происхождению — здесь, в этой выноске, а не в числе.
>
> **Что из этого следует по существу:** действие нового ключа — `CASCADE` на `users(id)`,
> то есть удаление человека сносит его членства, а не упирается в них. Это верно и намеренно
> (членство — не самостоятельная сущность в отсутствие человека), и на вывод §1.2 о цикле
> **не влияет**: цикл держат `accounts_owner_fk` и `users_account_fk`, оба `RESTRICT`, оба
> перемерены кругом 4 без изменений.

Четыре живых `RESTRICT`: `accounts_owner_fk` · `service_account_oauth_clients_created_by_fk` ·
`session_revocations_user_fk` · `user_oauth_clients_created_by_fk` (его `DROP TABLE` стоит в
секции `-- +goose Down`, то есть таблица жива).

**Предикат, называющий ключи ПОИМЁННО** (круг 9; единица — именованный ключ, объявленный
`ALTER TABLE … ADD CONSTRAINT`). Он склеивает строки перед разбором, потому что объявление
ключа бывает многострочным:

```sh
fkacts() { git grep -h -A2 'ADD CONSTRAINT' "$1" -- services/iam/internal/migrations/ \
  | tr '\n' ' ' | sed 's/ADD CONSTRAINT/\nADD CONSTRAINT/g' \
  | grep 'REFERENCES kacho_iam.users' \
  | sed -E 's/.*ADD CONSTRAINT ([a-z_]+).*ON DELETE (RESTRICT|CASCADE|SET NULL).*/\1 \2/'; }
fkacts "$R" | sort -u | grep RESTRICT
#   → 8 имён: четыре живых выше + четыре `break_glass_grants_*` на дропнутой таблице
fkacts "$R" | grep -c CASCADE
#   → 3 — КОНТРОЛЬ: команда различает действие, а не находит слово. Единица здесь ДРУГАЯ,
#     чем в переписи выше (5): там считаются все строки `REFERENCES`, включая объявленные
#     внутри `CREATE TABLE`, здесь — только именованные через `ALTER TABLE`. Обе истинны
```

> [!warning] Круг 9: здесь стояли НОМЕРА СТРОК, а `user_oauth_clients_created_by_fk`
> однострочным предикатом не находится вовсе
> Прежняя редакция называла четыре ключа координатами `файл:строка` — то есть ровно той
> формой, которую §1.5а объявляет устаревающей молча, и делала это в главе, чей §1.0 требует
> предиката. Проверить «живых RESTRICT четыре» было **нечем**: перепись выше даёт разбивку
> `8/5/1` по действиям, но не называет, какие из восьми живы.
>
> Круг 9 попробовал очевидное — `git grep -h "<ключ>" | grep -c RESTRICT` — и получил на
> `user_oauth_clients_created_by_fk` **0** при трёх остальных по **1**. Дерево при этом
> **не менялось**: в `0046` имя ключа стоит на одной строке, а `ON DELETE RESTRICT` на
> следующей, и однострочный предикат их не видит. У трёх остальных объявление однострочное,
> поэтому промах пришёлся ровно на один ключ из четырёх и выглядел как находка о дереве.
>
> **Урок ровно тот, что глава уже несёт дважды** (класс 3 §1.5а, окно `grep -A<N>` §1.5):
> предикат, ключующийся на **форму записи**, стареет молча. Здесь он даже не устарел — он
> был негоден с рождения, и негодность заметна только потому, что рядом стояли три
> одинаковых случая. Один ключ без соседей дал бы ложную находку, и чинили бы дерево.

**Контроль предиката в обе стороны.** Перепись по одному файлу `0001_initial.sql` дала бы
**семь** живых ключей вместо четырёх (засчитав четыре мёртвых `break_glass`) и одновременно
**пропустила** бы четвёртый, введённый `0046`. Число «четыре» верно только для предиката по
каталогу.

**Зачем это здесь.** Отрыв не трогает ни один из трёх неаккаунтных ключей — они про удаление
**человека**, а не про принадлежность. Перепись приведена, чтобы исполнитель не принял их за
следствие перехода и не «починил» заодно: их предмет — §6 п. 9.

### 1.4 Права уже ключуются на каноническую строку; членство уже выражается привязкой

Два факта, вместе определяющие цену перехода.

**Субъект привязки резолвится в каноническую строку личности.** Метод
`resolveCanonicalSubjectID` (`services/iam/internal/apps/kacho/api/user/invite.go`) ищет
**старейшую `ACTIVE` строку по почте** — ту самую, в которую край резолвит токен, — и на неё
выдаёт право.
Значит авторизация де-факто **уже** работает по одной глобальной строке личности, а
пер-аккаунтная строка прав не несёт: она занимает пару ключей уникальности и держит FK на
аккаунт, то есть выполняет роль отметки о членстве.

**Оговорка, которую нельзя опустить:** резолв best-effort, и ошибка чтения **не отличается**
от «ноль активных» — **одно** условие возвращает пер-аккаунтную строку в обоих случаях
(`if err != nil || len(actives) == 0`). Значит в базе **могут** лежать привязки, выданные не
на каноническую строку. Это измеряется предикатом П3 (§3) и обязано быть измерено **до**
любого шага, опирающегося на «субъект не меняется».

**Членство уже читается тремя источниками.** `ListAccountsForUser`
(`services/iam/internal/repo/kacho/pg/user_repo.go`) отвечает объединением: строка
пользователя · владение аккаунтом · **активная привязка на аккаунт**. Третий путь работает
сегодня и от `users.account_id` не зависит — то есть механизм членства, независимый от
строки, в продукте уже есть, и пер-аккаунтная строка его **дублирует**.

**Предикаты** (единица — вхождение; оба утверждения ключуются на **имя**, не на номер строки):

```sh
# канонический резолв существует и зовётся с пути выдачи права
git grep -n 'resolveCanonicalSubjectID' "$R" -- services/iam | wc -l          # 5
#   из них: 1 объявление метода, 1 вызов с пути приглашения, 1 godoc,
#   2 упоминания в миграции 0011 — то есть предикат находит и определение, и потребителя

# запасной путь СЛИВАЕТ ошибку чтения с «ноль активных» — одно условие на оба случая
git grep -c 'err != nil || len(actives) == 0' "$R" \
  -- services/iam/internal/apps/kacho/api/user/invite.go                       # 1

# контроль — КОМАНДОЙ и в правильной единице: сравниваются ВСЕ тесты значения с теми,
# что стоят в слитом условии. Равенство означает, что раздельной формы не существует:
git grep -c 'len(actives)' "$R" \
  -- services/iam/internal/apps/kacho/api/user/invite.go                       # 1 — всего
#   1 и 1 ⇒ единственная проверка значения и есть слитая; ветки, отличающей «сбой чтения»
#   от «активных нет», в файле НЕТ — поэтому снаружи они неотличимы

# объединение трёх источников существует
git grep -n 'ListAccountsForUser' "$R" -- services/iam | wc -l                 # 29 (было 20 на 43d332ac)
git grep -n 'ListAccountsForUser' "$R" -- services/iam | grep -vc '_test\.go'  # 8 — прод-часть (было 6)
#   Два числа приведены НАМЕРЕННО: единица разная (все вхождения против непробных),
#   и обе истинны. Одно число без названной единицы здесь было бы неотличимо от ошибки.
#   Круг 9: выросли ОБА, и это рост потребителей, а не смена смысла — объединение
#   по-прежнему то же, третий источник по-прежнему не зависит от снимаемой колонки
```

> [!warning] Контроль запасного пути стоял ПРОЗОЙ и, прочитанный буквально, был неверен
> Прежняя редакция писала: «раздельная форма (`if err != nil {` рядом с `len(actives)`) даёт
> **0**». Буквально — `git grep -c 'if err != nil {' … invite.go` → **8**, а не 0. Проза
> имела в виду «раздельной формы **на этом месте** нет», но набранная по её словам команда
> отвечает про весь файл и опровергает утверждение, которое должна была подпирать.
>
> Два урока, и второй общий. Первый: **единица счёта здесь — тесты значения, а не вхождения
> идиомы**; проверка ошибки в Go встречается в любом файле десятками, и считать её бессмысленно.
> Второй: попытка починить это многострочным предикатом (`grep -E` через `\n`) была бы
> **неисполнимой** — `git grep` многострочных совпадений не делает, команда молча вернула бы
> пустоту, которую читают как ноль (§1.0 п. 1). Обе версии написаны и обе отвергнуты замером;
> записано, чтобы следующий не прошёл тот же круг.
>
> Числа перемерены на обеих ревизиях: `len(actives)` → 1 и слитое условие → 1 и на
> `554a8d49`, и на `43d332ac`.

### 1.5 Контракт: где сегодня стоит аккаунт — пять мест, четыре разных смысла

```sh
git grep -n "account_id" "$R" -- proto/kacho/cloud/iam/v1/user.proto \
                                 proto/kacho/cloud/iam/v1/user_service.proto
```

**Координата — имя сообщения и поля, а не номер строки** (номер устаревает от любого коммита
выше по файлу, и проверить его нечем). Предикат каждой строки — в последнем столбце; `msg` —
вырезка блока сообщения по его границам, а не окно фиксированной высоты:

```sh
U=proto/kacho/cloud/iam/v1/user_service.proto
msg() { git show "$R":$U | awk -v v="message $1 " 'index($0,v)==1{f=1} f{print} f&&/^\}$/{exit}'; }
# контроль в обратную сторону: `msg GetUserRequest | grep -c account_id` → 0
```

| # | Что | Что значит сегодня | Исход | Предикат |
|---|---|---|---|---|
| 1 | `User.account_id = 6` — **СНЯТО, исход исполнен** | было: домашний аккаунт человека | **исполнено**: поля нет, `reserved 6` и `reserved "account_id"` объявлены | `git grep -c 'string account_id = 6' "$R" -- proto/kacho/cloud/iam/v1/user.proto` → **0** (было 1); подтверждение — `git grep -cE 'reserved 6;\|reserved "account_id"' "$R" -- proto/kacho/cloud/iam/v1/user.proto` → **2** |
| 2 | `InviteUserRequest.account_id = 1`, `required` | аккаунт, **в который** приглашают; вход анти-BOLA-гейта | **переезжает вместе с глаголом** на ресурс членства | `msg InviteUserRequest \| grep -c account_id` → 1 |
| 3 | `ListUsersRequest.account_id = 4` | сужение области списка | **снять**; вопрос «кто в этом аккаунте» отвечает список членств. **У поля есть живые потребители** — §1.5а | `msg ListUsersRequest \| grep -c 'account_id = 4'` → 1 |
| 4 | внутренние сообщения, `account_id = 2` | ось арендатора **операции** | **остаются** — другой предмет | предикат класса 8 §1.5а (перепись по имени поля) |
| 5 | `scope_extractor.from_request_field: "account_id"` — у `Invite` **и у `RemoveFromAccount`** | объект, про который спрашивает гейт | у `Invite` — переезжает вместе с п. 2; у `RemoveFromAccount` — **остаётся**: глагол посажен и аккаунт есть его область по построению (§2.5) | `git grep -c 'from_request_field: "account_id"' "$R" -- proto/kacho/cloud/iam/v1/user_service.proto` → **2** (было 1) |

**Поле №2 нельзя просто вырезать.** У `Invite` объявлены `required_relation = "editor"`,
`scope_extractor { object_type: "account", from_request_field: "account_id" }` и
`required_acr_min = "2"`. Поле — это то, **про что** гейт задаёт вопрос; без него у гейта нет
объекта, вызов становится unscoped и fail-closed, то есть глагол перестаёт работать вовсе.
Единственный законный исход — перенести глагол вместе с полем и всеми тремя опциями.

```sh
U=proto/kacho/cloud/iam/v1/user_service.proto
# блок ИМЕННО этого глагола, а не окно фиксированной высоты
blk() { git show "$R":$U | awk -v v="  rpc $1 " 'index($0,v)==1{f=1} f{print} f&&/^  \}$/{exit}'; }

blk Invite | grep -cE 'required_relation|scope_extractor|required_acr_min'   # 3 — все три
blk List   | grep -cE 'required_relation|scope_extractor|required_acr_min'   # 0 — контроль

# полоса `List` — СУЖЕНИЕ НА ДАННЫХ, а не освобождение (круг 9, было наоборот):
blk List   | grep -c '"<exempt>"'                                            # 0 — было 1 на 43d332ac
blk List   | grep -c 'scope_filtered'                                        # 2 — объявление и комментарий
# Круг 11: здесь стояла форма `permission += +"[^"]+"`, и она печатала ПУСТОТУ — между
# `permission` и `=` стоит `)`, которого в выражении не было. По §1.0 п. 1 предикатом такая
# строка не является. Несущая пара выше (`scope_filtered` 2 · 0) работала, поэтому вывод §1.5
# не менялся ни на один знак — менялась проверяемость утверждения.
blk List   | grep -oE 'permission\)[[:space:]]*=[[:space:]]*"[^"]+"'          # permission)     = "iam.users.list"
# КОНТРОЛЬ в обратную сторону — тот же признак у СОСЕДНЕГО глагола, где его быть не должно:
blk Invite | grep -c 'scope_filtered'                                        # 0
```

> [!warning] Окно `grep -A<N>` здесь **лжёт в обе стороны** — проверено кругом 3
> Первая редакция этого предиката резала блок как `grep -A14 'rpc Invite'` и давала **2**:
> объявление повышенного уровня стоит ниже четырнадцатой строки, за длинным комментарием.
> Контроль `grep -A14 'rpc List'` давал **3** — окно съезжало в блок `Invite` ниже, и
> отрицательный контроль отвечал больше положительного. Предикат с фиксированным окном над
> вложенным синтаксисом не измеряет свойство **никогда**; блок берётся по границам блока.

**Поле №3 — отдельный класс, и он серьёзнее, чем выглядит.** `UserService.List` несёт
`permission = "iam.users.list"` и `scope_filtered = true`: за него отвечает **не** единый
per-RPC Check, а **пообъектное сужение на данных** — обработчик читает страницу курсором и
спрашивает модель про идентификаторы **этой** страницы, оставляя только людей тех аккаунтов,
где вызывающий состоит. Сегодня «состоит» выражается через `users.account_id`. Отрыв меняет
то, чем выражается членство, значит **предикат сужения обязан быть перевыражен через членства
в том же изменении** — и перевыражение идёт **внутри уже действующей полосы**, а не
восстанавливает прежнюю. Это не косметика: список пользователей — та поверхность, где
ослабление сужения превращается в перечисление. Сценарий IAM-ID-1-21 утверждает **исход**
сужения, а не факт его вызова.

> [!warning] Круг 9: здесь стояло «объявлен `permission = "<exempt>"`» — полоса СМЕНИЛАСЬ, и
> ошибка была направлена в сторону СНЯТОЙ защиты
> Предикат прежней редакции — `blk List | grep -c '"<exempt>"'` → **1** с контролем
> `blk Invite` → **0**. Сегодня **оба конца дают 0**: команда перестала различать глаголы и
> отвечает одинаково на предмете и на контроле. Предикат, у которого обе стороны совпали,
> не измеряет свойство (§1.0 п. 2) — и молчит он ровно так же, как молчал бы на исправном
> дереве.
>
> **Что сменилось в дереве.** Шесть списочных глаголов iam ушли из освобождения в сужение на
> данных (`91a04dc16`). Полосу выбрали не по вкусу: контракт объясняет выбор в самом блоке
> глагола — у `<exempt>` есть **второй смысл**, при котором решение принимается не по личности
> вызывающего, а пообъектное сужение без личности бессмысленно; значит лоток обязан быть тот,
> который идёт **после** её извлечения.
> Это в точности `security.md` §«Отношение, выполнимое подстановочным знаком»: глагол, чей
> ответ касается объектов с индивидуальными владельцами, единого объекта для одного вопроса
> не имеет **by construction**, и авторизуется на уровне данных.
>
> **Почему это блокировало вердиктом круга 8.** Сценарий `21` — действующий, приземляется
> шагом S3.3, и в него же круг 7 внёс остаток `66`. Исполнитель, читающий «объявлен
> `<exempt>`», пошёл бы искать освобождение, не нашёл — и либо вернул бы его, либо
> остановился. Первое есть **откат чужой защитной работы**, и откат тихий: `<exempt>` в
> каталоге выглядит как обычная запись.
>
> **Предикат заменён на различающий** (блок выше): `scope_filtered` у `List` → **2** против
> **0** у `Invite`. Обе стороны разные, значит команда мерит полосу, а не находит слово.

### 1.5а Потребители снимаемого — и почему перепись делится на две разные работы

Снять поле, у которого есть живой потребитель, значит выкатить сломанную поверхность.
Но **способ найти потребителей у типизированного и нетипизированного потребителя разный**, и
смешивать их в одну перепись — ошибка, которая стоила бы пропущенного класса.

**Типизированные потребители перечисляет КОМПИЛЯТОР, а не grep.** Снятие поля структуры
делает каждое его чтение ошибкой сборки: перечень исчерпывающий, получается бесплатно и не
зависит от того, как назвали переменную. Мерить их грепом — мерить соглашение об именовании:

```sh
# предикат по имени переменной
git grep -n "\.AccountID" "$R" -- services/iam/internal \
  | grep -v _test | grep -icE "u\.AccountID|user\.AccountID|usr\.AccountID"     # 15 (было 16)
```

**Пятнадцать — это пол, а не число** (круг 9: было 16 на `43d332ac`; величина пола дрейфует
от любой правки, и это ровно то, почему она названа полом, а не числом). Второй предикат, по другому признаку, немедленно
находит пропущенные тем же классом: четыре чтения через `primary.AccountID`
(`service/token_enrichment_service.go:430` и `:434`,
`handler/iamhooks/refresh_hook_handler.go:262`,
`apps/kacho/api/user/internal_on_recovery.go:281`) — то есть предикат A промахнулся мимо
**самых дорогих** мест. Поэтому исчерпывающую перепись прод-кода в приёмке **не объявляем**:
её производит сборка на S4, и это правильный механизм. Приёмка обязана назвать не список, а
**решения по тем местам, где смысл меняется**, — они ниже.

**Нетипизированные потребители не найдёт никто, кроме переписи.** JSON края, HCL провайдера,
оболочечный скрипт, кейс сквозной пробы, отношение модели прав — сборка о них не знает, и они
ломаются **молча**. Это и есть настоящий предмет §1.5а.

> [!note] Номера строк в столбце «Координаты» — снимок `554a8d49`, а не действующая ссылка
> Авторитетен **предикат класса** (блок под таблицей): он ключуется на имя символа или на
> текст, а не на позицию, и потому переживает правку файла. Номер строки устаревает от любого
> коммита выше по файлу, молча и без конфликта; переписывать здесь все шестьдесят цитат
> поимённо круг 3 не стал осознанно — это завело бы шестьдесят новых возможностей ошибиться
> ради утверждений, которые уже проверяемы (§11.2 п. 9).

| # | Класс | Предмет | Координаты | Что происходит без перевода |
|---|---|---|---|---|
| 1 | **консоль, фильтр списка** | `listUsers({ account_id })` — «кто состоит в этом аккаунте» | `ui-future/iam/src/pages/iam/AccessPage/AccessPage.tsx:67` и `:245` | край отбрасывает неизвестное поле **молча**: обе страницы покажут состав всей видимой вселенной вместо аккаунта. Отказа не будет вовсе |
| 2 | **провайдер инфраструктуры** | ресурс `kacho_iam_user_invitation`, `account_id` — **Required** | `terraform/internal/provider/iam_user_invitation_resource.go:84`, `:173`; потребители — `terraform/modules/iam-project/` и `tests/wiring.tftest.hcl:88` | ресурс перестаёт применяться: глагол, который он зовёт, снят целиком |
| 3 | **писатели отношения принадлежности на объекте личности** — прежде оперативный скрипт восстановления, **сегодня типизированный прод-код** | кортеж `account:<acc>` → `account` → `iam_user:<id>`, значение берётся из снимаемой колонки | оперативный скрипт восстановления прав в консольном дереве **удалён** (`c78901a33`, работа этого же перехода; путь намеренно не воспроизводится координатой — файла нет); писатели живут в `apps/kacho/api/user/`: `invite.go` · `internal_upsert.go` (два места) · `delete.go` · `remove_from_account.go` (`membershipScopeTuple`) | без перевода кортеж продолжает утверждать «личность принадлежит аккаунту» единственным числом там, где аккаунтов станет несколько. **Отказ теперь виден СБОРКЕ** — источник значения типизирован |
| 4 | **утверждения токена** | `kacho_active_account` и `kacho_account_id` собираются из аккаунта строки | `service/token_enrichment_service.go:430`, `:434`, `:545`, `:546`; `handler/iamhooks/refresh_hook_handler.go:323` | у обоих исчезает источник; для человека в N аккаунтах «активный аккаунт» **не определён by construction** |
| 5 | **сквозные пробы** — **ДВА** утверждающих исходника, а не один | захватывают `kacho_account_id` и **утверждают его непустоту** | `services/iam/tests/newman/cases/iam-token-facade-conformance.py` (**5** вхождений, было 2) и `services/iam/tests/newman/scripts/selftest_token_facade_forms.py` (самопроверка форм: подаёт утверждение пустым и требует падения) | обе краснеют, а замаскировать их нельзя (`testing.md` §E2E) — значит S4 планово роняет прогон, и **радиус планового красного вдвое шире**, чем считал круг 4 |
| 6 | **каскад супер-доступа** | факт «объект личности принадлежит аккаунту» — **производителей ТРИ, и решающий назван третьим** | (а) Go-функция `domain.AccountScopedStructuralFact` — **жива, вызывающих ноль**; (б) план чтения материализации `iamDirectScanSpecs`, `services/iam/internal/repo/kacho/pg/reconcile_adapter.go` — для `iam.user` **уже переведён**: `parentAccountsExpr` над `kacho_iam.memberships`, а не `parentAccountExpr: "o.account_id"`; (в) **решающий — звено цепи областей `kacho_iam.resource_scope_edge`**, ветвь (4a): его читает `admin from account`, и он **уже переведён** миграцией `944001_identity_scope_comes_from_membership.sql` | у каскада над объектом личности не остаётся источника; при N членствах он вдобавок перестаёт быть однозначным. **Отказ РАНТАЙМНЫЙ и тихий**: источник — строка SQL, сборка о ней не знает. **Что здесь ОСТАЛОСЬ работой S3** — не перевод источника (он сделан), а **смена объекта**, про который спрашивает гейт (§2.3а) |
| 7 | **самовалидация домена** | требует непустоты аккаунта у строки | `domain/user.go:104` | строка без аккаунта становится невалидной — то есть целевая форма невыразима в домене |
| 8 | **ось арендатора у операции** | метаданные операции несут поле аккаунта, и общая библиотека читает его **по точному имени** (`pkg/operations/repo.go:375`, `extractAccountID`) — от него зависит попадание операции в аккаунт-скоупный фид | **7** файлов контракта iam, **11** сообщений-метаданных (перепись 2026-08-16: `git grep -c "Read by exact field name .account_id." proto/kacho/cloud/iam/v1/`) | операция уходит не в тот фид либо не попадает никуда — и **отказа не будет**: поле читается по имени, а не по типу |
| 9 | **снимок прав собственного профиля** | ответ **на вопрос о себе** несёт перечень аккаунтов, собираемый из **трёх** источников одним `UNION`: активное членство при активной личности · владение · активная привязка уровня аккаунта (`authorize_service.proto`, `WhoAmIResponse`; сборка — `apps/kacho/api/authorize/whoami.go`, источник — `repo/kacho/pg/user_repo.go`, `ListAccountsForUser`) | публичный глагол собственного профиля; **параметра пользователя у запроса нет** — субъект берётся из контекста | членская часть снимка после отрыва получает **второго** производителя, если ресурс членства не станет её единственным источником. Это не «расхождение с ресурсом», а два написания одного факта — разойтись они обязаны, и видно это будет только арендатору |

> [!warning] Класс 8: радиус назван, но перепись НЕ закончена — и это разные вещи
> Одиннадцать сообщений несут поле, читаемое по имени. **Не все** берут значение из
> принадлежности строки пользователя: у аккаунта проекта свой источник, у служебной учётки —
> свой. Что именно питается снимаемой колонкой, устанавливается переписью **по источнику
> значения**, а не по имени поля, и она входит в S0. Здесь названо ровно то, что измерено:
> сколько мест этого класса существует и чем они опасны.
>
> Опасны они тем, что молчат: поле читается **по имени**, поэтому его исчезновение не ломает
> ни сборку, ни контракт — операция просто перестаёт попадать в фид, и заметно это только
> прогоном фида.

> [!warning] Класс 6: производитель не исчез, он ПЕРЕЕХАЛ ИЗ Go В SQL — и это опаснее, чем
> исчезновение (перемерено кругом 4)
> Круг 3 записал координату «`AccountScopedStructuralFact` в `services/iam/internal/authzcascade/`»
> и число **7**. На `43d332ac` их **4**, и все четыре — не то, чем были: объявление функции,
> её godoc и две прозаические ссылки (в комментарии соседнего читателя и в комментарии
> миграции). **Вызовов — ноль.** Четыре места, строившие факт из `string(u.AccountID)`,
> `string(g.AccountID)`, `string(ro.AccountID)` и `string(sa.AccountID)`, исчезли вместе с
> файлами: пакет `authzcascade` ужался с 10 файлов до 3, `repo/kacho/pg/structural_facts_repo.go`
> в дереве больше нет.
>
> **Если остановиться здесь, вывод получится ровно обратный истинному.** «Производителя не
> осталось» означало бы, что класс 6 закрылся сам и переводить нечего. Это неверно: факт
> «объект личности принадлежит аккаунту» продолжал производиться — **SQL-выражением** в плане
> чтения материализации и **звеном цепи областей** `kacho_iam.resource_scope_edge` в базе. Тот
> же смысл, тот же источник (снимаемая колонка), другой слой.
>
> **Круг 7: ОБА SQL-производителя для `iam.user` уже переведены на членство** — план чтения
> (`parentAccountsExpr` над `kacho_iam.memberships`) и ветвь (4a) звена цепи областей
> (миграция `944001`). Работой S3 здесь остаётся **не перевод источника**, а **смена
> объекта**, про который спрашивает гейт (§2.3а): пока объектом остаётся личность, второе
> членство даёт администратору **любого** аккаунта доступ к общему объекту.
>
> **Почему переезд ухудшил положение, а не улучшил:**
>
> | | было (Go) | стало (SQL-строка) |
> |---|---|---|
> | что делает снятие колонки | **ошибка сборки** в четырёх местах | **ничего** на сборке |
> | когда проявится | до коммита | в рантайме, на пути авторизации |
> | как проявится | компилятор называет файл и строку | материализация тихо не находит предка ⇒ доступ **не выдаётся** |
> | чем ловится | компилятором, бесплатно и исчерпывающе | только пробой на исход |
>
> То есть класс 6 переехал из «типизированных потребителей, которых перечисляет компилятор»
> в «нетипизированных, которых не найдёт никто, кроме переписи», — а это, по вступлению к
> §1.5а, **и есть настоящий предмет этой главы**. Он попал в неё, не будучи туда переписан.
>
> **Радиус — ТРИ записи, и `iam.user` среди них БОЛЬШЕ НЕТ** (перемерено кругом 7 на
> `release/identity-4` `8a3e00f18`). Выражение `parentAccountExpr: "o.account_id"` стоит в
> записях `iam.project`, `iam.group`, `iam.serviceAccount` — у всех трёх `account_id` их
> **собственная** колонка в их **собственной** таблице, и §2.4 п. 6 её не трогает. Запись
> `iam.user` выводит предка иначе:
>
> ```sh
> A=services/iam/internal/repo/kacho/pg/reconcile_adapter.go
> grep -c 'parentAccountExpr: "o.account_id"' "$A"                       # 3
> grep -B3 'parentAccountExpr: "o.account_id"' "$A" | grep -oE 'objectType: "[^"]+"'
> #   → iam.project · iam.group · iam.serviceAccount — iam.user ОТСУТСТВУЕТ
> # чем он выводит предка сегодня — предикат называет источник, а не отсылает к чтению файла:
> grep -A3 'objectType: "iam.user"' "$A" | grep -c 'kacho_iam.memberships'   # 1
> # контроль в обратную сторону: предикат различает выражения, а не находит слово
> grep -c 'parentAccountExpr: "o.membership_id"' "$A" || echo 0          # 0
> ```
>
> **Это не косметика числа: разница есть работа, которую раздел заказывал бы ПОВТОРНО.**
> Круг 4 измерил **4** и был прав про `43d332ac`. Сегодня перевод `iam.user` **сделан** —
> и на уровне плана чтения, и на уровне звена цепи областей (`944001`). Исполнитель,
> читающий «четыре», пошёл бы переводить уже переведённое: тот же класс, ради которого круг 6
> чинил строки «S3 НЕ НАЧАТА» (§4).
>
> **Что осталось незакрытым — названо, чтобы «сделано» не прочиталось шире.** Переведён
> **источник** факта; **объект**, про который спрашивает гейт, по-прежнему личность, а не
> членство. Пока членств у каждого одно, дельта пуста; на втором членстве администратор
> **любого** аккаунта достанет до общего объекта личности. Это и есть S3.1 (§2.3а).
>
> **Мёртвая функция — отдельный предмет, и этот документ её не заказывает.**
> `AccountScopedStructuralFact` без единого вызывающего есть declared-but-never-called
> (`architecture.md` §LEAN, ban #11). Снимать её **в этом переходе нельзя**: она не наш предмет,
> и её удаление смешало бы уборку с отрывом. Названа здесь, потому что её присутствие делает
> координату класса 6 обманчиво живой: греп по имени находит символ и создаёт вид, что
> производитель на месте.

> [!warning] Класс 9 перевыражен кругом 6: прежнее требование было НЕИСПОЛНИМО, а удалить
> его нельзя — забота верна
> **Что здесь стояло.** «Проба обязана читать оба пути и требовать совпадения **по составу**»
> — то есть чтобы состав снимка прав и состав своего списка членств **совпадал**.
>
> **Почему это неисполнимо by construction, а не «трудно».** Множества собираются по разным
> правилам, и правила эти — контракт, а не случайность. Измерено на `5533d21e`, предикат
> ниже:
>
> | ось | снимок прав (`WhoAmI`) | свой список членств |
> |---|---|---|
> | состояние членства | только **`ACTIVE`** | **`ACTIVE` и `PENDING`** — приглашённый видит, куда приглашён |
> | не-членские источники | **владение** и активная **привязка уровня аккаунта** входят | не входят **by construction**: владеть ≠ состоять (`PRO-Robotech/kacho#610`, сняты 09/10) |
> | состояние **личности** | членская часть отсекается `u.invite_status = 'ACTIVE'` — у **заблокированного** снимок членской части не отдаёт | членства заблокированного остаются `ACTIVE`: блокировка есть свойство личности, а не членства |
>
> То есть для **приглашённого** — ровно того, ради кого писалась директива владельца, —
> совпадение невозможно: его членство в списке есть, в снимке его нет и быть не должно
> (приглашение доступа не даёт). Требование «составы совпадают» краснело бы на верной
> реализации и зеленело бы только на сломанной.
>
> **Забота класса при этом ВЕРНА и обязана уцелеть:** один факт не должен иметь двух
> написаний. Перевыражается так:
>
> 1. **производитель один** — ресурс членства. Ни снимок, ни список не вычисляют членство
>    своим выражением; оба читают одну таблицу;
> 2. **проекции две, и различие НАЗВАНО** — таблицей выше, в контракте обоих чтений, а не в
>    голове реализующего;
> 3. **проба утверждает включение с названной разностью, по ЧЛЕНСКОЙ части:** множество
>    снимка, суженное до членских источников, **⊆** множества своего списка, и разность —
>    **ровно** записи в состоянии «приглашён». Отдельным утверждением: аккаунт, попавший в
>    снимок **не через членство** (владение либо привязка), в список членств **не попадает**,
>    и это не потеря;
> 4. **положительный контроль обязателен** — у человека **без приглашений и без
>    владения-без-членства** оба ответа совпадают **элемент в элемент**. Без него
>    «включение» зеленело бы на поверхности, отвечающей **пустым списком**: пустое множество
>    вложено во всякое.
>
> **Стадия и сценарий у класса 9 теперь есть.** До круга 6 их не было ни одного: класс
> отсутствовал в таблице «каждый класс переводится в той же стадии» и не имел
> сценария-производителя — то есть требование существовало, а исполнять его было **некому**.
> Стадия — **S3**, сценарий — **IAM-ID-1-73**.

Предикаты — **каждому классу свой**, общим грепом они слипаются. Ревизия — из §1.0; прежняя
редакция писала здесь `origin/main`, то есть читала движущуюся ссылку при числах, стоящих на
неподвижной (§11.2):

```sh
# 1 консоль
git grep -n "listUsers({" "$R" -- ui-future/ | grep -c account_id                        # 2
# 2 провайдер инфраструктуры
git grep -ln "kacho_iam_user_invitation" "$R" -- terraform/ | wc -l                      # 4 файла
# 3 писатели отношения принадлежности на объекте личности.
#   ПРЕЖНИЙ предикат читал ui-future/ и сегодня НЕ РАЗЛИЧАЕТ — оба конца дают 0,
#   потому что скрипт удалён целиком (см. выноску ниже):
git grep -nE '"account"[[:space:]]+"iam_user:|iam_user:[^ ]*#account@account:' "$R" \
  -- ui-future/ | wc -l                                                                  # 0 (было 1)
git grep -nE '"account"[[:space:]]+"project:|project:[^ ]*#account@account:' "$R" \
  -- ui-future/ | wc -l                                                                  # 0 (было 1) — контроль умер вместе с предметом
#   ДЕЙСТВУЮЩИЙ предикат — производители кортежа в прод-коде, единица «место записи»;
#   ключуется на пару «отношение + тип объекта», а не на форму строки:
git grep -nE -A2 'Relation: *"account"' "$R" -- services/iam/internal \
  | grep -v '_test\.go' | grep -c 'Object: *fmt.Sprintf("iam_user:\|Object: *"iam_user:'   # 5 (было 4 на 43d332ac)
#   контроль в обратную сторону — то же отношение на ДРУГИХ типах объекта обязано найтись,
#   иначе пятёрка читалась бы как «предикат находит любое упоминание»:
git grep -nE 'Relation: *"account"' "$R" -- services/iam/internal | grep -vc '_test\.go'   # 15
#   зеркало, обязанное остаться нулевым до S3: отношения членства ещё не пишет никто
git grep -nE 'Relation: *"membership"' "$R" -- services/iam/internal | grep -vc '_test\.go' # 0
# 4 утверждения токена — ПИШУТСЯ (кладутся в claims)
git grep -nE '"kacho_account_id"[[:space:]]*[:=]|claims\["kacho_account_id"\]' "$R" \
  -- 'services/iam/internal/*.go' | grep -vc '_test\.go'                                 # 4
git grep -nE '"kacho_active_account"[[:space:]]*[:=]|claims\["kacho_active_account"\]' "$R" \
  -- 'services/iam/internal/*.go' | grep -vc '_test\.go'                                 # 5
# 4' и ЧИТАЮТСЯ (значение достаётся обратно) — вот на чём стоит решение В-7 §7
git grep -n 'getString(enriched, "kacho_account_id")'     "$R" -- 'services/iam/internal/*.go' | wc -l   # 0
git grep -n 'getString(enriched, "kacho_active_account")' "$R" -- 'services/iam/internal/*.go' | wc -l   # 1
# 5 кейс сквозной пробы утверждает НЕПУСТОТУ утверждения
git grep -c "kacho_account_id" "$R" \
  -- services/iam/tests/newman/cases/iam-token-facade-conformance.py                      # 5 (было 2)
#   и утверждающих ИСХОДНИКОВ теперь ДВА, а не один — единица тут решает всё:
git grep -ln "kacho_account_id" "$R" -- services/iam/tests/ | grep -vc 'collections/'      # 2
#   → cases/iam-token-facade-conformance.py · scripts/selftest_token_facade_forms.py
#   порождённая коллекция из счёта исключена намеренно: она артефакт, а не исходник,
#   и правится генератором. Без исключения число было бы 3 и мерило бы генерацию
# 6 каскад — ДВА предиката, потому что производитель переехал (круг 4)
#   6a Go-функция: жива, но ВЫЗЫВАЮЩИХ НЕТ. Считать её вхождения бессмысленно —
#      4 из 4 суть объявление, godoc и две прозаические ссылки:
git grep -n "AccountScopedStructuralFact" "$R" -- services/iam/internal | grep -vc '_test\.go'  # 4
git grep -nE 'AccountScopedStructuralFact\(' "$R" -- services/iam/internal \
  | grep -v '_test\.go' | grep -vc 'func AccountScopedStructuralFact'                            # 0 — вызовов нет
#      контроль в обратную сторону, на ревизии, где вызовы были:
git grep -nE 'AccountScopedStructuralFact\(string\(' 554a8d49 -- services/iam/internal \
  | grep -vc '_test\.go'                                                                         # 4 на 554a8d49
#   6b план чтения материализации — iam.user из него УШЁЛ (перемерено кругом 7, 8a3e00f18):
git show "$R":services/iam/internal/repo/kacho/pg/reconcile_adapter.go \
  | grep -c 'parentAccountExpr: "o.account_id"'                                                  # 3 типа
#      какие именно — предикат называет их сам, а не отсылает к чтению файла:
git show "$R":services/iam/internal/repo/kacho/pg/reconcile_adapter.go \
  | grep -B3 'parentAccountExpr: "o.account_id"' | grep -oE 'objectType: "[^"]+"'
#      → iam.project · iam.group · iam.serviceAccount — iam.user ОТСУТСТВУЕТ (было 4 на 43d332ac)
#      чем iam.user выводит предка сегодня — источник назван, а не подразумевается:
git show "$R":services/iam/internal/repo/kacho/pg/reconcile_adapter.go \
  | grep -A3 'objectType: "iam.user"' | grep -c 'kacho_iam.memberships'                          # 1
#      контроль в обратную сторону: та же форма с несуществующим выражением
git show "$R":services/iam/internal/repo/kacho/pg/reconcile_adapter.go \
  | grep -c 'parentAccountExpr: "o.membership_id"' || echo 0                                     # 0
#   6c РЕШАЮЩИЙ производитель — звено цепи областей, читаемое `admin from account`.
#      Его ветвь (4a) переведена на членство миграцией S3, и это ЕДИНСТВЕННОЕ место,
#      где факт «личность принадлежит аккаунту» участвует в РЕШЕНИИ о доступе:
git cat-file -e "$R":services/iam/internal/migrations/944001_identity_scope_comes_from_membership.sql \
  && echo есть                                                                                   # есть
git show "$R":services/iam/internal/migrations/944001_identity_scope_comes_from_membership.sql \
  | grep -c 'resource_scope_edge'                                                                 # ≥1
#      контроль в обратную сторону — представление заведено НЕ этой миграцией, а более ранней;
#      предикат обязан находить ВСЮ цепочку, иначе он мерит «кто написал последним», а не
#      производителя. Единица — ФАЙЛ миграции, поэтому проба рядом из счёта исключается:
git grep -l 'resource_scope_edge' "$R" -- services/iam/internal/migrations \
  | grep -c '\.sql$'                                                                              # 4
#      → 740001 (заводит) · 781001 · 785001 · 944001 (переводит ветвь (4a) на членство)
# 7 самовалидация домена ОТВЕРГАЕТ строку без принадлежности
git grep -c 'Illegal argument account_id: required' "$R" \
  -- services/iam/internal/domain/user.go                                                  # 1
#   контроль в обратную сторону: тот же текст в `domain/account.go` — 0.
#   Считать здесь вхождения `AccountID` НЕЛЬЗЯ: их 3 у пользователя и 1 у аккаунта,
#   но у аккаунта это ОБЪЯВЛЕНИЕ ТИПА поля, а не проверка. Предикат обязан ключеваться
#   на текст отказа — он и есть наблюдаемое требование
# 8 ось арендатора операции
git grep -c "Read by exact field name .account_id." "$R" -- proto/kacho/cloud/iam/v1/ | wc -l   # 7 файлов
git grep -c "Read by exact field name .account_id." "$R" -- proto/kacho/cloud/iam/v1/ \
  | awk -F: '{s+=$NF} END{print s}'                                                        # 11 сообщений
# 9 снимок прав собственного профиля — предикат ключуется на ИСТОЧНИК множества,
#   а не на имя сообщения: имя переживает смену правил сборки, а правила и есть предмет
git grep -n "WhoAmIResponse" "$R" -- proto/ services/iam/internal | grep -vc '_test\.go'  # 6
#   9' три источника перечня — по одному условию на источник, в ОДНОМ выражении:
src() { git show "$R":services/iam/internal/repo/kacho/pg/user_repo.go \
        | awk '/func \(r \*userReader\) ListAccountsForUser/{f=1} f{print} f&&/^}$/{exit}'; }
src | grep -c "m.state = 'ACTIVE' AND u.invite_status = 'ACTIVE'"   # 1 — членство + личность
src | grep -c 'FROM accounts WHERE owner_user_id'                    # 1 — владение
src | grep -c "resource_type = 'account' AND status = 'ACTIVE'"      # 1 — привязка уровня аккаунта
#   контроль в обратную сторону — состояние «приглашён» в этом выражении не упоминается
#   вовсе, то есть снимок его не отдаёт и отдать не может:
src | grep -c "PENDING" || echo 0                                    # 0
```

**Перемерено кругом 9 на `475f8527`.** Не изменились классы **1** (2), **2** (4 файла),
**4** (4 и 5), **4'** (0 и 1), **6** (все семь чисел: 4/0/4 · 3 типа · 1 · 0 · миграция есть ·
4 файла), **7** (1 при контроле 0), **8** (7 файлов / 11 сообщений), **9** (6 и три источника
по 1 при контроле 0). Разошлись **два**: класс **3** — предмет исчез, предикат заменён
(выноска выше), и класс **5** — 2 → **5** вхождений при **двух** утверждающих исходниках.
Ниже — история круга 4 о ложном нуле класса 3; она сохранена, потому что объясняет, почему
предикат этого класса переписывался уже дважды:

> [!warning] Круг 9: предмет класса 3 ИСЧЕЗ и вернулся другим родом — скрипт удалён ЭТИМ ЖЕ
> переходом, а забота переехала в типизированный код
> **Что произошло с деревом.** Оперативный скрипт восстановления прав **удалён целиком**
> (177 строк) коммитом `c78901a33 [#944] Аккаунт человека читается из членства, а не из
> колонки строки`. Его путь здесь намеренно **не воспроизводится координатой**: имя в
> обратных кавычках читается как живое утверждение о дереве, и разбор снятия сам стал бы
> его повторением
> — то есть работой **самого этого перехода**. Скриптов восстановления прав в дереве больше
> нет ни одного: `git grep -ln 'fga_ensure' 475f8527` → **пусто**.
>
> **Следствие для предиката, и оно то самое, от которого глава защищалась.** Прежняя команда
> даёт **0** — и её контроль на соседнем объекте тоже **0**. Оба конца совпали, значит
> команда перестала измерять свойство и молчит одинаково на «предмета нет» и на «предикат
> негоден». Круг 4 записал этот же класс с **обратной** стороны — ложный ноль на **живом**
> потребителе; сегодня ноль честный, но нечитаемый.
>
> **Забота класса при этом НЕ исчезла, и это главное.** Кортеж
> `account:<acc>` → `account` → `iam_user:<id>` продолжает писаться, а значение берётся из
> снимаемой колонки. Мест записи **пять** (было **4** на `43d332ac`): путь приглашения
> (`invite.go`, `out.user.AccountID`), два места пути активации (`internal_upsert.go`),
> удаление строки (`delete.go`) и — пятое — **посаженный глагол исключения из аккаунта**
> (`remove_from_account.go`), который тот же кортеж **снимает**.
>
> **Класс сменил РОД, и смена эта в нашу пользу — зеркало класса 6.** Класс 6 переехал из Go
> в SQL, то есть из «перечисляет компилятор» в «не найдёт никто, кроме переписи». Класс 3
> проделал обратный путь:
>
> | | было (оболочечный скрипт) | стало (типизированный Go) |
> |---|---|---|
> | что делает снятие колонки | **ничего** на сборке | **ошибка сборки** в пяти местах |
> | когда проявится | при запуске восстановления — то есть когда уже сломалось | до коммита |
> | чем ловится | только переписью | компилятором, бесплатно и исчерпывающе |
>
> **Что из этого следует для стадии.** Требование «привести оперативный скрипт восстановления
> к новому смыслу отношения» **исполнять нечем** — предмета нет. Критерий завершённости,
> который нечем исполнить, критерием не является (дословная формулировка круга 6 по Б3),
> поэтому строка §4 заменена на то, что осталось предметом: **перевод пяти писателей**, и он
> обеспечен сборкой, а не дисциплиной. Пятый писатель уже сегодня называет свой кортеж
> `membershipScopeTuple` — дерево назвало предмет раньше, чем документ.

> [!warning] Предикат класса 3 давал ЛОЖНЫЙ НОЛЬ на живом потребителе — и это худший из
> возможных исходов для этой таблицы
> Прежний предикат — `git grep -n "iam_user:.*#account@account:" "$R" -- ui-future/` — на
> `554a8d49` давал **1**, на стволе круга 4 даёт **0**. Потребитель при этом **жив и
> семантически тождествен**: скрипт переписан так, что кортеж собирается не литералом, а
> тройкой аргументов — `fga_ensure "account:${account_id}" "account" "iam_user:${user_id}"`
> (сигнатура `fga_ensure <user> <relation> <object>` объявлена в том же файле). Тот же
> субъект, то же отношение, тот же объект. **Изменилась форма записи, не смысл.**
>
> **Чем это грозило.** Исполнитель S3, добросовестно перечитывающий предикат (а документ
> прямо этого требует, §1.0), получил бы «потребителя нет, переводить нечего» — и выкатил бы
> стадию со скриптом восстановления, работающим по **прежнему** смыслу отношения. Запускают
> такой скрипт именно тогда, когда доступ уже сломан; ошибка проявилась бы в момент, когда
> проверять её некому. Ноль, полученный от предиката, неотличим от ноля, полученного от
> дерева, — и здесь он был первым.
>
> **Класс — предикат, ключующийся на ФОРМУ ЗАПИСИ.** Он стареет **так же молча, как номер
> строки**, и по той же причине: и то и другое суть свойства текста, а не предмета. Номер
> строки мы из документа уже вывели (§11.2 п. 9), а форму записи — нет, потому что она
> выглядит содержательной: строка `iam_user:...#account@account:` читается как «отношение», хотя
> на деле это **синтаксис одного места вызова**. Починка — ключеваться на пару «отношение +
> тип объекта», которая переживает любой рефакторинг вызова, и проверить это **на всех четырёх
> ревизиях**: новый предикат даёт 1 на `554a8d49`, `90a54650`, `78d062bb` и `43d332ac`.
>
> **Почему прошлые круги этого не поймали — и почему их в этом не упрекнуть.** Круг 3 приписал
> сюда «числа 1-3, 5, 8 проверены и на стволе — не изменились», и это было **правдой**:
> прежний предикат даёт 1 на всех трёх ревизиях круга 3 (`554a8d49`, `90a54650`, `78d062bb`) и
> 0 только на `43d332ac`. Скрипт переписан **2026-08-20** (`901f0f4b8`) — через двое суток
> после круга 3. Утверждение было верным в момент записи и стало ложным **без единой правки
> документа**; заметить это было нечем, потому что документ лежал на гейте, а не в работе.
> Ровно тот случай, ради которого заведён §1.0а.

**Каждый класс переводится в ТОЙ ЖЕ стадии, где снимается его предмет:**

| Класс | Стадия | Сценарий |
|---|---|---|
| 1 консоль · 2 провайдер | S3 | IAM-ID-1-55, IAM-ID-1-56 |
| 3 скрипт · 6 каскад | **S3** (круг 3: вместе с правкой модели, §2.3а) | IAM-ID-1-31, IAM-ID-1-57 |
| 7 домен | S2 | IAM-ID-1-58 |
| 4 утверждения токена · 5 кейс пробы | S3 (решение) → S4 (снятие) | IAM-ID-1-59 |
| **9 снимок прав собственного профиля** | **S3** (круг 6) | **IAM-ID-1-73** |
| 8 ось арендатора операции | **S0** — перепись **по источнику значения**, не по имени поля | сценария не несёт **намеренно**: перепись есть замер, а не поведение (см. выноску класса 8) |

> [!note] До круга 6 в этой таблице не было ни класса 9, ни класса 8 — и молчание читалось
> как «этих классов нет»
> Класс 9 нёс требование («ресурс членства обязан стать единственным производителем») и не
> имел **ни стадии, ни сценария**: исполнять его было некому, и покраснеть было нечему.
> Требование без производителя неотличимо от отсутствующего — с той разницей, что создаёт вид
> покрытия. Класс 8 сценария не несёт по существу (его предмет — перепись, то есть замер), и
> теперь это **сказано**, а не подразумевается: строка «нет сценария» и пустое место в таблице
> выглядят одинаково только для того, кто таблицу не писал.

**Предмет задачи `kacho#473` закрыт решением «столбец снят»** и переходом не возобновляется:
консольная поверхность членств принадлежит приёмке IAM-ID-2 (§13.1). До круга 6 здесь стояло
«та задача про столбец на странице пользователей, стадия S5, — другая поверхность и другой
вопрос»; стадии S5 больше нет, и отсылка к ней была бы указанием в снятое.

**Куда переезжают классы 1 и 2 после сужения круга 6.** Оба спрашивают «кто состоит в **этом**
аккаунте» — то есть **вторую** из двух законных форм чтения (членство в названном аккаунте),
и переводятся именно на неё. Форма «в каких аккаунтах состоит названный человек» им не нужна
**и не заводится**: ни одному из двух мест она не отвечала бы на их вопрос. Это удача
измерения, а не проектное решение: перепись искала потребителей снимаемого поля и заодно
показала, что запрещённой формы среди их вопросов нет (IAM-ID-1-55).

**Попутно измерено, в предмет не входит:** оба консольных вызова читают `pageSize: "1000"`,
то есть берут аккаунт целиком одним запросом. Замена обязана быть курсорной — но это
требование принадлежит списку членств как **новому** глаголу, а не этому переходу; названо
здесь, чтобы форма «прочитать всё разом» не переехала вместе со старым вызовом.

### 1.6 Материализация личного аккаунта — предикат ворот

`services/iam/internal/apps/kacho/api/user/internal_upsert.go` (директива RC-5,
переворачивающая прежнее решение D-7): bootstrap срабатывает, когда у разрешённой/
активированной строки число **собственных** аккаунтов равно нулю («owns-zero-accounts gate»),
и заводит личный аккаунт, проект `default` и две самоадминистративные привязки
(`bootstrapPersonalResources`).

**Это и есть источник отношения 1:1 в замере — не совпадение, а прямое следствие правила.**
Директива распространяется на **любого** пользователя, включая приглашённого.

**Предикат — это тот самый предикат, который защищает директиву п. 3 §0.1.** Ворота обязаны
остаться «собственных аккаунтов ноль» и **не** стать «членств ноль»: вторая формулировка
отменяет директиву для приглашённого человека (§0.1, IAM-ID-1-16).

```sh
# ворота названы своим именем в самом коде — по нему их и находят
git grep -in 'owns-zero-accounts' "$R" -- services/iam | wc -l                 # 11
# производитель личных ресурсов существует и зовётся с пути появления человека
git grep -n 'bootstrapPersonalResources' "$R" -- services/iam | grep -vc '_test\.go'   # 4
# ЗЕРКАЛО, которое обязано остаться нулевым: ворот «ноль членств» в дереве нет
git grep -inE 'zero-memberships|memberships == 0|len\(memberships\) == 0' "$R" -- services/iam | wc -l   # 0
```

Третий предикат — **зеркальный**: он и есть механическая форма запрета из §0.1. Пока он даёт
ноль, подмены ворот не произошло; станет ненулевым — директива отменена, и это находка, а не
рефакторинг.

### 1.7 Консоль: столбца «Аккаунт» у пользователей БОЛЬШЕ НЕТ — перемерено кругом 3

```sh
spec() { git show "$1":ui-future/shared/src/lib/resource-registry.tsx \
         | awk '/^    id: "/{s=$0} /header: "Аккаунт"/{gsub(/[ ",]/,"",s); sub(/id:/,"",s); print s}'; }
spec 554a8d49    # projects  service-accounts  users  groups   → 4
spec 90a54650    # projects  service-accounts  groups          → 3
spec 78d062bb    # projects  service-accounts  groups          → 3
spec 43d332ac    # projects  service-accounts  groups          → 3
```

| Ревизия | Спек со столбцом | `users` среди них |
|---|---:|---|
| `554a8d49` (база APPROVED) | 4 | **да** |
| `90a54650` (ствол, круг 3) | **3** | **нет** |
| `78d062bb` (ветка работы круга 3) | **3** | **нет** |
| `43d332ac` (ствол, круг 4) | **3** | **нет** |

> [!warning] Здесь стояло «колонка стоит в четырёх спеках, радиус — одна спека `users`» —
> и предикат самого документа это опровергает
> Утверждение было верно на `554a8d49` и **перестало быть верным до того, как по нему начали
> работать**: столбец снят со спеки `users` решением по `PRO-Robotech/kacho#473` и влит в
> ствол. Причина снятия записана в самом дереве, рядом с местом, где столбец стоял: строка
> `users` — это **членство**, «аккаунты человека» столбцом не выражаются, а показать их
> нечем — такого поля нет ни на одном чтении контракта; поле без источника показывать
> запрещено (`ui.md` правило 9).
>
> Предикат: `spec 90a54650 | grep -c users` → **0**; контроль в обратную сторону —
> `spec 554a8d49 | grep -c users` → **1**.
>
> **Класс — тот же, ради которого написан §1.0:** число стояло на неподвижной ревизии,
> предикат читал `origin/main`. Расхождение обязано было появиться и появилось; заметить его
> было нечем, потому что документ не просил перечитывать предикат.

> [!warning] ЧТО ИЗ ЭТОГО СЛЕДУЕТ — переписано кругом 6: прежний вывод стоял на
> ОПРОВЕРГНУТОМ прочтении собственного свидетельства
> Здесь стояло: предмет S5 — «вернуть перечень членств вместе с ресурсом членства», радиус —
> «по-прежнему одна спека `users`, столбец заводится заново», форма — «первый элемент ссылкой
> + тег `+N`». Все три опираются на одно основание: будто дерево, снимая столбец, **обещало
> вернуть его перечнем**. Дерево обещает **другое**.
>
> **Что дерево говорит на самом деле** (перемерено кругом 6 на `5533d21e`; координаты
> продукта, предикат ниже):
>
> | Место | Что обещает дословно |
> |---|---|
> | комментарий на месте снятого столбца (`ui-future/shared/src/lib/resource-registry.tsx`) | «читаемая **принадлежность** вернётся вместе с ресурсом членства (#1085)» |
> | шапка расширения карточки (`ui-future/iam/src/registerExtensions.tsx`) | «Аккаунта в обзоре НЕТ… Вернётся вместе с ресурсом членства (#1085), не раньше» |
> | сквозная проба (`ui-future/e2e/specs/users.spec.ts`) | «появится ресурс членства — эта половина заменяется на **„карточка называет аккаунт членства“**… Предмет и предикат — задача про читаемую принадлежность (#1085)» |
>
> Все три говорят о **карточке** и адресуют задачу **#1085**. **Ни одно** не обещает ни
> столбца-перечня, ни формы «первый + счётчик»; проба вдобавок утверждает **отсутствие**
> столбца двумя половинами — в шапке таблицы и в выборе столбцов, — то есть требует, чтобы
> столбца не обещала ни одна страница.
>
> **Предикат** (в дереве продукта):
> ```sh
> git grep -n '1085' "$R" -- ui-future/                 # 4 вхождения, все три места выше
> spec "$R" | grep -c users                             # 0 — столбца нет
> ```
> контроль в обратную сторону: `spec 554a8d49 | grep -c users` → **1**.
>
> **Следствие для тона правки, и оно несущее.** Группа F **не «устарела от нового решения»** —
> она была **ошибочна уже на момент круга 4**: её основание опровергается тем самым
> свидетельством, на которое она ссылалась. Ответ владельца по В-3 не изменил предмет, он
> **обнаружил** ошибку. Формулировка «решение изменилось» была бы мягче и **неверна**: она
> списала бы дефект документа на движение мира.

**Что из этого следует после круга 6.** Со стадии **S5** и со сценариев IAM-ID-1-43…-48
**снята санкция** (§13.1); сами сценарии сохранены с записью об отзыве, а IAM-ID-1-49 не
отозван — его предмет порядок работ, а не величина. Предмет — читаемая принадлежность на **карточке**, и он принадлежит задаче
`PRO-Robotech/kacho#1085` и приёмке IAM-ID-2, а не этому документу. Ветвь В-3 §7 закрыта
ответом владельца, а не деревом (§7, В-3).

**Радиус здесь больше не называется** — ни «одна спека», ни какой-либо другой: он принадлежит
той приёмке, где живёт предмет. Прежняя редакция называла радиус **и** форму ячейки, то есть
проектировала консоль в документе, чей предмет — отрыв данных; заимствуемая форма
(`accessBindingSubjectsCell`) названа здесь была ради столбца, которого решение не
предусматривает.

**Что из §1.7 остаётся действующим и после сужения:** сам замер. Столбца нет ни на одной
ревизии начиная с `90a54650`, и это факт о дереве, нужный любому, кто будет решать судьбу
поверхности, — в каком бы документе он это ни решал.

### 1.8 Что изменилось со дня написания документа решения — перемерено

Документ решения писался на `7ebb670d`; ствол ушёл на `554a8d49`. Четыре его утверждения
перепроверены, предикаты названы, чтобы следующий читатель повторил их одной командой, а не
поверил (`ui.md`, урок «препятствие без предиката — это мнение с датой»).

| Утверждение документа решения | На `554a8d49` | Предикат |
|---|---|---|
| «столбец не трогается, пока не сделана `kacho#420`» (§9, §11 п. 12) | **устарело**: `#420` закрыта, влита `554a8d49`; спека `users` несёт серверный поиск. Блокатор снят. **Круг 3: устарело второй раз** — самого столбца больше нет (§1.7) | `git show "$R":ui-future/shared/src/lib/resource-registry.tsx \| grep -c serverTerm` → 1, и это спека `users` |
| «`InviteUserRequest.account_id` — номер строки» | номер поля и `required` — те же; **номер строки здесь не воспроизводится**: он и был предметом расхождения, то есть ровно та координата, которая устаревает молча | `msg InviteUserRequest \| grep -c account_id` → 1 (§1.5) |
| «живых `RESTRICT` на `users` — три сверх названных гвардом» | подтверждено; общее число живых `RESTRICT` на `users(id)` — **4** вместе с `accounts_owner_fk` | §1.3, разбивка по действию |
| «отображение отказов знает девять имён ограничений, `users_account_fk` среди них нет» | подтверждено; на стволе круга 3 — те же **9** | `fk() { git show "$R":services/iam/internal/repo/kacho/pg/pgmaperr.go \| awk '/^func fkText\(/{f=1} f{print} f&&/^\}$/{exit}'; }` затем `fk \| grep -cE '^\tcase "'` → **9** и `fk \| grep -c users_account_fk` → **0** (перемерено кругом 9 на `475f8527` — те же 9 и 0). Считать `case "` по всему файлу нельзя — их **41** (32 на `554a8d49`, 35 на `43d332ac`): число верно для другого предиката (§1.0 п. 3), **и оно дрейфует на каждом круге**, тогда как несущие 9 и 0 стоят четвёртый круг подряд. Число-ловушка приведено именно затем, чтобы этот контраст был виден; круг 9 обновил его в третий раз |

**Пятое — уточнение, а не устаревание.** Ветка, проглатывающая отказ активации второй
строки (`services/iam/internal/apps/kacho/api/user/internal_upsert.go`), **не покрыта ничем**;
ни один тест дерева не сеет двух `PENDING`-строк одной почты перед `UpsertFromIdentity`.
Существующий `TestUserInvite_0011A_InviteExistingEmail_SecondAccount`
(`services/iam/internal/repo/kacho/pg/user_invite_second_account_integration_test.go`)
проверяет **вставку** второй строки и активации не зовёт ни разу. Это меняет план проб:
сценарий IAM-ID-1-02 заводится **с нуля**, а не «дописывается к имеющемуся».

```sh
git grep -rn "Len(t, pendings, 2" "$R" -- services/iam       | wc -l   # 0
git grep -rn "SecondAccount"      "$R" -- services/iam/tests | wc -l   # 0
# контроль в обратную сторону: тот же предикат по всему сервису находит файлы —
# то есть ноль выше означает «нет сквозной пробы», а не «предикат не находит ничего никогда»
git grep -rln "SecondAccount"     "$R" -- services/iam       | wc -l   # 2 (было 1)
#   → repo/kacho/pg/user_invite_second_account_integration_test.go — проба ВСТАВКИ
#     apps/kacho/seed/bootstrap_admin_row_choice_integration_test.go — выбор строки на посеве
#   ВЫВОД НЕ МЕНЯЕТСЯ, и это проверено, а не предположено: ни один из двух файлов не зовёт
#   активацию — `UpsertFromIdentity` в обоих даёт 0. Сценарий IAM-ID-1-02 по-прежнему
#   заводится с нуля
```

### 1.9 Глагол исключения человека из аккаунта УЖЕ ПОСАЖЕН — и адресуется парой, а не идентификатором членства

Заведено кругом 9. До него документ **не упоминал этот глагол ни разу**, при том что глагол
работает, опирается на миграцию **этого же документа** и закрывает предмет, который §5
заказывает сценарием IAM-ID-1-65.

**Что стоит в дереве** (единица — объявление; все шесть проверяются одной командой каждая):

| Что | Предикат на `"$R"` | Значение |
|---|---|---|
| глагол в контракте | `git show "$R":proto/kacho/cloud/iam/v1/user_service.proto \| grep -c 'rpc RemoveFromAccount'` | **1** |
| REST-путь | тот же блок, `grep -o 'users/{user_id}:removeFromAccount'` | `POST /iam/v1/users/{user_id}:removeFromAccount` |
| отношение модели прав | `git show "$R":proto/kacho/cloud/iam/v1/fga_model.fga \| grep -c member_remover` | **4** |
| запись каталога прав | `git grep -l 'iam.users.removeFromAccount' "$R" -- gateway services/iam ':!*/collections/*'` | **3**: обе вшитые копии каталога + **исходник** кейса newman. Без исключения тот же предикат даёт **4** — четвёртая есть **порождённая** коллекция, артефакт генератора, а не исходник; единица счёта та же, что у §1.5а класса 5, и названа здесь по той же причине: одно число без названной единицы неотличимо от ошибки |
| реализация | `git cat-file -e "$R":services/iam/internal/apps/kacho/api/user/remove_from_account.go` | есть |
| повышенный уровень | тот же блок, `grep -c 'required_acr_min'` | **1** (`"2"`) |

**Адресация — пара «человек + аккаунт», и это не случайность реализации.**
`RemoveUserFromAccountRequest` несёт ровно два обязательных поля: `user_id` и `account_id`;
второе объявлено **и** входом анти-BOLA-гейта (`scope_extractor { object_type: "account",
from_request_field: "account_id" }`, `required_relation = "member_remover"`). Идентификатора
членства в контракте iam нет вовсе:

```sh
git grep -c 'membership_id'    "$R" -- proto/kacho/cloud/iam/v1/ | awk -F: '{s+=$NF} END{print s+0}'   # 0
git grep -c 'MembershipService' "$R" -- proto/kacho/cloud/iam/v1/ | awk -F: '{s+=$NF} END{print s+0}'  # 0
```

**Порядок «сперва права, потом участие» держит БАЗА, и держит его миграция стадии S2 этого
документа.** Отложенный триггер `membership_carrying_rights_is_kept` (миграция
`472002_membership_carries_rights.sql`, `CREATE CONSTRAINT TRIGGER … DEFERRABLE INITIALLY
DEFERRED`) отвергает снятие членства, несущего живую выдачу в этом аккаунте; отказ приходит
**на коммите** и отображается в `FAILED_PRECONDITION` контракт-тоном. То есть порядок —
конструкция, а не дисциплина вызывающего (ban #10).

```sh
git ls-tree "$R" --name-only services/iam/internal/migrations/ | grep 472002
git show "$R":services/iam/internal/migrations/472002_membership_carries_rights.sql \
  | grep -c 'CREATE CONSTRAINT TRIGGER'                                                    # 1
```

> [!important] РЕШЕНИЕ круга 9: приёмка ПРИЗНАЁТ посаженную адресацию; второго глагола
> снятия не заводится
> Вердикт круга 8 назвал два исхода и потребовал выбрать. Выбран первый, и вот на чём он
> стоит.
>
> **Область решения о доступе — аккаунт, и пара называет её ПО ПОСТРОЕНИЮ.** Право исключать
> выдаётся на аккаунт (`member_remover` @ `account`), а пара уже несёт аккаунт в запросе:
> гейту нечего резолвить. Глагол, адресованный `mbr-…`, потребовал бы от края обратного
> резолва «идентификатор членства → аккаунт» **до** того, как он сможет задать вопрос, —
> то есть завёл бы путь разрешения ради величины, которая у вызывающего уже была
> (`security.md` Hardening #3, анти-BOLA).
>
> **Два глагола об одном предмете — запрещённый класс, а не вкус.** «Исключить человека X из
> аккаунта A» и «снять членство `mbr-…`» суть **один** факт с двумя написаниями; разойтись
> они обязаны, и разойдутся тихо — ровно то, что §1.5а класс 9 разбирает про снимок прав.
> Плюс ban #11: глагол посажен и production-complete, второй был бы новым долгом.
>
> **Чем платим — названо, а не умолчано.** Ресурс членства получает чтения и создание (через
> переезд приглашения, S3.2), но **не получает `Delete`**: снятие остаётся суффикс-действием
> на человеке. Это отступление от «стандартных методов ресурса» (`api-conventions.md`), и оно
> объявляется **решением**: жизненный цикл членства принадлежит **аккаунту** — приглашение
> заводит, `member_remover` снимает, — а не арендатору, держащему непрозрачный идентификатор.
> Молчаливое отступление осталось бы нарушением; объявленное — нет.
>
> **Что из этого следует для ban #15.** Ничего не нарушено: `mbr-…` остаётся
> внешне-адресуемым идентификатором членства для **чтения** (IAM-ID-1-64), а суффикс-действие
> адресуется по `usr-…` — тоже неизменяемому идентификатору. Ни одна durable-координата не
> ключуется на имя.
>
> **РЕШАЮЩИЙ довод — ЕСТЕСТВЕННЫЙ КЛЮЧ, и он в схеме, посаженной этим же переходом** (внесено
> кругом 11). Всё сказанное выше стоит на **области решения о доступе**, и этого довольно для
> анти-BOLA. Но опровергнуть адресацию парой можно ровно одним способом — показав, что пара
> **не определяет членство однозначно**. Отвечает на это не рассуждение, а ключ:
>
> ```sh
> M=services/iam/internal/migrations/470001_memberships_expand.sql
> git show "$R":$M | grep -A1 'CREATE UNIQUE INDEX memberships_user_account_unique'
> #   → ON kacho_iam.memberships (user_id, account_id);   — ключ ПОЛНЫЙ
> git show "$R":$M | grep -A2 'CREATE UNIQUE INDEX memberships_user_account_unique' \
>   | grep -c 'WHERE'                                                              # 0
> # контроль в обратную сторону — тот же предикат на ЗАВЕДОМО частичном ключе того же сервиса,
> # иначе ноль выше читался бы как «команда никогда не находит WHERE»:
> git show "$R":services/iam/internal/migrations/20260823050000_users_identity_uniqueness_goes_global.sql \
>   | grep -A1 'CREATE UNIQUE INDEX users_identity_external_id_uniq' | grep -c 'WHERE'   # 1
> ```
>
> Миграция сама объясняет, почему частичным этот ключ быть не может: состояний у членства два
> («приглашён» и «активно»), снятие есть **удаление строки**, — то есть предиката, по которому
> строку следовало бы исключить из ключа, не существует. Значит пара «человек + аккаунт» есть
> **естественный ключ** членства, и `mbr-…` для снятия не несёт ничего, чего не несёт пара.
>
> **ПРЕДИКАТ ИСТЕЧЕНИЯ решения — без него оно стареет молча.** Решение живёт, пока ключ полон.
> Заведут **третье** состояние членства (приостановку — сегодня она вне охвата, §6 п. 3) и
> сделают ключ частичным — пара перестанет быть ключом, и адресация по паре станет
> неоднозначной **молча**: ни один сегодняшний гейт этого не заметит, потому что запрос
> останется синтаксически законным и вернёт одну из строк. Предикат — контроль выше, вывернутый
> наизнанку: `grep -c 'WHERE'` у `memberships_user_account_unique` перестал давать **0**. Тогда
> решение обязано быть пересмотрено, а не «уточнено».
>
> **Что при этом ОСТАЁТСЯ работой S3.1** — чтобы «признано» не прочиталось шире: тип членства
> в модели прав, перевыраженный источник каскада для личности, префикс `mbr` (§2.1а),
> **остальные** глаголы ресурса членства (создание и два чтения §5.0 п. 0), записи каталога
> прав на новый тип, строка таблицы форм ответа края. Снятие из этого перечня **выведено** —
> оно уже есть.

> [!warning] Глава находила этот глагол СВОЕЙ ЖЕ командой — и не заметила
> Предикат §1.5 строки 5 (`from_request_field: "account_id"`) объявлял **1** и сегодня даёт
> **2**: второе вхождение — блок `RemoveFromAccount`. То есть факт лежал внутри числа,
> которое глава уже мерила, и разошёлся молча вместе с ней.
>
> Это довод не за внимательность, а за норму §1.0а: **перемер сплошной, а не адресный**.
> Круг 7 тронул три места по перечню вердикта и был прав по заданию; расхождение, о котором
> никто не знал, мог найти только сплошной прогон. Ровно так же круг 4 нашёл восемь там, где
> вердикт круга 3 называл два.

---

## §2 Целевая форма

### 2.1 Одна строка на человека, членство — отдельный ресурс

| Было | Стало |
|---|---|
| `users` = «человек **в** аккаунте» | `users` = «человек» |
| принадлежность — колонка `account_id NOT NULL` | принадлежность — строка `memberships` |
| уникальность парная `(account_id, …)` | уникальность **глобальная** по почте и по внешнему идентификатору |
| один человек в двух аккаунтах = две строки, два идентификатора, два набора прав | одна строка, один идентификатор, **два членства**, права раздельны |

`memberships`: идентификатор дефис-канона (`api-conventions.md` §id-prefix), ссылка на
человека `RESTRICT`, ссылка на аккаунт `CASCADE`, состояние, кто пригласил, отметки времени,
частичная уникальность «человек × аккаунт». **Асимметрия направлений и есть исчезновение
цикла:** членство принадлежит аккаунту, владение остаётся структурным, а второе ребро
уходит вместе с колонкой.

**Идентификатор человека НЕ перечеканивается.** `usr-` внешне-адресуем — сегмент публичного
пути, цель выдачи, субъект модели прав; смена этой идентичности — прямое нарушение ban #15.
Слияние двух строк одного человека сохраняет **каноническую** (старейшую `ACTIVE` по почте) —
ту, на которую права уже выданы (§1.4), — и снимает остальные. Чтение по снятому
идентификатору отвечает `NOT_FOUND` дословным контракт-тоном владельца: никаких таблиц
псевдонимов и мягких переадресаций (`security.md` §Hardening #6 — различимый ответ есть
оракул).

### 2.1а Префикс `mbr` обязан стоять в каталоге дефис-префиксов платформы — иначе КОРРЕКТНЫЙ идентификатор членства отвергается

**Норма.** Ресурс, чей идентификатор адресуется снаружи, регистрирует свой префикс в
**платформенном** каталоге дефис-префиксов. Без записи `corevalidate.ResourceID` отвергает
идентификатор, который **сам же продукт и произвёл**.

**Признак — механический, и он выполняется прямо сейчас** (перемерено кругом 7 на
`release/identity-4` `8a3e00f18`):

```sh
# каталог префикса НЕ несёт
grep -c '"mbr"' pkg/ids/ids.go                                                   # 0
# при том что идентификаторы такой формы УЖЕ чеканятся — миграцией S1, посаженной в ствол
grep -n 'mbr' services/iam/internal/migrations/470001_memberships_expand.sql
#   → SELECT 'mbr-' || substr(md5(…), 1, 17);
#   → CHECK (id ~ '^mbr-[0-9abcdefghjkmnpqrstvwxyz]{17}$')
# контроль в обратную сторону — предикат исполним и различает: соседний ресурс iam,
# прошедший этот путь, в каталоге ЕСТЬ
grep -c 'PrefixLimitHyphen' pkg/ids/ids.go                                       # ≥1
```

**Прецедент назван деревом, а не выведен нами.** Комментарий рядом с записью предела в том же
каталоге говорит дословно: «Без записи здесь `validate.ResourceID` отвергал бы КОРРЕКТНЫЙ id,
который сам же генератор и произвёл, — то есть `Get`/`Update`/`Delete` предела отвечали бы
`INVALID_ARGUMENT` на всяком входе». Тот же класс, тот же каталог, другой ресурс.

**Чьё это предусловие — здесь, а не у соседа.** Оно принадлежит **этому** документу, потому
что от него зависит его собственный **действующий** сценарий IAM-ID-1-64: чтение
собственного несуществующего членства даёт `NOT_FOUND` — и оно **неконструируемо** без
записи, потому что всякий корректный идентификатор членства отвергается формой раньше, чем
доходит до чтения. Норма, признак, контроль и прецедент ниже кругом 9 не тронуты.

> [!note] Круг 9: потребителей стало ОДИН, а не два — IAM-ID-1-65 перенацелен и `mbr` ему
> больше не нужен
> Здесь стояло «64 **и** 65». Круг 9 признал посаженный глагол исключения из аккаунта
> (§1.9): он адресуется парой «человек + аккаунт», идентификатора членства не принимает —
> значит его сценарий предусловием о префиксе не связан **by construction**.
>
> Правка вынужденная, а не заказанная: перечень потребителей есть утверждение о документе, и
> оставить в нём сценарий, которому предусловие уже не нужно, значило бы завести два места об
> одном предмете — ровно то, чем блокировал вердикт круга 8 в другой точке. **Сам §2.1а от
> этого не слабеет**: у него остаётся живой потребитель, и предусловие по-прежнему
> обязательно.

> [!warning] До круга 7 условие было названо ровно ОДИН раз — внутри ОТОЗВАННОГО сценария
> Оно стояло в приписке к IAM-ID-1-63 и адресовалось соседнему документу (IAM-ID-2-04).
> Сценарий 63 санкции не имеет, значит его текст никого не связывает; исполнитель,
> дошедший до 64 и 65, о префиксе не узнавал **ниоткуда**, а отказ увидел бы уже прогоном —
> в форме, которая читается как дефект чтения, а не как отсутствие записи в каталоге.
>
> **Регистрация входит в шаг S3.1** (§2.3а, назначенный порядок), а не в соседний документ:
> она нужна раньше, чем появится любое чтение членства, и нужна **обоим** документам.

### 2.2 Приглашение неизвестной почты — та же одна строка

Приглашение по почте, которой в платформе нет, заводит **одну** строку человека в состоянии
«приглашён» (внешний идентификатор пуст — существующая проверка
`users_invite_status_consistency`, `0001_initial.sql:847`) и **одно** членство. Второе
приглашение той же почты в другой аккаунт **второй строки не заводит** — глобальная
уникальность по почте этого не допускает — а добавляет **второе членство** к той же строке.

Первый вход человека переводит его строку в «активен» и переводит **все** его приглашённые
членства в «активно». Ровно это дерево обещает сегодня
(godoc `UpsertFromIdentity` в `services/iam/internal/apps/kacho/api/user/internal_upsert.go` — «ActivateInvite каждой»; предикат: `git grep -c 'ActivateInvite' "$R" -- services/iam/internal/apps/kacho/api/user/internal_upsert.go` → 6), и ровно это оно сегодня не
исполняет; после отрыва обещание становится исполнимым **by construction**, потому что
активировать нужно одну строку, а не N.

**Приглашение человека, уже активного,** заводит членство сразу в состоянии «активно»:
шага принятия приглашения в продукте нет сегодня, и этот документ его не вводит (§6 п. 4).

**Ответ приглашения обязан быть неотличим** для «человек в платформе есть» и «человека
нет» — глобальная уникальность почты делает эту поверхность перечислимой, если ответы
различаются (`security.md` §Hardening #6, `data-integrity.md` §B15). IAM-ID-1-27.

### 2.3 Что уходит из контракта

Поле аккаунта снимается с методов пользователя по правилу
`api-conventions.md` §«Принято-и-проигнорировано»: исходов три, здесь верен третий —
**снять с резервированием номера И имени**, объявив ломающее изменение в сообщении коммита.
Перечень мест и их исходы — §1.5. Приглашение **не теряет** требования аккаунта: оно
переезжает вместе с глаголом на ресурс членства, вместе со всеми тремя опциями авторизации.

### 2.3а Правка модели прав — адрес, условие, радиус, **и стадия S3, а не S2**

> [!note] ПОРЯДОК ВНУТРИ S3 НАЗНАЧЕН ВЕРДИКТОМ круга 6 — предупреждение снято как исполненное
> Здесь стояло предупреждение «объявленный порядок опровергнут деревом и не исправлен»
> (`PRO-Robotech/kacho-workspace#357`). Его предикат снятия выполнен **не автором**: смена
> порядка стадии меняет санкционируемую работу, поэтому порядок назначен **вердиктом круга 6**
> (шапка, «Решение по вопросу 2»). Он и есть действующий; текст ниже приведён к нему.
>
> | шаг | что приземляется | почему не раньше и не позже |
> |---|---|---|
> | **S3.1** — **одним изменением** | тип членства в модели прав · перевыраженный источник каскада для личности · **префикс `mbr` в каталоге дефис-префиксов платформы** (§2.1а) · глаголы ресурса членства **сверх посаженного исключения из аккаунта** (создание и два чтения §5.0 п. 0 — снятие уже есть, §1.9) · записи каталога прав на новый тип · строка таблицы форм ответа края | модель и её замена обязаны приземлиться **вместе**: звено цепи областей уже берётся из членства (миграция `944001`), поэтому снятие `admin from account` в одиночку **отнимает** доступ у владельца аккаунта и делегированного распорядителя. Запись каталога и строка края возможны только при живом RPC — обратный гейт объявляет находкой строку таблицы форм ответа края, до которой не дотягивается ни одна запись каталога |
> | **S3.2** | переезд глагола приглашения на ресурс членства | переезд меняет **контракт**, а не конструируемость: два членства на одну строку личности строятся **уже сегодня**, обычным приглашением известной почты во второй аккаунт (доказано посевом — §16.2), поэтому IAM-ID-1-31, -57 и **-65** исполнимы без синтетики **с конца S3.1**, а не с этого шага. Раньше S3.1 — запрещено по-прежнему, и довод стал **сильнее**: предпосылка окна (членств больше одного) выполнима уже сегодня, значит S3.1 закрывает настоящее, а не предотвращает будущее |
> | **S3.3** | читатели: фильтр `ListUsers` **внутри сервиса** (IAM-ID-1-21), консоль (IAM-ID-1-55), провайдер инфраструктуры (IAM-ID-1-56); `User.account_id` продолжает заполняться | читатель, переведённый до существования источника, читает пустоту; колонка жива до S4 (откат S3 полон, IAM-ID-1-51) |
>
> **Контроль шага S3.1 — дельта доступа до/после ПУСТА, пока у каждого одно членство.** Это
> **положительный контроль**, а не признак завершённости: на одном членстве сломанная и
> починенная формы отвечают одинаково **by construction**. Различающий сценарий —
> IAM-ID-1-31, и он исполним только после S3.2.
>
> **Что из прежней гипотезы этого раздела ОТВЕРГНУТО.** Она предлагала «читатели → публичная
> поверхность членства (`kacho#470`) → объект прав и глагол». Последний элемент верен и вошёл
> в S3.1; **средний называет отозванное** — публичная поверхность чтения этим документом
> больше не заказывается (§0.2), поэтому шагом стадии быть не может. Первый исполним лишь
> частью: перевыражение фильтра `ListUsers` внутри сервиса — да, консоль — нет, она ждёт
> источника, чей контракт принадлежит IAM-ID-2.

> [!note] Сопутствующее, названное тем же вопросом, — исход по каждому пункту
> Молчание о них читалось бы как «проверено и верно», поэтому исход назван у каждого:
>
> | пункт | исход |
> |---|---|
> | «механизм применения модели: мята идентификатора версии, перекат сервисов, гейт дрейфа конфигмапа чарта» — **предмета нет** | **снят и заменён действующим механизмом** — абзац «Применение» ниже и DoD п. 13 |
> | «§1.5а класс 6 называет ОДНОГО производителя факта, их три, и решающий не назван» | **исполнено** — звено цепи областей `kacho_iam.resource_scope_edge` названо в §1.5а классом 6 и здесь; его ветвь `iam_user → account` **уже** переведена на членство миграцией `944001` |
> | «§1.5а класс 3 требует перевести скрипт восстановления, который не исполняется вовсе» | **закрыто исходом, а не переносом (круг 9): предмета нет.** Скрипт удалён целиком коммитом `c78901a33` — работой этого же перехода; `git grep -ln 'fga_ensure' "$R"` → пусто. Класс 3 переписан на **действующий** предмет — пять писателей кортежа в прод-коде (§1.5а), и он обеспечен сборкой. Требование к `kacho#471` отсюда снято: критерий, который нечем исполнить, держал бы стадию в состоянии, когда её нельзя объявить завершённой |

> [!warning] Круг 3 перенёс эту работу из S2 в S3, потому что прежняя посылка опровергнута
> деревом (`PRO-Robotech/kacho#616`)
> Прежняя редакция считала, что вопрос о доступе к объекту личности можно сделать
> **аккаунт-относительным**, правя только тип `iam_user` в модели, — то есть **не меняя
> объекта**, про который спрашивает гейт. Это неверно.
>
> Аккаунт у человека перестаёт быть единственным (в этом весь предмет перехода), поэтому
> аккаунт-скоупным объектом становится **членство**, а не личность: «этот человек в этом
> аккаунте» — это членство и есть. Смена объекта — не правка модели, а правка **контракта**:
> у записей каталога прав меняется `object_type`, у края меняется то, из какого поля запроса
> берётся объект, и вместе с ними — таблица форм ответа края, ключуемая тем же типом.
>
> **Радиус смены объекта — измерен, а не назван:**
>
> ```sh
> C=gateway/internal/middleware/embed/permission_catalog.json
> git show "$R":$C | grep -c '"object_type": "iam_user"'    # 10 — записей каталога на объекте личности
> git show "$R":$C | grep -c '"object_type": "account"'     # 11 — контроль: предикат исполним и различает тип
> git grep -c '"iam_user":' "$R" -- gateway/internal/middleware/permission_denied_response.go   # 1
> #   ^ таблица форм ответа края, ключуемая типом объекта: «скрыть существование» обязано быть
> #     побайтово равно настоящему промаху владельца (`security.md` §Hardening #6). Сменив тип
> #     объекта и не тронув её, получаем различимый ответ, то есть оракул
> ```
>
> Обе встроенные копии каталога **байт-в-байт равны** (предикат — DoD п. 13,
> `make -C gateway permission-catalog-check`), поэтому число одно на обе.
>
> **Число, названное в задаче, — 11; мой предикат даёт 10** на объекте личности (и 11 — на
> объекте аккаунта, что похоже на источник расхождения). Вывод от числа не зависит
> (`measurement-discipline`: число не есть находка), но принимать чужое число как своё
> нельзя — поэтому здесь стоит моё, с предикатом и единицей счёта: **запись каталога**.
>
> **Следствие для порядка стадий, и оно снимает натяжку, а не добавляет.** Прежняя редакция
> сама признавала, что различающий сценарий IAM-ID-1-31 требует человека **в двух аккаунтах**,
> и выкручивалась синтетической парой членств, заведённой прямо в пробе. Перенос работы в S3
> делает эту конструкцию ненужной: к моменту правки предмет существует по-настоящему.
>
> **Круг 11: здесь стояло «а такого человека раньше S3 не существует» — на сегодняшнем дереве
> это неверно.** Приглашение известной почты во второй аккаунт заводит **второе членство на ту
> же строку**, и так с той поры, как ключ идентичности стал глобальным (§16.2 — предикат,
> посаженная проба, утверждающая состояние таблиц). Вывод абзаца не меняется (синтетика не
> нужна), меняется **когда** предмет появляется: не «внутри S3», а уже сейчас.
>
> **Что взамен становится обязательным — ordering ВНУТРИ S3.** Правка модели и смена
> `object_type` обязаны приземлиться **первыми** — до переезда глагола приглашения, который
> сделает множественное членство **массовым**. Иначе существует окно, в котором членств больше
> одного, а вопрос о доступе к личности по-прежнему разрешается «через любой из аккаунтов» —
> то есть административный уровень одного арендатора действует на общем объекте личности. Это
> утверждается пробой IAM-ID-1-31, а не порядком в намерении исполнителя.
>
> **Круг 11 уточнил здесь ОДНО слово: «сделает конструируемым» → «сделает массовым».**
> Конструируемым множественное членство уже сделано — приглашением известной почты во второй
> аккаунт (§16.2). Требование к порядку от этого только строже: S3.1 закрывает окно, которое
> уже открыто, а не предупреждает то, которого ещё нет.

Ниже — то, без чего предмет нельзя ни исполнить, ни проверить.

**Адрес — ДВА, а не один, и второй решающий.** Модель называет отношение; **источником**
факта «личность принадлежит аккаунту» служит звено цепи областей в базе.

1. `proto/kacho/cloud/iam/v1/fga_model.fga`, тип `iam_user`:

```
define account: [account]                  # присваиваемое отношение
define super_admin: admin from account     # уровень ВЫВОДИТСЯ из него
```

2. `kacho_iam.resource_scope_edge` — представление, чью ветвь (4a) читает `admin from
   account`, отвечая на вопрос «кто предок-аккаунт у этого объекта личности».

**С `super_admin` объединены ТРИ глагола этого типа, и их у типа всего три** — перемерено
кругом 7, единица счёта — объявление глагола:

```sh
M=proto/kacho/cloud/iam/v1/fga_model.fga
blk() { awk '/^type iam_user$/{f=1;next} /^type /{f=0} f' "$M"; }
blk | grep -cE '^ *define v_[a-z]+:.*super_admin'          # 3
blk | grep -oE '^ *define v_[a-z]+:.*super_admin' | grep -oE 'v_[a-z]+'
#   → v_get · v_list · v_delete
# контроль в обратную сторону — ВСЕ глаголы типа, не только объединённые:
blk | grep -oE '^ *define (v_[a-z]+):' | grep -oE 'v_[a-z]+'
#   → те же три. Два числа названы НАМЕРЕННО: равенство и есть утверждение
#     «необъединённого глагола у типа нет», а одно число его не выражает
```

> [!warning] Здесь стояло «**все четыре**» — верно на `43d332ac`, ложно сегодня, и разница
> есть работа, которую этот раздел заказывал бы **повторно**
> `v_update` снят у типа задачей `PRO-Robotech/kacho#1128`: правку строки личности спрашивает
> `record_writer`, запрет и его снятие — `identity_suspender`, и ни у того, ни у другого
> источников уровня аккаунта нет. Замер по трём ссылкам продукта: `43d332ac` → **4**,
> `origin/main` → **3**, `release/identity-4` (`8a3e00f18`) → **3**.
>
> Число здесь не украшение: им исполнитель считает объём правки. Заказав четвёртый глагол, он
> искал бы в модели то, чего нет, и либо завёл бы его заново, либо решил, что модель не та.

Пока членство одно, множество предков вырождено; при N членствах вывод читает **любое** из
них — то есть административный уровень одного арендатора начинает действовать на общем
объекте личности.

> [!important] Ветвь (4a) УЖЕ переведена на членство — значит вторая половина работы
> обязана приземлиться ВМЕСТЕ с первой, а не после
> Миграция `944001_identity_scope_comes_from_membership.sql` (S3, посажена) заменила источник
> ветви: предка-аккаунт даёт теперь **таблица членств**, а не колонка строки. Её собственная
> шапка называет это «половиной работы, вторая половина которой названа и имеет предикат (тип
> `iam_membership` в модели прав)».
>
> **Следствие для порядка, и оно назначено вердиктом (выноска в начале раздела).** Пока
> членство у каждого одно, дельта пуста. Но снять `admin from account` **в одиночку** значит
> отнять доступ у владельца аккаунта и делегированного распорядителя: путь-замена ещё не
> существует. Поэтому модель, её замена, префикс, глаголы ресурса членства **сверх
> посаженного исключения из аккаунта** (§1.9), записи каталога и строка форм ответа края идут
> **одним изменением** — шаг S3.1.

**Условие, которому обязана удовлетворять целевая форма** (сама форма — за исполнителем):

1. вопрос о доступе к объекту личности **разрешим только относительно названного аккаунта** —
   и «названный аккаунт» приходит **объектом членства**, а не выводится из личности: у
   личности аккаунт перестаёт быть единственным, поэтому выводить его неоткуда;
2. администратор аккаунта `A` получает «да» на объект личности **через членство в `A`** и
   «нет» на то же самое, если спрашивают относительно `B`;
3. каскад облака и бутстрапа отвечает «да» **независимо** от числа членств — этот уровень
   и заводился ради аварийного пути (`security.md` §«Три уровня супер-доступа»);
4. форма выразима в модели, а не достраивается проверкой в коде (`security.md`
   §«Авторизация живёт в МОДЕЛИ»).

**Радиус — 6 и 6, и второе число надо получить предикатом по СВОЙСТВУ.** Присваиваемое
`account: [account]` несут **6** типов (`project`, `iam_user`, `iam_service_account`,
`iam_group`, `iam_role`, `iam_access_binding`); выводят уровень из аккаунта — те же **6**:
у четырёх это голая форма, у `project` и `iam_access_binding` — **один дизъюнкт среди
прочих**.

```sh
M="$R:proto/kacho/cloud/iam/v1/fga_model.fga"
git show $M | grep -c "define account: \[account\]"                        # 6 — присваиваемое
git show $M | grep -c "define super_admin:.*admin from account"             # 6 — по СВОЙСТВУ
git show $M | grep -cE "^ *define super_admin: admin from account *$"       # 4 — голой формой
```

Все три числа проверены кругом 3 и на стволе (`90a54650`), и на ветке работы (`78d062bb`) —
**не изменились**.

> [!warning] Здесь стояло «**5**, все кроме `iam_access_binding`» — предикат мерил ПОРЯДОК
> дизъюнктов
> Число получалось префиксным грепом `grep -c "define super_admin: admin from account"`. Он
> ловит `project` (там искомое стоит **первым**, дальше ещё дизъюнкт) и **не ловит**
> `iam_access_binding` (там оно **второе**). То есть предикат отвечал на вопрос «с чего
> начинается строка», а не «выводит ли тип уровень из аккаунта», — и скобка «все, кроме
> `iam_access_binding`» была прямо неверна: этот тип выводит.
>
> Найдено на круге 2 ревью; вывод раздела не изменился, потому что он опирается на
> **однозначность принадлежности**, а не на это число. Записано, чтобы следующий не
> воспроизвёл предикат: греп по началу строки над языком с дизъюнкцией не измеряет свойство
> **никогда**.

**Трогается ровно один из шести — `iam_user`.** Обоснование, а не умолчание: у остальных пяти
принадлежность аккаунту **остаётся однозначной** — проект принадлежит одному аккаунту, группа
одному, роль одной, служебная учётка одной, привязка одной. Множественной становится
принадлежность **человека**, и только его объект перестаёт иметь единственный аккаунт.
Тронуть остальные пять значило бы менять то, у чего предмет не изменился.

**Чем удерживается порядок ВНУТРИ S3.** «Сравнение доступа до/после» на человеке с **одним**
членством **тождественно пусто** — сломанная и починенная формы дают одинаковый ответ by
construction, поэтому признаком завершённости оно быть не может. Различающий сценарий
IAM-ID-1-31 требует человека в двух аккаунтах — и такой человек **строится уже сегодня**,
обычным приглашением известной почты во второй аккаунт (§16.2: предикат, посаженная проба,
опровергнутая посылка). Отсюда две поправки к прежней редакции этого абзаца: она отправляла
31 за S3.2, а сравнение «до/после» называла пустым безусловно. Верно первое — 31, 57 и 65
исполнимы **с конца S3.1**; верно второе — только для **преобладающего** случая, и потому это
положительный контроль шага, а не его признак завершённости. Порядок работ внутри стадии от
этого не меняется: он **назначен вердиктом** (выноска в начале раздела), а не выведен
автором, и довод за него стал сильнее — предпосылка окна выполнима уже сегодня:

1. **S3.1** — одним изменением: тип членства в модели, перевыраженный источник каскада для
   личности, префикс `mbr`, глаголы ресурса членства **сверх посаженного исключения из
   аккаунта** (§1.9 — снятие уже есть), записи каталога прав, строка таблицы форм ответа
   края;
2. **S3.2** — переезд глагола приглашения на ресурс членства;
3. **S3.3** — читатели: фильтр `ListUsers` внутри сервиса, консоль, провайдер инфраструктуры;
4. IAM-ID-1-31 зелен на конце стадии — иначе S4 выкатывать нечем.

Шаг S3.1 при этом **проверяем на живых данных**: у преобладающего большинства членство одно,
поэтому правка не меняет ничьего доступа, и сравнение «до/после» обязано быть пустым — это
положительный контроль шага, а не его признак завершённости. «У преобладающего», а не «у
каждого»: множественное членство на дереве уже есть (§16.2), и признаком завершённости
остаётся IAM-ID-1-31.

**Применение модели: механизм назван ДЕЙСТВУЮЩИЙ, а не тот, у которого нет предмета**
(перемерено кругом 7 на `release/identity-4` `8a3e00f18`):

```sh
# конфигмапов модели прав в чартах — предмета у «гейта дрейфа конфигмапа» НЕТ
git ls-files deploy/ | grep -ci -E 'fga|authz.*model'                      # 0
# посадка внешнего движка отношений снята целиком — и это закреплено пробой
git ls-files deploy/ | grep -i retired
#   → deploy/r7_3_32_engine_landing_retired_test.go
# модель ВШИТА в бинарь сервиса
git grep -n 'go:embed fga_model.fga' -- services/iam                       # 1
#   → services/iam/internal/authzmodel/authzmodel.go
```

Отсюда действующая форма требования, и она исполнима:

| было объявлено | предмет | взамен |
|---|---|---|
| «мята нового идентификатора версии модели» | движка нет — версии модели нет и мять нечего | пересборка **вшитой** копии: `make -C deploy fga-model-embed` |
| «перекат сервисов» | остаётся верным как следствие: копия вшита, значит меняется бинарь | перекат iam в том же изменении |
| «гейт дрейфа конфигмапа чарта из модели» | конфигмапов **0** | пробы дрейфа вшитой копии: `services/iam/internal/authzmap/fga_model_drift_test.go` · `services/iam/internal/authzmodel/identity_test.go` |

Так это и записано в DoD п. 13. Пункт, который нечем исполнить, — не критерий, а место, где
исполнитель остановится и решит, что документ описывает другое дерево.

### 2.4 Что НЕ меняется — перечень закрытый

1. **Материализация личного аккаунта при первом появлении человека** (RC-5) — дословно,
   включая приглашённого; предикат ворот остаётся «собственных аккаунтов ноль».
2. **Владелец — структурный источник прав на своём аккаунте**; `accounts_owner_fk` остаётся
   `RESTRICT`, владение остаётся неизменяемым (`security.md` §«Три уровня супер-доступа»).
3. **Политика трёх верхних уровней супер-доступа** — облако каскадом на всё, бутстрап в
   пределах облака, администратор аккаунта каскадом внутрь аккаунта. Политика не
   пересматривается.
   > [!warning] Но её РЕАЛИЗАЦИЯ на объекте личности трогается, и это не то же самое
   > Прежняя редакция этого пункта гласила «каскад не трогается ни в модели, ни в коде».
   > **Это было неверно, и дерево говорит обратное:** факт принадлежности объекта личности
   > аккаунту строится из снимаемой колонки. После S4 у него не остаётся источника, а при N
   > членствах источник перестаёт быть **однозначным**: каскад обязан отвечать «да» для
   > администратора **того** аккаунта, через членство в котором задан вопрос, и «нет» для
   > остальных.
   >
   > **Круг 4: производитель переехал из Go в SQL, и координата круга 3 больше не действует.**
   > Стояло `AccountScopedStructuralFact(string(u.AccountID), "iam_user", objectID)` в
   > `services/iam/internal/authzcascade/` (7 непробных вхождений). Сегодня у этой функции
   > **ноль вызывающих**, а факт производят **два** SQL-места: план чтения материализации
   > (`iamDirectScanSpecs`, `services/iam/internal/repo/kacho/pg/reconcile_adapter.go`) и
   > **звено цепи областей** `kacho_iam.resource_scope_edge`, которое и читает `admin from
   > account`. Разбор и цена переезда — §1.5а, выноска класса 6.
   >
   > **Круг 7: для `iam.user` ОБА уже переведены на членство** — запись плана выводит предка
   > `parentAccountsExpr` над `kacho_iam.memberships`, ветвь (4a) звена переведена миграцией
   > `944001`. Осталась **смена объекта** гейта (§2.3а), а не перевод источника.
   >
   > **Предикат — по ДЕЙСТВУЮЩЕМУ производителю, а не по имени функции:**
   >
   > ```sh
   > A=services/iam/internal/repo/kacho/pg/reconcile_adapter.go
   > git show "$R":$A | grep -c 'parentAccountExpr: "o.account_id"'          # 3 типа (было 4 на 43d332ac)
   > git show "$R":$A | grep -A3 'objectType: "iam.user"' \
   >   | grep -c 'kacho_iam.memberships'                                     # 1 — iam.user уже на членстве
   > git grep -nE 'AccountScopedStructuralFact\(' "$R" -- services/iam/internal \
   >   | grep -v '_test\.go' | grep -vc 'func AccountScopedStructuralFact'    # 0 — вызовов нет
   > ```
   >
   > Оставшиеся **три** типа переход не трогает: у них `account_id` — собственная колонка
   > собственной таблицы (п. 6 ниже).
   >
   > **Что этот переезд меняет для стадии.** Отказ становится **рантаймным и тихим**: раньше
   > снятие колонки роняло сборку в четырёх местах, теперь не роняет нигде. Значит готовность
   > S3 нельзя больше подпирать компилятором — она подпирается **пробой на исход**
   > (IAM-ID-1-57), и §4 приведён к этому.
   >
   > Пункт оставлен, а не удалён, потому что различие несущее: **что обещано арендатору**
   > (политика) не меняется, **чем это исполнено** (вывод из одной колонки) меняется
   > обязательно. Стадия — **S3** (круг 3: вместе со сменой объекта, §2.3а), сценарий —
   > IAM-ID-1-57.
4. **Внутренние сообщения оси арендатора операции** (§1.5 п. 4) — не трогаются.
5. **Идентификатор человека** — не перечеканивается (§2.1).
6. **Колонка аккаунта у `projects` / `service-accounts` / `groups`** — не трогается: у этих
   трёх ресурсов аккаунт остаётся **одним** значением, их предмет не изменился. Предикат —
   `spec` §1.7: на всех трёх ревизиях эти три спеки в выводе присутствуют, а `users` — только
   на `554a8d49`.

---

## §3 Замер до первого шага — стадия S0

Четыре предиката. Первые два решают, есть ли у перехода предмет и не блокирован ли он;
вторые два дают числа, которые войдут в сценарии группы E. **Все читающие, ничего не
меняют.** Прогоняются на каждой инсталляции, куда поедет миграция, а не только на нашей:
«у нас не воспроизводится» здесь означает «мы не проверяли там, где это единственно опасно».

| # | Что | Предикат | Как читать |
|---|---|---|---|
| П1 | висящие приглашения | `SELECT count(*) FROM kacho_iam.users WHERE invite_status='PENDING'` | не ноль — число идёт в IAM-ID-1-40 как размер класса «приглашённый» |
| П2 | дубли почты | `SELECT lower(email), count(*) FROM kacho_iam.users GROUP BY 1 HAVING count(*)>1` | **не ноль — переход БЛОКИРОВАН** до появления процедуры слияния (§6 п. 7) |
| П3 | неканонические привязки | привязки рода «пользователь», чей субъект не есть старейшая `ACTIVE` строка по своей почте | не ноль — это ровно те права, которые потерялись бы молча при схлопывании; каждая переносится поимённо (IAM-ID-1-36) |
| П4 | состав 74 по классам | сколько владеют ровно одним аккаунтом · сколько владеют более чем одним · сколько в состоянии «приглашён» · сколько посевных служебных | сумма обязана сойтись с общим числом строк — IAM-ID-1-42 |

**Замер владельца (74/74/74) — чужой, с атрибуцией, здесь не перемерян.** Он доказывает
одно: у каждого человека ровно одна строка, то есть поток «один человек в двух арендаторах»
на этой инсталляции **не исполнялся ни разу**. Он **не доказывает**, что многоарендность
сломана — это следует из дерева (§1.5, §1.8), а не из замера. Числа П1-П4 подставляются в
сценарии группы E в момент прогона; до этого они переменные, а не константы.

---

## §4 Порядок перехода — стадии, откат, production-completeness

Каждая стадия **production-complete в своих границах** (ban #14): дерево на её конце
собирается, пробы зелены, поверхность безопасна, отложенное вынесено в §6, а не оставлено
обещанием. Номера миграций назначаются в момент написания по
`docs/specs/migration-coordination-policy.md`.

**Старший занятый номер — величина движущаяся, поэтому она берётся предикатом, а не отсюда:**

```sh
git ls-tree -r --name-only "$R" services/iam/internal/migrations \
  | grep -oE '/[0-9]+' | tr -d '/' | sort -n | tail -1
#   554a8d49 → 0096 · 90a54650 → 0100 · 78d062bb → 472002 · 43d332ac → 893002
#   Ствол нумерует по задаче (`<задача><порядок>`), и величина растёт скачками, а не на
#   единицу: за три дня между кругами 3 и 4 она ушла с 472002 на 893002. Ещё одна причина
#   брать её предикатом на том дереве, куда пишут, а не отсюда.
```

> [!note] Здесь стояло «на `554a8d49` старший занятый — `0096`» без предиката — и оно
> устарело за 225 коммитов (круг 3)
> Число было верным и осталось верным **про свою ревизию**; ложным его делает то, что
> исполнитель читает документ, работая на другом дереве, и берёт номер оттуда, откуда взял
> остальное. Предикат снимает вопрос: номер добывается на том дереве, куда пишут.

| # | Стадия | Род | Что делается | Откат |
|---|---|---|---|---|
| **S0** | замер | чтение | четыре предиката §3 | не требуется |
| **S1** | ресурс членства заводится и пишется параллельно | **expand** | таблица `memberships` + инварианты + бэкфилл (одно членство на строку) + двойная запись; читателей нет, `users.account_id` остаётся авторитетом | полный — таблица снимается новой миграцией |
| **S2** | права: отзыв как факт БД | **migrate** | привязка субъектов привязок к членству с `RESTRICT`; отзыв прав снятием членства становится фактом БД, а не шагом кода; самовалидация домена перестаёт требовать принадлежности (§1.5а класс 7) | обратная миграция |
| **S3** | читатели, контракт и **объект прав** | **migrate** | **порядок назначен вердиктом круга 6** (§2.3а), три шага, и они не переставляются: **S3.1** — одним изменением: тип членства в модели прав · перевыраженный источник каскада для личности · **префикс `mbr`** (§2.1а) · глаголы ресурса членства **сверх посаженного исключения из аккаунта** (создание и два чтения §5.0 п. 0 — снятие уже есть, §1.9) · записи каталога прав на новый тип · строка таблицы форм ответа края; **S3.2** — переезд глагола приглашения на ресурс членства (**контракт**, а не конструируемость: два членства на одну строку строятся уже сегодня — §16.2, поэтому IAM-ID-1-31, -57, -65 исполнимы с конца S3.1); **S3.3** — читатели: фильтр области `ListUsers` внутри сервиса (IAM-ID-1-21), консоль (IAM-ID-1-55), провайдер инфраструктуры (IAM-ID-1-56); §1.5а классы 1, 2, 3, 6. Столбец страницы пользователей не трогается; `User.account_id` **продолжает заполняться**. **ВНЕШНЕЕ ПРЕДУСЛОВИЕ:** стадия **не закрывается** раньше `APPROVED` у IAM-ID-2 — четыре её действующих сценария (01, 55, 68, 73) имеют производителя только там (§0.2, DoD п. 16) | полный — оба источника живы; возврат прежней версии модели |
| **S4** | contract | **contract** | снятие колонки, её ключа и пары пер-аккаунтных ключей; ввод глобальных **с названной заменой** сериализации гонки; `reserved` на номер и имя; снятие `InviteUserRequest`; переписывание стража `0069` **в том же изменении**; сервис и край катятся вместе | **ТОЧКА НЕВОЗВРАТА** |
| ~~S5~~ | — | — | **СТАДИИ НЕТ.** Вердикт круга 6 (вопрос 3): S5 **снимается как стадия этого документа**. Её консольный предмет принадлежит IAM-ID-2 (§2.6 таблица Б, §5 группа F5: IAM-ID-2-19…-22), сценарии 43-48 остаются здесь текстом с записью об отзыве, номера в обращении (§9.3). Строка не удалена, потому что удалённая стадия неотличима от забытой: переход имеет **четыре** рабочих стадии — S1…S4, — и это надо прочитать, а не вывести из пропуска | — |

### Фактическое состояние каждой стадии — измерено на `43d332ac` (круг 4)

> [!warning] Эта таблица мерена на СТВОЛЕ, а работа садится в релизную линию — одно её
> утверждение там уже ложно (найдено 2026-08-24 при правке IAM-ID-2, перепись НЕ полная)
> Строка **S4** говорит «НЕ НАЧАТА», обосновывая это тем, что `reserved` на поле 6 не
> объявлен. На `origin/release/identity-3` (продукт, `e0d089acc`) он **объявлен**:
> ```sh
> git show origin/release/identity-3:proto/kacho/cloud/iam/v1/user.proto | grep -n 'reserved'
> #   34:  reserved 6;
> #   35:  reserved "account_id";
> ```
> Единица счёта названа честно: перемерен **один** предикат таблицы — тот, на который
> опирался соседний документ. Остальные строки §4 и все 47 предикатов §1 **мной не
> перемерялись** и остаются мерены на `43d332ac`; между стволом продукта и релизной линией
> **53** коммита, из них **38** трогают пути §1
> (`git log --oneline origin/main..origin/release/identity-3 -- services/iam proto/kacho/cloud/iam pkg/ids ui-future/iam ui-future/e2e/specs/users.spec.ts gateway | wc -l`).
> Перепись остального — работа круга 6, и она названа здесь, а не подразумевается.


Таблица выше говорит, **что стадия делает**. Эта — **что из этого уже стоит в стволе**. Две
разные вещи, и до круга 4 документ утверждал только первую, из-за чего читался как «ничего не
начато» при двух с половиной исполненных стадиях (§0.3).

| Стадия | Состояние | Предикат — им и проверяется, а не этой строкой |
|---|---|---|
| **S0** | **исполнена** | числа §3 сняты координатором (89/89) и владельцем (74/74/74); П2 = 0 ⇒ переход не блокирован |
| **S1** | **ИСПОЛНЕНА ПОЛНОСТЬЮ** | `git cat-file -e "$R":services/iam/internal/migrations/470001_memberships_expand.sql` → есть; таблица, 2 FK, 2 CHECK, полный `UNIQUE(user_id, account_id)`, бэкфилл, зеркалящий триггер, секция `-- +goose Down`; пробы — `membership_backfill_integration_test.go`, **2** интеграционные функции (`^func Test`), **50** утверждений |
| **S2** | **ИСПОЛНЕНА ЧАСТЬЮ** | есть: `472002_membership_carries_rights.sql` — отложенный `CONSTRAINT TRIGGER … DEFERRABLE INITIALLY DEFERRED`, то есть отзыв как факт БД. **Нет: самовалидация домена всё ещё требует принадлежности** — `git grep -c 'Illegal argument account_id: required' "$R" -- services/iam/internal/domain/user.go` → **1** (обязано стать 0, IAM-ID-1-58) |
| **S3** | **ИСПОЛНЕНА ЧАСТЬЮ** (перемерено кругом 6 на `5533d21e`) | **есть:** `944001_identity_scope_comes_from_membership.sql` — звено цепи областей `iam_user → account` берётся из **членства**, а не из колонки строки (`git cat-file -e "$R":services/iam/internal/migrations/944001_identity_scope_comes_from_membership.sql` → есть). **Нет:** правки модели прав — `git grep -cE '^type .*membership' "$R" -- proto/kacho/cloud/iam/v1/fga_model.fga` → **0**; ресурса членства в контракте — `git ls-tree -r --name-only "$R" proto/kacho/cloud/iam/v1/ \| grep -ci member` → **0**; консоль всё ещё на снимаемом фильтре — `git grep -n 'listUsers({' "$R" -- ui-future/ \| grep -c account_id` → **2** (IAM-ID-1-55) |
| **S4** | **ИСПОЛНЕНА ЧАСТЬЮ** (перемерено кругом 6 на `5533d21e`) | **есть:** глобальный ключ идентичности — `20260823050000_users_identity_uniqueness_goes_global.sql` (обе объявляют себя «Стадия S4-expand»); активация **всех** членств первым входом — `20260823053000_first_login_activates_every_membership.sql`; снятие поля с контракта с резервированием — `git grep -cE '^\s*reserved' "$R" -- proto/kacho/cloud/iam/v1/user.proto` → **2** (`reserved 6` и `reserved "account_id"`), объявлений `account_id` в контракте iam **51** против **52** на `43d332ac`. **Нет:** колонка `users.account_id` **жива** — `git grep -ciE 'ALTER TABLE.*users.*DROP COLUMN.*account_id' "$R" -- services/iam/internal/migrations/` → **0** |
| ~~S5~~ | **СТАДИИ НЕТ** (вердикт круга 6, вопрос 3) | состояния не имеет by construction: снятая стадия не «не начата» и не «исполнена». `spec "$R" \| grep -c users` → **0** (§1.7) — столбца нет и не появится; предмет ушёл в `kacho#1085` / IAM-ID-2 |

**Единица счёта названа отдельно, потому что здесь легко ошибиться.** «52 поля» (на
`43d332ac`; на `5533d21e` — **51**) — это **объявления** поля
(`grep -hE '^\s*(string|repeated string)\s+account_id\s*='`), а не вхождения строки
`account_id`: последних в том же каталоге **151** на `43d332ac` и **154** на `5533d21e` — и оба
числа истинны. Взяв второе, читатель
решил бы, что контракт втрое больше затронут, чем на самом деле. **Разница 52 → 51 — это
ровно снятое `User.account_id`**, и она названа здесь потому, что иначе движение числа
читалось бы как дрейф предиката, а не как исполненная работа.

> [!warning] «S3 НЕ НАЧАТА» и «S4 НЕ НАЧАТА» были ЛОЖНЫ обе — и это тот же класс, ради
> которого написан §1.0а (найдено кругом 6)
> Круг 4 измерил состояние стадий на `43d332ac` и был прав **про свою ревизию**. За два дня в
> ствол вошли **три** миграции этого перехода: звено области из членства (S3), глобальный
> ключ идентичности и активация всех членств первым входом (обе S4-expand), плюс снятие поля
> с контракта с резервированием. Строка «НЕ НАЧАТА» пережила свой предмет и стала указанием
> **начать заново** то, что стоит в стволе.
>
> **Цена этого класса измерена самим документом** и здесь только подтверждается: §0.2 прямо
> называет её — «приёмка, разрешающая сделанное, не санкционирует ничего: исполнитель
> начинает работу заново либо ищет, чего не хватает, по документу, который об этом молчит».
> Через два дня после того, как этот абзац был написан, он описал сам себя.
>
> **Что из этого следует для §1.0а.** Норма требует перемерить §1 перед подачей на вердикт.
> Замер стадий живёт в §4, а не в §1, — и устарел он **быстрее** §1, потому что мерит
> движущуюся величину by construction. Норма §1.0а обязана покрывать и §4; это записано
> замечанием 4 круга 5 («расширить охват нормы») и здесь подтверждается вторым случаем.

> [!note] «Исполнена частью» — это состояние, а не оценка качества
> S2 сделана верно в той части, которая сделана: отложенный constraint-триггер выражает отзыв
> **DB-конструкцией**, как требует ban #10 и признак завершённости ниже. Недостаёт одного
> названного элемента — снятия требования принадлежности в самовалидации домена, и он
> **измерим одной командой**. Формулировка «частью» выбрана вместо «сделана» именно затем,
> чтобы остаток не потерялся: стадия, объявленная закрытой, остатка не имеет by construction.

### Как эти стадии соотносятся с шагами документа решения

Документ решения нумерует шаги `Ш0…Ш10` и делит их между двумя вариантами. **Два места об
одном предмете — поэтому соответствие называется здесь явно, а не оставляется читателю:**

| Здесь | Там | Примечание |
|---|---|---|
| S0 | Ш0 | тот же замер, те же четыре предиката |
| — | Ш1-Ш5 | **вариант В1**, который этот документ НЕ заказывает (§6 п. 13): его предмет — удаляемость личного аккаунта **до** отрыва, и он расходуется на S4 |
| S1 | Ш6 | ресурс членства заводится и пишется параллельно |
| S2 | Ш8 | отзыв как факт БД. **Круг 3 вынул отсюда Ш7** (правку модели): её нельзя исполнить, не сменив объект, а смена объекта — контракт |
| S3 | **Ш7** + Ш9 | правка модели идёт первым шагом стадии (§2.3а), затем читатели и контракт; плюс перевод классов потребителей (§1.5а), которого в Ш9 не было |
| S4 | Ш10 | точка невозврата, там и здесь одна и та же |
| ~~S5~~ | (внутри Ш9) | **стадия снята вердиктом круга 6** — соответствовать нечему. `kacho#473` закрыта решением «столбец снят»; читаемая принадлежность на карточке — `kacho#1085` / IAM-ID-2, другой документ. Строка оставлена, чтобы Ш9 не читался как непокрытый: его консольная половина покрыта **там**, а не потеряна |

**Расхождение по существу ровно одно** и названо в §1.8: документ решения ставил условием
незакрытую задачу консоли, а она закрыта. Остальные различия — группировка, не смысл.

### Почему порядок именно такой — что ломается при перестановке

- **S2 перед S3 — обязательно.** Отзыв обязан стать фактом БД **до** того, как членства
  станут авторитетом. Иначе в окне переключения снятое членство доступ не отзывает, а
  «отозвано намеренно» неотличимо от «не доехало»: обе формы выглядят как отсутствие
  кортежа, и разобрать их постфактум нечем.
- **S3.1 перед S3.2 — обязательно** (порядок назначен вердиктом круга 6, §2.3а). Правка
  модели делается, **пока членство одно у преобладающего большинства**: тогда она не меняет
  ничьего доступа и проверяется сравнением «до/после» на живых данных. Переставить эти два
  шага внутри стадии — значит расширить окно, в котором членств больше одного, а вопрос о
  доступе к личности разрешается через любое из них.

  **Круг 11 уточнил тут ОДНО слово, и оно меняет срочность, а не порядок.** Здесь стояло
  «пока членство у каждого одно: множества ещё нет». Множество уже есть — у тех, кого свела
  миграция S2, и у всякого, кого приглашение известной почты привело во второй аккаунт
  (§16.2). Значит S3.1 закрывает **настоящее** окно, а не предотвращает будущее, и сравнение
  «до/после» остаётся **положительным контролем** шага, а не доказательством его
  завершённости — доказывает её IAM-ID-1-31.

  **И S3.1 неделим — это второе, чего прежняя редакция не говорила.** Звено цепи областей уже
  берётся из членства (миграция `944001`), поэтому снятие `admin from account` **в одиночку**
  отнимает доступ у владельца аккаунта и делегированного распорядителя: путь-замена ещё не
  существует. А запись каталога прав на тип «членство» невозможна, пока нет RPC, чей
  `scope_extractor` на этот тип указывает, — обратный гейт объявляет находкой строку таблицы
  форм ответа края, до которой не дотягивается ни одна запись каталога. Оба ограничения
  тянут в одну сторону: модель, префикс, глаголы ресурса членства **сверх посаженного
  исключения из аккаунта** (§1.9), каталог и край идут **одним изменением**.

- **S3.3 после S3.2 — обязательно.** Читатель, переведённый до существования источника,
  читает пустоту. Колонка при этом жива до S4, поэтому откат S3 полон (IAM-ID-1-51).

  > [!note] Здесь стояло «S2 перед S1-в-многоаккаунтной-форме», и посылка была верна, а
  > адресат — нет
  > Требование «править модель, пока множественное членство не стало массовым» **остаётся в
  > силе**; изменилось только то, в какой стадии оно исполняется. Формулировку «пока членство
  > одно» круг 11 уточнил: одно оно у преобладающего большинства, а не у каждого (§16.2). Прежняя редакция помещала правку в S2, потому
  > что считала её выразимой без смены объекта; §2.3а показывает, что нет.
- **S4 после S3.** Снятие колонки, пока на неё смотрит хоть один читатель, оставляет
  читателя без источника; §1.5 п. 3 называет самого опасного из них.
> [!note] Здесь стояло «S5 после S4» — довод остаётся ВЕРНЫМ и переезжает вместе с предметом
> Утверждение «пока источник не авторитетен, консоль утверждала бы о человеке то, чего сервис
> не гарантирует» — не про эту стадию, а про **всякую** консольную поверхность членств. Оно
> обязано жить там, где живёт предмет (`kacho#1085` / IAM-ID-2), и снимается отсюда **вместе**
> со стадией, а не остаётся висеть указанием на порядок между S4 и тем, чего больше нет.
>
> Довод дословно, чтобы он не потерялся при переезде: **консольная поверхность членств не
> заводится раньше, чем ресурс членства станет авторитетным источником** — иначе она
> показывает то, чего сервис не гарантирует.

### Что делает каждую стадию production-complete

| Стадия | Признак завершённости (не «код написан») | Достигнут? |
|---|---|---|
| S1 | инварианты `memberships` выражены **DB-конструкцией** (ban #10), бэкфилл взаимно однозначен и проверен переписью, двойная запись покрыта пробой гонки | **да** — 2 CHECK, 2 FK, полный `UNIQUE(user_id, account_id)`, бэкфилл + зеркалящий триггер, 2 интеграционные пробы |
| S2 | отзыв утверждается **исходом** (`Check` → DENY), а не фактом вызова; осиротить право снятием членства невозможно by construction (`RESTRICT`, IAM-ID-1-60); самовалидация домена принимает строку без принадлежности (IAM-ID-1-58) | **частью** — отложенный constraint-триггер есть; **самовалидация всё ещё требует принадлежности** (предикат §4, таблица состояния) |
| S3 | **S3.1** приземлился одним изменением и проверен сравнением доступа до/после (пустым — положительный контроль); `object_type` сменён и в каталоге, и в таблице форм ответа края; префикс `mbr` в каталоге дефис-префиксов (§2.1а); **S3.2** — глагол приглашения переехал; IAM-ID-1-31, -57 и -65 зелены **без синтетики** (и зелены уже с конца S3.1 — конструируемость двух членств от этого шага не зависит, §16.2); **S3.3** — предикат сужения `ListUsers` перевыражен **внутри действующей полосы** (`scope_filtered`, а не `<exempt>` — §1.5 п. 3) и покрыт пробой на исход; **пять писателей кортежа области на объекте личности** переведены (§1.5а класс 3) — перечень исчерпывающий, его даёт сборка, а не перепись; **ни один живой потребитель не остался на снимаемом поле**; приглашение по-прежнему отвергает чужой аккаунт; край и сервис катятся вместе. **И внешнее предусловие выполнено: у IAM-ID-2 стоит `APPROVED`** — иначе четыре действующих сценария (01, 55, 68, 73) производителя не имеют (§0.2, DoD п. 16) | нет |
| S4 | замена сериализации гонки **названа и проверена**; страж `0069` переписан; каждый разрыв контракта проведён через перечень объявленных сломов и **адъюдикация проходит** | нет |
| ~~S5~~ | **стадии нет** (вердикт круга 6) — признака завершённости не имеет by construction. Требование `ui.md` правила 12 (проба браузером написана до фикса и падает на дефекте) **перенесено вслед за фиксом** в IAM-ID-2 (IAM-ID-2-22); здесь сценарий IAM-ID-1-49 стоит с записью о переезде и производителя не заказывает (§9.3). Наблюдаемое, которое этот документ всё ещё обязан утверждать, утверждает **IAM-ID-1-55** | — |

---

## §5 Сценарии Given-When-Then

Идентификатор `IAM-ID-1-NN` трассируется в имена integration- и newman-проб. REST-пути —
`/<service>/v1/<resource>`, JSON — camelCase. Мутации возвращают `Operation`; «дождаться
операции» ниже означает: полл `OperationService.Get(id)` до `done=true`, **затем проверка
`!error`**, и только затем чтение идентификатора из `metadata` — идентификатор чеканится
**до** асинхронного отказа, поэтому обратный порядок публикует фантом.

Имена полей ресурса членства (`membershipId`, `accountId`, `userId`, `state`) в сценариях
употребляются как **проектные**: точные имена фиксирует `.proto` на стадии S1, и расхождение
правится здесь, а не обходится в реализации.

### §5.0 Три вещи, общие для всей главы: формы чтения членства, чем снимается доступ, в каком окне он утверждается

Без них половина сценариев неисполнима, а другая половина флейкала бы.

**0. Форм чтения членства ДВЕ, и третьей нет** (указание владельца 2026-08-23, В-3 §7;
**контракт обеих форм принадлежит IAM-ID-2 §2.1** и здесь не переизлагается — это
предусловие, а не определение). Это предусловие **каждого** сценария главы, где вообще
читается членство, поэтому стоит первым:

| Форма | Кто субъект | Что в запросе | Права проверяются |
|---|---|---|---|
| **свой список** | сам вызывающий | **параметра пользователя нет вовсе** — субъект берётся из контекста вызова | на самом вызывающем; чужого субъекта назвать нечем **by construction** |
| **членство в НАЗВАННОМ аккаунте** | вызывающий спрашивает про аккаунт | идентификатор **аккаунта** | на **этом аккаунте** |

**Запрещена форма «в каких аккаунтах состоит НАЗВАННЫЙ человек».** Причина названа владельцем
дословно: «отображать всем в каких аккаунтах он приглашён неправильно». Приглашение — факт о
человеке, который сообщил ему **пригласивший**; третьему лицу он не адресован.

**Запрет исполняется ОТСУТСТВИЕМ ПАРАМЕТРА, а не проверкой прав.** Это несущее различие:
проверка прав может быть выражена отношением, выполнимым подстановочным знаком (`security.md`
§«Отношение, выполнимое подстановкой»), и тогда она отвечает «да» всякому
аутентифицированному. Параметра, которого в запросе нет, не обойти никаким отношением.
Признак нарушения — механический: у глагола своего списка появился аргумент, называющий
пользователя.

**Что из этого следует для проб.** Сценарий, утверждающий «у человека N членств», снимает это
**одной из двух** форм и **никогда** третьей: либо от лица самого человека (свой список), либо
пообъектно — по одному чтению на **каждый названный аккаунт**. Второй способ дороже, и это
цена запрета, а не его обход.

**A. «Перечень доступных объектов» снимается страницей и пообъектной проверкой — НИКОГДА
перечислением вселенной.** Перечисление разрешённых объектов у хранилища прав имеет жёсткий
серверный предел, **не имеет продолжения** и этот предел **общий на тип, а не по-арендаторный**
(`security.md` §«Фильтрация — страница → проверка страницы»). Сравнение двух **усечённых**
множеств «элемент в элемент» сойдётся, не доказав ничего, — и выглядеть будет как успех.

Поэтому всюду, где сценарий говорит «перечень доступа» (IAM-ID-1-29, 30, 42), он снимается
так: **страница курсором из своей БД → пообъектный `Check` партиями ≤100 → следующая
страница**. Бюджет времени принадлежит запросу, а не партии. Это тот же приём, которым
устроен списочный фильтр, и он **не** повторяет его код.

**B. Окно утверждения — ограниченный повтор, и у отказа он опаснее, чем у разрешения.**
Материализация eventually-consistent by design (IAM-ID-1-32), поэтому:

| Что утверждается | Как | Почему так |
|---|---|---|
| **доступ появился** (`Check` → разрешение) | ограниченный повтор до появления, бюджет ≈10 с, по истечении — **красное** | это read-your-writes окно, ровно тот случай, ради которого повтор законен (`testing.md` §e2e-инварианты) |
| **доступ исчез** (`Check` → отказ) | **дождаться исхода дренажа очереди**, затем утверждать отказ **однократно**, без повтора | повтор на отказе маскирует **невыполненный** отзыв: «ещё не доехало» и «не поедет никогда» выглядят одинаково, и повтор превращает второе в первое |

Различить «не доехало» от «отозвано» нечем **снаружи** — обе формы суть отсутствие кортежа.
Поэтому сценарии отзыва (33, 34) утверждают отказ **после** наблюдаемого признака завершения
дренажа (счётчик доставленного вырос / очередь пуста), а не по таймеру. Признак обязан
существовать; если его нет — это долг наблюдаемости (IAM-ID-1-62), а не повод ослабить пробу.

### Группа A — отрыв данных (`kacho#470`)

#### IAM-ID-1-01 · Один человек в двух аккаунтах — одна строка, два членства
**Given** аккаунты `A` и `B`, у вызывающего `editor` на обоих
**And** человек с почтой `p@example.test` ещё не заведён
**When** клиент вызывает `POST /iam/v1/memberships` с `accountId=A`, `email=p@example.test`
**And** дождавшись операции, вызывает то же с `accountId=B`, той же почтой
**Then** обе операции `done && !error`
**And** `GET /iam/v1/users?filter=email="p@example.test"` возвращает **ровно одну** строку
**And** её `id` совпадает в обоих ответах приглашения
**And** чтение членств аккаунта `A` возвращает членство этого человека, чтение членств
аккаунта `B` — тоже: **два раздельных чтения по названному аккаунту**, по одному на аккаунт
(производитель обоих — IAM-ID-2-01/-03)
**And** сам человек, войдя, видит **оба** членства в **своём** списке — субъект берётся из
контекста вызова, идентификатора пользователя в запросе нет (производитель — IAM-ID-2-07)
**And** третьей формы в сценарии не используется: «в каких аккаунтах состоит НАЗВАННЫЙ
человек» не спрашивается ни разу, и спросить её нечем (§5.0 п. 0)

> [!warning] Последнее утверждение переписано кругом 6 — здесь стояла ЗАПРЕЩЁННАЯ форма
> Было: `GET /iam/v1/memberships?userId=<id>` возвращает ровно два членства. Это ровно та
> поверхность, которую указание владельца запретило: параметр называет **чужого** человека, и
> ответ показывает спрашивающему, куда тот приглашён. Так же это записано и в IAM-ID-2 §2.6,
> таблица А: строка заменяется своим списком приглашённого — `GET /iam/v1/me/memberships`
> (IAM-ID-2-07).
>
> **Проверяется то же самое, и это не смягчение.** «Одна строка, два членства» — предикат
> закрытия эпика, и он снимается двумя чтениями по названным аккаунтам (вызывающий имеет
> `editor` на обоих — предусловие сценария) плюс своим списком от лица самого человека. Оба
> пути законны, вместе они дают то же множество, и **ни один** не отвечает на вопрос о
> третьем лице.
>
> **Предмет утверждения остаётся предметом ЭТОГО документа** — одна строка личности на все
> аккаунты есть свойство **данных** (S1), а не чтения; чтения здесь лишь канал наблюдения, и
> их контракт принадлежит IAM-ID-2.
>
> **Цена названа честно:** вместо одного чтения — `N` чтений по числу названных аккаунтов.
> Это и есть стоимость запрета; обходить её тем, что «нам-то нужно всего два», значит завести
> форму, которой воспользуется не только эта проба.

> Это сценарий, которого не было в исходной задаче, и он — предикат закрытия эпика.

#### IAM-ID-1-02 · Приглашение известной почты во второй аккаунт не заводит второй строки
**Given** человек `p@example.test` **активен** и состоит в `A`
**When** клиент приглашает ту же почту в `B`
**Then** операция `done && !error`
**And** число строк пользователя с этой почтой остаётся **1** (то же значение `id`)
**And** новое членство создано в состоянии «активно» — человек уже активен, шага принятия нет

> До перехода этот путь заводит вторую строку, активировать которую нельзя; проба заводится
> **с нуля** — покрытия у него нет (§1.8).

#### IAM-ID-1-03 · Приглашение неизвестной почты заводит ровно одну строку и одно членство
**Given** почты `q@example.test` в платформе нет
**When** клиент приглашает её в `A`
**Then** операция `done && !error`
**And** заведена **одна** строка человека в состоянии «приглашён», внешний идентификатор пуст
**And** заведено **одно** членство в `A` в состоянии «приглашён»

#### IAM-ID-1-04 · Первый вход активирует строку и ВСЕ её членства
**Given** `q@example.test` приглашена в `A` и в `B`, ни разу не входила
**When** человек входит в платформу впервые
**Then** его строка переходит в «активен», внешний идентификатор заполнен
**And** **оба** членства перешли в «активно» — ни одно не осталось в «приглашён»
**And** ни одна ошибка активации не проглочена: отказ активации любого членства прерывает вход отказом, а не пропуском

#### IAM-ID-1-05 · Повторное приглашение в тот же аккаунт идемпотентно
**Given** `p@example.test` состоит в `A`
**When** клиент приглашает ту же почту в `A` повторно
**Then** операция `done && !error`, второго членства не появилось
**And** число членств человека в `A` остаётся **1**

#### IAM-ID-1-06 · Глобальная уникальность почты
**Given** человек `p@example.test` существует
**When** любой путь пытается завести **вторую** строку с той же почтой (в другом регистре тоже)
**Then** отказ на уровне БД, наружу — `ALREADY_EXISTS` контракт-тоном
**And** число строк с этой почтой остаётся **1**

#### IAM-ID-1-07 · Гонка конкурентного первого входа сериализуется
**Given** `q@example.test` приглашена, ни разу не входила
**When** два входа этого человека исполняются **одновременно**
**Then** ровно один заводит строку/активирует; второй наблюдает уже заведённую и завершается успехом
**And** строк с этой почтой ровно **1**, личных аккаунтов у неё ровно **1**

> Свойство держит сегодня глобальный ключ по внешнему идентификатору
> `users_active_external_id_uniq`. Он снимается на S4 — **замена обязана быть названа в том
> же изменении**, а проба обязана быть конкурентной (`data-integrity.md` §Чек-лист, п. 5), а
> не последовательной.
>
> **Предикат:** `git grep -n 'users_active_external_id_uniq' "$R" -- services/iam/internal/migrations/ | grep -c 'CREATE UNIQUE INDEX'` → **1**.
> Единица — **объявление индекса**; голых вхождений имени в тех же миграциях **5** (`DROP` в
> обратном шаге, страж `0069`, комментарии), и считать их за объявления нельзя — это разные
> величины, обе истинные.

#### IAM-ID-1-08 · Удаление аккаунта уносит членства и не трогает людей
**Given** аккаунт `B` без проектов, служебных учёток, групп и ролей; в нём состоят двое
**When** клиент вызывает `DELETE /iam/v1/accounts/{B}` и дожидается операции
**Then** операция `done && !error`
**And** членства в `B` исчезли
**And** обе строки людей **существуют**, их идентификаторы не изменились
**And** их членства в других аккаунтах не тронуты

> **Порядок внутри транзакции — часть сценария, а не деталь реализации.** Субъекты привязок
> связаны с членством `RESTRICT` (условие У4, IAM-ID-1-33), поэтому дренаж привязок аккаунта
> обязан идти **до** каскада членств; в обратном порядке `RESTRICT` отвергнет удаление
> аккаунта, у которого всё в порядке. Проба обязана исполнять аккаунт **с выданными правами**,
> а не пустой: на пустом оба порядка зелены.

> [!warning] СНЯТЫ кругом 3: IAM-ID-1-09 и IAM-ID-1-10 — «владелец состоит в своём аккаунте»
> Номера **выведены из обращения и не переиспользуются** (§9.3). Ниже — что снято и почему;
> удалить без обоснования нельзя: снятый сценарий, о котором не сказано, неотличим от
> потерянного.
>
> **Что снято.** Парный инвариант: (09) аккаунт без членства владельца отвергается **и**
> снятие последнего членства владельца отвергается, обе стороны DB-конструкцией; (10) при
> удалении самого аккаунта инвариант не срабатывает.
>
> **Почему — решение владельца по `PRO-Robotech/kacho#610`: владеть ≠ состоять.**
> Четыре довода, ни один не вкусовой:
>
> 1. **владение объявлено самостоятельным источником прав, а не производным от участия.**
>    `security.md` §«Три уровня супер-доступа» говорит о владельце «структурно на свой
>    аккаунт»; слово противопоставлено материализации через привязки — право владельца
>    работает независимо от состояния конвейера. Право, идущее через членство, сделало бы
>    владельца участником с ролью, то есть ровно тем, чем директива запрещает ему быть;
> 2. **инвариант отверг бы штатный ввод, а не фикстуры.** Отвергается **создание второго
>    аккаунта существующим человеком** — обычная работа платформы. Проверка, отвергающая
>    нормальный ввод, — не защита, а поломка с формой защиты;
> 3. **вторая половина инварианта сделала бы выход из аккаунта невозможным.** «Создал» стало
>    бы «вступил навсегда»: человек, заведший аккаунт для другой команды, не смог бы из него
>    выйти, не сняв себя владельцем;
> 4. **это противоречит предмету самого перехода.** Смысл отрыва — человек **может**
>    принадлежать многим аккаунтам и **не обязан** принадлежать тому, которым владеет.
>    Инвариант вернул бы жёсткую сцепку с другой стороны: вместо «одна строка на аккаунт» —
>    «одно членство на каждый владеемый аккаунт».
>
> **Что действует вместо.** Аккаунт имеет владельца — это уже выражено внешним ключом
> `accounts_owner_fk` (§1.2), добавлять нечего. Видимость «мои аккаунты» = **владею ∪
> состою** — единственное следствие решения, требующее работы; оно вынесено **отдельной
> задачей** линии `release:identity` (`PRO-Robotech/kacho#610`, переведена в неё же) и
> покрывается пробой «человек, владеющий аккаунтом без членства, видит его в своём списке».
> Здесь этой пробы нет намеренно: у неё другой предмет и другой предикат закрытия.
>
> **Утверждение о дереве, которое здесь стояло и оказалось ложным** (это и есть первый из
> двух поводов круга 3): «создающую сторону через публичный глагол не построить —
> `AccountService.Create` членство владельца заводит сам». **Не заводит.** Предикат, на
> ветке, где таблица членства уже существует:
>
> ```sh
> B=78d062bb   # ХЕШЕМ, а не именем ветки: ветка снята с origin (§1.0)
> git grep -nE "(INSERT INTO|UPDATE|DELETE FROM)[[:space:]]+(kacho_iam\.)?memberships\b" "$B" \
>   -- 'services/iam/*.go' | grep -vc '_test\.go'                                        # 0
> # контроль в обратную сторону — та же форма на соседней таблице:
> git grep -nE "(INSERT INTO|UPDATE|DELETE FROM)[[:space:]]+(kacho_iam\.)?accounts\b" "$B" \
>   -- 'services/iam/*.go' | grep -vc '_test\.go'                                        # 4
> # круг 4: то же на стволе `43d332ac` — 0 и 4, вывод не изменился
> ```
>
> > [!warning] Здесь стояло `B=origin/release/identity-decoupling` — имя пережило свою ветку
> > Ветка **снята с origin** (`git ls-remote --heads origin 'refs/heads/release/identity-decoupling'`
> > → пусто). У читателя, сохранившего локальную отслеживающую ссылку, она указывает на
> > **другой** коммит — при моём замере на `244f6500`, а не на названный `78d062bb`. То есть
> > предикат либо не резолвился бы вовсе, либо **молча ответил бы про другое дерево**; вторая
> > форма опаснее, потому что даёт число, и число правдоподобное.
> >
> > §1.0 запрещает эту форму прямо, и запрет писался про `origin/main` — движущуюся ссылку.
> > Здесь тот же класс с другой стороны: ссылка не сдвинулась, а **исчезла**. Хеш переживает и
> > снятие ветки, и её перезапись.
> >
> > **Существо утверждения перепроверено независимо от ветки** и стоит: писателей таблицы
> > членства в непробном Go-коде **ноль** на обеих ревизиях, контроль по соседней таблице —
> > **4** на обеих. Вывод «владелец нового аккаунта членства в нём не получает by construction»
> > держится.
>
> Единственный писатель таблицы членства — **бэкфилл и триггер миграции** `470001`, то есть
> зеркало колонки `users.account_id`; создание **второго** аккаунта эту колонку не меняет,
> поэтому владелец нового аккаунта членства в нём не получает **by construction**.
>
> > [!note] Уточнение круга 4: «производителя нет» верно для ИНВАРИАНТА, но не буквально для ФАКТА
> > Снятые сценарии утверждали **инвариант** — «владелец обязан состоять», обеими сторонами
> > DB-конструкцией. Такого производителя действительно нет: ограничений БД **0**, проверок
> > домена **0**, проб **0**.
> >
> > Но **факт** «владелец состоит в своём аккаунте» сегодня, как правило, **истинен** — и не
> > случайно. Материализация личного аккаунта (RC-5) ставит человеку `users.account_id` на его
> > собственный аккаунт, а зеркалящий триггер `470001` переносит эту колонку в членство. То
> > есть у всякого, кто завёл аккаунт бутстрапом, членство в нём **есть** — как следствие
> > зеркала, а не как чьё-то требование. То же и в системном посеве.
> >
> > Различие несущее, потому что определяет, что покраснеет. Проба, утверждающая «владелец
> > состоит», будет **зелёной сегодня** — и останется зелёной, ничего не проверяя, ровно до
> > первого владельца **второго** аккаунта, где она и должна была бы упасть, если бы инвариант
> > существовал. Формулировка «производителя нет» относится к инварианту; тот, кто прочтёт её
> > как «факт не наблюдается», построит на этом ложное ожидание.
>
> **Считать здесь слово «member» нельзя** — и это ловушка, а не мелочь: в непробном Go-коде
> сервиса оно встречается несколько раз, и **ни одно** вхождение не про эту таблицу — все про
> членство в **группе** и про материализацию кортежей прав. Предикат обязан считать
> **писателей таблицы**, а не вхождения слова:
> `git grep -n 'memberships' "$B" -- 'services/iam/*.go' | grep -vc '_test\.go'` → **7** на
> `78d062bb` и **6** на стволе `43d332ac`. Само это расхождение и есть довод: число ловушки
> **дрейфует само по себе**, никак не относясь к вопросу, — поэтому опираться на него нельзя
> ни в какую сторону.

#### IAM-ID-1-11 · Бэкфилл взаимно однозначен
**Given** дерево до S1, N строк пользователей (N берётся из П4)
**When** применена миграция S1 с бэкфиллом
**Then** членств ровно **N**
**And** для каждой строки её членство ведёт **в тот же** аккаунт, что был в снятой колонке
**And** состояние членства соответствует состоянию приглашения строки
**And** обратная перепись пуста: нет членства без строки и строки без членства

#### IAM-ID-1-12 · После S4 личный аккаунт удаляется
**Given** человек `u` со своим личным аккаунтом, из которого удалены проект `default` и прочие дети
**And** у `u` не осталось грантов, мешающих снятию
**When** клиент вызывает `DELETE /iam/v1/accounts/{личный}`
**Then** операция `done && !error`, аккаунт снят
**And** строка `u` существует, идентификатор не изменился
**And** попытка снять **самого `u`**, пока он владеет другим аккаунтом, по-прежнему отвергается `FAILED_PRECONDITION` **немедленно** — структурное владение сохранено намеренно

#### IAM-ID-1-13 · Отказы, которые создаёт ЭТО изменение, правдивы
**Given** человек, снятие которого упирается в новую ссылку из членства
**When** клиент вызывает `DELETE /iam/v1/users/{id}`
**Then** `FAILED_PRECONDITION`, сообщение называет **членство** и предмет, а не общий запасной текст
**And** рядом стоит `reason`-токен, и клиент ключуется на него, а не на прозу

> **Токена под этот отказ в таблице сегодня НЕТ, и это часть работы, а не деталь.** Пять
> объявленных токенов (`api-conventions.md` §By-lane code-split) описывают полосы формата,
> собственного чтения и обращения к владельцу. **Предикат** (дерево воркспейса, переменная
> `W` из §1.0 — правила живут в другом репозитории, и путать ревизии нельзя):
> `git show "$W":.claude/rules/api-conventions.md | grep -cE '^\| `(INVALID_RESOURCE_ID|RESOURCE_NOT_FOUND|PEER_RESOURCE_MISSING|PEER_RESOURCE_STATE|PEER_UNAVAILABLE)`'` → **5**.
> Отказ «человека не снять — мешает членство» —
> **within-service предусловие**, и ни один из пяти ему не подходит. Значит таблица
> расширяется новым токеном; это **правка контракта**, она идёт вопросом В-5 §7 и вносится
> в тот же раздел правил тем же изменением. Взять близкий по смыслу чужой токен — худший
> исход из возможных: клиент начнёт различать полосы неверно.
**And** отказ, приходящий на `COMMIT` отложенной проверки, маршрутизируется тем же constraint-aware отображением и **не уезжает в `INTERNAL`**

> Отказы, существовавшие **до** этого изменения (§6 п. 9), здесь не чинятся — иначе радиус
> расползается и краснота одного предмета объясняется правкой другого.

#### IAM-ID-1-71 · Отказ при ВНЕШНЕМ гранте называет внешний грант
**Given** человек, у которого остался грант в аккаунте, **недоступном удаляющему**
**When** удаляющий вызывает снятие этого человека
**Then** отказ называет **именно это**: препятствие находится за пределами его периметра
**And** отказ **не** предлагает действие, которое вызывающий выполнить не может, и не выдаёт
идентификатор чужого аккаунта — иначе отказ сам становится оракулом
**And** положительный контроль: тот же человек без внешних грантов снимается штатно

> Тупик с ложной табличкой хуже непонятного: без этого оператор идёт снимать гранты и
> упирается в объект, которого не видит. Дренаж этого не меняет — у удаляющего туда нет
> доступа by construction.

### Группа A2 — членство как production-ресурс: негативы, снятие, поверхность

Членство — **новый first-class ресурс**, и ban #14 требует сразу полной формы, а не
happy-path. Ниже — классы, которых у группы A нет by construction.

#### IAM-ID-1-63 · Malformed идентификатор отвергается СИНХРОННО, первым стейтментом
> **[САНКЦИЯ ОТОЗВАНА 2026-08-24]** плоская поверхность **чтения** членства отменена решением
> `PRO-Robotech/kacho#1085`; предмет сценария (синхронный отказ формы) сохранён и перенацелен
> на аккаунт-скоупный путь — IAM-ID-2-04.
>
> **Условие про префикс `mbr` ОТСЮДА ВЫНЕСЕНО** (круг 7, Б2). Оно стояло здесь и адресовалось
> соседнему документу — то есть единственное место, где оно называлось, было местом **без
> санкции**, а зависят от него **действующие** сценарии этого документа (64, 65). Норма,
> предикат и обоснование — **§2.1а**; шаг, на котором она приземляется, — S3.1 (§2.3а).

**Given** дерево после S1
**When** клиент вызывает `GET /iam/v1/memberships/{id}` с `id = "не-идентификатор"`
**Then** `INVALID_ARGUMENT` / HTTP **400**, текст контракт-тоном `"invalid <res> id '<X>'"`
**And** отказ синхронный, до любого обращения к хранилищу
**And** положительный контроль: well-formed, но несуществующий идентификатор даёт **`NOT_FOUND`**, а не 400
**And** то же верно для **второй** формы чтения — членств **названного аккаунта**: malformed
идентификатор аккаунта отвергается синхронно, первым стейтментом, тем же тоном
**And** у **первой** формы — своего списка — поверхности malformed-идентификатора **нет
вовсе**: параметра, называющего пользователя, в запросе не существует (§5.0 п. 0). Это
утверждается **отдельно и явно**, а не подразумевается умолчанием

> **Отсутствие входа — тоже свойство, и оно проверяемо.** Утверждение «параметра нет»
> снимается контрактом (в запросе своего списка нет поля пользователя) и пробой: запрос с
> подставленным полем пользователя **не** сужает и не расширяет ответ, потому что край такое
> поле отбрасывает молча (`api-conventions.md` §«Принято-и-проигнорировано»). Именно поэтому
> поле обязано **отсутствовать в контракте**, а не «игнорироваться»: молча принятое поле
> читается вызывающим как работающее.

#### IAM-ID-1-64 · Полоса резолва — ПРЯМОЕ ЧТЕНИЕ, потому что соседа здесь нет
**Given** дерево после S1
**And** предусловие §2.1а выполнено: префикс `mbr` стоит в каталоге дефис-префиксов платформы
**When** клиент создаёт членство с несуществующим **аккаунтом**
**Then** `NOT_FOUND` / HTTP **404** с `reason`-токеном `RESOURCE_NOT_FOUND`, контракт-тоном
`"Account <id> not found"` — **аккаунт есть собственная строка iam**, и полоса прямого чтения
**And** чтение собственного несуществующего членства даёт **тот же** `NOT_FOUND` с тем же токеном
**And** утверждаются **и код, и токен**: тон сообщения стабилен, но клиент ключуется на токен
(`api-conventions.md` §By-lane code-split)
**And** чтение членств **названного** аккаунта, которого не существует, отвечает **той же**
полосой, что и создание — иначе один и тот же факт («такого аккаунта нет») получал бы у
одного сервиса два разных ответа
**And** отрицательный контроль полосы: **ни один** ответ этих глаголов не несёт токена
семейства `PEER_*`. Предикат — `git grep -nE 'PEER_[A-Z]' "$R" -- services/iam | grep -vc
'_test\.go\|/tests/'` даёт **0** (единственное вхождение под каталогом — проза кейса newman);
контроль в обратную сторону — **та же команда** по дереву находит потребителей **чужих**
сервисов, и их **пять**: `compute` · `nlb` · `registry` · `storage` · `vpc`, — то есть
различает полосы, а не находит слово
**And** у **своего списка** вопроса о полосе нет **by construction**: чужого идентификатора он
не принимает, поэтому ни peer-полосы, ни own-miss'а на нём не возникает — и это утверждается
**отдельно**, чтобы отсутствие ветви не читалось как её пропуск

> [!note] Круг 9: скобка перечисляла результат ДРУГОГО предиката — исправлено названием
> Здесь стояло «та же команда … (`compute`, `nlb`, `registry`, `vpc`)». Четвёрка получена
> переписью **по конструктору полосы**, а не грепом по токену, которым объявлен предикат:
>
> ```sh
> # А — по токену (это и есть «та же команда»): пять сервисов
> git grep -lE 'PEER_[A-Z]' "$R" -- services/ | cut -d/ -f2 | sort -u
> #   → compute · iam(только проза кейса) · nlb · registry · storage · vpc
> # Б — по конструктору полосы: четыре
> git grep -lE 'ReasonPeerResourceMissing|ReasonPeerResourceState|ReasonPeerUnavailable' \
>   "$R" -- services/ | cut -d/ -f2 | sort -u
> #   → compute · nlb · registry · vpc
> ```
>
> **Обе величины истинны, и подгонять было нечего** — расходится единица счёта, ровно как в
> расхождении переписей `PEER_*` между кругами 6 и 7, которое вердикт круга 8 принял по той
> же причине. Неверна была **атрибуция**: перечень объявлен выводом команды, которая его не
> производит, и следующий читатель, набрав команду по её словам, получил бы пятый сервис и
> счёл бы документ ошибочным.
>
> **Вывод сценария не меняется ни в одну сторону:** несущее утверждение — **ноль** у `iam`
> вне проб, и оно верно у обоих предикатов. Контроль сохраняет работу при любом из двух:
> он доказывает, что команда умеет находить, а не молчит всегда.

> [!warning] Здесь стояло `FAILED_PRECONDITION` + токен полосы «чужой ресурс отсутствует» —
> и это противоречило РЕШЕНИЮ, которое тот же сервис уже принял ЯВНО (найдено кругом 6, Б4;
> исправлено кругом 7)
> Обоснование прежней редакции — «аккаунт резолвится у своего владельца». Владелец аккаунта
> — **сам iam**: аккаунт и членство лежат в одной базе одного сервиса, peer-вызова здесь нет
> ни одного. Полоса `peer-validate` (`api-conventions.md` §By-lane code-split) описывает
> **cross-service** ссылку, а внутрисервисная — это `direct-read`.
>
> **Дерево решило этот вопрос ЯВНО, прокомментировало решение и поставило его НА ПУТЬ
> ЗАПИСИ** (перемерено кругом 7 на `release/identity-4` `8a3e00f18`). У соседнего ресурса
> того же сервиса — предела — общий маппер SQLSTATE переопределён отдельной ветвью:
> `services/iam/internal/repo/kacho/pg/limit_repo.go`, `mapLimitErr`, ветвь 23503. Её
> собственный комментарий говорит дословно: общий маппер читает 23503 как
> `FAILED_PRECONDITION`, что верно для настоящего внешнего ключа (предусловие соседа);
> **здесь соседа нет — аккаунты и проекты суть собственные строки iam, значит полоса прямого
> чтения и ответ `NOT_FOUND`**. Тот же выбор повторён на пути чтения
> (`services/iam/internal/apps/kacho/api/limit/usecases.go`, «Direct-read lane: accounts and
> projects are iam's OWN rows»), а строит отказ закрытый тип полосы
> (`kerrors.ReasonResourceNotFound`), чтобы код и токен не могли разойтись.
>
> **Почему это блокировало, а не «стилистика».** Сценарий пиннит контракт, и проба его
> **закрепит**. Закрепив `FAILED_PRECONDITION`, мы получили бы у одного сервиса два
> противоположных ответа на один и тот же факт «названного аккаунта нет»: `404` у предела и
> `400` у членства. Клиент, ключующийся на токен (а именно этого требует конвенция),
> различал бы их как **разные события**.
>
> **`kerrors.PeerRef` полосы НЕ объявляет** — это носитель координаты («о каком ресурсе
> отказ»), и iam пользуется им **вместе с** `ReasonResourceNotFound`. Считать его признаком
> peer-полосы значит мерить имя типа вместо смысла.
>
> **Если членство обоснует отступление — оно объявляется РЕШЕНИЕМ, а не умолчанием.**
> Запись идёт в `services/iam/docs/engineering/architecture/` со ссылкой отсюда
> (`api-conventions.md` требует ровно этого от всякого отступления от полос). Молчаливое
> отступление остаётся нарушением.

> [!important] Чем аккаунт-скоупное чтение НЕ вправе отвечать — и это самый тихий способ
> вернуть запрещённую форму
> Чтение членств аккаунта, к которому у вызывающего нет прав, обязано отвечать **одинаково**
> для существующего и несуществующего аккаунта: иначе оно становится оракулом существования
> (`security.md` Hardening #6, hide-existence байт-в-байт). Иначе спрашивающий, перебирая
> идентификаторы аккаунтов, восстановит по различию ответов ровно то, что запрет закрывал, —
> только с другой стороны и по одному аккаунту за раз.

#### IAM-ID-1-65 · Исключение из аккаунта: код, идемпотентность, несуществующее, порядок с выдачами
**Given** человек состоит в `A` (не личном) **и в `B`** — оба членства на **одной** строке
личности; такая пара строится обычным приглашением известной почты во второй аккаунт и
синтетики не требует (конструируемость доказана посевом — §16.2)
**And** вызывающий несёт `member_remover` на `A`, а вызов — повышенный уровень (`acr ≥ 2`)
**When** клиент вызывает `POST /iam/v1/users/{userId}:removeFromAccount` с телом
`accountId = A` и дожидается операции
**Then** операция `done && !error`
**And** **строка личности не тронута**: тот же `usr-…`, та же почта, то же состояние, те же
метки — и это утверждается **чтением человека после вызова**, а не обещанием
**And** тот же человек продолжает работать в `B`, где его членство осталось
**And** повторный вызов с той же парой **не роняет** вызывающего: исход объявлен явно
(идемпотентный успех либо `NOT_FOUND`) и **один и тот же** при каждом повторе — аргумент есть
**отсутствие** членства, а не переход между состояниями
**And** исключение из аккаунта, в котором человек не состоит, даёт `NOT_FOUND`, а не `INTERNAL`
**And** **порядок «сперва права, потом участие» проверяется ИСХОДОМ**: пока в `A` жива выдача
на этого человека, вызов отвергается `FAILED_PRECONDITION` контракт-тоном — и отказ приходит
**на коммите**, от отложенного триггера `membership_carrying_rights_is_kept` (миграция
`472002`, стадия S2), а не от проверки в use-case (ban #10)
**And** положительный контроль к предыдущему: сняв выдачу, тот же вызов **проходит** — иначе
`FAILED_PRECONDITION` зеленел бы на глаголе, отвергающем всё
**And** исключение владельца из **своего** аккаунта **проходит** штатно — владеть не значит
состоять (`PRO-Robotech/kacho#610`), и особого запрета здесь нет: положительный контроль на
то, что снятое кругом 3 требование не всплыло обратно проверкой в use-case
**And** указатель области снимается вместе с участием, и это утверждается **парой**: **до**
вызова аккаунт `A` — предок этой личности, **после** вызова он ей не предок. Голое «после
вызова отсутствует» зеленеет на системе, которая такого указателя не пишет **вовсе**, — то
есть ровно там, где сценарий обязан краснеть
**And** форма указателя названа **по концу стадии, а не по сегодняшнему дню**: до S3.1 это
кортеж `account:A` → `account` → `iam_user:<id>`, после S3.1 — его перевыраженная замена,
ключуемая на членство (§1.5а класс 3, пятый писатель; зеркало предиката — выноска `31`).
Утверждается **наблюдаемое** — предковость, а не написание кортежа: иначе на конце стадии
утверждение станет тождественно истинным по посторонней причине, и снятие указателя перестанет
проверяться в тот самый момент, когда его форму меняют

> [!important] Круг 9 ПЕРЕНАЦЕЛИЛ сценарий на ПОСАЖЕННЫЙ глагол — и это исход по Б8.3, а не
> смена предмета
> Прежняя редакция называла предмет «снятие членства», требовала предусловия §2.1а (префикс
> `mbr`) и тем самым предполагала адресацию **по идентификатору членства**. В дереве при этом
> уже работает `UserService.RemoveFromAccount`, адресуемый парой «человек + аккаунт», с
> отношением `member_remover`, записью каталога в обеих вшитых копиях, повышенным уровнем и
> реализацией (§1.9 — там предикаты и решение целиком).
>
> **Чем это грозило исполнителю.** Он либо завёл бы **второй** глагол снятия членства — два
> места об одном предмете, — либо решил, что документ описывает другое дерево и остановился.
> Выбирать между этими двумя не его работа: что заказывается, а что уже сделано, обязан
> назвать документ.
>
> **Предусловие §2.1а из `Given` СНЯТО** — глагол идентификатора членства не принимает,
> поэтому требовать префикс здесь значило бы объявить предусловие, от которого сценарий не
> зависит. У §2.1а остаётся живой потребитель — IAM-ID-1-64, чтение членства **по
> идентификатору**.
>
> **Что сценарий приобрёл взамен.** Он пиннит то, чего прежняя редакция не утверждала вовсе:
> неприкосновенность строки личности, работу в остальных аккаунтах, порядок с выдачами
> **исходом** (плюс положительный контроль к нему) и снятие указателя области. Первое и
> третье суть несущие свойства перехода — без них «исключён из аккаунта» неотличимо от
> «удалён с платформы» и от «снята выдача».
>
> **Идемпотентность здесь — свойство предмета, а не приём**: аргументом объявлено отсутствие
> членства, поэтому повтор безопасен by construction, а не потому, что кто-то помнит о нём.

#### IAM-ID-1-66 · Пагинация проверяется ДО замыкания на пустом гранте
> **[САНКЦИЯ ОТОЗВАНА 2026-08-24]** плоская поверхность **чтения** членства отменена решением
> `PRO-Robotech/kacho#1085`; предмет (порядок «формат раньше замыкания») сохранён и
> перенацелен — IAM-ID-2-05, где вдобавок названа **полоса** гейта, от которой зависит
> наблюдаемый исход у вызывающего без прав.
>
> **Остаток про `ListUsers` ОТСЮДА ВЫНЕСЕН — он живёт в IAM-ID-1-21** (круг 7, Б6). Здесь
> стояло «остаётся предметом этого документа», и это было **два состояния в одном сценарии**:
> санкции нет, а требование объявлено действующим. Исполнителю нечем было выбрать, а счёт
> §9.3 считает сценарий целиком, не половинами.
>
> `ListUsers` этот документ перевыражает **сам** (шаг S3.3), поэтому требование «проверка
> формата стоит в той же функции, которая замыкается на пустом гранте» переехало в
> **действующий** сценарий IAM-ID-1-21 своим утверждением, а не осталось долгом при
> отозванном тексте. Ниже — исходное тело, сохранённое как свидетельство; **производителя оно
> не заказывает**.

**Given** вызывающий, которому не выдано ничего
**When** он вызывает **каждую** из двух форм чтения (§5.0 п. 0) с `pageSize=5000` и, отдельно,
с мусорным `pageToken`: свой список и членства названного аккаунта
**Then** **все четыре** вызова — `INVALID_ARGUMENT` / **400**, а не `200 []`
**And** то же верно для перевыраженного `ListUsers`
**And** проверка стоит **в той же функции**, которая замыкается на пустом гранте, — у **каждой**
формы своя, потому что замыканий два и они в разных функциях
**And** tree-wide гейт `internal/repohygiene` `TestEmptyPageNeverPrecedesPaginationValidation` остаётся зелёным

> **У своего списка замыкание на пустом гранте тоже есть — и это не очевидно.** Свой список
> ключуется на вызывающего, поэтому кажется, будто грант тут ни при чём. Но членства
> вызывающего фильтруются правами на **аккаунты**, в которые они ведут, и у человека без
> единого гранта множество пусто — то есть тот же ранний выход по той же причине. Форма,
> у которой «грант ни при чём», — это форма, которая ничего не фильтрует.

> Юнит на саму функцию проверки регрессией на это **не является**: он зовёт её без
> вызывающего и без замыкания, поэтому о порядке не утверждает ничего.

#### IAM-ID-1-67 · Список членств фильтруется по правам — ПЕРЕНАЦЕЛЕН на обе формы, третьей не существует
> **[САНКЦИЯ ОТОЗВАНА 2026-08-24]** плоская поверхность **чтения** членства отменена решением
> `PRO-Robotech/kacho#1085`; «фильтруется по правам» отменено не из-за фильтрации, а из-за
> **формы вопроса**: плоский список спрашивает про все аккаунты сразу, и его правильность
> держалась бы кодом, который можно забыть. Замена — аккаунт-скоупный список, у которого
> аккаунт **обязателен** и входит в отбор строк (IAM-ID-2 §2.1, IAM-ID-2-01/-03), и свой
> список (IAM-ID-2-07/-21). Так это и записано в IAM-ID-2 §2.6, таблица А.
>
> **Тело сценария сохранено и переписано под обе формы не для того, чтобы завести здесь
> второй контракт чтения** — он принадлежит IAM-ID-2.
>
> **Требование к их ПАРЕ снято отсюда и адресовано держателю** (круг 7, Б6). Здесь стояло
> «остаётся требование к их паре» — при объявленной отозванной санкции. Два состояния в
> одном сценарии: реализовывать нельзя, а требование действует. Исход выбран второй из двух,
> названных вердиктом, — **снятие со ссылкой на держателя**, и вот почему именно он:
> предмет пары есть **публичная форма чтения**, а перевыразить её здесь значило бы завести
> второй контракт ровно того, что абзацем выше объявлено чужим предметом.
>
> **Держатель назван поимённо, чтобы требование не потерялось при снятии:** обе половины
> указания владельца живут в IAM-ID-2 — «третьему лицу не показываем» (IAM-ID-2-03, -13,
> -15) и «приглашённый видит, куда он приглашён» (IAM-ID-2-08, -21). Требование, которого
> **ни один из них не несёт по отдельности** — что обе половины утверждаются **вместе**, —
> адресовано IAM-ID-2 как замечание к её кругу: односторонняя проба зеленеет на реализации,
> не показывающей приглашение **никому, включая самого приглашённого**.
>
> Ниже — исходное тело, сохранённое как свидетельство; **производителя оно не заказывает**.

**Given** аккаунты `A` и `B`, в обоих есть члены; вызывающий имеет доступ только к `A`
**And** человек `p@example.test` состоит в `A` и **приглашён** в `B`
**When** вызывающий читает членства **названного** аккаунта `A`
**Then** ответ содержит членства `A`, в том числе членство `p@example.test`
**When** он читает членства **названного** аккаунта `B`
**Then** он **не** узнаёт, что `p@example.test` туда приглашён: ответ по недоступному аккаунту
не различает «нет прав» и «нет аккаунта» (IAM-ID-1-64)
**When** сам `p@example.test` читает **свой** список
**Then** он видит **оба** своих членства — активное в `A` и приглашение в `B`, потому что
приглашённый вправе знать, куда он приглашён
**And** положительный контроль: получив доступ к `B`, вызывающий видит и его членства
**And** гейт `make -C services/iam audit-list-filter` зелёный и **покрывает оба новых глагола**
**And** отрицательное утверждение стоит в паре с положительным на **каждой** форме: «не видит»
без «а вот это видит» зеленеет на глаголе, отвечающем пустым списком всегда

> [!important] Этот сценарий — главный производитель указания владельца, и его отрицание
> **односторонним быть не может**
> Указание несёт **две** половины сразу: «отображать всем в каких аккаунтах он приглашён
> неправильно» **и** «приглашённый в своём списке может увидеть где он приглашён». Проба,
> утверждающая только первую, зеленеет на реализации, которая не показывает приглашение
> **никому, включая самого приглашённого**, — то есть на нарушении второй половины. Поэтому
> обе стоят здесь, в одном сценарии, а не разнесены по двум.

#### IAM-ID-1-68 · Каталог прав полон для каждого нового RPC
**Given** заведены глаголы ресурса членства
**When** прогнан `make -C gateway permission-catalog-check`
**Then** гейт зелёный, обе встроенные копии каталога **байт-в-байт** совпадают
**And** у каждого нового RPC есть запись каталога — её отсутствие даёт fail-closed отказ в рантайме, а не «работает по умолчанию»
**And** ни один новый RPC не гейтится отношением, выполнимым подстановочным знаком (`security.md` §«Отношение, выполнимое подстановкой»)
**And** записи заведены **на обе** формы чтения (§5.0 п. 0), и область каждой названа своим
объектом: у чтения членств названного аккаунта область — **этот аккаунт**; у своего списка
область — **сам вызывающий**
**And** записи, которая принимала бы **чужой** идентификатор пользователя как область, в
каталоге **нет** — предикат: перечислить записи новых глаголов и потребовать, чтобы ни одна не
извлекала область из поля, называющего пользователя

> [!warning] У СВОЕГО списка объект проверки — сам вызывающий, и это ЕДИНСТВЕННАЯ форма,
> при которой отношение не вырождается
> Соблазн — загейтить свой список кластерным отношением («любой аутентифицированный вправе
> читать свои членства»). Такое отношение выполняется подстановочным кортежем и означает
> **«аутентифицирован»**, а не «авторизован» (`security.md` §«Отношение, выполнимое
> подстановкой»). Пока входов у глагола нет, это безвредно — отвечать он может только про
> вызывающего. **Вредно оно становится ровно в тот момент, когда кто-нибудь добавит глаголу
> параметр пользователя**: гейт продолжит отвечать «да» всем, и запрещённая форма откроется
> без единой правки каталога.
>
> Поэтому область объявляется **вызывающим**, а не кластером: тогда добавление параметра
> ломает запись каталога и требует явного решения, а не проходит молча.

#### IAM-ID-1-73 · Снимок прав и свой список членств — один производитель, две проекции, разность названа
**Given** дерево после S3
**And** человек `p@example.test`: активное членство в `A`, **приглашение** в `B`, и он **владеет**
аккаунтом `C`, не состоя в нём (владеть ≠ состоять, `PRO-Robotech/kacho#610`)
**When** он читает **снимок прав** собственного профиля и, отдельно, **свой список членств**
(производитель этого чтения — IAM-ID-2-07; здесь оно канал наблюдения, а не предмет)
**Then** членская часть снимка — **подмножество** списка: `A` есть в обоих
**And** разность внутри членской части — **ровно** записи в состоянии «приглашён»: `B` есть в
списке и **отсутствует** в снимке, потому что приглашение доступа не даёт
**And** `C` есть в снимке и **отсутствует** в списке — источник не членский (владение), и это
**не потеря**: утверждается явно, а не вычитается молча
**And** оба ответа производит **один** источник в части членства — таблица членств; ни одна из
проекций не вычисляет членство своим выражением
**And** положительный контроль: у человека **без приглашений и без владения-без-членства** оба
ответа совпадают **элемент в элемент**
**And** заблокированный человек в снимке членской части **не получает** (отбор по состоянию
личности), а его членства остаются активными — блокировка есть свойство личности, а не
членства (§7, В-8)

> [!important] Односторонняя проба здесь зеленеет на пустом ответе, и это не гипотетика
> «Снимок ⊆ список» выполняется тождественно, если список отвечает **всем**, и тождественно
> же, если снимок **пуст**. Оба состояния суть поломка, и оба дают зелёное. Поэтому
> положительный контроль (совпадение элемент в элемент на человеке без приглашений)
> **обязателен**, а не желателен: он и есть то утверждение, которое падает на пустом ответе.
>
> Предметом сценария является **класс 9 §1.5а**, у которого до круга 6 не было ни стадии, ни
> производителя. Единица счёта: **аккаунты**, а не членства — у человека в аккаунте членство
> одно (`UNIQUE(user_id, account_id)`, миграция `470001`), поэтому оба множества сравнимы.

### Группа B — материализация личного аккаунта СОХРАНЯЕТСЯ (директива, п. 3)

#### IAM-ID-1-14 · Свежий человек получает личный аккаунт, проект и привязки
**Given** человек в платформе не заведён и не приглашён
**When** он входит впервые
**Then** заведён личный аккаунт, владелец — он
**And** в нём заведён проект `default`
**And** заведены две самоадминистративные привязки
**And** заведено его членство в собственном личном аккаунте — потому что **бэкфилл и путь
появления человека его заводят**, а не потому, что этого требует инвариант: инвариант снят
(см. выноску группы A), и утверждение здесь — про **исход штатного пути**, а не про запрет

> Различие несущее. «Членство есть, потому что его завели» фальсифицируемо: путь может
> перестать его заводить, и проба покраснеет. «Членство есть, потому что иначе нельзя»
> опиралось на конструкцию, которой нет, — и зеленело бы ровно до первого второго аккаунта.

#### IAM-ID-1-15 · ПРИГЛАШЁННЫЙ тоже получает личный аккаунт
**Given** `q@example.test` приглашена в чужой аккаунт `A`, ни разу не входила
**When** она входит впервые
**Then** её членство в `A` перешло в «активно»
**And** **сверх того** заведён её личный аккаунт с проектом `default` — директива RC-5 действует дословно
**And** членств у неё **два**: в `A` и в своём личном

> Именно этот сценарий директива защищает: соблазн «приглашённому личный аккаунт не нужен»
> отменил бы её.

#### IAM-ID-1-16 · Предикат ворот остаётся «собственных аккаунтов ноль»
**Given** человек, состоящий в N чужих аккаунтах (N ≥ 1) и **не владеющий** ни одним
**When** он входит
**Then** личный аккаунт **заводится**
**And** проба падает, если ворота переформулированы как «членств ноль»

> **Состояние конструируется двумя путями, и оба законны:** (а) приглашённый, ни разу не
> входивший, — тогда это первый вход и сценарий смыкается с IAM-ID-1-15; (б) человек,
> удаливший свой личный аккаунт и сохранивший чужие членства (IAM-ID-1-18). Путь (б)
> предпочтителен: он даёт состояние, **отличимое** от «впервые вижу человека», поэтому
> различает именно предикат ворот, а не факт первого появления.
>
> Отрицание в паре с положительным контролем: IAM-ID-1-17 — противоположный вход.

#### IAM-ID-1-17 · Второго личного аккаунта не появляется
**Given** человек, уже владеющий личным аккаунтом
**When** он входит снова (в том числе многократно и одновременно)
**Then** число аккаунтов, которыми он владеет, остаётся **1**
**And** ни одного дублирующего проекта `default` не заведено

#### IAM-ID-1-18 · Удалил личный — при следующем входе он возвращается
**Given** человек удалил свой личный аккаунт (IAM-ID-1-12)
**When** он входит снова
**Then** личный аккаунт **заводится заново** — ворота «собственных аккаунтов ноль» выполнены

> Поведение прямо следует из сохранённой директивы и **неочевидно**: без этой пробы оно
> всплывёт как «откуда взялся аккаунт». Ответ на вопрос «а надо ли иначе» — В-1 §7; до
> ответа владельца действует сегодняшнее правило дословно.

### Группа C — контракт (`kacho#471`)

#### IAM-ID-1-19 · `User` не несёт аккаунта, номер и имя зарезервированы
**Given** дерево после S4
**When** клиент вызывает `GET /iam/v1/users/{id}`
**Then** тело ответа поля `accountId` **не содержит**
**And** в `.proto` стоят `reserved 6;` и `reserved "account_id";`
**And** разрыв объявлен записью в `proto/declared-breaks.yaml` со всеми пятью обязательными полями (`rule`, `path`, `symbol`, `reason`, `issue`), и адъюдикация конвейера **проходит**

#### IAM-ID-1-69 · Схлопнутый идентификатор отвечает надгробной плитой, побайтово
**Given** при слиянии сохранена каноническая строка, неканоническая снята (§2.1)
**When** клиент читает человека по **снятому** идентификатору
**Then** `NOT_FOUND` / HTTP **404**
**And** тело ответа **побайтово совпадает** с ответом на заведомо несуществующий, никогда не выданный идентификатор того же вида
**And** различимого ответа не существует ни в одном поле: любой различающийся байт есть оракул (`security.md` §Hardening #6)
**And** таблицы псевдонимов и мягкой переадресации в тракте нет — предикат по дереву даёт ноль

#### IAM-ID-1-70 · Фильтр списка снят с резервированием номера и имени
**Given** дерево после S4
**When** проверяется контракт запроса списка пользователей
**Then** стоят `reserved 4;` и `reserved "account_id";`
**And** запрос с этим полем не принимается молча: край его отбрасывает, а сервис поля не имеет — то есть возможности «сузить по аккаунту» через старую дверь не остаётся
**And** разрыв объявлен записью перечня со всеми пятью полями

> Разворот решения назван в §1.5 п. 3: документ решения оставлял поле, сменив ему смысл.
> Здесь оно **снимается**, и §1.8 объясняет, почему — вопрос «кто в этом аккаунте»
> принадлежит списку членств, а у поля обнаружились живые потребители (§1.5а), которых
> смена смысла всё равно потребовала бы переписать.

#### IAM-ID-1-20 · `Update` с аккаунтом в маске отвергается
**Given** дерево после S4
**When** клиент вызывает `PATCH /iam/v1/users/{id}` с `updateMask=accountId`
**Then** `INVALID_ARGUMENT` / HTTP **400**
**And** сообщение — про **неизвестное поле маски**, а не «immutable after User.Create»: поля больше нет, и текст про неизменяемость утверждал бы о существующем

#### IAM-ID-1-21 · Список пользователей сужается членствами, а не колонкой
**Given** вызывающий состоит в `A` и не состоит в `B`
**And** в `B` есть люди, не состоящие в `A`
**When** он вызывает `GET /iam/v1/users`
**Then** ответ содержит людей, с которыми он делит хотя бы один аккаунт
**And** ответ **не содержит** ни одного человека, состоящего только в `B`
**And** ответ **не называет, ЧЕРЕЗ КАКОЙ аккаунт** человек попал на страницу: у `User` поля
аккаунта нет (`reserved 6`), и перевыражение фильтра его не возвращает ни в каком виде
**And** утверждается **исход** фильтра — состав страницы, — а не факт его вызова
**And** положительный контроль: получив членство в `B`, тот же вызывающий видит этих людей
**And** **формат проверяется РАНЬШЕ замыкания на пустом гранте** (принято из IAM-ID-1-66,
круг 7): вызывающий, которому не выдано ничего, на `pageSize=5000` и, отдельно, на мусорном
`pageToken` получает `INVALID_ARGUMENT` / **400**, а не `200 []`
**And** проверка стоит **в той же функции**, которая замыкается на пустом гранте: «валидирует
репозиторий» верно ровно для пути, который до репозитория доходит, а замыкание до него не
доходит by construction (`api-conventions.md` §Gotcha'и, `security.md` §Hardening #7)
**And** tree-wide гейт `internal/repohygiene` `TestEmptyPageNeverPrecedesPaginationValidation`
остаётся зелёным

> `UserService.List` несёт `permission = "iam.users.list"` и `scope_filtered = true` —
> **сужение на данных**, а не освобождение: обработчик читает страницу курсором и спрашивает
> модель про идентификаторы этой страницы. Отрыв меняет то, чем выражается членство, поэтому
> предикат сужения перевыражается в том же изменении — **внутри этой полосы**. Проба,
> утверждающая «фильтр вызван», на этом предмете зеленеет при любом предикате.
>
> **Перевыражение идёт ВНУТРИ сервиса и поверхностного вопроса не заводит** (круг 6). Фильтр
> отвечает «делим ли мы хотя бы один аккаунт» — предикат существования над таблицей членств
> (`membershipInAny`), а не перечень аккаунтов. Ответ по-прежнему **не называет** аккаунтов
> вовсе, поэтому директива владельца по В-3 этот сценарий **не** задевает: она про то, кому
> показывают перечень, а здесь перечня нет ни в запросе, ни в ответе.
>
> **Это сказано явно потому, что молчание читалось бы наоборот.** Список пользователей —
> самое очевидное место, куда «заодно» просится колонка аккаунтов; документ, перечисливший
> запрет в §5.0 и промолчавший здесь, оставил бы вопрос открытым ровно там, где он возникает
> первым.
>
> **Предикат** (`blk` — §1.5): `blk List | grep -c 'scope_filtered'` → **2**; контроль в
> обратную сторону — `blk Invite | grep -c 'scope_filtered'` → **0**, то есть команда
> различает глаголы, а не находит слово в файле.
>
> [!warning] Круг 9: здесь стоял предикат по `"<exempt>"`, и он БОЛЬШЕ НЕ РАЗЛИЧАЕТ
> Прежняя пара — `blk List` → **1** при контроле `blk Invite` → **0**. Сегодня **оба конца
> дают 0**: полоса глагола сменилась (`91a04dc16`, шесть списков iam ушли из освобождения в
> сужение на данных), и команда отвечает одинаково на предмете и на контроле.
>
> **Ошибка была направлена в сторону снятой защиты, и это худшее направление.** Исполнитель
> S3.3, читающий «объявлен `<exempt>`», пошёл бы искать освобождение и, не найдя, либо
> вернул бы его, либо остановился. Возврат `<exempt>` тих: запись каталога выглядит обычной,
> а смысл у неё **второй** — решение по ней принимается не по личности вызывающего.
> Пообъектное сужение без личности бессмысленно, поэтому смена полосы была не косметикой;
> разбор — §1.5 п. 3.
>
> **Что это значит для работы S3.3 — сужается, а не растёт.** Перевыражается **предикат**
> («делим ли мы хотя бы один аккаунт») внутри действующей полосы; сама полоса, запись
> каталога и признак `scope_filtered` **не трогаются**. Утверждения сценария от этого не
> меняются ни одним словом: он и прежде пиннил **исход** сужения, а не устройство гейта.

> [!note] Требование о порядке «формат раньше замыкания» ПРИНЯТО СЮДА из IAM-ID-1-66 (круг 7, Б6)
> У `66` санкция отозвана вместе с плоской поверхностью чтения, но его часть про `ListUsers`
> к той поверхности не относилась: `ListUsers` перевыражает **этот** документ, шагом S3.3.
> Прежде остаток был объявлен «предметом этого документа» **внутри отозванного сценария** —
> то есть требование действовало, а реализовывать его было нельзя. Теперь оно стоит в
> действующем сценарии своим утверждением и имеет производителя.
>
> **Юнит на саму функцию проверки регрессией на это НЕ является:** он зовёт её без
> вызывающего и без замыкания, поэтому о порядке не утверждает ничего и остаётся зелёным при
> любом. Нужна **парная** проба уровня use-case: один и тот же мусорный курсор у названного
> вызывающего и у того, кто попадает в замыкание, плюс положительный контроль (законная
> страница проходит), иначе отрицание зеленеет на всём сломанном.

#### IAM-ID-1-55 · Живые потребители снятого фильтра переведены В ТОЙ ЖЕ стадии
**Given** консоль спрашивает «кто состоит в этом аккаунте» вызовом списка пользователей с
аккаунтом (два места, §1.5)
**When** применена стадия S3
**Then** оба места спрашивают то же **списком членств НАЗВАННОГО аккаунта** — вторая из двух
законных форм чтения (§5.0 п. 0), а не снятым фильтром и **не** перечнем аккаунтов человека
**And** **сквозная проба браузером** (`ui-future/e2e/specs/`) открывает страницу доступа
аккаунта `A` под вызывающим с правами на `A` и **видит поимённо** тех же людей, что состоят
в `A`, — утверждение о том, **что видит человек на том адресе, куда зашёл**
**And** та же проба на странице выбора приглашаемого **видит** того же человека в предлагаемом
составе
**And** **отрицание в паре с положительным**: человек, состоящий только в `B`, на обеих
страницах **не виден**, при том что человек из `A` виден в том же прогоне — иначе «не виден»
зеленело бы на пустой странице
**And** переименование класса или замена компонента пробу **не роняют**: она утверждает
наблюдаемое, а не разметку (`ui.md` правило 12)
**And** предикат `git grep -n "listUsers({" -- ui-future/ | grep account_id` даёт **ноль**
(сегодня — **2**, §1.5а класс 1) — это **сопутствующее** утверждение о дереве, а не то, чем
сценарий проверяется
**And** ни одно место не осталось на снятом поле к моменту S4 — иначе оно молча получит пустой список
**And** ни одно место **не заводит** запрещённой формы: вопрос обеих страниц — «кто состоит в
**этом** аккаунте», и он выражается аккаунт-скоупным чтением целиком

> [!important] Круг 7: `Then` переписан — он держался ОТСУТСТВИЕМ ВЫЗОВА, а не наблюдаемым
> Прежняя редакция снималась предикатом `listUsers({ account_id }) → 0` плюс прозой «состав
> тот же, что до перехода». Первое есть утверждение о **тексте дерева**: оно зеленеет на
> консоли, которая не спрашивает **ничего** и показывает пустую страницу. Второе не названо
> способом снятия вовсе — «тот же состав» нечем сравнить, если ни один прогон не назван.
>
> **Почему это важно именно здесь.** После решения вердикта по вопросу 4 (сценарий 49
> перенесён вслед за фиксом) IAM-ID-1-55 — **единственная** консольная правка, которую
> заказывает этот документ. Требование `ui.md` правила 12 («находка по консоли закрывается
> сквозной пробой браузером, и проба утверждает наблюдаемое») исполнять больше негде.
>
> **Модульная проба сюда не годится и это не придирка.** Она монтирует компонент и по
> построению не видит ни маршрутизации, ни федеративной загрузки, ни края, ни материализации
> прав — то есть не видит ровно того, что здесь меняется. Нужна проба браузером
> (`e2e-flow.md` §9, долг D4: «страница ответила 200» утверждением не является — признак
> загрузки модуля есть **элемент**, а не код ответа документа).
>
> **Проба несёт `// verifies #<N>` внутри вызова `test(…)`** — гейт продукта
> `TestConsoleProbeIssueLinksNameATaskAndLiveInsideTheProbe` требует, чтобы ссылка называла
> годный номер и стояла **внутри** пробы, а не над файлом: на верхнем уровне она переживёт
> пробу, к которой относилась.
>
> **Повтора пробы целиком не бывает** (`retries: 0` объявлено в конфигурации), и ждём мы
> **условие**, а не время: повтор скрыл бы гонку материализации прав, ради которой проба и
> написана.

> [!note] Совпадение формы здесь — результат замера, а не удобство (круг 6)
> Оба консольных места спрашивают про **аккаунт**, а не про человека, — это видно из самого
> вызова (`listUsers({ account_id })`). Значит запрещённая форма им не нужна **и не была бы
> использована**, даже если бы существовала. Названо, чтобы переезд не выглядел уступкой
> запрету: запрет ничего у этих двух мест не отнял.

#### IAM-ID-1-56 · Ресурс приглашения в провайдере инфраструктуры переехал вместе с глаголом
**Given** провайдер несёт ресурс приглашения, у которого аккаунт — обязательный атрибут (§1.5а)
**When** применена стадия S3
**Then** ресурс зовёт **переехавший** глагол членства, аккаунт остаётся обязательным атрибутом
**And** модуль, который его использует, и проба этого модуля применяются **без правки
пользовательского кода** — это ветвь по умолчанию, потому что атрибуты ресурса не меняются:
меняется глагол, который провайдер зовёт, а он вызывающему не виден
**And** если сохранить совместимость атрибутов окажется невозможно — слом объявляется в
перечне ломающих изменений провайдера с путём перехода, и это **решение**, фиксируемое
до начала S3, а не выбор в момент прогона
**And** проба модуля прогнана тем же инструментом и той же версией, что в конвейере
(`testing.md` §«Локальный прогон обязан воспроизводить конвейер»), а не «что стояло в системе»
**And** тексты отказов провайдера почту по-прежнему не называют — свойство существующее, и переход обязан его сохранить

#### IAM-ID-1-22 · Приглашение переехало и по-прежнему требует аккаунт
**Given** дерево после S4
**When** клиент вызывает глагол приглашения без аккаунта
**Then** `INVALID_ARGUMENT`, текст называет поле, требование сохранено дословно
**And** `InviteUserRequest` в контракте **отсутствует** целиком
**And** глагол пользователя приглашения больше не несёт

#### IAM-ID-1-23 · Приглашение в чужой аккаунт по-прежнему отвергается
**Given** вызывающий не имеет `editor` на аккаунте `C`
**When** он приглашает кого-либо в `C`
**Then** отказ — `PERMISSION_DENIED` либо форма скрытия существования, но **никогда 2xx**
**And** положительный контроль: с `editor` на `C` тот же вызов проходит
**And** запись каталога прав у переехавшего глагола сохраняет `object_type: "account"`, отношение `editor` и повышенный уровень аутентификации

#### IAM-ID-1-24 · Повышенный уровень аутентификации сохранён
**Given** токен вызывающего не удовлетворяет повышенному уровню
**When** он вызывает переехавший глагол приглашения
**Then** отказ
**And** положительный контроль: токен с повышенным уровнем проходит

> Приглашение — поверхность **выдачи права**; понижение требования сделало бы её дешёвой
> дверью к тому же, что выдаёт создание привязки.

#### IAM-ID-1-25 · Ось арендатора операции не тронута
**Given** дерево после S4
**When** исполняется любая мутация пользователя
**Then** внутренние сообщения по-прежнему несут `account_id` (предикат класса 8 §1.5а: перепись по имени поля даёт **7** файлов и **11** сообщений, и после S4 число не меняется)
**And** значение читается по точному имени поля, как и раньше

#### IAM-ID-1-26 · Набор полей у края и у сервиса совпадает
**Given** стенд после S4
**When** один и тот же ресурс читается **через край** и **напрямую у сервиса**
**Then** множества имён полей в двух ответах **совпадают**
**And** ни одно поле, которое отдаёт сервис, край не теряет молча
**And** положительный контроль: заведомо расходящаяся пара (край прежней сборки) даёт **красное** — иначе проба зеленела бы при любом состоянии

> Класс `api-conventions.md` §«Поле контракта не выходит наружу, пока КРАЙ не пересобран»:
> край перекодирует ответ своими стабами и **отбрасывает неизвестное молча**. Утверждение
> про «оба образа катятся одним изменением» — это порядок работ, ему место в DoD п. 12; здесь
> утверждается наблюдаемое следствие.

#### IAM-ID-1-27 · Ответ приглашения не различает «человек есть» и «человека нет»
**Given** почта `p@example.test` в платформе есть; почты `z@example.test` нет
**When** клиент приглашает каждую из них в свой аккаунт
**Then** коды ответов совпадают
**And** тела ответов **неотличимы** по всему, что не является идентификатором созданного членства
**And** время ответа не даёт устойчивого различия

### Группа D — права переносятся, не расширяясь (`kacho#472`)

> **Что именно сливается — измерено, потому что иначе эта группа требует несуществующего
> состояния.** Задача `#472` формулирует предмет как «сегодня один человек в двух аккаунтах
> имеет два идентификатора и два набора прав». Проверка по дереву показывает, что состояние
> у́же и конкретнее, и знать это обязательно — иначе Given группы D неконструируем.
>
> **Двух `ACTIVE` строк у одного человека быть не может**: глобальный ключ
> `users_active_external_id_uniq` этого не допускает (предикат — IAM-ID-1-07). Значит «два
> набора прав» в форме «две живые личности» не существует ни на одной инсталляции.
>
> **Существует другое, и оно конструируемо.** Приглашение резолвит субъект в старейшую
> `ACTIVE` строку по почте; для **ни разу не входившего** приглашённого таковой нет, и
> право выдаётся на его пер-аккаунтную строку — это записано в самом коде как намерение:
> «once that PENDING row is activated by first-login it becomes the identity's row»
> (предикат: `git grep -c 'becomes the identity' "$R" -- services/iam/internal/apps/kacho/api/user/invite.go`
> → **1**). Пригласив такого человека в **два** аккаунта, получаем две строки в
> состоянии «приглашён» с **разными** субъектами и по праву на каждой. Первый вход
> активирует **одну**; вторая остаётся неактивируемой, а выданное на неё право —
> **осиротевшим**: оно есть в леджере и не действует ни для кого.
>
> **Отсюда предмет переноса, и он ровно из трёх частей:**
> 1. строки в состоянии «приглашён» → членства выжившей строки личности;
> 2. **осиротевшие права** на неактивированных строках → на выжившую строку личности,
>    **с сохранением области**: право остаётся в том аккаунте, где выдано (IAM-ID-1-28);
> 3. привязки, выданные не на каноническую строку из-за запасного пути при ошибке чтения
>    (§1.4) — их число даёт П3 (IAM-ID-1-36).
>
> **Следствие для ожиданий:** у человека, чьи права уже сидят на канонической строке, —
> а это подавляющее большинство — множество доступа при переходе **не меняется вовсе**, и
> IAM-ID-1-29 обязан это подтвердить, а не «примерно совпасть».

#### IAM-ID-1-28 · Право остаётся в границах аккаунта, где выдано
**Given** человек имеет право `X` на объект в `A` и право `Y` на объект в `B`
**When** переход применён
**Then** `Check` на объект в `A` отвечает разрешением для `X` и **отказом** для `Y`
**And** `Check` на объект в `B` — зеркально
**And** ни одно право не стало действовать в обоих

#### IAM-ID-1-29 · Множества доступа поаккаунтно совпадают до и после
**Given** до перехода снят перечень объектов, доступных человеку, **отдельно по каждому аккаунту**
**When** переход применён
**Then** перечни совпадают **поаккаунтно**, элемент в элемент
**And** сравнение ведётся по идентификаторам объектов, а не по их числу

#### IAM-ID-1-30 · Ни одного лишнего объекта
**Given** тот же перечень
**When** переход применён
**Then** множество «после» **не содержит** ни одного объекта, которого не было в «до»
**And** положительный контроль: удаление одного права из «до» делает проверку красной — иначе она зеленела бы на пустом множестве

#### IAM-ID-1-31 · Административный уровень одного аккаунта не растекается на человека
**Given** человек состоит в `A` и `B`; в `A` кто-то имеет административный уровень над ним
**When** этот кто-то пытается действовать над человеком **в контексте `B`**
**Then** отказ
**And** положительный контроль: в контексте `A` то же действие проходит
**And** свойство держится **моделью**, а не проверкой в коде (`security.md` §«Авторизация живёт в МОДЕЛИ»)

> Правка модели делается на **S3, шагом S3.1** (§2.3а объявляет перенос своим заголовком;
> §4, §9.1 говорят то же) — **до** того как членств станет больше у преобладающего
> большинства. Отложить её означает выкатить состояние, в котором уровень одного арендатора
> читается на общем объекте личности.
>
> **Круг 11: здесь стояло «делается на S2».** Метка пережила решение о переносе (круг 3) и
> держалась против двенадцати мест, где стадия названа S3, — то есть два места об одном
> предмете, из которых верно одно. Рассуждение вокруг метки остаётся верным целиком; устарела
> ровно метка. Заодно снята посылка «пока членство у каждого одно, множества нет»: множество
> уже есть (§16.2), и потому «до/после пусто» — положительный контроль шага, а не признак его
> завершённости.
>
> **У этого отношения ПЯТЬ живых писателей, и все ВНУТРИ сервиса** (перемерено кругом 9,
> §1.5а класс 3): путь приглашения · два места пути активации · удаление строки ·
> посаженный глагол исключения из аккаунта, который тот же кортеж **снимает**. Все правятся
> **в той же стадии**, и перечень их исчерпывающ **by construction**: источник значения
> типизирован, поэтому снятие колонки роняет сборку в каждом из пяти.
>
> **Проба — на исход, а не на вызов**: после стадии кортеж области, записанный любым из пяти,
> ключуется на членство, а не на колонку строки; предикат-зеркало
> `git grep -nE 'Relation: *"account"' … | grep 'Object: …"iam_user:'` обязан дойти до нуля,
> а его пара по `"membership"` — стать ненулевой.
>
> > [!warning] Круг 9: здесь стояло «живой писатель ВНЕ сервиса — скрипт восстановления
> > с путём и номерами строк» — такого файла в дереве НЕТ, поэтому путь здесь не цитируется
> > Скрипт удалён целиком коммитом `c78901a33`, работой этого же перехода; координата с
> > номерами строк указывала в пустоту, а требование «после S2 скрипт на исправном стенде —
> > no-op» было **неисполнимо**: исполнять нечего.
> >
> > Существенное при этом уцелело и стало **сильнее**: писатели никуда не делись, их пять, и
> > они видны компилятору. Прежняя редакция описывала худший случай — нетипизированного
> > писателя, ломающегося молча; сегодня класс перешёл в лучший, и утверждение сценария
> > обеспечено сборкой, а не памятью того, кто запустит скрипт.

#### IAM-ID-1-32 · Перенос не гейтится на видимость
**Given** переход применён, очередь материализации намеренно придержана
**When** клиент читает состояние перехода
**Then** операции перехода `done` **не ждут** видимости кортежей (ban #9)
**And** доступ материализуется в ограниченном окне, а первый доступ к своему свежему
предмету переживается **ограниченным повтором на клиенте**, не серверным барьером
**And** ни один путь не отвечает «уже существует» на повторе, породив фантом

#### IAM-ID-1-33 · Снятие членства отзывает права ЭТОГО аккаунта — по исходу
**Given** человек состоит в `A`
**And** у него есть право `X` на объект в `A`
**When** клиент снимает его членство в `A` и дожидается операции
**Then** операция `done && !error`
**And** `Check` на объект в `A` отвечает **отказом** в пределах окна §5.0
**And** утверждается именно `Check`, а не «отзыв был вызван» и не «кортеж эмитирован»

> **Что именно держит свойство — три роли, и их нельзя путать.** Разделение выведено из
> вопроса ревьюера, потому что первая редакция объявляла `RESTRICT` и «гранты уходят вместе»
> одновременно, а это взаимоисключающие вещи: `RESTRICT` снятие **запрещает**, «уходят
> вместе» — семантика `CASCADE`.
>
> | Роль | Чем | Что гарантирует |
> |---|---|---|
> | **невозможность осиротить** | `RESTRICT` со стороны субъектов привязок на членство | членство **нельзя** снять, пока на него ссылается живая привязка — молчаливого сиротства не бывает (ban #10) |
> | **снятие привязок** | шаг use-case **в той же транзакции**, до удаления членства | отзыв происходит атомарно со снятием членства, а не «потом» |
> | **исчезновение доступа** | материализация из леджера привязок | наблюдаемый `Check` → отказ, в ограниченном окне (ban #9) |
>
> То есть шаг use-case здесь **законен и обязателен** — запрещено другое: полагаться на него
> как на **гарантию**. Гарантию даёт `RESTRICT`: если дренаж не выполнен, транзакция не
> проходит вовсе. Проба на это — IAM-ID-1-60.

#### IAM-ID-1-34 · Снятие членства не трогает другие аккаунты
**Given** тот же человек имеет право `Y` в `B`
**When** его членство в `A` снято
**Then** `Check` на объект в `B` по-прежнему отвечает **разрешением**
**And** его строка и идентификатор не изменились

#### IAM-ID-1-35 · Отзыв работает УЖЕ на дереве S2 — до того, как членства станут авторитетом
**Given** дерево **на конце S2** (членства пишутся, авторитет ещё у колонки)
**When** прогоняются IAM-ID-1-33, IAM-ID-1-34 и IAM-ID-1-60
**Then** все три **зелены на этом дереве**, а не только после S3
**And** между снятием членства и исчезновением доступа не существует окна, в котором доступ
действует, а членства уже нет

> Порядок стадий сам по себе — не наблюдаемое, и утверждать его сценарием нельзя. Наблюдаемо
> другое: **на дереве S2 отзыв уже исполняется**. Если это верно, порядок соблюдён by
> construction; если нет — S3 выкатывать нечем. Удерживает порядок DoD п. 10, а доказывает
> его — этот прогон.

#### IAM-ID-1-57 · Каскад супер-доступа разрешается относительно НАЗВАННОГО аккаунта
**Given** человек состоит в `A` и `B`; администратор аккаунта `A` — не администратор `B`
**When** администратор `A` действует над объектом личности **в контексте `A`**
**Then** доступ разрешён
**And** тот же администратор в контексте `B` получает **отказ**
**And** администратор облака получает разрешение в **обоих** контекстах — уровень аварийного пути не ослаб (§2.3а, условие 3)
**And** свойство держится **моделью**: факт принадлежности не выводится из снятой колонки — ни Go-функцией, ни SQL-выражением плана материализации (§1.5а класс 6)

> **Проверяется ИСХОДОМ, а не сборкой — и это изменилось с круга 3.** Пока факт строила
> Go-функция, снятие колонки роняло компиляцию, и «перевели» было видно бесплатно. Сегодня
> производители — **строки SQL** (запись плана чтения материализации и ветвь звена цепи
> областей `kacho_iam.resource_scope_edge`), о которых сборка не знает: непереведённый каскад
> соберётся, поднимется и **молча перестанет находить предка**, то есть откажет в доступе там,
> где обязан его дать. Поэтому сценарий утверждает решения `Check` в обоих контекстах, а не
> факт правки. Предикат живости производителей — §1.5а, класс 6, блоки 6b и 6c.
>
> **Круг 7: источник для `iam.user` в обоих местах УЖЕ переведён на членство** (§1.5а класс 6).
> Что этот сценарий проверяет сегодня — **смену объекта** гейта, то есть шаг S3.1: пока
> объектом остаётся личность, второй `Then` («в контексте `B` — отказ») **невыполним**, и
> зелёным он станет только вместе с типом членства в модели прав.

#### IAM-ID-1-58 · Домен допускает строку человека без аккаунта
**Given** дерево после S4
**When** конструируется доменная сущность человека без принадлежности аккаунту
**Then** самовалидация её **принимает** — требование непустоты снято (`domain/user.go:104`, §1.5а класс 7)
**And** положительный контроль: прочие обязательные поля по-прежнему валидируются, и пустая почта отвергается

> Без этого целевая форма **невыразима в домене**: строка, которую переход обязан создать,
> не проходит собственную валидацию.

#### IAM-ID-1-59 · Утверждения токена про аккаунт: исход назван, кейс не замаскирован
**Given** токен человека, состоящего в N аккаунтах
**When** токен выпущен и прочитан
**Then** утверждения `kacho_active_account` и `kacho_account_id` ведут себя по **объявленному**
исходу — и он один из трёх, не «как получится»: сняты · вычисляются из явно выбранного
аккаунта запроса · требуют выбора от вызывающего
**And** исход записан в контракте и в документации, а не выведен из кода
**And** кейс `services/iam/tests/newman/cases/iam-token-facade-conformance.py` приведён к нему
**тем же изменением** — он сегодня утверждает непустоту `kacho_account_id` (предикат:
`git grep -c 'kacho_account_id' "$R" -- services/iam/tests/newman/cases/iam-token-facade-conformance.py`
→ **2**: захват значения и утверждение о нём), и после S4 у неё нет производителя
**And** кейс **не** замаскирован, не пропущен и не ослаблен (`testing.md` §E2E) — он либо
утверждает новый исход, либо снимается вместе со своим предметом

> «Активный аккаунт» человека в N аккаунтах **не определён by construction**. Это и есть
> причина, по которой исход обязан быть выбран явно, а не унаследован.

#### IAM-ID-1-60 · Осиротить право снятием членства НЕЛЬЗЯ
**Given** человек с живой привязкой, субъект которой ссылается на его членство в `A`
**When** предпринимается попытка снять членство **без** снятия привязки в той же транзакции
**Then** транзакция **не проходит** — `RESTRICT` отвергает
**And** наружу это контракт-тон `FAILED_PRECONDITION`, называющий предмет, а не общий запасной текст
**And** положительный контроль: штатный путь (дренаж привязок и снятие членства одной транзакцией) **проходит** (IAM-ID-1-33)

> Это проба на **гарантию**, а не на шаг: она утверждает, что молчаливое сиротство
> невозможно by construction, даже если код дренажа однажды напишут неверно.

#### IAM-ID-1-61 · Кортежи личности снимаются вместе со строкой
**Given** человек, чьи кортежи личности в модели прав записаны отдельным путём
**When** его строка снимается
**Then** кортежи сняты
**And** проба утверждает **их отсутствие** в модели, а не факт вызова снятия
**And** повторное снятие идемпотентно

#### IAM-ID-1-62 · Проглоченный отказ становится наблюдаемым, почта в лог не пишется
**Given** путь активации, на котором отказ сегодня проглатывается веткой пропуска
**When** отказ происходит
**Then** он **виден**: счётчик либо метрика, не только строка лога (`security.md` §Hardening #8)
**And** «ноль таких отказов за всю жизнь» отличимо от «никто не пробовал» — перепись объявляется
**And** ни на успешном, ни на отказном пути **почта в лог не попадает** (`security.md` §Hardening #2); проба утверждает отсутствие подстроки в буфере лога
**And** признак завершения дренажа очереди, на который опирается §5.0/B, **существует** и читается снаружи

#### IAM-ID-1-36 · Неканонические привязки перенесены поимённо
**Given** П3 дал непустое множество
**When** переход применён
**Then** каждая такая привязка перенесена на каноническую строку **поимённо**, с перечнем в отчёте
**And** множество доступа её субъекта не изменилось (IAM-ID-1-29 распространяется и на них)
**And** при П3 = 0 сценарий проходит, объявляя перепись «записей 0» — он **не падает** на достижении своей цели

### Группа E — что происходит с существующими людьми (классы)

Числа берутся из П4 в момент прогона.

> **Ось разбиения — ОДНА, и это состояние строки.** Прежняя редакция называла классами
> «единственный аккаунт», «владелец», «приглашённый», «посевная» — и требовала, чтобы ни
> одна строка не попала в два. Это неудовлетворимо by construction: владелец единственного
> аккаунта попадает и в первый, и во второй, посевная строка — ещё и в четвёртый.
>
> Разбиение (взаимно исключающее, исчерпывающее) — по **состоянию строки**:
>
> | Класс | Предикат | Сценарий |
> |---|---|---|
> | **К1** активен | `invite_status='ACTIVE'` | IAM-ID-1-37 |
> | **К2** приглашён | `invite_status='PENDING'` | IAM-ID-1-39 |
> | **К3** заблокирован | `invite_status='BLOCKED'` | IAM-ID-1-72 |
>
> Сумма К1+К2+К3 обязана сойтись с общим числом строк — это и проверяет IAM-ID-1-41.
>
> **Владение и посевное происхождение — СРЕЗЫ, а не классы:** они пересекают К1 и
> проверяются отдельно (IAM-ID-1-38, IAM-ID-1-40), без требования взаимной исключительности.
> Требовать разбиения от среза значило бы требовать невозможного и получить вечно красную
> пробу — то есть ту, которую снимут.

#### IAM-ID-1-37 · Класс «единственный аккаунт, он же личный, он же владеет»
**Given** человек владеет ровно одним аккаунтом, состоит только в нём (сегодняшний вид всех 74)
**When** переход применён
**Then** у него **одно** членство, ведущее в тот же аккаунт
**And** идентификатор, почта, внешний идентификатор, отметка создания не изменились
**And** множество доступа совпадает (IAM-ID-1-29)
**And** владение сохранено

#### IAM-ID-1-38 · Класс «владелец» — структурное владение не ослабло
**Given** человек — владелец аккаунта
**When** переход применён
**Then** владение неизменяемо, глагола передачи по-прежнему нет
**And** `accounts_owner_fk` остаётся `RESTRICT` и отвергает снятие владеющего человека **немедленно**
**And** каскад трёх верхних уровней супер-доступа работает как прежде

#### IAM-ID-1-39 · Класс «приглашённый» — состояние переезжает, не теряется
**Given** строка в состоянии «приглашён» (число — П1)
**When** переход применён
**Then** ей соответствует **одно** членство в состоянии «приглашён»
**And** её строка человека сохраняет состояние «приглашён» и пустой внешний идентификатор
**And** первый вход активирует и строку, и членство (IAM-ID-1-04)

#### IAM-ID-1-40 · Класс «посевная служебная строка-якорь»
**Given** посевные строки, заведённые применёнными миграциями вместе со своими аккаунтами
**When** переход применён
**Then** они получают членство наравне с прочими
**And** ни одна применённая миграция не тронута (ban #5) — состояние правится **новой**
**And** страж, охраняющий эти строки, продолжает исполняться и не стал вакуумным

#### IAM-ID-1-41 · Разбиение исчерпывающе, срезы — нет, и это разные утверждения
**Given** П4 прогнан
**When** строки разложены по К1/К2/К3
**Then** сумма К1+К2+К3 равна общему числу строк
**And** ни одна строка не попала в два **класса** (взаимная исключительность — свойство разбиения)
**And** срезы «владеет» и «посевная» **пересекают** классы, и от них взаимной исключительности не требуется
**And** перепись печатает **объём осмотренного**, чтобы «ноль находок» было отличимо от «ноль прочитанного»

#### IAM-ID-1-72 · Класс «заблокирован» переезжает, не оживая
**Given** строка в состоянии «заблокирован»
**When** переход применён
**Then** ей соответствует членство, а сама строка остаётся заблокированной
**And** переход **не** снимает блокировку и не открывает доступ: `Check` по её объектам отвечает отказом до и после
**And** попытка входа заблокированного человека отвергается тем же кодом и текстом, что до перехода

#### IAM-ID-1-42 · Ни один существующий человек не потерял доступа
**Given** снятые до перехода перечни доступа по каждому человеку и каждому аккаунту
**When** переход применён
**Then** для **каждого** человека перечни совпадают поаккаунтно
**And** расхождение в любую сторону — красное: потеря есть дефект, приобретение есть расширение

### Группа F — консоль (`kacho#473`) — САНКЦИЯ ОТОЗВАНА 2026-08-24, сценарии сохранены

> [!warning] ОТОЗВАНЫ IAM-ID-1-43 … IAM-ID-1-48 — консольная поверхность перечня членств
> Решение `PRO-Robotech/kacho#1085` (шапка · IAM-ID-2 §2.0): перечень членств, отдаваемый по
> аккаунт-скоупному пути, содержит членство **только в том аккаунте, о котором спрашивают**.
> Группа F написана под противоположную величину — «все аккаунты человека» на странице
> пользователей и полным перечнем на его карточке, — и потому не реализуется ни в одной из
> этих шести строк.
>
> **IAM-ID-1-49 не отозван — он ПЕРЕНЕСЁН, и это разные состояния** (круг 7). Его предмет —
> **порядок работ** (проба браузером написана до фикса и падает на дефекте, `ui.md`
> правило 12), а не величина, которую продукт отдаёт: отзывать здесь нечего, предмет верен.
> Но фикс, который проба стережёт, живёт в IAM-ID-2-22, а правка, которую заказывает **этот**
> документ, ровно одна — IAM-ID-1-55, и требование правила 12 внесено в неё. Поэтому вердикт
> круга 6 (вопрос 4) перенёс требование **вслед за фиксом**, а не оставил его без предмета.
> Отзыв «всей группы» одним движением унёс бы верное требование вместе с неверными — и это
> по-прежнему причина, по которой 49 не помечен отзывом.
>
> **Сценарии не удалены, и это осознанно.** Круг 6 обязан видеть, что именно отменено и по
> какому доводу: удалённый сценарий неотличим от забытого, а «форма показа перечня» вернётся
> как вопрос при первом же разговоре о карточке человека. Замену — что консоль показывает
> вместо перечня — несёт IAM-ID-2 §5 группа F5 (IAM-ID-2-19, -20, -21, -22): карточка
> называет членство **в текущем аккаунте**, не называет ни одного другого и не намекает на
> их число.
>
> **Причин отзыва ДВЕ, и они разного рода. Обе названы, потому что одной было бы
> недостаточно.**
>
> **Причина 1 — указание владельца (2026-08-23) запрещает предмет группы.** Все шесть
> описывают **столбец на странице пользователей** и полный перечень на карточке, то есть «в
> каких аккаунтах состоит **названный** человек» — ровно ту форму, про которую сказано
> «отображать всем в каких аккаунтах он приглашён неправильно». Сценарий 46 («первый плюс
> счётчик») запрещён и **буквально**: он показывает **число** аккаунтов человека всякому, кто
> открыл список, — состав вычисляется без единого имени.
>
> **Причина 2 — основание группы было ОПРОВЕРГНУТО собственным свидетельством ещё на круге 4,
> и это не «устарело», а «было неверно».** Группа стояла на утверждении, что дерево, снимая
> столбец, обещало вернуть его **перечнем**. Дерево обещает **читаемую принадлежность на
> КАРТОЧКЕ** и адресует её задаче `PRO-Robotech/kacho#1085`; ни столбца-перечня, ни формы
> «первый + счётчик» не обещает **ни одно** из трёх мест, а сквозная проба консоли утверждает
> **отсутствие** столбца двумя половинами — в шапке таблицы и в выборе столбцов — и называет
> `#1085` сроком годности своего утверждения. Перечень мест с цитатами и предикатом — §1.7.
>
> **Различие между двумя причинами несущее.** По первой группа «устарела бы» — это движение
> мира, вины документа нет. По второй она **была ошибочна на момент написания**: её основание
> опровергается тем самым свидетельством, на которое она ссылалась. Списать всё на указание
> владельца было бы мягче и **неверно** (`doc-truthfulness`: довод улучшился или просто
> уцелел).
>
> > [!note] Столбец: два документа предписывали противоположное, и право — за IAM-ID-2
> > IAM-ID-1-48 предписывал завести столбец **заново** как перечень членств; IAM-ID-2-22
> > объявляет, что столбца в списке **нет и не появится** («список охватывает несколько
> > аккаунтов, и столбец солгал бы»). Два места об одном предмете, из которых верно одно.
> > Право за IAM-ID-2: её предмет — консольная поверхность членства, а этого документа —
> > отрыв данных. Дерево сегодня на её стороне: `spec "$R" | grep -c users` → **0**, столбца
> > нет; контроль в обратную сторону — `spec 554a8d49 | grep -c users` → **1** (§1.7).
> >
> > **Разрешено не «поздним прочтением», а предметом.** Выбирать по дате было бы правилом,
> > которое в следующий раз выберет неверно: побеждает документ, чей предмет — спорная
> > поверхность, а не тот, кто высказался последним.
>
> **Что из группы НЕ потеряно, а переехало вместе с предметом** — названо поимённо, иначе
> отзыв унесёт верные требования вместе с неверными:
>
> | Требование | Куда |
> |---|---|
> | проекция, не заполняющая поле, обязана это сказать (было 43; `api-conventions.md` §«Пустое значение обязано означать „пусто“», подкласс B) | **остаётся здесь** как свойство контракта членства и производится сценарием **IAM-ID-1-73**; для двух новых чтений то же требование перевыражено в IAM-ID-2 §3.1 и IAM-ID-2-01 |
> | ноль как **факт**, а не прочерк (было 44; `ui.md` правило 6) | IAM-ID-2-19 |
> | ссылка на чужой ресурс вместо идентификатора текстом (было 45; `ui.md` правило 2) | IAM-ID-2-19 |
> | порядок повторяющегося поля объявлен незначимым, сверка по составу (было 47; `api-conventions.md` §«Пустое значение обязано означать „пусто“», подкласс A) | **остаётся здесь** — это свойство **контракта** ресурса членства, а не консоли; производится **IAM-ID-1-73**; для новых чтений — IAM-ID-2 §3.1 |
> | радиус правки консольных спек (было 48) | IAM-ID-2-22 — вместе с решением о судьбе столбца |
> | проба браузером написана до фикса и падает на дефекте (49) | **ПЕРЕНЕСЕНО в IAM-ID-2-22** вслед за фиксом (круг 7, вердикт круга 6, вопрос 4). Наблюдаемое, которое этот документ всё ещё обязан утверждать, утверждает **IAM-ID-1-55** — единственная консольная правка, им заказываемая |
>
> **Что отзыв НЕ означает.** Он не означает, что «аккаунты человека» нельзя показать
> **никому**: свой список остаётся, и приглашённый видит в нём, куда он приглашён (§5.0 п. 0).
> Отозвана **поверхность про третье лицо**, а не сама читаемость.
>
> **Производитель этого утверждения — IAM-ID-2-08 и IAM-ID-2-21, а не IAM-ID-1-67** (круг 7,
> Б6). Здесь стояла ссылка на `67` как на живого производителя — при том что у самого `67`
> санкция отозвана. Ссылка на отозванный сценарий как на держателя требования есть та же
> ошибка, что и «остаётся предметом этого документа» в его теле: она объявляет действующим
> то, что реализовывать нельзя.

#### IAM-ID-1-43 · Списочное чтение ОБЯЗАНО заполнять членства
> **[САНКЦИЯ ОТОЗВАНА 2026-08-24]** санкция снята решением `PRO-Robotech/kacho#1085` — предмет сценария есть **перечень всех** членств человека, одинаковый у обоих чтений; именно он и отменён. Требование «одна и та же величина у списка и у одиночного чтения» (`api-conventions.md` §«Пустое значение обязано означать „пусто“», подкласс B) остаётся верным и перевыражено в IAM-ID-2 §3.1 и IAM-ID-2-01 — уже про членство в **названном** аккаунте.

**Given** дерево после S4, человек с N членствами (N ≥ 2)
**When** клиент читает его **обоими** путями — `GET /iam/v1/users/{id}` и `GET /iam/v1/users`
**Then** оба ответа несут **одно и то же** множество членств, элемент в элемент
**And** списочный ответ **не пуст** там, где одиночный не пуст

> Утверждение фальсифицируемо и односторонне: «заполняет» либо «не заполняет», третьего
> исхода проба не принимает. **Выбор ветви консоли сюда не входит** — он стоит вопросом
> В-3 §7 и принимается владельцем, а не пробой. Если реализация списочного чтения поле
> заполнять не станет, красным становится **этот** сценарий, и это правильный исход: он
> сообщает, что предусловие ветви (б) не выполнено, — а не молча разрешает обе.
>
> `api-conventions.md` §«Пустое значение обязано означать „пусто“», подраздел B: столбец,
> заполняемый только одиночным чтением, показал бы «нет аккаунтов» у человека с пятью
> членствами, и администратор прочёл бы это как «его отовсюду убрали».
>
> **Круг 3: ветвь (а) уже выбрана — но 44-47 не снимаются, и вот почему.** Прежняя редакция
> обещала снять их «вместе с ветвью (а)», считая ветви взаимоисключающими навсегда. Дерево
> показало, что они **последовательны**: столбец снят (§1.7) именно потому, что предусловие
> ветви «список» не выполнено — поля членств нет ни на одном чтении контракта, а поле без
> источника показывать запрещено (`ui.md` правило 9). Ресурс членства этот источник и
> заводит. Поэтому S5 — не «выбрать ветвь», а **вернуть столбец в форме перечня**, когда
> источник появился; 44-47 описывают именно эту форму и предмет сохраняют.
>
> **УСЛОВИЕ, НАЗВАННОЕ ЗДЕСЬ КРУГОМ 3, НАСТУПИЛО — и абзац оставлен именно поэтому.** Он
> гласил: «если владелец решит, что „аккаунты человека“ на странице пользователей не
> показываются **вовсе** — тогда 44-48 снимаются одним решением и §9 пересчитывается; это
> вопрос В-3 §7». Владелец решил (2026-08-23), и решение пришло **шире** предсказанного: снят
> не только столбец, но и сам перечень всех членств как величина — вместе с 43. Абзац не
> удалён, потому что он **сработал**: предикат снятия был назван заранее и оказался годным;
> удалив его, следующий читатель не увидел бы, что условие было объявлено, а не придумано
> задним числом. Проба, оставшаяся без предмета, — находка, а не «лишняя» (`testing.md`
> §«Гейт на класс» п. 5).

#### IAM-ID-1-44 · Ноль членств называется словами
> **[САНКЦИЯ ОТОЗВАНА 2026-08-24]** санкция снята решением `PRO-Robotech/kacho#1085` — столбец, о котором он говорит, не заводится. Норма «ноль есть факт, а не прочерк» (`ui.md` правило 6) остаётся и применяется к состоянию членства на карточке (IAM-ID-2-19).

**Given** человек без членств
**When** открыта страница пользователей
**Then** в столбце — «нет аккаунтов», а не прочерк
**And** прочерк в этой консоли означает «данных нет», а ноль членств есть **факт о человеке** (`ui.md` правило 6)

#### IAM-ID-1-45 · Одно членство показывается ссылкой на аккаунт
> **[САНКЦИЯ ОТОЗВАНА 2026-08-24]** санкция снята решением `PRO-Robotech/kacho#1085` — предмет — столбец «аккаунты человека». Форма ссылки (`ui.md` правило 2) остаётся и применяется к аккаунту на карточке (IAM-ID-2-19).

**Given** человек с одним членством (сегодняшний вид всех существующих)
**When** открыта страница
**Then** иконка типа, имя аккаунта, переход на его карточку — по форме ссылки на чужой ресурс (`ui.md` правило 2)
**And** значение — **ссылка**, а не идентификатор текстом: положительный контроль — адрес перехода ведёт на карточку именно этого аккаунта

> [!note] Круг 3 переписал Then: «ровно как до перехода» ссылалось на то, чего в дереве нет
> Столбец «Аккаунт» снят со спеки `users` до начала работ (§1.7), поэтому «как сегодня» и
> «как до перехода» **не имеют референта**: сравнивать не с чем, и проба в такой формулировке
> неисполнима. Утверждение перевыражено через **форму**, которая в дереве есть и проверяема
> (`ui.md` правило 2, ссылка на чужой ресурс), а не через исчезнувший образец.

#### IAM-ID-1-46 · Много членств — первый плюс счётчик
> **[САНКЦИЯ ОТОЗВАНА 2026-08-24]** санкция снята решением `PRO-Robotech/kacho#1085` — «первый плюс счётчик» есть **признак числа других аккаунтов**, то есть ровно то, что IAM-ID-2-20 запрещает показывать. Из всей группы этот сценарий противоречит решению сильнее прочих: он не просто показывает лишнее, он показывает **величину**, по которой чужой состав вычисляется без единого имени.

**Given** человек с N членствами, N > 1
**When** открыта страница
**Then** первый элемент — ссылка, рядом тег «+(N−1)» с перечнем в подсказке
**And** форма та же, что уже применена к субъектам привязки (`ui.md` правило 3) — новой молекулы не заводится
**And** полный перечень доступен на карточке человека таблицей значений (`ui.md` правило 11)

#### IAM-ID-1-47 · Порядок объявлен незначимым
> **[САНКЦИЯ ОТОЗВАНА 2026-08-24]** санкция снята решением `PRO-Robotech/kacho#1085` — предмет — повторяющееся поле членств на ресурсе человека, которого не будет. Само требование объявить порядок незначимым перенесено в IAM-ID-2 §3.1 для обоих новых чтений.

**Given** контракт несёт повторяющееся поле членств
**When** читатель сверяет ответ
**Then** порядок объявлен **незначимым** в комментарии контракта, и это сказано словами
**And** консоль сверяет элементы по составу, а не по индексу
**And** проба подаёт ответ **переставленным** и требует, чтобы вид не изменился

#### IAM-ID-1-48 · Радиус правки — одна спека
> **[САНКЦИЯ ОТОЗВАНА 2026-08-24]** санкция снята решением `PRO-Robotech/kacho#1085` — радиус считался от заведения столбца заново; столбец не заводится, поэтому и радиус не тот. Предикат `spec` после круга 6 обязан по-прежнему давать **3** спеки без `users`.

**Given** столбец «Аккаунт» стоит в **трёх** спеках, и спеки `users` среди них нет (§1.7)
**When** правка применена
**Then** изменена **только** спека `users` — в неё столбец **заводится заново**, как перечень членств
**And** `projects`, `service-accounts`, `groups` не тронуты — у них аккаунт остаётся одним значением
**And** предикат `spec` (§1.7) после правки даёт **4** спеки, и добавившаяся — ровно `users`

#### IAM-ID-1-49 · Проба браузером ПАДАЕТ на дереве до правки
> **[ПЕРЕНЕСЁН В IAM-ID-2 — 2026-08-24, вердикт круга 6, вопрос 4]** Из двух исходов, между
> которыми круг 6 обязан был выбрать, выбран **перенос вслед за фиксом**: требование порядка
> («проба браузером написана до фикса и падает на дефекте», `ui.md` правило 12) переезжает к
> **IAM-ID-2-22**, где живёт сам фикс. Здесь сценарий **производителя не заказывает**.
>
> **Почему перенос, а не перепривязка `Given`.** Второй исход требовал привязать пробу к
> правке, которую **этот** документ заказывает; после решения по вопросу 3 такая правка
> ровно одна — IAM-ID-1-55, и требование `ui.md` правила 12 внесено **в неё** своими
> утверждениями. Держать сверх этого отдельный сценарий «о порядке вообще» значило бы завести
> второе место об одном предмете.
>
> **Состояние ТРЕТЬЕ, и в счёте оно считается отдельно** (§9.3): не «снят» (номер остаётся в
> обращении, текст остаётся), не «санкция отозвана» (предмет не признан ошибочным — он
> **верен и исполняется в другом документе**). Отсюда третье слагаемое арифметики:
> 71 объявлено − 9 с отозванной санкцией − **1 перенесённый** = **61** действующий.
>
> **Текст ниже сохранён как свидетельство** — удалённый сценарий неотличим от забытого, а
> ссылка из IAM-ID-2 обязана резолвиться в текст, а не в пустоту (§9.3, «почему номера не
> переиспользуются»).

**Given** сборка консоли **до** правки столбца
**When** прогоняется сквозная проба браузером из `ui-future/e2e/specs/`
**Then** она **красная**, и текст падения называет предмет находки
**And** на сборке после правки та же проба — зелёная
**And** проба утверждает **наблюдаемое** — что видит человек на том адресе, куда зашёл, — а не разметку: переименование класса или замена компонента её не роняют

> Это инъекция в обе стороны (`testing.md` §«Гейт на класс» п. 2), а не декларация «написана
> до фикса»: последнее — порядок работ, и его держит DoD п. 2.

### Группа G — стадии, откат, невозврат

#### IAM-ID-1-50 · Откат S1 полон
**Given** применена S1
**When** применён обратный шаг
**Then** таблица членств снята **новой** миграцией, применённая не тронута (ban #5)
**And** дерево собирается, пробы зелены, поведение продукта то же, что до S1

#### IAM-ID-1-51 · Откат S3 полон — оба источника живы
**Given** применена S3
**When** читатели возвращены на колонку
**Then** данные не потеряны: колонка продолжала заполняться всю стадию
**And** продукт работоспособен на любом из двух источников

#### IAM-ID-1-52 · Миграция точки невозврата САМА отказывается идти на негодных данных
**Given** база, в которой П2 ≠ 0 (есть дубли почты)
**When** применяется миграция стадии S4
**Then** миграция **отказывает** и называет предмет: слияние дублей не выполнено
**And** ни одна колонка не снята, ни один ключ не изменён — состояние базы то же, что до попытки
**And** положительный контроль: на базе с П2 = 0 та же миграция **проходит**
**And** отказ читается оператором и называет, что сделать, — это рантайм-диагностика, а не публичный артефакт (`security.md` §«Три места, которые НЕ подпадают»)

> Точка невозврата, охраняемая только текстом в шапке, охраняется намерением читателя.
> Здесь она охраняется **самой миграцией**: предикат готовности исполняется, а не
> декларируется. Остальные условия готовности (зелень IAM-ID-1-29..31, 33) — DoD п. 10:
> их миграция проверить не может и не притворяется, что может.

#### IAM-ID-1-53 · Страж уникальности переписан в том же изменении
**Given** страж `services/iam/internal/migrations/0069_users_identity_uniqueness_guard.sql` требует три имени индекса и запрещает два

> **Предикат Given:** `git show "$R":services/iam/internal/migrations/0069_users_identity_uniqueness_guard.sql | grep -cE 'users_account_email_unique|users_account_external_id_unique|users_active_external_id_uniq'` → **7** (единица — вхождение, не имя: одно имя встречается в страже несколько раз). Проверять надо **исход стража**, а не число вхождений, — этим и занят сам сценарий.
**When** S4 снимает пару пер-аккаунтных ключей и вводит глобальные
**Then** страж переписан **в том же изменении** под новый контракт
**And** доказано инъекцией: снятие любого из новых ключей делает его красным, а законный набор — зелёным
**And** проба на то, что старый страж **не** остался бы зелёным на новом дереве: без правки он охранял бы контракт, которого больше нет

#### IAM-ID-1-54 · Замена сериализации гонки названа, а не потеряна
**Given** S4 снимает глобальный ключ по внешнему идентификатору
**When** применена S4
**Then** названо, **что именно** теперь сериализует конкурентный первый вход
**And** IAM-ID-1-07 прогнан **после** S4 и зелен
**And** проба конкурентная (несколько одновременных путей), а не последовательная

---

## §6 Что этот документ НЕ покрывает

Каждый пункт несёт **предикат**, которым читатель проверит его сам, — иначе это мнение с
датой, а не граница охвата.

| # | Не покрывается | Почему и чем это закрывать |
|---|---|---|
| 1 | **федеративная идентичность** | надстраивается над этим переходом; восстанавливает три осознанно снятых предмета; ослабляет структурного владельца — это пересмотр решения владельца, а не уборка. Предикат: `git ls-files services/iam/internal/migrations \| grep -i scim` — снято `0006` |
| 2 | **передача владения аккаунтом** | владение неизменяемо, глагола нет и здесь не заводится. Предикат: `git grep -n "owner_user_id" "$R" -- proto/kacho/cloud/iam/v1/ \| grep -i transfer` → ноль |
| 3 | **приостановка членства отдельным состоянием** | требует, чтобы путь авторизации читал состояние членства; это отдельная конструкция. Состояния здесь два: «приглашён» и «активно»; снятие — удаление строки **Предикат** (анкер — объявление ЗНАЧЕНИЯ перечисления, не подстрока): `git grep -nE "^[[:space:]]+[A-Z_]*SUSPEND[A-Z_]*[[:space:]]*=[[:space:]]*[0-9]+" "$R" -- proto/kacho/cloud/iam/v1/ | wc -l` → **0**; контроль в обратную сторону — тот же предикат на `PENDING` даёт **2**. Греп по подстроке здесь **лжёт**: он даёт 3, и все три — проза комментариев про приостановку. |
| 4 | **шаг принятия приглашения** | сегодня его нет: активация происходит на первом входе. Документ сохраняет сегодняшнюю семантику и не вводит новой **Предикат:** `git grep -cn "accept.*invit\|InviteAccept" "$R" -- proto/kacho/cloud/iam/v1/` → **0**; активация происходит на первом входе (`services/iam/internal/apps/kacho/api/user/internal_upsert.go`, `UpsertFromIdentity`), и семантика сохраняется. |
| 5 | **общий профиль на все аккаунты** | имя и почта после отрыва одни на всех; разделение сегодня бесплатно и станет платным. Названо, чтобы не всплыло как сюрприз **Предикат:** после S4 `users` несёт по одной строке на почту — то есть `SELECT count(*) FROM users GROUP BY lower(email) HAVING count(*)>1` тождественно пуст, и второго профиля хранить негде. |
| 6 | **корреляция арендаторами по общему идентификатору** | не решается: решение через пер-арендаторный псевдоним запрещено ban #15 (псевдоним сменил бы внешне-адресуемую идентичность). Явный Out-of-scope **Предикат:** `git grep -in "alias\|redirect" "$R" -- proto/kacho/cloud/iam/v1/user.proto \| wc -l` → **0**; контроль в обратную сторону — та же команда по **всему каталогу** `proto/kacho/cloud/iam/v1/` даёт **23**, то есть она исполнима и что-то находит. (Круг 4: прежний контроль «та же команда на `account` даёт 29» **не воспроизводился** — ни `account.proto`, ни `account_service.proto` не дают ничего: 0 и 0 на всех четырёх ревизиях. Утверждение про `user.proto` при этом истинно, но контроля не имело, а без контроля ноль неотличим от «предикат не находит ничего никогда», §1.0 п. 2.) Форма `grep -c` здесь была бы **неисполнимым предикатом**: на файле без совпадений она печатает пустоту, а не ноль (§1.0 п. 1). Таблицы псевдонимов нет и не заводится (ban #15, IAM-ID-1-69). |
| 7 | **слияние дублей почты** | процедуры в дереве **нет**; единственная существующая (`0002_users_unique_email_dedup.sql`) объявлена стражем `0069:24-51` недостижимой и небезопасной. **П2 ≠ 0 блокирует переход**, а не «решается по ходу». **Предикат** — сам П2 (§3) |
| 8 | **аккаунт с нулём активных членств** | после отрыва достижим по правам и не попадает ни в один список — нужна перепись таких аккаунтов, иначе тупик меняется на тихую утечку. Отдельный предмет **Предикат:** после S4 — `SELECT count(*) FROM accounts a WHERE NOT EXISTS (SELECT 1 FROM memberships m WHERE m.account_id=a.id AND m.state='ACTIVE')`; не ноль — предмет заведён отдельной задачей, не «решается по ходу». |
| 9 | **правдивость отказов, существовавших ДО этого изменения** | три живых `RESTRICT` на `users` сверх названных гвардом (§1.3), односторонний разбор у `accounts_owner_fk` в `fkText` (предикат §1.8), комментарий в `services/iam/internal/apps/kacho/api/user/delete.go`, обещающий текст, которого функция не производит. **Предикат — по ПРОИЗВОДИТЕЛЮ, а не по вхождению:** `git grep -n "owns accounts and cannot be deleted" "$R" -- services/ \| grep -cE 'fmt\.Sprintf\|Wrapf'` → **0** (единственное вхождение — сам комментарий). Контроль в обратную сторону: та же команда на тексте, у которого производитель **есть**, — `"contains projects and cannot be deleted"` → **2**. Считать голые вхождения нельзя: у второго текста их **8**, у первого **1**, и по этой паре чисел вывод получился бы обратный истинному. Отдельный предмет: смешивать их с этим переходом значит объяснять красноту одного правкой другого |
| 10 | **ось арендатора в аудите** | 4 эмиссии из 32 (в 27 файлах) пишут пустую строку; предикат: `git grep -n "TenantAccountID:" "$R" -- services/iam/internal \| grep -v _test \| grep -c '""'` → **4**. Существующий дефект, переходом не создаваемый |
| 11 | **записи каталога прав с областью объекта личности, требующие отсутствующего отношения** | существующий дефект; переход делает эту поверхность кросс-арендной, поэтому он назван здесь, но заводится **своим** предметом со своим замером **Предикат:** перечислить записи каталога с областью объекта личности и сверить требуемое отношение с объявленными в `proto/kacho/cloud/iam/v1/fga_model.fga` для `type iam_user`; расхождение — находка. Число здесь намеренно не называется: оно не мерено мной, и приводить чужое как своё нельзя. |
| 12 | **окно резолва личности на крае** | кэш успехов; не дыра, но названо, иначе первый наблюдатель прочтёт его как «удаление не сработало» **Предикат:** назвать окно кэша и показать, что оно **ограничено**; неограниченное окно — уже не «названо», а дефект. |
| 13 | **удаление личного аккаунта ДО стадии S4** | до снятия колонки цикл §1.2 держит схема, и порядка снятия пары не существует. Кто хочет закрыть это раньше — берёт **отдельное** изменение (замена действия ключа `users_account_fk` на `NO ACTION`), и оно расходуется на S4. Этот документ его **не** заказывает и не запрещает **Предикат:** `git grep -n "users_account_fk" "$R" -- services/iam/internal/migrations/ | grep -c "NO ACTION"` → **0** сегодня; станет ненулевым, только если это отдельное изменение возьмут. |

---

## §7 Открытые вопросы владельцу

Ни один из них не блокирует APPROVED: у каждого назван исход **по умолчанию**, действующий
до ответа. Блокирующим является только П2 ≠ 0 (§6 п. 7).

**В-1. Что делает материализация личного аккаунта после того, как человек удалил свой?**
Сегодняшнее правило вернёт аккаунт на первом же входе (IAM-ID-1-18). **По умолчанию —
сегодняшнее правило дословно**, потому что директива п. 3 требует «как есть». Альтернативы
(запомнить отказ; заводить только при самом первом появлении) меняют смысл директивы и
требуют состояния, которого в схеме нет.

**В-2. Подтверждаете ли снятие `InviteUserRequest` целиком** — вместо вырезания из него поля
аккаунта? **По умолчанию — снятие целиком**: вырезание оставило бы гейт без объекта, а
глагол — неработающим. Переезд сохраняет и область, и требование повышенного уровня.

**В-3. Кому и в какой форме видно, в каких аккаунтах человек?**

> [!important] ОТВЕЧЕНО ВЛАДЕЛЬЦЕМ 2026-08-23 — вопрос ЗАКРЫТ, умолчания больше нет
> Ответ дословно:
>
> > «человек может состоять в n аккаунтах и членство определено инвайтом. При этом отображать
> > всем в каких аккаунтах он приглашён неправильно, при этом приглашённый в своём списке
> > может увидеть где он приглашён».
>
> **Что из него следует — три утверждения, и все три нормативны:**
>
> 1. **форм чтения членства две** — свой список (субъект сам, параметра пользователя в запросе
>    нет) и членство в **названном** аккаунте (права проверяются на нём). Развёрнуто — §5.0 п. 0;
> 2. **форма «в каких аккаунтах состоит НАЗВАННЫЙ человек» запрещена** — это и есть
>    «отображать всем»;
> 3. **приглашённый видит СВОИ приглашения** — вторая половина ответа, и она не менее
>    обязательна, чем первая: реализация, скрывающая приглашение от самого приглашённого,
>    нарушает директиву так же, как и показывающая его всем.
>
>    *Круг 7: здесь стояла ссылка на IAM-ID-1-67 как на держателя этой половины — при
>    отозванной у него санкции (Б6). Держатели — **IAM-ID-2-08 и IAM-ID-2-21**.*
>
> **Следствие для документа: со стадии S5 и сценариев 43-48 снята санкция** (§13.1); сценарии
> сохранены, номера в обращении. §9 пересчитан — **71** сценарий вместо 70: кругом 6 заведён
> **IAM-ID-1-73**, снятых номеров не прибавилось.
>
> *Круг 7: стадия S5 вердиктом круга 6 **снята как стадия**, а IAM-ID-1-49 **перенесён** в
> IAM-ID-2 вслед за фиксом — здесь стояло «не отозван», и это по-прежнему верно (отзыва нет),
> но состояние у него теперь третье, и в счёте оно считается отдельно: `71 − 9 − 1 = 61`
> (§9.3, §14.4).*

> [!warning] Умолчание, стоявшее здесь до круга 6, было ОБОСНОВАНО ССЫЛКОЙ НА ДЕРЕВО — и
> дерево говорит обратное
> Здесь стояло: «**По умолчанию — возвращать**: форма „первый + счётчик“, полный перечень на
> карточке; **именно это обещает и комментарий, оставленный в дереве** на месте снятого
> столбца». Ссылка на свидетельство — сильная форма довода, и именно поэтому её надо было
> проверить. Проверена кругом 6 на `5533d21e`:
>
> | Место в дереве | Что обещает дословно | Обещает ли столбец-перечень |
> |---|---|---|
> | комментарий на месте снятого столбца | «читаемая **принадлежность** вернётся вместе с ресурсом членства (#1085)» | **нет** |
> | шапка расширения карточки | «Вернётся вместе с ресурсом членства (#1085), **не раньше**» | **нет** |
> | сквозная проба консоли | «эта половина заменяется на **„карточка называет аккаунт членства“**» | **нет**, и вдобавок утверждает **отсутствие** столбца в шапке **и** в выборе столбцов |
>
> Предикат: `git grep -n '1085' "$R" -- ui-future/` → **4** вхождения, все три места выше;
> `spec "$R" | grep -c users` → **0**, контроль `spec 554a8d49 | grep -c users` → **1**.
>
> **То есть умолчание опиралось на ПРОЧТЕНИЕ свидетельства, а не на само свидетельство.** Все
> три места говорят о **карточке** и адресуют `#1085`; про столбец не говорит ни одно, а
> проба прямо требует, чтобы столбца не обещала ни одна страница.
>
> **Отсюда — тон правки, и он несущее** (`doc-truthfulness`, «довод улучшился или просто
> уцелел»): группа F **не «устарела от нового решения»**, она была **ошибочна уже на момент
> круга 4**. Ответ владельца не изменил предмет — он **обнаружил** ошибку. Написать «решение
> изменилось» было бы мягче и неверно: это списало бы дефект документа на движение мира.
>
> **И недосчёт в самой формулировке умолчания.** «Ответ „не возвращать“ снимает **44-48**» —
> неверно: 43 (предусловие ветви) отзывается тем же решением, то есть отзыв накрывает
> **43-48**, шесть сценариев, а не пять. Число стояло в двух местах — здесь и в выноске
> сценария 43 — и было занижено в обоих.
>
> **Перемер 2026-08-24: круг 6 ошибся здесь же в ДРУГУЮ сторону, назвав «43-49, семь».**
> IAM-ID-1-49 отзыву не подлежит: его предмет — **порядок работ** (проба пишется до фикса и
> падает на дефекте, `ui.md` правило 12), а не величина, которую продукт отдаёт; так это и
> решено в IAM-ID-2 §2.6, таблица Б. Класс один и тот же с обеих сторон: перечень, выписанный
> вручную, расходится с собственным предметом — сперва занижением, потом снятием **группой**
> вместо разбора по существу. Правильная единица — не «группа», а **вопрос к каждому номеру**:
> отменяет ли решение его предмет.

**В-4. Готовы ли платить общим профилем и глобальной уникальностью почты** (§6 п. 5, 6) ради
участия человека в нескольких аккаунтах? **По умолчанию — да**: без глобальной уникальности
«одна строка на человека» неисполнимо, а анти-перечислительное требование к ответу
приглашения (IAM-ID-1-27) остаётся обязательным при любом ответе.

**В-5. Тексты отказов — часть контракта, и это изменение их правит** (IAM-ID-1-13).
Подтверждаете как осознанное, включая **введение нового `reason`-токена** под полосу
within-service предусловия? Существующие пять токенов эту полосу не покрывают, а взять
близкий чужой — значит научить клиента различать полосы неверно.

**В-6. Что «удалить аккаунт» означает для человека — три разных обещания, и приёмка по
умолчанию выдаёт второе.**

| Обещание | Что даёт |
|---|---|
| «аккаунт исчез, человек может войти заново с чистого листа» | было предметом исходного наблюдения владельца |
| «аккаунт исчез, **человек остался**, история сохранена, идентификатор тот же» | **выдаёт этот переход** |
| «человека в платформе больше нет» | ни один — отдельный предмет (право на забвение) со своим следом в аудите |

И следствие, которое обязано быть названо, а не обнаружено: по сохранённой директиве п. 3
удалённый личный аккаунт **возвращается на следующем входе** (IAM-ID-1-18). То есть «удалить»
сегодня означает «удалить, но не насовсем». Это законное следствие директивы — но оно меняет
смысл слова, которым владелец описывал задачу, и потому выносится вопросом, а не умолчанием.
Связан с В-1: ответ «б» или «в» на В-1 меняет и ответ здесь.

**В-7. Что происходит с утверждениями токена про аккаунт — исход виден арендатору.**
`kacho_active_account` и `kacho_account_id` сегодня собираются из принадлежности строки
(§1.5а класс 4). Для человека в N аккаунтах «активный аккаунт» **не определён by
construction**, и исходов три: снять оба утверждения · вычислять из явно выбранного аккаунта
запроса · требовать выбора от вызывающего. Умолчания здесь **нет намеренно**: любой из трёх
меняет то, что читает арендатор в своём токене, — то есть это продуктовое решение, а не
инженерный выбор. IAM-ID-1-59 требует, чтобы исход был **записан**, и остаётся верным при
любом ответе.

> [!important] РЕШЕНО 2026-08-16: снимаются ОБА утверждения
> Замер по дереву решил вопрос сам: `kacho_account_id` пишется в четырёх местах и читается
> прод-кодом **ноль** раз; `kacho_active_account` пишется в пяти и читается **один** — осью
> арендатора в аудите. То есть исход «снять» — самый дешёвый из трёх, а не самый
> разрушительный, как читалось без числа.
>
> **Предикаты, которых у этого замера не было** (дописаны кругом 3 — на них стоит решение,
> и решение без предиката проверить нечем; полностью они приведены в §1.5а, класс 4):
>
> ```sh
> git grep -nE '"kacho_account_id"[[:space:]]*[:=]|claims\["kacho_account_id"\]' "$R" \
>   -- 'services/iam/internal/*.go' | grep -vc '_test\.go'                    # 4 — пишется
> git grep -n 'getString(enriched, "kacho_account_id")' "$R" \
>   -- 'services/iam/internal/*.go' | wc -l                                   # 0 — читается
> # то же на kacho_active_account → 5 и 1; единственный читатель — ось арендатора в аудите
> ```
>
> Единица счёта названа отдельно, потому что общий греп по имени утверждения даёт другое
> число (**10** и **6** вхождений в непробном дереве сервиса) и включает документацию и
> кейсы сквозных проб. Оба набора чисел истинны; решение опирается на **первый**.
>
> Ось арендатора в аудите переводится на **область запроса**. Причина не в цене: утверждение
> в токене есть **снимок**, переживающий изменение, — человека исключили из аккаунта, а токен
> продолжает утверждать членство до истечения. После отрыва к этому добавляется второе:
> «активный аккаунт» при N членствах не определён, и любое умолчание (первый, старейший)
> фабрикует факт, которого никто не сообщал.
>
> **Что покраснеет и что с этим делать:** проба соответствия фасада утверждает непустоту
> утверждений. Её краснота закрывается **переводом на область запроса**, а не маскировкой.
> Затронуты также объявление в конфигурации выдачи, кейс сквозной пробы и страница
> документации — перечень взят из того же замера.

**В-8. Блокировка человека после отрыва становится ГЛОБАЛЬНОЙ — подтверждаете?**

> [!warning] ПОСЫЛКА ЭТОГО ВОПРОСА ОПРОВЕРГНУТА ДЕРЕВОМ, и решение принято 2026-08-16
> Ниже сказано, что блокировка сегодня действует в пределах одного арендатора. **Это не так.**
> Авторизуемая строка у человека одна на кластер — её держит частичный ключ уникальности среди
> активных `users_active_external_id_uniq` (предикат — §1.1), — поэтому заблокированный **уже
> сегодня** теряет доступ везде. Комментарий рядом с записью блокировки
> (`services/iam/internal/apps/kacho/api/user/set_blocked.go`) утверждает обратное; он и ввёл
> в заблуждение, и правится в той же стадии.
>
> **Предикат живости предмета** (сам факт, что писатель блокировки один и он ключуется на
> строку личности): `git grep -ln 'func.*Block' "$R" -- 'services/iam/internal/apps/kacho/api/user/*.go' | grep -vc '_test\.go'`
> → **2** (обработчик и use-case); контроль в обратную сторону — тот же предикат по
> `api/account/` даёт **0**, то есть блокировка живёт у личности, а не у аккаунта.
>
> Следствие для решения: «сделать блокировку свойством членства» — не сохранение поведения, а
> **новое сужение**. Хуже, оно требует читать состояние членства на пути авторизации, что §6
> п.3 прямо вывел из охвата: два места об одном предмете, из которых верно одно.
>
> **РЕШЕНО: блокировка остаётся свойством ЛИЧНОСТИ** — платформенным действием, как она и
> работает сегодня. Администратору аккаунта для «человек больше не работает у меня» служит
> **снятие членства** — ресурс, который этот же переход и заводит: предмет действия совпадает
> с его областью, и изобретать ничего не требуется. §6 п.3 остаётся в силе, противоречие снято.
>
> **Побочно найден второй механизм того же класса, в приёмке не названный:** отзыв сессий «до
> момента T» ключуется на строку человека, то есть после слияния строк становится свойством
> личности ровно так же и по той же причине. Перепись механизмов, ключуемых на строку человека
> (а не ссылающихся на неё), входит в стадию S0.

**Что вопрос спрашивал (сохранено как исходная постановка; ответ — в выноске выше).**
Посылка была такая: сегодня `Block`/`Unblock` ложатся в пер-аккаунтную строку, то есть
действуют в пределах одного арендатора; после слияния строк блокировка станет свойством
**личности**, и администратор аккаунта `A`, заблокировав человека, закроет ему доступ и в `B`,
где у `A` никаких прав нет. Формально это покрыто IAM-ID-1-72 (класс «заблокирован» переезжает,
не оживая) и запретом расширения доступа, но **сужение** доступа за пределами своего аккаунта —
отдельный предмет, и он не менее чувствителен, чем расширение. Исходов рассматривалось два:
блокировка остаётся свойством личности (и тогда правом блокировать обязан обладать не всякий
администратор аккаунта) либо становится свойством **членства** (новое поле ресурса членства).

**Действует первый исход** — блокировка остаётся свойством личности; следствие о том, кто
вправе блокировать, идёт вместе с ним. Посылка вопроса («сегодня — в пределах арендатора»)
**опровергнута деревом**: авторизуемая строка у человека одна на кластер уже сегодня.

> [!warning] Здесь стояло «До ответа — второй исход как более узкий» — прямо ОБРАТНОЕ решению
> §7 объявляет свои умолчания несущими: «до ответа действует такое-то» читается исполнителем
> как указание. Умолчание пережило собственный ответ — выноска над этим абзацем фиксирует
> «РЕШЕНО: блокировка остаётся свойством ЛИЧНОСТИ», то есть **первый** исход, а текст под ней
> продолжал предписывать **второй**, блокировку как свойство членства. Два места об одном
> предмете, из которых верно одно, — и читающий подряд уносил последнее прочитанное, то есть
> отменённое.
>
> Вдобавок отменённое умолчание было **несовместимо с §6 п. 3**: свойство членства требует
> читать состояние членства на пути авторизации, а §6 п. 3 прямо выводит это из охвата. Круг 3
> заметил противоречие и написал об этом в выноске — но снял его только там, оставив исходный
> абзац нетронутым. **Правка решения не закончена, пока не убрано умолчание, которое оно
> отменяет**: строчка «до ответа» переживает ответ молча.

---

## §8 Критерии приёмки (DoD)

1. **Все 61 действующий сценарий** имеют исполнимого производителя: integration-проба
   (testcontainers) и/или newman-кейс. Объявлено **71** (§9.3, там же предикат сверки), из них
   **девять** стоят с **отозванной санкцией** и производителя здесь не заказывают: 43-48
   (консольный перечень) и 63, 66, 67 (плоская поверхность чтения, перенацелена в IAM-ID-2);
   **один** — IAM-ID-1-49 — **перенесён** в IAM-ID-2 вслед за фиксом (IAM-ID-2-22). Отсюда
   71 − 9 − 1 = **61**. Сверх того у IAM-ID-1-01 отозвано одно **And** — сценарий остаётся
   действующим, потому что его предмет (одна строка личности) отзыву не подвергся.
   **Сквозную пробу браузером этот документ заказывает ровно одним сценарием** —
   **IAM-ID-1-55** (единственная консольная правка, им заказываемая); требование `ui.md`
   правила 12 внесено в его утверждения, а не держится отдельным сценарием о порядке.
2. **Строгий TDD**: по каждому сценарию показана пара «RED → GREEN»; проба написана и
   **прогнана** до кода. **Для S1 и части S2 это требование предъявлено быть не может** — они
   посажены до вердикта (§0.3), и порядок написания пробы относительно кода из дерева
   невосстановим. Признание §0.3 распространяется и на этот пункт: он связывает **S3 и S4**
   (с S5 санкция отозвана). **И его же придётся предъявить к той части S3 и S4, что села в ствол
   после круга 5** (§4): порядок написания пробы относительно кода там так же
   невосстановим — названо здесь, а не умолчано.
   Что по влитому проверено вместо — сверка против сценариев поимённо, §12.3.
3. **Инварианты на DB-уровне** (ban #10): уникальность и связь субъектов привязок с членством
   — конструкциями, не проверками в use-case. Инвариант владельца **снят** (см. выноску
   группы A); проверять, что он не всплыл обратно в use-case, — предмет IAM-ID-1-65.
4. **Гонки покрыты конкурентными пробами** (`data-integrity.md` §Чек-лист п. 5):
   IAM-ID-1-07, IAM-ID-1-17.
5. **Отрицания стоят в паре с положительным контролем**: IAM-ID-1-16/17, 21, 23, 24, 30, 31,
   **65** (внесён кругом 11: у `65` два отрицания — отказ по живой выдаче и снятие указателя
   области, — и оба обязаны нести близнеца; без него «указателя нет» зеленеет на системе,
   которая его не пишет вовсе).
6. **Пробы утверждают ИСХОД, а не вызов**: IAM-ID-1-21, 33 — на этих двух предметах проба
   «вызвали/эмитировали» зеленеет при неверном поведении.
7. **Гейты доказаны инъекцией в обе стороны**: IAM-ID-1-53 (страж уникальности).
8. **Проба, стерегущая пустой перечень, на пустом проходит**, объявляя перепись:
   IAM-ID-1-36.
9. **Ни одной маски, пропуска и вычета** ни в одной суите (`testing.md` §E2E,
   `e2e-flow.md` §2). Кейс, который не может упасть, хуже отсутствующего.
10. **Каждая стадия §4 самостоятельно зелена** перед следующей; порядок S2 → S3 → S4
    зафиксирован пробой IAM-ID-1-35, а не намерением.
11. **Применённые миграции не тронуты** (ban #5); каждая новая несёт своё заявление в своей
    шапке.
12. **`buf lint` зелён; адъюдикация объявленных сломов ПРОХОДИТ** — не «`buf breaking`
    краснеет ожидаемо»: красный `buf breaking` в конвейере есть упавший прогон, а не
    одобренный слом. Осознанный разрыв идёт **только** через `proto/declared-breaks.yaml`
    (пять обязательных полей; шаг конвейера — `adjudicate-declared-breaks`). Перечень
    **самоистекает**: запись без находки — тоже красное, поэтому запись снимается тем же
    изменением, которое поднимает базу сравнения. Сервис и край собраны и выкачены одним
    изменением.
13. **Гейты дерева зелены и покрывают новое** — каждый назван, потому что «прогнали тесты»
    их не включает: `make -C services/iam audit-list-filter` (списочный фильтр) ·
    `make -C gateway permission-catalog-check` (обе копии каталога байт-в-байт) ·
    `internal/repohygiene` `TestEmptyPageNeverPrecedesPaginationValidation` (порядок проверки
    пагинации) · **`make -C deploy fga-model-embed` + `services/iam/internal/authzmap/fga_model_drift_test.go`
    и `services/iam/internal/authzmodel/identity_test.go`** (согласие вшитой копии модели с
    каноном) · `internal/repohygiene` `TestNoDeferredWorkInTheTree` (ban #11).

    > [!warning] Здесь стоял «гейт дрейфа конфигмапа чарта из модели прав» — предмета у него
    > НЕТ (найдено кругом 6, Б3; исправлено кругом 7)
    > Замер на `release/identity-4` (`8a3e00f18`): `git ls-files deploy/ | grep -ci -E 'fga|authz.*model'`
    > → **0**. Посадка внешнего движка отношений снята целиком, и это закреплено пробой
    > `deploy/r7_3_32_engine_landing_retired_test.go`; модель **вшита** в бинарь сервиса
    > (`git grep -n 'go:embed fga_model.fga' -- services/iam` → **1**,
    > `services/iam/internal/authzmodel/authzmodel.go`).
    >
    > **Пункт DoD, который нечем исполнить, — не критерий.** Он хуже отсутствующего: критерий
    > занимает слот, объявляется невыполненным на каждом прогоне и обучает исполнителя тому,
    > что перечень DoD можно читать выборочно. Замена названа выше и **исполнима одной
    > командой каждая** — механизм у дрейфа есть, просто другой.

14. **Trail в vault** обновлён: записка задачи, `resources/`, `rpc/`, `edges/` по затронутым
    сущностям (`vault.md`).
15. **Публичный текст выдерживает публикацию**: оба репозитория публичны; ни коммит, ни
    приёмка, ни записка не собирают «координата + условие, при котором защита не
    действовала + следствие для постороннего» (`security.md` §«Публичные артефакты»).

16. **ВНЕШНЕЕ ПРЕДУСЛОВИЕ S3 выполнено: у IAM-ID-2 стоит `APPROVED`** (заведено кругом 7,
    Б1). Четыре действующих сценария этого документа — **01, 55, 68, 73** — потребляют формы
    чтения членства, чей контракт и производитель принадлежат
    `docs/specs/sub-phase-IAM-ID-2-membership-read-acceptance.md` (IAM-ID-2-01/-03, -06, -07).
    Пока у соседа нет вердикта, у этих четырёх **нет исполнимого производителя**, и п. 1
    по ним не выполним — значит **S3 не production-complete** (ban #14: стадия завершается
    в своих границах, а не по чужому вердикту).

    **Предикат — машинный, тем же способом, что читается вердикт этого документа:**
    ```sh
    ./scripts/docs-gate/run-all.sh     # строка переписи вердиктов у check-01
    ```
    либо адресно:
    ```sh
    sed -n '1,6p' docs/specs/sub-phase-IAM-ID-2-membership-read-acceptance.md
    ```
    **Шаги S3.1 и S3.2 от этого предусловия НЕ зависят** и исполняются целиком здесь; зависит
    S3.3 и **закрытие стадии**. Названо отдельным пунктом, а не оговоркой в §4, потому что
    иначе зависимость существует, а спросить её негде.

17. **Отступление от стандартных методов ресурса записано В ДЕРЕВЕ ПРОДУКТА, а не только
    здесь** (заведено кругом 11). §1.9 объявляет **решением** отсутствие `Delete` у ресурса
    членства: снятие остаётся суффикс-действием на человеке
    (`UserService.RemoveFromAccount`). `api-conventions.md` §«Структура ресурсов» требует, чтобы
    осознанное дизайн-решение легло в `docs/architecture/` **своего сервиса**; у iam это
    `services/iam/docs/engineering/architecture/`. Документ требует ровно этого от чужого
    отступления (выноска `64`) и обязан потребовать от своего.

    **Почему это не дублирование приёмки, а её единственный долговечный адрес.** Приёмка
    закрывается и уходит из поля зрения; страница сервиса остаётся и читается тем, кто заводит
    следующий глагол. Без неё следующий заведёт `Delete` «по стандарту» — и получит два места
    об одном предмете, ровно тот класс, который §1.9 и запрещает. Туда же **ссылается** сосед
    IAM-ID-2 (§16.5), а не на этот файл.

    **Предикат — и он сегодня не выполнен:**
    ```sh
    git grep -c 'RemoveFromAccount' -- services/iam/docs/engineering/architecture/
    #   → 0. Единственное упоминание глагола в документации iam — страница арендатора
    #   services/iam/docs/content/api/user.mdx (8 вхождений), а она описывает ВЫЗОВ, а не
    #   решение: «почему у ресурса членства нет Delete» там не спрашивают.
    # контроль в обратную сторону — предикат исполним и находит:
    git grep -c 'RemoveFromAccount' -- services/iam/internal | awk -F: '{s+=$NF} END{print s}'
    #   → 25
    ```
    Форма записи — по образцу соседа, у которого этот класс уже закрыт: nlb ведёт
    `services/nlb/docs/engineering/architecture/08-known-divergences.md`; у iam тот же файл
    называется `known-divergences.md`, и место для записи в нём готово —
    `grep -cE '^## [0-9]+\. ' services/iam/docs/engineering/architecture/known-divergences.md`
    → **17** пронумерованных расхождений (единица — заголовок расхождения, не файл: файлов в
    каталоге 26, `git ls-tree -r --name-only services/iam/docs/engineering/architecture/ | wc -l`).

---

## §9 Трассировка

### 9.1 Стадия каждого сценария — по КОНСТРУИРУЕМОСТИ, а не по теме

Стадия сценария определяется тем, **когда его Given впервые можно построить**, и ничем
другим. Это и есть ответ на «относительно какого множества стадия зелена» (DoD п. 10).

| Стадия | Сценарии | Почему именно здесь |
|---|---|---|
| **S0** замер | — | П1-П4 §3; сценариев не несёт, несёт числа для группы E |
| **S1** ресурс и бэкфилл | 11, 37, 38, 39, 40, 41, 72 | бэкфилл и классы существующих людей проверяются, как только членства заведены; авторитет ещё у колонки |
| **S2** права: отзыв | 28, 29, 30, 32, 33, 34, 35, 36, 58, 60, 61, 62 | отзыв как факт БД; 58 — на доменной сущности. **Круг 3 вынул отсюда 09 и 10** (сняты) **и 31, 57** (уехали в S3 вместе с правкой модели, §2.3а) |
| **S3** читатели, контракт и объект прав | 01\*, 02, 03, 04, 05, 08, 13, 22, 23, 24, 25, 27, **31**, 55\*, 56, **57**, 63, 64, 65, 66, 67, 68\*, 71, **73**\* | **порядок назначен вердиктом круга 6** (§2.3а): **S3.1** — модель, префикс `mbr`, глаголы ресурса членства **сверх посаженного исключения из аккаунта** (§1.9 — снятие уже есть), каталог, край **одним изменением**; **S3.2** — переезд глагола приглашения, и он меняет **контракт**, а не конструируемость: «два членства на одну строку» строится **уже сегодня**, обычным приглашением (§16.2, предикат и посаженная проба), поэтому **31, 57 и 65** исполнимы без синтетики **с конца S3.1**; **S3.3** — читатели (21 внутри сервиса, 55 в консоли, 56 в провайдере). **Формы чтения членства здесь НЕ появляются** — их контракт принадлежит IAM-ID-2 (§0.2, §5.0 п. 0); отмеченные `*` сценарии (01, 55, 68, 73) их **потребляют** и потому закрываются не раньше `APPROVED` соседа |
| **S4** contract | 06, 07, 12, 18, 19, 20, 26, 52, 53, 54, 59, 69, 70 | глобальная уникальность, снятие колонки и полей контракта; 12 и 18 требуют, чтобы колонка уже была снята |
| ~~**S5** консоль~~ | 43, 44, 45, 46, 47, 48, 49 | **СТАДИИ НЕТ** (вердикт круга 6, вопрос 3). У 43-48 **санкция отозвана** (§13.1): сценарии сохранены с записью об отзыве, номера в обращении, консольный предмет — `kacho#1085` / IAM-ID-2. **49 ПЕРЕНЕСЁН** в IAM-ID-2 вслед за фиксом, который он стережёт (IAM-ID-2-22) — состояние третье, и в счёте оно считается отдельно (§9.3). Ни один из семи стадии этого документа не имеет |
| **сквозные** | 14, 15, 16, 17, 21, 42, 50, 51 | директива материализации проверяется на **каждой** стадии (14-17); 21 — на S3 и повторно на S4; **42 — на S2, S3 и S4**; 50-51 — пробы отката своих стадий |

> [!warning] Здесь стояло «**обе** формы чтения членства появляются здесь же» — и это прямо
> противоречило §0.2 и шапке (найдено кругом 6, Б1; исправлено кругом 7)
> Формы чтения — предмет IAM-ID-2, и санкция на них отозвана решением по `kacho#1085`
> (2026-08-24). Строка S3 продолжала обещать их **здесь**, то есть два места об одном
> предмете, из которых верно одно. Исполнитель, читающий трассировку (а её читают именно
> затем, чтобы понять объём стадии), взял бы отсюда работу, которой этот документ не
> санкционирует.
>
> **Что осталось верным и потому не вычеркнуто:** четыре сценария этого документа формы
> чтения **потребляют**, и без них неисполнимы. Это выражено пометкой `*` и внешним
> предусловием (§0.2), а не обещанием построить формы здесь.

**Сценарий, попавший в несколько стадий, назван в каждой** — это не дубль:

- **21** на S3 утверждает перевыраженный фильтр, на S4 — что снятие поля его не ослабило;
- **42** («ни один существующий человек не потерял доступа») обязан прогоняться на **каждой
  стадии, способной изменить доступ**: S2 (отзыв как факт БД), S3 (правка модели **и**
  переключение авторитета — стадия меняет доступ дважды, поэтому 42 прогоняется на её конце
  и после первого шага), S4 (снятие колонки). Поставить его только на S1 было бы хуже, чем
  не ставить вовсе: на S1
  доступ не менялся, поэтому он **тождественно зелен** и создавал бы вид проверки там, где её
  нет. Ошибка первой редакции этого раздела — исправлена здесь же, чтобы её не повторили.

### 9.2 Задачи

| Задача | Сценарии |
|---|---|
| `kacho#470` данные | 01-08, 11-13, 37-42, 50-54, 63-67, 69, 71, 72 |
| `kacho#471` контракт | 19-27, **31**, 55, 56, **57**, **59**, 68, 70, **73** |
| `kacho#472` права | 28-30, 32-36, 58, 60, 61, 62 |
| `kacho#473` консоль | 43-49 — у **43-48** санкция отозвана решением по `kacho#1085` (§13.1), сценарии сохранены; **49 перенесён** в IAM-ID-2 вслед за фиксом (IAM-ID-2-22, вердикт круга 6, вопрос 4). Из группы F действующих сценариев **не осталось**; наблюдаемое по консоли утверждает **IAM-ID-1-55** (группа C) |
| директива п. 3 (материализация) | **14-18** |

**Что изменилось кругом 3 в этой таблице:** из `#470` ушли 09 и 10 (сняты); 31 и 57 переехали
из `#472` в `#471` — их предмет теперь смена `object_type` в каталоге и на крае, то есть
контракт, а не только модель (§2.3а). Задача `#472` сохраняет предмет «права переносятся, не
расширяясь».

**Что изменилось кругом 6:** у сценариев 43-48 задачи `kacho#473` **отозвана санкция** (строка
и номера остались, §13.1); заведён **IAM-ID-1-73** и назначен `#471` — его предмет контрактный
(две проекции одного факта и их разность), а не консольный.

**Что изменилось кругом 4: IAM-ID-1-59 назначен `#471`.** Он не был назначен **ни одной**
задаче: §9.1 распределяет по стадиям все 70 сценариев (на тот момент), а сумма этой таблицы
давала **69**. Предикат, который это ловит, — сверка двух множеств, а не сложение столбцов:

```sh
D=docs/specs/sub-phase-IAM-ID-1-identity-account-decoupling-acceptance.md
# назначенные: столбец сценариев таблицы §9.2, диапазоны развёрнуты поэлементно.
# Берётся ИМЕННО третье поле строки — иначе в множество попадут номера самих задач
# (470-473) и предикат отчитается о четырёх «призраках», которых нет.
assigned=$(awk -F'|' '/^### 9.2 Задачи/{f=1} f&&/^### 9.3/{exit} f&&NF>2{print $3}' "$D" \
  | tr -d '*`' | tr ',' '\n' | tr -s ' ' '\n' | grep -E '^[0-9]+(-[0-9]+)?$' \
  | awk -F- '{if(NF==2){for(i=$1;i<=$2;i++)print i+0}else print $1+0}' | sort -n -u)
# объявленные: заголовки сценариев
all=$(grep -o '^#### IAM-ID-1-[0-9]\+' "$D" | sed 's/.*-//' | sed 's/^0*//' | sort -n -u)

comm -23 <(echo "$all") <(echo "$assigned")   # СИРОТЫ  — пусто
comm -13 <(echo "$all") <(echo "$assigned")   # ПРИЗРАКИ — пусто
# обе стороны: 71 объявлено, 71 назначено (круг 6 завёл 73; до него — 70 и 70)

# контроль в обратную сторону — предикат обязан УМЕТЬ найти сироту:
comm -23 <(echo "$all") <(echo "$assigned" | grep -vx 59)   # → 59
```

Контроль здесь не украшение: обе рабочие строки печатают **пустоту**, а пустота — ровно то,
что печатает и сломанный предикат. Третья строка показывает, что он различает.

**Почему `#471`, а не `#470`.** Предмет сценария — **утверждения токена**, то есть то, что
арендатор читает в своём токене; это контрактная поверхность, и решение по ней принято
вопросом В-7. Тот же довод, по которому кругом 3 туда же переехали 31 и 57: предметом стал
контракт, а не механика внутри.

> [!warning] Сирота не ловится сложением — и именно поэтому прожил круг
> §9.3 несёт **предикат полноты по номерам** (множество = `1..73` без выведенных `09`/`10`),
> и он был и остаётся зелёным: сценарий 59 в документе есть, номер не пропущен. Но полнота **нумерации**
> и полнота **назначения** — разные свойства, и первое о втором не говорит ничего. Сценарий,
> у которого нет задачи, не потеряется в счёте: он потеряется в **работе**, а это дороже —
> его предмет (что арендатор читает в своём токене после отрыва) просто не окажется ничьим.

### 9.3 Счёт

**Сценариев — 71, из них действующих 61.** Арифметика названа целиком, чтобы её можно было
проверить, а не принять: объявлено **73** номера · кругом 3 **сняты** 09 и 10 (выноска группы
A) · кругом 6 заведён **73** (производитель класса 9 §1.5а). Итого **73 − 2 = 71** сценария в
документе.

**Состояний ТРИ, а не два, и счёт у каждого свой.**

| Состояние | Что с текстом и номером | Кто | Сколько |
|---|---|---|---:|
| **снят** | текст исчезает, номер выводится из обращения | 09, 10 (круг 3) | не в счёте |
| **санкция отозвана** | текст и номер остаются, отозвано **позволение реализовывать** | 43-48 (консольный перечень) · 63, 66, 67 (плоская поверхность чтения) | **9** |
| **перенесён** | текст и номер остаются, предмет **верен и исполняется в другом документе** | **49** → IAM-ID-2-22 (вердикт круга 6, вопрос 4) | **1** |

Отсюда второе число: **71 − 9 − 1 = 61** действующий, и его требует DoD п. 1.

> [!important] Круг 7: третье состояние заведено, и число 62 стало 61 — это НЕ правка того,
> что вердикт признал верным
> Вердикт круга 6 принял арифметику `71 / 9 / 62` с контролями в обе стороны, и **метод
> здесь не тронут**: предикаты, их анкеры и обратные контроли — те же, что он проверял. Но
> тот же вердикт **перенёс** сценарий 49, а перенесённый сценарий не может числиться среди
> тех, у кого этот документ заказывает производителя: DoD п. 1 требует исполнимого
> производителя **для каждого** из действующих.
>
> **Смешать перенос с отзывом было бы прямой ложью о предмете.** У отозванного санкция снята
> потому, что реализовывать его **нельзя**; у 49 предмет верен и **реализуется** — в другом
> документе, другим номером. Записав его девятым десятым в «отозванные», документ утверждал
> бы, что требование `ui.md` правила 12 к этой линии не предъявляется. Оно предъявляется —
> и здесь его несёт IAM-ID-1-55.
>
> Ровно этого требует и Б6 того же вердикта: **развести состояния**, а не подогнать число.

**Номер — устойчивая ручка, а не позиция, и снятый номер НЕ переиспользуется.** Номера
назначены в порядке появления сценария в работе, поэтому 55-72 стоят физически там, где их
предмет, а не в конце файла. Отсюда два разных утверждения, и их нельзя смешивать:

```sh
D=docs/specs/sub-phase-IAM-ID-1-identity-account-decoupling-acceptance.md
# ЕДИНСТВЕННЫЙ действующий предикат полноты: множество номеров = 1..73 БЕЗ выведенных
diff <(grep -o '^#### IAM-ID-1-[0-9]\+' "$D" | sed 's/.*-//' | sed 's/^0*//' | sort -n) \
     <(seq 1 73 | grep -vxE '9|10')
# пусто — пропусков и дублей нет; 43-48 в множестве ОСТАЮТСЯ: у них отозвана санкция,
# а не выведен номер (см. абзац выше — состояния разные)

# зеркало: выведенные номера НЕ вернулись как заголовки сценариев
grep -cE '^#### IAM-ID-1-(09|10) ' "$D"          # 0
# и о них по-прежнему сказано, почему их нет — предикат АНКЕРОВАН на строку выноски,
# ПО ОДНОЙ на круг: общий анкер слил бы два разных решения в одно утверждение
grep -cE '^> \[!warning\] СНЯТЫ кругом 3' "$D"   # 1
grep -cE '^> \[!warning\] ОТОЗВАНЫ IAM-ID-1-43' "$D"   # 1
# второе число DoD — действующие: объявленные минус отозванные минус перенесённые.
# Анкеры РАЗНЫЕ, по одному на состояние: общий слил бы три решения в одно утверждение
W='^> \*\*\[САНКЦИЯ ОТОЗВАНА 2026-08-24\]\*\*'
T='^> \*\*\[ПЕРЕНЕСЁН В IAM-ID-2 — 2026-08-24'
echo $(( $(grep -c '^#### IAM-ID-1-' "$D") - $(grep -c "$W" "$D") - $(grep -c "$T" "$D") ))   # 61
# и сами эти девять — поимённо, чтобы число не пришлось принимать на веру:
awk '/^#### IAM-ID-1-/{id=$2} /^> \*\*\[САНКЦИЯ ОТОЗВАНА 2026-08-24\]\*\*/{print id}' "$D"
#   63, 66, 67, 43, 44, 45, 46, 47, 48
# и перенесённый — поимённо тем же способом:
awk '/^#### IAM-ID-1-/{id=$2} /^> \*\*\[ПЕРЕНЕСЁН В IAM-ID-2 — 2026-08-24/{print id}' "$D"
#   49
# контроль в обратную сторону — анкеры НЕ пересекаются: сценарий не может быть в двух
# состояниях сразу, и пересечение множеств обязано быть пусто
w() { awk '/^#### IAM-ID-1-/{id=$2} /^> \*\*\[САНКЦИЯ ОТОЗВАНА 2026-08-24\]\*\*/{print id}' "$D" | sort; }
t() { awk '/^#### IAM-ID-1-/{id=$2} /^> \*\*\[ПЕРЕНЕСЁН В IAM-ID-2 — 2026-08-24/{print id}' "$D" | sort; }
comm -12 <(w) <(t)          # пусто
# и сам этот контроль обязан УМЕТЬ найти пересечение — иначе «пусто» неотличимо
# от «команда никогда ничего не находит»:
comm -12 <(t) <(t)          # IAM-ID-1-49 — команда различает, а не молчит всегда
# контроль в обратную сторону — предикат обязан УМЕТЬ найти пропуск:
diff <(grep -o '^#### IAM-ID-1-[0-9]\+' "$D" | sed 's/.*-//' | sed 's/^0*//' | sort -n) \
     <(seq 1 73 | grep -vxE '9')                 # → строка про 10: команда различает
```

> [!note] Почему верхняя граница выросла до 73, а не «до 72, ведь семь сняли»
> Снятый номер из множества **не исчезает** — он выводится из обращения (ниже). Поэтому
> граница `seq` есть **старший ВЫДАННЫЙ** номер, а не число живых сценариев: `73`, а не `71`.
> Предикат по `seq 1 71` мерил бы мощность вместо множества и покраснел бы на верном
> документе — тот же класс, о котором сказано двумя абзацами ниже, только с новой стороны.

> [!note] Анкер здесь не украшение: неанкерованная форма считает саму себя — и её число РАСТЁТ
> Первая редакция этой строки была `grep -c 'СНЯТЫ кругом 3'` и давала **2** — выноску и
> **процитированную рядом команду**. Утверждение о себе не может быть своим же
> свидетельством; предикат обязан ключеваться на форму, которой цитата не обладает.
>
> **Круг 4: та же неанкерованная форма даёт уже 4.** Число выросло от одних лишь правок
> документа — каждый круг, объясняя эту ошибку, добавляет ещё одно вхождение строки.
> То есть неанкерованный предикат здесь не просто неверен, он **неверен нарастающе**: его
> величина есть функция того, сколько раз о нём написали. Анкерованная форма как давала **1**,
> так и даёт — она ключуется на начало строки выноски, которого нет ни у цитаты, ни у прозы.

**Почему номера не переиспользуются.** На них ссылаются вне этого файла: имена
integration- и newman-проб трассируются по идентификатору, задача `PRO-Robotech/kacho#610`
называет IAM-ID-1-09/10 поимённо, и ветка `issue-472` уже прогоняла инвариант под этим
номером. Отдать номер другому предмету значит сделать все эти ссылки **тихо ложными** —
они продолжат резолвиться и будут указывать не туда.

**Для 43-48 это правило работает С ДРУГОЙ СТОРОНЫ, и потому они не сняты.** Их предмет
уезжает в **другой документ** (`kacho#1085` / IAM-ID-2), где у него **свои** номера
(IAM-ID-2-19…-22). Освободить здесь 43-48 значило бы либо оставить в двух документах одной
линии по номеру на один предмет, либо — при переиспользовании — завести два разных предмета
под одним идентификатором, и различить их можно было бы только по тому, какой файл читатель
открыл первым. Поэтому номера остаются при своих сценариях, а сценарии — в документе, с
записью об отзыве: так ссылка из IAM-ID-2 §2.6 резолвится в текст, а не в пустоту.

**Предикат по `seq 1 71` был бы ложной находкой** — он мерил бы мощность, а не множество, и
покраснел бы на верном документе. Прежняя редакция уже наступала на этот класс с другой
стороны: её первая версия требовала **порядка следования** и стала неверной на первом же
сценарии, дописанном в середину. Записано оба раза, чтобы следующий не «починил» предикат
обратно.

| Группа | Предмет | Сценариев |
|---|---|---:|
| A | отрыв данных | **12** |
| A2 | членство как production-ресурс | **7** (круг 6: +73) |
| B | материализация личного аккаунта сохраняется | 5 |
| C | контракт | 13 |
| D | права переносятся, не расширяясь | 15 |
| E | существующие люди по классам | 7 |
| F | консоль — **санкция отозвана** у 43-48, предмет ушёл в `kacho#1085` / IAM-ID-2; **49 перенесён** туда же. Действующих в группе — **0** | **7** |
| G | стадии, откат, невозврат | 5 |
| | **итого** | **71** |

**Числа этой таблицы получены обходом файла, а не сложением в уме** — предикат, печатающий
её целиком, чтобы расхождение было видно строкой, а не итогом:

```sh
awk '/^### (Группа|~~Группа)/{g=$0} /^#### IAM-ID-1-/{c[g]++} END{for(k in c)print c[k], k}' "$D" \
  | sort -k2
#   12 Группа A · 7 Группа A2 · 5 Группа B · 13 Группа C · 15 Группа D · 7 Группа E ·
#   7 Группа F · 5 Группа G — F печатается: её сценарии в документе стоят, отозвана санкция
grep -c '^#### IAM-ID-1-' "$D"    # 71 — итог, снятый той же командой, что и разбивка
```

---

## §10 Разбор круга 1 — 20 замечаний, что сделано по каждому

Круг 1: `acceptance-reviewer`, 2026-08-16, база `49c3404`, вердикт **❌ CHANGES REQUESTED**.
Закрыто **20 из 20**; возвращённых нет. Чужие измерения перед правкой перепроверены — два
уточнены, и уточнения ниже названы, потому что принять чужое число как своё значило бы
повторить ровно тот класс, который эта приёмка ловит.

### Блокирующие (1-8)

| # | Замечание | Что сделано | Где смотреть |
|---|---|---|---|
| 1 | перепись §1.5а не покрывает прод-код | §1.5а **переписана**: перепись разделена на типизированных потребителей (их перечисляет **сборка**, и это правильный механизм) и нетипизированных (их не найдёт никто, кроме переписи) — классов стало **7**. Уточнение: 16 — **пол**, а не число; второй предикат находит ещё 4 через `primary.AccountID` | §1.5а |
| 2 | §2.4 п. 3 утверждает неверное про каскад | утверждение **снято** и заменено разделением «политика не меняется / её реализация меняется обязательно», с координатой и стадией | §2.4 п. 3, IAM-ID-1-57 |
| 3 | утверждения токена теряют источник | заведены классом 4 и 5 переписи; исход обязан быть выбран из трёх явно; названный кейс сквозной пробы приводится тем же изменением и **не маскируется** | §1.5а, IAM-ID-1-59 |
| 4 | правка модели — название, а не работа | заведён §2.3а: адрес (файл, тип, оба отношения), 4 условия целевой формы, радиус модели и то, чем удерживается порядок. **Радиус — 6 и 6** (не «6/5»: см. ниже и §2.3а). **Круг 3 перенёс саму работу в S3** — условие 1 невыразимо без смены объекта | §2.3а, §4, DoD п. 13 |
| 5 | IAM-ID-1-33 противоречит себе | разведены **три роли**: `RESTRICT` даёт гарантию, дренаж в той же транзакции — шаг, материализация — наблюдаемое. Given оговаривает, что `A` не личный аккаунт | IAM-ID-1-33, IAM-ID-1-60 |
| 6 | «`buf breaking` краснеет ожидаемо» неверно | исправлено во **всех трёх** местах: разрыв идёт через перечень объявленных сломов, пять полей, адъюдикация **проходит**; самоистечение записи внесено в DoD | S4, IAM-ID-1-19, DoD п. 12 |
| 7 | IAM-ID-1-43 не может упасть | переписан односторонне и фальсифицируемо; выбор ветви вынесен в В-3; названо, что происходит с 44-47 при ветви (а) | IAM-ID-1-43 |
| 8 | негативы членства неполны | заведена **группа A2**: malformed id, полосы кода и токена, снятие с идемпотентностью, пагинация до замыкания, списочный фильтр, полнота каталога прав | IAM-ID-1-63…68 |

### Важные (9-20)

| # | Замечание | Что сделано |
|---|---|---|
| 9 | классы не образуют разбиения | ось разбиения **одна** — состояние строки (К1/К2/К3); владение и посевное происхождение объявлены **срезами**, от которых исключительность не требуется; заведён IAM-ID-1-72 |
| 10 | не сказано, чем снимается перечень доступа | §5.0/A: страница курсором → пообъектная проверка партиями ≤100; названо, почему перечисление вселенной даёт ложное совпадение |
| 11 | не различить «не доехало» от «отозвано» | §5.0/B: у появления доступа — ограниченный повтор, у **исчезновения** — однократное утверждение после наблюдаемого признака дренажа; признак обязан существовать (IAM-ID-1-62) |
| 12 | §6 обещает предикат, несёт 4 из 13 | предикаты дописаны — **13 из 13** |
| 13 | `reason`-токен не существует | названо прямо: подходящего токена нет, таблица расширяется, это правка контракта → В-5 |
| 14 | разворот по фильтру списка не объявлен | назван разворотом с причиной; заведён IAM-ID-1-70 на `reserved` номера и имени |
| 15 | четыре «сценария» — процессные | все четыре **переписаны через наблюдаемое**: 26 — совпадение набора полей, 35 — прогон отзыва на дереве S2, 49 — инъекция в обе стороны, 52 — миграция сама отказывается идти на негодных данных |
| 16 | три требования решения потеряны | заведены: IAM-ID-1-61 (кортежи), IAM-ID-1-62 (наблюдаемость + почта в лог), IAM-ID-1-71 (внешний грант) |
| 17 | «надгробная плита» без производителя | заведён IAM-ID-1-69 с **побайтовым** сравнением |
| 18 | стадия не проставлена | §9.1: стадия у **каждого** из 72, назначенная по конструируемости; проверено, что не назначенных нет |
| 19 | дизъюнкция в IAM-ID-1-56 | названа ветвь по умолчанию; вторая — решение, фиксируемое до S3 |
| 20 | вопрос В-2 решения потерян | заведён В-6, включая следствие «удалить — не насовсем» |

### Что уточнено в чужих измерениях

**Не придирка, а дисциплина:** число, принятое без проверки, ошибается увереннее открытого разбора.

1. **«16 прод-мест»** — верно для предиката по имени переменной и **является полом**.
   Предикат по другому признаку немедленно находит `primary.AccountID` в четырёх местах,
   включая оба утверждения токена. Вывод изменился не в числе, а в **методе**: исчерпывающую
   перепись типизированных читателей приёмка не объявляет вовсе — её производит сборка.
2. **«шесть типов»** — верно для присваиваемого `account: [account]` (**6**); конструкцию
   `super_admin: admin from account` несут **тоже 6**. Единицы разные, числа совпали.

   > [!warning] Здесь стояло «**5**», и это было двумя местами об одном предмете, из которых
   > верно одно (исправлено кругом 3)
   > Шапка круга 2 уже объявляла исправление: число 5 получалось **префиксным грепом**, то
   > есть мерило **порядок дизъюнктов**, а не свойство. Но исправлено оно было только в
   > §2.3а, а здесь — нет; §10 продолжал утверждать «5» и читался как источник, потому что
   > стоит в разделе «что уточнено в чужих измерениях». Предикат по свойству — §2.3а; на всех
   > трёх ревизиях круга 3 он даёт **6**.
   >
   > Урок ровно тот, ради которого написан §1.0: **правка числа обязана идти по всем местам,
   > где оно названо**, а не только там, где его заметили. Радиус берётся предикатом по имени
   > механизма, а не по абзацу, который правишь.

**Замечание про правки во время ревью принято.** Ревизия круга 2 зафиксирована в шапке.

---

## §11 Круг 3 — что чинилось и что найдено сверх заказанного

Повод — `PRO-Robotech/kacho#616`. Правку вносит **автор приёмки**, а не исполнитель стадии:
предмет здесь — утверждения документа, а не поведение продукта.

### 11.1 Два утверждения, названные задачей: каждое стоило прогона

| # | Утверждение | Где стояло | Чем опровергнуто |
|---|---|---|---|
| 1 | «`AccountService.Create` членство владельца заводит сам» | выноска IAM-ID-1-09 | писателей таблицы членства в Go-коде **0** при **4** у соседней таблицы той же формой (предикат — выноска группы A). Единственный писатель — бэкфилл и триггер миграции, то есть **зеркало снимаемой колонки**; создание второго аккаунта её не меняет |
| 2 | «объект личности можно спрашивать относительно аккаунта, не меняя объекта» | §2.3а, стадия S2 | аккаунт-скоупным объектом становится **членство**: `object_type` у **10** записей каталога прав плюс таблица форм ответа края (предикат — §2.3а). Это контракт, то есть **S3** (`PRO-Robotech/kacho#471`) |

**Исходы.** По первому — сценарии IAM-ID-1-09/10 **сняты** (решение владельца по
`PRO-Robotech/kacho#610`: владеть ≠ состоять; четыре довода — в выноске группы A). По
второму — правка модели **перенесена в S3**, вместе со сценариями 31 и 57.

**Общее у обоих, и это предмет задачи, а не совпадение:** ни одно не несло предиката. Их
нельзя было опровергнуть чтением — только прогоном, и прогоном они и опровергнуты.

### 11.2 Найдено сверх названных двух — 9 утверждений, из них 3 уже разошлись с деревом

Перепись велась **по всему документу**, а не по разделам, где нашлись первые два. Единица —
**утверждение о дереве**; координаты и числа считались вместе, потому что чинить приходится
и то и другое.

| # | Утверждение | Состояние | Что сделано |
|---|---|---|---|
| 1 | «колонка «Аккаунт» стоит в **четырёх** спеках, радиус — спека `users`» (§1.7) | **РАЗОШЛОСЬ**: спек **3**, и `users` среди них нет — столбец снят решением по `PRO-Robotech/kacho#473` и влит в ствол | §1.7 перемерена на трёх ревизиях; предмет S5 перевыражен как «вернуть перечень», IAM-ID-1-45 и -48 переписаны, В-3 объявлен отвеченным деревом |
| 2 | «старший занятый номер миграции — `0096`» (§4) | **РАЗОШЛОСЬ**: на стволе `0100`, на ветке работы `472002` | число заменено **предикатом**: номер добывается на том дереве, куда пишут |
| 3 | «конструкцию `super_admin: admin from account` несут **5** типов» (§10) | **РАЗОШЛОСЬ САМО С СОБОЙ**: шапка круга 2 объявляла исправление на **6**, §2.3а его несло, §10 — нет | §10 приведён к 6 с предикатом; записано, что правка числа идёт по **всем** местам |
| 4 | предикат про приостановку членства, стоящий в таблице §0.1 | предикат **не о том утверждении**, под которым стоит | снят; на месте — §6 п. 3. Директива §0.1 получила **свой** предикат (§1.6), в том числе **зеркальный** — «ворот „ноль членств“ в дереве нет» |
| 5 | предикаты §1.5а и §6 читали `origin/main` при числах, стоящих на `554a8d49` | форма, при которой команда и число **обязаны** разойтись | все предикаты пиннят ревизию переменной `$R`; заведён §1.0 с правилом чтения |
| 6 | «Пять объявленных `reason`-токенов» (IAM-ID-1-13) | верно, предиката не было | предикат по дереву **воркспейса** (переменная `$W`) — правила живут в другом репозитории |
| 7 | «`kacho_account_id` пишется в 4, читается 0; `kacho_active_account` — 5 и 1» (В-7) | верно, предиката не было — **а на этих числах стоит решение** | предикаты дописаны, единица счёта названа: общий греп даёт **10** и **6**, и это другая величина |
| 8 | «блокировка уже сегодня глобальна» (В-8) | верно, опоры не было названо | опора названа (частичный ключ среди активных, §1.1) плюс предикат живости писателя с контролем |
| 9 | координаты вида `файл.go:строка` без пути — **60** цитат в документе | неверифицируемы by construction (файлов с таким именем в дереве может быть несколько или ни одного), номер устаревает от любого коммита выше по файлу | все несущие переведены на **имя символа** и снабжены предикатом; см. оговорку ниже |

**Оговорка по п. 9 — размен назван, а не спрятан.** Переведены координаты, на которых стоит
утверждение. Часть номеров строк осталась в таблицах §1.5а как **снимок `554a8d49`**: у
каждого класса там есть свой предикат, и именно он авторитетен. Переписывать все шестьдесят
поимённо значило бы завести шестьдесят новых возможностей ошибиться ради утверждений, которые
уже проверяемы предикатом; это осознанный выбор, а не недосмотр.

### 11.3 Три ошибки предиката, пойманные на себе при этой же правке

Записаны, потому что предикат, который лжёт, хуже отсутствующего — он **заимствует доверие**
у документа вокруг.

1. **`--include=*.go` — флаг GNU grep, а не git.** `git grep … -- <путь> --include=*.go` его
   молча игнорирует и считает **всё** дерево, включая `.sql` и пробы. Первая редакция
   предиката о писателях членства так и была написана и дала **2** там, где верно **0**.
   Пути в git задаются **pathspec** (`-- 'services/iam/*.go'`), и разница видна только на
   контроле.
2. **Окно `grep -A<N>` над вложенным синтаксисом.** Счёт опций глагола `Invite` дал **2**
   вместо 3 (объявление уехало за четырнадцатую строку), а **отрицательный контроль на
   `List` дал 3** — окно съехало в соседний блок. Блок берётся по своим границам, иначе
   контроль отвечает больше положительного случая.
3. **Предикат, считающий сам себя.** `grep -c 'СНЯТЫ кругом 3'` дал **2**: выноску и
   **процитированную рядом команду**. Анкер на форму строки, которой цитата не обладает.

Плюс два числа, исправленные по ходу: `ListAccountsForUser` — **20** вхождений и **6**
непробных (обе величины истинны, единицы разные), `case "` в отображении отказов — **9** в
самой функции против **32** по файлу.

### 11.4 Что круг 3 НЕ делал

- **не менял ни одного сценария по существу**, кроме снятых 09/10 и перевыраженных 45/48 —
  и оба перевыражения названы прямо в самих сценариях;
- **не перемерял §3**: это утверждения о **стенде**, они проверяются на стенде и каждый раз
  заново (§1.0);
- **не перебазировал §1 на свежую ревизию.** Числа §1 остаются на `554a8d49` — базе, которую
  APPROVED и санкционировал; там, где ствол круга 3 даёт другое, это названо в самой строке.
  Полная перебазировка — отдельная работа, и делать её заодно значило бы смешать две правки;

  > [!warning] ЭТОТ ПУНКТ ОТМЕНЁН КРУГОМ 4 — допущение прожило четырнадцать часов
  > Довод был защитим и **оказался неверен по факту**, а не по логике: смешивать две правки
  > действительно дефект, но §1 успел разойтись с деревом раньше, чем нашёлся повод его
  > трогать. Из **38** предикатов главы значение изменилось у восьми, четыре стояли в тексте
  > как действующие, и один давал ложный ноль на живом
  > потребителе безопасности. Круг 4 перебазировал §1 целиком; норма, из этого выведенная, —
  > §1.0а, разбор — §12.1. Пункт оставлен, а не удалён: он показывает, **как** выглядит
  > защитимое решение, которое дерево опровергает без единого возражения;
- **не трогал §7 В-1, В-2, В-4, В-5, В-6** — у них нет утверждений о дереве.

---

## §12 Круг 4 — перебазировка §1, признание влитого, десять замечаний

Вердикт круга 3 — ❌ CHANGES REQUESTED: **3 блокирующих + 7 важных**, закрыто **10 из 10**.
Оценка самой работы круга 3 была высокой (67 из 70 предикатов воспроизвелись точно, оба
заказанных предмета подтверждены), и блокировало не качество, а **устаревшая база**: §1
описывал дерево трёхдневной давности.

### 12.1 Перебазировка §1 — перемерены ВСЕ 38 предикатов, значение изменилось у восьми

Перечень приведён целиком, а не по находкам: перечень, называющий одни расхождения,
неотличим от перечня, в котором остального не смотрели (`testing.md` §«Гейт на класс», п. 3).
Ревизии — `554a8d49` (база APPROVED) и `43d332ac` (ствол круга 4).

| § | Предикат (единица) | `554a8d49` | `43d332ac` | |
|---|---|---:|---:|---|
| 1.1 | `account_id text NOT NULL` (строки) | 4 | 4 | = |
| 1.1 | три ключа уникальности (объявления индекса) | 3 | 3 | = |
| 1.1 | сообщение неизменяемости (формы ключа маски) | 2 | 2 | = |
| 1.2 | `accounts_owner_fk` · `users_account_fk` — `RESTRICT` | 1 · 1 | 1 · 1 | = |
| 1.2 | те же на `CASCADE` (контроль) | 0 · 0 | 0 · 0 | = |
| 1.3 | ссылки на `users(id)` (ключи) | 13 | **14** | **≠** |
| 1.3 | разбивка `RESTRICT`/`CASCADE`/`SET NULL` | 8/4/1 | 8/**5**/1 | **≠** |
| 1.4 | `resolveCanonicalSubjectID` (вхождения) | 5 | 5 | = |
| 1.4 | слитое условие запасного пути | 1 | 1 | = |
| 1.4 | `len(actives)` всего (контроль) | 1 | 1 | = |
| 1.4 | `ListAccountsForUser` — все / непробные | 20 / 6 | 20 / 6 | = |
| 1.5 | `User.account_id = 6` | 1 | 1 | = |
| 1.5 | `msg InviteUserRequest \| grep -c account_id` | 1 | 1 | = |
| 1.5 | `msg ListUsersRequest \| grep -c 'account_id = 4'` | 1 | 1 | = |
| 1.5 | `msg GetUserRequest` (контроль) | 0 | 0 | = |
| 1.5 | `from_request_field: "account_id"` | 1 | 1 | = |
| 1.5 | `blk Invite` — три опции гейта | 3 | 3 | = |
| 1.5 | `blk List` — те же опции (контроль) | 0 | 0 | = |
| 1.5 | `blk List \| grep -c '"<exempt>"'` | 1 | 1 | = |
| 1.5а | класс 1 — консоль | 2 | 2 | = |
| 1.5а | класс 2 — провайдер (файлы) | 4 | 4 | = |
| 1.5а | класс 3 — скрипт, **прежний** предикат (форма записи) | 1 | **0** | **≠ ложный ноль** |
| 1.5а | класс 3 — **новый** предикат (отношение) | 1 | 1 | = |
| 1.5а | класс 4 — пишется `account_id` / `active_account` | 4 / 5 | 4 / 5 | = |
| 1.5а | класс 4' — читается `active_account` | 1 | 1 | = |
| 1.5а | класс 5 — кейс сквозной пробы | 2 | 2 | = |
| 1.5а | класс 6 — `AccountScopedStructuralFact` (непробные) | 7 | **4** | **≠** |
| 1.5а | класс 6 — **вызовы** функции | 4 | **0** | **≠ производитель переехал** |
| 1.5а | класс 7 — самовалидация домена | 1 | 1 | = |
| 1.5а | класс 8 — файлы / сообщения | 7 / 11 | 7 / 11 | = |
| 1.5а | класс 9 — `WhoAmIResponse` | 6 | 6 | = |
| 1.6 | `owns-zero-accounts` | 11 | 11 | = |
| 1.6 | `bootstrapPersonalResources` (непробные) | 4 | 4 | = |
| 1.6 | зеркало «ворот ноль членств» | 0 | 0 | = |
| 1.7 | спеки со столбцом «Аккаунт» | 4 | 3 | ≠ (названо кругом 3) |
| 1.8 | `fkText` — имён / `users_account_fk` | 9 / 0 | 9 / 0 | = |
| 1.8 | `case "` по всему файлу (число-ловушка) | 32 | **35** | **≠** |
| 4 | старший занятый номер миграции | 0096 | **893002** | **≠** |

**Единица счёта — строка предиката, и без неё числа этого раздела спутать легко:**

| Величина | Сколько | Что означает |
|---|---:|---|
| строк-предикатов перемерено | **38** | весь §1 плюс предикат номера миграции §4 |
| строк, где значение изменилось | **8** | помечены `≠` в таблице выше |
| из них документ утверждал как **действующие** | **4** | §1.3 (перепись), класс 3, класс 6, §1.8 (число-ловушка) |
| из них уже были названы и обезврежены кругом 3 | **2** | §1.7 (спеки 4→3) и §4 (номер миграции — круг 3 заменил его предикатом, поэтому дрейф ожидаем и безвреден) |
| одна находка дала **две** строки | **2 пары** | §1.3 (перепись и её разбивка) и класс 6 (вхождения и вызовы) — это по одному факту, посчитанному в двух единицах |

**Три расхождения из четырёх вердикт круга 3 назвать не мог** — он нашёл два (классы 3 и 6).
Перепись §1.3 (13 → 14) не называл никто; она найдена только потому, что перемер был
**сплошным**, а не адресным. Это и есть довод §1.0а в одну строку: адресный перемер чинит
названное и оставляет класс.

**Число «8» без единицы было бы вдвое завышено, «4» — вдвое занижено**, и обе величины
истинны. Поэтому здесь стоят обе, с названным основанием каждой: одно и то же утверждение
даёт разные числа в разных единицах (§1.0 п. 3), и приводить одно молча — тот самый дефект,
который эта глава ловит у других.

### 12.2 Три класса, найденных кругом 3 и кругом 4 — предикат стареет не только по ревизии

| # | Класс | Признак | Противоядие |
|---|---|---|---|
| 1 | **предикат, ключующийся на ФОРМУ ЗАПИСИ** | число падает до нуля, а предмет жив: переписали синтаксис места вызова, не тронув смысла | ключеваться на **отношение / имя символа / текст отказа** — то, что переживает рефакторинг вызова; проверить на **всех** известных ревизиях |
| 2 | **предикат, ключующийся на ИМЯ ВЕТКИ** | ветка снята с origin — предикат либо не резолвится, либо молча отвечает про **другой** коммит, на который смотрит локальная отслеживающая ссылка | пиннить **хешем**: он переживает и снятие ветки, и её перезапись |
| 3 | **неполный клон делает `merge-base` лжецом** | `git log --diff-filter=A` и `git merge-base` отвечают уверенно и неверно: предок, отрезанный глубиной, неотличим от несуществующего | `git rev-parse --is-shallow-repository` → `false` **до** первого замера о происхождении |

Первый — самый дорогой из трёх, потому что даёт **ложный ноль**, а ноль читается как «предмета
нет» и закрывает работу. Второй и третий отвечают **числом**, и число правдоподобно.

Все три — родственники уже записанного класса «номер строки устаревает молча» (§11.2 п. 9):
во всех четырёх случаях предикат ключуется на свойство **текста**, а не **предмета**.

### 12.3 Влитое сверено против сценариев — расхождений по существу нет

S1 и часть S2 посажены до вердикта (§0.3). Сверка велась не «похоже ли», а по сценариям,
которые миграции цитируют сами: IAM-ID-1-08, 11, 13, 39, 40, 41, 50, 72.

| Что требовал документ | Что в стволе | |
|---|---|---|
| таблица членства с инвариантами **DB-конструкцией** (ban #10) | 2 CHECK (состояние из словаря, форма id), 2 FK, полный `UNIQUE(user_id, account_id)` | сходится |
| уникальность **полная**, а не частичная | `CREATE UNIQUE INDEX … ON kacho_iam.memberships (user_id, account_id)` без `WHERE` | сходится |
| бэкфилл взаимно однозначен (IAM-ID-1-11) | `INSERT … SELECT … FROM kacho_iam.users` + проба `MembershipBackfillIsBijective` | сходится |
| двойная запись на время S1 | триггер `membership_mirrors_user_row`, зеркалящий колонку | сходится |
| полный откат стадии | секция `-- +goose Down` + проба `MembershipExpandRollsBackWhole` | сходится |
| отзыв как **факт БД** (S2) | `CONSTRAINT TRIGGER … DEFERRABLE INITIALLY DEFERRED` | сходится |
| самовалидация домена принимает строку без принадлежности (IAM-ID-1-58) | **нет**: текст отказа на месте, предикат даёт 1 | **остаток S2** |

**Побочный эффект влитого, названный отдельно:** ключ `memberships_user_fk` вошёл в перепись
§1.3 — то есть глава «что уже верно в дереве» теперь описывает в том числе работу, которую
документ санкционирует. Число оставлено фактическим (14), разделение по происхождению — в
выноске §1.3, а не в числе.

### 12.4 Что круг 4 НЕ делал

- **не перемерял §3** — это утверждения о стенде (§1.0), они проверяются на стенде;
- **не трогал ни одного сценария по существу.** Правки коснулись только того, чем сценарий
  **проверяется**: IAM-ID-1-57 переведён с опоры на сборку на опору на исход `Check`, потому
  что производитель факта переехал из Go в SQL (§1.5а класс 6). Условия и утверждения
  сценариев не менялись;
- **не снимал мёртвую `AccountScopedStructuralFact`** — она не предмет этого перехода
  (§1.5а класс 6), и её уборка смешала бы два изменения;
- **не заводил гейт на ban #1**, хотя §0.3 показывает, что он держится вниманием. Предмет
  назван и **не обещан**: этот документ его не заказывает и своим APPROVED не покрывает;
- **не переписывал §10 и §11** — разборы прошлых кругов суть свидетельство о том, что было
  сделано, и правка их задним числом сделала бы прошлые вердикты непроверяемыми. Отменённое
  помечено на месте (§11.4), а не удалено.

---

## §13 Круг 6 — указание владельца по В-3, сужение области, отзыв санкции с S5

Круг 6, ре-ревью: `acceptance-reviewer`, 2026-08-23, **CHANGES REQUESTED с СУЖЕННОЙ
ОБЛАСТЬЮ**. Правки внесены автором 2026-08-23; ревизия замера — продукт `5533d21e`, воркспейс
`origin/main`.

> [!warning] Что произошло ПОСЛЕ этого ре-ревью и чего эта глава ещё не знала
> 2026-08-24 решением по `PRO-Robotech/kacho#1085` часть санкции круга 5 **отозвана**, и
> документ вернулся в `DRAFT` (шапка). Разбор ниже остаётся верным по существу — он о том,
> **что** сужается и почему; но формулировка «APPROVED снят не целиком» описывает намерение
> ре-ревью, а не состояние: носитель вердикта несёт **один** вердикт на документ и сегодня
> несёт `DRAFT`. Действующего вердикта нет ни у одной части.

**Почему сужение не трогает данные — и почему это не смягчение.** Указание владельца — про
**чтение** членства, а не про его хранение. Строка человека, таблица членств, глобальный ключ
идентичности и звено области из членства ему не противоречат ни в одной части и **уже
приземлены миграциями** (§4). Снимать вердикт с них значило бы переоткрыть работу, которая
верна и стоит в стволе, — то есть заплатить откатом за формальность, ровно как §0.3 разобрал
для S1/S2. Переоткрыто **только** то, чего ответ касается: публичная поверхность чтения
членств и стадия S5.

### 13.1 Сорок три места, и исход по каждому

Перечень **поимённый**, потому что число без перечня неотличимо от оценки. Единица счёта —
**место правки**: связный кусок текста, несущий отменённое указание; строка таблицы, абзац,
сценарий и выноска считаются по одному. Вердикт назвал **34**; расхождение разобрано ниже и
**не подогнано** — числа названы все три, с единицей у каждого.

**Пятнадцать сценариев:**

| Сценарий | Что несло | Исход |
|---|---|---|
| 01 | последнее `And` читало членства **названного** человека (`?userId=`) | переписан: два чтения по названным аккаунтам + свой список от лица самого человека |
| 21 | молчал о том, называет ли ответ аккаунт | дописано: фильтр перевыражается **внутри** сервиса, ответ аккаунтов не называет, поверхностного вопроса не заводит |
| **43-49** | вся консольная поверхность столбца членств | у **43-48** санкция отозвана — тексты и номера сохранены, производителя не заказывают (§9.3); **49 не отозван**: его предмет — порядок работ (`ui.md` правило 12), а фикс, который он стережёт, уехал в IAM-ID-2-22. *Круг 7: вопрос закрыт — вердикт круга 6 (вопрос 4) **перенёс** 49 вслед за фиксом; наблюдаемое взамен утверждает IAM-ID-1-55* |
| 55 | «оба места спрашивают то же **списком членств**» — форма не названа | уточнено: списком членств **названного аккаунта**, то есть второй законной формой |
| 63 | malformed-идентификатор только у одиночного чтения | дописаны обе формы; отдельно сказано, что у своего списка поверхности malformed-идентификатора нет **by construction** |
| 64 | полосы резолва только у создания и одиночного чтения | дописано аккаунт-скоупное чтение; добавлено требование hide-existence, иначе перебор аккаунтов восстанавливает запрет с другой стороны |
| 66 | пагинация проверялась у одного глагола | дописаны обе формы, у каждой своё замыкание в своей функции |
| 67 | «список фильтруется по правам» — **односторонне** | переписан: обе половины директивы владельца в одном сценарии (третьему не видно · приглашённому видно) |
| 68 | каталог прав «для каждого нового RPC» без области | дописано: область у своего списка — **сам вызывающий**, не кластер; записи с чужим идентификатором пользователя как областью в каталоге нет |

**Не-сценарные места — 28.** Столбец «назв.» говорит, назвал ли это место вердикт: без него
«нашёл больше, чем просили» неотличимо от «переписал что попало».

| # | Место | Что несло | Исход | назв. |
|---|---|---|---|:---:|
| 1 | шапка, строка вердикта | APPROVED без оговорок | `DRAFT`: вердикта нет, часть санкции круга 5 отозвана; таблица суженной области первым делом | да |
| 2 | шапка, «Разрешено: S3, S4, S5» | разрешала стадию, с которой санкция снята | **S3 и S4**; сказано, что с S5 санкция отозвана и куда ушёл предмет | да |
| 3 | шапка, «частью уже в стволе» | называла только S1 и S2 | дописаны S3 и S4 с предикатами | нет |
| 4 | шапка, состояние §1 | молчала о том, что §1 не перебазирован | §1.0а исполнена дословно: сказано вслух, названо перемеренное | нет |
| 5 | §0.2 | разрешала S3, S4, S5 | суженная область **первым делом**, затем разрешение на S3 и S4 | да |
| 6 | §0.3, «S3, S4 и S5 кодируются только после APPROVED» | называла снятую стадию | S3 и S4 | да |
| 7 | §0.3, предикат «восемь сценариев» | предикат **шире** утверждения: считал каталог, утверждал про два файла | сужен до двух файлов, значение **3**; дрейф 8 → 16 назван | нет |
| 8 | §1.5а, строка класса 9 | «разойдётся с ресурсом членства» | «два написания одного факта»; источник назван авторитетно (`ListAccountsForUser`) | да |
| 9 | §1.5а, выноска класса 9 | требовала **совпадения** составов — неисполнимо | один производитель, две проекции, разность названа, включение по членской части, положительный контроль обязателен | да |
| 10 | §1.5а, таблица «класс → стадия» | класса 9 в ней **не было** | заведена строка: S3, IAM-ID-1-73 | да |
| 11 | §1.5а, та же таблица | класса **8** в ней тоже не было | сказано: предмет — перепись, сценария не несёт **намеренно** | нет |
| 12 | §1.5а, «ни один из семи не предмет `kacho#473`» | отсылала к стадии S5 | предмет `#473` закрыт решением «столбец снят»; поверхность — IAM-ID-2 | да |
| 13 | §1.5а, куда переезжают классы 1 и 2 | молчало | сказано: на аккаунт-скоупную форму; запрещённая им не нужна | нет |
| 14-16 | §1.7, три абзаца (предмет S5 · радиус · форма «первый + тег») | стояли на опровергнутом прочтении дерева | сняты; на их месте — что дерево обещает **дословно**, с цитатами и предикатом | да |
| 17-21 | §4, пять строк S5 (таблица стадий · состояние · соответствие шагам · порядок · production-completeness) | описывали снятую стадию | помечены снятыми; довод «S5 после S4» сохранён дословно и отправлен вместе с предметом | да |
| 22 | §4, состояние S3 | «**НЕ НАЧАТА**» — ложно | **исполнена частью**: `944001` в стволе; нет модели прав, контракта членства, перевода консоли | да¹ |
| 23 | §4, состояние S4 | «**НЕ НАЧАТА**» — ложно | **исполнена частью**: глобальный ключ, активация всех членств, `reserved` на контракте; колонка жива | да¹ |
| 24 | §4, «вхождений `account_id` — 151» | число без ревизии, дрейфнуло до **154** | обе величины названы со своими ревизиями | нет |
| 25 | §7 В-3 | умолчание «возвращать», обоснованное **ссылкой на дерево** | вопрос закрыт указанием владельца; прежнее умолчание разобрано с цитатами дерева; назван недосчёт «44-48» вместо «43-48» — и перемером 2026-08-24 назван встречный перебор круга 6 («43-49», семь) | да |
| 26 | §8 DoD п. 1 | «все 70 сценариев», сквозная проба браузером у группы F | **71** объявлено, **62** действующих (девять с отозванной санкцией); проба браузером остаётся заказанной одним сценарием — 49 | да |
| 27 | §8 DoD п. 2 | «связывает **S3, S4, S5**» — четвёртое вхождение | S3 и S4; дописано, что признание §0.3 предъявляется и к посаженному после круга 5 | да² |
| 28 | §9.1 · §9.2 · §9.3 | распределяли и считали снятые номера | S5 и `kacho#473` помечены как **отозванные по санкции** (номера в обращении); 73 назначен `#471` и S3; счёт **71 / 62**; предикат полноты `seq 1 73` без 9, 10 + контроль + поимённый перечень отозванных | да |

¹ вердикт назвал предмет («§4 разошёлся с деревом») и перечислил приземлённое, но какая именно
строка ложна и чем это меряется — устанавливалось замером здесь.
² вердикт отнёс это вхождение к §0.3; оно стоит в §8, DoD п. 2. Нашлось грепом по строке, а не
чтением подряд — предикат оказался надёжнее адреса.

> [!warning] Число вердикта — **34**; моя перепись даёт **43**, и подгонять я не стал
> Вердикт круга 6 назвал 34 места, разложив их как «15 сценарных + 19 не-сценарных».
>
> **Сценарная половина сходится ТОЧНО:** 01, 21, 43-49 (семь), 55, 63, 64, 66, 67, 68 — ровно
> **15**. Единица здесь — **место правки**, а не «отозванный сценарий»: 49 в этот перечень
> входит потому, что его текст ссылался на правку столбца и был тронут, а **не** потому, что
> его отозвали, — отзыву он не подлежит (§13.1, IAM-ID-2 §2.6). Смешение этих двух единиц и
> дало кругу 6 «семь отозванных» там, где отозвано шесть.
>
> **Не-сценарная не сходится ни с 19, ни сама с собой.** Перечисление вердикта («4 вхождения
> „S3, S4, S5“ · §1.5а класс 9 — 3 · §1.7 — 3 · §4 — 5 · §7 В-3 — 3 · §8 DoD п. 1 · §9.1 ·
> §9.2 · §9.3») даёт в сумме **22**, а не 19: сложить его строки и получить 19 нельзя ни при
> какой группировке, которую я смог построить. Моя таблица даёт **28** — те же 22 плюс шесть
> мест со столбцом «нет».
>
> **Единица счёта названа, потому что без неё спорить не о чем:** место = связный кусок текста,
> несущий отменённое указание; строка таблицы, абзац, сценарий и выноска считаются по одному.
> В этой единице всего **43** (15 + 28).
>
> **Почему я не подогнал.** Совпасть с 34 можно было, объявив отзыв 43-48 «одним указанием», а
> §4 — «одной строкой»: число сошлось бы, ничего не измерив. Приёмка, чьи числа сходятся
> подбором единицы, теряет ровно то свойство, ради которого §1.0 требует предиката — величину
> становится нечем опровергнуть.
>
> **Третье истинное число, в единице «применённая замена»:** анкерованных замен — **45**, плюс
> одно блочное снятие группы F и одна дописанная глава. Оно больше 43, потому что часть мест
> правилась двумя заходами (место 28 — шестью), а часть правок — **дописывания**, а не замены.
>
> **Что заведено кругом 6** (в счёт мест не входит — это не правка отменённого, а новое):
> сценарий **IAM-ID-1-73** (производитель класса 9, у которого не было ни стадии, ни пробы) ·
> пункт **0 в §5.0** (две формы чтения как предусловие всей главы) · эта глава.

### 13.2 Что круг 6 перемерил — и что НЕ мерил

**Перемерено на `5533d21e` (ствол продукта 2026-08-23), с контролем в обратную сторону:**

| Что | Значение | Прежде |
|---|---|---|
| столбец «Аккаунт» у спеки `users` (`spec` §1.7) | **0** | 0 на `43d332ac` — не изменилось |
| места в консоли, обещающие `#1085` | **4** | не мерено |
| источники перечня аккаунтов в снимке прав (`ListAccountsForUser`) | **3** (членство+личность · владение · привязка), `PENDING` — **0** | не мерено |
| миграции S3 в стволе | `944001` — есть | «S3 НЕ НАЧАТА» |
| миграции S4-expand в стволе | `20260823050000`, `20260823053000` — есть | «S4 НЕ НАЧАТА» |
| `reserved` в `user.proto` | **2** | «`reserved` на поле 6 не объявлен» |
| объявлений `account_id` в контракте iam | **51** | 52 на `43d332ac` |
| `DROP COLUMN account_id` у `users` | **0** — колонка жива | так же |
| типы членства в модели прав | **0** | 0 — не изменилось |
| `listUsers({ account_id })` в консоли | **2** | 2 — не изменилось |
| сценарии, цитируемые двумя миграциями S1/S2 | **3** (08, 13, 50) | «восемь» — предикат был шире утверждения |

**НЕ мерено кругом 6, и это названо, а не подразумевается:**

- **§1 целиком** — перебазировка на текущий ствол не делалась; сказано вслух в шапке, как
  требует §1.0а. Признак нарушения нормы исполняется прямо сейчас и назван там же;
- **§3** — утверждения о стенде, они проверяются на стенде (§1.0), а не предикатом;
- **§6** — границы охвата; их предикаты круг 6 не прогонял;
- **§11, §12** — разборы прошлых кругов. Они суть свидетельство о том, что было сделано;
  правка их задним числом сделала бы прошлые вердикты непроверяемыми. Где утверждение прошлого
  круга сегодня неверно, это сказано **на месте нового текста** (§1.7, §7 В-3), а не подчисткой
  старого.

### 13.3 Что круг 6 НЕ делал

- **не трогал миграции** (ban #5) — и не требовалось: поверхности чтения членств в дереве нет
  **ни в одной форме**, поэтому правится непостроенное. Предикат:
  `git ls-tree -r --name-only "$R" proto/kacho/cloud/iam/v1/ | grep -ci member` → **0**;
- **не объявлял ни один сценарий «исполненным в прежней редакции с оговоркой».** Такая
  развилка обсуждалась и оказалась мнимой: ни один из переписанных сценариев в дереве не
  реализован — реализовывать было нечего;
- **не трогал приёмку IAM-ID-2** — её предмет её собственный. Спор о столбце разрешён в её
  пользу **по предмету**, а не по дате (§13.1, выноска группы F). *Перемерено 2026-08-24: на
  момент круга 6 она «писалась параллельно» и координатой здесь не называлась; сегодня она
  **в дереве** — `docs/specs/sub-phase-IAM-ID-2-membership-read-acceptance.md`, круг 2, — и
  ссылки на неё в этом документе даны координатой и номерами сценариев;*
- **не заводил гейт на ban #1** — предмет назван кругом 4 и по-прежнему не обещан здесь;
- **не перебазировал §1** — работа следующего круга, названа предметом (§13.2);
- **не правил ведомость вердиктов в репозитории продукта** — она там, документ здесь, и это
  **разные изменения**. Но расхождение заведено уже сейчас и обязано быть названо, иначе его
  найдёт гейт, а не человек.

> [!warning] Ведомость `docs/acceptance-ledger.yaml` продукта РАСХОДИТСЯ с этим документом
> прямо сейчас — предмет для отдельного изменения
> Шапка этого документа сама называет ведомость механизмом переноса вердикта в продукт («гейт
> продукта прочитать вердикт не может по построению»). Значит расхождение между ними — не
> мелочь оформления, а два места об одном предмете, из которых верно одно.
>
> **Что в ведомости стоит сегодня** (замер на `origin/main` продукта — `7bb0a6e14`, 2026-08-24;
> в релизной линии она уже приведена, см. развязку ниже):
> `verdict: APPROVED`, `verdict_dated: "2026-08-21"`, ревизия воркспейса `5d6a2d6`, примечание
> «Разрешены стадии S3-S5». **Все три утверждения устарели**: действующего вердикта нет
> (`DRAFT`), дата отзыва — 2026-08-24, а по существу не пересматриваются лишь данные S3 и S4.
>
> **Предикат расхождения:**
> ```sh
> # в дереве продукта
> git show origin/main:docs/acceptance-ledger.yaml \
>   | awk '/IAM-ID-1-identity-account-decoupling/{f=1} f{print} f&&/^$/{exit}'
> # в дереве воркспейса — вердикт из шапки самого документа
> sed -n '1,6p' docs/specs/sub-phase-IAM-ID-1-identity-account-decoupling-acceptance.md
> ```
> Значения не совпадают — это находка, а не состояние.
>
> **Чем это грозит, если не поправить, — и цена НЕсимметрична.** Ведомость с `APPROVED`
> пропустит миграцию, цитирующую **любой** сценарий этого документа, включая переоткрытые.
> Поправленная на `CHANGES REQUESTED` — заблокирует и ту работу, что по существу не
> пересматривается (данные S3/S4). Ни одно из двух значений не выражает **суженной** области:
> ведомость несёт один вердикт на документ, а сужение — раздельное.
>
> **Это ограничение самой ведомости, а не ошибка её автора.** Названо здесь, потому что первый
> же, кто пойдёт править запись, упрётся в него и будет выбирать между двумя неверными
> значениями.
>
> > [!note] Развязка — перемерено 2026-08-24: выбран ТРЕТИЙ вариант, и он уже применён в линии
> > Запись приведена **не** к `CHANGES REQUESTED`, а к `verdict: DRAFT` с полями
> > `verdict_withdrawn: true` и `debt_issue: 1085` — то есть ведомость говорит «санкции нет и
> > вот чей это долг», не притворяясь, что несёт область. Сделано отдельной работой, видно на
> > ссылке продукта `release/identity-4` (`e315e43f5`); на `origin/main` продукта
> > (`7bb0a6e14`) запись **всё ещё** `APPROVED @ 5d6a2d6`. Предикат — в шапке этого документа.
> >
> > Абзацы выше не удалены: они назвали ограничение **до** того, как развязка нашлась, и
> > именно поэтому её искали. Удалив разбор, следующий читатель увидел бы только решение и
> > принял бы его за очевидное.

---

## §14 Круг 7 — что сделано по вердикту круга 6

Круг 7, правка автора: 2026-08-24, по вердикту `acceptance-reviewer` (шапка). **Вердикт
кругом 7 не ставится и не снимается** — его ставит только `acceptance-reviewer`; шапка
остаётся `CHANGES REQUESTED` до ре-ревью.

**Ревизии замера этого круга** (все числа ниже относятся к ним и ни к чему больше): продукт —
`release/identity-4` @ `8a3e00f18`; воркспейс — `acc/iam-id-1-b3` @ `42af441`. Числа круга 6
снимались на продукте `e315e43f5`; там, где значение изменилось, это сказано на месте.

**Все числа §14.5 перемерены ВТОРОЙ раз на `e4da590cf`** — за время правки накопительная ветка
волны ушла вперёд на **3** коммита, и ни одно значение не изменилось. Второй замер сделан не из
педантизма: соседнее дерево движется, а вердикт о дереве устаревает за минуты (`git rev-list
--count 8a3e00f18..e4da590cf` → 3; ни один из трёх не трогает путей замера — предикат
`git log --oneline 8a3e00f18..HEAD -- <пути §14.5>` даёт пусто).

### 14.1 Семь блокирующих — что сделано по каждому

| # | Предмет | Что сделано |
|---|---|---|
| **Б1** | четыре действующих сценария неисполнимы средствами документа | **внешнее предусловие объявлено явно** в трёх местах: §0.2 (таблица «сценарий → производитель»), §4 строка S3 и §4 «что делает стадию production-complete», **DoD п. 16** (новый) с машинным предикатом. §9.1 приведена к нему: строка S3 больше **не** обещает формы чтения здесь, четыре потребителя помечены `*`. Сценарии «в обход» IAM-ID-2 **не** перевыражены — вердикт этого не требовал |
| **Б2** | префикс `mbr` назван один раз, внутри отозванного сценария | заведён **§2.1а** — норма, признак, предикат с контролем в обе стороны, прецедент из дерева; приписка `63` перенаправлена туда; **предусловием названо в `64` и `65`** своими `Given`; регистрация стоит шагом **S3.1** (§2.3а, §4) |
| **Б3** | DoD п. 13 и §2.3а требуют гейта, у которого нет предмета | §2.3а: абзац «Применение» переписан таблицей «было объявлено → предмет → взамен»; DoD п. 13: требование заменено на `make -C deploy fga-model-embed` + две пробы дрейфа вшитой копии, с выноской о замере |
| **Б4** | `64` назначает внутрисервисной ссылке чужую полосу | сценарий приведён к решению дерева: `NOT_FOUND` / `RESOURCE_NOT_FOUND`, добавлен **отрицательный контроль полосы** (`PEER_*` в непробном Go iam — 0) с контролем в обратную сторону; выноска называет решение, его координату на **пути записи** и цену закрепления неверной полосы |
| **Б5** | порядок S3 внесён вердиктом | §2.3а: предупреждение снято **как исполненное**, назначенный порядок S3.1 → S3.2 → S3.3 внесён таблицей; §4 строка S3, §4 «почему порядок именно такой», §9.1 строка S3 — приведены |
| **Б6** | счёт смешивает «санкция отозвана» с «остаётся предметом здесь» | состояния разведены: остаток `66` (порядок «формат раньше замыкания» у `ListUsers`) **вынесен в действующий IAM-ID-1-21** своими утверждениями; остаток `67` (требование к паре) **снят со ссылкой на держателя** — IAM-ID-2-03/-13/-15 и -08/-21; группа F больше **не** ссылается на `67` как на живого производителя |
| **Б7** | три места §1/§2.3а разошлись с деревом | §2.3а «все четыре глагола» → **три**, с предикатом и контролем; §1.5а класс 6 «4 типа» → **три**, и `iam.user` из них **ушёл**; звено цепи областей `kacho_iam.resource_scope_edge` **названо** — в §2.3а (адрес 2), в строке класса 6 и предикатом 6c |

### 14.2 Три важных

| # | Предмет | Что сделано |
|---|---|---|
| **В1** | `55` держался предикатом «вызова нет», а не наблюдаемым | `Then` переписан: сквозная проба браузером видит состав **поимённо** на обеих страницах, отрицание — в паре с положительным в **том же прогоне**, устойчивость к переименованию класса, `// verifies` внутри пробы, `retries: 0`, ожидание условия. Предикат «`listUsers({account_id})` → 0» оставлен **сопутствующим** утверждением о дереве, а не способом снятия |
| **В2** | §0.2 «Разрешает: S3, S4, S5» | сужено до **S3 и S4**; выноска называет, почему абзац не переписывался раньше (сужение — предмет вердикта, ban #1) и что вердикт состоялся |
| **В3** | §4, строка S5 в таблице соответствия шагам | снята вместе со стадией — во **всех четырёх** таблицах §4 (стадии · фактическое состояние · соответствие Ш0-Ш10 · production-completeness), с записью, почему строка не удалена целиком |

### 14.3 Решения вердикта, внесённые как решённые

- **порядок внутри S3** — S3.1 (одним изменением) → S3.2 → S3.3; средний элемент прежней
  гипотезы («публичная поверхность членства») **отвергнут**: он называет отозванное;
- **стадия S5 снята как стадия**; сценарии 43-48 остаются текстом, номера в обращении;
- **сценарий 49 перенесён** в IAM-ID-2 (к IAM-ID-2-22); наблюдаемое взамен утверждает
  **IAM-ID-1-55**.

### 14.4 Что изменилось в арифметике — и почему это не правка принятого

Вердикт принял `71 / 9 / 62` и метод, которым числа получены. **Метод не тронут**: те же
анкеры, те же контроли в обе стороны, та же граница `seq 1 73`. Изменилось **третье
слагаемое**, и изменил его сам вердикт, перенеся сценарий 49:

```
71 объявлено − 9 с отозванной санкцией − 1 перенесённый = 61 действующий
```

**Перенос — третье состояние, и смешать его с отзывом было бы ложью о предмете**: у
отозванного санкция снята потому, что реализовывать **нельзя**; у 49 предмет верен и
**реализуется** — в другом документе, другим номером. Разбор и предикаты — §9.3.

### 14.5 Перемерено кругом 7 — числа с предикатами

| Что | Значение | Прежде | Предикат |
|---|---|---|---|
| глаголы типа `iam_user`, объединённые с `super_admin` | **3** (`v_get`, `v_list`, `v_delete`) | «все четыре» | блок типа в `fga_model.fga`, `grep -cE '^ *define v_[a-z]+:.*super_admin'`; контроль — **все** глаголы типа дают те же 3 |
| типы, выводящие предка выражением `parentAccountExpr: "o.account_id"` | **3** (`iam.project`, `iam.group`, `iam.serviceAccount`) | 4 (с `iam.user`) | `grep -c` по `reconcile_adapter.go`; контроль — та же форма с несуществующим выражением даёт 0 |
| чем `iam.user` выводит предка сегодня | `parentAccountsExpr` над `kacho_iam.memberships` | колонка строки | `grep -A3 'objectType: "iam.user"' \| grep -c 'kacho_iam.memberships'` → 1 |
| миграции, называющие звено цепи областей | **4** `.sql` (740001, 781001, 785001, **944001**) | не мерено | `git grep -l … \| grep -c '\.sql$'`; единица — **файл миграции**, проба рядом исключена |
| префикс `mbr` в каталоге дефис-префиксов | **0** | не мерено здесь | `grep -c '"mbr"' pkg/ids/ids.go`; контроль — `PrefixLimitHyphen` в том же каталоге даёт **3** |
| конфигмапы модели прав в чартах | **0** | «гейт дрейфа обязан остаться зелёным» | `git ls-files deploy/ \| grep -ci -E 'fga\|authz.*model'` |
| модель вшита в бинарь | **1** | не мерено | `git grep -n 'go:embed fga_model.fga' -- services/iam` |
| токены `PEER_*` в непробном Go `services/iam` | **0** | «полоса чужого ресурса» | `git grep -nE 'PEER_[A-Z]' -- services/iam` вне проб; контроль — по дереву их эмитят `compute`, `nlb`, `registry`, `vpc` (потребители `geo`) |
| объявлено · отозвано · перенесено · действующих | **71 · 9 · 1 · 61** | 71 · 9 · — · 62 | §9.3, три анкера, пересечение множеств пусто (и контроль, что `comm` умеет находить пересечение) |

> [!note] Число `PEER_*`-эмитентов по дереву у круга 7 и круга 6 РАЗОШЛОСЬ — и оба верны
> Вердикт назвал `nlb`, `registry`, `storage`; круг 7 на `8a3e00f18` получил `compute`,
> `nlb`, `registry`, `vpc`. Ревизии разные, и предикаты разные (вердикт мерил по имени
> токена, круг 7 — по конструктору полосы). **Подгонять не стал**: здесь стоит число с
> названным предикатом и названной ревизией, его можно повторить и опровергнуть. Вывод от
> расхождения не зависит — обе переписи дают **ноль** у `iam`, а это и есть предмет Б4.

### 14.6 Что круг 7 НЕ делал

- **не ставил и не снимал вердикт** — шапка остаётся `CHANGES REQUESTED` (ban #1);
- **не перебазировал §1** — вердикт этого не требовал, и требование сохраняется предметом
  следующего круга (§13.2). Тронуты **только** места, названные Б7, плюс два, где то же
  число стояло второй раз (§2.4 п. 6 и сноска сценария 57): оставить их значило бы завести
  два места об одном предмете, из которых верно одно;
- **не трогал §3, §6, §11, §12** — вердикт исключил их прямо;
- **не трогал §5.0, §0.3, §9.2 сверх строки `kacho#473`, таблицу фактического состояния
  стадий** — вердикт назвал их принятыми;
- **не переписывал арифметику §9.3 как метод** — только третье слагаемое, и это следствие
  решения самого вердикта (§14.4);
- **не правил ведомость `docs/acceptance-ledger.yaml` продукта** — она в другом репозитории
  и это другое изменение; расхождение по-прежнему названо в §13.3;
- **не заводил новых номеров сценариев** — арифметика объявленного (`71`) и предикат
  полноты (`1..73` без `9,10`) не тронуты; остаток `66` принят **существующим** сценарием 21.

### 14.7 Что остаётся кругу 8

1. **перебазировка §1 на текущий ствол** — норма §1.0а требует её перед подачей на вердикт;
   круг 6 её не делал и назвал это вслух, круг 7 тронул только три места Б7. **47 предикатов
   §1 мерены на `43d332ac`** и с тех пор не перемерялись;
2. **вердикт по существу** — его ставит `acceptance-reviewer`, не автор;
3. **вердикт IAM-ID-2** — от него зависит закрытие S3 (DoD п. 16). Это не работа этого
   документа, но его предикат снятия;
4. **замечание к IAM-ID-2, адресованное кругом 7**: требование, что обе половины указания
   владельца утверждаются **вместе**, ни один из её производителей по отдельности не несёт
   (снято отсюда вместе с остатком `67`, §5 группа A2).

---

## §15 Круг 9 — перебазировка §1 целиком, четыре блокирующих круга 8, пять важных

Ревизии замера: продукт — `release/identity-4` @ **`475f8527`** (накопительная ветка волны
линии); воркспейс — `acc/iam-id-1-b3` @ `b472689`. Числа ниже относятся к ним и ни к чему
больше. Дерево двигалось во время работы: между ревизией вердикта круга 8 (`e4da590cf`) и
моей — **4** коммита, поэтому ревизия названа явно у каждой величины, а не подразумевается.

> [!note] Проверка устойчивости: несущие числа пересняты на вершине, ушедшей вперёд на 6 коммитов
> Пока шла правка, ветка волны уехала на `03d8ce07a`. Одиннадцать несущих величин перемерены
> там повторно и **все одиннадцать совпали** с `475f8527`: полоса `List` (2 и 0),
> `from_request_field` (2), снятое поле (0), посаженный глагол (1), `ListAccountsForUser`
> (29 и 8), писатели кортежа (5), класс 5 (5), отсутствие скрипта, число-ловушка (41).
>
> Это **не** отменяет нормы §1.0а и не продлевает срок годности главы: совпадение
> одиннадцати чисел на шести коммитах есть свидетельство, а не гарантия — ровно те же
> одиннадцать разошлись бы на другой шестёрке. Пересъём назван затем, чтобы следующий круг
> знал: расхождение, которое он найдёт, накопилось **после** `03d8ce07a`, а не осталось
> непроверенным здесь.

### 15.1 Перебазировка §1 — перемерены ВСЕ 70 объявленных чисел, разошлось 12

Норма §1.0а требует перечня **всех** предикатов главы с обеими величинами, а не только
разошедшихся: перечень одних находок неотличим от перечня, в котором остального не смотрели.
Единица — **объявленное число** (одна исполняемая команда с приведённым рядом значением).

Столбец «исход»: `=` не изменилось · `≠` изменило значение · `✗` было неверно **на обеих**
ревизиях · `+` найдено кругом 9 сверх перечня вердикта круга 8 · `новое` — предиката прежде
не было.

| # | Раздел | Что меряет | `43d332ac` | `475f8527` | Исход |
|---:|---|---|---:|---:|:--:|
| 1 | 1.1 | `account_id text NOT NULL` в `0001` | 4 | 4 | = |
| 2 | 1.1 | три ключа как `CREATE UNIQUE INDEX` | 3 | 3 | = |
| 3 | 1.1 | `accountId is immutable after User.Create` | 2 | 2 | = |
| 4 | 1.1 | контроль: `inviteStatus is immutable …` | 0 | 0 | = |
| 5 | 1.1 | полож. контроль: `externalId is immutable …` | **2** заявлено / 3 в дереве | 3 | ✗ |
| 6 | 1.2 | `RESTRICT` у каждого из двух ключей цикла | 1 · 1 | 1 · 1 | = |
| 7 | 1.2 | контроль: тот же счётчик на `CASCADE` | 0 | 0 | = |
| 8 | 1.3 | всего `REFERENCES kacho_iam.users (` | 14 | 14 | = |
| 9 | 1.3 | из них `RESTRICT` | 8 | 8 | = |
| 10 | 1.3 | из них `CASCADE` | 5 | 5 | = |
| 11 | 1.3 | из них `SET NULL` | 1 | 1 | = |
| 12 | 1.3 | живых `RESTRICT` — теперь ПОИМЁННО, склейкой многострочных | 4 (прозой) | 4 (командой) | новое |
| 13 | 1.3 | контроль того же предиката на `CASCADE` | — | 3 | новое |
| 14 | 1.4 | `resolveCanonicalSubjectID` | 5 | 5 | = |
| 15 | 1.4 | слитое условие `err != nil \|\| len(actives) == 0` | 1 | 1 | = |
| 16 | 1.4 | контроль: все тесты `len(actives)` | 1 | 1 | = |
| 17 | 1.4 | `ListAccountsForUser`, все вхождения | 20 | **29** | ≠ |
| 18 | 1.4 | `ListAccountsForUser`, непробные | 6 | **8** | ≠ |
| 19 | 1.5 | `string account_id = 6` в `user.proto` | 1 | **0** | ≠ |
| 20 | 1.5 | `msg InviteUserRequest` → `account_id` | 1 | 1 | = |
| 21 | 1.5 | `msg ListUsersRequest` → `account_id = 4` | 1 | 1 | = |
| 22 | 1.5 | `from_request_field: "account_id"` | 1 | **2** | ≠ + |
| 23 | 1.5 | контроль: `msg GetUserRequest` → `account_id` | 0 | 0 | = |
| 24 | 1.5 | `blk Invite` — три опции гейта | 3 | 3 | = |
| 25 | 1.5 | контроль: `blk List` — те же три опции | 0 | 0 | = |
| 26 | 1.5 | `blk List` → `"<exempt>"` | 1 | **0** | ≠ |
| 27 | 1.5а | пол типизированных: `u\|user\|usr .AccountID` | **16** заявлено / 15 в дереве | 15 | ✗ |
| 28 | 1.5а·1 | консоль: `listUsers({` c `account_id` | 2 | 2 | = |
| 29 | 1.5а·2 | провайдер: файлов `kacho_iam_user_invitation` | 4 | 4 | = |
| 30 | 1.5а·3 | скрипт: отношение `account` на `iam_user` | 1 | **0** | ≠ |
| 31 | 1.5а·3 | контроль: то же отношение на `project` | 1 | **0** | ≠ |
| 32 | 1.5а·3 | зеркало: членство в `ui-future/` | 0 | 0 | = |
| 33 | 1.5а·4 | `kacho_account_id` пишется | 4 | 4 | = |
| 34 | 1.5а·4 | `kacho_active_account` пишется | 5 | 5 | = |
| 35 | 1.5а·4' | `getString(enriched, "kacho_account_id")` | 0 | 0 | = |
| 36 | 1.5а·4' | `getString(enriched, "kacho_active_account")` | 1 | 1 | = |
| 37 | 1.5а·5 | `kacho_account_id` в кейсе пробы | 2 | **5** | ≠ |
| 38 | 1.5а·6a | `AccountScopedStructuralFact`, непробные | 4 | 4 | = |
| 39 | 1.5а·6a | её вызовов | 0 | 0 | = |
| 40 | 1.5а·6a | контроль на `554a8d49` (вызовы были) | 4 | 4 | = |
| 41 | 1.5а·6b | `parentAccountExpr: "o.account_id"` | 3 | 3 | = |
| 42 | 1.5а·6b | перечень типов — предикат называет сам | 3 имени | те же 3 | = |
| 43 | 1.5а·6b | `iam.user` выводит предка через `memberships` | 1 | 1 | = |
| 44 | 1.5а·6b | контроль: `parentAccountExpr: "o.membership_id"` | 0 | 0 | = |
| 45 | 1.5а·6c | миграция `944001` существует | есть | есть | = |
| 46 | 1.5а·6c | `resource_scope_edge` в ней | ≥1 | ≥1 | = |
| 47 | 1.5а·6c | файлов миграций с `resource_scope_edge` | 4 | 4 | = |
| 48 | 1.5а·7 | `Illegal argument account_id: required` в `domain/user.go` | 1 | 1 | = |
| 49 | 1.5а·7 | контроль: тот же текст в `domain/account.go` | 0 (прозой) | 0 (командой) | = |
| 50 | 1.5а·8 | файлов «Read by exact field name `account_id`» | 7 | 7 | = |
| 51 | 1.5а·8 | сообщений (сумма по файлам) | 11 | 11 | = |
| 52 | 1.5а·9 | `WhoAmIResponse`, непробные | 6 | 6 | = |
| 53 | 1.5а·9' | источник «членство + активная личность» | 1 | 1 | = |
| 54 | 1.5а·9' | источник «владение» | 1 | 1 | = |
| 55 | 1.5а·9' | источник «привязка уровня аккаунта» | 1 | 1 | = |
| 56 | 1.5а·9' | контроль: `PENDING` в том же выражении | 0 | 0 | = |
| 57 | 1.6 | `owns-zero-accounts` | 11 | 11 | = |
| 58 | 1.6 | `bootstrapPersonalResources`, непробные | 4 | 4 | = |
| 59 | 1.6 | зеркало: ворота «ноль членств» | 0 | 0 | = |
| 60 | 1.7 | спек со столбцом «Аккаунт» | 3 | 3 | = |
| 61 | 1.7 | `users` среди них | 0 | 0 | = |
| 62 | 1.7 | контроль: `spec 554a8d49 \| grep -c users` | 1 | 1 | = |
| 63 | 1.7 | `1085` в `ui-future/` | 4 | 4 | = |
| 64 | 1.8 | `serverTerm` в реестре ресурсов | 1 | 1 | = |
| 65 | 1.8 | `fkText`: имён ограничений | 9 | 9 | = |
| 66 | 1.8 | `fkText`: `users_account_fk` среди них | 0 | 0 | = |
| 67 | 1.8 | `case "` по всему файлу — **число-ловушка** | 35 | **41** | ≠ |
| 68 | 1.8 | `Len(t, pendings, 2` | 0 | 0 | = |
| 69 | 1.8 | `SecondAccount` в `tests/` | 0 | 0 | = |
| 70 | 1.8 | контроль: файлов `SecondAccount` в сервисе | 1 | **2** | ≠ + |

**Итого: 70 · не изменилось 58 · изменило значение 10 · неверно на обеих 2 · заведено новых 2.**
Четыре расхождения меняли работу исполнителя и разобраны вердиктом круга 8 как блокирующие
(строки 19, 26, 30-31 и отсутствие §1.9); остальные восемь — важные и косметические, каждое
внесено в свою строку главы вместе с прежним значением и ревизией.

> [!important] Три расхождения найдены СВЕРХ перечня вердикта — и одно из них глава мерила сама
> Помечены `+` в таблице.
>
> **Строка 22 — самая поучительная.** Предикат §1.5 строки 5 объявлял **1** и даёт **2**;
> второе вхождение есть блок `RemoveFromAccount` — тот самый посаженный глагол, отсутствие
> которого вердикт назвал блокирующим Б8.3. То есть факт лежал **внутри числа, которое глава
> уже мерила**, и разошёлся вместе с ним, никем не замеченный. Это довод не за
> внимательность, а за сплошной перемер: адресный чинит названное и оставляет класс.
>
> **Строка 27** — пол типизированных потребителей: **15** на обеих ревизиях при заявленных
> 16. Пол на то и пол, что дрейфует; неверно было **само число**, а не вывод из него.
>
> **Строка 70** — контроль §1.8: файлов стало **2**. Вывод сценария IAM-ID-1-02 не меняется,
> и это **проверено, а не предположено**: ни один из двух файлов не зовёт активацию
> (`UpsertFromIdentity` → 0 в обоих).

> [!warning] Одно место, где негодным оказался ПРЕДИКАТ, а не дерево — записано, чтобы
> следующий круг не «починил» дерево
> Проверяя §1.3 «живых `RESTRICT` четыре», круг 9 прогнал очевидное —
> `git grep -h "<ключ>" | grep -c RESTRICT` — и получил на
> `user_oauth_clients_created_by_fk` **0** при трёх остальных по **1**. Выглядело как
> находка: ключ, потерявший действие.
>
> Дерево не менялось. В `0046` имя ключа стоит на одной строке, а `ON DELETE RESTRICT` — на
> следующей; однострочный предикат их не видит. У трёх остальных объявление однострочное,
> поэтому промах пришёлся ровно на один ключ из четырёх.
>
> **Спасли соседи, а не внимательность.** Три одинаковых случая рядом сделали единичный ноль
> подозрительным; один такой ключ без соседей дал бы ложную находку, и чинили бы миграцию —
> то есть **применённую** миграцию, править которую нельзя (ban #5). Предикат заменён на
> склеивающий строки и называющий ключи поимённо (§1.3).

### 15.2 Четыре блокирующих круга 8 — исход по каждому

| # | Что требовал вердикт | Исход круга 9 |
|---|---|---|
| **Б8.1** | привести §1.5 п. 3 и выноску `21` к действующей полосе; предикат — к различающему | **приведено.** §1.5 п. 3 переписан: `permission = "iam.users.list"` + `scope_filtered = true`, сужение **на данных**; предикат заменён на `blk List \| grep -c 'scope_filtered'` → **2** при контроле `blk Invite` → **0** (прежняя пара давала 0 и 0 — не различала). Сказано, что перевыражение идёт **внутри** полосы, а не восстанавливает прежнюю; утверждения сценария не тронуты — он и прежде пиннил исход |
| **Б8.2** | класс 3 переписать как исполненный; строку §4 снять либо заменить; §2.3а привести | **закрыто исходом.** Скрипт удалён (`c78901a33`, работа этого же перехода), скриптов восстановления в дереве **ноль**. Класс 3 переписан на действующий предмет — **пять** писателей кортежа в прод-коде (было 4), предикат ключуется на пару «отношение + тип объекта», зеркало по членству обязано стать ненулевым. §4 требует перевода пятерых, а не правки скрипта; §2.3а закрыт исходом, требование к `kacho#471` снято. Мёртвая координата `heal-authz.sh:148-150` снята и из сценария `31` |
| **Б8.3** | назвать посаженный глагол; вынести решение об адресации; привести S3.1 и `Given` у `65` | **решено: приёмка ПРИЗНАЁТ посаженную адресацию.** Заведён §1.9 — шесть предикатов поверхности, адресация парой, триггер `472002` стадии S2, решение с ценой. Второго глагола снятия **не заводится** (область решения — аккаунт, пара называет её by construction; два глагола об одном предмете запрещены; ban #11). `65` перенацелен на `RemoveFromAccount`, предусловие §2.1а из его `Given` снято, взамен он пиннит неприкосновенность строки личности, работу в остальных аккаунтах, порядок с выдачами **исходом** и снятие указателя области. Перечень S3.1 сужен в обоих местах; §2.1а сохранил живого потребителя (`64`) |
| **Б8.4** | §1.5 строку 1 привести, исход назвать исполненным | **приведено.** Строка объявляет исход **исполненным**: `string account_id = 6` → **0**, `reserved 6` + `reserved "account_id"` → **2**. Расхождение с §4 снято — оба места теперь говорят одно |

### 15.3 Пять важных — что сделано

| # | Предмет | Сделано |
|---|---|---|
| 1 | класс 5: было 2, стало 5; утверждающих мест два | вхождения **5** в строке и в предикате; в таблице классов названы **оба** исходника (кейс и самопроверка форм), порождённая коллекция из счёта исключена с объяснением; сказано, что радиус планового красного на S4 **вдвое шире** |
| 2 | `ListAccountsForUser`: 20 → 29 и 6 → 8 | оба числа приведены с прежними значениями рядом; сказано, что это рост потребителей, а не смена смысла |
| 3 | §1.1 положительный контроль неверен на обеих ревизиях | число приведено к **3**, заведена выноска: ложным было **объяснение** числа, а контроль по существу остаётся годным (его работа — ненулевое значение) |
| 4 | §9.2 говорит `1..72` против `1..73` в §9.3 | приведено к `1..73` без выведенных `09`/`10` — два места об одном предикате сведены |
| 5 | скобка контроля в `64` приписана не той команде | названы **оба** предиката с их выводами: по токену — **пять** сервисов (включая `storage`), по конструктору полосы — **четыре**. Обе истинны, неверна была атрибуция; несущий ноль у `iam` верен у обоих |

### 15.4 Что круг 9 НЕ делал

- **не пересматривал принятое вердиктом** — семь блокирующих круга 6 и три важных, арифметика
  `71 / 9 / 1 / 61` и третье состояние, §2.1а как норма, внешнее предусловие (§0.2, DoD п. 16,
  §4 строка S3, §9.1), порядок `S3.1 → S3.2 → S3.3`, §5.0, §0.3, §3, §6, §11, §12, §13, коды
  и тон отказов, публикуемость;
- **не трогал историю вердиктов в шапке** — строка S3.1 вердикта круга 6 сохранена дословно,
  хотя её перечень работ сегодня сужен: вердикт есть свидетельство о назначенном, а не
  описание дерева. Сужение внесено в **действующие** места (§2.3а и §4), и это две разные
  сущности;
- **не заводил новых номеров сценариев** — перепись `71 · 9 · 1 · 61` после правки та же, что
  до неё; `65` перенацелен, а не заменён, и номер остаётся при своём предмете;
- **не правил §9.2 сверх одного числа**, названного вердиктом важным, — раздел принят;
- **не мерил на стенде**: всё выше — утверждения о **дереве** на названной ревизии. Утверждения
  о стенде живут в §3 и проверяются на стенде каждый раз заново (§1.0).

**Признак восстановимости прогнан по СВОЕМУ тексту, а не только по чужому** (`security.md`
§«Правило связывает и того, кто его применяет»). Разбор полосы `List` — единственное место
круга 9, где текст касается защиты, и он переписан **после** самопроверки: осталось то, что
связывает исполнителя (у `<exempt>` второй смысл, решение по нему принимается не по личности
вызывающего, значит возвращать его нельзя), и снято то, что складывалось в восстановимую
сборку. Предмет, решение и следствие для контракта названы; координата вместе с условием и
следствием для постороннего — нет. Остальные правки круга 9 говорят о собственных решениях,
каталогах и переписях.

### 15.5 Что остаётся кругу 10

1. **вердикт по существу** — его ставит `acceptance-reviewer`, не автор;
2. **перемер §1 перед следующей подачей** — норма §1.0а не истекает: между моей ревизией и
   вершиной ветки коммиты появляются в течение смены, и предикат нарушения тот же
   (`git rev-list --count 475f8527..<вершина>` не ноль);
3. **вердикт IAM-ID-2** — от него зависит закрытие S3 (DoD п. 16); у соседа сегодня `DRAFT`.
   Это не работа этого документа, но его предикат снятия;
4. **решение §1.9 адресовано и соседу — ПЕРЕПИСАНО кругом 11: условие было уже разрешимо,
   а владелец у решения один.** Здесь стояло условное «если IAM-ID-2 заказывает ресурс
   членства с полным набором стандартных методов, отсутствие `Delete` обязано быть названо
   **там же**». Условное «если» не связывает никого: пока оно не разрешено, замечание
   исполнимо и при бездействии. Разрешается оно **замером**, и замер сделан:

   ```sh
   N=docs/specs/sub-phase-IAM-ID-2-membership-read-acceptance.md
   grep -c 'RemoveFromAccount\|member_remover' "$N"   # 8 — предмет у соседа НАЗВАН
   grep -c 'DeleteMembership\|rpc Delete'      "$N"   # 0 — второго глагола он НЕ заказывает
   ```
   Восьмёрка — не украшение: сосед не молчит о снятии, а **судит** его тиром аккаунта
   (`| UserService/RemoveFromAccount | member_remover … | исключить человека из аккаунта вправе
   тир аккаунта |`), ссылаясь на посаженный глагол. Ноль — контроль в обратную сторону: если бы
   сосед заказывал `Delete`, предикат нашёл бы его тем же способом.

   Отсюда **действующая** форма замечания, и она о другом. Требовать «названо там же» значит
   приглашать **второе изложение одного предмета** — ровно тот класс, ради которого круг 9
   перенацеливал `65`. У решения **один** владелец, и сосед обязан **ссылаться**, а не
   пересказывать. Ссылаться при этом — на **страницу сервиса в дереве продукта** (DoD п. 17),
   а не на эту приёмку: приёмка закроется и уйдёт из поля зрения, страница останется.

   Предметом кругу 11 это не становится: контракт ресурса членства принадлежит той приёмке, и
   правит его её автор.

---

## §16 Круг 11 — два блокирующих круга 10, семь важных, перемер §1

Правки внесены **автором**; **вердикта нет и здесь он не ставится** — его ставит
`acceptance-reviewer`. Действующее состояние документа — вердикт круга 10 в шапке.

**Ревизии замера круга 11** (числа этого раздела относятся к ним и ни к чему больше):
продукт — `release/identity-4` @ **`f58089a44`** (2026-08-24 17:35 +0300); воркспейс —
`acc/iam-id-1-b3` @ `a47450a`.

### 16.1 Перемер §1 по норме §1.0а — расхождений нет, и это установлено ДВУМЯ способами

Норма §1.0а требует перемера перед подачей, а не доверия к прошлому кругу. Вершина ушла:

```sh
git rev-list --count ec133b94e..f58089a44        # 2 — от ревизии круга 10
git rev-list --count 475f8527..f58089a44         # 12 — от ревизии круга 9
```

**Способ первый — по составу изменения.** Два коммита трогают **14** файлов, и ни один из них
не лежит в дереве, чьи координаты называет §1:

```sh
git diff --name-only ec133b94e f58089a44 | wc -l                                  # 14
git diff --name-only ec133b94e f58089a44 \
  | grep -cE '^(proto/|services/iam/internal/migrations/|gateway/|ui-future/)'     # 0
# контроль в обратную сторону — предикат исполним и НАХОДИТ, иначе ноль выше читался бы
# как «команда никогда ничего не находит»:
git diff --name-only ec133b94e f58089a44 | grep -cE '^services/'                  # 13
```
Предмет обоих — идемпотентность отзыва удостоверения (`sa_keys`, `user_tokens`, клиенты
OAuth). Это **не** доказательство: §1 несёт и предикаты, обходящие всё дерево, а такой
предикат может измениться от файла вне перечисленных каталогов. Поэтому —

**способ второй: широкие предикаты §1 прогнаны на `f58089a44` заново.** Все дали прежние
значения:

| Предикат §1 | Круг 10 (`ec133b94e`) | Круг 11 (`f58089a44`) |
|---|---:|---:|
| `ListAccountsForUser` все · непробные | 29 · 8 | **29 · 8** |
| `membership_id` · `MembershipService` в контракте iam | 0 · 0 | **0 · 0** |
| `rpc RemoveFromAccount` · `member_remover` | 1 · 4 | **1 · 4** |
| `from_request_field: "account_id"` в `user_service.proto` | 2 | **2** |
| `blk List \| grep -c scope_filtered` · контроль `blk Invite` | 2 · 0 | **2 · 0** |
| `blk List \| grep -c '"<exempt>"'` | 0 | **0** |
| `string account_id = 6` | 0 | **0** |
| `"mbr"` в каталоге дефис-префиксов · контроль | 0 · ≥1 | **0 · 12** |
| файлов с `iam.users.removeFromAccount` | «три места» | **4** — см. 16.4 п. 6 |

Единственное расхождение — последняя строка, и оно не про дерево, а про **число, названное
приёмкой**: §1.9 говорила «три места», предикат находил четыре. Закрыто как важное 6.

**Что перемер НЕ доказывает.** Совпадение на двенадцати коммитах есть **свидетельство**, а не
гарантия: §1.0а не истекает, и кругу 12 перемер предстоит заново.

### 16.2 Б10.2 — ИСХОД: конструируемость ДОКАЗАНА посевом, посылка блокирующего опровергнута

Вердикт назвал два исхода: перенести `65` к стадии, где он конструируем, **либо** доказать
конструируемость двух членств на одну личность до S3.2 **посевом, а не рассуждением**. Выбран
второй, и вот замер.

**Посылка блокирующего** («второе членство на одну строку личности сегодня неконструируемо;
человек, приглашённый в два аккаунта, существует как две строки») **была верна на день, когда
её записали, и перестала быть верной ДО ревизий кругов 9 и 10** — то есть оба круга мерили
дерево, в котором она уже не действовала. Это не оценка, а достижимость:

```sh
# глобальный ключ идентичности и посев, доказывающий пару, — предки ОБЕИХ ревизий ревью
for c in c83eb6bc1 5ce4e89e8; do
  git merge-base --is-ancestor $c 475f8527 && echo "$c ⊂ круг 9"
  git merge-base --is-ancestor $c ec133b94e && echo "$c ⊂ круг 10"
done
#   → c83eb6bc1 ⊂ круг 9 · c83eb6bc1 ⊂ круг 10 · 5ce4e89e8 ⊂ круг 9 · 5ce4e89e8 ⊂ круг 10
```

Опровергается посылка четырьмя командами, каждая проверяется отдельно:

```sh
R=f58089a44
# 1. арбитр приглашения — ГЛОБАЛЬНЫЙ ключ почты: второй СТРОКИ человеку не заводится
git show "$R":services/iam/internal/migrations/20260823050000_users_identity_uniqueness_goes_global.sql \
  | grep -c 'CREATE UNIQUE INDEX users_identity_email_uniq'                                  # 1

# 2. путь приглашения пишет ЧЛЕНСТВО явно, в том же стейтменте, что и строку
git show "$R":services/iam/internal/repo/kacho/pg/user_repo.go \
  | awk '/func \(w \*userWriter\) InsertPending/{f=1} f{print} f&&/^}/{exit}' \
  | grep -c 'INSERT INTO memberships'                                                        # 1
# вызывающий — ПУБЛИЧНЫЙ глагол приглашения, и считается ВЫЗОВ, а не упоминание имени:
git grep -c 'UsersW()\.InsertPending' "$R" \
  -- services/iam/internal/apps/kacho/api/user/invite.go                                     # 1
# контроль в обратную сторону — тот же предикат по всем ОСТАЛЬНЫМ use-case сервиса:
git grep -c 'UsersW()\.InsertPending' "$R" \
  -- services/iam/internal/apps ':!*invite.go' ':!*_test.go' \
  | awk -F: '{s+=$NF} END{print s+0}'                                                        # 0
#   ^ греп по голому имени дал бы 3 — оно стоит ещё в двух комментариях того же файла;
#     единица здесь ВЫЗОВ, и она названа, потому что три и один мерят разные вещи

# 3. зеркало БОЛЬШЕ НЕ СНИМАЕТ членств других аккаунтов — ветвь снята при S2
git show "$R":services/iam/internal/migrations/20260824010000_membership_mirror_does_not_invent_a_membership.sql \
  | awk '/\+goose Up/{u=1} /\+goose Down/{u=0} u' \
  | grep -c 'account_id <> NEW.account_id'                                                   # 0
# контроль в обратную сторону — в ПЕРВОЙ редакции зеркала эта ветвь была:
git show "$R":services/iam/internal/migrations/470001_memberships_expand.sql \
  | grep -c 'account_id <> NEW.account_id'                                                   # 1

# 4. и это не вывод, а ПОСЕВ: посаженная проба строит пару и утверждает СОСТОЯНИЕ таблиц
git show "$R":services/iam/internal/repo/kacho/pg/user_invite_second_account_integration_test.go \
  | grep -c 'require.Len(t, mems, 2'                                                         # 1
```

Проба называется `TestUserInvite_ExistingActiveEmail_SecondAccount` и утверждает дословно то,
чего требует `Given` сценария `65`: та же строка личности и **два действующих членства**. Её
собственная шапка объясняет, почему прежний исход («свежая строка `PENDING` в аккаунте B»)
больше не наступает.

**Что из этого следует, по пунктам:**

1. **`65` конструируем на S3.1** и переносить его некуда — исход блокирующего закрыт вторым
   способом, тем, который вердикт назвал предпочтительным («конструируемость доказывается
   посевом»);
2. **то же верно для `31` и `57`**, а значит устарела не одна строка. Правлены **одиннадцать**
   мест, и разбиты они по тому, как посылка в них выражена — иначе одно число ничего не
   говорит о радиусе:
   - **дословно, три**: «с этого момента второе членство конструируемо» — таблица §2.3а
     (строка S3.2), таблица стадий §4 (строка S3), §9.1 (строка S3);
   - **производной формой, восемь**: «пока членство у каждого одно, множества нет» (проза
     §2.3а «Чем удерживается порядок», её хвостовой абзац, §4 «Почему порядок именно такой»,
     выноска `31`), «такого человека раньше S3 не существует» (историческая выноска круга 3 в
     §2.3а), «до того как глагол членства сделает второе членство конструируемым» (§2.3а «Что
     взамен становится обязательным»), «править модель, пока членство одно» (историческая
     выноска S2-vs-S3 в §4), «31/57 зелены без синтетики» после S3.2 (таблица
     production-completeness §4).

   Двенадцатая правка — не исправление, а **добавление**: `Given` сценария `65` теперь прямо
   называет второе членство и способ его построить;
3. **шаг S3.2 остаётся на своём месте и остаётся после S3.1** — он меняет **контракт**
   (приглашение переезжает на ресурс членства), а не конструируемость. Порядок назначен
   вердиктом круга 6 и автором не пересматривается;
4. **довод за «S3.1 первым» стал сильнее, а не слабее.** Прежняя редакция обосновывала порядок
   тем, что перестановка **завела бы** окно с множественным членством. Предпосылка этого окна
   выполнима уже сегодня — значит S3.1 закрывает настоящее, а не предотвращает будущее. Разбор
   самого окна стоит в §2.3а и здесь не повторяется: два места об одном предмете расходятся;
5. **сравнение доступа «до/после» на шаге S3.1 остаётся положительным контролем**, но
   обоснование у него теперь честное: оно пусто для **преобладающего** случая (одно членство),
   а не тождественно. Признак завершённости шага — `31`, и он исполним.

**Класс, ради которого этот пункт записан так подробно:** утверждение приёмки о дереве
пережило свой предмет и было унаследовано вердиктом как данность. Ни автор, ни ревьюер его не
перемеряли — оно выглядело следствием замысла, а не замером. Противоядие названо в корпусе
(`doc-truthfulness`): всякое «раньше стадии X это неконструируемо» обязано нести предикат
конструируемости, а не рассуждение о замысле.

### 16.3 Б10.1 — перепись мест шире объявленной вердиктом: не шесть, а восемь

Вердикт назвал **шесть** мест, называющих состав работ шага S3.1, и был прав в том, что круг 9
считал их двумя. Моя перепись даёт **восемь**; расхождение — в **единице счёта**, и обе
величины истинны для своей единицы.

**Единица круга 11 — МЕСТО: строка-ячейка таблицы либо абзац** (блок между пустыми строками),
**перечисляющий состав S3.1**. Механический признак — в одном месте стоят и «префикс», и
«глагол…»: состав S3.1 всегда называет префикс `mbr` и глаголы.

```sh
F=docs/specs/sub-phase-IAM-ID-1-identity-account-decoupling-acceptance.md
S=$(grep -n '^## §0 Предмет' "$F" | cut -d: -f1)          # действующая часть — §0…§9,
E=$(grep -n '^## §10 Разбор круга 1' "$F" | cut -d: -f1)  # шапка и §10-§16 суть история
awk -v s="$S" -v e="$E" 'NR>=s && NR<e' "$F" \
  | awk '/^$/{ if (b!="" && b ~ /префикс/ && b ~ /глагол/) c++; b=""; next }
         { b = b " " $0 } END { if (b ~ /префикс/ && b ~ /глагол/) c++; print c }'
#   → 12 кандидатов; после адъюдикации перечисляют состав S3.1 — ВОСЕМЬ
```

Предикат намеренно **шире** предмета: он даёт 12, из которых 4 говорят о другом (потребители
§2.1а, таблица production-completeness, `Given` сценария `64`, выноска круга 9 у `65`). Сузить
его до восьми **до** правки машинно нечем — «перечисляет состав» есть суждение, а не признак;
поэтому названы **оба** числа и способ отсева, а не одно число, принимаемое на веру.

**После правки восьмёрка перестала быть суждением и стала предикатом** — все восемь несут одну
формулировку, и это проверяется тем же обходом:

```sh
awk -v s="$S" -v e="$E" 'NR>=s && NR<e' "$F" | awk '
  function norm(x){ gsub(/[[:space:]>]+/," ",x); return x }
  /^$/ { if (b ~ /префикс/ && b ~ /глагол/) { n=norm(b)
           if (n ~ /сверх посаженного исключения/ || n ~ /Снятие из этого перечня/) k++; else o++ }
         b=""; next } { b = b " " $0 }
  END { print "несут канон: " k "; говорят о другом: " o }'
#   → несут канон: 8; говорят о другом: 4
```
Два числа названы **намеренно**: одно «8» неотличимо от ошибки отбора, а пара «8 и 4» в сумме
даёт те же 12 кандидатов, то есть отсев виден целиком.

| # | Место | Состояние до круга 11 | Что сделано |
|---|---|---|---|
| 1 | **§0.2**, внешнее предусловие, п. 1 | **ПРОТИВОРЕЧИЛО**: «глаголы записи **и снятия** членства … строятся здесь и целиком» | приведено к §1.9 |
| 2 | §1.9, «что остаётся работой S3.1» | сужено и верно | не трогалось (вердиктом не заказано) |
| 3 | §2.3а, таблица, строка S3.1 | сужено и верно | не трогалось |
| 4 | §2.3а, выноска «ветвь (4a) уже переведена» | **родовое** «глаголы» | сужено |
| 5 | §2.3а, проза, нумерованный список | **родовое** «глаголы ресурса членства» | сужено |
| 6 | §4, таблица стадий, строка S3 | сужено и верно | не трогалось |
| 7 | §4, «Почему порядок именно такой» | **родовое** «глаголы» | сужено |
| 8 | §9.1, строка S3 | **родовое** «глаголы членства» | сужено |

**Действующее утверждение — одно, и оно теперь стоит во всех восьми:** состав шага S3.1 несёт
глаголы ресурса членства **сверх посаженного исключения из аккаунта** (создание и два чтения
§5.0 п. 0; снятие уже есть — §1.9, второго глагола не заводится).

**Почему §0.2 блокировало, а §9.1 — нет.** §0.2 сам объясняет, почему стоит первым:
исполнитель читает документ подряд и несёт форму в голове с §1. Указание, отменённое в конце,
из головы не выгружается — оно оттуда исполняется.

### 16.4 Семь важных — исход по каждому

| # | Что | Что сделано |
|---|---|---|
| **1** | `65`: голое отрицание про кортеж, и оно пиннит до-S3-форму | утверждение разбито на **пару** («до вызова `A` — предок личности, после — не предок») и на **форму конца стадии** (после S3.1 — перевыраженная замена, ключуемая на членство). Утверждается **предковость**, а не написание кортежа, иначе на конце стадии оно станет тождественно истинным по посторонней причине. `65` внесён в DoD п. 5 |
| **2** | §1.5: предикат извлечения полосы **не исполнялся** | форма заменена на исполнимую: `grep -oE 'permission\)[[:space:]]*=[[:space:]]*"[^"]+"'` → `permission)     = "iam.users.list"`. Прежняя (`permission += +"…"`) печатала пустоту: между `permission` и `=` стоит `)`. Вывод §1.5 не изменился ни на знак — несущая пара `scope_filtered` 2 · 0 работала; изменилась **проверяемость** утверждения (§1.0 п. 1). Причина, по которой строка прожила круг: она стояла в блоке, которым закрывался блокирующий, и читалась как часть закрытого |
| **3** | §1.9: назвать естественный ключ и предикат истечения решения | внесено в выноску решения: полный `UNIQUE (user_id, account_id)` из `470001` (контроль — тот же предикат на заведомо частичном `users_identity_external_id_uniq` даёт **1** против **0**), и предикат истечения: решение живёт, пока ключ полон; третье состояние членства сделает его частичным, и адресация парой станет неоднозначной **молча** |
| **4** | родовые перечни работ S3.1 | сужены — §16.3 |
| **5** | отступление от стандартных методов объявлено только в приёмке | заведён **DoD п. 17**: решение обязано лечь в `services/iam/docs/engineering/architecture/` дерева продукта. Предикат сегодня **не выполнен** (`git grep -c 'RemoveFromAccount' -- services/iam/docs/engineering/architecture/` → 0) при контроле 25 по коду сервиса. Туда же ссылается сосед (§16.5) |
| **6** | §1.9: каталог назван тремя местами, предикат даёт четыре | предикат сужен исключением `':!*/collections/*'` → **3**, и разница названа: четвёртая есть **порождённая** коллекция, а не исходник. Та же единица счёта, что у §1.5а класса 5 |
| **7** | `31`: «правка модели идёт на S2» против двенадцати мест, где она на S3 | метка исправлена на **S3, шаг S3.1**, с записью о том, что устарела ровно метка, а рассуждение вокруг неё верно. Заодно снята посылка «пока членство у каждого одно, множества нет» (§16.2) |

### 16.5 Замечание соседу переписано — условие заменено фактом с предикатом

§15.5 п. 4 адресовал IAM-ID-2 условное «если сосед заказывает полный набор стандартных методов
…». Условие **уже разрешимо**, и оно разрешено замером: сосед `Delete` не заказывает
(`grep -c 'DeleteMembership\|rpc Delete'` → **0**), а снятие судит **тиром аккаунта** со
ссылкой на посаженный глагол (`grep -c 'RemoveFromAccount\|member_remover'` → **8**).

Действующая форма замечания — другая по существу: сосед обязан **ссылаться**, а не излагать
решение во второй раз (у решения один владелец — §1.9), и ссылаться на **страницу сервиса в
дереве продукта** (DoD п. 17), а не на эту приёмку, которая закроется. Предметом круга 11 это
не становится: контракт ресурса членства принадлежит той приёмке.

### 16.6 Что круг 11 НЕ делал

- **не ставил вердикта** — его ставит `acceptance-reviewer`;
- **не трогал принятое**: §1 (кроме одного негодного предиката §1.5), §1.9 (кроме двух
  заказанных дополнений), §2.1а, §5.0, §9.2, §9.3, §13, §15 (кроме переписанного п. 4 §15.5) и
  одиннадцать закрытых блокирующих;
- **не пересматривал порядок стадий** — он назначен вердиктом круга 6; §16.2 меняет
  **обоснование** одного его шага, а не сам порядок;
- **не заводил новых сценариев и не менял их счёта** — арифметика `71 / 9 / 1 / 61` не
  тронута;
- **не переписывал §2.3а по существу окна** — разбор окна стоит там и здесь не дублируется.

### 16.7 Что остаётся кругу 12

1. **вердикт по существу** — `acceptance-reviewer`;
2. **перемер §1 перед следующей подачей**: норма §1.0а не истекает, предикат тот же
   (`git rev-list --count f58089a44..<вершина>` не ноль);
3. **вердикт IAM-ID-2** — предикат снятия внешнего предусловия (DoD п. 16); у соседа сегодня
   `DRAFT`;
4. **DoD п. 17 — работа исполнителя, а не автора приёмки**: запись отступления в
   `services/iam/docs/engineering/architecture/` делается тем же изменением, что и S3.1, и её
   предикат сегодня даёт **0**.
