# Sub-phase QUOTA-FATE-1 — величину потолка объявляет ПОСАДКА домена: приёмка

> **Статус:** **❌ CHANGES REQUESTED** — второй круг `acceptance-reviewer` от 2026-09-14,
> запись §13.3. **`Б1` закрыто ПО СУЩЕСТВУ** (витрина: производитель заведён, стадия `S2`
> названа), `З1` учтено, четвёртая колонка §7.1 заведена, запись §13.1 не правлена ни одним
> символом — всё это проверено прогонами, а не прочтением. Блокирующее одно и новое — `Б2`:
> **путь МУТАЦИИ гейтится тем же, чем гейтилась витрина** — значением `authority_state` в
> `quota_sync_cursor`, а не наличием строки учёта с величиной, как утверждает колонка `Q-01`.
> Отказ `QUOTA_EXCEEDED` недостижим ни при каком входе, и ни одна стадия §8 этого не меняет;
> `Q-01` при этом помечен **есть**. Род — тот же, что отверг линию дважды, третий раз; почему
> перенос класса его не поймал — §13.3. Решение (исход **В**) под сомнение не ставится.
> Автор приёмки её не одобряет и одобрить не может: `APPROVED` ставит только
> `acceptance-reviewer` (ban #1).
> **Дата:** 2026-09-14. **Предмет задачи:** `PRO-Robotech/kacho#2190`.
> **Ревизии сверки — ОДНА на каждое дерево, все числа §1 получены на них:**
> продукт `PRO-Robotech/kacho` @ **`13bdec4175`**, вершина 2026-09-14T14:53+03;
> служба доступа `PRO-Robotech/kaname` @ **`b673325f`**;
> фундамент `PRO-Robotech/corelib` @ **`fef214c`**; воркспейс @ **`d77fff14`**.
> **Здесь стояло «обновить ссылки не удалось», и это была МОЯ ошибка чтения, а не свойство
> машины — называю её первой.** Я прочитал строку `sign_and_send_pubkey: signing failed …
> agent refused operation` как отказ операции. Она отказ ОДНОГО ключа агента; `ssh` берёт
> следующий, и обращение проходит. Перемерено не повтором того же `fetch`, а **вопросом к
> удалённой стороне**: `git ls-remote origin refs/heads/main` отвечает
> `13bdec4175…	refs/heads/main` — ровно то, что держит `origin/main`. То есть ссылки
> **свежие**, расхождения с удалённой стороной **нет**, и перемерять рецензенту нечего.
> Класс — `testing-verdict.md` §7а: исход читался по виду вывода, а не по тому, что вернул
> вопрос. Числа §1 верны для названных ревизий, и названные ревизии суть вершины.
> **Нормативка — ссылками по названию раздела, в тело не дублируется:**
> `.claude/rules/00-kacho-core.md` — ban #1, #9, #10, #11, #14, #15, #16, #18;
> `.claude/rules/data-integrity.md` — §«Счётчик потолка живёт РЯДОМ С РЕСУРСОМ, а величина —
> у владельца величин», §«Within-service инварианты», §«Данные СТЕНДА заводятся посевом»;
> `.claude/rules/api-conventions.md` — §«Принято-и-проигнорировано», §«Неисполнимая
> возможность», §«Пустое значение обязано означать „пусто“», §«gRPC-код → HTTP-статус»;
> `.claude/rules/security.md` — §«Пустой список — это „не сужаем“», §Hardening п.9;
> `.claude/rules/polyrepo.md` — §«Runtime cross-domain edges», §«Копия между репозиториями
> ЗАПРЕЩЕНА»; `.claude/rules/testing.md` — §«Гейт на класс».
> **Предшественники — наследуются, не отменяются:**
> `sub-phase-KAN-QUOTA-1-limit-authority-leaves-iam-acceptance.md` (решения `Д1`, `Д2`, `П25`),
> `sub-phase-quota-v2-materialised-usage-acceptance.md` (APPROVED),
> `sub-phase-vpc-quota-resource-count-acceptance.md` (APPROVED),
> `sub-phase-quota-account-scope-acceptance.md` (APPROVED).

---

## §0. Что решает этот документ, и почему он приёмка, а не отчёт

Задача `kacho#2190` просит **решение**, а не работу: «сохраняется ли в продукте возможность
назначать пределы, чей это домен, и что происходит с уже назначенными величинами». По
`git-issues-issue-lifecycle.md` §«Второй законный исход» артефактом закрытия такой задачи
служит **APPROVED-приёмка**, а реализация заводится своей задачей со своим предикатом.

**Этот документ решение принимает и называет его цену.** Он не открывает написание кода — это
сделает круг `acceptance-reviewer` (ban #1).

---

## §1. Что измерено (предикаты приведены, контроль в обратную сторону — где предикат может
ответить «ноль» по слепоте)

### §1.1. Потребительская машинерия ПОЛНА — у пяти доменов провязано всё, кроме источника

```sh
git grep -ln 'StartLimitSync' origin/main -- 'services/*/cmd/**' 'services/*/internal/**'
git ls-tree -r origin/main --name-only | grep 'quota_authority\.go$'
git grep -n '^service QuotaService' origin/main -- 'proto/**/*.proto'
```

| что | число | единица счёта |
|---|---:|---|
| доменов, поднимающих синхронизатор величин | **5** | файл корня сборки с `StartLimitSync` |
| доменов с ручкой объявления авторитета | **5** | файл `quota_authority.go` |
| доменов с арендаторским чтением | **5** | объявление `service QuotaService` в контракте |
| **производителей величины** | **0** | объявление службы авторитета в любом из трёх деревьев |

**Контроль в обратную сторону:** предикат на производителя ищется не по слову, а по символу —
`git grep -lE 'InternalLimitService' -- 'services/**' 'pkg/quota/**'` даёт **пусто** при
непустом `git grep -l quota_authority.go -- 'services/*/internal/**'` → **5**. Ноль слева без
парного замера справа означал бы «перестали читать», а не «перестали находить».

### §1.2. Все пять профилей объявляют домен величин ОТСУТСТВУЮЩИМ

```sh
for s in vpc compute storage nlb registry; do
  git grep -h -A3 'quota:' origin/main -- "services/$s/deploy/values.yaml" | grep -m1 authority
done
```
→ пять раз `authority: "not-deployed"`. Иного значения ни один профиль не несёт и нести не
может: `pkg/quota/quotaedge` `ValidateAuthorityHasAProducer` отвергает адрес **стартом**.

### §1.3. Следствие для арендатора СЕГОДНЯ — потолков нет ни у кого, и это состояние
**объявленное**, а не поломка

| поверхность | поведение при `not-deployed` | производитель |
|---|---|---|
| путь запроса | списание идёт, отказа нет **никогда** | `…/migrations/20260908041500_quota_absent_authority_charges_unconditionally.sql` |
| арендаторское чтение | `FAILED_PRECONDITION`, признак `QUOTA_AUTHORITY_ABSENT` | `PRO-Robotech/corelib:quota/quotaread/posture.go` `Posture.Refusal()` |
| синхронизатор | не заводится; состояние курсора называет причину | `PRO-Robotech/corelib:quota/authority.go` `AuthorityAbsent` |

**То есть работа сделана в обе стороны развилки и ведёт в никуда.** Это и есть цена ожидания:
механизм полон, а потолок не действует ни в одной установке.

### §1.4. Видов, которые списываются, — ДЕВЯТНАДЦАТЬ, и восемь у самого крупного домена

**Предикат ТЕРПИМ К ПЕРЕНОСУ СТРОКИ, и это не мелочь** — объявление триггера переносит вид на
следующую строку у двух владельцев из пяти:

```sh
for s in vpc compute storage nlb registry; do
  for f in $(git ls-tree -r origin/main --name-only "services/$s/internal/migrations"); do
    git show "origin/main:$f"
  done | tr '\n' ' ' | grep -oE "kacho_quota_count\( *'[a-zA-Z0-9.]+'" \
       | sed "s/.*'\([a-zA-Z0-9.]*\)'/\1/"
done | sort -u | wc -l
```

| домен | видов |
|---|---:|
| vpc | 8 |
| compute | 3 |
| storage | 3 |
| nlb | 3 |
| registry | 2 |
| **всего уникальных** | **19** |

> **Здесь стояло «17», и это была слепота МОЕГО предиката, а не свойство дерева.** Однострочная
> форма `kacho_quota_count\('…'` не видит объявления, где вид перенесён на следующую строку, и
> теряла `loadbalancer.listeners` и `registry.repositories`. Поймано не перечитыванием, а
> расхождением двух своих же замеров (`testing-verdict.md` §14). Число исправлено **после**
> последней правки документа, как того требует дисциплина.

Величина несущая: она решает, выразима ли посадка файлом настроек. Девятнадцать ручек на пять
профилей — выразимо; сто девяносто не было бы.

### §1.5. Старшинство областей УЖЕ разрешено фундаментом, и оно упорядочено

```sh
git show origin/main:quota/limitsync.go   | sed -n '/func (s Scope) rank/,/^}/p'   # corelib
git show origin/main:quota/projection.go  | sed -n '/func precedencePredicate/,/^}/p'
```
→ `DEFAULT` 1 < `ACCOUNT` 2 < `PROJECT` 3; проекция дельты области `DEFAULT` правит **только**
строки, чей текущий `source_scope = 'DEFAULT'`.

**Это самое важное число документа**, и §2 стоит на нём целиком.

### §1.6. Прецедент посадочной величины УЖЕ ПОСАЖЕН и одобрен — в службе доступа

`kaname@b673325f`: `internal/apps/kaname/config/own_ceilings.go`,
`internal/domain/limit_posture_stated.go`, приёмка `KAN-QUOTA-1` `П25`.

| ось | что сделано у службы доступа |
|---|---|
| сколько видов | **3** (`iam.account`, `iam.user.credential`, `iam.serviceAccount.credential`) |
| где величина | ручка посадки, по одной на вид, привязка ключа к переменной **выведена**, а не выписана |
| умолчание | **нет ни одного**; незаданное — отказ старта |
| ноль | законное значение («этого вида не заводить»), поэтому тип — указатель |
| что видит арендатор | `source_scope = 'DEFAULT'` (`internal/repo/kaname/pg/identity_quota_repo.go`, `scopeDefault`) |

**Контроль:** у службы доступа ручки внешнего авторитета **не осталось вовсе** —
`git grep -rn 'QUOTA_AUTHORITY\|StartLimitSync' -- deploy internal` даёт пусто. То есть она
не «временно без авторитета», а **устроена без него**.

### §1.7. Открытые задачи линии просят КВОТЫ ВИДЕТЬ, а не отменять

`kacho#411` (P1, epic) — «единым делается всё, кроме места хранения счётчика»;
`kacho#705` (P2, bug) — «потолок, который отвергает арендатора, арендатору не виден».
Ни одна не просит поарендаторной величины и ни одна не просит снятия квот.

---

## §2. РЕШЕНИЕ — исход **В**: величину объявляет ПОСАДКА домена; отдельного авторитета не
заводится

**Одной фразой:** каждый из пяти доменов объявляет величины своих видов в собственном профиле
развёртывания — ровно тем механизмом, каким служба доступа уже объявляет свои три, — и
`source_scope` этих величин есть `DEFAULT`.

### §2.1. Довод, которого не было в постановке: **В и Б НЕ ВЗАИМОИСКЛЮЧАЮЩИ**

Задача `#2190` называет два исхода как альтернативу. Замер §1.5 её опровергает.

Старшинство областей упорядочено, а `DEFAULT` — **низшая**. Значит:

- посадка заполняет полосу `DEFAULT` — ту, что сегодня пуста;
- авторитет, если он когда-нибудь появится, заполняет `ACCOUNT` и `PROJECT` и **перекрывает**
  посадку по построению, не трогая её;
- проекция дельты `DEFAULT` не может затереть поарендаторное перекрытие — предикат
  `source_scope = 'DEFAULT'` этого не позволяет.

**Отсюда: выбор В не отзывает исход Б, он его РАЗБЛОКИРУЕТ.** Сегодня полоса `DEFAULT` пуста, и
поэтому весь механизм мёртв; заполнив её, продукт получает действующий потолок немедленно, а
вопрос поарендаторных величин становится тем, чем он и является, — **коммерческим предметом со
своей ценой**, а не условием работоспособности.

Формулировка «одно из двух» была верна, пока старшинство не измерили. Это второе опровержение
посылки в этом документе.

### §2.2. Что В НЕ является — авторитетом под другим именем

Различие названо **по осям**, а не словами, и взято дословно из прецедента
(`PRO-Robotech/kaname:internal/domain/limit_posture_stated.go`):

| ось | авторитет величин | посадка |
|---|---|---|
| контракт | есть, со своими глаголами | **нет** |
| область | три, со старшинством | **одна**, `DEFAULT` |
| дельта и ревизия | есть | **нет**: величина одна на вид |
| кто назначает | администратор облака, снаружи, во время работы | **оператор установки**, файлом, при выкатке |

### §2.2а. МНОЖЕСТВО объявленных величин — либо ПУСТОЕ, либо РАВНОЕ каталогу домена

**Это несущая часть решения, а не деталь реализации:** без неё два сценария §7 требуют
противоположного на одном входе, и проверил это не автор, а круг рецензии — §2.2б.

**Норма.** Величины объявляются **множеством**, и у него ровно два законных состояния:

| множество объявленных | что значит | исход подъёма |
|---|---|---|
| **пустое** | установка потолков не объявляет; поведение сегодняшнее | **поднимается** |
| **равно каталогу** домена | установка объявила потолки целиком | **поднимается**, потолки действуют |
| **любое промежуточное** | часть видов накрыта, часть — нет | **отказ старта** |

**Почему промежуточного не бывает — довод не эстетический, он уже записан корпусом.**
Частично объявленный набор есть **половина пары**, и она хуже отсутствия обеих, потому что
**выглядит настроенной** (`security-hardening.md` §«Контроль, у которого нет МЕХАНИЗМА
исполниться»). Оператор, объявивший семь величин из восьми, читает свой профиль как
«потолки заведены», а восьмой вид остаётся неограниченным — и не сообщает об этом ничем:
ни отказом, ни записью, ни строкой витрины. Ровно этот разрыв между «выглядит» и «есть»
правило и запрещает.

**Каталог ВЫВОДИТСЯ, а не выписывается.** Предикат сравнивает объявленное с таблицей
«ключ → вид» самого домена — тем единственным местом, где ключ настройки связан с видом
(прецедент `PRO-Robotech/kaname:internal/apps/kaname/config/own_ceilings.go`,
`OwnCeilingKnobs`). Второго перечня не заводится: он разошёлся бы с первым молча. Что
каталог домена **уже** сошёлся с тем, что домен списывает, — измерено в §9.1 у всех пяти.

**Предикат ОДИН, и звать его нечего забыть.** Судит он множество целиком, а не величину
за величиной: две отдельные проверки дают потребителю возможность позвать первую и забыть
вторую, и забытая половина есть предмет `KAN-Q1-05` (довод взят дословно у
`PRO-Robotech/corelib:quota/authority.go`, `ResolveAuthority`).

**Ноль этим НЕ затрагивается.** Вид, чья величина объявлена нулём, — объявлен
(«этого вида не заводить», Q-03); вид, чья величина не объявлена, — не объявлен. Различие
несёт **тип-указатель**, и оно то же самое, каким прецедент уже пользуется.

### §2.2б. Честно: пустое множество — это состояние, в котором потолков нет

**Назвать надо прямо, потому что §3.1 запрещает величину, подставляемую построением.**
Пустое множество означает «потолков в этой установке нет», и это **умолчание**: оператор
не сказал ничего, а продукт повёл себя определённо.

**Почему здесь оно законно, а у ручки `quota.authority` — нет.** Различает **направление
изменения**, и оно измерено, а не выведено:

| | умолчание у `quota.authority` | пустое множество величин |
|---|---|---|
| что было бы **до** умолчания | потолки ДЕЙСТВОВАЛИ (авторитет жив) | потолки не действуют **нигде** (§1.3) |
| что умолчание **снимает** | действующий потолок | **ничего: снимать нечего** |
| чем это является для арендатора | расширение поверхности, никем не названное | **сегодняшнее состояние**, не меняющееся ни на байт |

Фундамент отверг умолчание `not-deployed` доводом «сняло бы потолки МОЛЧА — расширение
поверхности, которого никто не называл» (`PRO-Robotech/corelib:quota/authority.go`). Довод
верен и **не отзывается**: он о мире, где потолки действовали. Сегодня их нет ни в одной
установке, поэтому пустое множество не снимает ничего.

**Второй довод — страж остаётся НЕВАКУУМНЫМ, а это и есть предмет запрета §3.1.** Запрет
бьёт в умолчание, при котором страж зелен **при любом входе**. Здесь страж падает на
промежуточном множестве, то есть на **единственном** входе, где намерение оператора и
поведение продукта могут разойтись. На пустом они совпадают: он не объявил — не действует.

**Что отсюда НЕ следует.** Не следует, что у ручки `quota.authority` появляется умолчание:
она остаётся без него, незаданная роняет старт, и это не трогается
(`PRO-Robotech/corelib:quota/authority.go`, ветка `raw == ""`).

### §2.2в. Витрина отказывает тогда и только тогда, когда величину не объявляет НИ ОДИН источник

**Это вторая несущая часть решения, и её здесь НЕ БЫЛО — нашёл круг рецензии, не автор**
(§13.1, `Б1`). Без неё `Q-04` требовал от дерева того, что дерево не производит ни при каком
входе.

**Что производит дерево сегодня** — измерено, предикат ниже, а не прочитан:

```sh
# 1. чем питается посадка витрины: два аргумента, объявленных величин среди них НЕТ
git -C project/kacho grep -ho 'ReadPosture([a-zA-Z]*, "[a-z]*")' origin/main \
    -- 'services/*/cmd/**' ':!*_test.go'          # -> пять вызовов формы ReadPosture(authority, "<домен>")
# 2. что она из него выводит — БЕЗУСЛОВНО
git -C project/corelib show origin/main:quota/authority.go | sed -n '/func ReadPosture/,/^}/p'
#    -> a.Deployed() ? AuthorityDeclared() : AbsentAuthority(serviceDomain)
# 3. отказ короткозамыкает ДО полосы чтения
git -C project/kacho show origin/main:pkg/quota/quotapb/convert.go | grep -n 'Refusal()'
#    -> if refusal := posture.Refusal(); refusal != nil { return nil, refusal }
```

`a.Deployed()` ложно **в каждой поднявшейся установке**: адрес отвергается стартом
(`quotaedge.ValidateAuthorityHasAProducer`, производителей ноль — §1.1), поэтому единственная
поднимающаяся посадка даёт `deployed == false`. Значит витрина отказывает **навсегда**,
включая установку, объявившую каталог целиком.

**Норма, которая это снимает.** Посадка витрины перестаёт быть функцией ручки и становится
функцией **источников величины**:

| авторитет величин | множество объявленных величин | витрина | достижимо сегодня |
|---|---|---|---|
| отсутствует | **пусто** | отказ `QUOTA_AUTHORITY_ABSENT` (`Q-07`) | **да** — это состояние всех пяти установок (§1.2) |
| отсутствует | **равно каталогу** | **200**, строка на каждый вид (`Q-04`) | **нет**: становится достижимым стадией `S2` |
| объявлен адресом | пусто либо равно каталогу | **200** — величину объявляет он, старшинство разрешает спор (§2.1) | **нет, и не станет этим решением**: адрес отвергается стартом, пока у контракта авторитета нет производителя. Строка описывает мир исхода Б и стоит здесь затем, чтобы норма пережила его появление без правки |

Одной фразой: **отказ ровно тогда, когда величину не объявляет НИ ОДИН источник.**
Промежуточного множества в таблице нет by construction — оно роняет старт (§2.2а), и это
верно в каждой строке, включая третью.

**Третья строка НЕ является обещанием — и это надо сказать прямо, иначе она сама есть тот
класс, ради которого правится документ.** Она не сценарий §7, ничего не требует от дерева и
никакой стадией не заводится: её единственная работа — зафиксировать, что норма
сформулирована **дизъюнкцией источников**, а не «функцией мощности». Сформулируй её через
мощность — и появление авторитета потребовало бы переписывать норму; сформулированная через
источники, она принимает его молча.

**Почему это НЕ третье значение ручки и не вторая ручка — §5.3 уже ответил.** Ручка
`quota.authority` не трогается и держит свои два значения; новой ручки не заводится. Довод
§5.3 против отдельной ручки `quota.ceilings` — «всё, что она объявляет, уже объявлено
мощностью множества величин» — здесь применяется **в прямую сторону**: раз мощность уже
объявляет это, её и читает витрина. Заводить рядом второй объявитель значило бы завести
пару, половины которой могут разойтись.

**Почему это НЕ пара, способная разойтись.** Условия соединены **дизъюнкцией источников**, а
не согласием двух объявлений об одном: каждое условие говорит про **свой** источник и про
него одно. Разойтись им негде — им не о чем спорить. Спор о ВЕЛИЧИНЕ, когда источников два,
разрешает старшинство областей (§1.5, §2.1), а не эта таблица.

**Величина по-прежнему читается ПРИ СТАРТЕ, а не на пути запроса** — посадка вычисляется в
композиционном корне домена, там же, где сегодня стоит `ReadPosture`. §3.1 («ни одного нового
ребра, ни одного пути запроса к соседу») остаётся верным дословно.

**Прочтение ОДНО на пять доменов.** Пять композиционных корней, написавших это каждый
по-своему, есть ровно тот класс, против которого сам фундамент оговаривает неэкспортируемые
поля посадки («пять владельцев написали бы пять разных его прочтений»,
`PRO-Robotech/corelib:quota/quotaread/posture.go`). Функция одна, живёт рядом с каталогом
величин — то есть в дереве платформы, потому что об объявленных величинах фундаменту знать
неоткуда.

#### §2.2в-1. Почему исход «витрина продолжает отказывать» ОТВЕРГНУТ, а не выбран

Круг назвал три исхода и второй — «переписать `Q-04`, `Q-06` и §6 под производимое» — счёл
законным. **Он не законен, и довод не эстетический: он оставил бы на пути запроса отказ,
чья проза утверждает обратное тому, что установка делает.** Текст дословно:

> `resource count limits are not stated in this installation: no limit authority is deployed,
> so no ceiling applies to any kind and no mutation is refused for exceeding one`

В установке, прошедшей `S4`, потолок **применяется** и мутация сверх него **отвергается**
(`Q-01`, производитель `kacho_quota_refuse` — есть). Оба придаточных становятся **ложными**,
и арендатор читает их не в журнале оператора, а **в ответе на свой запрос**. Это тот же
класс, который §12 уже применяет к строке журнала фундамента («утверждение становится
ЛОЖНЫМ — правится вместе с S1»), только дороже на порядок: журнал читает оператор, отказ
читает арендатор.

Второе, независимо достаточное: исход 2 воспроизводит предмет **открытой** задачи линии
`kacho#705` — «потолок, который отвергает арендатора, арендатору не виден» (§1.7). Приёмка,
чьё одобрение заводит ровно то, на что жалуется открытый дефект, годной не бывает.

Исход 3 («снять `Q-04`/`Q-06` вместе с обещанием витрины») отвергнут той же первой причиной:
снятие сценариев не снимает ложного текста — он производится деревом, а не сценарием.

### §2.3. Что происходит с уже назначенными величинами

**Ничего не переносится, потому что переносить нечего.** Производителей величин **ноль**
(§1.1), значит `limit_value` в строках учёта сегодня либо не существует, либо стоит с той
поры, когда авторитет был жив, и с тех пор не обновлялся. Строка при этом несёт
`source_scope = 'DEFAULT'` — ту самую полосу, которую посадка и заполняет.

Первое же списание после выкатки разрешит потолок заново из посадки. **Миграции данных
арендатора не требуется ни одной** — ось «перспективность» это и проверяет.

**Потребление не теряется:** материализация затравляет строку **фактом**, а не нулём
(`…/0042_quota_usage_seeded_from_rows.sql`), и страница арендатора это уже обещает.

---

## §3. Пять вопросов — у каждого признак ОТКАЗА, а не одобрения

| вопрос | ОТВЕРГАЕТСЯ, когда | вердикт по В |
|---|---|---|
| **благо продукта** | единственный выигрыш — меньше работы сейчас | **проходит**: выигрыш в том, что потолок начинает ДЕЙСТВОВАТЬ; сегодня он не действует ни в одной установке (§1.3). Меньше работы — следствие, а не довод |
| **конкурентоспособность** | выигрыш формулируется как «как у …» | **проходит**: своя ось — цена эксплуатации. Оператор задаёт потолок там же, где задаёт всё остальное; ноль новых компонентов, ноль новых рёбер, ноль новых состояний отказа на пути мутации |
| **перспективность** | ошибка необратима: ломает внешне-адресуемую координату либо требует миграции данных арендатора | **проходит**: внешне-адресуемого не трогается ничего (контракт `QuotaService`, поля ответа, `source_scope` — все остаются); миграции данных арендатора **ноль** (§2.3); отзыв решения = заведение авторитета на полосы `ACCOUNT`/`PROJECT`, посадка при этом остаётся законной низшей полосой (§2.1) |
| **безопасность** | решение расширяет поверхность, и расширение не названо | **проходит с НАЗВАННЫМ расширением** — §3.1 |
| **производственность** | есть режим, годный только для стенда, либо часть, отложенная «на потом» | **проходит**: механизм один во всех профилях, включая боевой; отложенного нет. **Отвергает исход Б** — у него отложенная часть есть by construction: до появления нового домена остаётся `not-deployed`, то есть сегодняшнее «потолков нет» |

### §3.1. Расширение поверхности — названо

**Что заводится:** у оператора установки появляется возможность **запретить вид целиком**,
объявив его величину нулём. Ноль — законное и осмысленное значение, и именно поэтому он
опасен: ошибка в профиле останавливает создание ресурса для всех арендаторов установки.

**Чем это удержано, и это часть решения, а не примечание:**

1. **у объявленного набора нет ПРОМЕЖУТОЧНОГО состояния** — множество либо пусто, либо равно
   каталогу домена (§2.2а). Именно это делает стража невакуумным: он падает на единственном
   входе, где намерение оператора расходится с поведением продукта, и молчит на двух, где они
   совпадают. Величина, которую подставляет построение, предметом стража быть не может — он
   зелен при любом входе (прецедент `own_ceilings.go`), и §2.2б называет прямо, что пустое
   множество умолчанием является и почему здесь оно не снимает ничего;
2. **ноль выразим отдельно от отсутствия** — тип указатель, не `int64`;
3. **страж старта называет КАЖДЫЙ необъявленный вид и его ручку** в тексте отказа — не первый
   из них: оператор, узнающий перечень по одному имени за перезапуск, платит перекатом за
   строку (`multierr` прецедента). Без имени ручки стенд не поднять — одно из трёх мест,
   выведенных из-под `security-disclosure.md` §«Публичные артефакты».

**Чего решение НЕ расширяет:** ни одного слушателя, ни одного ребра, ни одной записи каталога
разрешений, ни одного пути запроса к соседу. Величина читается **при старте**, а не на пути
запроса, — и это, отдельно, снимает целый класс: при исходе Б недоступность авторитета
означала бы `fail-closed` на мутациях **пяти доменов сразу**.

---

## §4. Имитация клиента — четыре направления

> **Условие годности названо честно и НЕ выполнено полностью.** Правило требует прогонов с
> **пустым** контекстом. Отдельного исполнителя на каждое направление в этой полосе не было —
> инструмента порождения подчинённого агента у неё нет. Поэтому прогон **приближён**: собран
> ровно тот вход, что доступен клиенту (контракт `quota.proto` и `quota_service.proto`,
> страница `services/⟨домен⟩/docs/content/api/quotas.mdx`, раздел консоли «Квоты», тексты признаков
> `QUOTA_EXCEEDED` / `QUOTA_NOT_PROVISIONED` / `QUOTA_AUTHORITY_ABSENT`, профиль
> `services/⟨домен⟩/deploy/values.yaml`), и на вопросы отвечено **только из него**; всё, чего в этом входе
> нет, записано находкой, а не достроено знанием автора. Рецензенту стоит прогнать четыре
> направления отдельными исполнителями — это и будет настоящая проверка.

### §4.1. Архитектор

| вопрос | ответ из клиентского входа |
|---|---|
| чей это домен и почему именно его | **владельца ресурса**. Контракт объявляет `QuotaService` внутри домена (`kacho.cloud.vpc.v1`), а не отдельным доменом квот — то есть продукт уже говорит, что квота есть свойство домена, а не самостоятельная сущность |
| что придётся отозвать, если решение неверно | **ничего внешне-адресуемого**: `source_scope` в ответе допускает `ACCOUNT` и `PROJECT`, значит контракт уже описывает мир, где величина приходит откуда-то ещё. Отзыв = добавление производителя, а не смена формы |
| какое ребро заводится и куда ведёт через год | **ни одного**. Это единственное направление, где В и Б расходятся резко: Б заводит ребро `пять доменов → новый домен` на пути запроса, с `fail-closed` |

**Находка направления:** из клиентского входа **не следует**, кто такой «администратор
облака», которого называет страница. Это не дефект решения — это дефект страницы, и он
существует уже сегодня.

### §4.2. Разработчик

| вопрос | ответ из клиентского входа |
|---|---|
| сколько шагов от «открыл страницу» до первого успешного вызова | **один**: `GET /vpc/v1/quotas?projectId=P`. Постраничности нет намеренно, параметр один |
| чем этот ресурс отличается от соседнего, решал ли кто-нибудь | отличается тем, что **только читается**: страница говорит это прямо и говорит, что ручки повышения «нет и не будет». Решал — да, и записано |
| говорит ли отказ, что делать дальше | **три признака, три разных следующих шага**, и они различены: `QUOTA_EXCEEDED` → удалить лишнее; `QUOTA_NOT_PROVISIONED` → просить завести предел; `QUOTA_AUTHORITY_ABSENT` → **ничего**, предела нет |

**Находка направления, и она блокирующая для сегодняшнего состояния, а не для решения:**
сегодня единственно достижимый признак — `QUOTA_AUTHORITY_ABSENT`, у которого следующий шаг
«ничего». То есть **вся витрина квот отвечает отказом при каждом обращении в каждой
установке**. Решение В это состояние снимает — **стадией `S2`**, которая делает посадку
витрины функцией объявленного множества (§2.2в); после неё достижимы первые два признака.

> **Адрес «стадией S2» здесь появился ТРЕТЬИМ кругом, и до него абзац обещал снятие без
> производителя.** Прежняя редакция говорила «решение В это состояние и снимает» — а решение,
> как оно тогда было записано, не снимало ничего: витрина читала ручку, которой ни одна стадия
> не касалась (§13.1, `Б1`). Находка направления была верной, а обещание её закрытия — нет.
> Это и есть цена имитации клиента, прогнанной автором: вопрос она задала правильный, ответ
> приняла свой.

### §4.3. Девопс / SRE

| вопрос | ответ из клиентского входа |
|---|---|
| по чему в три часа ночи отличить «сломан продукт» от «не создано условие» | **по признаку в ответе**, не по коду: `QUOTA_AUTHORITY_ABSENT` — посадка, `QUOTA_NOT_PROVISIONED` — величина не заведена, `QUOTA_EXCEEDED` — исчерпание. Различие машинное и объявлено контрактом |
| что видно до чтения кода | профиль: `quota.authority`. При В — ещё и перечень величин прямо в `values.yaml`, то есть **действующий потолок читается из профиля, а не выясняется запросом** |
| что откатывают и чего это стоит | **откат профиля** — величина возвращается предыдущей выкаткой. При Б откатывали бы состояние чужой базы |

**Находка направления:** при В смена величины требует **выкатки**, и это надо сказать вслух —
см. §5.3, где это названо ценой, а не умолчано.

### §4.4. Продакт

| вопрос | ответ из клиентского входа |
|---|---|
| какой шаг клиент делает первым | смотрит раздел «Квоты» проекта — четыре столбца: «Ресурс», «Предел», «Занято», «Кто задал предел» |
| чем платит за переименование, удаление, смену предела | переименование — ничем (вид адресуется токеном, не именем); удаление ресурса — освобождает занятое; смену предела **сам не делает** и об этом предупреждён |
| за что выбирает продукт — **его** словами | «я вижу, сколько мне можно и сколько я занял, и когда упрусь — пойму, во что именно» |

**Находка направления:** столбец «Кто задал предел» при В всегда покажет одно и то же
(`DEFAULT`). Столбец, у которого одно значение, ничего не сообщает — но **снимать его нельзя**:
он перестанет быть вырожденным в день появления авторитета. Исход — §7 `Q-06`: консоль
объясняет `DEFAULT` словами установки, а не прочерком.

---

## §5. Что отвергнуто — у каждого СВОЯ причина, не «хуже»

### §5.1. Исход **А** — квоты уходят из продукта целиком

**Отвергнут осью «безопасность»**, и это главное: снятие потолка есть **расширение
поверхности** — арендатор становится неограничен, — а расширение не названо ни в одном
артефакте продукта. Оно же отвергается осью «перспективность»: возврат требует вернуть
таблицу, триггеры, чтение и страницу у **пятерых**.

Довод не нов — ровно им `KAN-QUOTA-1` §3.4 уже отверг «снять учёт у пяти доменов вместе с
авторитетом». Здесь он применён к более широкому предмету и остаётся верным.

**Отдельно:** две открытые задачи линии (§1.7) просят квоты **видеть лучше**. Исход А сделал
бы обе беспредметными — то есть закрыл бы их не решением, а исчезновением предмета.

### §5.2. Исход **Б** — платформа заводит новый домен величин

**Отвергнут осью «производственность»**: у него есть часть, отложенная «на потом», и она не
косметическая — **до появления нового домена потолок не действует**. То есть исход Б
оплачивает заведение целого домена (контракт, хранилище, каталог разрешений, посадка,
документация, приёмка) и **до самого конца этой работы не даёт ни одного действующего
потолка**.

**Не отвергнут по существу и не отозван** (§2.1): полосы `ACCOUNT`/`PROJECT` остаются
законными и незанятыми. Отвергнуто ровно одно — делать его **сейчас и вместо** посадки.

**Предикат пересмотра — внешний и измеримый:** появился клиент, которому нужна величина,
отличная от величины установки (арендатор с отдельным договором, пробный период с суженным
потолком). До этого поарендаторная величина есть возможность без потребителя, и заводить её —
ban #11 в форме «на будущее».

### §5.3. Отвергнуто: **третьим законным значением ручки** сделать «величины из посадки»

Соблазн: оставить `quota.authority` и добавить ей значение `posture`. **Отвергнуто осью
«перспективность», и довод решающий: третье значение ОТМЕНИЛО БЫ §2.1.** Ручка держит одно
значение, поэтому `authority: posture` и `authority: <адрес>` взаимно исключаются — а §2.1
стоит ровно на том, что посадка занимает полосу `DEFAULT`, **пока** авторитет занимает
`ACCOUNT`/`PROJECT`. Под одной ручкой это состояние невыразимо, то есть обратимость решения,
которую утверждает `Q-05`, пропадает by construction.

> **Прежний довод здесь был слабее и вдобавок неверен фактически — называю свою ошибку.**
> Стояло: «ручка объявляет **адрес соседа**, и третье значение, соседа не называющее, сделало
> бы её ручкой двух разных предметов». Перемерено: ручка **уже** не про адрес — её значение
> `not-deployed` соседа не называет, а разрешается она в тип суммы
> `Authority{endpoint, deployed}` (`PRO-Robotech/corelib:quota/authority.go`). То есть предикат ручки и сегодня
> «откуда берётся величина», и довод «двух разных предметов» к ней не относится. Довод выше
> от этого не зависит и остаётся в силе.

**Правильная форма — прецедент:** величины объявляются **своими ручками, по одной на вид**, а
`quota.authority` остаётся тем, чем была, со своими двумя значениями.

**Отвергнута и ОТДЕЛЬНАЯ ручка «объявляет ли эта установка потолки»** (`quota.ceilings:
stated | none`). Она выразима и §2.1 не ломает, но **работы у неё нет**: всё, что она
объявляет, уже объявлено мощностью множества величин (§2.2а). Единственное, что она
добавляет сверх, — случай «величины объявлены, но действовать не должны», которого никто не
хочет, а взамен заводит **пару, половины которой могут разойтись**, — тот самый класс, ради
которого §2.2а держит **один** предикат на всё множество.

### §5.4. Названная цена решения В — она есть, и умалчивать её нельзя

| цена | величина | почему принята |
|---|---|---|
| смена величины требует **выкатки** | одна выкатка домена | величина установки меняется редко; у авторитета та же смена стоила бы живого домена с хранилищем |
| величина **одна на установку** | нет `ACCOUNT`/`PROJECT` | полосы остаются свободными, перекрытие добавляется без отзыва (§2.1) |
| **19 ручек** в профилях | 8 максимум у одного домена | выразимо; прецедент несёт 3. Здесь стояло «17» — число, которое §1.4 уже исправил на 19 своим же перемером, а сюда поправка не доехала: два места об одном предмете, и разошлось то, которое не считали |
| столбец «Кто задал предел» **вырожден** | одно значение | снимать нельзя — §4.4, `Q-06` |

---

## §6. Что видит арендатор — до и после

| | сегодня (`not-deployed`, все установки) | после решения В, **у установки, объявившей каталог целиком** |
|---|---|---|
| `GET /⟨домен⟩/v1/quotas` | `FAILED_PRECONDITION` · `QUOTA_AUTHORITY_ABSENT` | **200**, строка на каждый вид каталога домена |
| «Предел» | не показывается | число из посадки установки |
| «Занято» | не показывается | фактическое потребление (оно считалось и при отсутствии потолка) |
| «Кто задал предел» | не показывается | `DEFAULT` — «умолчание установки» |
| создание сверх потолка | **проходит** | `RESOURCE_EXHAUSTED` · `QUOTA_EXCEEDED` |
| кто поднимает предел | **никто** | оператор установки, выкаткой |

**Правая колонка описывает ОДНУ из двух законных посадок, и это надо сказать.** Установка,
не объявившая ни одной величины (§2.2а, Q-07), остаётся в левой колонке целиком — у неё не
меняется ничего.

**Различие между колонками производят ДВЕ стадии, а не одна, и порознь ни одна его не даёт:**

| | что делает | без неё правая колонка |
|---|---|---|
| **S2** | посадка витрины становится функцией объявленного множества (§2.2в); величина попадает в строку учёта через проекцию старшинства | **недостижима**: витрина отказывает при любом профиле, потому что читает ручку `quota.authority` |
| **S4** | профиль установки заполняется величинами | **недостижима**: множество пусто, посадка отказная — и это верно |

**Здесь стояло «различие производит S4», и это было неверно** — нашёл круг рецензии (§13.1,
`Б1`). S4 заполняет профиль величин и ручку `quota.authority` **не трогает**, поэтому один
лишь S4 правую колонку не производит: без S2 витрина отказывала бы и у заполненного профиля,
причём отказом, чья проза утверждает обратное происходящему (§2.2в-1).

**Переход НЕ ломающий:** сегодня витрина отвечает отказом, значит ни один клиент не мог
построить логику на её успешном ответе. Отказ сменяется успехом — расширение, а не слом.

**А вот путь мутации МЕНЯЕТСЯ, и это надо объявить громко:** сегодня количественная мутация не
отвергается никогда, после — отвергается. Значит выкатка обязана идти в порядке §8 и с
величинами, **заведомо превышающими нынешнее потребление**, иначе установка с уже
накопленными ресурсами получит отказ на первом же создании.

---

## §7. Наблюдаемое поведение — сценарии Given-When-Then

> **У каждого «Тогда» назван ПРОИЗВОДИТЕЛЬ.** Где производителя нет, написано «заводит стадия
> S⟨N⟩» — это обязательство реализации, а не обещание прозой.

### Q-01 — величина объявлена посадкой и действует

**Дано** установка, где профиль домена `vpc` объявляет **все восемь** величин своего каталога
(иначе процесс не поднимается — `Q-02`), и среди них `vpc.network = 3`
**И** в проекте `P` создано 3 сети
**Когда** арендатор создаёт четвёртую
**Тогда** отказ `RESOURCE_EXHAUSTED` (HTTP **429**) с признаком `QUOTA_EXCEEDED`
**И** текст называет вид и величину
> **Производитель:** `kacho_quota_refuse` + `kacho_quota_count` — **есть**
> (`…/0041_quota_refusal_single_producer.sql`). Величина в строке учёта приходит из посадки —
> **заводит S2**.

### Q-02 — ЧАСТИЧНО объявленный набор есть ОТКАЗ СТАРТА

**Дано** профиль домена `vpc`, чей каталог несёт **8** видов
**И** профиль объявляет величины **семи** из них, а величину `vpc.gateway` не объявляет
**Когда** процесс поднимается
**Тогда** он **не поднимается**: страж старта отвергает посадку
**И** текст отказа называет **каждый** необъявленный вид и **ручку** каждого — не первый из
них: оператор, узнающий перечень по одному имени за перезапуск, платит перекатом за строку
**И** ни одно умолчание не подставлено ни одному из семи объявленных
> **Производитель:** **заводит S1**. Форма отказа — прецедент
> `PRO-Robotech/kaname:internal/apps/kaname/config/own_ceilings.go`, `Validate` (`multierr`, отказ по каждой
> величине сразу); норма — §2.2а; довод — `security-hardening.md` §«Контроль, у которого нет
> МЕХАНИЗМА исполниться» (половина пары хуже отсутствия обеих).
>
> **Здесь стоял другой сценарий, и он ПРОТИВОРЕЧИЛ Q-07 — нашёл это круг рецензии, не автор.**
> Прежняя редакция говорила «величина **хотя бы одного** вида не объявлена → не поднимается».
> Мир Q-07 (не объявлено **ни одной**) есть подмножество этого мира, а исходы противоположны:
> один вход, два требования. Хуже: оправдание Q-07 ссылалось на «объявленное намерение иметь
> потолки», **механизма которого в документе не было**, — то есть «Тогда» опиралось на
> производителя, которого никто не заводил (`multi-agent-flow-orchestration.md` §«Класс,
> отвергший работу ДВАЖДЫ»). Снято не ослаблением сценария, а **названным различающим
> предикатом** (§2.2а): судится не «есть ли пропуск», а **мощность** объявленного множества.

### Q-03 — ноль выразим и означает «этого вида не заводить»

**Дано** профиль объявляет **все восемь** величин каталога `vpc`, и среди них
`vpc.gateway = 0`
**Когда** арендатор создаёт первый шлюз в любом проекте
**Тогда** отказ `RESOURCE_EXHAUSTED` · `QUOTA_EXCEEDED`
**И** это отличимо от незаданной величины: незаданная роняет **старт** (Q-02), а не запрос
> **Производитель:** **заводит S1** (тип-указатель у величины) и **S2** (перенос в строку учёта).

### Q-04 — арендатор видит перечень, а не отказ

**Дано** установка, где профиль домена объявляет величины **всех** видов его каталога —
множество равно каталогу (§2.2а), авторитет величин по-прежнему отсутствует
**И** стадии `S1`, `S2` посажены
**Когда** клиент вызывает `GET /vpc/v1/quotas?projectId=P`
**Тогда** ответ **200** и строка на **каждый** вид каталога домена
**И** `source_scope` каждой строки — `DEFAULT`
**И** ответ **не** пуст и **не** содержит отказа `QUOTA_AUTHORITY_ABSENT`
> **Чем гейтится на входе:** `quotaread.Posture.Refusal()` — он стоит **первым** в
> `pkg/quota/quotapb/convert.go` `ListQuotas` и короткозамыкает полосу чтения. На входе этого
> сценария он обязан быть **пуст**, и пустым его делает посадка, вычисленная из **непустого
> объявленного множества** (§2.2в).
> **Производитель:** полоса чтения `PRO-Robotech/corelib:quota/quotaread` — **есть**; посадка
> как функция объявленного множества — **заводит S2**; `DEFAULT` в строке — **заводит S2**.
> Прецедент значения: `PRO-Robotech/kaname:internal/repo/kaname/pg/identity_quota_repo.go`, `scopeDefault`.
>
> **Здесь «Тогда» было НЕДОСТИЖИМО, и нашёл это круг рецензии, не автор** (§13.1, `Б1`).
> Прежняя редакция называла производителем полосу чтения и помечала строку §7.1 как **есть**;
> производитель и вправду существует, но на входе `Q-04` он **не исполняется** —
> `posture.Refusal()` непуст в любой поднявшейся установке, потому что читает ручку
> `quota.authority`, а не объявленные величины. Снято не ослаблением сценария, а **заведением
> производителя** (§2.2в) с адресом в стадии `S2`. Род — «„Тогда“ без производителя», тот же,
> что отверг линию дважды; перенос класса в форму работы — четвёртая колонка §7.1.

### Q-05 — посадка НЕ затирает поарендаторное перекрытие (проверка обратимости §2.1)

**Дано** строка учёта, чей `source_scope` — `PROJECT` (величина заведена будущим авторитетом)
**Когда** домен поднимается и переносит величину посадки в строки учёта
**Тогда** эта строка **не меняется**
**И** строки с `source_scope = 'DEFAULT'` — меняются
> **Производитель:** `PRO-Robotech/corelib:quota/projection.go` `precedencePredicate` — **есть** и уже
> ровно это делает. Сценарий утверждает, что перенос посадки идёт **через него**, а не
> безусловным `UPDATE`. **Заводит S2.**
>
> **Это несущий сценарий документа:** он и есть машинная проверка того, что решение В
> обратимо. Провались он — ось «перспективность» отвергает решение.

### Q-06 — вырожденная область объяснена словами, а не прочерком

**Дано** установка входа `Q-04` — множество объявленных величин равно каталогу, стадии `S1`,
`S2` посажены, поэтому витрина отвечает перечнем, а не отказом
**И** консоль, раздел «Квоты» проекта, все строки с областью `DEFAULT`
**Когда** человек читает столбец «Кто задал предел»
**Тогда** он видит словами, что величина есть умолчание установки
**И** столбец **не** снят и **не** показывает прочерк
> **Чем гейтится на входе:** тот же `posture.Refusal()` — консоли неоткуда взять строки, пока
> витрина отказывает. Достижимость «Дано» этого сценария есть **следствие** `Q-04`, и потому
> оно здесь выписано, а не подразумевается.
> **Производитель:** **заводит S3**. Довод — `ui.md` правило 6 (булево/вырожденное свойство
> называется следствием) и правило 9 (поле без источника не показывается — здесь источник
> **есть**, вырождено значение).
>
> **«Дано» было недостижимым по той же причине, что у `Q-04`** (§13.1, `Б1`): строк с областью
> `DEFAULT` консоль не получала бы вовсе. Правится тем же заведением производителя, своего
> предмета у этого сценария круг не нашёл.

### Q-07 — ПЕРВЫЙ положительный близнец Q-02: пустое множество законно

**Дано** профиль не объявляет **ни одной** величины — множество объявленных пусто
**И** авторитет величин отсутствует (`quota.authority: not-deployed`) — то есть величину не
объявляет **ни один** источник (§2.2в)
**Когда** процесс поднимается
**Тогда** он **поднимается**, и страж не говорит ничего
**И** поведение прежнее: списание идёт, отказа на пути мутации нет
**И** витрина отвечает отказом с признаком `QUOTA_AUTHORITY_ABSENT` — как сегодня
> **Чем гейтится на входе:** тот же `posture.Refusal()`, и здесь он обязан быть **непуст**.
> Это второй конец пары `Q-04`/`Q-07`: один производитель, два входа, два противоположных
> исхода — ровно то, чего у прежней редакции не было.
> **Производитель:** миграция безусловного списания и `Posture.Refusal()` — **есть**; то, что
> посадка остаётся отказной **по пустоте множества**, а не по ручке, — **заводит S2**.
>
> **`quota.authority` вернулся в «Дано», и это НЕ отмена прежнего изъятия.** Прежде он стоял
> там как объявитель обязательности величин — и был изъят верно: §5.3 запрещает связывать
> обязательность с этой ручкой, а сегодня `not-deployed` — единственное значение, при котором
> процесс поднимается (§1.2), то есть условие, истинное в каждой установке, ничего не
> различает. Теперь он стоит как **второй конъюнкт дизъюнкции источников** (§2.2в): пока
> авторитета нет, он по-прежнему не различает ничего и выписан ради полноты мира; появись
> авторитет — он станет различающим, и сценарий останется верным без правки.
>
> **Следствие для ПРИЗНАКА названо, а не умолчано.** Этот признак различается с соседним
> (`QUOTA_NOT_PROVISIONED`) тем, что у него **нет следующего шага**: «величину в такой
> установке завести негде, потому что заводить её некому»
> (`PRO-Robotech/corelib:quota/quotaread/posture.go`). Решение В эту посылку **сужает**: после S1 величину есть
> где завести — в профиле установки, — и следующий шаг у арендатора появляется («попросить
> оператора установки объявить величины»). Текст полосы поэтому пересматривает **S2**, а не
> S3, и это записано в её строке §8: на S2 меняется **причина**, которую текст называет
> («авторитета нет» → «эта установка величин не объявила»), а текст, называющий не ту причину,
> есть утверждение о продукте, а не проза. Сам токен на проводе не трогается: смена машинного
> признака — ломающее изменение контракта, и её предмет отдельный.
> Сценарий утверждает, что S1 **не делает** величины обязательными сам по себе. **Заводит S1.**
>
> **Почему это не ослабление Q-02, а его контроль.** Страж §2.2а судит **мощность**
> множества, и у такого стража отрицание пиннится **двумя** близнецами, а не одним: страж,
> падающий на «не полное», сломал бы Q-07; страж, падающий на «не пустое», сломал бы Q-08.
> Ни один близнец в одиночку предиката не закрепляет — поэтому их два.
>
> **Довод «иначе решение ломающее» остаётся, но он ВТОРОЙ, а не первый.** Первый — §2.2б:
> пустое множество не снимает ни одного действующего потолка, потому что действующих нет
> ни в одной установке (§1.3). Прежняя редакция называла первым доводом «объявленное
> намерение иметь потолки» — у этой фразы не было производителя, и она блокировала приёмку.
>
> **`quota.authority` ОБЯЗАТЕЛЬНОСТЬЮ величин по-прежнему НЕ управляет.** Различие, которое
> надо удержать: «Дано» называет её как **источник величины**, а не как объявитель того,
> обязаны ли величины быть объявлены. Второе связывало бы обязательность с ручкой, объявляющей
> соседа, — ровно то, что §5.3 отвергает, и это не отзывается. Что мощность множества решает,
> решает §2.2а; ручка в этом не участвует ни здесь, ни в `Q-02`, ни в `Q-08`.

### Q-08 — ВТОРОЙ положительный близнец Q-02: полный каталог молчит

**Дано** профиль объявляет величины **всех** видов каталога домена — множество равно каталогу
**И** величина хотя бы одного вида объявлена нулём (законное значение, Q-03)
**Когда** процесс поднимается
**Тогда** он поднимается, и страж не говорит ничего
> **Производитель:** **заводит S1**. Второй контроль к Q-02: без него отрицание зеленело бы
> на страже, отвергающем всё. Ноль в «Дано» стоит намеренно — он пинит, что страж считает
> **объявленность**, а не истинность величины: страж, считающий ноль незаявленным, прошёл бы
> Q-02 и Q-07 и упал бы здесь.

---

### §7.1. ПЕРЕЧЕНЬ «сценарий → производитель → ГЕЙТ ВХОДА» — предикат проверяемости §7

**Раздел заведён потому, что класс «„Тогда“ без производителя» отверг работу этой линии
ДВАЖДЫ**, и корпус велит переносить такой класс в форму работы, а не оплачивать третьим
кругом (`multi-agent-flow-orchestration.md` §«Класс, отвергший работу ДВАЖДЫ»). Рецензент
идёт по этой таблице — и **отдельно** по каждому сценарию, которого в ней нет.

**Четвёртая колонка добавлена ТРЕТЬИМ кругом, и это признание, что первых трёх не хватило.**
Перечень спрашивал, **существует ли** производитель по имени, и не спрашивал, **исполняется
ли** он на входе этого сценария. `Q-04` прошёл первую проверку и провалил вторую:
производитель существовал и был короткозамкнут (§13.1, `Б1`). Колонка «чем гейтится на входе»
делает второй вопрос обязательным — строка не заполняется, не назвав того, что стоит **до**
производителя и решает, дойдёт ли до него управление.

| сценарий | что обязан произвести | производитель | чем гейтится на входе ЭТОГО сценария | состояние |
|---|---|---|---|---|
| **Q-01** | отказ `RESOURCE_EXHAUSTED` · `QUOTA_EXCEEDED` на превышении | `services/*/internal/migrations/*_quota_refusal_single_source.sql` (+ `0041_quota_refusal_single_producer.sql` у vpc) | `kacho_quota_count` находит строку учёта с величиной; при её отсутствии списание безусловно (`…_quota_absent_authority_charges_unconditionally.sql`) — значит вход требует посаженного `S2` **и** заполненного профиля | **есть** |
| | величина в строке учёта приходит из посадки | — | тот же гейт: без него строки с величиной нет | **S2** |
| **Q-02** | отказ старта на промежуточном множестве, с именем каждого вида | — (форма: `PRO-Robotech/kaname:…/config/own_ceilings.go`, `Validate`) | ничем: страж мощности — **первый** читатель объявленных величин, до него в этом пути ничего не стоит | **S1** |
| **Q-03** | ноль отличим от незаявленного | — (тип-указатель; форма — тот же прецедент) | страж мощности `S1`: ноль обязан считаться **объявленным**, иначе вход `Q-03` роняет старт как промежуточный | **S1** |
| **Q-04** | `200` и строка на каждый вид каталога | `PRO-Robotech/corelib:quota/quotaread` | **`quotaread.Posture.Refusal()`** — первым стейтментом `ListQuotas` (`pkg/quota/quotapb/convert.go`), короткозамыкает полосу. На входе обязан быть **пуст**; пустым его делает посадка из непустого множества — §2.2в | **S2** |
| | `source_scope = 'DEFAULT'` в строке | — (значение: `PRO-Robotech/kaname:…/pg/identity_quota_repo.go`, `scopeDefault`) | тот же `Refusal()`: до строк управление не доходит, пока он непуст | **S2** |
| **Q-05** | строка области `PROJECT` не меняется переносом посадки | `PRO-Robotech/corelib:quota/projection.go`, `precedencePredicate` | ничем на пути: перенос идёт при старте домена, гейта перед ним нет. «Дано» конструируется посевом — `CHECK (source_scope IN …)` строку области `PROJECT` допускает | **есть** |
| | перенос идёт ЧЕРЕЗ неё, а не безусловным `UPDATE` | — | тот же: предикат старшинства и есть гейт строки | **S2** |
| **Q-06** | `DEFAULT` объяснён словами в консоли | — | **`posture.Refusal()`** у витрины: консоли неоткуда взять строки, пока он непуст. Вход `Q-06` есть выход `Q-04` | **S3** |
| **Q-07** | подъём при пустом множестве | страж §2.2а | ничем: страж — первый читатель | **S1** |
| | списание без отказа | `…/20260908041500_quota_absent_authority_charges_unconditionally.sql` | отсутствие строки учёта с величиной; на пустом множестве её не появляется и после `S2` | **есть** |
| | витрина отвечает `QUOTA_AUTHORITY_ABSENT` | `PRO-Robotech/corelib:quota/quotaread/posture.go`, `Posture.Refusal()` | он же, и здесь обязан быть **непуст** — второй конец пары с `Q-04` | **есть** |
| | посадка остаётся отказной ПО ПУСТОТЕ МНОЖЕСТВА, а не по ручке | — | вычисление посадки в композиционном корне (§2.2в) | **S2** |
| **Q-08** | подъём при полном множестве, включая величину-ноль | страж §2.2а | ничем: страж — первый читатель | **S1** |

**Как это читать.** «Есть» означает, что производитель **измерен в дереве** предикатом §1 и
сценарий проверяем сегодня. «S⟨N⟩» означает **обязательство стадии**, а не обещание прозой:
у каждой такой строки есть адрес в §8, и без неё стадия не production-complete. Колонка
гейта читается **до** колонки производителя: производитель, до которого управление не
доходит, состояния «есть» не даёт — этому и научил третий круг.

**«Ничем» в колонке гейта — ЗАКОННЫЙ ответ и не заглушка.** Он означает «на этом входе перед
производителем ничего не стоит», и он проверяем: для стража мощности это правда, потому что
он и есть первый читатель объявленных величин. Ответ «ничем» там, где гейт на самом деле
есть, — то же нарушение, что пустая клетка.

**Проверяемый признак самого перечня:** сценариев в §7 — **8**, строк таблицы — **14**, из них
«есть» — **4**, «заводит стадия» — **10**; сумма сходится без остатка. Строк больше, чем
сценариев, потому что у **четырёх** (Q-01, Q-04, Q-05, Q-07) обязательство составное и даёт
**6** добавочных строк. Ни одной строки без третьей и **четвёртой** колонки: строка без
третьей и была тем дефектом, ради которого раздел заведён, строка без четвёртой — тем, ради
которого он расширен.

**«Есть» стало на одну МЕНЬШЕ, и это главный числовой след правки.** Строка `Q-04` несла
**есть** и переведена в **S2**: производитель полосы чтения в дереве по-прежнему есть, но
сценарий сегодня **не проверяем** — до производителя не доходит управление. Число, упавшее
там, где работа сделана, и есть честный вид этой правки: перечень перестал считать
существование производителя достаточным.

> **Числа этого абзаца я сперва написал от руки и ошибся в обеих величинах** — стояло «есть 6,
> стадия 7» при измеренных 5 и 8. Поймано не перечитыванием, а прогоном предиката по
> собственной таблице **после** её написания. Предикат повторяем: отобрать строки блока §7.1,
> считать те, что кончаются `**есть** |`, и те, что кончаются `**S⟨N⟩** |`. Это ровно тот
> класс, который §1.4 уже ловил на видах, — и он повторился в том же документе.
>
> **Третий круг повторил его в ТРЕТИЙ раз, и я ошибся ДВАЖДЫ подряд — оба раза поймал прогон,
> ни разу чтение.** Сперва я оставил под таблицей прежние «13 · 5 · 8 · 5», которые моя же
> правка опровергла. Переписав их от руки на «14 · 5 · 9 · 6», я ошибся снова: прогон дал
> **14 · 4 · 10 · 6** — от руки я не заметил, что строка `Q-04` перестала быть «есть» по моей
> же воле. Урок записан рядом, а не вместо: число перемеряется **после** последней правки, и
> перемеряется **прогоном**; рукописная поправка к рукописному числу воспроизводит тот же
> класс, а не закрывает его.

---

## §8. Порядок работ — вынужденный, и обратный ломает установки

**Правило: сперва величина становится ВЫРАЗИМОЙ, потом ДЕЙСТВУЮЩЕЙ, потом ВИДИМОЙ.**

| стадия | что делает | почему не раньше и не позже | production-complete сама по себе? |
|---|---|---|---|
| **S1** | величины видов становятся выразимы посадкой: ручка на вид, **страж мощности** (пусто либо каталог целиком, §2.2а), ноль отдельно от отсутствия. Профили **не заполняются** | до S2 обязательно: перенести в строку учёта нечего, пока величина не выразима. Поведение продукта **не меняется ни на байт**, и это свойство держит именно страж мощности: пустое множество остаётся законным — Q-07 | **да**: ручки заведены, страж заведён и пиннится ДВУМЯ близнецами (Q-07, Q-08), отложенного нет |
| **S2** | величина посадки попадает в строку учёта — **через** проекцию старшинства, а не безусловным `UPDATE`; `source_scope = 'DEFAULT'`. **И там же — посадка витрины становится функцией объявленного множества** (§2.2в), а текст полосы `QUOTA_AUTHORITY_ABSENT` пересматривается под новую причину отказа | после S1 (нечего переносить) и до S3 (нечего показывать). Обратимость решения проверяется здесь — Q-05 | **да**: перенос покрыт, старшинство соблюдено, витрина не отстаёт от того, что установка делает |
| **S3** | консоль перестаёт быть вырожденной: `DEFAULT` объяснён словами (Q-06). Страница `quotas.mdx` называет, кто меняет величину. Машинный токен на проводе **не трогается** — его смена ломающая и предмет у неё свой | последней: до S2 показывать нечего | **да** |
| **S4** | профили установок заполняются величинами, **заведомо превышающими нынешнее потребление** | последней и **по одной установке**: это единственная стадия, меняющая наблюдаемое поведение мутаций (§6) | **да**: посев профиля — `data-integrity.md` §«Данные СТЕНДА заводятся посевом» |

**Обратный порядок ломает установки:** заполнить профили (S4) прежде, чем величина выразима
и перенесена (S1–S2), нельзя — ручек нет; перенести (S2) прежде, чем заведён отказ старта
(S1), значит подставить умолчание молча.

**Почему посадка витрины — предмет S2, а не S3 и не S1: довод ВЫВЕДЕН, а не выбран.**

- **не S1:** там величина ещё не доезжает до строки учёта, и витрина ответила бы `200` с
  перечнем без величин — то есть сказала бы «потолки объявлены» раньше, чем они объявлены
  хоть чему-нибудь;
- **не S3:** ручки существуют с S1, и **S4 есть наш порядок выкатки НАШИХ установок, а не
  запрет оператору**. После S2 всякий оператор вправе заполнить профиль — и получил бы
  потолок, который отвергает мутацию (`Q-01` уже работает) и **не виден** витрине. Это
  предмет открытой `kacho#705`, заведённый нашей же рукой;
- **значит S2** — та же стадия, что заводит отказ мутации из посадочной величины. Свойство
  «потолок, который отвергает, обязан быть виден» держится **одной** стадией с тем, что
  делает потолок действующим, иначе между стадиями есть окно, в котором оно нарушено.

---

## §9. Out-of-scope — названо, чтобы не переоткрывали

| предмет | почему не здесь | где его дом |
|---|---|---|
| **поарендаторная величина** (`ACCOUNT`/`PROJECT`) | возможность без потребителя (§5.2); полосы остаются свободными и перекрывают посадку без отзыва | своя задача, по внешнему предикату §5.2 |
| **счётчик, из которого вычитают** | решено и не пересматривается: лежит в базе ресурса, списывается в той же транзакции | `data-integrity.md` §«Счётчик потолка живёт РЯДОМ С РЕСУРСОМ» |
| **дом каталога видов** (`ПР-10` из `KAN-QUOTA-1`) | **беспредметен при исходе В** — и это ИЗМЕРЕНО, а не выведено: см. §9.1 | закрывается этим решением; см. §9.1 и §10 |
| **кто отвергает вид вне каталога** (`ПР-10`) | вид, которого не списывает триггер, видом не является. Отвергает **гейт дерева**, а не рантайм — §9.1 | там же |
| **сведение пяти рукописных таблиц учёта** | проектная работа, а не рендер: пять файлов дают пять разных хешей после нормализации | `kacho#411` |
| **носитель, отличный от проекта, в полосе чтения** | другой предмет: здесь про источник **величины**, там про **носителя** | `kacho#705` |
| **витрина квот у личности без проектов** | другой носитель | `kacho#622` |

### §9.1. Почему `ПР-10` закрывается этим решением — замер, а не рассуждение

`KAN-QUOTA-1` оставил открытым: **где живёт каталог видов** после ухода авторитета и **кто
отвергает вид вне каталога**. При исходе В у обоих вопросов нет предмета, и вот почему.

**Каталог уже распределён по владельцам, и множества СОШЛИСЬ у всех пяти.** Сверены две
стороны: что домен **спрашивает** у совещательной полосы и что **списывает** его триггер.

```sh
# сторона вопроса — ОБЕ формы вызова и класс `.`, а не `[^)]`
git grep -hoE '(Admit|AdmitCarrier)\(ctx,.*"[a-zA-Z0-9.]+"' origin/main \
  -- "services/$s/internal/" ':!*_test.go' | grep -oE '"[a-zA-Z0-9.]+"'
# сторона списания — предикат §1.4, терпимый к переносу строки
```

| домен | спрашивается | списывается | расхождение |
|---|---:|---:|---|
| vpc | 8 | 8 | — |
| compute | 3 | 3 | — |
| storage | 3 (через **именованные константы**) | 3 | — |
| nlb | 3 | 3 | — |
| registry | 2 | 2 | — |

**Следствие:** центральный каталог заводить некуда и незачем — он стал бы **вторым местом об
одном предмете**, а первое уже держится тем, что вид пишется там, где он списывается. Вид вне
каталога отвергается **гейтом дерева**, а не проверкой во время работы: несуществующий вид не
списывает никто, и строки учёта у него не заводится.

> **Два «расхождения» этого замера оказались слепотой МОЕГО предиката, а не дефектами дерева**,
> и оба стоит назвать, потому что следующий повторит их: однострочная форма поиска списания
> теряла перенос строки (§1.4), а класс `[^)]` в форме поиска вопроса не дотягивается до вида —
> аргумент проекта сам содержит скобки (`string(n.ProjectID)`). О втором **прямо предупреждает
> шапка** `internal/repohygiene/quotaadmitkinds_test.go`, и я воспроизвёл его, прочитав её.

**Что при этом НАЙДЕНО и предметом этой приёмки не является** (две находки, обе про гейт
сходимости, обе заведены отдельно — §11):

1. гейт судит **один домен из пяти** — координаты `quotaTriggerKindsFile` и
   `quotaAdmitCallDir` выписаны на `vpc`. Вне его взгляда **11 видов из 19**;
2. его шапка объявляет предпосылкой свойство дерева — «в этом дереве виды пишутся литералами,
   первое же отступление станет находкой для читателя». Отступление **есть**: `storage` подаёт
   вид именованной константой (`quota.KindVolumes`), — и оно лежит **вне** охвата гейта, то
   есть находкой не стало ни для кого. Предпосылка верна относительно популяции, на которой
   гейт писался (`testing.md` §«Гейт на класс», п. 3).

---

## §10. Чего этот документ НЕ утверждает

- **не утверждает, что исход Б неверен** — он отложен с названным внешним предикатом (§5.2), а
  не отвергнут по существу;
- **не утверждает, что потолки начнут действовать вливанием этой приёмки** — они начнут
  действовать на стадии S4, по одной установке;
- **не отменяет ни одного решения предшественников** — `Д1`, `Д2`, `П25` `KAN-QUOTA-1`
  остаются в силе целиком; `Д2` («домен величин не развёрнут») остаётся законной посадкой и
  после этого решения (Q-07);
- **не проводил имитацию клиента в полном соответствии правилу** — §4 говорит об этом первой
  строкой; четыре прогона отдельными исполнителями с пустым контекстом остаются за рецензентом;
- **не утверждает, что величины обязательны всегда** — обязательным является не объявление
  величины, а **связность** объявленного: пусто либо каталог целиком (§2.2а). Установка,
  не объявившая ничего, законна и после этого решения;
- **не утверждает, что пустое множество НЕ является умолчанием** — является, и §2.2б говорит
  это первой строкой; названа причина, по которой здесь оно не снимает ничего, а не причина,
  по которой оно умолчанием не считается;
- **не решает судьбу машинного признака `QUOTA_AUTHORITY_ABSENT`** — решение сужает его
  посылку и меняет **условие**, при котором полоса срабатывает (пустота объявленного
  множества вместо значения ручки, §2.2в); текст полосы пересматривает **S2**, сам токен на
  проводе остаётся (Q-07). Смена токена ломает контракт и предмет у неё свой;
- **не утверждает, что витрина начнёт отвечать перечнем у ВСЯКОЙ установки** — только у той,
  что объявила каталог целиком. Установка с пустым множеством продолжает получать отказ, и
  это верно, а не остаток (Q-07);
- **не сверял числа §1 со свежим `origin/main`** — **пункт ОТОЗВАН**: ссылки свежие, сверка
  сделана, разбор моей ошибки чтения стоит в шапке. Оставлен строкой, а не удалён, потому что
  утверждение о непроверенности успело прожить редакцию, и следующий читатель обязан увидеть,
  что оно снято, а не обнаружить его исчезновение.

---

## §11. Находки по дороге — заведены отдельно, в предмет приёмки не входят

| # | находка | признак | почему не чинится здесь |
|---|---|---|---|
| **Н1** | `KAN-QUOTA-1` (APPROVED) утверждает для полосы отсутствия авторитета `UNIMPLEMENTED` / HTTP **501** в **7** строках, включая сценарий `KAN-Q4-04`; дерево производит `FAILED_PRECONDITION` / HTTP **400** | `grep -c UNIMPLEMENTED docs/specs/sub-phase-KAN-QUOTA-1-*.md` → **7**; контроль в обратную сторону: `git grep -c Unimplemented origin/main -- quota/` в фундаменте → **0**; производитель — `PRO-Robotech/corelib:quota/quotaread/posture.go` `Posture.Refusal()` | чужая приёмка, чужой предмет: правка её вердикта — круг её рецензента, а не эта полоса. Сценарий `KAN-Q4-04` сегодня **без производителя** |
| **Н2** | гейт сходимости видов судит **1 домен из 5**; вне охвата **11 видов из 19** | `git show origin/main:internal/repohygiene/quotaadmitkinds_test.go \| grep -n 'const quota(TriggerKindsFile\|AdmitCallDir)'` → обе координаты на `vpc` | расширение охвата — своя работа со своей инъекцией по каждому владельцу |
| **Н3** | предпосылка того же гейта («виды пишутся литералами») опровергнута `storage` — именованные константы; отступление вне охвата, поэтому находкой не стало | `git grep -n 'KindVolumes = ' origin/main -- services/storage` | закрывается вместе с Н2: расширенный охват обязан знать **обе** формы записи вида |

**Ни одна из трёх не блокирует это решение** и ни одна не меняет его доводов: `Н1` про чужой
документ, `Н2`/`Н3` про охват гейта, а не про свойство, которое он стережёт, — свойство
**выполняется у всех пяти** (§9.1).

---

## §12. Что правится ВМЕСТЕ с одобрением — и почему не раньше

Три места в дереве утверждают, что развилка **открыта**. Сегодня это **правда**, и править их
до вердикта `acceptance-reviewer` значило бы объявить решение принятым, не будучи одобренным —
ровно тот класс, который корпус ловит.

**Строк в таблице пять, а не три, и рамка раздела шире его заголовка.** Две последние — не про
открытость развилки, а про **тексты, которые решение делает неверными**: одна ложной целиком,
другая — неполной в части причины и следующего шага. Их адрес не «вместе с одобрением», а
«вместе со стадией», и стадия названа в их клетках. Рамку расширяю, а не подгоняю строки под
неё: подогнанная строка потерялась бы.

| место | что там стоит | что станет верным после APPROVED |
|---|---|---|
| `.claude/rules/polyrepo.md`, врезка о снятом ребре величин | «Судьба авторитета величин ведётся задачей продукта **#2190** и владельцем не решена» | решена; исход В; посадка заполняет полосу `DEFAULT`, полосы `ACCOUNT`/`PROJECT` остаются свободными |
| `services/⟨домен⟩/docs/content/api/quotas.mdx`, §«Кто и чем меняет величину» | «Сегодня — никто, и это не временная неисправность» | «оператор установки, выкаткой»; правится **стадией S3**, не раньше — до S2 это было бы обещанием |
| `proto/kacho/cloud/⟨домен⟩/v1/quota_service.proto`, комментарий службы | «The fate of the authority is decided by PRO-Robotech/kacho#2190» | ссылка на эту приёмку |
| **`PRO-Robotech/corelib:quota/startsync.go`, строка журнала на полосе отсутствия авторитета** | «no ceiling is stateable in this installation» | **утверждение становится ЛОЖНЫМ**: потолок объявим — посадкой. Правится **вместе с S1**, у которого этот текст и есть предмет; вхождение в фундаменте **одно** |
| **`PRO-Robotech/corelib:quota/quotaread/posture.go`, проза отказа витрины** | «…no limit authority is deployed, so no ceiling applies to any kind and no mutation is refused for exceeding one» | **утверждение остаётся истинным, но перестаёт называть ПРИЧИНУ и следующий шаг**: после S1 причина отказа — «эта установка величин не объявила», а следующий шаг у арендатора появляется (попросить оператора объявить). Правится **вместе с S2**, где меняется условие срабатывания полосы (§2.2в). Оставленная без правки, она отправляет арендатора в бездействие там, где действие есть, — то же семейство, что строка выше, только адресат не оператор, а арендатор |

> **Четвёртая строка найдена ПОЗЖЕ трёх первых, и найдена не чтением документа, а проверкой
> предупреждения самой задачи.** `#2190` предостерегает: «сперва перестают спрашивать, потом
> снимается отвечающий», — иначе мутации пяти доменов встанут. Порядок соблюдён **не был**:
> отвечающий снят (`#2117`), а спрашивающие провязаны у всех пяти (§1.1). Мутации не встали
> потому, что `StartLimitSync` на объявленном отсутствии тянущего не заводит и путь запроса
> не гейтит, — то есть опасность сняла **ручка**, а не порядок работ. Читая её ветку, я и
> увидел строку журнала, которую решение делает ложной.

**Признак, по которому это проверяется:** после одобрения `grep -rn '2190'` по трём деревьям
не должен возвращать ни одного места, утверждающего **нерешённость**. Свидетельства о прошлом
(записка хранилища, предшествующая приёмка) переписывать нельзя — они верны о своём дне.

---

## §13. История ревью — записи ДОБАВЛЯЮТСЯ, прежние не переписываются

Каждая редакция получает **соседнюю** запись. Перезапись стирает единственное свидетельство
того, что редакция рассматривалась: вердикт привязан к **содержимому**, а не к имени файла
(`change-graph.md` §«Вердикт привязан к ОТПЕЧАТКУ»).

### §13.1. Круг `acceptance-reviewer`, 2026-09-14 — ❌ CHANGES REQUESTED

**Рассмотренная редакция:** вершина `1cf8e896` ветки `lane/wc-quota-approve`.
**Ревизии сверки рецензента — СВОИ, а не взятые из шапки:** продукт
`PRO-Robotech/kacho` @ `13bdec4175` (сверено: это ровно `origin/main` в копии рецензента,
`git rev-parse origin/main`), служба доступа `PRO-Robotech/kaname` @ `b673325f`, фундамент
`PRO-Robotech/corelib` @ `fef214c`.

#### Блокирующее Б1 — витрина: «Тогда» без производителя, а названный производит ОБРАТНОЕ

**Затронуто:** `Q-04` целиком · `Q-06` (его «Дано» становится недостижимым) · правая колонка
§6 · строка `Q-04` в §7.1, помеченная **есть** · стадии `S3`/`S4` в §8.

**Признак.** `Q-04` требует `200`, строку на каждый вид и **отсутствие** отказа
`QUOTA_AUTHORITY_ABSENT`. `Q-07` требует **того же самого отказа**. Эти два мира различаются
**только** объявленным множеством величин — а производитель отказа объявленных величин **не
читает вовсе**: он читает ручку `quota.authority`, у которой в обоих мирах одно и то же
значение. Один производитель, один вход — значит один ответ, а сценарии требуют
противоположных.

**Предикат — три шага, воспроизводится за минуту:**

```sh
# 1. чем питается посадка витрины: пять доменов, два аргумента, величин среди них нет
git -C project/kacho grep -ho 'ReadPosture([a-zA-Z]*, "[a-z]*")' origin/main \
    -- 'services/*/cmd/**' ':!*_test.go'
#    -> ReadPosture(authority, "vpc") и ещё четыре той же формы

# 2. что она из него выводит — БЕЗУСЛОВНО
git -C project/corelib show origin/main:quota/authority.go | sed -n '/func ReadPosture/,/^}/p'
#    -> a.Deployed() ? AuthorityDeclared() : AbsentAuthority(serviceDomain)

# 3. отказ короткозамыкает ДО полосы чтения
git -C project/kacho show origin/main:pkg/quota/quotapb/convert.go | grep -n 'Refusal()'
#    -> convert.go:64  if refusal := posture.Refusal(); refusal != nil { return nil, refusal }
```

**Почему `a.Deployed()` ложно в КАЖДОЙ поднявшейся установке — и это уже измерено самой
приёмкой, только следствие не доведено.** `ResolveAuthority` возвращает `Authority` без
ошибки ровно на двух ветках: `not-deployed` (даёт `Authority{}`, то есть `deployed == false`)
и адрес (даёт `deployed == true`) — а адрес отвергается стартом у всех пяти
(`quotaedge.ValidateAuthorityHasAProducer`, §1.1: производителей **0**). Значит единственная
поднимающаяся посадка даёт `deployed == false` → `AbsentAuthority` → `Refusal()` непуст →
`ListQuotas` возвращает `FAILED_PRECONDITION` **до** полосы. §1.2 и примечание `Q-07` это
уже утверждают («`not-deployed` есть единственное значение, при котором процесс вообще
поднимается»); не доведено следствие: **из этого же факта витрина отказывает НАВСЕГДА, у
всякой посадки, включая объявившую каталог целиком**.

**Где именно приёмка утверждает недостижимое:**

| место | что утверждает | что производит дерево |
|---|---|---|
| §6, правая колонка | `GET /⟨домен⟩/v1/quotas` → **200** | `FAILED_PRECONDITION` · `QUOTA_AUTHORITY_ABSENT` |
| §6, под таблицей | «Различие между колонками производит **S4**» | `S4` заполняет профиль величин и `quota.authority` не трогает |
| §7.1, строка `Q-04` | «`200` и строка на каждый вид каталога» → `corelib:quota/quotaread` → **есть** | производитель **есть**, но на входе `Q-04` он короткозамкнут и не исполняется |
| §8, `S3` | текст полосы пересматривается, **токен остаётся** | согласуется с `Q-07` и **противоречит** `Q-04` |

**Предмет — ОДИН и он маленький:** посадка витрины обязана стать функцией **объявленного
множества величин**, а не ручки `quota.authority`. Сегодня такого производителя нет ни в
одной стадии: `S1` объявлен как «поведение продукта не меняется ни на байт», `S2` — перенос
в строку учёта, `S3` — проза и консоль при сохранённом токене, `S4` — заполнение профиля.

**Исходов три, четвёртого нет** (какой выбрать — предмет автора, не рецензента):

1. **завести производителя и назвать стадию** — `ReadPosture` (либо его вызывающий) читает
   объявленное множество: пусто → `AbsentAuthority` (`Q-07` сохраняется дословно), равно
   каталогу → `AuthorityDeclared` (`Q-04` становится достижим). Тогда `S3` перестаёт быть
   местом, где полоса лишь переименовывается, и у §6 появляется производитель различия колонок;
2. **переписать `Q-04`, `Q-06` и §6 под производимое** — витрина продолжает отказывать, и
   решение В наблюдаемо арендатору **только** отказом мутации (`Q-01`), а не перечнем. Это
   законный исход, но он меняет §6 целиком и обязан быть назван решением, а не умолчанием;
3. **снять `Q-04`/`Q-06` вместе с обещанием витрины**, объявив её отдельным предметом со
   своей задачей и предикатом.

«Оставить как есть» исходом не является: приёмка обещала бы возможность, неисполнимую ни при
каком входе (`api-conventions.md` §«Неисполнимая возможность»).

#### Род назван прямо — он ТОТ ЖЕ, что отверг эту линию дважды

Род — **«Тогда» без производителя**. §7.1 заведён ровно против него и свою работу сделал
наполовину: он спрашивает, **существует ли** производитель по имени, и не спрашивает,
**исполняется ли** он на входе этого сценария. `Q-04` прошёл первую проверку и провалил
вторую — производитель существует и короткозамкнут.

**Перенос класса в форму работы** (`multi-agent-flow-orchestration.md` §«Класс, отвергший
работу ДВАЖДЫ»): в §7.1 добавляется **четвёртая** колонка — «чем гейтится на входе этого
сценария». Строка `Q-04` тогда не могла бы получить **есть**, не назвав `posture.Refusal()`,
а назвав его — обнажила бы, что на входе `Q-04` он непуст. Это дешевле третьего круга и
ловит класс у следующего сценария, а не у этого.

#### Замечание З1 (НЕ блокирующее) — мощность множества не названа в «Дано» у `Q-01` и `Q-03`

**Признак.** §2.2а делает мощность объявленного множества решающей: промежуточное — отказ
старта. «Дано» `Q-01` («профиль объявляет `vpc.network = 3`») и `Q-03` («профиль объявляет
`vpc.gateway = 0`») о мощности молчат. Прочитанные буквально — одна величина из восьми, —
они описывают мир, где процесс **не поднимается**, и сценарий недостижим.

**Почему не блокирующее.** Достраивание однозначно: §2.2а допускает ровно одно состояние, в
котором процесс поднимается и величина действует, — каталог объявлен целиком. То есть мир
`Q-01` существует, он просто не выписан. Правка — одна оговорка в «Дано» обоих
(«…профиль объявляет **все восемь** величин каталога, среди них `vpc.network = 3`»).

#### Что рецензент проверил ЭКСПЕРИМЕНТОМ и что подтвердилось

Числа §1 воспроизведены **своими** прогонами на `13bdec4175`, не прочтением:

| утверждение | предикат рецензента | исход |
|---|---|---|
| синхронизатор у пяти · ручка у пяти · чтение у пяти · производителей ноль | предикаты §1.1 дословно | **5 · 5 · 5 · 0** — сошлось |
| пять профилей объявляют `not-deployed` | предикат §1.2 | пять раз — сошлось |
| видов девятнадцать, распределение 8·3·3·3·2 | предикат §1.4 (терпимый к переносу) | **19**, распределение сошлось |
| старшинство `DEFAULT` 1 < `ACCOUNT` 2 < `PROJECT` 3 | `quota/limitsync.go`, `rank` | сошлось |
| проекция области `DEFAULT` правит только строки `DEFAULT` | `quota/projection.go`, `precedencePredicate` | сошлось дословно |
| `Q-01`: отказ несёт вид и величину МАШИННО | `kacho_quota_refuse` + `quotadetail.MetadataFromError`; гейт `clienttruth_quota_refusal_metadata.go` | производитель **есть** |
| `Q-05`: «Дано» **конструируемо посевом**, а не запрещено схемой | `CHECK (source_scope IN ('DEFAULT','ACCOUNT','PROJECT'))` в `0040_project_resource_quotas.sql` и `353002_nested_quota_carrier.sql` | строка области `PROJECT` **сеется** — сценарий достижим |
| три области входа `Q-02`/`Q-07`/`Q-08` не пересекаются и покрывают всё | мощность: `0` · `(0,N)` · `N` при `M ⊆ каталог` by construction (ключи выводятся из таблицы, прецедент `OwnCeilingKnobs`) | **противоречие прошлого круга снято ПО СУЩЕСТВУ** |
| `quota.authority` в «Дано» `Q-07` был бы неразличающим | обе ветки разбора прочитаны: поднимается только `not-deployed` | изъятие ручки из «Дано» **верно** |

**Отдельно подтверждено то, что автор оспорил у прошлого круга.** Автор был прав: `Q-02` не
«недостижим by construction» — «Дано» `Q-07` есть конъюнкция, и универсален лишь первый её
конъюнкт. Проверено по действующей редакции: `Q-02` описывает мощность `7` из `8`, `Q-07` —
`0`; пересечения нет. Возражение прошлого круга в этой части **снято правильно**.

#### Ошибки рецензента — называю первыми, как требует норма

1. **Мой первый предикат на производителя `Q-01` дал ноль по слепоте, а не по дереву.** Я
   искал `QUOTA_EXCEEDED` только в миграциях (`-- 'services/*/internal/migrations/*.sql'`) и
   получил **0**, тогда как по дереву продукта файлов **45**. Признак полосы производится в
   Go (`serviceerr/quota.go`), а не в SQL; SQL производит `DETAIL` с величинами. Поймано не
   перечитыванием, а расхождением с утверждением приёмки — то есть ровно тем механизмом,
   который `testing-verdict.md` §14 называет единственным рабочим детектором.
2. **Я едва не записал в блокирующие `З1`.** Проверив достраивание, увидел, что оно
   однозначно, и понизил до замечания: возражение без опровергающей команды — мнение, а
   «Дано» неполно, но не противоречиво.

#### Чего этот круг НЕ проверял — названо, а не подразумевается

- **имитация клиента четырьмя исполнителями с пустым контекстом** (§4, §10) — не прогонялась
  и этим кругом; приёмка честно объявляет это своим остатком, и он остаётся остатком;
- **истинность величин §9.1** (что домен спрашивает ровно то, что списывает) — предикаты
  прочитаны, самостоятельно по всем пяти не перемерялись; на вердикт не влияет: §9.1 —
  обоснование `Out-of-scope`, а не сценарий;
- **находки `Н1`–`Н3` §11** — приняты как заведённые отдельно, по существу не судились.

**Что делать автору:** закрыть `Б1` одним из трёх названных исходов, учесть `З1`, добавить
четвёртую колонку в §7.1 — и вернуть на круг. Запись §13.1 при этом **не правится**: новая
редакция получает запись §13.2.

### §13.2. Ответ автора на круг §13.1, 2026-09-14 — редакция вторая, возвращена на круг

**Запись §13.1 не правлена ни одним символом** — вердикт привязан к содержимому, а не к имени
файла (`change-graph.md` §«Вердикт привязан к ОТПЕЧАТКУ»). Эта запись — соседняя.

**Посылку рецензента я перемерил своими прогонами, а не принял на слово** — его возражение
тоже утверждение (`git-issues-issue-lifecycle.md` §«Возражение рецензента — ТОЖЕ утверждение»).
**Она подтвердилась целиком, всеми тремя шагами:**

| шаг предиката | что вернул мой прогон |
|---|---|
| чем питается посадка витрины | пять вызовов `corequota.ReadPosture(authority, "<домен>")` в корнях сборки пяти доменов; объявленных величин среди аргументов **нет** |
| что она выводит | `a.Deployed() ? AuthorityDeclared() : AbsentAuthority(serviceDomain)` — **безусловно** |
| где короткозамыкает | `pkg/quota/quotapb/convert.go`, `ListQuotas`: `if refusal := posture.Refusal(); refusal != nil { return nil, refusal }` — **до** полосы чтения и до проверки её провязки |

Сверх названного рецензентом я прочитал обе ветки `ResolveAuthority` и убедился, что
`deployed == true` достижимо **только** адресом, а адрес отвергается стартом
(`quotaedge.ValidateAuthorityHasAProducer`). То есть `Refusal()` непуст **в каждой
поднявшейся установке**, и `Q-04` был недостижим не «пока», а **ни при каком входе**.

**Выбран исход 1 из трёх — производитель заведён, стадия названа (`S2`).** Довод, которого у
круга не было, и он мой:

> **Исход 2 («витрина продолжает отказывать») не является законным, хотя круг счёл его
> таковым.** Он оставил бы на пути запроса отказ, чья проза дословно утверждает
> `no ceiling applies to any kind and no mutation is refused for exceeding one` — тогда как в
> установке, прошедшей `S4`, потолок применяется и мутация отвергается (`Q-01`). Оба
> придаточных стали бы **ложными**, и читает их не оператор в журнале, а **арендатор в ответе
> на свой запрос**. Это то же семейство, которому §12 уже подчинила строку журнала фундамента,
> только дороже на порядок. Вторая, независимо достаточная причина: исход 2 заводит нашей
> рукой предмет **открытой** `kacho#705` — «потолок, который отвергает арендатора, арендатору
> не виден» (§1.7). Исход 3 отвергнут первой же причиной: снятие сценариев не снимает ложного
> текста — его производит дерево, а не сценарий.

**Что правлено** (предмет — годность приёмки, решение В не трогалось):

| место | правка |
|---|---|
| **§2.2в** (новый) | посадка витрины — функция **источников величины**, а не ручки: отказ ровно тогда, когда величину не объявляет **ни один**. Таблица трёх миров, предикат, доводы «не третье значение ручки», «не пара, способная разойтись», «читается при старте» |
| **§2.2в-1** (новый) | почему исходы 2 и 3 отвергнуты — цитата прозы отказа и `kacho#705` |
| **§6** | «различие производит S4» заменено на **две** стадии: S2 заводит производителя, S4 заполняет профиль. Названо, что прежнее утверждение было неверным и кем найдено |
| **`Q-04`** | «Дано» называет полный каталог и посаженные `S1`/`S2`; добавлен гейт входа; состояние **есть → S2** |
| **`Q-06`** | «Дано» стало достижимым: оно есть выход `Q-04` |
| **`Q-07`** | `quota.authority` вернулся в «Дано» — **как второй конъюнкт дизъюнкции источников**, а не как объявитель обязательности; пересмотр текста полосы переехал `S3 → S2` |
| **`Q-01`, `Q-03`** | замечание `З1`: «Дано» называет мощность («все восемь величин каталога») |
| **§7.1** | **четвёртая колонка** «чем гейтится на входе ЭТОГО сценария», заполнена по каждой строке; числа перемерены прогоном |
| **§8** | `S2` несёт посадку витрины и текст полосы; `S3` сведён к консоли и документации; выведен довод, почему именно `S2` |
| **§10** | оговорка про токен уточнена: меняется **условие** срабатывания полосы; добавлено, чего документ не утверждает про витрину |
| **§12** | пятая строка — проза отказа витрины; рамка раздела расширена честно |
| **§4.2** | находка направления «разработчик» получила адрес стадии: прежде абзац обещал снятие отказа витрины без производителя |

**Перенос класса в форму работы — сделан тем способом, который назвал сам круг.** Четвёртая
колонка §7.1 спрашивает не «существует ли производитель», а «что стоит **до** него на входе
этого сценария». `Q-04` в ней не может получить «есть», не назвав `posture.Refusal()`, а
назвав — обнажает, что на его входе он непуст. Цена третьего круга оплачена один раз.

**Мои ошибки этого захода — называю первыми:**

1. **Числа §7.1 я написал от руки и ошибся ДВАЖДЫ ПОДРЯД.** Сперва оставил под правленной
   таблицей прежние `13 · 5 · 8 · 5`; поправив их от руки на `14 · 5 · 9 · 6`, ошибся снова —
   не заметил, что строка `Q-04` перестала быть «есть» **по моей же воле**. Верное `14 · 4 ·
   10 · 6` дал прогон предиката по собственной таблице. Рукописная поправка к рукописному
   числу воспроизводит класс, а не закрывает его.
2. **Я едва не оставил текст отказа витрины без адреса.** Заведя производителя, я сперва
   записал пересмотр прозы туда же, где он стоял, — в `S3`. Это было бы окном, в котором
   полоса срабатывает по новому условию и объясняет себя по старому. Довод, что адрес — `S2`,
   выведен из того, что **ручки существуют с S1, а `S4` есть наш порядок выкатки, а не запрет
   оператору**: оператор вправе заполнить профиль сразу после `S2`.

**Чего этот заход НЕ сделал — названо, а не подразумевается:**

- **имитация клиента четырьмя исполнителями с пустым контекстом** (§4, §10) не прогонялась и
  этим заходом — инструмента порождения подчинённого агента у полосы нет. Остаток остаётся
  остатком, и он тот же, что назвал круг;
- **находки `Н1`–`Н3`** (§11) не двигались: чужой документ и охват чужого гейта;
- **истинность величин §9.1** самостоятельно по всем пяти не перемерялась — предикаты
  прочитаны, на вердикт они не влияют;
- **свежесть `origin/main` продукта перемерена**: `13bdec4175` — ровно то, что держит
  `origin/main` в копии этой полосы, числа §1 верны для него. У службы доступа ствол ушёл
  вперёд (`b41f8f98`), но `b673325f` остаётся его предком, и все координаты прецедента в нём
  резолвятся — цитаты верны для названной ревизии.

**Вердикт по-прежнему за `acceptance-reviewer`.** Автор приёмку не одобряет и одобрить не
может (ban #1).

### §13.3. Круг `acceptance-reviewer`, 2026-09-14 (второй) — ❌ CHANGES REQUESTED

**Рассмотренная редакция:** вершина `99525b80` ветки `lane/wd-quota-approve-2`.
**Ревизии сверки — СВОИ, снятые этим кругом:** продукт `PRO-Robotech/kacho` @ **`3eeeef0a`**
(`git ls-remote origin refs/heads/main`), служба доступа `PRO-Robotech/kaname` @ `b673325f`,
фундамент `PRO-Robotech/corelib` @ `origin/main`.

**Ствол продукта ушёл вперёд ревизии автора (`13bdec4175` → `3eeeef0a`, 6 коммитов), и это
числа §1 НЕ трогает** — проверено, а не предположено:
`git diff --stat 13bdec4175 origin/main -- 'pkg/quota/**' 'services/*/internal/migrations/*quota*' 'services/*/cmd/**'`
→ **пусто**. Все замеры ниже сняты на `3eeeef0a`, и они сходятся с объявленными.

#### Что подтвердилось — прогонами, а не прочтением

| утверждение автора | мой предикат | исход |
|---|---|---|
| `Б1` закрыто заведением производителя | §2.2в читается как дизъюнкция источников; `Q-04`/`Q-07` получают **разные** исходы на **разных** входах при **одном** производителе | **снято по существу** |
| запись §13.1 не правлена ни одним символом | блок между заголовками §13.1 и §13.2, срез хвостовых пустых строк, `sha256sum` обеих редакций | `1c53a82a…` = `1c53a82a…` — **сошлось** |
| §1.1 — 5 · 5 · 5 · 0 | предикаты §1.1 дословно | **сошлось** |
| §1.2 — пять профилей `not-deployed` | предикат §1.2 | **сошлось** |
| §1.4 — 19 видов, 8·3·3·3·2 | предикат §1.4 (терпимый к переносу строки) | **сошлось** |
| §1.5 — `DEFAULT` 1 < `ACCOUNT` 2 < `PROJECT` 3; проекция правит только `DEFAULT` | `quota/limitsync.go` `rank`, `quota/projection.go` `precedencePredicate` (`default:` → `source_scope = 'DEFAULT'`) | **сошлось дословно** |
| `Q-05` «Дано» конструируемо посевом | `CHECK (source_scope IN ('DEFAULT','ACCOUNT','PROJECT'))` в `0040` | строка области `PROJECT` **сеется** |
| числа §7.1 — 14 · 4 · 10 · 6 | прогон по самой таблице: строк, кончающихся `**есть** |` и `**S⟨N⟩** |` | **14 · 4 · 10 · 6** — сошлось; у всех 14 строк по пять колонок |
| `Н1` — 7 строк `UNIMPLEMENTED` при 0 в фундаменте | предикат §11 с контролем в обратную сторону | **сошлось** |

**Второго источника истины о посадке дизъюнкция НЕ вводит** — проверено переписью:
`git grep -n 'AuthorityDeclared()\|AbsentAuthority(' origin/main -- 'services/**' 'pkg/**' ':!*_test.go'`
→ **5** попаданий, все — ветка нулевого приёмника `readPosture()` у хендлера, а не второе
вычисление. Посадка вычисляется **одним** `ReadPosture` в композиционном корне; §2.2в
«прочтение одно на пять доменов» этим не нарушается. Старшинство областей дизъюнкцию
принимает: `DEFAULT` — низшая, авторитет перекрывает её предикатом, а не спором объявлений.

#### Кто прав про исход 2 — ПРАВ АВТОР, и вот чем это установлено

Прошлый круг (моя запись §13.1) назвал исход 2 законным. **Он не законен; автор опроверг меня
верно, и оба его довода подтверждены замером, а не согласием.**

| довод автора | мой предикат | исход |
|---|---|---|
| проза отказа станет ложной | `git show origin/main:quota/quotaread/posture.go` — текст дословно тот, что цитирует §2.2в-1: `…no ceiling applies to any kind and no mutation is refused for exceeding one` | **цитата точна** |
| путь мутации и посадка витрины НЕЗАВИСИМЫ, поэтому расхождение реально | отказ мутации производит SQL-функция `kacho_quota_refuse`, зовомая триггером и совещательной полосой; `quotaread.Posture` она не читает — `git grep 'Refusal()\|quotaread\.' origin/main -- 'pkg/**' 'services/**' ':!*_test.go'` не даёт ни одного попадания на пути мутации | **подтверждено** |
| исход 2 завёл бы предмет ОТКРЫТОЙ задачи | `gh issue view 705 -R PRO-Robotech/kacho` → **OPEN**, `bug`, `P2`, `release:quota`, «потолок, который отвергает арендатора, арендатору не виден» | **подтверждено** |

Значит два придаточных прозы («no ceiling applies», «no mutation is refused») в установке с
действующим потолком становятся ложными при истинном первом («no limit authority is
deployed»), и читает их **арендатор в ответе на свой запрос**. Исход 2 из перечня прошлого
круга **изымается**: их было не три, а два.

**Оговорка, которая стоит дороже самого согласия:** вывод автора верен, а его ОПОРА сегодня не
обеспечена. Довод опирается на «в установке, прошедшей `S4`, потолок применяется и мутация
отвергается (`Q-01`, производитель — **есть**)». Именно это и оказалось неверным — `Б2`.

#### Блокирующее `Б2` — путь МУТАЦИИ гейтится значением курсора, а не строкой учёта

**Затронуто:** `Q-01` (помечен **есть**) · `Q-03` (его «Тогда» в §7.1 обязательства не имеет
вовсе) · строка §6 «создание сверх потолка» · `S4` в §8 · довод §3 «потолок начинает
ДЕЙСТВОВАТЬ» · довод §2.2в-1 против исхода 2.

**Признак.** §7.1 объявляет гейтом входа `Q-01`: «`kacho_quota_count` находит строку учёта с
величиной; **при её отсутствии** списание безусловно — значит вход требует посаженного `S2` и
заполненного профиля». **Гейт не тот.** Безусловность определяется значением
`authority_state` в `quota_sync_cursor`, и наличие строки учёта с величиной её не снимает.

**Предикат — четыре шага на `3eeeef0a`, воспроизводится за минуту:**

```sh
# 1. что читает списывающий триггер (действующая версия — последняя по номеру)
git -C project/kacho show origin/main:services/vpc/internal/migrations/\
20260908041500_quota_absent_authority_charges_unconditionally.sql | grep -n "authority_state = 'not-deployed'"
#    -> 143:    SELECT authority_state = 'not-deployed' INTO v_absent ... FROM quota_sync_cursor

# 2. что из этого следует — потолок ОБОЙДЁН и производитель отказа НЕ ЗОВЁТСЯ
... | grep -n 'COALESCE(v_absent'
#    -> 168: AND (COALESCE(v_absent, false) OR used < limit_value)   ← предикат потолка снят
#    -> 177: IF COALESCE(v_absent, false) THEN RETURN NULL           ← и ранний выход до refuse

# 3. НЕЗАВИСИМОЕ подтверждение — парная проба самого дерева, с близнецом
git -C project/kacho show origin/main:services/vpc/internal/repo/kacho/pg/\
quota_absent_authority_integration_test.go | sed -n '/StillChargesTheExistingRow/,/^}/p'
#    -> seedQuota(..., "vpc.network", 1); declareAuthority(..., AuthorityAbsent)
#       строка учёта ЕСТЬ и ПОЛНА -> require.NoError(createNetworkRow(...)) — мутация ПРОХОДИТ
#    близнец TestQuota_DeployedAuthorityRefusesTheFullRow: тот же мир, AuthorityPresent -> ОТКАЗ

# 4. класс, а не экземпляр vpc
for s in vpc compute storage nlb registry; do
  git -C project/kacho grep -l "authority_state = 'not-deployed'" origin/main \
    -- "services/$s/internal/migrations/*.sql" | wc -l; done   # -> 1 1 1 1 1
```

**Почему это делает `Q-01` недостижимым НИ ПРИ КАКОМ входе.** `authority_state` пишет
`StartLimitSync` из `authority.State()` (`corelib:quota/startsync.go`, `RecordAuthority` — до
разбора согласованности, фатально при отказе). `AuthorityPresent` достижимо **только** при
`deployed == true`, то есть только при объявленном адресе, а адрес отвергается стартом
(`quotaedge.ValidateAuthorityHasAProducer`: «единственное действующее значение ручки —
`not-deployed`»). Значит в **каждой поднявшейся** установке курсор несёт `AuthorityAbsent`,
`v_absent` истинно, и `kacho_quota_refuse` не зовётся. **Это ровно та же цепочка, которой был
недостижим `Q-04`,** только через курсор вместо посадки.

**Ни одна стадия §8 этого не снимает.** Перепись по документу:
`grep -niE 'authority_state|quota_sync_cursor|v_absent|курсор'` → **одно** попадание (§1.3,
строка «синхронизатор»), и оно не про путь запроса. Механизм, гейтящий мутацию, в приёмке **не
назван вовсе**: `S1` — «поведение не меняется ни на байт», `S2` — перенос величины и посадка
витрины, `S3` — консоль и документация, `S4` — заполнение профиля.

**Второй, независимый гейт — резолвер величин.** `services/vpc/cmd/vpc/quotasync.go` отдаёт
`Limits: nil` **безусловно** («полосы пути запроса НЕТ и быть не может»), поэтому
`quotaGuard` не собирается (`if quotaLimits != nil && quotaAccounts != nil`), и совещательная
полоса возвращает `nil` — ранний синхронный отказ, обещанный приёмкой `quota-v2`, не
появляется. **Заявляю не шире сделанного:** `Limits: nil` я перемерил у **vpc** и **compute**;
у `storage`, `nlb`, `registry` провязка устроена иначе и мной **не** перемерена. На вывод это
не влияет: решающий гейт — триггер, и он подтверждён у **всех пяти**.

**Что именно неверно в документе — четыре места:**

| место | что утверждает | что производит дерево |
|---|---|---|
| §7.1, строка `Q-01`, колонка гейта | безусловность — «при отсутствии строки учёта» | безусловность — при `authority_state = 'not-deployed'`, независимо от строки |
| §7.1, строка `Q-01`, состояние | **есть** (значит «проверяем сегодня») | недостижим ни при каком входе |
| §8, `S4` | «единственная стадия, меняющая наблюдаемое поведение мутаций» | заполнение профиля поведения мутаций не меняет, пока курсор несёт `not-deployed` |
| §7.1, `Q-03` | обязательство только «ноль отличим от незаявленного» | «Тогда» `Q-03` требует ещё и отказа мутации — обязательства у него в перечне **нет** |

**Строка `Q-07` «списание без отказа» — ответ верный, обоснование неверное:** там сказано
«отсутствие строки учёта с величиной», а держит это тот же `v_absent`. На пустом множестве
исходы совпадают случайно, поэтому строка и не выдала дефекта.

**Предмет — ОДИН и он того же размера, что был у `Б1`:** назвать производителя действующего
потолка и приписать его стадии. Сегодня приёмка обещает, что «потолок начинает ДЕЙСТВОВАТЬ»
(§2.1, §3 «благо продукта»), и не заводит ничего, что сделало бы его действующим.

**Исходов три, четвёртого нет** (выбор — предмет автора):

1. **завести производителя и назвать стадию** — `authority_state` (либо предикат `v_absent`)
   становится функцией **источников величины**, той же дизъюнкцией, что уже написана в §2.2в
   для витрины, плюс резолвер величин из посадки наполняет `Limits`. Тогда `S4` действительно
   меняет поведение мутаций, а §2.2в-1 получает опору;
2. **переписать `Q-01`, `Q-03`, §6 и §3 под производимое** — решение В наблюдаемо арендатору
   только витриной, потолок не действует. Законный исход **только** если названо решением, а
   не умолчанием; заметим, что он обнуляет довод §3 «благо продукта»;
3. **снять `Q-01`/`Q-03` вместе с обещанием действующего потолка**, объявив путь мутации
   отдельным предметом со своей задачей и предикатом.

«Оставить как есть» исходом не является: перечень §7.1 объявлял бы проверяемым сегодня то,
что не проверяемо ни при каком входе (`api-conventions.md` §«Неисполнимая возможность»).

#### Род назван прямо: ТОТ ЖЕ, третий раз — и почему перенос класса его не поймал

Род — **«Тогда» без производителя**, в том же изводе, что у `Б1`: производитель существует
(`kacho_quota_refuse` — есть, я это подтвердил и прошлым кругом) и **короткозамкнут на входе
сценария**.

**Четвёртая колонка — верный механизм, и она НЕ виновата. Виноват способ её заполнения.**
Колонка спрашивает «что стоит до производителя», и `Q-01` получил ответ, добытый **чтением**,
а не прогоном: автор прочитал **имя файла** миграции (`quota_absent_authority_charges_
unconditionally`) и вывел из него механизм («при отсутствии строки учёта»), тогда как имя
называет настоящее условие — `absent_authority`. Это `multi-agent-flow-orchestration.md` §14а,
**вид 2 — гипотеза о МЕХАНИЗМЕ**: свойство чужого механизма взято из его назначения, а не из
его кода; звучит как знание, является допущением. Стоило одной команды — `grep` по телу той же
миграции, которую строка уже цитирует.

**Отсюда правка формы работы, а не ещё один круг по тому же роду:** четвёртая колонка обязана
нести **предикат, которым гейт установлен**, а не его описание. Строка без прогнанной команды
в этой колонке не заполнена — она угадана. Это ловится машинно у автора до отправки: по каждой
строке колонки есть команда и её вывод.

**Почему круг всё же назначается, а не приостанавливается** (`§«Круг не запускается там, где
ПРЕДМЕТА ещё нет»`): производитель здесь **может** появиться этим же кругом — предмет
существует в дереве, блокирующих задач у приёмки нет, и число блокирующих **убывает**
(`Б1` → 2 → 1 в первом круге, **1** во втором, и это другой предмет, а не остаток прежнего).
Цикл сходится.

#### Замечания (НЕ блокирующие)

**`З2` — «первым стейтментом `ListQuotas`» неточно.** §7.1 (`Q-04`) и §2.2в говорят, что
`posture.Refusal()` стоит первым. Первой стоит проверка `projectID == ""`
(`pkg/quota/quotapb/convert.go`), и комментарий рядом объясняет порядок намеренным
(«арендатор, не назвавший проект, обязан узнать о своей ошибке, а не выслушать рассказ об
установке»). На входе `Q-04` проект назван, поэтому вывод верен, а формулировка — нет. Это тот
же класс, что `Б2`, в миниатюре: описание гейта вместо его предиката.

**`З3` — `S2` затрагивает ДВА репозитория, и §8 этого не говорит.** Проза отказа
(`corelib:quota/quotaread/posture.go`) и строка журнала (`corelib:quota/startsync.go`) живут в
**фундаменте** — §12 это называет, а §8 нет. По `polyrepo.md` §«Порядок работ» это означает
выпуск версии `corelib` и подъём пина у потребителей, то есть `S1`/`S2` не однорепозиторные.
Назвать стоит в §8, где читают порядок работ.

#### Ошибки рецензента — называю первыми

1. **`Б2` существовало и на прошлом круге, и я его не нашёл — хотя предикат был тот же.**
   Прошлый круг проверил у `Q-01` ровно то, за что я упрекал автора по `Q-04`: **существует
   ли** производитель («`kacho_quota_refuse` + `quotadetail.MetadataFromError` — производитель
   **есть**»), и **не** проверил, исполняется ли он на входе сценария. Я применил к одному
   сценарию строгость, которой не применил к соседнему, и потому четвёртая колонка, которую я
   же потребовал, была заполнена на `Q-01` неверно и прошла мимо меня. Цена — этот круг.
2. **Первый мой замер сохранности §13.1 дал расхождение, и это была слепота МОЕГО разреза.**
   `awk` между заголовками захватывал пустую строку перед §13.2, которой в прежней редакции не
   было (§13.1 была последней секцией): `sha256` разошлись, строк 147 против 148. Дефекта нет
   — после среза хвостовых пустых строк хеши совпадают побайтово. Поймано не перечитыванием, а
   тем, что я посмотрел **дифф**, а не поверил своему числу (`testing-verdict.md` §14: свой
   разборщик — тоже распознаватель).
3. **Я едва не заявил шире сделанного про `Limits: nil`.** Перемерив у vpc и compute, я
   собирался написать «у всех пяти»; предикат по остальным трём вернул пусто — у них провязка
   устроена иначе. Заявление сужено до измеренного, а решающий гейт назван отдельно.

#### Чего этот круг НЕ проверял — названо, а не подразумевается

- **имитация клиента четырьмя исполнителями с пустым контекстом** (§4, §10) — не прогонялась
  и этим кругом; остаток остаётся остатком, и он тот же;
- **истинность §9.1** (домен спрашивает ровно то, что списывает) — сторону списания я
  перемерил (19 видов, 8·3·3·3·2), сторону вопроса **нет**; на вердикт не влияет: §9.1
  обосновывает `Out-of-scope`, а не сценарий;
- **находки `Н2`/`Н3`** (охват чужого гейта) — приняты как заведённые отдельно, по существу не
  судились; `Н1` перемерена и сошлась;
- **провязка совещательной полосы у `storage`, `nlb`, `registry`** — см. ошибку 3.

**Что делать автору:** закрыть `Б2` одним из трёх названных исходов, учесть `З2` и `З3`,
заполнить четвёртую колонку §7.1 **прогнанными предикатами** вместо описаний — и вернуть на
круг. Записи §13.1 и §13.2 при этом **не правятся**: новая редакция получает запись §13.4.
