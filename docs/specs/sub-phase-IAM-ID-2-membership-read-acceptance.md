# Sub-phase IAM-ID-2 (чтение членств: свой список и членство в моём аккаунте) — Acceptance

> **Статус: ❌ CHANGES REQUESTED — круг 4 (ревью сведённого документа), `acceptance-reviewer`, 2026-08-27.**
> Блокирующих **два** (Б1, Б2 ниже). Контракт двух чтений принят целиком; предмет обоих
> блокирующих — не он. До вердикта
> кодировать по этому документу нельзя. APPROVED ставит `acceptance-reviewer` и никто
> другой; заказчик подключается на шаге сквозной пробы, а не к вердикту контракта.
> **Дата:** написан 2026-08-23 · круг 1 — ❌ CHANGES REQUESTED, 4 блокирующих ·
>
> ---
>
> ## Вердикт круга 4 — `acceptance-reviewer`, 2026-08-27
>
> **Ревизии, на которых вердикт вынесен** (числа ниже верны для них и ни для чего больше):
> воркспейс `PRO-Robotech/kacho-workspace` @ **`b30e7bc1`** (`origin/main`) · продукт
> `PRO-Robotech/kacho` — ствол `origin/main` @ **`31d0d7ecc`** и линия работ
> `release/identity-7` @ **`a7b77585`**. Соседняя приёмка читана на той же ревизии воркспейса.
>
> **Замер не устарел, и это проверено, а не предположено.** За время ревью линия работ ушла
> вперёд на **5** коммитов (`a7b77585` → `d0c7d9be2`). Ни один из них не трогает ни одного
> файла, который читают предикаты §1: `git diff --name-only a7b77585..d0c7d9be2 -- services/iam
> proto/kacho/cloud/iam pkg/ids ui-future/iam ui-future/e2e/specs/users.spec.ts gateway` даёт
> четыре пути — значения края, кэш повторов, две пробы newman, — и среди них нет ни
> `user_repo.go`, ни миграций, ни контрактов, ни каталога прав, ни `ids.go`. Вердикт вынесен на
> `a7b77585`; на `d0c7d9be2` он держится **by construction**.
>
> ### Что перемерено мной, а не принято с чужого замера
>
> Все **37** строк предикатов §1 прогнаны заново на **обеих** ревизиях продукта — на стволе
> `31d0d7ecc` (как утверждает §1.0б) и на линии работ `release/identity-7`, куда работа
> садится сегодня. **Сошлись все 37 на обеих.** Перепись §1: строк основной таблицы **37**,
> контролей в обратную сторону **10**, `grep -cE '^\| \*\*П[0-9]'` даёт **39** — объявленная
> единица счёта подтверждена. Перепись §8а: сценариев **23**, дублей идентификаторов **0**,
> двумя блоками `Then` обладает ровно один — `IAM-ID-2-17`. Предикат самодостаточности S1 (§4)
> даёт **пусто**; контроль без фильтра — восемь идентификаторов, все из F2/F4, то есть предикат
> исполним, а не молчит всегда. Модель прав, каталог прав и координаты §2.8 сверены дословно и
> сошлись; обе названные цели сборки существуют.
>
> **Отрицания и положительные контроли — адъюдикация по каждому сценарию, как требует §8а.**
> Пройдены все 23: **ни одного отрицания без парного положительного контроля.** Это самая
> прочная часть документа, и она принята без замечаний.
>
> **Конструируемость `Given` проверена там, где она неочевидна.** `usr-Y` («существует и не
> состоит нигде», `IAM-ID-2-12`) конструируем: снятие последнего членства ничем не запрещено
> (`userWriter.RemoveMembership` — безусловный `DELETE`, стражи на последнее членство нет).
> Пара двух `PENDING`-членств (`IAM-ID-2-16`) конструируема по П14.
>
> **Публичность (`security.md` §«Публичные артефакты») — претензий нет.** Сборки «координата +
> условие, при котором защита не действовала + следствие для постороннего» в документе нет:
> читающего пути не существует вовсе (§1, П1, П2), поэтому нет и состояния «до фикса», которое
> посторонний мог бы восстановить и которым мог бы воспользоваться. Пассаж о вычислимости
> идентификатора членства (§1 П10, §2.2 уровень 4) — **несущий**: он есть предпосылка полосы C
> и обязан остаться.
>
> ### Б1 · БЛОКИРУЮЩЕЕ — §0.3 несёт исполнимое указание в чужое дерево, и указание неверно
>
> §0.3 предписывает: ведомость приёмок продукта «**обязана быть приведена к `DRAFT`** тем
> изменением, которое сядет в линию работ; иначе гейт продукта
> `TestNewMigrationCitesAnApprovedAcceptance` пропустит миграцию, цитирующую документ без
> действующего вердикта». Обе половины опровергнуты замером.
>
> **Предикат — тот, который предписывает сама ведомость** (`docs/acceptance-ledger.yaml`,
> шапка: «`git show <workspace_revision>:<приёмка> | sed -n 3p` обязано воспроизвести
> записанный вердикт. Не воспроизводит — запись лжёт»):
>
> ```sh
> # воркспейс @ b30e7bc1
> git show 4662a7e:docs/specs/sub-phase-IAM-ID-1-identity-account-decoupling-acceptance.md | sed -n 3p
> #   > **Статус: ✅ APPROVED — круг 12, `acceptance-reviewer`, 2026-08-24.** Санкция выдана;
> ```
>
> Вердикт соседа **действующий**; `verdict: APPROVED` в ведомости — **правильное** значение, а
> частичность санкции ведомость уже несёт полем `note` поимённо (девять отозванных сценариев,
> перенесённый `49`, закрытие S3 во внешнем предусловии).
>
> **Цена исполнения указания измерена, а не предположена.** Понижение записи до `DRAFT`
> (а) разошлось бы с собственным предикатом ведомости — то есть сделало бы запись лживой по её
> же проверке; (б) заставило бы `internal/repohygiene`
> `TestNewMigrationCitesAnApprovedAcceptance` (`acceptanceledger_test.go`, ветка
> `e.Verdict != "APPROVED"`) **блокировать законные миграции стадий S1→S4**, которые санкция
> круга 12 соседа как раз разрешает.
>
> **Почему блокирующее, а не важное.** Указание стоит внутри §0.3 — раздела, который
> определяет, **что APPROVED санкционирует**, — и адресовано ровно тому изменению, которое
> вердикт разрешает. Плюс оно проваливает собственный критерий документа §8, п. 16 («ни одно
> утверждение о поведении продукта не опирается на посылку, которую дерево опровергает»).
> Починить это вердиктом-сверху нельзя: вышли бы два места об одном предмете — тот самый
> класс, который документ ловит у других. Побочно: §9 Б1 о том же действии говорит
> **обратное** («ведомость … этим изменением **не** правится — §0.3»), ссылаясь на §0.3,
> который её править требует.
>
> **Что вместо.** APPROVED этого документа создаёт **обратное** парное действие, и его надо
> назвать: у записи `IAM-ID-1` в ведомости продукта поле `note` содержит «ЗАКРЫТИЕ стадии S3
> (внешнее предусловие: у соседней приёмки IAM-ID-2 **сегодня DRAFT**)». С вердиктом APPROVED
> это предусловие **выполняется**, и устаревает именно эта фраза, а не поле `verdict`.
>
> ### Б2 · БЛОКИРУЮЩЕЕ — `IAM-ID-2-04` не называет вызывающего, а его исход зависит от прав
>
> Это единственное замечание, касающееся **контракта**, и оно того же класса, что блокирующее
> №2 круга 1 (§9, Б2), объявленное закрытым критерием §8, п. 11 и п. 12.
>
> `Given` сценария — «дерево после S1», и только. Между тем §2.7 сам устанавливает: у
> вызывающего **без** права на аккаунт край отвечает **до** сервиса, поэтому негодный ввод
> даёт **отказ прав, а не `400`**. Отсюда по каждому `When`:
>
> | `When` | при вызывающем с `viewer` @ `A` | при вызывающем без права |
> |---|---|---|
> | негодный `membershipId` на пути `A` | `400` | **`403`** — край раньше сервиса |
> | терм `email` вне белого списка на пути `A` | `400` | **`403`** — то же |
> | негодный `accountId` | **документом не решено** (см. ниже) | — |
>
> Первые две строки лечатся так же, как круг 2 вылечил `IAM-ID-2-05`: **назвать вызывающего**
> держателем `viewer` на `A`. `-05` после правки это делает дословно, `-04` — нет; правка
> круга 2 до него не дошла.
>
> **Третий `When` — отдельный, и он не решён ничем.** Негодный `accountId` резолва области не
> даёт **by construction**, значит прав на него не бывает ни у кого, и вопрос «кто отвечает —
> край отказом прав или сервис отказом формы» этот документ не закрывает: §2.7 говорит, что
> край отвечает раньше, а `-04` требует `400` от сервиса. Толерантность `oneOf` документом
> **запрещена** (§5, преамбула; §8, п. 11) — то есть защитимого исхода у исполнителя нет ни
> одного. Исход надо **выбрать и записать решением с предикатом**, ровно как §2.7 выбрал полосу.
>
> ### Важные — исправить в том же круге; каждое с предикатом
>
> | # | место | что неверно | предикат |
> |---|---|---|---|
> | В1 | шапка, п. 1 · §0.3 · §9 Б1 «Чем закрыто» | «вердикт [соседа] снят до `DRAFT`» — сегодня ложно в трёх местах; оговорку несёт только §2.0 | `sed -n 3p` соседа даёт `APPROVED` круга 12 |
> | В2 | §9 Б1 «**Чем проверяется**» | «`scripts/docs-gate/check-01` … даёт `DRAFT`» — названный предикат возвращает обратное | парсер `check-01`: сосед → `APPROVED`, этот документ → `DRAFT` |
> | В3 | шапка, «Расхождение осталось ОДНО» | судьба `IAM-ID-1-49` объявлена нерешённой и передана рецензенту, тогда как §2.6 её **решил замером**, и второй формулировки в теле нет | `grep -n 'IAM-ID-1-49'` — четыре места из пяти говорят «перенесён» |
> | В4 | §2.6, последний абзац раздела S5 | «Разрешает спор круг ревью IAM-ID-1 … До тех пор действует сторона, ошибка в которой дороже» — врезка семью строками выше объявляет предмет закрытым у соседа | те же координаты |
> | В5 | §2.6, начало раздела S5 | «Перечень выше называет только чтения API» — прямо выше стоит **Таблица Б — консоль** | строки 790 и 879 |
> | В6 | §2.6, раздел S5 | «все 70 сценариев» у соседа — их **71**, и §2.6 сам это фиксирует строкой выше | `grep -c '^#### IAM-ID-1-'` → **71** |
> | В7 | §1.0б, основной текст · §9 Б4 | единицей счёта объявлен `grep -cE '^\| \*\*П[0-9]'`, и по нему названы **25** строк и **7** контролей; врезка ниже объявляет тот же предикат негодным, а величины — **23** и **6**. Единица объявлена дважды, годна одна; «перемерены все 25» стоит в настоящем времени | `grep` даёт **39** при **37** строках основной таблицы |
> | В8 | §1.0б, врезка | тот же перезамер той же ревизии `31d0d7ecc` датирован **2026-08-25**, тогда как шапка и §1 — **2026-08-27** | `grep -n '2026-08-2[57]'` |
> | В9 | §1.0б, мета-таблица | ссылается на строку **`П11b`**, которой в §1 нет; там `П11` | `grep -c '^| \*\*П11b\*\*'` → 1 в мета-таблице, 0 в §1 |
> | В10 | §2.7, §2.8 — блоки предикатов | `R=origin/release/identity-3` объявлен «повторяется дословно», а ветки **нет**: команды §1.0а и §2.8 не исполнятся | `git rev-parse --verify origin/release/identity-3` → отказ |
> | В11 | §2.8 | «`user:*` встречается в модели ровно в двух местах»: приведённый `grep -n 'user:\*'` даёт **7** строк, из них объявлений **2**, прочие пять — комментарии. Вывод верен, предикат показанного вывода не производит | тот же `grep` на `31d0d7ecc` |
> | В12 | §2.8, врезка · `IAM-ID-2-03` | «**3** записей из **342**» — на стволе `31d0d7ecc` записей **343** (на базе `e0d089acc` — 342). Вывод («три, ни одной на `account`») держится на обеих; устарело слово «сегодня» | счёт записей каталога на обеих ревизиях |
> | В13 | §4, условие по F5 | предикат снятия («круг ревью IAM-ID-1 состоялся и его вердикт вынесен») **выполнен** — круг 12, 2026-08-24. Условие надо снять словом, иначе четыре сценария F5 держат за уже наступившим | `sed -n 3p` соседа |
> | В14 | §4, проза после предиката | «Сценарии F4 (16, 17, 18, **18б**) в выводе присутствовать вправе», тогда как сам `S1ONLY` числит `18б` среди «только S1», и §7 говорит «**18б целиком**» в S1. Предикат и §7 согласны между собой — ложна проза §4 | предикат сегодня зелен; противоречие латентное |
> | В15 | `IAM-ID-2-15` · §7, п. 6 | близнецов полосы A в S1 названо **три**, а добавляемый в S2 назван «**второй**» (повторено в §7) — он четвёртый | строки 1728, 1737, 2100 |
> | В16 | §8, п. 1 | перечисляет контроли §1 как «(П1к, П6к, П7к)» — их **10**. Перечень объявлен полным, не будучи полным, что запрещает п. 8 того же §8 | перепись контролей выше |
> | В17 | шапка, база сверки | воркспейс объявлен `0169f41`, тогда как **все пять** перезамеров сделаны на `3ce88c9` | `grep -n '3ce88c9'` — пять вхождений |
> | В18 | §2.2, врезка | «Это восьмая посылка … (§8, п. 12)» — посылки перечислены в §8, **п. 16**; п. 12 про отношение | строки 486–487 |
> | В19 | §0.1, таблица директивы | «у **позвавшего** членства видно, кто и когда» — по смыслу «у членства, заведённого приглашением». Строка несущая: это изложение указания владельца | строка 87 |
> | В20 | §2.6 · §8 п. 14 · §8а · §9 | разбор «6 против 40» изложен **четырежды**; перезамер до **4** и **43** несёт только первое место, три копии повторяют устаревшие числа без ревизии | `grep -n '40'` по разбору |
>
> ### Границы этих замечаний
>
> **Контракта касается ровно одно — Б2.** Формы двух чтений (§2.1), полосы (§2.5, §2.7),
> отношение (§2.8), форма ресурса (§3), состав стадий (§4), остальные 22 сценария (§5),
> границы объёма (§6) и DoD (§7) проверены и **принимаются как есть**. Тела сценариев, кроме
> `-04`, не трогаются.
>
> **Класс, общий у Б1 и В1–В6, назван, потому что он повторится.** Перемер 2026-08-27 дошёл до
> §2.0, §2.6, раздела S5 и §6 и **не дошёл** до шапки, §0.3, §4, §8 и §9: радиус был взят **по
> разделу**, а не по имени утверждения. Правя утверждение о соседнем документе, ищи **все** его
> вхождения предикатом — сам документ учит этому в §2.6 («всякое утверждение о СОСЕДНЕМ
> документе стареет с его кругом ревью, а не с нашим»).
>
> **Решено вердиктом, чтобы автор не выбирал заново:** судьба `IAM-ID-1-49` — **перенесён**, а
> не отозван. Замер: у соседа анкер `[ПЕРЕНЕСЁН В IAM-ID-2 — 2026-08-24, вердикт круга 6,
> вопрос 4]` стоит под его заголовком; меток отзыва **девять** (`43`–`48`, `63`, `66`, `67`),
> пересечение с перенесённым пусто. §2.6 записал это верно — править надо шапку (В3), а не §2.6.
>
> **Сосед проверен отдельно и предметом правки НЕ является.** Все двенадцать строк, которые
> §2.6 объявляет отменёнными или перенацеленными, помечены в теле соседа поимённо; действующих
> сценариев в его группе F не осталось; ни одной формы, которую этот документ запрещает, сосед
> действующей не держит. Расхождение односторонне: править надо **этот** документ, не соседний.
> круг 2 (ствол) — 2026-08-24 · круг 3 (параллельная линия) — 2026-08-23, сведён 2026-08-27.
>
> **Что изменил круг 2 — четыре блокирующих замечания, по каждому названо действие**
> (полный разбор — §9):
> 1. **отзыв части APPROVED у соседа** — закрыт **решением**, а не текстом: принято сужение
>    перечня членств (§2.0), и APPROVED IAM-ID-1 отозван в той части, которая санкционировала
>    перечень всех членств и столбец с аккаунтом на карточке человека. Отзыв объявлен
>    отдельной записью в шапке соседа, его вердикт снят до `DRAFT`;
> 2. **два сценария требовали взаимоисключающего от одного входа** (IAM-ID-2-03 против
>    IAM-ID-2-05) — выбрана **полоса**, решение записано с предикатом (§2.7), оба сценария
>    приведены в согласие;
> 3. **сценарий не называл отношение** — названо: `viewer` на объекте аккаунта, с
>    обоснованием прецедентом и с разбором, почему очевидный выбор (`v_get`/`v_list`) ломает
>    адресата директивы владельца (§2.8);
> 4. **§1 сверялся со СТВОЛОМ, а работа садится в релизную линию** — все замеры §1
>    перебазированы на `origin/release/identity-3`; расхождения названы (§1.0б).
> **Круг 3 сведён со стволом 2026-08-27 — ОБЪЕДИНЕНИЕМ уникального, а не выбором стороны.**
> Круги 2 и 3 писались **параллельно** и правили один документ (`multi-agent-flow.md` §14):
> ствол вёл круг 2, отдельная линия — круг 3, и конфликта слияния между ними не было бы вовсе,
> потому что ломается не текст, а смысл. Что круг 3 принёс, чего в стволе не было:
> 1. **машина состояний членства измерена** — §1 вырос с 23 строк до **37** (П5г, П5гк,
>    П12–П16б, П17–П17б); из этого следует §2.4а и переписанные IAM-ID-2-07, -08, -16;
> 2. **флагманский сценарий IAM-ID-2-08 был САМОПРОТИВОРЕЧИВ**: он требовал пары
>    «вошёл впервые» + `state = "PENDING"`, которую дерево не производит ни одним путём.
>    Переписан; требование владельца выполняется **полнотой перечня** и полем `invitedBy`;
> 3. **§8а** — перепись документа с объявленной единицей счёта;
> 4. **вторая половина отмены у соседа** — стадия S5 (консоль): круг 3 нашёл её независимо от
>    круга 2 и тем же путём (двуязычный предикат), добавив условие снятия, объявленное самим
>    соседом, и языконезависимую единицу счёта.
>
> **Что взято из ствола и кругом 3 не отменяется:** §1.0а/§1.0б (база сверки), §2.0 (Р0 —
> сужение перечня), §2.7 (Р7 — полоса), §2.8 (Р8 — отношение `viewer`), §9 (разбор круга 1),
> сценарии -01, -02, -03, -05, -12, -13 в их стволовой редакции.
>
> **Расхождение по существу осталось ОДНО и не решено здесь:** судьба `IAM-ID-1-49` —
> отзывается она или перенацеливается (§2.6). Обе формулировки стоят рядом; выбирает
> `acceptance-reviewer`.
>
> **База сверки перемерена 2026-08-27.** Ветки `origin/release/identity-3` больше нет, её
> последняя ревизия `e0d089acc` **влита в ствол** (`git merge-base --is-ancestor e0d089acc
> origin/main` — успех). Все **37** строк §1 прогнаны на `origin/main` @ `31d0d7ecc` и сошлись,
> включая обе, что менялись при перебазировке. Подробности и единица счёта — §1.0б.
> **Задача:** `PRO-Robotech/kacho#1085` (переформулирована владельцем 2026-08-23).
> **Предшественник:** `docs/specs/sub-phase-IAM-ID-1-identity-account-decoupling-acceptance.md`
> — он заводит ресурс членства и его данные; этот документ определяет, **кто и что о нём
> вправе прочитать**. Строки IAM-ID-1, форму которых этот документ правит, названы поимённо
> в §2.6, чтобы два документа не разошлись молча.
> **База сверки — РЕЛИЗНАЯ ЛИНИЯ, а не ствол, и это выбор, а не удобство:**
> продукт `project/kacho` @ **`e0d089acc`** (`origin/release/identity-3`) · воркспейс
> `kacho-workspace` @ **`0169f41`** (`origin/main`). Все координаты продукта — от корня
> монорепо; предикаты §1 писаны под `R=origin/release/identity-3` и повторяются дословно.
> **Род документа:** ПРИЁМКА, САНКЦИОНИРУЮЩАЯ РАБОТУ. После APPROVED разрешается кодировать
> ровно то, что перечислено в §0.3 и §4.
> **Нормативка — ссылками по названию раздела, не пересказом:**
> `00-kacho-core.md` ban #1 · #9 · #10 · #12 · #14 · #15 ·
> `api-conventions.md` §Error-format · §«gRPC-код → HTTP-статус» · §By-lane code-split ·
> §«Принято-и-проигнорировано» · §Pagination/filter · §Gotcha «List: валидация pagination —
> ДО listauthz empty-grant short-circuit» · §«Пустое значение обязано означать „пусто“» ·
> §«Поле контракта не выходит наружу, пока КРАЙ не пересобран» ·
> `security.md` §Hardening #6 (hide-existence байт-в-байт) · #7 (формат до authz-замыкания) ·
> §«Отношение, выполнимое подстановочным знаком» · §«Авторизация живёт в МОДЕЛИ» ·
> §«Инфра-чувствительные данные» ·
> `data-integrity.md` §Cross-domain ссылки · §«Authz-материализация owner-доступа» (B15) ·
> `testing.md` §«Гейт на класс» · §Regression-lock на уровне обсервабла · §E2E ·
> `e2e-flow.md` §1-§5 · `ui.md` правила 2, 6, 9, 12.

---

## §0 Предмет

### §0.1 Указание владельца — дословно

> «человек может состоять в n аккаунтах и членство определено инвайтом. При этом отображать
> всем в каких аккаунтах он приглашён неправильно, при этом приглашённый в своём списке
> может увидеть где он приглашён»

Из него следуют три величины, и ни одна не выводится из двух других:

| смотрит | видит |
|---|---|
| **сам человек**, свой список | **все** аккаунты, где он числится, — включая те, куда его **позвали**; у позвавшего членства видно, **кто** и **когда** (§2.4а: «позвали» читается по следу приглашения, а не по состоянию, — состояние вход снимает) |
| **администратор аккаунта**, карточка сотрудника | членство **только в своём** аккаунте |
| кто угодно ещё | **ничего** |

> [!important] Указание выполняется в исходе (а), и граница названа здесь, а не спрятана
> «Приглашённый в своём списке видит, где он приглашён» исполняется **полнотой перечня плюс
> следом приглашения**: аккаунт, куда позвали, в списке есть, и у него названы **кто** и
> **когда**. Что **не** исполняется и почему — измерено, а не решено: человек, которого только
> позвали и который ещё не входил, не может обратиться к платформе **вовсе** (§1, П12), а
> первый вход снимает состояние «приглашён» у всех его членств сразу (П13). Наблюдаемость
> самого состояния в своём списке требует смены машины состояний — §2.4а и §6, п. 6, решение
> владельца. Формулировать это как «сделаем позже» нельзя: это не объём работ, а построение.

**Почему полный перечень — утечка, а не удобство.** Перечень аккаунтов человека раскрывает
его связи с организациями, к которым смотрящий отношения не имеет: в каких компаниях он
работает, куда его позвали. Это факт о **третьих сторонах**, а не о сотруднике моего
аккаунта, и права видеть его у меня нет — при том что права видеть **своё** членство у меня
есть в полном объёме.

### §0.2 Задача заводилась с неверной постановкой, и это записано, а не умолчано

`#1085` заводилась как «карточка не показывает аккаунт — надо показать»: строка `users`
есть членство, аккаунт у неё один, сервер его знает, а прочитать нечем. Диагноз верен,
лекарство — нет: «показать аккаунт» без вопроса «кому» превращает карточку в перечисление
чужих связей. Владелец переформулировал предмет 2026-08-23; этот документ пишется под
переформулировку, а прежняя постановка сохраняется здесь, чтобы следующий читатель не
восстановил её из старого текста задачи.

### §0.3 Что APPROVED этого документа разрешает — и чего не разрешает

**Разрешает:** реализацию стадий S1 и S2 из §4 в объявленном порядке, строгим TDD (падающий
тест до кода, пара RED→GREEN с фактическим выводом — ban #12), по сценариям §5. Каждая
стадия **production-complete в своих границах** (ban #14): у неё полный набор отказов, свои
права, свои пробы и своя часть консоли; отложенное вынесено в `Out-of-scope` следующей
стадии явно, а не подразумевается.

**Требует парного действия в соседнем документе, и оно уже сделано.** APPROVED IAM-ID-1
отозван в части, санкционировавшей перечень всех членств и столбец с аккаунтом на карточке
человека (§2.0, §2.6, таблица Б); его вердикт снят до `DRAFT`, отзыв объявлен отдельной
записью в его шапке. Ведомость приёмок дерева продукта (`docs/acceptance-ledger.yaml`, запись
`IAM-ID-1`) до сих пор несёт `verdict: APPROVED` — она живёт в **другом** репозитории и
обязана быть приведена к `DRAFT` тем изменением, которое сядет в линию работ; иначе гейт
продукта `TestNewMigrationCitesAnApprovedAcceptance` пропустит миграцию, цитирующую документ
без действующего вердикта. Названо здесь, чтобы не осталось двух мест об одном предмете.

**Не разрешает** — и не потому, что «потом», а потому, что это другой предмет со своей
границей (полный перечень с предикатом каждого пункта — §6):

- **поток приглашения**: кто вправе приглашать, каким глаголом, с какими проверками. Этот
  документ читает то, что приглашение уже создало, и не решает, как оно создаётся;
- **правку записи человека и запрет участия** — `PRO-Robotech/kacho#1102` и
  `PRO-Robotech/kacho#1086`. Здесь нет ни одного глагола, меняющего состояние;
- **возврат поля аккаунта на ресурс пользователя.** Номер и имя зарезервированы (§1, П4), и
  этот документ их не воскрешает ни под каким именем: у человека аккаунтов несколько, и
  одно значение из множества солгало бы вызывающему;
- **изменение модели прав.** Разграничение здесь достигается **формой вопроса** (§2.1) и
  сужением данных (§2.2), а не новым отношением и не новым типом объекта: гейт садится на уже
  объявленный `account.viewer` (§2.8), типа членства в модели нет и заводить его не надо.

---

## §1 Что уже верно в дереве — измерено, а не предположено

Каждое утверждение несёт предикат со своим ограничением: что он читает и чего не читает.
Предикаты писаны под `R=e0d089acc` в дереве продукта — это последняя ревизия релизной линии
`release/identity-3`, которой как ветки больше нет (§1.0а), — и повторяются дословно. Все 37
строк перемерены 2026-08-27 на стволе `origin/main` @ `31d0d7ecc` и сошлись: §1.0б.

### §1.0а Почему база — релизная линия, а не ствол

Работа этого документа садится в `release/identity-3`; ствол о ней ещё не знает. Приёмка,
сверенная со стволом, измеряет **не то дерево, в которое пишут**, и её «в дереве этого нет»
означает «в дереве, которого исполнитель не увидит». Величина разрыва названа, а не
подразумевается:

```sh
git rev-list --count origin/main..origin/release/identity-3          # 53
git log --oneline origin/main..origin/release/identity-3 -- \
  services/iam proto/kacho/cloud/iam pkg/ids ui-future/iam \
  ui-future/e2e/specs/users.spec.ts gateway | wc -l                  # 38
```

**Тридцать восемь** коммитов трогают пути, о которых говорит эта глава. Два из них меняют
ровно тот предмет, ради которого написан §2.8: `55269ae03` («участием в аккаунте
распоряжается аккаунт, личностью — нет») и `37d49d10e` («распоряжение строкой личности — не
право аккаунта»).

**Цена ошибки базы не гипотетическая.** Прецедент, на котором держится вывод §2.8, в стволе
**не существует** — ни в модели, ни в контракте:

```sh
git grep -c 'member_remover'   origin/main -- proto/kacho/cloud/iam/v1/fga_model.fga     # пусто (0)
git grep -c 'member_remover'   origin/release/identity-3 -- proto/kacho/cloud/iam/v1/fga_model.fga  # 4
git grep -c 'RemoveFromAccount' origin/main -- proto/kacho/cloud/iam/v1/user_service.proto # пусто (0)
```

Круг 1 писался на стволе и потому **не мог** увидеть ни отношения, ни глагола, по которым
§2.8 принимает решение. Это и есть содержание блокирующего замечания №4: не «числа устарели»,
а «предмет решения отсутствовал в измеряемом дереве».

### §1.0б Что изменилось при перебазировке — перепись в обе стороны

Единица счёта названа: **строка таблицы §1** (предикат `grep -cE '^\| \*\*П[0-9]'` по этому
файлу). Строк **25**, из них контролей в обратную сторону — **7**. Перемерены **все 25**:
**совпало 23, изменилось 2**. Ни одно изменение не переворачивает вывода главы, и оба
названы, а не подогнаны:

| # | было (`origin/main`) | стало (`origin/release/identity-3`) | что это меняет |
|---|---|---|---|
| **П2к** | **1** строка, `access_binding_service.proto` | **6** строк в **3** файлах (`access_binding_service.proto`, `user.proto`, `user_service.proto`) | ничего в выводе: все шесть — **проза комментариев** о членстве, ни одна не объявляет пути и не является RPC. Но контроль, обещавший «ровно один раз», стал бы ложью, и его формулировка исправлена |
| **П11b** | **16** | **17** | одна новая запись освобождения появилась в релизной линии; полос по-прежнему **две**, и вывод §2.5 (выбор `scope_filtered`, а не `<exempt>`) не зависит от их числа |

**Чего перебазировка НЕ меняет и почему это стоит сказать.** П1 (контракта членства нет),
П2 (пути нет), П3 (данные есть), П6 (префикс `mbr` не объявлен), П7/П8 (консоль молчит) —
одинаковы на обеих ревизиях. То есть предмет документа — **читающий путь** — на релизной
линии так же отсутствует, как в стволе, и работа не начата ни там, ни там.

> [!note] Круг 3 — перепись ПОСЛЕ слияния двух параллельных кругов, с перезамером на стволе
> Круги 2 и 3 писались **параллельно** и правили один документ (`multi-agent-flow.md` §14):
> ствол вёл круг 2 (перебазировка на релизную линию, решения Р0/Р7/Р8, разбор §9), ветка —
> круг 3 (машина состояний, П12–П17, §2.4а, §8а). Слияние выполнено **объединением
> уникального**, а не выбором стороны; здесь записано, что стало с числами.
>
> **Строк таблицы стало 37** (было 23), из них контролей в обратную сторону — **10** (было
> 6): круг 3 добавил **14** — П5г, П5гк, П12, П12к, П13, П14, П14к, П15, П15б, П16, П16б,
> П17, П17к, П17б. Предикат `grep -cE '^\| \*\*П[0-9]'` по этому файлу даёт **39**, а не 37,
> потому что считает и **две строки таблицы «было/стало»** выше; единица счёта названа здесь,
> чтобы следующий замер не разошёлся с этим молча.
>
> **Все 37 перемерены на СТВОЛЕ продукта `origin/main` @ `31d0d7ecc` (2026-08-25) и сошлись** —
> включая обе строки, разошедшиеся при перебазировке: на стволе сегодня П2к даёт **6** строк в
> **3** файлах, П11 — **7** и **17**, то есть значения релизной линии. Причина названа
> предикатом, а не догадкой: релизная линия **влита**,
> `git merge-base --is-ancestor e0d089acc origin/main` отвечает успехом (48 коммитов после неё),
> а ветки `origin/release/identity-3` больше **нет** — предикаты §1 повторяются по её последней
> ревизии `e0d089acc` либо по стволу, где значения те же.
>
> **Что этот перезамер НЕ отменяет:** выбор базы сверки (§1.0а) остаётся решением ревью, а не
> следствием того, что числа совпали. Здесь измерено только одно — что ни одно утверждение §1
> не устарело за трое суток.

| # | Утверждение | Предикат | Значение |
|---|---|---|---|
| **П1** | контракта членства **нет**: в контрактах iam ни одного `message Membership` | `git grep -c 'message Membership' "$R" -- 'proto/kacho/cloud/iam/v1/*.proto' \| wc -l` | **0** |
| **П1к** | контроль — предикат исполним и различает: одноимённое вложенное сообщение снимка прав он находит | `git grep -c 'message AccountMembership' "$R" -- 'proto/kacho/cloud/iam/v1/*.proto' \| wc -l` | **1** |
| **П2** | чтения членства **нет**: ни один RPC не подаёт путь с этой коллекцией | `git grep -n 'iam/v1/memberships' "$R" -- 'proto/**'` | пусто |
| **П2к** | контроль — предикат исполним и различает: слово в контрактах встречается, и **каждое** вхождение есть проза комментария (про членство в **группе** и про строки принадлежности), а не объявление пути | `git grep -n 'memberships' "$R" -- 'proto/kacho/cloud/iam/v1/*.proto'` | **6** строк в **3** файлах — `access_binding_service.proto`, `user.proto`, `user_service.proto`; RPC и путей среди них **0** |
| **П3** | данные членства **есть**: таблица заведена, состояний ровно два | `git show "$R":services/iam/internal/migrations/470001_memberships_expand.sql \| grep -c "state IN ('PENDING', 'ACTIVE')"` | **1** |
| **П4** | аккаунт снят с ресурса пользователя, зарезервированы **и номер, и имя** | `git show "$R":proto/kacho/cloud/iam/v1/user.proto \| grep -c 'reserved "account_id"'` | **1** |
| **П5** | перечень аккаунтов человека — **объединение трёх источников**, а не «только активные членства»: активное членство · владение аккаунтом · действующая выдача **с областью на аккаунт** | `git show "$R":services/iam/internal/repo/kacho/pg/user_repo.go \| awk '/func \(r \*userReader\) ListAccountsForUser/,/^}/' \| grep -c 'UNION'` | **2** (то есть три ветви) |
| **П5б** | эту функцию зовёт **снимок прав**, и он единственный её не-тестовый вызывающий | `git grep -l 'ListAccountsForUser' "$R" -- 'services/iam/internal/apps/**' ':!*_test.go' \| wc -l` | **1** — `services/iam/internal/apps/kacho/api/authorize/whoami.go` |
| **П5г** | первая ветвь требует **ОБОИХ** признаков сразу — «членство активно» **и** «человек активен»; значит до первого входа она молчит, а **после** — выбирает, потому что вход делает истинными оба (П13) | `git show "$R":services/iam/internal/repo/kacho/pg/user_repo.go \| awk '/func \(r \*userReader\) ListAccountsForUser/,/^}/' \| grep -c "m.state = 'ACTIVE' AND u.invite_status = 'ACTIVE'"` | **1** |
| **П5гк** | контроль — предикат различает ветви, а не совпадает со всей функцией: третья ветвь опознаётся своим условием отдельно | тот же `awk`, `grep -c "resource_type = 'account' AND status = 'ACTIVE'"` | **1** |
| **П5в** | публичный список аккаунтов её **не зовёт вовсе**: он читает страницу курсором и судит пообъектно | `git show "$R":services/iam/internal/apps/kacho/api/account/list.go \| grep -c 'ListAccountsForUser'` | **0** |
| **П5вк** | контроль — та же команда на снимке прав находит вызовы, то есть предикат исполним | тот же `grep -c` по `services/iam/internal/apps/kacho/api/authorize/whoami.go` | **2** |
| **П9** | приглашение **с названным проектом и ролью ВСТАВЛЯЕТ ВЫДАЧУ** — область «весь проект», рядом строки субъектов и событие сверки, всё в транзакции приглашения | `git show "$R":services/iam/internal/apps/kacho/api/user/invite.go \| grep -c 'AccessBindingsW().Insert(ctx, ab)'` | **1** |
| **П9б** | область выдачи — весь проект целиком | тот же `git show`, `grep -c 'AllInScope: true'` | **1** |
| **П9в** | вставка стоит **внутри ветки непустого проекта** — то есть приглашение **без** проекта выдачи не создаёт | тот же `git show`, `awk '/if in.ProjectID != ""/,/^\t\t\t}/' \| grep -c 'AccessBindingsW().Insert(ctx, ab)'` | **1** |
| **П10** | идентификатор членства **вычислим офлайн**: `mbr-` + первые 17 знаков md5 от пары «человек:аккаунт», **без соли** | `git show "$R":services/iam/internal/migrations/470001_memberships_expand.sql \| grep -c "substr(md5('membership:'"` | **1** |
| **П10б** | функция объявлена **неизменяемой** — один и тот же вход даёт один и тот же выход всегда | тот же `git show`, `grep -c 'LANGUAGE sql IMMUTABLE'` | **1** |
| **П10в** | уникальность пары **полная**, а не частичная ⇒ снятие есть удаление строки, а повторное приглашение того же человека в тот же аккаунт даёт **тот же** идентификатор | тот же `git show`, `grep -A3 'CREATE UNIQUE INDEX memberships_user_account_unique' \| grep -c 'WHERE'` | **0** |
| **П10вк** | контроль — та же форма на **частичном** уникальном индексе того же сервиса `WHERE` находит | `git show "$R":services/iam/internal/migrations/0003_access_bindings_unique_active_grant.sql \| grep -A3 'CREATE UNIQUE INDEX access_bindings_active_grant_uniq' \| grep -c 'WHERE'` | **1** |
| **П11** | полос освобождения на крае **две, и смысл у них разный**: `<exempt>` (+ обязательный `exempt_reason`) и `scope_filtered` | `git grep -c 'scope_filtered) = true' "$R" -- 'proto/kacho/cloud/iam/v1/*.proto'` и то же по `'exempt_reason)  = '` | **7** и **17** (на стволе было 7 и 16 — §1.0б) |
| **П6** | префикс членства **не объявлен** платформе: `mbr` отсутствует в каталоге дефис-префиксов | `git show "$R":pkg/ids/ids.go \| awk '/^var hyphenFormPrefixes/,/^}/' \| grep -c '"mbr"'` | **0** |
| **П6к** | контроль — предикат исполним: соседний, объявленный, префикс он находит | тот же `awk`, `grep -c '"usr"'` | **1** |
| **П7** | консоль **молчит**: на карточке пользователя подписи аккаунта нет | `git show "$R":ui-future/iam/src/registerExtensions.tsx \| awk '/registerDetailExtension\("users"/,/^}\);/' \| grep -c 'Аккаунт'` | **0** |
| **П7к** | контроль — предикат исполним: у соседнего ресурса та же подпись стоит | тот же `awk` по `"groups"` | **1** |
| **П8** | молчание закреплено сквозной пробой, и её срок годности назван в ней самой | `git show "$R":ui-future/e2e/specs/users.spec.ts \| grep -n '1085'` | ссылка на эту задачу |
| **П17** | публичных чтений, принимающих идентификатор человека **без** обязательного аккаунта, на поверхности iam **три**, а не ноль | `for f in proto/kacho/cloud/iam/v1/*.proto; do git show "$R":"$f" \| grep -oE 'get: "[^"]*"'; done \| grep user_id \| sort -u` | **3**: `/iam/v1/users/{user_id}` · `/iam/v1/users/{user_id}/operations` · `/iam/v1/users/{user_id}/tokens` |
| **П17к** | контроль — предикат видит поверхность целиком, а не три строки: всего публичных `get:` в контрактах iam | тот же цикл без `grep user_id`, `\| wc -l` | **45** |
| **П17б** | **ни один** из этих трёх ответов аккаунта не называет: у ресурса человека номер и имя аккаунта зарезервированы (П4), а ответы операций и удостоверений несут только свои сущности и токен страницы | `git show "$R":proto/kacho/cloud/iam/v1/user.proto \| awk '/^message User \{/,/^\}/' \| grep -cE '^\s+[\w.]+ account_id'` и разбор полей обоих списочных ответов | **0** — ни одного поля аккаунта или членства |
| **П12** | приглашённому **нечем аутентифицироваться**: конструкция строки требует «приглашён ⟺ внешнего субъекта нет» | `git show "$R":services/iam/internal/migrations/0001_initial.sql \| grep -c "invite_status = 'PENDING'::text) AND (external_id = ''::text)"` | **1** |
| **П12к** | контроль — предикат исполним и различает: соседний CHECK, стерегущий **словарь** статусов, той же командой находится отдельно | тот же `git show`, `grep -c 'users_invite_status_check'` | **1** |
| **П13** | первый вход переводит в «активно» **ВСЕ** членства человека, а не одно | `git show "$R":services/iam/internal/migrations/20260823053000_first_login_activates_every_membership.sql \| grep -c "WHERE user_id = NEW.id AND state = 'PENDING'"` | **1** |
| **П14** | приглашение **уже активного** человека заводит членство сразу в «активно» — и так делают **оба** писателя строки | `git show "$R":services/iam/internal/repo/kacho/pg/user_repo.go \| grep -c "CASE WHEN i.invite_status = 'PENDING' THEN 'PENDING' ELSE 'ACTIVE' END"` | **2** — `Upsert` и `InsertPending`, по одному в каждой (проверено `awk`-срезом по функции) |
| **П14к** | контроль — предикат не совпадает со всем подряд: путь активации этой ветви не несёт | тот же `git show`, `awk '/func \(w \*userWriter\) ActivateInvite\(/,/^}/' \| grep -c 'CASE WHEN i.invite_status'` | **0** |
| **П15** | снятие членства под **живой выдачей** отвергается **конструкцией**: отложенный constraint-триггер роняет транзакцию на COMMIT | `git show "$R":services/iam/internal/migrations/472002_membership_carries_rights.sql \| grep -c 'CREATE CONSTRAINT TRIGGER membership_carrying_rights_is_kept'` | **1** |
| **П15б** | граница запрета накрывает **и проект аккаунта**, а не только сам аккаунт | тот же `git show`, `grep -c "b.resource_type = 'project' AND EXISTS"` | **1** |
| **П16** | у членства закреплён **словарь** состояний — и только он | `git show "$R":services/iam/internal/migrations/470001_memberships_expand.sql \| grep -c "memberships_state_check CHECK (state IN ('PENDING', 'ACTIVE'))"` | **1** |
| **П16б** | согласованность состояния членства со статусом человека конструкцией **не** закреплена: на таблице членств триггер ровно **один**, и он про выдачи (П15), а не про статус | обход всех миграций iam: `for f in $(git ls-tree -r --name-only "$R" services/iam/internal/migrations); do git show "$R:$f" \| awk '/CREATE (CONSTRAINT )?TRIGGER/{n=$0; getline; while ($0 !~ /ON /) getline; if ($0 ~ /memberships/) print n}'; done` | **1** строка — `membership_carrying_rights_is_kept` |

**Что из этого следует для объёма работ.** Данные есть (П3), контракта и чтения нет (П1,
П2), консоль молчит правильно (П7, П8). Значит предмет IAM-ID-2 — **читающий путь и его
разграничение**, а не хранилище и не сбор данных.

**П9 опровергает посылку, с которой этот документ писался, и посылка исправлена, а не
умолчана.** Первая редакция §2.3 утверждала, что приглашение «не даёт доступа ни к чему».
Дерево говорит обратное: приглашение, назвавшее проект и роль, **вставляет выдачу** с
областью на весь проект, пишет её субъектов и со-коммитит событие сверки — то есть право
материализуется **до первого входа**, а не после него. Верна оговорка, а не запрет:
приглашение **само по себе** права не даёт (П9в — без проекта вставки нет), а роль, названная
в приглашении, есть **отдельная выдача**, живущая по общим правилам модели прав. §2.3
переписан под это, сценарий приглашения расщеплён на два (IAM-ID-2-18 и IAM-ID-2-18б), потому
что прежний не различал два случая и потому был недетерминирован.

**П10 — несущий факт анти-оракула, а не подробность схемы.** Идентификатор членства не
чеканится, а **вычисляется** из пары «человек:аккаунт» неизменяемой функцией без соли: зная
`userId` и `accountId`, его получает кто угодно, ни разу не обратившись к платформе. Отсюда
три следствия, и каждое меняет решение:

1. **гейт IAM-ID-2-15 этого не ловит by construction.** Он ищет чтение, принимающее
   идентификатор **человека** без обязательного аккаунта; у чтения по идентификатору членства
   идентификатора человека нет **вовсе**, поэтому под предикат оно не подпадает. Предикат
   гейта расширен (IAM-ID-2-15, третья полоса) — иначе гейт был бы формой без содержания
   ровно на том входе, ради которого заведён.
2. **третью плоскую форму IAM-ID-1 нельзя оставить.** `GET /iam/v1/memberships/{id}` вместе с
   вычислимым идентификатором есть **полный межаккаунтный оракул**: перебирать нечего, адрес
   строится арифметикой. §2.6 отменяет её явно, а не по умолчанию.
3. **идентификатор переиспользуется** (П10в): уникальность полная, снятие есть удаление
   строки, функция неизменяема — значит повторное приглашение того же человека в тот же
   аккаунт вернёт **тот же** идентификатор, что был до исключения. IAM-ID-2-17 обязан
   утверждать не только неразличимость чтения, но и **что именно возвращается** в этом случае.

**П6 — не мелочь и стоит отдельной строки DoD.** Без записи в каталоге дефис-префиксов
`corevalidate.ResourceID` отвергает **корректный** идентификатор, который сам же генератор
и произвёл: `Get` членства отвечал бы `INVALID_ARGUMENT` на всяком входе, то есть возможность
существовала бы, была бы покрыта пробами формы запроса и не работала бы ни при каком вызове
(`api-conventions.md` §«ДВА ПРАВИЛА ОБ ОДНОМ ПОЛЕ»). Этот же класс уже ловили на пределе
арендатора — комментарий рядом с его префиксом в `pkg/ids/ids.go` описывает его дословно.

**П12–П14 — МАШИНА СОСТОЯНИЙ, и она опровергает половину сценариев первой редакции.** §1
первой редакции измерил **таблицу** членства и **чеканку идентификатора**, но не измерил то,
что эти строки заполняет. Три замера вместе закрывают обе ветви, и закрывают их **by
construction**, а не по недосмотру:

- **нового человека приглашают** → строка «приглашён», членство `PENDING` — и внешнего
  субъекта у строки нет, потому что конструкция его запрещает (П12). Аутентифицироваться
  ему **нечем**, значит своё чтение он вызвать не может;
- **он входит впервые** → строка перестаёт быть приглашённой, и тем же стейтментом **все**
  его членства становятся «активно» (П13). `PENDING` у него не остаётся ни одного;
- **уже активного человека приглашают** → членство заводится сразу «активно» (П14), обоими
  писателями строки. Состояния «приглашён» не возникает вовсе.

**Следствие, названное прямо: `state = "PENDING"` наблюдаемо аккаунт-скоупным чтением и НЕ
наблюдаемо своим списком.** Разные наблюдатели — разные ответы, и это не расхождение, а
арифметика: у аккаунт-скоупного чтения субъект **чужой** и вправе быть неаутентифицированным
(IAM-ID-2-02 конструируем), у своего списка субъект **сам вызывающий**, а вызвать он может,
только пройдя аутентификацию. Что из этого следует для контракта — §2.4а.

**П15 — снятие членства не всегда проходит, и документ обязан это знать.** Первая редакция
упоминала триггер удержания **ноль раз** и ставила два утверждения, которые он делает
неконструируемыми либо недетерминированными (IAM-ID-2-17 и IAM-ID-2-18б). Граница запрета
названа с обеих сторон в самой миграции и накрывает выдачи на аккаунт **и на его проект**
(П15б) — то есть ровно ту область, которой пользовался сценарий про приглашение с ролью.

**П16/П16б — что именно закреплено конструкцией, а что нет.** Закреплён **словарь**
состояний: третьего значения в колонке не появится. **Не** закреплена согласованность
состояния членства со статусом человека: триггер на таблице членств ровно один, и он про
выдачи. Согласованность держат **писатели** — зеркалящий триггер на строке человека и два
стейтмента репозитория, — поэтому прямая запись в таблицу даёт несогласованную пару
(«человек активен, членство приглашено»), и никакая конструкция её не отвергнет. Это меняет
формулировку §2.4, а не решение: инвариант остаётся, но держится он не тем, чем документ
утверждал.

---

## §2 Решения

### §2.0 Р0 — ПРИНЯТОЕ РЕШЕНИЕ: аккаунт-скоупное чтение отвечает только про СВОЙ аккаунт

> **Решение оркестратора, `PRO-Robotech/kacho#1085`, 2026-08-23.** Записано здесь потому, что
> здесь его предмет; соседний документ ссылается сюда и не пересказывает.

**Что решено, дословно:** перечень членств, отдаваемый по аккаунт-скоупному пути, содержит
**только членство в том аккаунте, о котором спрашивают**. Надзор облака видит всё;
распорядитель аккаунта — своё.

**Почему решение вообще понадобилось.** Две приёмки об одном предмете санкционировали
противоположное, и **обе стояли APPROVED**: IAM-ID-1 клал перечень **всех** членств человека
на его ресурс и возвращал его по умолчанию (IAM-ID-1-43…-48), этот документ объявлял ту же
величину утечкой (§0.1). Два места об одном предмете, из которых верно одно, — и хуже
обычного случая: оба несли вердикт, то есть по любому из них было **разрешено** кодировать.

**Четыре довода, по которым спор решён в сторону сужения. Ни один не выводится из остальных:**

1. **человек — сущность ГЛОБАЛЬНАЯ.** Одна строка на все его аккаунты (это несущее решение
   IAM-ID-1, и оно не отзывается), поэтому он может состоять в аккаунтах **разных
   организаций**. Перечень всех членств, отданный распорядителю одного из них, сообщает **о
   существовании остальных** — факт о третьих сторонах, к которым спрашивающий отношения не
   имеет;
2. **это тот же класс, что запрет инфраструктурных данных на публичной поверхности**
   (`security.md` §«Инфра-чувствительные данные»), только картируется не физика, а **состав
   арендаторов**. По нашей же мерке он **хуже**: раскрытие размещения помогает атакующему
   строить план, а раскрытие состава — уже само по себе разглашение отношений между
   организациями, и отменить его нельзя;
3. **анти-оракул.** По наличию строки в перечне вычисляется, состоит ли человек в
   **конкретном чужом** аккаунте. Отдельного глагола, отвечающего на этот вопрос, в продукте
   нет и **быть не должно** — а через перечень он получался бы бесплатно и без единого
   решения о том, что мы его завели;
4. **цена несимметрична.** Ось безопасности выигрывает много, удобство теряет мало:
   спрашивающему нужен ответ **про свой аккаунт**, и он его получает полностью. Величина,
   которой он лишается, — та, ради которой у него нет ни одного законного применения.

**Что решение НЕ отменяет.** Своё чтение (`GET /iam/v1/me/memberships`) отдаёт человеку
**все** его членства, включая приглашения: субъект — сам вызывающий, шире себя ответ не
бывает, и утечки нет by construction. Сужение касается только вопроса, задаваемого **о
другом человеке**.

**Следствие для соседнего документа — отзыв части APPROVED, а не правка.** У IAM-ID-1 снята
санкция на перечень всех членств и на столбец с аккаунтом на карточке человека; вердикт
круга 5 снят до `DRAFT`, отзыв объявлен **отдельной записью** в его шапке. Что именно
отозвано и что заменяет каждую строку — §2.6.

> [!note] Состояние соседа перемерено 2026-08-27 — он ушёл вперёд, и отзыв в нём УЖЕ УЧТЁН
> На воркспейсе `origin/main` @ `3ce88c9` IAM-ID-1 стоит ✅ **APPROVED, круг 12**
> (2026-08-24): снятие до `DRAFT` было состоянием круга 5→6, а не сегодняшним. Отзыв
> **пережил** этот цикл и записан у соседа как действующий: санкция снята у **девяти**
> сценариев (`43`–`48`, `63`, `66`, `67`), стадия S5 снята, `49` перенесён (§2.6), а закрытие
> его стадии S3 поставлено во внешнее предусловие — APPROVED **этого** документа.
>
> Абзац оставлен в прошедшем времени намеренно: он описывает **что сделало это решение**, а
> не сегодняшний статус соседа. Статус соседа стареет с его кругом, а не с нашим.

**Предикат, которым решение проверяется на реализации** (он же — предмет гейта IAM-ID-2-15,
полосы A и C): на публичной поверхности iam **ноль** чтений, отвечающих про членство без
обязательного поля аккаунта, кроме своего списка, у которого поля субъекта нет вовсе.

### §2.1 Р1 — ДВА чтения, а не одно с фильтром

**Решение.** Поверхность членства несёт **два разных чтения**, и разграничение достигается
тем, какие поля у запроса **есть**, а не тем, какие значения сервер потом отбрасывает:

| | «свой список» | «членство в моём аккаунте» |
|---|---|---|
| путь | `GET /iam/v1/me/memberships` | `GET /iam/v1/accounts/{accountId}/memberships` · `GET /iam/v1/accounts/{accountId}/memberships/{membershipId}` |
| **чей** — откуда берётся | из контекста аутентификации | — |
| **какой аккаунт** — откуда берётся | не спрашивается: отвечает про все | **из пути запроса**, обязательное поле |
| поле идентификатора человека | **отсутствует у запроса вовсе** | `filter=userId="usr-…"`, необязательное, действует **внутри** названного аккаунта |
| охват ответа | **все** членства вызывающего, включая заведённые приглашением | членства **только** названного аккаунта |

**Почему форма, а не фильтр.** Фильтр — это решение, принимаемое кодом на каждом вызове, и
его можно забыть: в новой ветке, в новом списочном методе, в оптимизации, которая «уже
проверила права выше». Отсутствующий параметр забыть нельзя — вопрос, которого нет, не
задаётся ни по ошибке, ни по невнимательности. Ровно поэтому второе чтение **по построению**
не может рассказать о чужих аккаунтах: оно их не спрашивает.

**Аккаунт в пути — сужение ДАННЫХ, а не только цель проверки прав (несущая часть решения).**
Даже вызывающий, которому выдано право прямо на объект членства, читая его по пути аккаунта
`A`, получает строку **только** если она принадлежит `A`. Иначе ответ был бы различим: «нет
такого членства» против «есть, но не у вас» — и разграничение держалось бы моделью прав,
которая отвечает на другой вопрос. Сужение стоит **в запросе к хранилищу** (аккаунт входит
в условие отбора), а не в проверке после чтения.

**Своё чтение отвечает про членства, а не про аккаунты — и это НЕ одно и то же множество.**
Перечень аккаунтов у вызывающего уже есть, но его строят **два разных механизма**, и ни один
из них не отвечает на вопрос владельца:

- `GET /iam/v1/me` — объединение **трёх** ветвей: активное членство · владение · выдача с
  областью **на аккаунт** (§1, П5). Это перечень **что мне доступно**, а не **где я числюсь**:
  ветви объединены, и по ответу нельзя сказать, какая из них выбрала аккаунт;
- `GET /iam/v1/accounts` — вообще другой путь: он этой функции **не зовёт** (§1, П5в), а
  читает страницу курсором и судит пообъектно.

> [!warning] Здесь стояло «приглашение не попадает ни в одну ветвь» — верно ТОЛЬКО до входа,
> а после входа неверно (перемерено круга 2)
> Первая ветвь требует **обоих** признаков — «членство активно» **и** «человек активен»
> (§1, П5г), — а первый вход делает истинными **оба** сразу (П13). Значит:
>
> | момент | что делает первая ветвь | может ли человек спросить |
> |---|---|---|
> | **до** первого входа | не выбирает (оба признака ложны) | **нет** — аутентифицироваться нечем (П12) |
> | **после** первого входа | **выбирает** — аккаунт приглашения в снимке **есть** | да |
>
> То есть снимок молчит об аккаунте приглашения ровно в том окне, когда его **некому
> спросить**. Утверждение «снимок приглашений не несёт» ложно в единственном наблюдаемом
> состоянии, и на нём держались §2.3 и сценарий IAM-ID-2-08; оба переписаны.

**Обоснование своего чтения от этого не рушится, а СУЖАЕТСЯ до того, что верно.** Снимок,
назвав аккаунт, не говорит **ничего** из того, ради чего своё чтение заводится:

| вопрос | отвечает ли `GET /iam/v1/me` |
|---|---|
| в каких аккаунтах я числусь **по членству**, а не по владению или выдаче | **нет** — три ветви объединены, различить их в ответе нечем |
| **кто** меня позвал и **когда** | **нет** — этих величин у него нет вовсе |
| каков идентификатор и состояние моего членства | **нет** |
| аккаунт, где членство есть, но моя личность не активна (заблокирована) | **нет** — первая ветвь требует активной личности (П5г) |

Ни то, ни другое существующее чтение не спрашивает «где я числюсь»: они спрашивают «что мне
доступно». Значит своё чтение — список **членств** со своим следом приглашения, а не
расширение снимка прав; и менять снимок под приглашения нельзя, иначе он перестанет отвечать
на свой вопрос.

### §2.2 Р2 — анти-оракул: четыре уровня, и первый — отсутствие вопроса

Различимый ответ выдаёт ровно то, что разграничение и должно было закрыть
(`security.md` §Hardening #6). Здесь он закрывается четырьмя уровнями сразу, потому что каждый
следующий страхует предыдущий от правки, которая его снимет:

1. **Структурный — вопроса не существует.** На публичной поверхности iam нет ни одного
   чтения, у которого **одновременно**: запрос называет человека · ответ называет аккаунт
   или членство · аккаунт в запросе не обязателен · ответ не сужен пообъектно правами
   вызывающего. Держится гейтом по дереву (сценарий IAM-ID-2-15), а не вниманием рецензента.

   > [!warning] Прежняя формулировка была ЛОЖНА о дереве, и ложность блокировала S1
   > Стояло: «нет ни одного чтения, принимающего идентификатор человека **без** обязательного
   > аккаунта, кроме своего списка». Их **три** (§1, П17): чтение человека, перечень его
   > операций, перечень его удостоверений. Полоса A, заведённая по этой букве, краснела бы в
   > S1 **на трёх законных RPC** — то есть §7 п. 6 («гейт заводится в S1 и здесь же полон»)
   > был бы неисполним: зелёным его не завести. А гейт, у которого все находки на существующем
   > дереве ложные, отключают первым же срабатом. Это восьмая посылка, опровергнутая замером
   > (§8, п. 12).
   >
   > **Почему эти три под запрет не подпадают — по существу, а не по списку исключений.** Ни
   > один из трёх ответов аккаунта **не называет** (§1, П17б): у ресурса человека номер и имя
   > аккаунта зарезервированы (П4), перечни операций и удостоверений несут только свои
   > сущности. Спрашивающий, назвав человека, не узнаёт **ни одного** аккаунта — значит
   > оракула нет. Опасен не вход, а **композиция входа и выхода**.
   >
   > **Список исключений здесь НЕ заводится, и это принципиально.** Перечень «эти три
   > простить» устарел бы в тот день, когда одному из трёх ответов добавят поле аккаунта:
   > запись осталась бы, а её основание исчезло. Сужен **предикат**, поэтому возврат поля
   > аккаунта на ресурс человека полосу A **роняет** — проверено инъекцией (IAM-ID-2-15), и
   > этим же полоса стала машинным исполнителем §6, п. 5.
2. **Байтовый — ответы дословно совпадают.** Три различных положения дают **один и тот же**
   ответ: тело байт-в-байт, HTTP-статус, gRPC-код, `reason`-токен. Проба утверждает
   **сообщение**, а не только код (`testing.md` §Regression-lock).
3. **Производный — ни одно поле не является функцией внешнего.** В ответе нет счётчика
   всего, признака «есть ещё где-то», отличающегося токена страницы и любого другого
   значения, вычисленного по строкам вне названного аккаунта. Обращения к таким строкам нет
   в самом запросе — поэтому различия неоткуда взять и во времени ответа.
4. **Адресный — идентификатор членства НЕ предъявитель.** Ни одно чтение не резолвит
   идентификатор членства **без названного аккаунта**. Это не дубль уровня 1, а его слепая
   зона: уровень 1 запрещает вопрос, называющий **человека**, а идентификатор членства
   человека не называет — он его **содержит**, вычислимый офлайн из пары «человек:аккаунт»
   (§1, П10). Поэтому чтение вида «дай членство по идентификатору», доступное без аккаунта,
   обходит уровень 1 целиком: спрашивающему не нужно ни перебирать, ни знать больше того, что
   он уже знает, — адрес строится арифметикой, без единого обращения к платформе.

   **Следствие для формы.** Одиночное чтение живёт **только** на аккаунт-скоупном пути
   `GET /iam/v1/accounts/{accountId}/memberships/{membershipId}`, и аккаунт в нём —
   **обязательная часть отбора**, а не украшение пути: строка отдаётся, лишь если принадлежит
   названному аккаунту. Плоской формы `GET /iam/v1/memberships/{id}` не существует (§2.6).
   Держится тем же гейтом по дереву (IAM-ID-2-15, третья полоса) — вычислимость
   идентификатора делает **прозу об этом недостаточной**: правка, вернувшая плоское чтение,
   выглядела бы удобством и открывала бы весь межаккаунтный перечень разом.

**Три положения, обязанные быть неразличимыми** (список — `200` с пустым перечнем):

| положение | что о нём знает спрашивающий сегодня | обязан узнать |
|---|---|---|
| человек существует и состоит в **другом** аккаунте | — | **ничего** |
| человек существует и не состоит **нигде** | — | ничего |
| идентификатор well-formed и не резолвится **ни во что** | — | ничего |

Отсюда следствие, которое легко упустить: well-formed, но не существующий `userId`
**не даёт** `NOT_FOUND`. Отказ по отсутствию был бы оракулом наоборот — он отличал бы
«такого человека нет» от «есть, но не у вас». Отвергается только **форма** идентификатора
(это факт о строке, а не о платформе), и отвергается синхронно.

**Отрицание идёт в паре с положительным контролем.** Утверждение «ответы неразличимы»
зеленеет на поверхности, которая не отвечает никому: пустой перечень на любой вход
удовлетворяет ему полностью. Поэтому каждый сценарий §5 группы F3 несёт **положительный
контроль**: человек, который в этом аккаунте **есть**, даёт другой, непустой ответ.

### §2.3 Р3 — членство САМО ПО СЕБЕ права не даёт; роль в приглашении — отдельная выдача

> [!warning] Прежняя редакция этого раздела была опровергнута деревом — исправлено, а не снято
> Здесь стояло: приглашение «не даёт доступа ни к чему». Замер (§1, П9) говорит обратное:
> приглашение, назвавшее **проект и роль**, вставляет выдачу с областью на весь проект, пишет
> её субъектов и со-коммитит событие сверки — в той же транзакции, то есть право
> материализуется **до первого входа**. Утверждение было ложным, и на нём держались §2.5 и
> сценарий приглашения; оба переписаны. Разбор оставлен, потому что ошибка правдоподобна:
> «приглашённый ещё не вошёл» интуитивно читается как «прав у него нет».

**Решение — различать ДВЕ величины, которые прежняя редакция смешивала:**

| величина | что это | даёт ли доступ |
|---|---|---|
| **членство** в состоянии `PENDING` | строка `memberships`: человек позван в аккаунт | **нет** — ни к содержимому аккаунта, ни к самому аккаунту как объекту |
| **выдача**, созданная тем же вызовом приглашения, когда назван проект и роль | обычная запись выдачи с областью на проект | **да** — по общим правилам модели прав, как у выдачи, сделанной напрямую |

**Норма.** Членство правами не распоряжается **ни в каком состоянии**: оно отвечает на вопрос
«где человек числится», а не «что он может». Право, приехавшее вместе с приглашением, есть
**выдача** и живёт своей жизнью: выдаётся, ограничивается областью, **отзывается** и видна в
аудите — отдельно от членства и переживая смену его состояния.

**Признак нарушения, который эта норма закрывает.** Рассуждение «приглашение прав не даёт,
значит проверять нечего» — и построенный на нём сценарий, чей исход зависит от того, назвали
ли при приглашении проект. Такой сценарий **недетерминирован**: два законных вызова
приглашения дают разные ответы на «может ли он читать ресурс `A`», и оба верны.

**Граница со снимком прав — временнáя, а не смысловая, и это выяснилось перемером круга 2.**
`GET /iam/v1/me` строит перечень объединением трёх ветвей (§1, П5). До первого входа его не
выбирает **ни одна**: членство `PENDING` и личность `PENDING` — первая ветвь требует **обоих**
активными (П5г); владения нет (вторая); выдача приглашения **проектная**, тогда как третья
спрашивает область **на аккаунт**. **После** первого входа первая ветвь выбирает — и снимок
аккаунт **называет** (П13 делает истинными оба признака одним стейтментом).

**Отсюда — что именно обязан сказать комментарий поля снимка**, и это не то, что стояло здесь
прежде. Не «приглашений он не несёт» (ложь в единственном наблюдаемом состоянии), а: перечень
отвечает на вопрос **«что мне доступно»**, ветви в нём объединены и в ответе неразличимы,
следа приглашения — кто и когда — у него нет; «где я числюсь по членству» читается
`GET /iam/v1/me/memberships`. Иначе получаются два места об одном предмете, и следующий
читатель «починит» снимок под несуществующее обещание.

Что при этом остаётся верным и несущим: **выдача с областью на аккаунт назвала бы аккаунт
и до первого входа** — по третьей ветви, — и это было бы правильным поведением, а не
дефектом. Тишина снимка никогда не была свойством «приглашение не может ничего».

### §2.4 Р4 — «исключён» это отсутствие строки, а не состояние

Состояний у членства **два** — «приглашён» и «активно», — и **словарь** этих состояний
закреплён конструкцией (§1, П3, П16): третьего значения в колонке не появится. Исключение из
аккаунта — **снятие строки**, а не третье состояние. Наблюдаемое следствие, и оно обязано быть
проверено: после исключения оба чтения отвечают **в точности так же**, как для человека,
которого в этом аккаунте не было никогда. Тень исключённого — тот же оракул: она говорит
«здесь кто-то был».

> [!warning] Здесь стояло «оба состояния закреплены конструкцией схемы» — это шире измеренного
> Конструкцией закреплён **словарь** (`memberships_state_check`, П16). Согласованность
> состояния членства со **статусом человека** конструкцией **не** закреплена: триггер на
> таблице членств ровно один, и он про выдачи (П16б). Её держат **писатели** — зеркалящий
> триггер на строке человека и два стейтмента репозитория (П13, П14), — поэтому прямая
> запись в таблицу даёт несогласованную пару «человек активен, членство приглашено», и
> отвергнуть её нечем.
>
> Различие не педантское: на «закреплено конструкцией» опираются утверждения §2.4а о том,
> **чего не бывает**. Они верны для путей, которые дерево несёт сегодня, и **не** являются
> гарантией уровня схемы. Формулировка сужена до фактически закреплённого; сам инвариант
> остаётся, но чем он держится — сказано верно.

**Снятие членства проходит НЕ ВСЕГДА, и это конструкция, а не отказ реализации.** Отложенный
constraint-триггер `membership_carrying_rights_is_kept` (§1, П15) роняет транзакцию **на
COMMIT**, если у человека остаётся **живая** выдача с областью на этот аккаунт **или на его
проект** (П15б). Границу он объявляет сам: держат членство только выдачи со статусом
«действует», адресующие **этого** человека и лежащие в области **этого** аккаунта; прочие
типы ресурсов в запрет не входят.

**Что из этого следует для сценариев этого документа** (и почему два из них переписаны):

| было бы | почему неконструируемо |
|---|---|
| «членство снято, а выдача жива» | транзакция снятия отвергается — такого состояния не бывает |
| «членство снимается» без оговорки о выдачах | исход зависит от неназванной величины: есть живая выдача — отказ, нет — успех. Недетерминированное утверждение |

Поэтому IAM-ID-2-17 несёт оговорку о выдачах **в предусловии**, а IAM-ID-2-18б разводит
членство и право **обратным** ходом — снятием **выдачи**, — и вдобавок утверждает сам отказ
снятия членства как наблюдаемое свойство. Это не обход препятствия: отказ строже, чем
исходное утверждение, потому что доказывает, что право **нельзя** осиротить, а не то, что
его забыли отозвать.

**Точный код и текст отказа снятия здесь НЕ фиксируются.** Снятие — глагол, меняющий
состояние, и он вне границ этого документа (§0.3): его контракт отказа принадлежит IAM-ID-1.
Здесь утверждается ровно то, что наблюдается **читающими** путями: транзакция не прошла, и
членство по-прежнему читается обоими чтениями.

**Запрет участия сюда не едет.** Он свойство личности, а не членства (так решено при
заведении таблицы), и его разведение на «исключить из моего аккаунта» и «закрыть вход на
платформу» — предмет `PRO-Robotech/kacho#1102`. Этот документ не пишет ни одного сценария,
чей вердикт зависел бы от запрета участия, — иначе он закреплял бы поведение, которое там
намеренно меняют.

### §2.4а Р4а — «приглашён» видит АДМИНИСТРАТОР; в своём списке этого состояния не бывает

**Предмет.** Требование владельца — «приглашённый в своём списке может увидеть где он
приглашён» — первая редакция прочла как «в своём списке он видит запись со `state = PENDING`».
Замер (§1, П12–П14) говорит, что такой записи **не бывает ни при каком входе**, и обе ветви
закрыты конструкцией:

| ветвь | что происходит | почему `PENDING` в своём списке недостижим |
|---|---|---|
| приглашён **новый** человек | строка «приглашён», членство `PENDING` | внешнего субъекта у строки нет (П12) ⇒ токена он получить не может ⇒ **своё чтение не вызовет** |
| он **вошёл впервые** | строка перестала быть приглашённой | тем же стейтментом **все** его членства стали «активно» (П13) ⇒ `PENDING` не осталось |
| приглашён **уже активный** человек | строка не меняется | членство заводится сразу «активно» (П14), обоими писателями ⇒ `PENDING` не возникал |

Аккаунт-скоупного чтения это **не** касается: там субъект чужой и вправе быть не входившим,
поэтому IAM-ID-2-02 конструируем и остаётся как есть. Асимметрия наблюдателей — свойство
предмета, а не дефект: своё чтение требует аутентификации, а аутентификация и есть то
событие, которое состояние снимает.

**РЕШЕНИЕ — исход (а): требование выполняется ПОЛНОТОЙ перечня, а не состоянием.** Свой
список отвечает **всеми** аккаунтами, где человек числится, — включая те, куда его **позвали**,
— и «позвали» читается по полю `invitedBy` (плюс `createdAt` — когда), а **не** по `state`.
Ни одно утверждение этого документа о своём списке не опирается на `state = "PENDING"` как на
наблюдаемую величину.

**Почему поле `invitedBy` для этого годится, а `state` — нет.** Активация трогает `state` и
`updatedAt` и **не** трогает `invitedBy` и `createdAt` (П13 — второй стейтмент перечисляет
изменяемые колонки поимённо; путь активации строки человека `invited_by` не переписывает,
П14к). То есть след приглашения переживает вход, а состояние — нет. Величина, которая
исчезает ровно в тот момент, когда её впервые можно прочитать, наблюдаемой не является.

**Что при этом ЧЕСТНО не достигается — названо, а не умолчано.** «Увидеть приглашение **до**
первого входа» неисполнимо не по объёму работ, а по построению: до первого входа человек не
может обратиться к платформе вовсе. Сделать это состояние наблюдаемым можно **только** сменой
машины состояний — приглашение как отдельный ресурс со своим шагом принятия и своим способом
предъявить себя без полноценной личности. Это **другой предмет** и **решение владельца**: §6,
п. 6 держит его вне границ вместе с предикатом.

**Отсюда — граница S2, записанная явно.** S2 кодируется **сейчас** и в исходе (а): своё
чтение, полный перечень, `invitedBy` как след приглашения. Она **не** ждёт решения о машине
состояний, потому что от него не зависит: полнота перечня — свойство отбора, а не состояний.
Появится шаг принятия — `state` в своём списке станет различающим, и ни один сценарий этого
документа от этого не покраснеет: ни один из них не утверждает, что `PENDING` там **не
встречается**.

**`state` при этом остаётся в контракте и заполняется всегда** (§3.1). Он различает состояния
там, где различать есть что, — в аккаунт-скоупном чтении; в своём списке он сегодня принимает
одно значение, и документ этого не скрывает. Убирать поле из проекции своего списка **нельзя**:
проекции не расходятся (§3.1), а «поле есть и всегда одинаково» честнее, чем «в одном чтении
есть, в другом нет».

### §2.5 Р5 — свой список гейтится ЛИЧНОСТЬЮ, а не моделью прав

Своё чтение объявляется самообслуживанием: субъект берётся из контекста аутентификации,
объекта, про который край мог бы спросить модель, у запроса нет by construction. Аутентификация
при этом **обязательна** — анонимный вызывающий получает `UNAUTHENTICATED`, а не пустую
страницу; освобождение снимает проверку **прав**, но никогда не проверку **личности**
(`security.md` §«AuthN+AuthZ ВЕЗДЕ»). Прецедент формы и её освобождения уже есть на этой же
поверхности — `GET /iam/v1/me`.

**Почему модель прав здесь не годится, и это не удобство.** Приглашение кортежа доступа не
создаёт — иначе оно давало бы доступ (§2.3). Значит гейт по модели сделал бы требование
владельца **неисполнимым by construction**: приглашённый не увидел бы приглашения ни при
каком состоянии дренажа. Утечки при этом нет: субъект — сам вызывающий, шире себя ответ не
бывает.

**Отсюда — единственное освобождение этого документа. Полос ДВЕ, и выбрать надо явно.**
Край объявляет их машинно, и смысл у них разный (§1, П11):

| полоса | что снимает | что оставляет | чем опасна не на своём месте |
|---|---|---|---|
| `scope_filtered = true` (при непустом `permission`) | пообъектную проверку **до** обработчика | принципала — **обязательным**; сужение переезжает на данные, ниже по стеку | ничем: она идёт **после** извлечения принципала |
| `<exempt>` (+ обязательный `exempt_reason`) | проверку прав целиком | — | несёт **второй смысл**: допускает вызов внутреннего слушателя **вообще без принципала**, по сетевому положению |

**Выбор — `scope_filtered`, а не `<exempt>`, и причина не стилистическая.** У своего списка
сужение и есть личность: строки отбираются по принципалу, других входов у запроса нет. Значит
вызов **без принципала сузить не к чему** — под `<exempt>` он был бы допущен на внутреннем
слушателе и вернул бы... что? Либо пусто (то есть «членств нет» — ложь), либо всё (то есть
межаккаунтный перечень без единой проверки). Полоса обязана быть той, что идёт **после**
извлечения принципала. Ровно этот выбор и с той же мотивировкой уже сделан на соседнем
списочном чтении этой же поверхности, и его причина записана в контракте, а не в чьей-то
памяти.

**Наблюдаемое следствие, и оно проверяется на ОБОИХ слушателях.** Раз субъект — сам принципал,
отсутствие принципала не есть «пустой ответ»: это отказ аутентификации. Поэтому IAM-ID-2-10
утверждается **и на публичном, и на внутреннем** слушателе — освобождение снимает проверку
**прав** и никогда проверку **личности** (`security.md` §«AuthN+AuthZ ВЕЗДЕ»: internal не
освобождён). Утверждение на одном публичном слушателе оставило бы непроверенной ровно ту
полосу, на которой `<exempt>` и опасен.

**И освобождение сужено формой запроса.** Как только у запроса появится хоть одно поле,
называющее чужого субъекта, оно перестаёт быть защитимым: сужение по принципалу перестаёт быть
единственным. Гейт этого — тот же IAM-ID-2-15.

### §2.6 Р6 — что этот документ правит в IAM-ID-1

> [!important] ПРЕДИКАТ КРУГА 1 БЫЛ ОДНОЯЗЫЧНЫМ И ПОТЕРЯЛ БЛОКИРУЮЩЕЕ — перемерено 2026-08-24
> Круг 1 считал затронутые строки IAM-ID-1 так:
> ```sh
> F=docs/specs/sub-phase-IAM-ID-1-identity-account-decoupling-acceptance.md
> awk '/^#### IAM-ID-1-/ { if (id!="" && hit) print id; id=$2; hit=0; next }
>      /memberships|MembershipService/ { if (id!="") hit=1 }
>      END { if (id!="" && hit) print id }' "$F"      # 6
> ```
> и получал **шесть**: `-01`, `-08`, `-54`, `-63`, `-66`, `-67`. Тот же предикат со **вторым
> языком корпуса** даёт **сорок**:
> ```sh
> awk '/^#### IAM-ID-1-/ { if (id!="" && hit) print id; id=$2; hit=0; next }
>      /memberships|MembershipService|членств/ { if (id!="") hit=1 }
>      END { if (id!="" && hit) print id }' "$F"      # 40   (всего сценариев в IAM-ID-1: 70)
> ```
>
> **Недостача пришлась ровно на блокирующее.** Среди тридцати четырёх пропущенных — вся
> группа F (`-43`, `-44`, `-45`, `-46`, `-47`, `-48`), то есть **те самые** строки, которые
> санкционировали перечень всех членств и столбец на карточке человека. Одноязычный предикат
> их не видел **by construction**: их тела называют предмет по-русски («перечень членств»,
> «ноль членств»), английского токена в них нет ни одного.
>
> Класс назван в корпусе (`testing.md` §«Предикат по ДВУЯЗЫЧНОМУ корпусу…»): недостача
> ложится не на случайные места, а на те, где предмет **объясняли словами, а не координатой**,
> — то есть на разборы и решения. Здесь он сработал в чистом виде.
>
> **Расширять grep до сорока — тоже не починка.** Сорок — верхняя граница кандидатов, а не
> перечень правимых строк: `-02`…`-05` говорят о заведении членства (предмет IAM-ID-1),
> `-08` и `-54` — о **таблице**, а не о чтении. Правимых строк — **десять**, и они названы
> поимённо в двух таблицах ниже, каждая со своим исходом. Единица счёта: блок сценария,
> **чью величину или адрес этот документ меняет**.
>
> **Спор двух рецензентов о числе («шесть» против «тридцати четырёх») разрешён замером, а не
> выбором большего.** Оба прокси негодны: шесть — недобор, доказуемо терявший блокирующее;
> сорок — перебор. Годным оказался вопрос «что этот документ **опровергает**», и ответ на
> него даёт адъюдикация кандидатов, а не совпадение слова.
>
> **Числа «6» и «40» относятся к соседу на ревизии круга 2 и уже сдвинулись — перемерено
> 2026-08-27** (воркспейс `origin/main` @ `3ce88c9`): одноязычный предикат даёт **4**,
> двуязычный — **43**, всего сценариев у соседа **71** (было 70). Вывод от этого не меняется
> ни на йоту — недобор остаётся недобором, — но число без ревизии неопровержимо, и потому
> ревизия названа.

IAM-ID-1 объявил поверхность членства **плоской**: `GET /iam/v1/memberships`,
`GET /iam/v1/memberships?userId=<id>`, `GET /iam/v1/memberships/{id}`. Отменяются **все три**.

**Таблица А — поверхность чтения (4 строки).** Санкция снята, предмет каждой строки сохранён
и перенацелен:

| строка IAM-ID-1 | что с ней |
|---|---|
| IAM-ID-1-01, последнее **And** (`GET /iam/v1/memberships?userId=<id>` даёт два членства) | **заменяется**: тот же факт утверждается своим списком приглашённого — `GET /iam/v1/me/memberships` (IAM-ID-2-07) |
| IAM-ID-1-67 («список членств фильтруется по правам») | **перенацеливается** на аккаунт-скоупный список; плоского списка не остаётся (IAM-ID-2-01, -03) |
| IAM-ID-1-63, IAM-ID-1-66 (malformed, пагинация) | **перенацеливаются** на новые пути; предмет не меняется (IAM-ID-2-04, -05) |
| IAM-ID-1-64 (полосы резолва) | **остаётся**; здесь она усиливается требованием байт-идентичности §2.2 |

**Таблица Б — консоль (6 строк), и это НОВОЕ круга 2; круг 3 нашёл её независимо и тем же путём.** Круг 1 их не видел (одноязычный
предикат выше). Здесь снимается не адрес, а **величина**: перечень всех членств человека:

| строка IAM-ID-1 | что с ней |
|---|---|
| IAM-ID-1-43 (списочное чтение обязано заполнять членства) | **отозвана**: предмет — перечень **всех** членств на ресурсе человека, по умолчанию. Требование «список и одиночное чтение заполняют одно и то же» остаётся и перевыражено в §3.1 и IAM-ID-2-01 — уже про членство в названном аккаунте. Держится **машинно**, а не отзывом на словах: инъекция *A-ter* полосы A (IAM-ID-2-15) краснеет ровно на этой форме, чей бы код её ни завёл |
| IAM-ID-1-44 (ноль членств называется словами) | **отозвана** вместе со столбцом; норма `ui.md` правило 6 применена к состоянию членства на карточке (IAM-ID-2-19). Решение, которым столбец снят, — §0.1: нечему называть ноль |
| IAM-ID-1-45 (одно членство — ссылкой) | **отозвана** вместе со столбцом; форма ссылки (`ui.md` правило 2) применена там же |
| IAM-ID-1-46 (много членств — первый плюс счётчик) | **отозвана**, и она противоречит решению сильнее прочих: счётчик есть **признак числа других аккаунтов** — ровно то, что IAM-ID-2-20 запрещает показывать. Перечень в подсказке хуже счётчика: он называет остальные аккаунты **поимённо** |
| IAM-ID-1-47 (порядок объявлен незначимым) | **отозвана** вместе с полем; требование объявить порядок незначимым перенесено в §3.1 для обоих новых чтений |
| IAM-ID-1-48 (радиус правки — одна спека) | **отозвана**: радиус считался от заведения столбца заново, а столбец не заводится. Со стороны пробы это же утверждает IAM-ID-2-22 |

**IAM-ID-1-49 (проба браузером падает до фикса) НЕ отозвана** — она про порядок работ
(`ui.md` правило 12), а не про величину; её предмет **перенесён** сюда, к пробам F5
(производитель — IAM-ID-2-22).

> [!note] Здесь два круга разошлись, и спор разрешил СОСЕД, а не выбор большего
> Круг 2 (ствол) читал `49` как порядок работ и оставлял её; круг 3 (параллельная линия) —
> как величину и отменял «вместе с предметом». Обе стороны согласны, что столбец не
> заводится; расходились они в том, **чем считать сценарий**.
>
> Разрешено замером, а не голосованием: соседний документ прошёл **круг 12** и стоит
> ✅ APPROVED (2026-08-24), где `49` записан **перенесённым**, а не отозванным, — и там же
> сказано, почему: у отозванного санкция снята потому, что реализовывать **нельзя**, а у `49`
> предмет верен. Отозванных у соседа **девять** (`43`–`48`, `63`, `66`, `67`), и `49` в них
> не входит. Перемерено 2026-08-27 на воркспейсе `origin/main` @ `3ce88c9`.

**Третья форма — `GET /iam/v1/memberships/{id}` — отменяется ЯВНО, и это не симметрия ради
порядка.** Прежняя редакция отменяла первые две («одно чтение с фильтром») и третью оставляла
как безобидную: идентификатор выглядит непредсказуемым, а значит неперебираемым. Он
**предсказуем**: `mbr-` + первые 17 знаков md5 от пары «человек:аккаунт», функция неизменяема
и без соли (§1, П10). Значит плоское чтение по идентификатору — это тот же вопрос «в каких
аккаунтах состоит этот человек», только заданный **арифметикой вместо параметра**: зная
`userId` и перебирая `accountId` (а перечень аккаунтов — не секрет), спрашивающий получает
полный межаккаунтный ответ, ни разу не назвав человека в запросе. Одиночное чтение живёт
**только** на аккаунт-скоупном пути (§2.2, уровень 4), и держит это гейт, а не проза.

**Расхождение, которое этот документ НЕ решает и потому называет.** Глагол создания членства
IAM-ID-1 держит на плоской коллекции (`POST /iam/v1/memberships`), а чтения переезжают на
аккаунт-скоупную и на свою. Поверхность становится **трёхадресной**:

| адрес | глаголы после обеих стадий |
|---|---|
| `/iam/v1/memberships` | **только создание**; **ни одного глагола чтения** |
| `/iam/v1/accounts/{accountId}/memberships[/{membershipId}]` | список и одиночное чтение |
| `/iam/v1/me/memberships` | свой список |

Что здесь важно сказать вслух: плоская коллекция **читающих глаголов не несёт вовсе** — иначе
уровень 4 §2.2 держался бы на том, что никто не добавит `GET` рядом с уже стоящим там `POST`.
Приводить поверхность к одной форме — работа IAM-ID-1 (поток приглашения — его предмет, §0.3),
и она обязана быть сделана одним из двух способов явно, а не оставлена как есть. Здесь она
названа, чтобы следующий круг ревью IAM-ID-1 её увидел.

> [!warning] Расхождение было живым, и сосед о нём НЕ ЗНАЛ — перемерено 2026-08-27, теперь знает
> IAM-ID-1 **APPROVED** (круг 5, 2026-08-21) и объявляет плоское чтение `GET
> /iam/v1/memberships/{id}` разрешённым. Его исполнитель вправе приземлить эту форму **уже
> сейчас**, и вердикт этого документа его не связывает: приёмки не читают друг друга.
> Измерено: `git show origin/main:<приёмка IAM-ID-1> | grep -c 'IAM-ID-2'` даёт **0** —
> отмены в соседнем документе нет, и обещать обратное нельзя (контроль: тот же `grep -c
> 'IAM-ID-1'` даёт **168**, то есть предикат исполним).
>
> **Отсюда следует ровно одно, и это не «договориться», а построение:** отмена держится
> **полосой C** гейта IAM-ID-2-15, которая красит плоское чтение по идентификатору
> независимо от автора и от того, какой документ его разрешил. Это и есть причина, по которой
> полоса C заведена гейтом, а не прозой: проза соседнего исполнителя не связывает.
>
> **Что обязано было случиться в соседнем круге — СЛУЧИЛОСЬ; перемерено 2026-08-27 на
> воркспейсе `origin/main` @ `3ce88c9`.** Соседний документ прошёл **круг 12** и стоит
> ✅ **APPROVED** (2026-08-24, база — продукт `release/identity-4` @ `76c12ec78`). В нём:
> санкция **отозвана у девяти** сценариев — `43`–`48`, `63`, `66`, `67`; плоская поверхность
> чтения членств и форма «в каких аккаунтах состоит НАЗВАННЫЙ человек» объявлены отменёнными;
> закрытие его стадии S3 поставлено во **внешнее предусловие** — APPROVED этого документа.
> Предикат и его контроль: `grep -c 'IAM-ID-2'` по соседу даёт **119** (было **0**),
> `grep -c 'IAM-ID-1'` — **255** (было **168**).
>
> **Полоса C от этого не снимается, и причина не в недоверии.** Она красит плоское чтение по
> идентификатору **независимо от того, какой документ его разрешил**, — то есть держит
> свойство дерева, а не согласие двух текстов. Согласие текстов достигнуто и может быть
> потеряно следующей правкой; гейт — нет.
>
> **Урок, ради которого абзац оставлен, а не удалён:** утверждение «сосед о нём не знает»
> было **верно в день записи** и стало ложью за трое суток. Всякое утверждение о СОСЕДНЕМ
> документе стареет с его кругом ревью, а не с нашим, — и потому обязано нести предикат,
> которым его перепроверяют (`doc-truthfulness`).

---

### Вторая половина отмены — КОНСОЛЬ: стадия S5 соседа (задача `#473`, сценарии 43–49)

**Это блокирующее, и прежняя редакция §2.6 его не видела.** Перечень выше называет только
чтения API. Но APPROVED-вердикт соседа разрешал и стадию **S5**, чей предмет — вернуть на
страницу пользователей столбец «Аккаунт» **как перечень членств**, с тегом «+(N−1)» и полным
перечнем в подсказке. Это ровно то, что §0.1 называет **утечкой**, а IAM-ID-2-20 запрещает
поимённо («нигде не показано число аккаунтов человека и признак „есть ещё“»).

> [!note] Прошедшее время здесь — не стиль: перемерено 2026-08-27
> На воркспейсе `origin/main` @ `3ce88c9` соседний документ прошёл **круг 12**
> (✅ APPROVED, 2026-08-24) и **снял S5 как свою стадию**: сценарии `43`–`48` остаются в нём
> текстом с отозванной санкцией. То есть предмет этого раздела **закрыт у соседа**, и раздел
> остаётся не как действующее предупреждение, а как разбор: он называет **чем** отмена
> держится машинно, **почему** дерево выглядело согласным до неё и **какой единицей** такое
> считать. Ровно та половина, которую вердикт соседа не заменяет.

**Сосед САМ назвал условие снятия этих сценариев, и это условие — решение владельца из §0.1.**
Его текст: «если владелец решит, что „аккаунты человека“ на странице пользователей не
показываются **вовсе** — тогда 44-48 снимаются одним решением и его раздел трассировки пересчитывается. Это
вопрос В-3 §7». **§0.1 этого документа и есть то решение** (указание владельца 2026-08-23).
То есть IAM-ID-2 отвечает на В-3 соседа, и ответ — «не показываются вовсе».

**Исход по каждому из шести — в Таблице Б выше**, и он там не пересказывается: два места
об одном предмете расходятся на первой же правке. Здесь названо то, чего таблица не
содержит, — **чем** отмена держится, **почему** дерево сегодня выглядит согласным и **какой
единицей** считать затронутое.

**Сегодня конфликта в дереве нет, и это ровно то, что делает его опасным.** Консоль столбца не
несёт, и комментарий рядом говорит почему — поле снято с ресурса человека, источника у столбца
нет. То есть дерево согласно с §0.1 **случайно**: не по решению, а потому что источник ещё не
приехал. Наша стадия S2 этот источник **и заводит**. Значит исполнитель S5 вправе взять свою
стадию сразу после неё и приземлить межаккаунтный перечень со счётчиком — с APPROVED-вердиктом
в руках и не нарушив ни одного правила.

**Условие «F5 не раньше круга IAM-ID-1» (§4) этого НЕ закрывает** — оно связывает **нашу**
консоль, а не соседскую. Единственное, что связывает соседа механически, — полоса A гейта
IAM-ID-2-15 (инъекция *A-ter*): она красит поле членств на ресурсе человека, то есть источник
столбца, независимо от того, чей код его завёл. Сам столбец, нарисованный из
аккаунт-скоупного чтения, гейтом не ловится — это **признано и вынесено в §6, п. 10** как
открытый предмет с адресом, а не закрыто прозой.

**Годная единица — та, которую сосед объявляет САМ, числами:** его собственный раздел
трассировки распределяет все 70
сценариев по стадиям и задачам таблицами, где стоят только номера и идентификаторы. Стадия
**S5** = задача **`#473`** = сценарии **43–49**, семь штук. Перепись становится двухчастной, и
обе части языконезависимы:

| часть | единица | предикат | охват |
|---|---|---|---|
| API-чтения | **путь** в контракте (координата, переводу не подлежит) | `awk` по блокам сценариев соседа с образцом `/iam/v1/memberships` | 01, 63, 64, 66, 67 |
| консоль | **строка таблицы трассировки соседа** — стадия и задача, в них только номера | `awk -F'\|' '/S5\|#473/{print $3}'` по его разделу трассировки | 43–49 |

Адъюдикация внутри названных групп остаётся ручной — и это честно: «что именно отменяется»
есть суждение о смысле, а не предикат. Но **множество кандидатов** теперь выводится числами
соседа, а не поиском слова, и потому от языка не зависит.

**Разрешает спор круг ревью IAM-ID-1** — там живёт предмет. До тех пор действует сторона,
ошибка в которой дороже: перечень аккаунтов человека, показанный администратору одного из
них, есть факт о третьих сторонах (§0.1).

### §2.7 Р7 — полоса аккаунт-скоупного чтения: пообъектный гейт КРАЯ, а не сужение на данных

> **Замечание круга 1, блокирующее №2.** Два сценария требовали от **одного и того же входа**
> взаимоисключающего: вызывающий без прав, аккаунт-скоупный путь, законная пагинация —
> IAM-ID-2-03 требовал **отказа**, IAM-ID-2-05 требовал **`200` с пустым перечнем**. Обе
> формулировки защитимы по отдельности и невыразимы вместе: исход выбирает **устройство**, а
> устройства было два.

**Решение. Аккаунт-скоупные чтения гейтятся ПООБЪЕКТНО НА КРАЮ**, по объекту аккаунта,
названного в пути. Полоса сужения на данных (`scope_filtered`) у них **не объявляется**;
она остаётся ровно у одного RPC — своего списка (§2.5).

**Почему эта полоса, а не сужение на данных — три довода, и первый решает сам по себе:**

1. **аккаунт в пути ОБЯЗАТЕЛЕН, значит объект вопроса существует.** Пообъектный гейт края
   применим тогда и только тогда, когда у запроса есть **один** объект, про который можно
   задать **один** вопрос. Здесь он есть by construction (§2.1). У своего списка его нет
   вовсе — там сужение и есть личность, и полоса обязана быть другой. То есть выбор не
   вкусовой: у двух чтений **разные** формы запроса, поэтому и полосы разные;
2. **сужение на данных здесь было бы вторым замком к уже закрытой двери.** Строки и так
   отбираются по аккаунту из пути (§2.1) — это не listauthz, а условие отбора. Объявить
   вдобавок `scope_filtered` значило бы снять пообъектную проверку края и заменить её кодом,
   который можно забыть в следующей ветке;
3. **третий сценарий уже это требовал, и требование остаётся в силе.** IAM-ID-2-06:
   «сужение объявлено **ровно у одного** RPC — своего списка». Сделай аккаунт-скоупный список
   `scope_filtered` — и в дереве стало бы два, то есть -06 краснел бы на нашей же реализации.
   Из трёх сценариев два склоняли к одной полосе, один к другой; правым оказалось
   большинство **по устройству**, а не по счёту.

**Предикат решения — машинный, читается из контракта и из ведомости края:**

```sh
R=<ревизия, на которой ведётся работа>
# 1) записей сужения на данных среди чтений членства — НОЛЬ, кроме своего списка:
git show "$R":gateway/internal/middleware/embed/permission_catalog.json | python3 -c "
import json,sys
for e in json.load(sys.stdin):
    if 'Membership' in e['fqn']:
        print(e['fqn'], '| scope_filtered=', bool(e.get('scope_filtered')),
              '| rel=', e.get('required_relation'),
              '| scope=', (e.get('scope_extractor') or {}).get('object_type'),
                          (e.get('scope_extractor') or {}).get('from_request_field'))"
# ожидается ровно:
#   ...MembershipService/List        | scope_filtered= False | rel= viewer | scope= account account_id
#   ...MembershipService/Get         | scope_filtered= False | rel= viewer | scope= account account_id
#   ...MembershipService/ListMine    | scope_filtered= True  | rel= <непустое>
```

**Наблюдаемое следствие, и оно НЕ нарушает порядок «формат раньше замыкания».** У
вызывающего **без** права на аккаунт край отвечает **до** сервиса, поэтому негодная
пагинация даёт **отказ прав**, а не `400`. Это задокументированная authz-first полоса
(`api-conventions.md` §«gRPC-код → HTTP-статус», абзац про единственное исключение;
`testing.md` §e2e-инварианты «Authz-first толерантность негативов»), а требование
`security.md` Hardening #7 говорит о **другом** предмете — о замыкании на пустом гранте
**внутри** сервиса, до которого такой вызов не доходит by construction.

**Как приведены в согласие оба сценария:**

| | было (круг 1) | стало (круг 2) |
|---|---|---|
| **IAM-ID-2-03** | «отказ по полосе, которой край гейтит объект аккаунта» — полоса не названа | полоса названа: пообъектный гейт края по `viewer` @ `account` (§2.8); исход — `403` / `PERMISSION_DENIED`, и почему **не** скрывающий `404`, сказано там же |
| **IAM-ID-2-05** | вызывающий **без прав** получает `200` с пустым перечнем при законной пагинации | предмет сценария сохранён, а **вызывающий заменён**: порядок «формат раньше замыкания» утверждается тем, кто **проходит** гейт края (право на `A` есть) — иначе сценарий проверял бы не порядок внутри сервиса, а поведение края. Для вызывающего **без** права добавлено утверждение, что исход **один и тот же** при любой пагинации: отказ прав, а не `400` — то есть ответ не является функцией того, годен ли ввод |

**Что этим НЕ решено и названо, а не умолчано.** Своё чтение (`scope_filtered`) до
обработчика доходит всегда, поэтому порядок «формат раньше замыкания» на нём проверяется
без оговорок — IAM-ID-2-11. Tree-wide гейт
`internal/repohygiene` `TestEmptyPageNeverPrecedesPaginationValidation` судит **функции**, а
не полосы края, и обязан покрыть обе новые списочные функции независимо от того, какая полоса
у их RPC.

### §2.8 Р8 — отношение: `viewer` на объекте аккаунта

> **Замечание круга 1, блокирующее №3.** Сценарий говорил «отказ по полосе, которой край
> гейтит объект аккаунта», не называя **отношения**. Отношение — часть контракта: от него
> зависит, кто прочитает членства, а кто получит отказ.

**Решение. `required_relation: viewer`, `scope_extractor {object_type: account,
from_request_field: account_id}`** — для обоих аккаунт-скоупных чтений (список и одиночное).

**Почему НЕ `v_get`/`v_list`, хотя это первый напрашивающийся выбор.** Глагольные отношения
объекта аккаунта объявлены **без тира администратора намеренно**, и у решения есть
структурный замок в пробе:

```
define v_get:  [user, service_account, group#member] or owner or super_admin
define v_list: [user, service_account, group#member] or owner or super_admin
```

Комментарий модели говорит прямо: тир `admin` сюда **не** включён, потому что он принимает
прямых субъектов, и чтение его дало бы **делегированному распорядителю аккаунта сам объект
аккаунта**, — а он распоряжается тем, что **внутри** аккаунта, и аккаунт есть граница этой
области, а не вещь внутри неё. Замок — `services/iam/internal/authzmap/account_owner_structural_test.go`.

**Следствие, из-за которого выбор `v_*` был бы дефектом:** под ним делегированный
распорядитель **не прочитал бы членства в своём же аккаунте** — то есть ровно тот, кого
директива владельца называет адресатом («администратор аккаунта, карточка сотрудника —
членство только в своём аккаунте», §0.1). Возможность была бы объявлена, задокументирована,
покрыта пробами формы запроса — и не работала бы для своего адресата
(`api-conventions.md` §«ДВА ПРАВИЛА ОБ ОДНОМ ПОЛЕ»).

**Прецедент, по которому выбрано `viewer` — снятие членства судится ТИРОМ АККАУНТА.**
Измерено на базе сверки:

| глагол | отношение | что это значит |
|---|---|---|
| `UserService/RemoveFromAccount` | `member_remover` (объявлено `define member_remover: editor` — **дословно** тир) | исключить человека из аккаунта вправе тир аккаунта |
| `UserService/Invite` | `editor` | ввести человека в аккаунт — тот же тир |
| `AccountService/Get` | `v_get` | прочитать **сам объект** аккаунта — тир НЕ читается (граница области) |

Предикат (повторяется дословно):
```sh
R=origin/release/identity-3
git show "$R":gateway/internal/middleware/embed/permission_catalog.json | python3 -c "
import json,sys
for e in json.load(sys.stdin):
    if (e.get('scope_extractor') or {}).get('object_type')=='account':
        print(e['fqn'], e['required_relation'])"
#   ... UserService/Invite            editor
#   ... UserService/RemoveFromAccount member_remover
#   ... AccountService/Get            v_get
#   всего записей с object_type=account: 12
```

**Отсюда вывод, и он односторонний.** Читать участие обязан **как минимум** тот, кто вправе
его создать и снять: иначе распорядитель убирал бы из аккаунта человека, которого не видит.
`viewer` — это в точности `editor` плюс прямые читатели:

```
define admin:  [user, service_account, group#member] or owner or super_admin
define editor: [user, service_account, group#member] or admin
define viewer: [user, service_account, group#member] or editor
```

то есть круг чтения **содержит** круг записи и добавляет к нему тех, кому выдано только
чтение. Обратный порядок (чтение уже записи) был бы дефектом; равенство кругов здесь не
нужно — читать разрешено шире, чем менять.

**Отношение НЕ выполнимо подстановочным знаком, и это проверено, а не предположено.**
`user:*` встречается в модели ровно в двух местах, и `account.viewer` среди них нет:

```sh
git show "$R":proto/kacho/cloud/iam/v1/fga_model.fga | grep -n 'user:\*'
#   cluster.viewer            — намеренно, глобальный справочник
#   registry_repository.v_get — намеренно, анонимное чтение публичного репозитория
```
Требование `security.md` §«Отношение, выполнимое подстановочным знаком» выполнено: наше
отношение сужает (IAM-ID-2-06).

**Модель прав этим решением НЕ правится, и это несущее свойство.** Ни нового типа, ни нового
отношения: `account.viewer` уже объявлен и уже имеет читателей. Цепочка тиров накрывает все
три уровня, которых требует директива (`security.md` §«Три уровня супер-доступа»): надзор
облака — через `super_admin: any_admin from cluster`, владелец — через `owner`, делегированный
распорядитель — через `admin`. Граница «распорядитель не трогает сам объект аккаунта» не
задевается: мы читаем **членства**, а не аккаунт.

> [!note] Побочное следствие, названное прямо: на отказе край отдаёт **403**, а не 404
> Скрытие существования на крае включается не «для чужого ресурса вообще», а для конкретного
> сочетания метода, отношения и области — это **поле записи** каталога, и сегодня оно стоит у
> **трёх** записей из **342**:
> ```sh
> git show "$R":gateway/internal/middleware/embed/permission_catalog.json | python3 -c "
> import json,sys
> c=json.load(sys.stdin); print(len(c), sum(1 for e in c if e.get('hide_existence')))"
> #   342 3      (session_reader@iam_user, v_delete@registry_registry, v_update@registry_registry)
> ```
> Ни одной — на `account`, ни одной — с `viewer`. Значит вызывающий без права получает
> `403` / `PERMISSION_DENIED`.
>
> **Оракула это не создаёт, и довод не в намерении, а в устройстве.** Модель прав не знает
> понятия «существует»: на несуществующий аккаунт она отвечает тем же «пути нет», что и на
> чужой, — то есть исход **тот же 403**. Сам текст отказа при этом не несёт ни имени ресурса,
> ни отношения: край подменяет причину неразглашающим описанием
> (`gateway/internal/middleware/permission_denied_response.go`), а разбор остаётся в журнале
> сервера. IAM-ID-2-03 этот исход прямо допускает и требует **положительного контроля
> байт-идентичности**: тот же вызов на несуществующий аккаунт даёт **тот же самый** ответ.

---

## §3 Форма ресурса и двух чтений

### §3.1 Ресурс членства — плоский, output-only, без инфраструктурных полей

| поле (JSON) | смысл | заполняется |
|---|---|---|
| `id` | идентификатор членства, дефис-канон `mbr-<17>` | всегда |
| `accountId` | аккаунт, которому принадлежит членство | всегда |
| `accountName` | человекочитаемое имя аккаунта, **зеркало** (источник истины — сам аккаунт) | best-effort; пустое допустимо и означает «имя не задано», а не «аккаунта нет» |
| `userId` | человек | всегда |
| `state` | `PENDING` · `ACTIVE` | **всегда, в обеих проекциях.** Различающим он является в аккаунт-скоупном чтении; в своём списке сегодня принимает одно значение — `PENDING` там недостижим by construction (§2.4а). Поле остаётся: проекции не расходятся, а «есть и всегда одинаково» честнее, чем «в одном чтении есть, в другом нет» |
| `invitedBy` | кто пригласил, идентификатор человека. **Это и есть след приглашения, переживающий вход** — активация трогает `state` и `updatedAt` и не трогает его (§1, П13; §2.4а) | пусто для членства, заведённого не приглашением |
| `createdAt` | когда членство заведено (для приглашения — когда выписано) | всегда, усечено до секунд |
| `updatedAt` | когда состояние менялось в последний раз | всегда, усечено до секунд |

**Порядок перечня объявлен: это НАБОР.** Порядок элементов обоих списков определяется
курсором `(createdAt, id)` и значимым **не является** — вызывающий сверяет состав, а не
индексы. Это сказано здесь и обязано быть сказано в комментарии поля контракта
(`api-conventions.md` §«Пустое значение обязано означать „пусто“», подкласс A).

**Обе проекции заполняют один и тот же набор полей.** Одиночное чтение и список отдают одно
сообщение с одинаково заполненными полями; расхождение проекций законно только там, где
контракт назвал их разными, — здесь он их разными не называет (тот же §, подкласс B).

**Инфраструктурного на этой поверхности нет.** Ни внутренних идентификаторов, ни признаков
размещения, ни следов очередей материализации (`security.md` §«Инфра-чувствительные данные»).

### §3.2 Формы запросов

```
GET /iam/v1/me/memberships?pageSize=&pageToken=
GET /iam/v1/accounts/{accountId}/memberships?pageSize=&pageToken=&filter=userId="usr-…"
GET /iam/v1/accounts/{accountId}/memberships/{membershipId}
```

- пагинация — курсорная, по общей конвенции: `pageSize` вне `[0..1000]` и негодный
  `pageToken` **отвергаются**, а не подрезаются;
- `filter` — ровно один терм из белого списка (`userId`); неизвестный терм —
  `INVALID_ARGUMENT` с именем поля;
- все три чтения синхронные: это чтения, операции они не порождают.

---

## §4 Scope — две стадии, каждая production-complete

| # | Стадия | Что входит | Почему в этом порядке |
|---|---|---|---|
| F1 | **S1** — членство в моём аккаунте | `Get` и `List` на аккаунт-скоупных путях, права, отказы, пагинация | закрывает предикат, названный в самой задаче: «существует чтение, отвечающее — к какому аккаунту относится это членство» |
| F2 | **S2** — свой список | `GET /iam/v1/me/memberships`, самообслуживание, приглашения | закрывает уточнение владельца: приглашённый видит, где он приглашён |
| F3 | **S1** — анти-оракул | неразличимость трёх положений; гейт **трёх полос** — отсутствие вопроса по субъекту, по терму фильтра и **по идентификатору членства** (он вычислим офлайн, §1 П10) | это **свойство S1**, а не отдельная работа: без него S1 не production-complete |
| F4 | **S1+S2** — состояния членства и граница «членство ≠ право» | что видно при «приглашён», «принял», «исключён»; и что приглашение **с ролью** даёт, а **без роли** — нет | состояния наблюдаемы обоими чтениями, поэтому проверяются на обоих; граница названа сценарной парой 18/18б, а не прозой |
| F5 | **S1+S2** — консоль | карточка сотрудника, свой раздел, перенацеливание существующей пробы | консоль показывает **источник**, а не подпись; появляется вместе с ним. **Приземляется не раньше круга ревью IAM-ID-1** — условие ниже |

> [!important] Условие по F5, поставленное рецензентом круга 1 — записано, а не удержано памятью
> **F5 не приземляется до круга ревью IAM-ID-1.** Причина в предмете: карточка сотрудника и
> перенацеливание существующей сквозной пробы стоят на поверхности, которую правит IAM-ID-1
> (поток приглашения — его предмет, §0.3), и приземлить консоль раньше значило бы закрепить
> пробой то, что соседний круг может изменить. До этого дня в документе следа условия не было,
> а единственный комментарий разбора в `PRO-Robotech/kacho-workspace#383` — **авторский**: то
> есть условие держалось памятью двух человек и исчезло бы вместе с ней.
>
> **Предикат снятия:** круг ревью IAM-ID-1 состоялся и его вердикт вынесен. До этого F5 в
> работу не берётся, даже если S1 и S2 закрыты; сценарии 19–22 остаются написанными и
> неприземлёнными, и это **не** долг без ответственного — у условия назван предмет и назван
> момент снятия.

**S1 самодостаточна — и это утверждение теперь ПРОВЕРЯЕМО, а не объявлено.** После неё
администратор читает членство в своём аккаунте, консоль называет его на карточке (с учётом
условия по F5 выше), а чужие аккаунты не раскрываются ни прямо, ни разностью ответов.
Приглашённый своего списка ещё не имеет — это объявлено `Out-of-scope` S1 и закрывается S2.

**Предикат самодостаточности** (единица счёта — блок сценария §5; проверяется по разделу §5 и
по столбцу «S1» таблицы §7):

```sh
F=docs/specs/sub-phase-IAM-ID-2-membership-read-acceptance.md
S1ONLY='IAM-ID-2-01|IAM-ID-2-02|IAM-ID-2-03|IAM-ID-2-04|IAM-ID-2-05|IAM-ID-2-06|IAM-ID-2-12|IAM-ID-2-13|IAM-ID-2-14|IAM-ID-2-15|IAM-ID-2-19|IAM-ID-2-20|IAM-ID-2-22|IAM-ID-2-18б'
sed 's/\*\*//g; s/`//g' "$F" | awk '
  /^## §5 /{on=1} /^## §6 /{on=0}
  on && /^#### IAM-ID-2-/ { if (id!="" && hit) print id; id=$2; hit=0; next }
  !on { next }
  /^>/ { next }                                      # цитата дефекта — не дефект
  /S1\)|S2\)|в S1|в S2|S1 —|S2 —|стади/ { next }     # строка с пометкой стадии не судится
  /me\/memberships|свой список|своего списка|своим списком|после S1 и S2/ { if (id!="") hit=1 }
  END { if (id!="" && hit) print id }' | grep -E "^($S1ONLY)$"
```
Пустой вывод — норма; непустой — находка. Прогон на этом документе даёт **0**.

**У предиката три условия нормализации, и каждое куплено собственной ошибкой** — все три
найдены кругом 3, и без них он давал «чисто» на дефекте либо кричал на исправном:

| нормализация | без неё | класс |
|---|---|---|
| снятие разметки (`sed 's/\*\*//g'`) | не находит **собственную** формулировку: в тексте `после S1 **и S2**`, а образец искал `после S1 и S2` | предикат меряет разметку, а не предмет |
| пропуск строк с пометкой стадии | краснеет на IAM-ID-2-06, где упоминание своего списка **размечено** как принадлежащее S2 | ложная находка на исправном |
| пропуск цитат (`^>`) | краснеет на IAM-ID-2-15, чья врезка **цитирует** снятую формулировку, объясняя её | `testing.md` §«Гейт на класс», п. 4 — гейт находит слово в комментарии, объясняющем этот же гейт |

**Контроль в обе стороны прогнан, а не объявлен.** Вставка `**Given** дерево после S1 **и
S2**` в сценарии «только S1» → предикат называет **IAM-ID-2-04** и **IAM-ID-2-14**; вставка
`**And** проверяется свой список членств` в IAM-ID-2-13 → называет **IAM-ID-2-13**. На
неизменённом документе — **0**.
Из его вывода **ни один** идентификатор не принадлежит стадиям «только S1» — то есть F1
(01–06), F3 (12–15) и F5 (19, 20, 22). Сценарии F4 (16, 17, 18, 18б) в выводе присутствовать
**вправе**: они объявлены S1+S2 и приписаны стадиям «в части» (§7).

**Приём «в части» применён ко ВСЕМ трём местам, где S1 упиралась в S2** — а не к одному, как
было: F4 нёс оговорку, а IAM-ID-2-06, IAM-ID-2-15 и пункт 6б DoD её не несли и требовали
предмета из S2, приписанного стадии S1.

**S2 самодостаточна.** Она не трогает ни путей, ни прав S1: у неё своя форма запроса, своя
полоса сужения (`scope_filtered`, §2.5) и свой раздел консоли.

---

## §5 Сценарии

Обозначения: **Given** — предусловие, **When** — вызов с конкретным телом, **Then** —
верифицируемое утверждение (код, поля, текст). Утверждается **пара** «HTTP-статус + gRPC-код»
(`api-conventions.md` §«gRPC-код → HTTP-статус»); `FAILED_PRECONDITION` — это **400**, а не
412, и допуска на 412 ни один сценарий не содержит.

## F1 — членство в моём аккаунте (S1)

#### IAM-ID-2-01 · Администратор читает членство сотрудника в своём аккаунте

**Given** аккаунт `A`, вызывающему выдано на нём отношение чтения `viewer` (§2.8)
**And** человек `usr-P` состоит в `A` в состоянии `ACTIVE`

**When** клиент вызывает `GET /iam/v1/accounts/A/memberships?filter=userId="usr-P"`

**Then** `200` / `OK`
**And** перечень содержит **ровно одну** запись
**And** у неё `accountId = "A"`, `userId = "usr-P"`, `state = "ACTIVE"`, непустые `id` формы
`mbr-<17>` и `createdAt`, усечённый до секунд
**And** повторное чтение той же записи по идентификатору —
`GET /iam/v1/accounts/A/memberships/{id}` — отдаёт **то же** сообщение с тем же набором
заполненных полей (проекции не расходятся)

#### IAM-ID-2-02 · Приглашённый в мой аккаунт виден администратору как приглашённый

**Given** аккаунт `A`, вызывающему выдано на нём отношение чтения `viewer` (§2.8)
**And** человеку `usr-Q` выписано приглашение в `A`, он ещё не входил

**When** клиент вызывает `GET /iam/v1/accounts/A/memberships?filter=userId="usr-Q"`

**Then** `200` / `OK`
**And** запись присутствует со `state = "PENDING"`
**And** `invitedBy` называет пригласившего
**And** `createdAt` — момент выписки приглашения
**And** положительный контроль соседа: у активного члена того же аккаунта состояние
`ACTIVE` — то есть поле различает состояния, а не отдаёт константу

#### IAM-ID-2-03 · Аккаунт, на который права нет, — отказ пообъектного гейта края

**Given** аккаунт `B`, вызывающему на нём не выдано ничего
**And** в `B` есть членства

**When** клиент вызывает `GET /iam/v1/accounts/B/memberships`

**Then** `403` / `PERMISSION_DENIED` — отказ пообъектного гейта края по объекту аккаунта
(§2.7), отношение `viewer` @ `account`, поле области — `account_id` из пути (§2.8)
**And** исход **один и тот же** при каждом повторе и **не зависит** от того, что лежит в `B`
**And** **не** скрывающий `404`: признак скрытия существования — **поле записи** каталога, и
у этой записи его нет; предикат — `hide_existence` отсутствует у записи чтения членства
(сегодня во всём каталоге такой признак стоит у **3** записей из **342**, ни одной на
`account`, §2.8)
**And** оракула отсюда не возникает, и это утверждается **пробой, а не доводом**:
положительный контроль байт-идентичности — тот же вызов на аккаунт, которого **нет**
(well-formed, не резолвится), даёт **тот же самый** ответ, тело в тело. Модель прав не знает
понятия «существует», поэтому «чужой» и «несуществующий» отвечают одинаково by construction
**And** утверждается **сообщение**, а не только код: проба, сверяющая один код, зеленеет на
любом расхождении текста (`testing.md` §Regression-lock)
**And** текст отказа **не называет** ни идентификатора аккаунта, ни отношения, ни причины
отказа модели: край подменяет её неразглашающим описанием, и это тоже утверждается дословно
**And** ответ не содержит ни одного поля, производного от содержимого `B` — ни числа
членств, ни токена страницы
**And** положительный контроль полосы: получив `viewer` на `B`, тот же вызывающий
получает `200` и непустой перечень — то есть отказ был свойством прав, а не сломанного пути
**And** положительный контроль **тира**: вызывающий, которому на `B` выдан тир распорядителя
(`admin`), получает `200` — то есть выбранное отношение накрывает адресата директивы
владельца, а не только владельца аккаунта. Без этой половины дефект §2.8 (выбор `v_get`,
не читающего тир) прошёл бы незамеченным: все прочие утверждения сценария остались бы верны

> [!note] Почему у этого сценария появился контроль ПО ТИРУ — круг 2
> Круг 1 не называл отношения вовсе, и любое из них удовлетворяло сценарий. Между тем
> глагольные отношения объекта аккаунта тир распорядителя **намеренно не читают** (§2.8), и
> под ними этот сценарий был бы зелёным, а продукт — неисполнимым для того, ради кого он
> написан. Контроль по тиру и есть та половина, которая различает два выбора.

#### IAM-ID-2-04 · Негодная форма отвергается синхронно, первым стейтментом

**Given** дерево после S1

**When** клиент вызывает
`GET /iam/v1/accounts/{accountId}/memberships/{membershipId}` с `membershipId = "не-идентификатор"`
**And** отдельно — тот же путь с `accountId = "не-идентификатор"`
**And** отдельно — `GET /iam/v1/accounts/A/memberships?filter=email="p@example.test"`
(терм вне белого списка)

**Then** каждый вызов — `400` / `INVALID_ARGUMENT`
**And** текст контракт-тоном: для идентификатора — `"invalid <res> id '<X>'"`, для терма —
с именем поля
**And** отказ синхронный, до любого обращения к хранилищу
**And** `reason`-токен полосы формы, а не полосы отсутствия
**And** положительный контроль: **well-formed** и несуществующий `membershipId` даёт
`404` / `NOT_FOUND`, а не `400` — формат и существование остаются разными полосами
**And** каталог дефис-префиксов платформы содержит `mbr` (иначе корректный идентификатор
отвергался бы как негодный — §1, П6)

#### IAM-ID-2-05 · Пагинация проверяется ДО сокращения — и вызывающий назван тот, кто до неё доходит

**Given** вызывающий `C`, которому на `A` выдано право чтения (`viewer`, §2.8) — то есть
пообъектный гейт края он **проходит** (§2.7)
**And** в `A` есть членства

**When** `C` вызывает `GET /iam/v1/accounts/A/memberships?pageSize=5000`
**And** отдельно — тот же путь с негодным `pageToken`

**Then** оба вызова — `400` / `INVALID_ARGUMENT`, а не `200` с пустым перечнем
**And** проверка формата стоит **в той же функции**, которая замыкается на пустом отборе
**And** тот же разбор курсора, что исполняется на пути чтения, — второго кодека не заводится
**And** `pageSize` вне `[0..1000]` **отвергается**, а не подрезается
**And** tree-wide гейт `internal/repohygiene`
`TestEmptyPageNeverPrecedesPaginationValidation` остаётся зелёным и **покрывает** обе новые
списочные функции
**And** **положительный контроль — законная страница проходит по ТОМУ ЖЕ пути**: тот же `C`
с **законными** `pageSize=10` и без `pageToken` получает `200` и непустой перечень. Без этой
половины отрицание зеленеет на поверхности, отвергающей **всё**: «негодный вход отвергнут»
истинно и у пути, который не принимает ни одного входа
**And** **второй положительный контроль — на ПУСТОМ отборе, а не на пустом гранте**:
вызывающий `D` с правом чтения на `A`, где нет ни одного членства, подходящего под его
`filter`, при законной пагинации получает `200` с пустым перечнем, а при `pageSize=5000` —
`400`. То есть порядок утверждён именно там, где замыкание и происходит: отбор пуст, но
формат уже проверен

**And отдельная половина — вызывающий БЕЗ права, и здесь исход ДРУГОЙ, объявленный:**
вызывающий `E`, которому на `A` не выдано ничего, получает **`403`** / `PERMISSION_DENIED`
и с законной пагинацией, и с `pageSize=5000`, и с негодным `pageToken` — **один и тот же**
ответ во всех трёх случаях
**And** это утверждается **явно**, а не терпится `oneOf`: исход не является функцией того,
годен ли ввод, — край отвечает раньше сервиса, и никакой ввод до проверки формата не доходит
**And** положительный контроль этой половины: тот же `E`, получив `viewer` на `A`,
получает на законной пагинации `200`, а на `pageSize=5000` — `400`; то есть `403` был
свойством прав, а не общим отказом пути

> [!important] Круг 1 требовал здесь `200` с пустым перечнем от вызывающего БЕЗ прав — и это
> противоречило IAM-ID-2-03, требовавшему от того же входа отказа
> Оба утверждения защитимы по отдельности; вместе они невыразимы, потому что исход выбирает
> **устройство**, а не кейс. Полоса выбрана и записана решением с предикатом (§2.7):
> аккаунт-скоупные чтения гейтятся пообъектно **на крае**. Отсюда: вызывающего без прав
> перенесли в **свою** половину с **своим** объявленным исходом, а порядок «формат раньше
> замыкания» утверждается тем, кто до замыкания **доходит**.
>
> Требование `security.md` Hardening #7 этим не ослаблено: его предмет — замыкание на пустом
> гранте **внутри** сервиса, и оно проверяется второй положительной половиной (пустой отбор
> при живом праве) и IAM-ID-2-11 на своём списке, где полоса другая и вызов до обработчика
> доходит всегда.

> Юнит на саму функцию проверки регрессией на это **не является**: он зовёт её без
> вызывающего и без замыкания, поэтому о порядке не утверждает ничего.

#### IAM-ID-2-06 · Каталог прав полон, и ни одно отношение не выполнимо подстановкой

> **Сценарий прогоняется НА КАЖДОЙ стадии — по RPC этой стадии.** Прежняя редакция ставила
> «сужение объявлено **ровно у одного** RPC — своего списка», а своего списка в S1 нет: таких
> RPC у неё **ноль**. Утверждение, приписанное S1, требовало предмета из S2, и S1 не могла его
> закрыть ни при какой реализации.

**Given** заведены чтения членства **своей стадии** — в S1 это аккаунт-скоупные список и
одиночное чтение, в S2 к ним добавляется свой список

**When** прогнан `make -C gateway permission-catalog-check`

**Then** гейт зелёный, обе встроенные копии каталога совпадают байт-в-байт
**And** у **каждого** нового RPC есть запись каталога — её отсутствие даёт fail-closed отказ
в рантайме, а не «работает по умолчанию»
**And** ни один аккаунт-скоупный RPC не гейтится отношением, выполнимым подстановочным
знаком (`security.md` §«Отношение, выполнимое подстановочным знаком»)
**And** **сужение на данных — по стадиям, и это не одно утверждение, а два:**
  - **S1** — полосы сужения не несёт **ни один** её RPC, и **ни один** не освобождён от
    проверки прав: у аккаунт-скоупных чтений объект, про который край спрашивает модель,
    есть — это сам аккаунт из пути. Число объявленных полос среди RPC S1 — **ноль**, и это
    утверждение, а не умолчание;
  - **S2** — сужение объявлено **ровно у одного** RPC, своего списка (§2.5), и той полосой,
    которая идёт **после** извлечения принципала (`scope_filtered`), а не той, что допускает
    вызов без принципала; причина записана в контракте, а не в чьей-то памяти
**And** у **обоих** аккаунт-скоупных чтений полосы сужения на данных **нет**, а есть
`required_relation: viewer` и `scope_extractor {object_type: account, from_request_field:
account_id}` (§2.7, §2.8) — предикат §2.7 печатает все три записи разом, и расхождение видно
одной строкой
**And** отношение аккаунт-скоупных чтений **читает тир распорядителя**: `viewer` выводится
через `editor` и `admin`, а глагольные отношения объекта аккаунта (`v_get`/`v_list`) тир
**намеренно не читают** — гейт утверждает выбор **сравнением двух объявлений**, а не
присутствием одного. Без этой половины запись `v_get` прошла бы все прочие проверки каталога
и оставила бы делегированного распорядителя без чтения членств в своём аккаунте
**And** **способность гейта упасть доказана инъекцией в обе стороны** — как у IAM-ID-2-15, и
по той же причине: гейт, ни разу не проверенный на возврате дефекта, неотличим от гейта,
который не читает предмет вовсе:
  - *возврат дефекта*: запись каталога у одного из новых RPC снята — гейт **красный**
    и называет **координату** (имя RPC), а не «каталог несогласован»;
  - *второй возврат*: у аккаунт-скоупного RPC отношение подменяется на выполнимое
    подстановочным знаком — гейт **красный** и называет этот RPC;
  - *третий возврат*: у аккаунт-скоупного RPC `viewer` подменяется на `v_list` — гейт
    **красный** и называет отношение, а не «каталог несогласован». Эта инъекция обязательна:
    подмена выглядит уместной (глагольное отношение на глагольном чтении), проходит все
    прочие проверки каталога и ломает ровно адресата директивы владельца (§2.8);
  - *законные близнецы молчат* — по одному на каждый возврат, иначе гейт ловит форму, а не
    существо, и первый же ложный срабат его отключит: `AccountService/Get`
    с `v_get` @ `account` — **не** находка, потому что он читает **сам объект аккаунта**,
    а граница области там проведена намеренно (§2.8); и `RemoveFromAccount` с
    `member_remover` находкой не становится — отношение без приставки глагола там выбрано
    осознанно и объяснено в модели;
  - *законный близнец молчит* — **в S1**: аккаунт-скоупное чтение с полной записью каталога
    и отношением, подстановкой не выполнимым, находкой не становится;
  - *законный близнец молчит* — **в S2**: свой список, объявленный `scope_filtered` с непустым
    `permission`, находкой **не** становится. Этот близнец приезжает **вместе со своим
    предметом**: в S1 объявлять нечего
**And** гейт печатает **объём осмотренного** (сколько записей каталога прочитано, сколько
RPC рассмотрено): «ноль находок» обязано быть отличимо от «ноль прочитанного»

## F2 — свой список членств (S2)

#### IAM-ID-2-07 · Свой список полон: все аккаунты, где числюсь, — включая те, куда позвали

> **Состояние в предусловии не называется, и это следствие замера, а не упрощение.** Прежняя
> редакция ставила «приглашён в `C`» и ждала `state = "PENDING"` в ответе. Такой записи в
> своём списке не бывает: чтобы его вызвать, надо аутентифицироваться, а первый вход
> переводит в «активно» **все** членства человека (§1, П13; приглашение уже активного и вовсе
> заводит членство активным — П14). Утверждение было неисполнимо ни при каком входе. Предмет
> сценария — **полнота перечня**, и она от состояний не зависит (§2.4а).

**Given** человек `usr-P` активен и аутентифицирован, вызывает от своего имени
**And** он состоит в `A` и в `B`
**And** в `C` он оказался **по приглашению**: строка членства заведена приглашением, и её
`invitedBy` называет пригласившего

**When** он вызывает `GET /iam/v1/me/memberships`

**Then** `200` / `OK`
**And** перечень содержит **ровно три** записи с `accountId` `A`, `B`, `C` — то есть аккаунт,
куда его **позвали**, из перечня не выпадает
**And** у записи `C` `invitedBy` **непуст** и называет пригласившего, `createdAt` — момент
выписки приглашения: «куда позвали» читается по следу приглашения, а не по состоянию (§2.4а)
**And** **положительный контроль поля `invitedBy`**: у записи `A`, заведённой не приглашением,
`invitedBy` **пуст** — то есть поле различает два случая, а не отдаёт константу
**And** `state` заполнен у **каждой** записи (проекции не расходятся, §3.1); документ **не**
утверждает, какие именно значения он здесь принимает — сегодня по построению одно, и
сценарий не покраснеет, если завтра их станет два
**And** у каждой записи заполнен `accountName` либо он пуст **и означает «имя не задано»** —
никогда «аккаунта нет»
**And** **положительный контроль поля**: у аккаунта с заданным именем `accountName` **непуст**
и равен имени аккаунта — то есть поле различает два состояния, а не отдаёт константу. Без него
«пусто означает „имя не задано“» зеленело бы на зеркале, которое не заполняется никогда
**And** состав сверяется по множеству `accountId`, а не по индексам: порядок объявлен
незначимым (§3.1)

#### IAM-ID-2-08 · Позванный видит аккаунт, куда его позвали, — и видит, КТО и КОГДА

> [!warning] Флагманский сценарий переписан: его прежнее предусловие было САМОПРОТИВОРЕЧИВО
> Стояло: «он вошёл впервые и его личность активна» **и** «приглашение ещё не принято» **и**
> в ответе `state = "PENDING"`. Три условия вместе неисполнимы: шага принятия отдельно от
> входа не существует (§6, п. 6), а первый вход переводит в «активно» **все** членства
> человека одним стейтментом (§1, П13). До входа же он не аутентифицируется вовсе (П12) и
> своего чтения не вызовет. То есть сценарий требовал состояния, которого нет ни в одной
> точке жизни человека, — и §6, п. 6 того же документа это прямо говорил.
>
> **Требование владельца при этом выполняется** — исходом (а) §2.4а: человек видит **аккаунт,
> куда его позвали**, вместе с тем, **кто** и **когда** его позвал. Не выполняется другое —
> «увидеть приглашение **до первого входа**», и это неисполнимо не по объёму работ, а по
> построению: до входа обратиться к платформе нечем. Наблюдаемость самого состояния требует
> смены машины состояний — §6, п. 6, решение владельца.

**Given** человеку `usr-Q` выписано приглашение в `A` **без проекта и роли** — то есть выдачи
оно не создало (§1, П9в); это часть предусловия, а не подробность: с названным проектом
ответы двух чтений ниже были бы другими, и оба были бы верны
**And** он вошёл впервые и его личность активна
**And** иных оснований числиться в `A` у него нет: ни владения, ни выдачи с областью на `A`

**When** он вызывает `GET /iam/v1/me/memberships`

**Then** `200` / `OK`
**And** перечень содержит запись с `accountId = "A"` — то есть аккаунт, куда его позвали, он
видит, и видит **своим** чтением, а не чужим
**And** `invitedBy` и `createdAt` заполнены — человек видит, **кто** и **когда** его позвал;
именно они переживают вход, тогда как `state` его не переживает (§2.4а)
**And** `GET /iam/v1/me` этот аккаунт **называет** — и это не противоречие, а причина, по
которой своё чтение всё равно нужно: вход сделал истинными оба признака первой ветви (§1,
П5г, П13). Снимок сказал «аккаунт `A` мне доступен» и **не сказал** ни по какой ветви он там
оказался, ни кто позвал, ни когда — §2.1
**And** **различающее утверждение вместо тишины**: у того же человека есть аккаунт `D`, где он
состоит **не по приглашению**. Снимок называет `A` и `D` **одинаково** — различить их в нём
нечем; своё чтение членств различает: у `A` `invitedBy` непуст, у `D` пуст. Именно эта пара и
доказывает, что своё чтение отвечает на **другой** вопрос, а не дублирует снимок
**And** `GET /iam/v1/accounts` этот аккаунт **не возвращает** — и по причине, к ветвям
отношения не имеющей: публичный список аккаунтов той функции не зовёт вовсе (§1, П5в), он
читает страницу курсором и судит пообъектно; аккаунт не попадает в ответ потому, что на нём
у вызывающего нет пообъектного права
**And** **положительный контроль к этому отрицанию**: выдав тому же человеку право чтения на
`A`, тот же вызов `GET /iam/v1/accounts` начинает `A` **возвращать** — то есть «не
возвращает» было свойством прав, а не списка, молчащего обо всём

> Это сценарий, ради которого переформулирована задача. Неисполнимо сегодня **не** «увидеть
> аккаунт» — снимок его называет, — а увидеть, что человек в нём **по приглашению**, **от
> кого** и **когда**. Этих величин нет ни у одного из двух существующих механизмов, и по
> **разным** причинам (оба **And** выше). Чтение заводится ради вопроса, которого ни один из
> них не задаёт.
>
> [!warning] Прежняя редакция обещала здесь тишину ОБОИХ механизмов — снимок не молчит
> Стояло «`GET /iam/v1/me` этот аккаунт **не называет**», и обоснование ссылалось на то, что
> членство «ещё `PENDING`». Под собственным предусловием сценария («он вошёл впервые») это
> ложно: вход переводит членство в «активно» (§1, П13), и первая ветвь снимка его **выбирает**
> (П5г). Утверждение было бы красным при исправном продукте. Предмет сценария от правки
> **усилился**: доказывается не тишина, а **неразличимость** — снимок называет аккаунт
> приглашения и аккаунт обычного членства **одинаково**.

> [!note] «Общая тишина двух механизмов» оказалась НЕ ОБЩЕЙ и НЕ ТИШИНОЙ — оба круга подряд
> Круг 1 нашёл, что два **And** объяснялись одной фразой «по той же причине», а причины
> разные: снимок прав и публичный список аккаунтов строятся **разными механизмами** (§1, П5в).
> Круг 2 нашёл, что и «тишина» — не общая: снимок аккаунт **называет** (П5г), молчит только
> публичный список аккаунтов.
>
> Урок, из-за которого записка остаётся: **общее наблюдаемое поведение — самое ненадёжное
> основание для объединения объяснений.** Дважды подряд оно связало вещи, у которых общим было
> только то, как они выглядели в один конкретный момент. Оба раза разрешил спор замер
> **механизма**, а не наблюдение ответа.

#### IAM-ID-2-09 · Поля «за кого спрашиваю» у своего списка нет вовсе

**Given** человек `usr-P` аутентифицирован

**When** он вызывает `GET /iam/v1/me/memberships?userId=usr-OTHER`
**And** отдельно — `GET /iam/v1/me/memberships?filter=userId="usr-OTHER"`

**Then** ответ **не зависит** от этих параметров: перечень — членства `usr-P` и только его
**And** исход объявлен явно и одинаков при каждом повторе: либо неизвестный параметр
отвергается `400` / `INVALID_ARGUMENT` с именем поля, либо путь не имеет такого поля в
контракте вовсе
**And** «принято и молча выброшено» исходом **не является** (`api-conventions.md`
§«Принято-и-проигнорировано»)
**And** гейт по дереву: у сообщения запроса своего списка **ноль** полей, называющих
субъекта
**And** **законный близнец гейта молчит**: у аккаунт-скоупного списка терм `userId` в белом
списке фильтра **есть**, и находкой он не является — там аккаунт обязателен, значит терм
действует **внутри** названного аккаунта (§2.1). Без этой половины гейт запрещал бы упоминание
субъекта где угодно, первый же ложный срабат его отключил бы
**And** **положительный контроль самого чтения**: тот же вызов **без** посторонних параметров
возвращает непустой перечень членств `usr-P` — то есть «ответ не зависит от параметра»
проверено на чтении, которое отвечает, а не молчит всем

#### IAM-ID-2-10 · Без личности — отказ, а не пустая страница, и на ОБОИХ слушателях

**Given** вызов без принципала в контексте

**When** клиент вызывает `GET /iam/v1/me/memberships` **на публичном слушателе**
**And** отдельно — то же чтение **на внутреннем слушателе** (`:9091`), где вызывающий
предъявил проверенный сертификат, но принципала не передал

**Then** оба вызова — `401` / `UNAUTHENTICATED`
**And** **не** `200` с пустым перечнем: пустая страница читалась бы как «членств нет» и
скрывала бы отказ аутентификации
**And** внутренний слушатель **не освобождён** — «internal = доверенный, сертификата
достаточно» есть запрещённое допущение (`security.md` §«AuthN+AuthZ ВЕЗДЕ», п. 4)
**And** сужение снимает проверку **прав** и **никогда** проверку **личности**

> **Почему утверждение на одном публичном слушателе было бы неполным.** У своего списка
> субъект — сам принципал, и **других входов у запроса нет**: сужать вызов без принципала
> **не к чему**. Полоса `<exempt>` при этом несёт второй смысл — она допускает вызов
> внутреннего слушателя вообще без принципала, по сетевому положению (§2.5, §1 П11). Значит
> ровно та полоса, где ошибка полосы опасна, публичной пробой не покрывается: она проверила бы
> путь, на котором `<exempt>` и `scope_filtered` ведут себя **одинаково**.

**And** **положительный контроль на каждом слушателе**: тот же вызов **с** принципалом даёт
`200` и перечень членств этого принципала — иначе `401` зеленел бы на чтении, отказывающем
всем, и проба не отличала бы «нет личности» от «путь сломан»

#### IAM-ID-2-11 · Пагинация своего списка — та же дисциплина

**Given** человек, у которого членств больше одной страницы

**When** он вызывает `GET /iam/v1/me/memberships?pageSize=5000`
**And** отдельно — с негодным `pageToken`
**And** отдельно — законный обход двумя страницами

**Then** первые два вызова — `400` / `INVALID_ARGUMENT`
**And** законный обход возвращает **каждое** членство ровно один раз, ни одного не пропустив
**And** положительный контроль: страница с `pageSize=1` отдаёт непустой `nextPageToken`,
и по нему приходит следующая запись

## F3 — анти-оракул (S1)

#### IAM-ID-2-12 · Три разных положения — один и тот же ответ

**Given** аккаунты `A` и `B`
**And** вызывающий держит на `A` отношение `viewer` (§2.8), на `B` ему не выдано ничего
**And** человек `usr-X` состоит **только** в `B`
**And** человек `usr-Y` существует и не состоит нигде
**And** строка `usr-Z…` — well-formed идентификатор, не резолвящийся ни во что

**When** клиент вызывает `GET /iam/v1/accounts/A/memberships?filter=userId="usr-X"`
**And** то же для `usr-Y`
**And** то же для `usr-Z…`

**Then** все три ответа — `200` / `OK` с **пустым** перечнем
**And** тела ответов совпадают **байт-в-байт** между собой
**And** ни один из трёх не `404` и не `403`: отказ по отсутствию отличал бы «такого человека
нет» от «есть, но не у вас»
**And** **положительный контроль**: тот же вызов для `usr-P`, который в `A` состоит, даёт
непустой перечень — то есть чтение отвечает содержательно, а не молчит всем

> Без положительного контроля утверждение о неразличимости зеленеет на поверхности, которая
> не отвечает никому.

#### IAM-ID-2-13 · Чтение чужого членства по идентификатору неотличимо от несуществующего

**Given** вызывающий держит на `A` отношение `viewer` (§2.8)
**And** `mbr-X` — существующее членство в аккаунте `B`
**And** `mbr-N` — well-formed идентификатор членства, которого нет нигде

**When** клиент вызывает `GET /iam/v1/accounts/A/memberships/mbr-X`
**And** то же для `mbr-N`

**Then** оба — `404` / `NOT_FOUND`
**And** текст сообщения совпадает **дословно** и следует контракт-тону владельца
(`"<Resource> <id> not found"`), причём подставлен **запрошенный** идентификатор, а не
вырезанный и не внутреннее имя таблицы
**And** `reason`-токен один и тот же — полоса собственного чтения
**And** тела ответов различаются **только** подставленным идентификатором и ничем иным
**And** **положительный контроль**: тот же вызов для членства, которое в `A` есть, даёт
`200` и сообщение ресурса
**And** тот же вызов от администратора `B` по пути `/accounts/B/memberships/mbr-X` даёт
`200` — то есть строка существует, и её недоступность в первом вызове была именно
сокрытием, а не отсутствием данных

#### IAM-ID-2-14 · Ни одно поле ответа не является функцией того, что вне аккаунта

**Given** дерево после S1

**When** прочитан контракт ответа обоих аккаунт-скоупных чтений

**Then** в нём нет поля «всего», «есть ещё», «в других аккаунтах» и любого иного счётчика
**And** `nextPageToken` вычисляется **только** по строкам названного аккаунта: два
вызывающих с разными правами на одном и том же аккаунте получают токен, не различающий
существование строк вне этого аккаунта
**And** запрос к хранилищу называет аккаунт в условии отбора — то есть строк других аккаунтов
он не читает вовсе, и различию неоткуда взяться ни в теле, ни во времени ответа
**And** утверждение проверяется **пробой уровня хранилища**, а не чтением кода, и проба
несёт **обе половины** — иначе она зеленеет на запросе, не выбирающем ничего:
  - *отрицательная*: чтение по пути аккаунта `A` при существующем членстве того же человека
    в `B` **не выбирает ни одной** строки `B`;
  - *положительная*: тот же вызов **выбирает ровно строку `A`** — с её идентификатором,
    состоянием и `accountId = "A"`. Без неё утверждение истинно у отбора, который всегда
    возвращает пусто, то есть у сломанного чтения (§2.2: отрицание идёт в паре с
    положительным контролем)
**And** обе половины утверждаются **в одном прогоне и на одних данных**: человек состоит и в
`A`, и в `B` одновременно — иначе «не выбрал `B`» доказывалось бы отсутствием `B`, а не
сужением отбора

#### IAM-ID-2-15 · Вопроса «в каких аккаунтах состоит этот человек» на поверхности нет — ни параметром, ни арифметикой

> **Гейт заводится в S1 и в S1 же полон.** Прежняя редакция ставила предусловием «дерево
> после S1 **и S2**», тогда как §4 объявляет анти-оракул **свойством S1** (F3), без которого
> S1 не production-complete. Противоречие несущее: гейт, ждущий S2, в S1 не заводится вовсе,
> и стадия сдаётся без того, что объявлено её собственным свойством. Полосы A, B и C судят
> **всю** публичную поверхность iam и не требуют существования своего чтения; что от S2
> действительно зависит — **один** законный близнец полосы A, и он назван ниже с указанием
> стадии.

**Given** дерево после S1 — гейт заводится вместе с ней и с ней же полон

**When** прогнан гейт по дереву контрактов iam

**Then** гейт судит по **трём** полосам, и третья заведена потому, что первые две её не
ловят **by construction** (§1, П10):

| полоса | что запрещает | почему отдельная |
|---|---|---|
| **A** — по композиции входа и выхода | публичное чтение, у которого **все четыре** условия сразу: **(а)** запрос называет человека (поле субъекта либо `{user_id}` в пути) · **(б)** ответ называет аккаунт или членство (транзитивно по сообщениям) · **(в)** аккаунт в запросе **не** обязателен · **(г)** ответ **не** сужен пообъектно правами вызывающего | прямая форма вопроса. **Судится композиция, а не вход**: по одному входу предикат краснеет на трёх законных RPC (§1, П17), по одному выходу — на **четырнадцати** (замер круга 3: перечни привязок, аккаунтов, снимок прав, группы, проекты, роли, служебные учётки — у каждого аккаунт свой собственный, а не «аккаунты человека») |
| **B** — по фильтру | терм фильтра, называющий субъекта, на чтении без обязательного аккаунта | та же форма, спрятанная в `filter` |
| **C** — по идентификатору членства | публичное чтение, резолвящее **идентификатор членства** без обязательного поля аккаунта | идентификатора **человека** в таком запросе нет вовсе, поэтому полосы A и B о нём молчат — а идентификатор вычислим офлайн из пары «человек:аккаунт», то есть вопрос задаётся **арифметикой вместо параметра** |

> [!important] Условие «г» — не послабление, а второй законный способ сужения
> §2.1 объявляет разграничение достижимым **двумя** путями: формой запроса **и** сужением
> данных. Условие «г» — это второй из них, а не вычет из первого: у чтения, суженного
> пообъектно правами вызывающего, ответ не есть «аккаунты этого человека» — это «те из них,
> которые вы и так вправе видеть». Оракула такой ответ не даёт **by construction**: он не
> сообщает ничего, чего спрашивающий не мог бы получить, перечислив своё.
>
> **И оно не голое обещание — у него свой держатель.** Пообъектное сужение энфорсится
> отдельным гейтом края и production-страж отказывает в старте, если объявленный фильтр не
> работает (`security.md` §«Отношение, выполнимое подстановочным знаком»). То есть «г»
> ссылается на **действующий механизм**, а не на комментарий. Снимут механизм — условие «г»
> обязано быть пересмотрено вместе с ним, а не унаследовано молча.

**And** гейт печатает **объём осмотренного** — сколько файлов контрактов прочитано, сколько
RPC распознано, сколько из них публичных, сколько рассмотрено каждой полосой, — чтобы «ноль
находок» было отличимо от «ноль прочитанного»
**And** **способность упасть доказана инъекцией по КАЖДОЙ полосе, настоящим входом**, а не
одной пробой на весь гейт:
  - *A*: списочному чтению **без** обязательного аккаунта, чей ответ аккаунт называет,
    добавлено поле `user_id` → **красное** с координатой (файл, RPC);
  - *A-bis*: номер аккаунта **возвращён** на ресурс человека → **красное** с координатой.
    Эта инъекция делает полосу A машинным исполнителем §6, п. 5: «возврат поля аккаунта на
    ресурс пользователя» перестаёт держаться обещанием;
  - *A-ter*: ресурсу человека добавлено **поле членств** → **красное** с координатой. Ею
    полоса A закрывает и форму «перечень членств вместе с человеком», которую предполагает
    соседняя приёмка (§2.6);
  - *B*: в белый список фильтра списка без обязательного аккаунта добавлен терм `userId` →
    **красное** с координатой;
  - *C*: заведено чтение `GET /iam/v1/memberships/{id}`, резолвящее членство по одному
    идентификатору → **красное** с координатой. **Эта инъекция обязательна**: без неё полоса C
    остаётся объявлением, а гейт зелен ровно на том входе, ради которого расширен
**And** **законные близнецы молчат** — каждый парен своей полосе, иначе гейт ловит форму, а
не существо. Стадия у каждого названа, потому что не все они существуют в S1:
  - к *A* (**S1**), **три близнеца, каждый гасит своё условие**:
    · аккаунт-скоупный список **как есть** — субъект называется, ответ называет аккаунт, но
      аккаунт **обязателен** (гасит «в»);
    · **три существующих чтения человека** — `/iam/v1/users/{user_id}` и его перечни операций
      и удостоверений: вход человека называют, ответ аккаунта **не** называет (гасит «б»,
      §1 П17б). Без этого близнеца гейт краснел бы в S1 на законном дереве и был бы снят;
    · чтение, называющее субъекта и отвечающее аккаунтами, но **суженное пообъектно** правами
      вызывающего (гасит «г»): такой ответ не «аккаунты этого человека», а «те из них, что
      вам и так видны». Их на поверхности **два**, и оба законны;
  - к *A* (**S2**, второй близнец): своё чтение, у которого поля субъекта нет вовсе. Он
    закрывает **другую** слепую зону — гейт, который красит всякое чтение без обязательного
    аккаунта, независимо от того, называет ли оно субъекта. В S1 такого чтения на поверхности
    нет, поэтому и зоны нет; близнец приезжает **вместе со своим предметом**, и это записано
    в DoD S2, а не подразумевается;
  - к *B* (**S1**): аккаунт-скоупный список с термом `userId` — у него аккаунт **обязателен**,
    значит терм действует **внутри** названного аккаунта и находкой не является;
  - к *C* (**S1**): аккаунт-скоупное одиночное чтение `.../accounts/{accountId}/memberships/{id}`
    — у него аккаунт обязателен, и находкой оно не становится
**And** гейт несёт проверку **своей предпосылки**: если разбор контрактов перестанет
распознавать RPC (сменилась форма объявления, переехал каталог), он **падает**, а не молчит;
пустой обход — отказ, а не успех
**And** предпосылка полосы C названа **в самом гейте**, рядом с ней: «идентификатор членства
вычислим из пары „человек:аккаунт“ без обращения к платформе». Перестанет быть вычислимым —
запрет обязан быть **пересмотрен**, а не унаследован молча; предикат снятия —
`grep -c "substr(md5('membership:'" <миграция>` перестал давать **1**

> Полоса C — не перестраховка. Без неё анти-оракул держится на том, что идентификатор
> «выглядит непредсказуемым». Он предсказуем, и предсказуем **по построению**, а не по
> недосмотру: та же форма чеканки идентификатора в SQL уже применена в этом сервисе и
> выбрана осознанно — ради того, чтобы два пути записывали одну и ту же строку.

## F4 — состояния членства (S1+S2)

#### IAM-ID-2-16 · Приглашён → вошёл: аккаунт-скоупное чтение меняет состояние вместе с входом

> **«Сам `usr-Q` видит своим списком ДО входа» снято из предусловия — оно неисполнимо.**
> До первого входа у него нет внешнего субъекта (§1, П12), то есть нет и вызова. Наблюдателем
> перехода остаётся **администратор**, и это не обеднение: переход в том и состоит, что до
> входа состояние видит только он.

**Given** человеку `usr-Q` выписано приглашение в `A`, состояние членства `PENDING`
**And** держатель `viewer` на `A` (§2.8) видит его аккаунт-скоупным чтением как `PENDING`
**And** у `usr-Q` есть **второе** членство, тоже `PENDING` — в аккаунте `E`

**When** `usr-Q` входит впервые и его личность становится активной

**Then** аккаунт-скоупное чтение отдаёт **ту же** запись (`id` не изменился) со
`state = "ACTIVE"`
**And** членство в `E` **тоже** стало `ACTIVE` — вход снимает состояние у **всех** членств
человека, а не у одного (§1, П13); это утверждение и есть причина, по которой второе членство
стоит в предусловии
**And** свой список `usr-Q`, ставший вызываемым **только теперь**, содержит **оба** аккаунта
**And** `updatedAt` продвинулся, `createdAt` — нет: момент выписки приглашения сохраняется
**And** `invitedBy` **пережил** переход и по-прежнему называет пригласившего — это и есть то
поле, по которому «позвали» читается после входа (§2.4а)
**And** теперь и только теперь `GET /iam/v1/me` называет `A`, причём **положительный контроль
стоит рядом с отрицанием и в том же прогоне**: **до** входа тот же вызов `A` **не называл**,
**после** — называет. Одна половина без другой не различает «снимок починился» от «снимок
всегда называл всё»
**And** утверждение о появлении доступа делается **ограниченным повтором** с названным
бюджетом; по его истечении сценарий **красный**, а не «подождём ещё»

#### IAM-ID-2-17 · Исключён: ответ тот же, что для никогда не бывшего

**Given** человек `usr-P` состоит в `A`
**And** **живых выдач у него на `A` и на проекты `A` нет** — снятие членства под живой
выдачей отвергается конструкцией (§1, П15, П15б; §2.4), и без этой оговорки исход сценария
зависел бы от неназванной величины
**And** зафиксирован ответ аккаунт-скоупного чтения для `usr-Y`, которого в `A` не было
никогда

**When** членство `usr-P` в `A` снимается и операция дошла до конца

**Then** `GET /iam/v1/accounts/A/memberships?filter=userId="usr-P"` отдаёт `200` с пустым
перечнем, **байт-в-байт** совпадающим с зафиксированным ответом для `usr-Y`
**And** `GET /iam/v1/accounts/A/memberships/{снятый id}` отдаёт `404` / `NOT_FOUND` тем же
дословным текстом, что и никогда не существовавший идентификатор
**And** свой список `usr-P` этого аккаунта больше не называет
**And** прочие его членства в перечне остались — снятие одного не задело других
**And** утверждение об **исчезновении** делается **однократно**, после наблюдаемого признака
завершения снятия, и **без** повтора: повтор на отрицании превращает «не выполнено» в «ещё
не доехало»
**And** **положительный контроль сохранности — к ЭТОМУ блоку**: до снятия тот же вызов для
`usr-P` отдавал непустой перечень — то есть проба различает «сняли» и «никогда не отвечало».
У второго блока `Then` ниже — **свой** контроль: этот к нему не относится, потому что
утверждает о состоянии **до** снятия, а второй блок говорит о состоянии **после** повторного
приглашения

**И отдельно — что возвращается при ПОВТОРНОМ приглашении того же человека в тот же аккаунт.**
Идентификатор членства вычисляется из пары «человек:аккаунт» неизменяемой функцией без соли, а
уникальность пары полная (§1, П10, П10в) — значит снятие есть удаление строки, и повторное
приглашение вернёт **тот же самый** идентификатор, что был до исключения. Это не дефект и
чинить его здесь нечем; но умолчать нельзя, потому что из него следует утверждение:

**Then** после повторного приглашения `usr-P` в `A` его членство получает идентификатор,
**дословно равный** зафиксированному до исключения
**And** тем не менее `createdAt` — **новый**, а `state` — `PENDING`: строка заведена заново, и
ответ не выдаёт себя за прежний
**And** ни одно поле ответа не сообщает, что членство здесь **уже было**: ни счётчика попыток,
ни прежней даты, ни признака «восстановлено». Тень исключённого — тот же оракул (§2.4), и
совпадение идентификатора её **не создаёт** ровно потому, что идентификатор вычислим и
посторонним: он не сообщает наблюдателю ничего, чего тот не мог бы получить арифметикой сам
**And** утверждение о совпадении идентификатора делается **сравнением с зафиксированным
значением**, а не проверкой формы `mbr-<17>`: форма совпала бы и у другого членства
**And** **положительный контроль второго блока — отдельный от контроля первого**: чтение
`GET /iam/v1/accounts/A/memberships/{тот же id}` **после** повторного приглашения отдаёт `200`
с непустой записью, у которой `accountId = "A"`, `userId = "usr-P"` и **непустой**
`createdAt`. Без него «ни одно поле не сообщает, что членство уже было» зеленеет **тремя**
разными способами, и ни один из них не есть проверяемое свойство: строка не заведена вовсе ·
чтение отдаёт `404` · поле срезано краем, который не перекатан вместе с сервисом
(`api-conventions.md` §«Поле контракта не выходит наружу, пока КРАЙ не пересобран»)
**And** та же запись читается **и списком** — `GET /iam/v1/accounts/A/memberships?filter=userId="usr-P"`
отдаёт ровно её: контроль стоит на **обеих** проекциях, иначе он подтверждал бы работу одной

#### IAM-ID-2-18 · Приглашение БЕЗ роли доступа не даёт — отрицание в паре с положительным

> [!warning] Предусловие «он вошёл, состояние членства `PENDING`» было САМОПРОТИВОРЕЧИВО
> Вход и есть то событие, которое состояние снимает: первый вход переводит в «активно» **все**
> членства человека (§1, П13). Пара «вошёл» + `PENDING` неисполнима, и сценарий был бы красен
> при исправном продукте. Предмет сценария от этого **не меняется** и даже усиливается:
> утверждается, что доступа нет **у активного члена аккаунта**, которому не выдали роли, — то
> есть отказ есть свойство **отсутствия выдачи**, а не незавершённости входа. Прежнее
> предусловие позволяло списать отказ на второе.

> **Условие расщеплено на два сценария, и это не дробление ради числа.** Прежняя редакция
> ставила одно условие «выписано приглашение» и утверждала отказ. Замер (§1, П9) показал, что
> условие **не различает** два законных случая: приглашение, назвавшее проект и роль,
> вставляет выдачу с областью на весь проект — и тогда чтение ресурса `A` проходит, а
> сценарий красен при исправном продукте. Утверждение, чей исход зависит от неназванной
> величины, — недетерминированное; оно ловило бы не дефект, а выбор пригласившего.

**Given** человеку `usr-Q` выписано приглашение в `A` **без проекта и без роли** — то есть
выдачи оно не создавало (§1, П9в)
**And** он вошёл впервые; членство в `A` тем самым стало `ACTIVE` (§1, П13)
**And** иных выдач у него на `A` и на его содержимое нет

**When** он вызывает чтение любого ресурса, принадлежащего `A`
**And** отдельно — `GET /iam/v1/accounts/A`
**And** отдельно — свой список членств

**Then** первые два — отказ по действующей полосе аккаунта, а не `200`
**And** третий — `200`, и аккаунт `A` в перечне: **числиться** в аккаунте и **распоряжаться**
им — разные вещи (§2.3). Именно эта пара и есть предмет сценария: перечень членств полон,
доступа при этом нет
**And** **положительный контроль**: тому же `usr-Q` выдаётся роль на `A` — и `GET
/iam/v1/accounts/A` начинает проходить, при **неизменном** членстве. То есть отказ был
свойством **отсутствия выдачи**, а не сломанного пути и не состояния членства
**And** **второй положительный контроль — на самом отказе**: тот же вызов от администратора
`A` даёт `200`, то есть путь исправен и отвергает именно этого вызывающего

#### IAM-ID-2-18б · Приглашение С РОЛЬЮ доступ даёт — и это не дефект, а вторая величина

**Given** человеку `usr-R` выписано приглашение в `A` **с названным проектом `A/pr1` и ролью**
**And** он **ещё не входил**: строка личности «приглашена», членство в `A` — `PENDING` (§1,
П12); наблюдателем этого состояния является **администратор** `A`, а не сам `usr-R` — своего
чтения он вызвать не может, ему нечем аутентифицироваться (§2.4а)

**When** проверяется, что даёт роль, выданная приглашением, — до первого входа
**And** отдельно — что о `usr-R` говорит аккаунт-скоупное чтение
**And** отдельно — что происходит при попытке снять его членство под этой выдачей

**Then** доступ, который дала **роль**, действует по общим правилам модели прав: чтение
ресурсов проекта `A/pr1` в границах роли **проходит**, и это верное поведение, а не находка —
выдача создана и материализована в транзакции приглашения (§1, П9)
**And** доступ **не шире выданной области**: ресурс `A`, лежащий **вне** `A/pr1`, по-прежнему
отказ — область выдачи есть проект, а не аккаунт
**And** членство при этом остаётся `PENDING` в аккаунт-скоупном чтении: состояние членства —
не функция выданных прав, и наоборот. **Своё** чтение здесь не утверждается и утверждаться не
может: право доступ дало, а личности не создало — аутентифицироваться `usr-R` по-прежнему
нечем (§1, П12). Это и есть самое короткое доказательство того, что «членство» и «право» —
разные величины: право уже действует, а вызвать своё чтение некому
**And** `GET /iam/v1/me` аккаунт `A` **не назвал бы** и здесь — выдача **проектная**, а ветвь
перечня требует область **на аккаунт** (§1, П5), — но проверяется это **не на `usr-R`**: он
снимка не запросит, аутентифицироваться ему нечем. Утверждение ставится на человеке, который
вошёл и держит **проектную** выдачу в `A` без членства и без владения; тишина снимка
сохраняется по названной причине, а не по совпадению
**And** **две величины разводятся наблюдаемо — и разводятся ТЕМ ходом, который конструируем**:

  - *снятие **выдачи*** закрывает доступ к `A/pr1` и **не трогает** членства: оно остаётся
    `PENDING` и по-прежнему читается аккаунт-скоупным чтением с тем же `id`. Это половина,
    которую можно поставить и наблюдать;
  - *снятие **членства** под живой выдачей* **отвергается** — транзакция не проходит (§1,
    П15; выдача на проект `A/pr1` попадает в границу запрета ровно как выдача на сам аккаунт,
    П15б). Наблюдаемое читающими путями следствие: после неудавшейся попытки членство
    **по-прежнему** читается аккаунт-скоупным чтением, тем же `id` и в том же состоянии.
    **Точный код и текст отказа здесь не фиксируются** — снятие есть глагол, меняющий
    состояние, его контракт отказа принадлежит IAM-ID-1 (§0.3, §2.4);
  - *снятие членства **после** отзыва выдачи* проходит — и тогда доступ уже закрыт **отзывом**,
    а не снятием членства.

  Именно эта тройка и разводит величины: право **нельзя осиротить**, а членство **нельзя
  подменить правом**. Утверждение прежней редакции — «снятие членства не отзывает выдачу» —
  неконструируемо: состояния «членство снято, выдача жива» не бывает, и проба на него была бы
  либо красной всегда, либо вакуумной (если выдачу отозвать заранее, утверждать станет нечего)
**And** **положительный контроль каждого отрицания** — иначе они зеленеют на вызывающем, у
которого нет доступа **никуда**:
  - к «вне области — отказ»: чтение **внутри** `A/pr1` в границах роли даёт `200`;
  - к «снимок не называет `A`»: тот же вызов делает **другой** человек, активный член
    аккаунта `D`, — его снимок `D` **называет**. Приписать `usr-R` активное членство в `D`
    нельзя: активным оно станет только с его первым входом, и тот же вход снял бы `PENDING`
    в `A` (§1, П13), разрушив предусловие сценария;
  - к «членство осталось `PENDING`»: в том же аккаунте `A` есть активный член, чьё членство
    читается как `ACTIVE` — то есть поле состояния различает, а не отдаёт константу;
  - к «снятие членства отвергается»: снятие членства **того же** активного члена, у которого
    живых выдач на `A` и его проекты нет, **проходит** — иначе «отвергается» зеленело бы на
    пути снятия, сломанном для всех

> Этот сценарий существует, чтобы **зафиксировать сегодняшнее поведение продукта как
> намеренное**, а не чтобы его изменить. Менять форму приглашения — предмет IAM-ID-1 (§6, п. 1);
> здесь она читается такой, какая есть.

## F5 — консоль (S1+S2)

#### IAM-ID-2-19 · Карточка сотрудника называет членство в текущем аккаунте

**Given** консоль открыта в аккаунте `A`
**And** человек `usr-P` состоит в `A`

**When** оператор открывает карточку `usr-P`

**Then** на карточке названо членство **в текущем аккаунте**: аккаунт и состояние
**And** аккаунт показан ссылкой на его карточку — иконка типа, человекочитаемое имя, переход
(`ui.md` правило 2), а не голым идентификатором
**And** состояние названо **следствием**, а не словом «да»: «приглашён» / «состоит»
(`ui.md` правило 6)
**And** источник значения — аккаунт-скоупное чтение с текущим аккаунтом во входе, а не
контекст списка: список охватывает несколько аккаунтов, поэтому контекст его не определяет

#### IAM-ID-2-20 · Карточка не называет ни одного другого аккаунта и не намекает на их число

**Given** консоль открыта в аккаунте `A`
**And** человек `usr-P` состоит в `A` и в `B`

**When** оператор открывает карточку `usr-P` и раскрывает все её вкладки

**Then** ни одна поверхность карточки не называет `B` — ни именем, ни идентификатором, ни
ссылкой
**And** нигде не показано число аккаунтов человека и признак «есть ещё»
**And** консоль **не задаёт** ни одного запроса, который спрашивал бы про человека без
аккаунта: перечень исходящих обращений карточки проверяется пробой
**And** **положительный контроль**: членство в `A` на карточке названо — то есть проба
краснела бы на карточке, которая молчит обо всём

#### IAM-ID-2-21 · Свой раздел показывает, где человек состоит и куда приглашён

**Given** человек аутентифицирован и состоит в `A`
**And** в `C` он оказался **по приглашению**: `invitedBy` у этой строки непуст

**When** он открывает раздел собственных настроек консоли

**Then** он видит перечень своих членств: и `A`, и `C` — то есть аккаунт, куда его позвали,
из перечня не выпадает
**And** у строки `C` названы **пригласивший и дата** — консоль читает их из `invitedBy` и
`createdAt`, а **не** выводит подпись из состояния: состояния «приглашён» в своём списке не
бывает (§2.4а), и подпись, выведенная из него, не появилась бы никогда
**And** **положительный контроль подписи**: у строки `A`, заведённой не приглашением,
пригласивший **не** называется — то есть подпись различает два случая, а не рисуется всегда
**And** консоль **не** показывает состояние членства как «приглашён/состоит» в этом разделе:
поле, принимающее одно значение, подписью не является (`ui.md` правило 9 — поле без источника
не показывается; здесь источник есть, но различающей величины в нём нет)
**And** аккаунты показаны ссылками (`ui.md` правило 2), перечень — таблицей значений одного
предмета (`ui.md` правило 11)
**And** перечень не показывается вовсе, пока источник не отвечает: подпись без источника не
рисуется (`ui.md` правило 9)
**And** **положительный контроль**: когда источник отвечает, перечень **отрисован** и его
первая строка читается — иначе «не рисуется без источника» зеленело бы на разделе, который не
рисуется никогда, и проба не отличала бы верную сдержанность от пустого экрана

#### IAM-ID-2-22 · Существующая проба перенацеливается ВМЕСТЕ с источником, не раньше

**Given** сквозная проба `ui-future/e2e/specs/users.spec.ts` сегодня утверждает **отсутствие**
подписи аккаунта на карточке и называет срок годности этого утверждения — эту задачу

**When** источник появился (S1 приземлена) и карточка называет членство

**Then** та половина пробы, что утверждала отсутствие подписи, **заменена** на утверждение
«карточка называет членство в текущем аккаунте», и заменена в том же изменении, что и
источник
**And** половина про **столбец** списка остаётся как есть: столбца аккаунта в списке
по-прежнему нет и не появляется — список охватывает несколько аккаунтов, и столбец солгал бы
**And** рядом заведена проба сценария IAM-ID-2-20 (карточка молчит о других аккаунтах) — с
`// verifies #1085` **внутри** вызова пробы
**And** положительный контроль каждой пробы сохранён: карточка отрисована и её первая
подпись читается, иначе утверждение зеленело бы на пустом экране

---

## §6 Out-of-scope — с предикатом каждого пункта

| # | Что не входит | Почему и предикат |
|---|---|---|
| 1 | **поток приглашения**: кто вправе приглашать, каким глаголом, какие проверки | предмет IAM-ID-1 и `#471`. Предикат: в §5 нет ни одного **When**, вызывающего глагол создания членства |
| 2 | **правка записи человека** — имя, почта | `PRO-Robotech/kacho#1102`. Предикат: в §5 нет ни одного `PATCH`/`POST` на ресурс человека |
| 3 | **запрет участия и его разведение** на «исключить из аккаунта» и «закрыть вход» | `#1102`, вопрос В-8 IAM-ID-1. Предикат: ни один сценарий §5 не утверждает исхода, зависящего от состояния запрета |
| 4 | **выпуск токенов и действие от имени** | `PRO-Robotech/kacho#1086`. Предикат: в §5 нет ни одного вызова чеканки |
| 5 | **возврат поля аккаунта на ресурс пользователя** | `#471`. Предикат: `git show "$R":proto/kacho/cloud/iam/v1/user.proto \| grep -c 'reserved "account_id"'` остаётся **1** после обеих стадий |
| 6 | **приглашение как отдельный ресурс** с принятием, отклонением и **наблюдаемым состоянием «приглашён» в СВОЁМ списке** | шага принятия в продукте нет: первый вход активирует **все** приглашённые членства человека (§1, П13), приглашённый аутентифицироваться не может вовсе (П12), а приглашение уже активного заводит членство сразу активным (П14). Значит `state = "PENDING"` в своём списке недостижим **by construction**, и достижимым его делает только смена машины состояний — **отдельный предмет и решение владельца** (§2.4а). Требование владельца при этом выполняется исходом (а): полнота перечня плюс `invitedBy`. Предикат, при срабатывании которого пункт снимается: `grep -c "WHERE user_id = NEW.id AND state = 'PENDING'"` по миграции первого входа перестал давать **1**, **либо** появился глагол принятия отдельно от входа. Пока оба верны — состояний членства остаётся **два** (§1, П3, П16) |
| 7 | **приведение поверхности членства к одной адресной форме** (создание и чтения) | §2.6; работа IAM-ID-1. **Предикат переформулирован под фактически сделанное, и перемерен 2026-08-27** (воркспейс `origin/main` @ `3ce88c9`): `git show origin/main:docs/specs/sub-phase-IAM-ID-1-identity-account-decoupling-acceptance.md \| grep -c 'IAM-ID-2'` даёт **119**, контроль `grep -c 'IAM-ID-1'` — **255**. В круге 3 (2026-08-23) те же предикаты давали **0** и **168**: «там» не было названо. Сегодня названо — круг 12 соседа (✅ APPROVED, 2026-08-24) отозвал санкцию у **девяти** сценариев (`43`–`48`, `63`, `66`, `67`) и перенёс `49`. Действующий предикат: расхождение названо **здесь** (§2.6) вместе с предикатом переписи, а исполняется оно **полосой C** гейта IAM-ID-2-15 — она красит плоское чтение по идентификатору **независимо от того, чей код его завёл**. Круг 3 добавил вторую половину: отменяется и стадия **S5** соседа (сценарии 43–49, консоль) — исход по каждому в §2.6, машинно держится инъекцией *A-ter* полосы A, а незакрытый остаток вынесен пунктом 10 ниже |
| 8 | **изменение модели прав** — новый тип, новое отношение | §0.3, §2.8. Предикат: `git diff <до>..<после> -- proto/kacho/cloud/iam/v1/fga_model.fga` **пуст**, и идентификатор модели не мятился. Гейт садится на уже объявленный `account.viewer`; типа членства в модели нет — `git grep -cE '^type .*membership' "$R" -- proto/kacho/cloud/iam/v1/fga_model.fga` даёт **0** и обязано остаться **0** |
| 9 | **аккаунт с нулём активных членств** — достижим по правам и не попадает ни в один перечень | унаследовано от IAM-ID-1 §6 п. 8; здесь не решается и не усугубляется |
| 10 | **машинный запрет на межаккаунтный перечень в КОНСОЛИ** — гейта нет, и это названо | Полоса A судит **контракты**: она красит источник (поле членств на ресурсе человека), но не рисунок. Столбец, собранный консолью из аккаунт-скоупных чтений по многим аккаунтам, ей невидим. Держится сегодня решением §0.1 и пробами IAM-ID-2-20/22 — то есть **нашей** консолью, а не чужой. Предикат заведения предмета: в спеке `users` появился столбец с перечнем аккаунтов. Адрес решения — круг ревью IAM-ID-1 (§2.6), а не этот документ |

---

## §7 DoD по стадиям

Общее для обеих (ban #12, ban #14 — ни один пункт не переносится в «следующую» стадию):

1. **контракт и регенерация**: `.proto` в `proto/`, `buf lint` и `buf breaking` зелёные,
   стабы перегенерированы плагинами **из модуля**, а не из системного `PATH`;
2. **край**: RPC зарегистрирован, запись каталога прав есть у каждого,
   `make -C gateway permission-catalog-check` зелёный, обе встроенные копии совпадают
   байт-в-байт; **край перекатан вместе с сервисом** — иначе новые поля молча отбрасываются
   при перекодировании ответа;
3. **каталог идентификаторов**: `mbr` объявлен в `pkg/ids/ids.go` среди дефис-префиксов
   (§1, П6), рядом — проба, что корректный идентификатор членства принимается, а негодный
   отвергается;
4. **integration** (testcontainers): чтение по пути аккаунта не выбирает строк других
   аккаунтов; курсор не пропускает и не дублирует строк; пагинация отвергает негодный вход
   раньше замыкания;
5. **newman**: по одному кейсу на каждый сценарий §5 своей стадии, минимум один
   положительный и один отрицательный на каждый новый путь; кейсы несут `{{runId}}` и
   убирают за собой; байт-идентичность утверждается **сравнением тел**, а не только кодов;
6. **гейт по дереву** IAM-ID-2-15 — **три полосы**; **обязательное условие приёмки гейта:
   на неизменённом дереве он даёт НОЛЬ находок** (иначе он не заводится зелёным и будет снят
   первым же срабатом — §2.2, п. 1). Инъекций по полосе A **три** (поле субъекта у чтения без
   обязательного аккаунта · возврат номера аккаунта на ресурс человека · поле членств на
   ресурсе человека), по B и C — по одной, включая заведённое в синтетике плоское чтение
   `GET /iam/v1/memberships/{id}`; законные близнецы — по числу условий полосы, а не по одному
   на полосу (у A их три, §5); печать объёма осмотренного и проверка собственной предпосылки;
   плюс инъекция гейта каталога прав (IAM-ID-2-06);
6б. **полоса сужения — утверждение ПО СТАДИЯМ, а не одно на обе** (иначе пункт, стоящий в
   «общем», требует от S1 предмета, которого у неё нет):
   **S1** — полосы сужения не несёт ни один её RPC и ни один не освобождён от проверки прав
   (число объявленных полос — **ноль**, проверяется IAM-ID-2-06);
   **S2** — свой список объявлен `scope_filtered` при непустом `permission`, **не**
   `<exempt>` (§2.5, §1 П11), и отказ без принципала проверен **на обоих** слушателях
   (IAM-ID-2-10);
6г. **полоса аккаунт-скоупных чтений объявлена ПРОТИВОПОЛОЖНОЙ и это утверждено**: у них
   `scope_filtered` **не** объявлен, а стоит `required_relation` + `scope_extractor
   {object_type: account, from_request_field: account_id}` (§2.7). Предикат §2.7 печатает все
   три записи одной командой — расхождение видно строкой, а не вычитыванием каталога;
6д. **отношение — `viewer` @ `account`, и выбор доказан контролем ПО ТИРУ**: проба, в которой
   держатель тира распорядителя (`admin`) читает членства в своём аккаунте, **зелёная**
   (IAM-ID-2-03, последняя половина), а инъекция подмены `viewer` → `v_list` роняет гейт
   каталога с именем отношения (IAM-ID-2-06). Модель прав при этом **не правится**: диффа по
   `fga_model.fga` нет, идентификатор модели не мятится (§6 п. 8);
6в. **плоская коллекция членств не несёт ни одного глагола чтения** после обеих стадий —
   ни `GET` списка, ни `GET` по идентификатору (§2.6); утверждение держится полосой C гейта
   IAM-ID-2-15, а не тем, что никто не добавит `GET` рядом с уже стоящим там `POST`;
7. **консоль**: сквозные пробы сценариев F5, `// verifies #1085` внутри вызова пробы;
   существующая проба перенацелена тем же изменением, что и источник;
7б. **ведомость приёмок — записи НЕ требуется, и это сказано словом, а не умолчано.** Гейт
   продукта `internal/repohygiene` `TestNewMigrationCitesAnApprovedAcceptance` судит **только
   миграции, ДОБАВЛЕННЫЕ изменением** относительно ствола, и требует от них записи в
   `docs/acceptance-ledger.yaml`. Обе стадии IAM-ID-2 миграций **не несут**: таблица членства
   и её состояния уже заведены (§1, П3, П16), а предмет этого документа — читающий путь,
   контракт, край, каталог идентификаторов и консоль. Значит и записи в ведомости не
   требуется. **Предикат, при срабатывании которого это перестаёт быть верным:**
   `git diff --name-only --diff-filter=A <ствол>..HEAD -- 'services/*/internal/migrations/*.sql'`
   даёт непусто — тогда запись обязана появиться **до** мёржа, вместе с вердиктом
   `acceptance-reviewer` и ревизией воркспейса, на которой он прочитан;
8. **vault-trail**: записка `obsidian/kacho/KAC/issue-1085.md` со ссылками на затронутые
   сущности (`resources/`, `rpc/`) и на PR; `./scripts/vault-gate/run-all.sh` зелёный;
9. **финальная верификация**: `go test ./... -race`, `golangci-lint run`,
   `make -C services/iam audit-list-filter` (покрывает **новые** глаголы), newman зелёные.

Отдельно по стадиям:

| стадия | сверх общего |
|---|---|
| **S1** | сценарии F1, F3, F4 (16, 17, 18 — **в части аккаунт-скоупного чтения**; **18б целиком** — после правки он своего чтения не утверждает вовсе, см. §2.4а), F5 (19, 20, 22 — **не раньше круга ревью IAM-ID-1**, §4); гейт IAM-ID-2-15 заводится здесь и здесь же полон — инъекция по всем трём полосам, близнецы к A/B/C из числа существующих в S1; в `Out-of-scope` S1 явно записано: своего списка ещё нет |
| **S2** | сценарии F2, F4 (16, 17, 18 — **в части своего списка**), F5 (21 — то же условие); к гейту IAM-ID-2-15 добавляется **второй** законный близнец полосы A — своё чтение, у которого поля субъекта нет вовсе (в S1 такого чтения нет, и близнец приезжает вместе со своим предметом); комментарий поля снимка прав говорит, **на какой вопрос он отвечает** («что мне доступно»), что ветви в нём объединены и в ответе неразличимы, что следа приглашения — кто и когда — у него нет, и где это читается (§2.3). Формулировка «приглашений он не несёт» **запрещена**: после первого входа снимок аккаунт приглашения называет (§1, П5г) |

---

## §8 Критерии, по которым этот документ проверяется

1. каждое утверждение о дереве §1 несёт предикат, а у отрицательных предикатов есть
   контроль в обратную сторону (П1к, П6к, П7к);
2. каждый сценарий утверждает **пару** «HTTP-статус + gRPC-код», а не один из них;
3. ни одно отрицание не стоит без положительного контроля;
4. ни один сценарий не описывает состояния, которого **машина состояний не порождает** — а
   не только такого, которого «схема не допускает». Различие стоило круга ревью: прежняя
   редакция этого критерия была **истинна** (схема допускает `PENDING`) при шести сценариях,
   требовавших пары «аутентифицирован» + `PENDING`, которую **ни один путь дерева не
   производит** (§1, П12–П14). Схема очерчивает **словарь** значений, а достижимость даёт
   **машина**; проверять надо вторую. Проверяемый признак: у каждого предусловия, называющего
   состояние, назван **путь**, которым это состояние возникает, и путь этот измерен в §1;
5. каждая строка `Scope` §4 имеет раздел со сценариями;
6. анти-оракул выражен **четырьмя** уровнями (§2.2), и первый из них — гейт по дереву, а не
   утверждение в прозе; четвёртый — адресный, и он заведён именно потому, что первый его не
   ловит by construction (§1, П10);
7. документ выдерживает публикацию: он называет предмет, решение и следствие для контракта
   и **не** собирает в одном месте координату, условие и следствие для постороннего
   (`security.md` §«Публичные артефакты»);
8. **ни один перечень не объявлен полным, не будучи полным.** Там, где перепись принадлежит
   соседнему документу, названы предикат и граница охвата, а не подразумевается
   исчерпывающий список (§2.6);
9. **каждый гейт несёт инъекцию по КАЖДОЙ своей полосе** и законного близнеца к каждой
   (IAM-ID-2-06, IAM-ID-2-15): гейт, у которого полос больше, чем доказательств, зелен ровно
   на той полосе, что осталась без инъекции;
10. **каждая полоса освобождения названа по имени и выбрана явно** (§2.5): «освобождение»
    без имени полосы неотличимо от той полосы, что допускает вызов без принципала;
11. **ни два сценария не требуют взаимоисключающего от одного входа.** Там, где исход
    выбирает устройство, а не кейс, полоса названа **решением с предикатом**, и оба сценария
    приведены к ней (§2.7; IAM-ID-2-03 и IAM-ID-2-05). Толерантность `oneOf` исходом **не
    является**: она перечисляет то, о чём никто не решал;
12. **каждый сценарий, чей исход зависит от прав, называет ОТНОШЕНИЕ**, а не «право чтения»:
    отношение — часть контракта, и от него зависит, кто прочитает, а кто получит отказ
    (§2.8);
13. **база сверки §1 — то дерево, в которое садится работа**, а не ствол. Разрыв между ними
    назван числом, и утверждения, изменившиеся при перебазировке, перечислены (§1.0а, §1.0б);
14. **предикат по этому корпусу — двуязычный.** Корпус называет предмет по-русски в разборах
    и по-английски в координатах; счёт по одному языку недобирает **молча** и недобирает
    именно там, где предмет объясняли словами (§2.6: 6 против 40, и потерянными оказались
    ровно блокирующие строки);
15. **всякое число о документе названо с ЕДИНИЦЕЙ СЧЁТА и предикатом — либо не названо
    вовсе.** Число без единицы неопровержимо, а значит бесполезно: два добросовестных замера
    дают разные величины и спорить не о чем. Перепись — §8а;
16. **ни одно утверждение о поведении продукта не опирается на посылку, которую дерево
    опровергает.** Опровергнутых замером посылок стало **восемь**, и все исправлены, а не
    сняты. Круг 1: приглашение прав не даёт (§2.3), перечень аккаунтов строится по активным
    членствам (§1 П5), идентификатор членства непредсказуем (§1 П10). Круг 2: состояние
    «приглашён» наблюдаемо в своём списке (§1 П12–П14, §2.4а), снятие членства проходит всегда
    (§1 П15, §2.4), согласованность состояния членства со статусом человека закреплена
    конструкцией (§1 П16б, §2.4), **снимок прав об аккаунте приглашения молчит** (§1 П5г,
    §2.1, §2.3). Круг 3: **на публичной поверхности iam нет чтения, принимающего идентификатор
    человека без обязательного аккаунта** (§1 П17, §2.2 п. 1) — их три, и полоса A,
    заведённая по прежней букве, краснела бы в S1 на законном дереве, то есть §7 п. 6 был бы
    неисполним. Седьмая найдена **не в вердикте, а собственной сверкой при правке по
    нему**: меняя предусловие IAM-ID-2-08 на «он вошёл», я обязан был перечитать всё, что от
    прежнего предусловия зависело, — и одно из зависевших утверждений стало ложным. Правка по
    вердикту опаснее правки на пустом месте ровно этим: она меняет посылку, на которую
    ссылались другие строки, и вердикт про них не говорит. Каждая правка оставила разбор: ошибка правдоподобна и
    вернётся, если не сказать, чем именно она опровергнута.

    **Что круг 1 измерил и чего не измерил — названо, потому что это и есть урок круга 2.**
    §1 первой редакции измерил **таблицу** членства и **чеканку идентификатора** — то есть
    покой данных. Он не измерил **машину состояний**: кто эти строки пишет, при каком условии
    и что происходит с ними при входе человека. Все три посылки круга 2 лежали ровно в этой
    слепой зоне, и ни одна не ловилась перечитыванием документа: они опровергаются только
    чтением писателей. **Приём, переносимый дальше:** измеряя ресурс, измеряй не только его
    строку и её ограничения, но и **каждого писателя** этой строки — предикатом, а не памятью.

---

## §8а Перепись документа — с ОБЪЯВЛЕННОЙ единицей счёта

Числа ниже названы затем, чтобы «полно» и «покрыто» не читались шире измеренного. У каждого
объявлено, **что именно** читает предикат — иначе два добросовестных замера дадут разные
величины и спорить будет не о чем. Ревизия воркспейса — та, на которой правится документ.

| величина | единица счёта | предикат | значение |
|---|---|---|---|
| сценариев | блок `#### IAM-ID-2-<N>` | `grep -c '^#### IAM-ID-2-' "$F"` | **23** |
| сценариев, чьё тело **называет** контроль или близнеца **словом** | тот же блок; предикат читает **слова** `положительный контроль` / `законный близнец` | `awk` по блокам с этим образцом | измеряется **соглашение об именовании**, а не свойство — см. оговорку ниже |

**Числа «сколько сценариев несут отрицательное утверждение» и «сколько отрицаний имеют
парный положительный контроль» здесь НЕ названы, и это решение, а не пропуск.** Обе величины
требуют **адъюдикации**, а не предиката: «отрицательное утверждение» — суждение о смысле
строки, а не её форма. Механическая мерка по словам даёт заведомо неверный ответ в **обе**
стороны, и это проверено на этом же документе:

- **вниз**: IAM-ID-2-14 несёт полноценную пару и называет её словами «*отрицательная*» и
  «*положительная*» — образец `положительный контроль` её не находит;
- **вверх**: сценарий, где слово «контроль» стоит, но парного утверждения нет, предикат
  засчитывает как парный.

Отчёт круга 1 называл «18 сценариев с отрицательным утверждением» и «21 с парным
положительным контролем» (в теле документа их не было — тем хуже: число, живущее только в
комментарии, не переживает своего круга и не может быть опровергнуто следующим читателем). Ни одна мерка не даёт **18** (строгий перечень даёт 17,
адъюдикация — 19), а **21** измеряло присутствие слова: из этого 21 четыре сценария отрицаний
не несут вовсе. Числа сняты, а не поправлены: поправленное число той же природы было бы столь
же неопровержимым.

**Круг 3 добавил третий способ соврать числом — ЯЗЫК предиката.** Перепись §2.6 искала
`memberships|MembershipService` и давала **6** сценариев соседа; тот же обход с русским корнем
`членств` даёт **40**. Корпус двуязычен by construction: координаты по-английски, разборы
по-русски, — и потерянным оказался ровно тот сценарий, чей предмет назван словами, а не
координатой. **Расширение до 40 починкой не является**: перебор так же негоден, как недобор, и
адъюдицировать сорок блоков никто не станет. Годная единица — та, что не зависит от языка: у
соседнего документа это **строка его собственной таблицы стадий и задач**, где стоят только
номера (§2.6). Класс кодифицирован — `testing.md` §«Предикат по ДВУЯЗЫЧНОМУ корпусу».

**Чем свойство держится вместо числа.** Требованием §8, п. 3 — «ни одно отрицание не стоит
без положительного контроля», — проверяемым **на ревью адъюдикацией по каждому сценарию**.
Это дороже числа и честнее его: у числа не было ни единицы, ни способа опровержения.

**Отдельно: «отрицаний без пары ноль» верно на единице «сценарий» и НЕВЕРНО на единице
«утверждение».** На единице «утверждение» их было **одно** — второй блок `Then` сценария
IAM-ID-2-17 (единственный сценарий документа с двумя блоками `Then`; контроль был привязан к
первому). Оно закрыто круга 2 отдельным контролем ко второму блоку. Единица «сценарий» этот
класс не ловит **by construction**: она считает блоки, а пара живёт между утверждениями
внутри блока.

## §9 Круг 1 — четыре блокирующих замечания, что сделано по каждому

Круг 1 вернул документ с ❌ CHANGES REQUESTED. Ниже — исход по каждому пункту: что было
неверно, чем закрыто, и чем это проверяется. Разбор оставлен в документе намеренно: каждая
из четырёх ошибок правдоподобна и вернётся, если не сказать, чем именно она опровергнута.

### Б1 · Две приёмки об одном предмете санкционировали противоположное

**Что было.** IAM-ID-1 (APPROVED, круг 5) клал перечень **всех** членств человека на его
ресурс и возвращал по умолчанию; этот документ ту же величину объявлял утечкой. Оба
документа несли вердикт, то есть кодировать было разрешено по любому.

**Чем закрыто.** Не текстом, а **решением**: `PRO-Robotech/kacho#1085` — сужение (§2.0,
четыре довода). У IAM-ID-1 **отозвана часть APPROVED**, вердикт снят до `DRAFT`, отзыв
объявлен отдельной записью в его шапке, отозванные строки помечены в самих сценариях.

**Чем проверяется.** Вердикт IAM-ID-1 читается машинно (`scripts/docs-gate/check-01`) и
даёт `DRAFT`; в §2.6 названы **десять** правимых строк с исходом по каждой.

**Что осталось незакрытым и названо.** Ведомость приёмок дерева продукта живёт в другом
репозитории и этим изменением не правится — §0.3.

### Б2 · IAM-ID-2-03 и IAM-ID-2-05 требовали взаимоисключающего от одного входа

**Что было.** Вызывающий без прав, аккаунт-скоупный путь, законная пагинация: -03 требовал
отказа, -05 — `200` с пустым перечнем. Оба утверждения защитимы порознь; исход выбирает
**устройство**, а его никто не выбирал. Третий сценарий (-06) при этом требовал, чтобы
сужение на данных стояло «ровно у одного RPC — своего списка», то есть уже склонял к первому.

**Чем закрыто.** §2.7: аккаунт-скоупные чтения гейтятся **пообъектно на крае**, полоса
сужения на данных у них не объявляется. -05 переписан: порядок «формат раньше замыкания»
утверждается тем, кто до замыкания **доходит**, а вызывающий без прав вынесен в свою половину
со **своим объявленным** исходом.

**Чем проверяется.** Предикат §2.7 печатает полосу, отношение и область всех трёх записей
одной командой. Толерантности `oneOf` не заведено ни в одном сценарии.

**Ошибка, которую здесь легко повторить.** Списать расхождение на «authz-first толерантность»
и разрешить `oneOf([200, 403])`. Это не исход: `oneOf` перечислил бы то, о чём не решали, и
покрыл бы обе реализации разом.

### Б3 · Сценарий не называл отношение, а очевидный выбор ломает адресата директивы

**Что было.** -03 говорил «отказ по полосе, которой край гейтит объект аккаунта». Любое
отношение удовлетворяло эту формулировку.

**Чем закрыто.** §2.8: `viewer` @ `account`. Обосновано **прецедентом дерева** — снятие
членства и приглашение судятся тиром аккаунта (`member_remover: editor`, `Invite: editor`), а
чтение обязано быть **не уже** записи. Названо и то, почему напрашивающийся `v_get`/`v_list`
негоден: они намеренно не читают тир распорядителя, и под ними делегированный администратор
не прочитал бы членства в своём же аккаунте.

**Чем проверяется.** Контроль **по тиру** в -03 (держатель `admin` получает `200`) и третья
инъекция в -06 (подмена `viewer` → `v_list` роняет гейт с именем отношения). Ни одна из
прочих проверок каталога этой подмены не ловит — потому обе половины и заведены.

**Побочное, записанное прямо:** под `viewer` край отдаёт на отказе `403`, а не скрывающий
`404`; оракула это не создаёт, и утверждается это **пробой байт-идентичности**, а не доводом
(§2.8, выноска; -03).

### Б4 · §1 сверялся со стволом, а работа садится в релизную линию

**Что было.** Предикаты §1 писаны под `R=origin/main`. Между стволом и линией работ — **53**
коммита, из них **38** трогают пути этой главы. Прецедент, на котором стоит §2.8, в стволе
**не существует вовсе**: ни отношения, ни глагола.

**Чем закрыто.** База перепинена на `origin/release/identity-3` @ `e0d089acc`; перемерены все
**25** строк §1 — совпало **23**, изменилось **2**, оба названы (§1.0б). Причина выбора базы
и величина разрыва — §1.0а.

**Ошибка, которую здесь легко повторить.** Считать это «устареванием чисел». Устарели не
числа: **предмет решения отсутствовал в измеряемом дереве**, и никакая аккуратность в рамках
ствола этого бы не показала.

### Сверх заказанного — найдено при перепроверке

1. **Предикат §2.6 был одноязычным и потерял блокирующее.** Счёт по `memberships|
   MembershipService` давал **6** строк IAM-ID-1; тот же предикат со вторым языком корпуса —
   **40**. В недостачу попала **вся группа F** (`-43`…`-48`) — те самые строки, которые
   санкционировали перечень всех членств. Класс назван в корпусе
   (`testing.md` §«Предикат по ДВУЯЗЫЧНОМУ корпусу…»); здесь он сработал в чистом виде, и
   спор круга 1 о числе («шесть» против «тридцати четырёх») разрешён замером, а не выбором
   большего: правимых строк **десять**, единица счёта названа.
2. **Одно утверждение §4 соседнего документа ложно на линии работ.** Строка **S4** говорит
   «НЕ НАЧАТА», обосновывая это тем, что `reserved` на поле 6 не объявлен; на
   `origin/release/identity-3` он объявлен. Перемерен **один** предикат — тот, на который
   опирался этот документ; остальные строки §4 и все 47 предикатов §1 соседа **не
   перемерялись**, и это сказано там же, где находка (IAM-ID-1 §4). Перепись остального —
   работа круга 6 соседа, а не этого документа.
