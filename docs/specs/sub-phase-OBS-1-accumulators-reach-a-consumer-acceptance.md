# Sub-phase OBS-1 (у накопителей появляется потребитель) — Acceptance

> **Статус:** ✅ APPROVED (раунд 2, 2026-08-14, `acceptance-reviewer`) — блокирующие
> замечания раунда 1 закрыты замером, воспроизведённым ревьюером на том же стенде и
> той же ревизии; непрозрачных замечаний не осталось. Можно начинать `class-exposure-analyst`
> → планирование → реализацию (ban #1 снят).
> **Дата:** 2026-08-14
> **Автор:** `acceptance-author` · **Ревьюер:** `acceptance-reviewer`
> **Задача:** PRO-Robotech/kacho#208 (`ci`, `tech-debt`, `P1`, `size:L`, `area:ci`),
> продолжение переписи по #197.
> **Ревизии замера:** продукт `PRO-Robotech/kacho` @ `5df7da76` (`origin/main`,
> 2026-08-14). Все числа ниже получены на ней; предикат назван у каждого.
> **Стенд замера:** `kind-kacho-vpc` (`https://127.0.0.1:40407`, релиз
> `kacho-umbrella`, 37 подов, поднят 2026-08-13), namespace `kacho`.
> **Раунд 2.** §1.2 перемерен **исполнением на стенде** после `❌ CHANGES REQUESTED`:
> прежняя редакция считала объявленный `containerPort`, а не привязанный слушатель,
> и потому дала «поднимают четверо» там, где поднимают **семеро**. Из той же ошибки
> вышли решения Р8 и Р9 — оба переписаны, один потерял предмет целиком. Разбор — §1.4.
> **Воспроизведено ревьюером** (независимо, тот же стенд и та же ревизия, 2026-08-14):
> П1 — семь `200` и отказ соединения у края; перепись объявивших себя для сбора — 4 пода,
> ни одного нашего; П0 = 7; П3 = 7 непустых умолчаний; носителей 13; ссылок на
> `services/{iam,geo}/deploy` — 0 при контроле в обратную сторону; единственная
> `prometheus.io/scrape` — в нерендерящемся чарте; типов ресурса описателя в кластере — 0
> из 6 CRD. Конструируемость Given проверена по продукту: фикстуры аварийного режима и
> мягкого прохода существуют, у vpc режим недостижим из конфигурации. `docs-gate` — 4/4.
> **Контракт затронут:** нет ни одного нового RPC и ни одного изменения `.proto`.
> Предмет приёмки — **диагностическая поверхность процесса** (`GET /metrics`,
> cluster-internal) и объявление её сбора в профиле развёртывания.

---

## Обзор

Накопитель, чью величину не читает ни один не-тестовый вызывающий, отвечает на два
разных вопроса одним молчанием: «событие не случилось ни разу» и «код, который его
считает, вообще не исполнялся». Задача #208 нашла три таких накопителя за пределами
`services/iam` и назвала структурный блокер: у **края** нет поверхности сбора метрик
вовсе, поэтому читателя некуда провязать.

Замер **на поднятом стенде** показал, что блокер и уже, и хуже, чем предполагалось.
Уже — потому что поверхность не только есть в коде у семи процессов из восьми, но и
**поднята и отвечает `200` у всех семи**; не поднимает её ровно тот процесс, у
которого её нет в коде, — край. Хуже — потому что **объявляет себя для сбора ноль
процессов из восьми**, а единственное объявление сбора, существующее в дереве, лежит
в чарте, который **не разворачивается ничем**. Агента сбора в умбрелле — ноль.

То есть величины края и сужателя надо действительно **завести** (их нет), а семи
поднятым поверхностям не хватает не подъёма, а **того, кто придёт**. Пока никто не
приходит, поднятая поверхность неотличима от отсутствующей — та же форма без
содержания, только этажом выше.

Приёмка закрывает предмет целиком и в три стадии, каждая production-complete в своих
границах: **S1** — поверхность края и обе его величины на ней; **S2** — величины
сужателя списков у всех пяти потребителей; **S3** — у величин появляется потребитель:
каждый из восьми процессов объявляет себя для сбора в том чарте, который
разворачивается, а расхождение между объявленным и поднятым роняет проверку.

---

## 1. Замер состояния — что верно на `5df7da76` и на стенде

### 1.1. Накопители и их читатели

**Предикат A** (носители): не-тестовый Go-файл объявляет поле
`atomic.Int64|Uint64|Int32|Uint32`.

```sh
git grep -n -E '^\s+[a-zA-Z_][a-zA-Z0-9_]*\s+atomic\.(Int64|Uint64|Int32|Uint32)$' \
  -- '*.go' ':!*_test.go' | sed 's/:.*//' | sort | uniq -c | sort -rn
```

Носителей — **13**. **Предикат B**: у метода, читающего эти поля, нет ни одного
не-тестового вызывающего. **Предикат C** (контроль в обратную сторону): величина не
уходит в наблюдаемый вывод в собственном пакете.

| Носитель | Читатель вне тестов | Итог |
|---|---|---|
| `gateway/internal/middleware` — решения края (5 полей + гистограмма) | **нет** | **предмет 1** |
| `gateway/internal/clients` — обращения клиента проверки прав (1 поле) | **нет** | **предмет 2** |
| `pkg/listnarrow` — сужение списков (3 поля) | **нет** | **предмет 3** |
| `services/iam` — теневое сравнение (4 поля) | есть (композиционный корень) | закрыт в #197 |
| `services/iam` — зеркало ключей проверки (3 поля) | есть (композиционный корень) | закрыт в #197 |
| `services/vpc` — наблюдатель шва плоскости данных (8 полей) | метода-слепка вне проб нет, **но** все восемь величин печатаются в журнале строкой события | исключён предикатом C |
| `pkg/grpcsrv` — приём соединений (3 поля) | есть (два корня) | не предмет |
| `services/iam` — основания меточной ветви (2 поля) | есть (через теневое сравнение) | не предмет |
| счётчик в наблюдаемости vpc / nlb / compute (по 1 полю) | есть (коллектор реестра) | не предмет |
| `tools/authzformbench`, `internal/pgtest` | оснастка прогонов | вне предмета |

**Контроль в обратную сторону пройден.** Наблюдатель шва плоскости данных подпадает
под предикаты A и B, но исключается C: восемь величин из восьми выходят в журнал
(`grep -c '_total"' services/vpc/internal/apps/kacho/api/dataplane/observer.go` →
**13** мест печати на 8 величин). То есть перепись не слепа и «ноль находок» отличимо
от «ноль прочитанного».

### 1.2. Куда величине идти — состояние поверхностей, **измеренное вызовом**

Вопрос «поднята ли поверхность» решается **обращением к ней**, а не чтением чарта.
Объявленный `containerPort` слушателя не создаёт и его отсутствие слушателя не
отменяет: в Kubernetes это информационное поле, процесс привязывает порт независимо
от него. Поэтому основной предикат здесь — исполненный запрос из кластера.

**П0 (поверхность есть в коде).** Ровно **7** файлов — по одному композиционному корню:

```sh
git grep -lE 'Handle\("(GET )?/metrics"' -- 'services/*/cmd/**' 'gateway/cmd/**' ':!*_test.go'
```

Обе формы записи обязательны в предикате: шесть процессов пишут `Handle("GET /metrics"`,
iam — `Handle("/metrics"`. Предикат по одной форме дал бы шесть и объявил бы iam
беспредметным.

**П1 (исполнен на стенде, 2026-08-14).** Изнутри кластера, из пода входа, по IP
каждого пода:

```sh
kubectl -n kacho exec <под-входа> -- \
  curl -s -o /dev/null -w '%{http_code}' --max-time 4 http://<podIP>:<порт>/metrics
```

**П2 (самоотчёт процесса).** `kubectl -n kacho logs <под> | grep 'servicehost: поверхность поднята'`
— строка несёт `addr`, `reach`, `auth`, `mode`. Подтверждает П1 **независимо**: П1
говорит «кто-то на этом порту отвечает», П2 — «этот процесс сам объявил, что поднял её
там». Шесть процессов из семи подтвердились обоими; у iam стартовые строки вытеснены из
журнала, и это записано как **недостающее подтверждение**, а не как отрицательный
результат.

**П3 (умолчание адреса в конфигурации).** Ровно **7** строк — по одной на процесс:

```sh
git grep -n -E 'METRICS_ADDR" default|SetDefault\("(api-server\.)?metrics[-.](endpoint|address)"' \
  -- '*.go' ':!*_test.go'
```

| Процесс | `/metrics` в коде | умолчание адреса (П3) | **ответ на стенде (П1)** | самоотчёт (П2) | порт в Service | объявление сбора |
|---|:--:|---|:--:|:--:|---|---|
| vpc | да | `:9095` | **`200`** | да, `:9095` | нет | **нет** |
| compute | да | `:9095` | **`200`** | да, `:9095` | нет | **нет** |
| geo | да | `:9095` | **`200`** | да, `:9095` | нет | **нет** |
| iam | да | `tcp://0.0.0.0:9095` | **`200`** | недоступен: журнал пода вытеснен (начинается через 16 ч после старта) | нет | **нет** |
| nlb | да | `:9101` | **`200`** | да, `:9101` | `kacho-nlb:9101` | описатель, выключен |
| storage | да | `:9095` | **`200`** | да, `:9095` | `kacho-storage:9095` | описатель, выключен |
| registry | да | `:9095` | **`200`** | да, `:9095` | `registry-internal:9095` | **нет** |
| **край (api-gateway)** | **нет** | ручки нет вовсе | **отказ соединения** (`curl` 7) | — | нет | **нет** |

**Умолчание адреса непусто у всех семи** (П3) — то есть слушатель поднимается и там,
где профиль развёртывания о наблюдении не говорит ничего. Ни один рендерящийся чарт его
не гасит:

```sh
git grep -rn -E 'METRICS_ADDR|metrics-endpoint|metrics\.(endpoint|address)|metrics:' \
  -- deploy/helm/umbrella/charts/kacho-iam deploy/helm/umbrella/charts/kacho-geo \
     services/vpc/deploy services/compute/deploy services/nlb/deploy gateway/deploy \
     services/registry/deploy services/storage/deploy | grep -v hpa.yaml
```
→ **8** строк, и все — присвоения **непустых** значений (nlb `:9101`, registry и
storage `:9095`) либо `metrics.enable: true` у vpc. Чарты iam, geo, compute и края о
диагностическом адресе не говорят **ничего** — и именно поэтому три первых поднимают
его умолчанием, а четвёртый не поднимает: у него нет ни ручки, ни поверхности.

**Кто объявляет себя для сбора — ноль.** Предикат на стенде:

```sh
kubectl -n kacho get pods -o json | jq -r '[.items[]
  | select((.metadata.annotations//{})|keys|map(select(test("prometheus")))|length>0)
  | .metadata.name] | .[]'
```
→ **4** пода из 37, и **ни один не наш**: `cert-manager` ×3 (порт 9402) и `openfga`
(порт 2112). Это чужие чарты, объявляющие себя сами.

> Пустой `//{}` в предикате — не украшение: без него `jq` падает на первом же поде без
> аннотаций, и «ноль находок» становится неотличимо от «предикат не доработал».

**Единственное объявление сбора в дереве лежит в чарте, который не разворачивается.**
`git grep -rn 'prometheus.io/scrape' -- '*.yaml'` → **одно** вхождение,
`services/iam/deploy/templates/deployment.yaml:37`. См. §1.3 — этого чарта нет ни в
одном рендере.

**Описатели сбора — два, оба по умолчанию выключены**
(`git grep -rln 'kind: ServiceMonitor' -- '*.yaml'` → `services/nlb/deploy`,
`services/storage/deploy`; `serviceMonitor.enable: false` / `serviceMonitor.enabled: false`
в их `values.yaml`). Профилей значений в умбрелле — **10**
(`ls deploy/helm/umbrella/values*.yaml | wc -l`); упоминает описатель **один**
(`values.dev.yaml`), и упоминает, чтобы **выключить**.

**Агентов сбора в умбрелле — 0**
(`git grep -il 'prometheus\|victoriametrics\|vmagent\|grafana' -- 'deploy/helm/umbrella/Chart.yaml'`).
Каталогов чартов наблюдаемости — **0** (`git ls-files | grep -c 'charts/observability/'`),
правил тревог — **0** (`git grep -l 'kind: VMRule\|kind: PrometheusRule' -- '*.yaml' | wc -l`),
панелей — **0**.

> Про панели: наивный предикат `git ls-files | grep -ci 'dashboard.*json\|grafana'`
> даёт **6** и все шесть — ложные: это `package.json`, `tsconfig*.json` и
> `.stylelintrc.json` модуля консоли `ui-future/dashboard`, к панелям отношения не
> имеющие. Число получено просмотром совпадений, а не их счётом; предикат, чьи
> совпадения не разобраны по референту, здесь дал бы шесть панелей из воздуха.

**Побочный факт, который стоит назвать, раз он измерен:** порт диагностики у nlb и
storage объявлен на том же Service, что и публичная gRPC-поверхность
(`kacho-nlb`, `kacho-storage`), а у registry — на внутреннем (`registry-internal`).
Сегодня все Service — `ClusterIP`, и за вход заведены только `api-gateway`, `ui` и
`openfga`, поэтому наружу это не выходит; но форма разъехалась, и разъехалась молча.
Решение — Р13.

### 1.3. Два чарта не разворачиваются ничем — и в одном из них лежала опора Р8

Стенд поднимается одним релизом умбреллы (`helm upgrade --install`), поэтому
рендерится ровно то, что умбрелла тянет. Она тянет **семь** чартов зависимостями
`file://` (`grep -n 'repository: file://' deploy/helm/umbrella/Chart.yaml`) и
**четыре** каталогом сабчартов (`ls -d deploy/helm/umbrella/charts/*/`), причём про
вторые её `Chart.yaml` прямо говорит, что зависимостями они объявлены **намеренно не**.

| Процесс | чарт, который рендерится |
|---|---|
| vpc · compute · nlb · registry · storage · край | `services/<svc>/deploy`, `gateway/deploy` (зависимости `file://`) |
| iam | `deploy/helm/umbrella/charts/kacho-iam` (каталог сабчарта) |
| geo | `deploy/helm/umbrella/charts/kacho-geo` (каталог сабчарта) |

Следовательно `services/iam/deploy` (**7** отслеживаемых файлов) и
`services/geo/deploy` (**2**) не разворачиваются **ничем**. Предикат:

```sh
git grep -rn -F -e 'services/iam/deploy' -e 'services/geo/deploy' \
  -- . ':!services/iam/deploy' ':!services/geo/deploy'
```
→ **ноль** строк. Контроль в обратную сторону — тот же поиск по более широкой подстроке
(`-e 'iam/deploy' -e 'geo/deploy'`) — даёт **одну**, и она подтверждает вывод, а не
опровергает его: комментарий в `values.fe3455-prod.yaml:328` сам говорит «а **НЕ** из
`../kacho-iam/deploy`». То есть ноль строгого предиката — настоящий ноль, а не
следствие слишком узкого поиска.

**Цена этого факта для настоящего документа названа прямо, а не сглажена.** Прежняя
редакция §1.2 называла iam носителем аннотации сбора и строила на нём решение Р8
(«аннотация выбрана потому, что её уже делает iam»). Аннотация действительно есть — в
чарте, которого нет ни в одном рендере. Опора решения была измерена на артефакте, у
которого нет потребителя, то есть ровно на том классе, ради которого заведена задача
#208. Р8 переписан на довод, не зависящий от чужого примера; появился Р12, требующий
писать объявление сбора в **рендерящийся** чарт и считающий объявление вне их —
находкой. Снятие самих осиротевших чартов — О-7.

### 1.4. Почему прежний замер солгал — и что из этого правило

Прежняя редакция брала предикат `grep -c 'name: metrics' <чарт>/templates/deployment.yaml`
и называла результат «порт в поде». Он дал четыре файла — `services/iam/deploy`,
`services/nlb/deploy`, `services/registry/deploy`, `services/storage/deploy`, — откуда
и вышло «поднимают четверо». Три ошибки в одной строке:

1. **считалось объявление, а не исход.** `containerPort` — информационное поле; порт
   привязывает процесс, и привязывает его по своей конфигурации;
2. **не был прочитан противоположный источник.** Умолчание адреса непусто у всех
   семи — то есть чтения одной строки конфигурации хватало, чтобы усомниться;
3. **один из четырёх файлов принадлежал чарту, который не рендерится** — то есть
   даже как замер объявлений число было завышено.

Правило, которое отсюда следует и применяется во всех сценариях ниже: **утверждение
«поверхность поднята» доказывается обращением к ней либо самоотчётом процесса, и
никогда — объявлением в чарте.** Объявление отвечает на другой вопрос — «кто про неё
знает», — и именно он в этом дереве отвечен отрицательно.

### 1.5. Что из этого следует для #208

Тело задачи верно во всём, что проверялось, и неполно в одном: оно называет блокером
отсутствие поверхности у края — и это так, край единственный без неё. Но следующий
шаг, который тело задачи оставляет неявным, замер делает явным: у **семи** остальных
поверхность есть, поднята и отвечает, а **потребителя нет ни у одной**. Значит
«провязать читателя» закрывает предмет только у края и сужателя; у остальных предмет —
не подъём, а объявление сбора. Приёмка это фиксирует решениями Р8, Р12 и стадией S3, а
не молча.

> [!note] `sub-phase-W3.2-observability-customisation-acceptance.md` — не опора
> Тот документ (**DRAFT**, 2026-05-24, не APPROVED) описывает чарт наблюдаемости с
> панелями и правилами тревог в прежнем полирепо развёртывания и опирается на него как
> на существующий baseline. В сегодняшнем дереве такого чарта нет (замер §1.2: 0
> каталогов, 0 панелей, 0 правил). Настоящая приёмка на него не ссылается, его предмет
> не забирает и его состояние не меняет: перечисленные им панели и тревоги — предмет
> преемницы (§6, О-1), а не этой работы.

---

## 2. Решения

Развилки решены здесь; по каждому решению — обоснование, цена и предикат пересмотра.

### Р1. Предмет 1 (решения края) — **завести поверхность и читателя**

Счётчики решений края — единственное место, где видно, сколько запросов платформа
разрешила, скольким отказала и сколько раз проверка не смогла состояться.
`security.md` §Hardening-инвариант 8 требует, чтобы «ноль отказов за всю жизнь
контроля» было заметно; снять эти величины значило бы сделать самый нагруженный пункт
принятия решения о доступе ненаблюдаемым.

**Цена:** ещё один слушатель в процессе края, ручка адреса с непустым умолчанием (Р9),
блок значений в чарте, коллектор. Порт в Service и `containerPort` в цену **не входят**
и не заводятся: досягаемость даёт аннотация пода (Р8). **Предикат пересмотра:** решение
о доступе перестаёт приниматься процессом края (уезжает в сеть служб) — поверхность
уезжает вместе с ним.

### Р2. Предмет 2 (обращения клиента проверки прав) — **завести читателя, отдельной серией**

Величина считает обращения **по проводу, включая повторы**, и потому не выводится из
числа решений: их отношение и есть усиление повторов, то есть ответ на вопрос «край
долбит владельца прав или спрашивает по разу». Пара «решения ↔ обращения» также и
есть различитель, которого требует #208: полосы решений — сторона исходов,
обращения — сторона обращений.

**Цена:** одна серия. **Предикат пересмотра:** если клиент когда-нибудь перестанет
повторять запросы, величина совпадёт с числом решений и станет лишней — тогда снять.

### Р3. Предмет 3 (сужение списков) — **завести читателя у всех пяти потребителей, добавив положительную величину**

Три существующие величины сужателя — аварийный проход, мягкий проход на доказанной
неверной настройке и мягкий проход на временном отказе — самые чувствительные из всех
трёх предметов: каждая означает **страницу, ушедшую несуженной**. Снятие исключено.

Но одних их **недостаточно**: три нуля не отличают «аварийных проходов не было» от
«сужателя не звали ни разу». Поэтому в набор величин добавляется **положительная
полоса** — число операций сужения, прошедших штатно. Единица счёта у всех четырёх
одна: **одна публичная операция сужения страницы**.

**Цена:** новое поле в наборе величин `pkg/listnarrow`, общий коллектор и пять
регистраций. **Предикат пересмотра:** сужатель перестаёт допускать мягкий проход и
аварийный режим — три полосы отказов уходят вместе с ветками, положительная остаётся.

### Р4. Отношение доли попаданий — **снять** (третий исход, в чистом виде)

Слепок величин края сегодня отдаёт производную `…_cache_hit_ratio`, а рядом живёт
её отдельный аксессор; у обоих читатель только в пробах. Доля, посчитанная в процессе
за всё время жизни, на поверхности бесполезна: её нельзя ни продифференцировать по
времени, ни сложить по репликам, а потребитель считает её из попаданий и промахов сам.
Оставить — значит завести второе место об одном предмете.

**Цена:** удаляется аксессор и его пробы. **Предикат пересмотра:** появится
потребитель, которому нужна именно пожизненная доля и который не может её вычислить, —
вернуть как производную у потребителя, не в процессе.

### Р5. Имена и единицы — платформенный словарь и базовые единицы, **сейчас**

Серии края ещё не читал никто, поэтому переименование сегодня бесплатно, а завтра —
ломающее изменение для панелей и тревог. Значит: базовые единицы (секунды, не
миллисекунды) и симметрия с отвечающей стороной того же ребра, у которой уже есть
`kacho_iam_authz_check_decisions_total{rpc,decision}` и
`kacho_iam_authz_check_duration_seconds`.

Итоговые имена — §3.1. **Цена:** правка констант интервалов гистограммы и места
наблюдения. **Предикат пересмотра:** платформа меняет словарь имён — меняются все
серии разом, а не одна.

### Р6. Поверхность края — **отдельный cluster-internal слушатель, объявленный профилем**

Собирается через `pkg/servicecontract.Surface` + `pkg/servicehost.ServeSurface`, как
все семь остальных: адрес — трёхсостоятельная ось; досягаемость —
cluster-internal; аутентификация — **объявленная неприменимость с причиной**
(задокументированное исключение `security.md` §AuthN+AuthZ ВЕЗДЕ: на проводе счётчики
процесса, ни секретов, ни данных арендатора, ни сведений о размещении); транспорт —
открытый текст, как у диагностических поверхностей остальных процессов.

`/healthz` и `/readyz` края **остаются там, где стоят** (на внешнем слушателе). Их
переезд — отдельный предмет со своим риском (пути проб пода меняются, значит меняется
раскатка), а дублирование дало бы два места об одном предмете.

**Цена:** край становится восьмым процессом с диагностической поверхностью, но его
композиционный корень по-прежнему поднимает **остальные** свои слушатели руками —
гейт профиля поверхностей осматривает только `services/*/cmd/*`. Расширение его области
на край — О-2 (§6). **Предикат пересмотра:** край перестаёт быть отдельным процессом.

### Р7. Метки полос и метода — **не заводить**

Накопитель решений в крае **один** (композиционный корень строит одну проверку и
отдаёт её и в перехватчики gRPC, и в цепочку HTTP), поэтому метка полосы означала бы
изменение самой проверки, а не наблюдения. Метка метода тоже не заводится: в каталоге
прав **319** записей (`python3 -c "import json;print(len(json.load(open(
'gateway/internal/middleware/embed/permission_catalog.json'))))"`), то есть метка дала бы
319 × 4 серии на реплику, а пометодная разбивка уже есть у отвечающей стороны ребра.

**Цена:** по числам не видно, какая из полос края отказывает; полосу называет журнал,
где стоит полное имя метода. **Предикат пересмотра:** разбор происшествия требует
различать полосы — метка добавляется в трёх местах, где полоса известна.

### Р8. Потребитель — **двусторонний: объявление сбора у всех восьми + проба стенда**

Наблюдаемость без потребителя — та же форма без содержания, поэтому «читатель есть»
засчитывается только при обоих:

1. **потребитель-оператор** — каждый из восьми процессов объявляет себя для сбора
   **аннотацией пода** (`prometheus.io/scrape|port|path`) в том чарте, который
   рендерится (Р12);
2. **потребитель-в-дереве** — проба стенда, которая **читает** поверхность каждого из
   восьми процессов изнутри кластера и требует объявленные семейства. Она умеет
   краснеть: снятие регистрации коллектора делает её красной с именем процесса.

**Почему аннотация, а не описатель сбора.** Довод стоит на двух измеренных фактах, и
ни один из них не «iam уже так делает» — прежняя редакция опиралась именно на это, а
чарт, который она приводила в пример, не разворачивается (§1.3):

- **типа ресурса для описателя в кластере нет.** Описатель (`ServiceMonitor`)
  обслуживается оператором сбора, а агентов сбора в умбрелле — **0** (§1.2). Два
  существующих описателя выключены умолчанием обоих чартов, и единственный профиль из
  десяти, который их упоминает, упоминает, **чтобы выключить**. То есть выбрать
  описатель значило бы завести объявление, у которого сегодня нет читателя, — ровно
  класс задачи #208;
- **аннотацию читают агенты обоих семейств** без установки собственного типа ресурса,
  и **обращаются они к поду напрямую** (`role: pod`, адрес и путь берутся из самой
  аннотации). Поэтому аннотация делает досягаемыми и те четыре процесса, у которых
  порта диагностики нет ни в одном Service, — без правки Service вообще.

**Следствие, названное явно, чтобы не сделать лишней работы:** порт в Service и
`containerPort` **не требуются** аннотационным путём и потому OBS-1 **не заводит их ни
одного**. Завести их у четырёх оставшихся «для порядка» значило бы добавить восемь
объявлений без читателя в приёмку, предмет которой — объявления без читателя.

Три уже существующих порта в Service **остаются**, и у каждого назван читатель, чтобы
это не читалось как исключение по инерции: у nlb и storage порт — цель их описателей
сбора (`servicemonitor.yaml`); у registry описателя нет, и его порт остаётся как
**именованный адрес** поверхности для оператора, а не как объявление для агента. Все три
обязаны совпадать с аннотацией — сверяет сценарий 24.

**Цена:** восемь чартов затронуто, один общий блок значений, одна проба стенда, одна
декларативная проверка. **Предикат пересмотра:** в кластер приходит оператор сбора
(появляется тип ресурса описателя) — тогда описатель становится первичным путём, а
аннотация остаётся для кластеров без оператора; либо платформа принимает сбор на уровне
сети служб — объявление уезжает туда целиком вместе с этим решением.

### Р9. Ручка адреса у края — **та же форма и то же непустое умолчание, что у семи остальных**

> **Предмет прежней редакции этого решения не существует, и это сказано, а не
> подчищено.** Р9 требовал сделать умолчание адреса непустым «потому что пустое
> умолчание и породило состояние §1.2». Замер П3 показал: умолчание непусто **у всех
> семи** уже сегодня, и состояние §1.2 порождено не им, а отсутствием объявления сбора.
> Решение, которое ничего не меняет, — не решение; поэтому оно сужено до того
> единственного места, где выбор действительно предстоит, — новой ручки у края.

Край получает ручку адреса диагностической поверхности. Она берёт **непустое**
умолчание (`:9095`) — по образцу семи измеренных, — а выключение требует **явно
объявленной пустоты с причиной**: трёхсостоятельная ось `pkg/servicecontract` это уже
поддерживает (значение · объявленная неприменимость с причиной · необъявленность =
отказ старта). Довод не «так принято», а тот же, что у оси аутентификации: снятие
требует слов, забытое слов не имеет, — и восьмой процесс не должен оказаться
единственным, чья поверхность зависит от того, вспомнил ли о ней профиль.

**Цена:** край занимает ещё один порт даже там, где сбора нет; в развёртываниях с
жёсткой картой портов это надо объявлять пустотой явно. **Предикат пересмотра:**
появится посадка, где привязка лишнего порта невозможна, — умолчание возвращается к
пустому у **всех восьми** разом, а единственным источником адреса становится чарт.

### Р10. Мягкий проход контроля — **отдельная полоса, а не общий счётчик отказа**

Сегодня «проверка не смогла состояться» считается одной величиной независимо от того,
чем это кончилось для запроса, — а кончиться может двумя противоположными вещами.
`security.md` §Hardening 8(б) требует, чтобы мягкий проход нёс собственный счётчик,
иначе он невидим. Поэтому полоса отказа расщепляется на **две**: запрос отклонён и
запрос пропущен.

**Цена:** одно значение метки вместо одного и место записи переезжает туда, где исход
известен. **Предикат пересмотра:** мягкий проход снят с контракта — полоса уходит
вместе с ним, и её исчезновение обязано быть находкой, а не тишиной.

### Р11. Полосы послаблений проверяются **в процессе**, а на стенде обязаны быть нулями

Это решение о конструируемости, и оно вынужденное. Состояния, ради наблюдаемости
которых полосы послаблений и заводятся, **на боевом стенде недостижимы**:

- край **отказывается стартовать** с мягким проходом в любой не-dev среде, причём
  пустая метка среды считается боевой (`gateway/cmd/api-gateway/authz_validation.go`,
  `validateProductionAuthzConfig`);
- у compute и registry аварийный режим — **отказ старта** в боевой посадке; у nlb в
  боевой посадке отказом являются и выключенный сужатель, и его мягкий проход; у
  storage отказом является выключенный сужатель;
- у vpc ручки аварийного режима сужателя **нет вовсе**: композиционный корень поле не
  заполняет (предикат: `git grep -n 'Breakglass' -- services/vpc/cmd
  services/vpc/internal/apps/kacho/config ':!*_test.go'` → пусто), то есть режим
  недостижим by construction.

Сценарий, требующий такой посадки на стенде, неконструируем и красным не станет
никогда (`security.md` §Production-mode обязателен ВЕЗДЕ).

Поэтому уровень у каждого сценария назван явно, и полосы послаблений разделены надвое:
**в процессе** — полоса растёт, когда режим объявлен (положительное утверждение);
**на стенде** — полоса присутствует и **равна нулю** всегда (отрицательное утверждение
с настоящим предметом: ненулевое значение означает стенд, поднятый в ослабленной
посадке, и это находка, а не шум).

**Цена:** каждое послабление проверяется двумя пробами вместо одной. **Предикат
пересмотра:** появится посадка, где послабление законно и наблюдаемо на стенде, —
тогда стендовая половина превращается из «всегда ноль» в «растёт по сценарию».

### Р12. Объявление сбора пишется в **рендерящийся** чарт; вне их — находка

Замер §1.3: два каталога чартов (`services/iam/deploy`, `services/geo/deploy`) не
разворачиваются ничем, и единственная в дереве аннотация сбора лежит в первом из них.
Правка такого чарта неотличима от сделанной работы и не производит ничего.

Поэтому: (а) объявление сбора заводится в том чарте, который умбрелла действительно
рендерит (перечень §1.3); (б) декларативная проверка S3 **выводит** этот перечень из
`deploy/helm/umbrella/Chart.yaml` и `deploy/helm/umbrella/charts/`, а не выписывает его
списком — девятый процесс попадает под проверку в момент появления; (в) объявление
сбора, найденное **вне** выведенного перечня, — находка с координатой, а не пропуск.
Сегодняшнюю единственную такую находку OBS-1 закрывает снятием аннотации из
нерендерящегося чарта: это её собственный предмет. Снятие самих осиротевших каталогов —
О-7, у него другой предмет и другой риск.

**Цена:** проверка обязана уметь читать объявление зависимостей умбреллы, а не только
шаблоны. **Предикат пересмотра:** стенд перестаёт подниматься одним релизом умбреллы —
источник перечня меняется вместе с ним.

### Р13. Порт диагностики — на том Service, который несёт **внутренний** контур

Замер §1.2: у registry порт диагностики стоит на `registry-internal`, у nlb и storage —
на том же Service, что и публичная gRPC-поверхность. Сегодня это ничем не грозит (все
Service `ClusterIP`, за входом только `api-gateway`, `ui`, `openfga`), но форма
разъехалась молча, а `security.md` §Internal-vs-external требует, чтобы
инфра-диагностика не соседствовала с арендаторской поверхностью by construction, а не
по факту типа Service.

Правило: там, где у процесса **есть** отдельный внутренний Service, порт диагностики
объявляется на нём. Новых Service ради этого **не заводится** — это было бы объявление
без читателя, запрещённое Р8. Замер по шаблонам сводит правило к **одному** переносу:

| Процесс | внутренний Service | где сегодня порт диагностики | что делает OBS-1 |
|---|---|---|---|
| registry | `registry-internal` | на нём | ничего, эталон |
| nlb | `kacho-nlb-internal` (`service.yaml:34`) | на публичном `kacho-nlb` | **переносит** |
| storage | отдельного нет — один Service несёт `grpc-public`, `grpc-internal`, `metrics` | на нём же | ничего, О-8 |

**Цена:** у nlb меняется адрес, по которому описатель сбора находит цель, — значит в
том же изменении правится `servicemonitor.yaml`. **Предикат пересмотра:** порт
диагностики перестаёт быть объявленным в Service хоть у кого-то (все переходят на
аннотацию) — решение уходит вместе с последним портом.

---

## 3. Стадии

Каждая стадия — самостоятельный сдаваемый срез со своим DoD. Отложенное названо в §6.

**Границы, в которых каждая стадия production-complete** (ban #14 — названы явно, потому
что «объявление сбора» приезжает только в S3 и без этой строки S1 читалась бы как
незаконченная):

| Стадия | Предмет, закрытый ПОЛНОСТЬЮ | Что за границей и почему это не долг |
|---|---|---|
| **S1** | у обоих накопителей края есть читатель на поверхности процесса, поверхность отвечает по адресу пода, серии присутствуют нулями, гейт держит | сбор величин: у края его нет **ни у кого** — это S3, общая для всех восьми, а не хвост края |
| **S2** | у величин сужателя есть читатель у всех пяти, положительная полоса отличает «не звали» от «прошло штатно», гейт краснеет на шестом потребителе | то же |
| **S3** | все восемь объявлены для сбора там, где это рендерится; проба стенда читает поверхность каждого | хранилище величин и тревоги — О-1, отдельное продуктовое решение с отдельной ценой |

Ни одна стадия не оставляет за собой полуработающего состояния: после S1 величины края
наблюдаемы обращением к поду, после S2 — то же у сужателя, после S3 их **забирают**.

### 3.1. S1 — край получает поверхность и обе свои величины на ней

Предметы 1 и 2. Семейства, которые появляются на поверхности края:

| Серия | Тип | Метки | Что различает |
|---|---|---|---|
| `kacho_api_gateway_authz_check_decisions_total` | счётчик | `decision="allow"\|"deny"\|"error_refused"\|"error_passed"` | разрешено · отказано · проверка не состоялась и запрос отклонён · проверка не состоялась и запрос пропущен |
| `kacho_api_gateway_authz_check_duration_seconds` | гистограмма | — | длительность решения; `_count` отличает «решений не было» от «не считали» |
| `kacho_api_gateway_authz_cache_total` | счётчик | `result="hit"\|"miss"` | окно вердиктов работает · окно не спрашивали |
| `kacho_api_gateway_authz_client_calls_total` | счётчик | — | обращения к владельцу прав по проводу, включая повторы |

**Все значения меток объявляются при регистрации**, поэтому серии присутствуют с нулями
с первой секунды жизни процесса: «ноль отказов» и «коллектора нет» различимы без
единого запроса.

**DoD S1.** Поверхность поднимается **непустым умолчанием**, без обязанности профиля о
ней говорить, и объявляема профилем явно — включая явно объявленную пустоту с причиной
(Р9) · коллектор читает оба
накопителя · доля попаданий и её аксессор сняты (Р4) · единицы и имена по Р5 · полоса
отказа расщеплена по Р10 · оба предмета внесены в таблицу гейта `internal/repohygiene`
`TestDeclaredAccumulatorsHaveANonTestReader` и доказаны инъекцией в обе стороны · ручка
адреса заведена с непустым умолчанием (Р9) и объявлена в значениях чарта края · **порт
в Service и `containerPort` края не заводятся** — у них нет читателя (Р8), досягаемость
даёт аннотация S3 · самоотчёт процесса при старте называет адрес
поверхности либо объявленную причину её отсутствия · сценарии **OBS-1-01…OBS-1-14** и
**OBS-1-29** (в части предметов 1 и 2) зелёные · trail в
`obsidian/kacho/KAC/issue-208.md` заведён (сегодня его нет) и обновлён.

### 3.2. S2 — сужение списков наблюдаемо у всех пяти потребителей

Предмет 3. Потребителей сужателя пять — `vpc`, `compute`, `nlb`, `storage`, `registry`
(предикат: `git grep -n 'listnarrow\.New(' -- '*.go' ':!*_test.go'` — по одному
конструктору у каждого, плюс подставные в оснастке проб общего фундамента).

| Серия | Тип | Метки | Что различает |
|---|---|---|---|
| `kacho_<сервис>_list_narrow_pages_total` | счётчик | `outcome="narrowed"\|"breakglass"\|"softpass_misconfigured"\|"softpass_transient"` | страница сужена штатно · ушла несуженной по аварийному режиму · ушла несуженной на доказанной неверной настройке · ушла несуженной на отказе, который может пройти сам |

Коллектор — **одна реализация** в общем фундаменте рядом с сужателем; каждый из пяти
регистрирует её в своём реестре наблюдаемости со своим именем сервиса. Пять копий
коллектора запрещены по той же причине, по которой единствен сам сужатель: они
разъезжаются молча.

**DoD S2.** Положительная полоса заведена в наборе величин сужателя · коллектор один и
зарегистрирован у всех пяти · предмет внесён в таблицу гейта накопителей · гейт требует
регистрации от **каждого** конструктора сужателя (появится шестой потребитель — гейт
покраснеет на нём) · сценарии **OBS-1-15…OBS-1-22**, **OBS-1-29** (в части предмета 3)
и **OBS-1-30** зелёные · trail обновлён.

### 3.3. S3 — у величин появляется потребитель

Замер §1.2 сузил эту стадию: **подъём поверхности здесь уже не работа** — семь из восьми
поднимают её и отвечают `200`, восьмой получает её в S1. Работа стадии — **объявление
сбора**, которого нет ни у одного, и проверки, которые не дадут ему исчезнуть.

**DoD S3.** Все восемь объявляют себя для сбора аннотацией пода в **рендерящемся**
чарте (Р12), и перечень рендерящихся выведен из объявления умбреллы, а не выписан ·
единственная сегодняшняя аннотация вне этого перечня (`services/iam/deploy`) снята ·
порт диагностики nlb перенесён на внутренний Service вместе с его описателем (Р13) ·
объявленный порт аннотации совпадает с адресом, который берёт процесс (сценарий 24, по
дереву), **и** по этому адресу поверхность действительно отвечает (сценарий 23, на
стенде) — ни одно из двух не подменяется чтением `containerPort` (§1.4) · там, где рядом
стоит описатель сбора, его порт и путь выводятся из тех же значений, что аннотация · проба
стенда читает поверхность каждого из восьми и требует объявленные семейства, различая
три исхода (зелёный · красный · «не выполнилось») и печатая число опрошенных ·
сценарии **OBS-1-23…OBS-1-29** и **OBS-1-31** зелёные · trail обновлён.

**Чего в DoD S3 намеренно НЕТ, и почему** (обе строки стояли в прежней редакции и сняты
замером, а не забыты): «vpc, compute и geo получают адрес» — адрес у них есть, и
слушатель поднят (П1, П2); «получают порт в поде и порт в Service» — аннотационный путь
обращается к поду напрямую, поэтому оба объявления были бы объявлениями без читателя
(Р8).

---

## 4. Об уровнях и о парах «HTTP-статус и gRPC-код»

**Уровень назван у каждого сценария**, и это не оформление: он отвечает на вопрос,
конструируемо ли предусловие.

| Уровень | Где исполняется | Почему предусловие конструируемо |
|---|---|---|
| **в процессе** | проба Go рядом с кодом; носитель и реестр собираются напрямую | посадка процесса не участвует, поэтому объявленные послабления (мягкий проход, аварийный режим) допустимы — их запрещает загрузочный страж, до которого проба не доходит |
| **на стенде** | поднятый стенд в боевой посадке | предусловие — обычный трафик арендатора либо чтение поверхности |
| **по дереву** | гейт или декларативная проверка | предусловие — состояние дерева, доказывается инъекцией |

Диагностическая поверхность — **не** gRPC-поверхность и не проходит через край:
`runtime.HTTPStatusFromCode` в её ответах не участвует, `google.rpc.Status` она не
производит. Поэтому сценарии над ней утверждают HTTP-статус и **тело в формате
экспозиции**, а пары не называют: названная здесь пара описывала бы производителя,
которого нет.

Пара утверждается там, где она есть, — в сценариях, где запрос идёт через край
(OBS-1-05, OBS-1-06, OBS-1-13, OBS-1-17, OBS-1-21).

---

## 5. Сценарии

### S1 — поверхность края и его величины

#### Сценарий 01: поверхность поднята, отвечает и несёт объявленные семейства

**ID:** OBS-1-01 · S1 · **уровень: на стенде**

**Given** стенд поднят в боевой посадке
**And** профиль развёртывания края об адресе диагностической поверхности **не говорит
ничего** — она поднимается непустым умолчанием (Р9)

**When** изнутри кластера выполняется
`GET http://<IP пода края>:9095/metrics` — по адресу **пода**, не Service: порта в
Service у края нет и не заводится (Р8)

**Then** ответ — HTTP `200`
**And** заголовок `Content-Type` — формат текстовой экспозиции
**And** в теле присутствуют все четыре семейства §3.1 хотя бы по одной серии каждое

> Предусловие выбрано умышленно самым бедным: если поверхность отвечает при профиле,
> который о ней молчит, то она отвечает и при любом, который её задаёт. Обратное
> предусловие («профиль задаёт адрес») проверяло бы профиль, а не процесс.
>
> **Опорная точка:** сегодня тот же запрос к поду края даёт отказ соединения (§1.2, П1),
> то есть сценарий красный до S1 и по названной причине.

#### Сценарий 02: все полосы присутствуют нулями у свежего процесса

**ID:** OBS-1-02 · S1 · **уровень: в процессе**

**Given** проверка края и её коллектор собраны, ни одного запроса не обслужено

**When** снимается экспозиция реестра

**Then** присутствуют все четыре строки со значением `0`:
  - `kacho_api_gateway_authz_check_decisions_total{decision="allow"} 0`
  - `…{decision="deny"} 0`
  - `…{decision="error_refused"} 0`
  - `…{decision="error_passed"} 0`
**And** присутствуют `kacho_api_gateway_authz_cache_total{result="hit"} 0` и
  `{result="miss"} 0`
**And** присутствует `kacho_api_gateway_authz_client_calls_total 0`
**And** присутствует `kacho_api_gateway_authz_check_duration_seconds_count 0`

> Это и есть предмет #208: отсутствие серии и нулевая серия обязаны быть различимы.
> Уровень «в процессе», потому что на стенде «ни одного запроса» не удержать.

#### Сценарий 03: снятие коллектора роняет пробу, а не молчит

**ID:** OBS-1-03 · S1 · **уровень: в процессе**

**Given** дерево, в котором регистрация коллектора края снята

**When** исполняется проба сценария 02

**Then** проба красная
**And** текст падения называет отсутствующее семейство
**And** после возврата регистрации проба зелёная

> Инъекция в обе стороны к OBS-1-02: без неё «серии присутствуют» неотличимо от пробы,
> которая ничего не читает.

#### Сценарий 04: разрешённое решение увеличивает полосу разрешений

**ID:** OBS-1-04 · S1 · **уровень: на стенде**

**Given** снят слепок поверхности до запроса
**And** арендатор имеет право на чтение своего ресурса

**When** арендатор выполняет через край `GET /vpc/v1/networks/{id}` своего ресурса и
получает HTTP `200`
**And** выполняется `GET /metrics`

**Then** `…decisions_total{decision="allow"}` вырос ровно на `1` относительно слепка
**And** `{decision="deny"}`, `{decision="error_refused"}`, `{decision="error_passed"}`
  не изменились

#### Сценарий 05: отказ увеличивает полосу отказов

**ID:** OBS-1-05 · S1 · **уровень: на стенде**

**Given** снят слепок поверхности
**And** арендатор не имеет права на чужой ресурс, идентификатор которого он знает

**When** арендатор выполняет через край чтение чужого ресурса и получает
HTTP `404` с `google.rpc.Status.code = 5` (`NOT_FOUND`) — сокрытие существования,
байт-идентичное настоящему отсутствию (`security.md` §Hardening-инвариант 6), **либо**
HTTP `403` с `code = 7` (`PERMISSION_DENIED`), если гейт края отказал до резолва цели
(толерантность по порядку полос, `testing.md` §e2e-инварианты)
**And** выполняется `GET /metrics`

**Then** `…decisions_total{decision="deny"}` вырос ровно на `1`
**And** `{decision="allow"}` не изменилась
**And** ответ **никогда** не `200`

> Два перечисленных исхода — **разные полосы с разными производителями**, а не
> неопределённость одного. Утверждение о величине при этом точное в обоих случаях,
> потому что обе полосы считаются как отказ.

#### Сценарий 06: несостоявшаяся проверка не считается отказом

**ID:** OBS-1-06 · S1 · **уровень: в процессе**

**Given** проверка края собрана с отклонением запроса при несостоявшейся проверке
**And** собеседник владельца прав отвечает недоступностью
**And** снят слепок реестра

**When** через проверку проходит один гейтируемый запрос
**And** вызывающий получает `UNAVAILABLE` (на крае — HTTP `503`)
**And** снимается экспозиция реестра

**Then** `…decisions_total{decision="error_refused"}` вырос ровно на `1`
**And** `{decision="deny"}` не изменилась
**And** `{decision="error_passed"}` не изменилась

#### Сценарий 07: мягкий проход виден отдельной полосой

**ID:** OBS-1-07 · S1 · **уровень: в процессе**

**Given** проверка края собрана с объявленным мягким проходом
**And** собеседник владельца прав отвечает недоступностью
**And** снят слепок реестра

**When** через проверку проходит один гейтируемый запрос и запрос **пропускается**
**And** снимается экспозиция реестра

**Then** `…decisions_total{decision="error_passed"}` вырос ровно на `1`
**And** `{decision="error_refused"}` не изменилась
**And** `{decision="allow"}` не изменилась

> Отрицательный контроль к OBS-1-06: без расщепления полосы (Р10) оба сценария
> увеличивали бы одну величину, и по числам нельзя было бы сказать, отклонил контроль
> запрос или пропустил.

#### Сценарий 08: на боевом стенде полоса мягкого прохода равна нулю всегда

**ID:** OBS-1-08 · S1 · **уровень: на стенде**

**Given** стенд поднят в боевой посадке
**And** сквозной прогон отработал полностью

**When** выполняется `GET /metrics` на поверхности края

**Then** `…decisions_total{decision="error_passed"}` присутствует
**And** его значение равно `0`

> Утверждение с настоящим предметом, а не украшение: мягкий проход в боевой посадке
> запрещён отказом старта, поэтому ненулевое значение означает стенд, поднятый в
> ослабленной посадке. Присутствие серии при этом обязательно — иначе «ноль» и
> «коллектора нет» снова слились бы.

#### Сценарий 09: окно вердиктов различает попадание и промах

**ID:** OBS-1-09 · S1 · **уровень: на стенде**

**Given** снят слепок поверхности
**And** окно вердиктов включено

**When** арендатор дважды подряд выполняет один и тот же разрешённый запрос
**And** выполняется `GET /metrics`

**Then** `kacho_api_gateway_authz_cache_total{result="miss"}` вырос на `1`
**And** `{result="hit"}` вырос на `1`
**And** `kacho_api_gateway_authz_client_calls_total` вырос ровно на `1` — второе решение
  до владельца прав не дошло

#### Сценарий 10: обращения по проводу считаются вместе с повторами

**ID:** OBS-1-10 · S1 · **уровень: в процессе**

**Given** собеседник владельца прав отвечает недоступностью на первое обращение и
успехом на повтор
**And** снят слепок реестра

**When** через проверку проходит один гейтируемый запрос
**And** снимается экспозиция реестра

**Then** `kacho_api_gateway_authz_client_calls_total` вырос на `2`
**And** `…decisions_total` суммарно по всем полосам вырос на `1`

> Именно это отношение и есть предмет 2: усиление повторов не выводится из числа
> решений.

#### Сценарий 11: гистограмма длительности в базовых единицах и внутренне согласована

**ID:** OBS-1-11 · S1 · **уровень: на стенде**

**Given** край обслужил не менее одного гейтируемого запроса

**When** выполняется `GET /metrics`

**Then** присутствует семейство `kacho_api_gateway_authz_check_duration_seconds` с
  `_bucket`, `_sum`, `_count`
**And** имени, оканчивающегося на `_latency_ms`, на поверхности нет ни в одной форме
**And** значение корзины `le="+Inf"` равно `_count`
**And** значения корзин не убывают с ростом `le`
**And** `_count` больше нуля

#### Сценарий 12: производной доли на поверхности нет

**ID:** OBS-1-12 · S1 · **уровень: на стенде**

**Given** край обслужил запросы, давшие и попадание, и промах окна вердиктов

**When** выполняется `GET /metrics`

**Then** серии с именем, оканчивающимся на `_hit_ratio`, в теле **нет**
**And** попадания и промахи присутствуют отдельными сериями (OBS-1-09)

> Проверяет исход Р4 — снятие. Без этого сценария снятая производная вернулась бы
> следующей правкой незамеченной.

#### Сценарий 13: публичный слушатель серий не отдаёт

**ID:** OBS-1-13 · S1 · **уровень: на стенде + по дереву**

**Given** стенд поднят в боевой посадке

**When** выполняется `GET https://<внешний адрес края>/metrics`

**Then** ответ — HTTP `404` с `google.rpc.Status.code = 5` (`NOT_FOUND`), **либо**
HTTP `401` с `code = 16` (`UNAUTHENTICATED`), **либо** HTTP `403` с `code = 7`
(`PERMISSION_DENIED`), если гейт края короткозамкнул раньше маршрутизации —
толерантность по порядку полос (`testing.md` §e2e-инварианты)
**And** тело **не содержит** ни одной строки, начинающейся с `kacho_`
**And** ответ **никогда** не `200`

**And** детерминированную половину этого утверждения держит проверка по дереву:
обработчик экспозиции провязан **ровно** на диагностическую поверхность и ни на один
другой слушатель края; провязка его на второй слушатель роняет проверку с координатой.

#### Сценарий 14: объявленное выключение поверхности не молчит

**ID:** OBS-1-14 · S1 · **уровень: в процессе**

**Given** конфигурация края объявляет адрес диагностической поверхности пустым

**When** процесс собирает свои поверхности

**Then** сборка проходит, отказа старта нет
**And** в журнале есть строка с причиной, называющей ручку адреса и следствие
  («сбора на этой посадке нет»)
**And** самоотчёт процесса о посадке несёт то же состояние
**And** слушатель на порту диагностики не привязывается

> Отличает «профиль забыл» от «сбора здесь нет намеренно» — различие, которого сегодня
> нет ни у одного из четырёх процессов §1.2.

### S2 — сужение списков

#### Сценарий 15: четыре полосы сужателя присутствуют нулями у каждого из пяти

**ID:** OBS-1-15 · S2 · **уровень: в процессе**

**Given** сужатель и его коллектор собраны у каждого из пяти потребителей
**And** ни одной операции сужения не выполнено

**When** снимается экспозиция реестра каждого

**Then** в каждой присутствуют все четыре строки со значением `0`:
`kacho_<сервис>_list_narrow_pages_total{outcome="narrowed"|"breakglass"|"softpass_misconfigured"|"softpass_transient"}`
**And** имя сервиса в имени серии совпадает с каталогом потребителя

#### Сценарий 16: штатное сужение растит положительную полосу

**ID:** OBS-1-16 · S2 · **уровень: в процессе**

**Given** снят слепок реестра
**And** владелец прав отвечает разрешением на часть идентификаторов страницы

**When** выполняется одна операция сужения страницы
**And** снимается экспозиция реестра

**Then** `…list_narrow_pages_total{outcome="narrowed"}` вырос ровно на `1`
**And** три полосы несуженного прохода не изменились

#### Сценарий 17: сужение видно снаружи на боевом стенде

**ID:** OBS-1-17 · S2 · **уровень: на стенде**

**Given** снят слепок поверхности процесса
**And** арендатор имеет право видеть часть ресурсов проекта

**When** арендатор выполняет через край `GET /<сервис>/v1/<ресурс>?projectId=…` и
получает HTTP `200` со страницей, содержащей только его ресурсы
**And** выполняется `GET /metrics` на диагностической поверхности этого процесса

**Then** `…list_narrow_pages_total{outcome="narrowed"}` вырос не менее чем на `1`
**And** три полосы несуженного прохода не изменились

> Положительный контроль на настоящем тракте: без него сценарии 15-16 доказывали бы
> только то, что коллектор умеет складывать числа в пробе.

#### Сценарий 18: аварийный режим считается и не считается штатным

**ID:** OBS-1-18 · S2 · **уровень: в процессе**

**Given** сужатель собран с объявленным аварийным режимом
**And** снят слепок реестра

**When** выполняется одна операция сужения и страница уходит несуженной
**And** снимается экспозиция реестра

**Then** `…{outcome="breakglass"}` вырос ровно на `1`
**And** `…{outcome="narrowed"}` **не** изменился

> Пара к OBS-1-16 в обе стороны: без неё «страница ушла несуженной» было бы неотличимо
> от «страница сужена».

#### Сценарий 19: мягкий проход на доказанной неверной настройке считается отдельно

**ID:** OBS-1-19 · S2 · **уровень: в процессе**

**Given** сужатель собран с объявленным мягким проходом
**And** собеседник владельца прав отвечает так, что ответ **доказывает** неверную
настройку (по адресу не тот эндпоинт)
**And** снят слепок реестра

**When** выполняется одна операция сужения
**And** снимается экспозиция реестра

**Then** `…{outcome="softpass_misconfigured"}` вырос ровно на `1`
**And** `…{outcome="softpass_transient"}` не изменился
**And** в журнале есть запись уровня не ниже ошибки

#### Сценарий 20: мягкий проход на временном отказе считается отдельно

**ID:** OBS-1-20 · S2 · **уровень: в процессе**

**Given** сужатель собран с объявленным мягким проходом
**And** собеседник владельца прав недоступен (отказ, который может пройти сам)
**And** снят слепок реестра

**When** выполняется одна операция сужения
**And** снимается экспозиция реестра

**Then** `…{outcome="softpass_transient"}` вырос ровно на `1`
**And** `…{outcome="softpass_misconfigured"}` не изменился

#### Сценарий 21: безымянный вызывающий не растит ни одной полосы

**ID:** OBS-1-21 · S2 · **уровень: в процессе + на стенде**

**Given** снят слепок реестра

**When** операция сужения вызывается без установленной личности и отвергается
**And** на стенде тот же путь даёт HTTP `401` с `google.rpc.Status.code = 16`
(`UNAUTHENTICATED`)
**And** снимается экспозиция реестра

**Then** ни одна из четырёх полос не изменилась

> Отказ до сужения — не сужение. Сценарий отделяет «сужатель не звали» от «сужатель
> отработал», иначе положительная полоса Р3 считала бы не то.

#### Сценарий 22: на боевом стенде три полосы несуженного прохода равны нулю

**ID:** OBS-1-22 · S2 · **уровень: на стенде**

**Given** стенд поднят в боевой посадке
**And** сквозной прогон отработал полностью

**When** выполняется `GET /metrics` на поверхности каждого из пяти процессов

**Then** полосы `breakglass`, `softpass_misconfigured` и `softpass_transient`
  **присутствуют** у каждого
**And** значение каждой равно `0`

> Аварийный режим и мягкий проход сужателя запрещены отказом старта в боевой посадке,
> поэтому ненулевое значение — находка о посадке стенда, а не о трафике.

### S3 — потребитель

#### Сценарий 23: каждый из восьми процессов отдаёт свою поверхность

**ID:** OBS-1-23 · S3 · **уровень: на стенде**

**Given** стенд поднят в боевой посадке со всеми восемью процессами
**And** перечень опрашиваемых процессов **выведен**, а не выписан в пробе литералом:
пересечение двух множеств — процессы продукта, чей композиционный корень несёт
обработчик экспозиции, и чарты, которые умбрелла рендерит (§1.3). Первое множество
сегодня даёт **7**, после S1 — **8**:

```sh
git grep -lE 'Handle\("(GET )?/metrics"' -- 'services/*/cmd/**' 'gateway/cmd/**' ':!*_test.go'
```

Девятый сервис попадает в пробу в момент появления обработчика, без её правки

**When** проба изнутри кластера выполняет `GET /metrics` на диагностическом адресе
каждого из восьми

**Then** каждый ответ — HTTP `200`
**And** каждое тело содержит хотя бы одну строку `kacho_<сервис>_…`
**And** число опрошенных процессов равно **восьми**, и проба это число печатает
**And** процесс, чья поверхность не ответила, даёт исход «не выполнилось» отдельно от
  красного — вердикта нет ни у одного утверждения о нём

> «Ноль находок» обязано быть отличимо от «ноль опрошенных»: без переписи проба на
> пустом перечне процессов была бы зелёной.
>
> **Опорная точка, измеренная до работы:** на `5df7da76` этот же запрос даёт `200` у
> семи процессов и отказ соединения у края (§1.2, П1). То есть сценарий уже сегодня
> красный ровно в одном месте, и S1 закрывает именно его — а не «поднимает
> поверхность у половины дерева», как читалось из прежнего замера.

#### Сценарий 24: объявление сбора указывает на тот адрес, который берёт процесс

**ID:** OBS-1-24 · S3 · **уровень: по дереву**

**Given** перечень рендерящихся чартов, **выведенный** из
`deploy/helm/umbrella/Chart.yaml` (зависимости `file://`) и
`deploy/helm/umbrella/charts/` (каталоги сабчартов) — сегодня их восемь (§1.3)

**When** исполняется декларативная проверка, читающая **объявления** (значения и
шаблоны), а не отрендеренный манифест

**Then** у каждого из восьми под пода объявлены `prometheus.io/scrape: "true"`,
  `prometheus.io/port` и `prometheus.io/path: /metrics`
**And** объявленный порт равен адресу, который процесс берёт из своей конфигурации
  (умолчание либо присвоение чартом) — сверяется **источник адреса**, а не
  `containerPort`
**And** там, где рядом стоит описатель сбора, его порт и путь выводятся из тех же
  значений
**And** порт диагностики, если он объявлен в Service, объявлен на **внутреннем** Service
  того процесса — там, где отдельный внутренний Service у него есть (Р13); процесс без
  отдельного внутреннего Service проверку **не** роняет и перечисляется в её выводе
**And** проверка печатает число прочитанных чартов и число выведенных процессов

> Последнее утверждение — с самоистечением: сегодня в списке «нет отдельного
> внутреннего Service» ровно один процесс (storage, О-8). Заведут ему внутренний
> Service — он уйдёт из списка и попадёт под требование сам, без правки проверки.

> Почему сверка идёт с источником адреса, а не с `containerPort`: последний ничего не
> привязывает, и именно на нём прежний замер этого документа дал неверный ответ (§1.4).
> Утверждение «поверхность на этом порту» доказывает OBS-1-23 обращением к ней; здесь
> доказывается лишь то, что **объявление указывает туда же**.

#### Сценарий 25: процесс без объявленного сбора роняет проверку

**ID:** OBS-1-25 · S3 · **уровень: по дереву**

**Given** дерево, в котором у одного из восьми чартов снята аннотация сбора

**When** исполняется проверка сценария 24

**Then** проверка красная
**And** текст падения называет чарт
**And** после возврата аннотации проверка зелёная

> Инъекция в обе стороны. Без неё проверка неотличима от такой, которая ничего не
> читает.

#### Сценарий 26: выключенный сбор объявляется словами и истекает

**ID:** OBS-1-26 · S3 · **уровень: по дереву**

**Given** профиль развёртывания объявляет сбор у одного процесса выключенным

**When** исполняется проверка сценария 24

**Then** проверка зелёная **только** если рядом с выключением стоит причина
**And** запись о выключении, у которой в дереве больше нет предмета (процесса, к
  которому она относится), — **находка**, а не молчание
**And** на пустом перечне выключений проверка **проходит**, объявляя перепись
  («записей 0»), а не падает: пустой перечень и есть цель, ради которой он заведён

#### Сценарий 27: на поверхности нет ни данных арендатора, ни сведений о размещении

**ID:** OBS-1-27 · S3 · **уровень: на стенде + по дереву**

**Given** стенд обслужил трафик нескольких арендаторов

**When** снимается тело `GET /metrics` каждого из восьми процессов

**Then** ни одно значение метки не является идентификатором проекта, аккаунта,
  субъекта, ресурса, узла или адресом
**And** множество значений каждой метки — **закрытый словарь**, объявленный при
  регистрации семейства
**And** число различных серий в теле не растёт с числом обслуженных арендаторов

**And** свойство держится проверкой по дереву: семейство, зарегистрированное с меткой,
чьё значение берётся из данных запроса, роняет её с координатой; законный близнец —
семейство с закрытым словарём меток — не роняет.

> `security.md` §«Инфра-чувствительные данные» и потолок кардинальности — один и тот же
> запрет с двух сторон.

#### Сценарий 28: поверхность отвечает, когда соседи недоступны

**ID:** OBS-1-28 · S3 · **уровень: в процессе + по дереву**

**Given** процесс собран так, что клиент соседа отсутствует либо отвечает
недоступностью

**When** снимается экспозиция реестра

**Then** экспозиция отдаётся полностью и в пределах объявленного срока запроса
**And** все объявленные семейства присутствуют

**And** свойство держится проверкой по дереву: ни один коллектор, зарегистрированный на
диагностической поверхности, не делает внешнего вызова в момент сбора — величины
читаются только из процесса.

> Диагностика, которая ходит наружу за своими числами, гаснет ровно тогда, когда нужна.

#### Сценарий 31: объявление сбора в чарте, который не рендерится, — находка

**ID:** OBS-1-31 · S3 · **уровень: по дереву**

**Given** дерево, в котором аннотация сбора стоит в каталоге чарта, отсутствующем в
выведенном перечне рендерящихся (§1.3) — сегодня такой ровно один,
`services/iam/deploy/templates/deployment.yaml`

**When** исполняется проверка сценария 24

**Then** проверка красная
**And** текст падения называет каталог чарта и причину: «объявление сбора здесь не
  рендерится — умбрелла тянет `<другой каталог>`»
**And** законный близнец — та же аннотация в **рендерящемся** чарте — проверку **не**
  роняет
**And** после снятия аннотации из нерендерящегося чарта проверка зелёная

> Это единственный сценарий, который поймал бы ошибку самого этого документа: прежняя
> редакция §1.2 засчитала iam как «объявляет себя для сбора», измерив чарт, который
> ничего не разворачивает (§1.3). Проверка без этого утверждения зеленела бы на дереве,
> где аннотация есть у восьми каталогов — и хотя бы один из них до кластера не доезжает.
> Число здесь не важно: важно, что «объявление есть» и «объявление действует» — разные
> утверждения, и без этого сценария проверка делала бы только первое.
>
> Инъекция обязана быть в **обе** стороны: без законного близнеца проверка отличала бы
> не «рендерится / не рендерится», а просто наличие аннотации, и покраснела бы на
> исправном дереве первой же.

### Гейты дерева, общие для стадий

#### Сценарий 29: накопитель без читателя роняет гейт дерева

**ID:** OBS-1-29 · S1 + S2 + S3 · **уровень: по дереву**

**Given** дерево, в котором у одного из трёх предметов снята провязка читателя

**When** исполняется `internal/repohygiene`
`TestDeclaredAccumulatorsHaveANonTestReader`

**Then** гейт красный
**And** текст падения называет носитель и причину, зачем ему читатель
**And** законный близнец той же формы (носитель, чей читатель на месте) гейт **не**
  роняет
**And** запись таблицы, у которой в дереве больше нет конструктора, роняет гейт своей
  предпосылкой, а не молчит

#### Сценарий 30: шестой потребитель сужателя не может появиться незамеченным

**ID:** OBS-1-30 · S2 · **уровень: по дереву**

**Given** дерево, в котором заведён новый потребитель сужателя без регистрации
коллектора

**When** исполняется гейт дерева по конструктору сужателя

**Then** гейт красный
**And** текст падения называет каталог нового потребителя
**And** законный близнец — существующий потребитель с провязанной регистрацией — гейт
  **не** роняет
**And** на дереве без нарушений гейт зелёный и печатает объём осмотренного: сколько
  конструкторов сужателя найдено (сегодня — **пять** в `services/`, плюс подставные в
  оснастке проб общего фундамента, которые в предмет не входят)

---

## 6. Out-of-scope — что эта работа НЕ делает

Каждая запись — предмет с предикатом снятия, а не намерение.

**О-1. Хранилище величин: агент сбора, срок хранения, панели, правила тревог.**
В дереве их ноль (§1.2), и это отдельное продуктовое решение с собственной ценой
(ресурсы кластера, срок хранения, кто дежурит по тревоге). Настоящая работа доводит
величины **до поверхности процесса и до объявления сбора**; кто их забирает и куда
складывает — предмет преемницы. **Предикат снятия:** в дереве есть агент сбора, и
проба стенда читает величины **из него**, а не из процесса.

**О-2. Область гейта профиля поверхностей не расширяется на край.** Гейт осматривает
`services/*/cmd/*`; край поднимает свои остальные слушатели руками, и расширение
области сделало бы его красным на них — работа, не связанная с наблюдаемостью.
**Предикат снятия:** слушатели края переведены на профиль, и область гейта расширена
тем же изменением.

**О-3. Диагностический порт не приводится к одному числу.** Сегодня процессы
используют два номера: `:9101` у nlb, `:9095` у шести остальных (§1.2, П3). Нести смысл
обязана **связь** «объявленный порт сбора = адрес, который берёт процесс» (сценарий 24),
а не совпадение чисел. **Предикат снятия:** появится профиль, где номера обязаны
совпадать (общая сетевая политика по номеру порта).

**О-4. `/healthz` и `/readyz` края не переезжают на диагностическую поверхность**
(Р6). **Предикат снятия:** пути проб пода перестают быть внешними.

**О-5. Метка метода у решений края не заводится** (Р7). **Предикат снятия:** разбор
происшествия потребовал пометодной разбивки на стороне края при том, что разбивка
отвечающей стороны его не закрыла.

**О-6. Наблюдатель шва плоскости данных vpc.** Он печатает все свои величины в журнал
(§1.1) и потому не является предметом #208. Перевод его величин на поверхность
сбора — законное улучшение и **не** входит сюда. **Предикат снятия:** гейт накопителей
начинает требовать поверхности, а не журнала, — тогда предмет появится у всех носителей
разом.

**О-7. Сами осиротевшие каталоги чартов не снимаются.** `services/iam/deploy` (**7**
отслеживаемых файлов) и `services/geo/deploy` (**2**) не разворачиваются ничем (§1.3).
OBS-1 снимает из первого **аннотацию сбора** — это её предмет, — но не каталоги: они
несут форму развёртывания (порты, пробы, значения), их снятие требует сверки с
рендерящимися сабчартами `deploy/helm/umbrella/charts/kacho-iam` и
`deploy/helm/umbrella/charts/kacho-geo` и принадлежит работе о
развёртывании, а не о наблюдаемости. **Предикат снятия:** каталогов `services/iam/deploy` и `services/geo/deploy` в дереве
нет (`git ls-files services/iam/deploy services/geo/deploy | wc -l` → `0`; сегодня —
**9**). Предикат §1.3 про ссылки для снятия не годится: он уже даёт ноль, и запись,
привязанная к нему, истекла бы, не дождавшись работы. До снятия запись остаётся
открытой, и
проверка сценария 31 держит её видимой — любая новая аннотация сбора в этих каталогах
роняет её с координатой.

**О-8. У storage не заводится отдельный внутренний Service.** Его единственный Service
несёт `grpc-public`, `grpc-internal` и `metrics` разом (§1.2), поэтому правило Р13 к
нему неприменимо: перенести порт некуда, а заводить Service ради переноса значило бы
завести объявление без читателя. **Предикат снятия:** у storage появляется отдельный
внутренний Service по любой другой причине — порт диагностики переезжает на него тем же
изменением.

---

## 7. Чем свойство держится после сдачи

| Свойство | Держит | Сценарий |
|---|---|---|
| у каждого объявленного накопителя есть не-тестовый читатель | гейт дерева `TestDeclaredAccumulatorsHaveANonTestReader`, таблица предметов, инъекция в обе стороны | OBS-1-29 |
| серии присутствуют нулями, а не отсутствуют | проба в процессе + её инъекция | OBS-1-02, OBS-1-03, OBS-1-15 |
| послабление контроля видно отдельной полосой | пара «в процессе растёт» + «на стенде ноль» | OBS-1-07 ↔ OBS-1-08, OBS-1-18…OBS-1-20 ↔ OBS-1-22 |
| каждый потребитель сужателя регистрирует коллектор | гейт дерева по конструктору сужателя, инъекция в обе стороны | OBS-1-30 |
| сужение видно на настоящем тракте, а не только в пробе | сквозной сценарий на стенде | OBS-1-17 |
| объявленный сбор совпадает с адресом, который берёт процесс | декларативная проверка чартов + инъекция | OBS-1-24, OBS-1-25 |
| объявление сбора лежит там, где оно рендерится | тот же чек, перечень рендерящихся **выведен** из умбреллы; инъекция в обе стороны | OBS-1-31 |
| порт диагностики не соседствует с арендаторской поверхностью в одном Service | тот же чек, с самоистекающим списком процессов без внутреннего Service | OBS-1-24 |
| на поверхности нет данных арендатора и неограниченной кардинальности | гейт дерева по меткам семейств + чтение стенда | OBS-1-27 |
| поверхность **отвечает**, а не только объявлена, и величины доходят до читателя на живом стенде | обращение к ней изнутри кластера, перепись опрошенных процессов, третий исход отдельно от красного | OBS-1-23 |
| выключение сбора объявлено словами и истекает | причина в журнале и в самоотчёте, самоистечение записи | OBS-1-14, OBS-1-26 |
| публичная поверхность края серий не отдаёт | сквозной отрицательный кейс + проверка провязки | OBS-1-13 |
| диагностика не зависит от соседей | проба в процессе + гейт «коллектор не ходит наружу» | OBS-1-28 |

**Чего эти проверки НЕ держат, и это названо честно:** ни одна из них не утверждает,
что кто-то величины **читает регулярно**. Это предмет О-1, и пока он открыт, «ноль
доставленных величин за всю жизнь поверхности» остаётся заметным только тому, кто
пришёл смотреть.

---

## 8. Порядок работ

1. `class-exposure-analyst` — по этому документу, до первой строки кода.
2. `superpowers:writing-plans` — план по стадиям S1 → S2 → S3.
3. `integration-tester` — красные пробы по сценариям, до кода (ban #12).
4. `rpc-implementer` — S1, затем S2, затем S3; каждая стадия сдаётся своим PR в
   релизную ветку.
5. Ревью: `go-style-reviewer` (код), `system-design-reviewer` (S2 — общий коллектор на
   пять потребителей), `landing-reviewer` (перед каждой посадкой).
6. `proto-api-reviewer` — **не зовётся**: контракт не затронут.
7. `db-architect-reviewer` — **не зовётся**: миграций нет.
8. Заказчик — финальный сквозной прогон на поднятом стенде (S3, сценарии 23 и 27).
