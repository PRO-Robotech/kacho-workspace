---
title: "deploy/helm/umbrella — зонт и семь цепочек профилей"
aliases:
  - umbrella chart
  - зонт развёртывания
repo: kacho-deploy
layer: chart
path: deploy/helm/umbrella
category: packages
status: stable
tags:
  - packages
  - kacho-deploy
  - config
verified_against: "Перемер 2026-09-26 на PRO-Robotech/kacho@7190c3e5274 (коммит слияния волны 2796 в ветку эпика 2564): цепочки — deploy/stacks.txt, посадка identityProvider обеих половин — слиянием YAML values.yaml и файлов каждой цепочки (рендеров helm не гонялось), содержимое values.own.yaml — git show. Прочие ручки таблицы — по записи 2026-09-22: ствол bec320cf47d и ветка issue-2735-loginlane-prereq @6e222f9c1b9"
---

# deploy/helm/umbrella — зонт: один чарт, семь цепочек, объявления по корням

Зонт развёртывания платформы. Состав **стендов** объявляет не он, а `deploy/stacks.txt` —
единственное место в дереве, где цепочка `-f` выписана; зонт даёт профили, из которых цепочки
складываются. Цепочек **семь**, и они накладываются слева направо, ровно как их получает helm:

| стенд | цепочка профилей |
|---|---|
| `dev` | `values.dev.yaml` |
| `dev-prod` | `values.dev.yaml` + `values.dev-prod.yaml` |
| `prod` | `values.prod.yaml` |
| `own` | `values.prod.yaml` + `values.own.yaml` + `values.own-stand.yaml` (перемер 2026-09-26: так и в стволе, и на голове эпика) |
| `fe3455` | `values.prod.yaml` + три накладки `fe3455*` |
| `prorobotech` | `values.dev.yaml` + `values.dev-prod.yaml` + `values.prorobotech.yaml` |
| `a8f60d` | `values.dev.yaml` + `values.dev-prod.yaml` + `values.a8f60d.yaml` |

Рендерные гейты зонта и их контракт исходов — у соседа, [[packages/kacho-deploy-tests-helm]];
здесь они не пересказываются. Гейты **края** о тех же стендах — [[packages/kacho-gateway-deploy]].

## Посадка личности: было 1 из 7, стало 7 из 7

Посадка `own` объявляется **двумя половинами** — службе доступа и краю, — и расхождение половин
есть стенд, у которого о личности решают по-разному. Покрытие по цепочкам (предикат — в
`verified_against`):

| ручка | ствол `bec320cf47d` | ветка `issue-2735-loginlane-prereq` (координата, не живая ссылка) @`6e222f9c1b9` |
|---|---:|---:|
| посадка `identityProvider` (обе половины) | **1 из 7** (`own`) | **7 из 7** |
| `api-gateway.authn.iamLoginLaneUrl` | **1 из 7** | **7 из 7** |
| `kaname.ports.loginLane` | **3 из 7** | **7 из 7** |
| `kaname.mtls.loginLane` — транспорт слушателя | **6 из 7** | **7 из 7** |
| `selfServiceFreshness` — окно свежести самообслуживания | **1 из 7** | **7 из 7** |

Перемер 2026-09-26: на голове эпика `7190c3e5274` (волна [[KAC/issue-2796|kacho#2796]], задача
[[KAC/issue-2735|kacho#2735]]) посадка `identityProvider` = `own` у обеих половин —
`kaname.config.authn.identityProvider` и `api-gateway.authn.identityProvider` — на **всех семи**
цепочках; объявления стоят в корнях `values.dev.yaml` и `values.prod.yaml`. Остальные четыре
строки таблицы на этой ревизии не перемерялись. В `main` переезда нет.

Ветка на origin была, в ствол **не влита** (база — сам `bec320cf47d`, сверху три коммита).
**В рабочей копии продукта её состояния нет**: `project/kacho` стоит на `main`, и правый
столбец таблицы там не воспроизводится — он снят на самой ветке, а не на дереве, где
записка писалась.

**Почему величины полосы стоят в корнях цепочек, а не в накладке посадки.** Под `external` страж
края их не читает вовсе, и объявление ничего не включает; смысл заблаговременного объявления
ровно один — перевод стенда на `own` **не заводит полосу с нуля**. Пока окно свежести стояло
только в накладке, перевод любого другого стенда ронял старт службы по имени этой ручки.

## `values.own.yaml` опустела — и это исход, а не недоделка

Накладка несла ровно три объявления (посадка службы, посадка края, адрес полосы формы). Все три
переехали в корни обеих цепочек, **копий не оставлено намеренно**: два места об одном предмете
разошлись бы молча, и стенд `own` исполнял бы своё объявление, а шесть остальных — чужое. После
переезда файл содержал только пояснение, почему он пуст. На голове эпика `7190c3e5274` в нём
одно объявление — `uif.enabled: true` (консоль на стенде `own`); объявлений посадки в нём
по-прежнему нет.

Сам стенд `own` при этом **не снимается**: состав стендов объявляет таблица, на имя `own`
ссылаются рецепты подъёма и приёмка, и решение об инвентаре — не следствие правки посадки. Это
находка со своим предикатом снятия: стек убирается из таблицы вместе со снятием файла и правкой
ссылок на имя.

## Затронутые сущности vault

- [[edges/api-gateway-to-kaname-login-lane]] — ребро, ради которого эти ручки и объявляются.
- [[edges/ui-future-deploy-to-umbrella]] — как консоль попадает в зонт подчартом.
- [[KAC/issue-2777]] — снятие чужой консоли личности: три координаты одним изменением.
- [[KAC/wave-identity-own-w0-2026-09-22]] — волна линии; на её голове этот переезд ещё не был сведён.
- [[KAC/issue-2735]] · [[KAC/issue-2796]] — задача и волна, которыми переезд влит в ветку эпика `2564`.

## History

- 2026-09-22 — записка заведена по возврату полосы: семь стендов из семи переведены на посадку
  `own`, накладка опустела. Числа перемерены мной на обеих ревизиях; номер задачи — `kacho#2735`.
- 2026-09-26 — переезд влит в ветку эпика `2564` волной #2796 (PR #2869, `7190c3e5274`); перемерена
  посадка по семи цепочкам. Исправлена таблица цепочек: у `own` три файла — `values.own-stand.yaml`
  есть в `deploy/stacks.txt` и ствола `1d42a6728bf`, и головы эпика, прежняя редакция его не называла.
  `values.own.yaml` на голове эпика несёт `uif.enabled: true`. В `main` переезда нет.

#packages #kacho-deploy #config
