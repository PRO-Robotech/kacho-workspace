---
title: "Внешняя федерация личностей — что уже построено и что решено"
aliases:
  - external subject federation
  - федерация внешних субъектов
category: docs
status: active
tags:
  - kaname
  - iam
  - federation
  - decision
---

# Внешняя федерация личностей: разбор идеи и принятые решения

Замер четырьмя осями, 2026-09-15. Ревизии: служба `kaname@b41f8f98`, платформа
`kacho@3eeeef0a`, фундамент `corelib@fef214cd`.

## Главное: механизм уже построен на три четверти

Идея звучала как «расширить службу поддержкой внешних субъектов, которые у нас не
создаются и не синхронизируются». **Это уже верно сегодня.** В дереве есть вид
удостоверения федеративного доверия, таблица доверенных издателей, чья ключевая пара —
именно `(издатель, субъект)`, проверка чужой подписи на пути запроса с тридцатью
четырьмя именованными исходами, полоса выдачи по внешнему подписанному утверждению и
страница документации. Пример из идеи владельца стоит в контракте **дословно** — вплоть
до формы имени служебной учётки кластера.

**Расхождение ровно одно, и в нём весь предмет разбора:** сегодня внешний субъект
разрешается в **нашу** служебную учётку и получает токен **от её имени**. Идея
предлагает сделать его самостоятельным принципалом.

> [!important] Формулировка выигрыша меняется, иначе спорить не о чем
> Не «внешний субъект без создания в службе» — так уже есть. А **«внешний субъект как
> принципал, а не заимствование нашей учётки»**. Судить надо на неё.

## Что подтвердилось из уточнения владельца

Владелец назвал четыре свойства. Три из них — **уже свойства дерева**:

| свойство | состояние |
|---|---|
| доверять внешнему поставщику | есть: ключ издателя **называется при выдаче**, а не тянется по адресу — исходящих обращений у полосы ноль |
| выдавать доступ конкретному субъекту | есть: пара — первичный ключ таблицы, с тем же доводом о детерминированности |
| не регистрировать внешние учётки у себя | есть: строк не заводится ни одной |
| доступ держит наша сторона, пока сама не снимет | **есть, и это же названо риском** — см. ниже |

## Решения

### Р1. Единица доверия — ИМЯ, и это объявляется контрактом, а не подразумевается

Уточнение владельца «если на той стороне удалили субъект, доступ остаётся, пока не
удалят с нашей стороны» принимается: отзыв на чужой стороне нас не касается, и это
законное свойство.

**Но оно покрывает не весь случай.** Если на той стороне субъект не удалён, а
**пересоздан под тем же именем**, доступ достаётся уже **другому** экземпляру. Это не
«доступ остался», это «доступ перешёл», и наша сторона не наблюдает ничего.

Требовать неизменяемый идентификатор экземпляра **отвергнуто**: у служебной учётки
кластера он есть, у распространённых систем сборки — нет, и требование отрезало бы
большинство случаев, ради которых механизм и заводится.

**Отсюда:** имя остаётся единицей доверия, и контракт **говорит это арендатору прямо**.
Взамен запись доверия получает **обязательный срок** — сегодня он необязателен.

### Р2. Внешний субъект становится ЧЛЕНОМ ГРУППЫ, а не четвёртым видом субъекта привязки

Цена измерена и различается на два порядка:

| исход | цена |
|---|---|
| четвёртый вид субъекта привязки | 108 объявлений глаголов модели (107 из них несут один и тот же состав) + новый тип + новая миграция на два закрытых словаря |
| **член группы** | **одна строка** модели; ни один глагол не трогается; одна миграция на словарь членства |

Второй исход **предписан самим корпусом**: право, которое получает больше одного
принципала или чей состав может измениться, выдаётся **группе**. Тогда привязки
остаются к группе, а внешний субъект в них не участвует вовсе.

Отдельно это снимает названное риском: пока внешний субъект не может быть членом
группы, право ему выдаётся **перечислением** — прямо против той же нормы, — и снятие
одного получателя стоит снятия кортежей по всем объектам роли.

### Р3. Порядок: сперва ОПЫТ, потом проектирование

Два вопроса меняют объём работы, и ни на один нельзя ответить чтением:

1. **работает ли полоса обмена на поставляемом профиле.** Тракт выдачи по внешнему
   утверждению на поставляемом чарте не смонтирован. Построенное поверх унаследует
   «выдача проходит успехом, обменять негде» и будет отлаживаться как дефект новой
   работы;
2. **переживает ли однократность файл проецируемого токена.** Наша полоса требует
   уникального идентификатора утверждения и гасит его однократно. Нагрузка, читающая
   токен из файла, получит из него **один** токен. Несёт ли проецируемый токен такой
   идентификатор и как часто меняется файл — факт о чужой системе, в этом дереве его
   нет ни строкой.

Исход второго опыта «повторный обмен отвергнут» означает, что первая реализация
неисполнима в своём **главном** сценарии, и решать это надо контрактом, а не подпоркой.

## Что отвергнуто по пяти вопросам — и чем закрывается

| вопрос | исход | чем закрывается |
|---|---|---|
| благо продукта | принят | убирает промежуточный объект, которого арендатор не просил: три шага → два |
| конкурентоспособность | принят | предсказуемость контракта; ответ «кто имеет доступ» перестаёт требовать знания, что за учёткой стоит чужой субъект |
| перспективность | **отвергался** | закрыт Р1: имя объявлено единицей доверия, срок записи стал обязательным |
| безопасность | **отвергался** | расширение было названо не полностью: чей домен у поставщика и кто вправе его завести · членство в группе · след в аудите. Индекс под класс событий федерации существует, **производителей ноль** |
| производственность | **отвергался** | перечень доверия сегодня неправим — глагола правки нет, снятие одного субъекта равно отзыву ключа целиком |

## Прецеденты — идея уже рассматривалась дважды

**Первый.** Отдельный вид субъекта существовал в модели прав и был **снят**
одобренной приёмкой после шести кругов ревью. Из четырёх её оснований идея снимает
первое (не было производителя — теперь есть) и **атакует посылку третьего**: та
исходила из того, что внешняя личность материализуется строкой пользователя, а идея
прямо говорит — не создаётся и не синхронизируется. Значит прецедент вопроса не
закрывает, но требует ответа именно по этому пункту, и Р2 его даёт.

**Второй.** Тяжёлая форма федерации — поставщик как первоклассный ресурс, отдельная
служба обмена, чтение набора ключей издателя по адресу — была построена и **отозвана**
в пользу нынешней лёгкой. Возврат к ней означал бы отмену решения, принятого с
доводом: адрес означал бы обращение к постороннему хосту, выбранному арендатором, из
процесса, принимающего решение о доступе.

**Третий, на краю платформы.** Полоса внешнего принципала существовала в вырожденной
форме и снята с прямой записью причины: такого вида субъекта в модели прав нет вовсе.
То есть предупреждение о главном препятствии уже стоит в дереве, написанное тем, кто на
него наткнулся.

## Связи

[[rpc/iam-sa-key-service]] · [[resources/iam-access-binding]] ·
[[lessons/checks-with-form-but-no-substance]] ·
[[lessons/required-check-without-a-producer-blocks-merge-forever]]
