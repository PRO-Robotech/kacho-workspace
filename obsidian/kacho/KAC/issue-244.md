---
title: "issue-244 — vpc: production-полнота модуля сети, волны 0-5"
tags: [kac, vpc, dataplane, authz, api-conventions, data-integrity]
status: in-progress
type: epic
ticket_id: "244"
issue_url: https://github.com/PRO-Robotech/kacho/issues/244
opened: 2026-08-13
repos: [kacho (agent/vpc-module-delta от release/2026-08-12), kacho-workspace]
prs: []
category: kac
---

# issue-244 — vpc: production-полнота модуля сети, волны 0-5

**Status:** in-progress. **Эпик:** [#244](https://github.com/PRO-Robotech/kacho/issues/244).
**Дочерние:** #245 (волна 0) · #246 (волна 1, ломающий релиз) · #247 (волна 2) · #248 (волна 3, шов
датаплейна) · #249 (волна 4-5) · #250 (величины).
**Ветка:** `agent/vpc-module-delta`, база `origin/release/2026-08-12` (`6637b186`).

## Что и зачем

Модуль сети приводится к production-форме целиком по четырём документам плана
(`docs/plans/vpc-module-delta.md` — список задач волнами; `vpc-nlb-production-module-plan.md` —
методы, JSON, границы, схема БД, §8.6 величины, §9 спецификация новых методов;
`vpc-nlb-dataplane-architecture.md` — выбор датапаса и требования к исполнителю;
`vpc-nlb-methods-matrix.md` — инвентарь). Объём — 52 задачи, выведены аудитом по двенадцати осям
плюс отдельной переписью величин.

## Основание, добытое до кода

- **Пин документов остаётся верным.** `aa6fa15f` отстал от точки интеграции всего на 6 коммитов, и
  ни один из них не тронул `services/vpc` или контракты vpc — значит все координаты §9 и списка
  действительны на `6637b186`.
- **Базовая линия сделана зелёной осознанно.** Гейт хедеров лицензии был красным на точке
  интеграции (один скрипт развёртывания без двух строк шапки, внесён чужим коммитом). Починено
  отдельным коммитом: на красной базе своя поломка неотличима от чужой.
- **Репозиторий имеет ~28 рабочих копий и несколько параллельных писателей**; `release/2026-08-12`
  занята другой сессией, общая копия продукта — в отсоединённой голове и двигается. Поэтому работа
  идёт в **своём** worktree, а агентам запрещены любые git-операции: индекс общий.

## Решения владельца, принятые в ходе планирования

1. **Каскад при удалении сети ОСТАЁТСЯ** для двух системных дочерних — отменяет директиву того же
   дня: снятие само создаёт окно, в котором сеть жива без группы по умолчанию, и роняет утверждённую
   приёмку VPC-1-19. Осталась одна правка — отказ перечисляет мешающее.
2. **Обещание верхней границы подсети остаётся** и начинает исполняться.
3. **Ревизия намерения = номер события доставки**, монотонный в пределах ресурса.
4. **Предел соединений на интерфейс — 10 000**, плотность зафиксирована 64 интерфейса на узел.
5. **Три метода под-перечисления сети снимаются** — тот же критерий, что у двух методов адреса.

## Затронутые сущности vault

Существующие: [[vpc-network]] · [[vpc-subnet]] · [[vpc-address]] · [[vpc-securitygroup]] ·
[[vpc-networkinterface]] · [[vpc-routetable]] · [[vpc-gateway]] · [[vpc-addresspool]] ·
[[vpc-network-service]] · [[vpc-address-service]] · [[vpc-gateway-service]] ·
[[vpc-internal-network-service]] · [[vpc-internal-watch-service]] · [[corelib-grpcsrv]] ·
[[corelib-validate]] · [[checks-with-form-but-no-substance]] · [[apigw-to-vpc]]

**Заводятся этой работой** (ссылок пока не ставлю — висячая ссылка это находка гейта, а не заявка):
записка ресурса `CidrGroup`, записка потока жизненного цикла, ребро «vpc → исполнитель датаплейна».
Они появятся вместе с волнами 3-5, и тогда же будут связаны отсюда.

## DoD

- [x] Волна 0 — предусловия, внутренние и текстовые правки (закрыта задачей 25)
- [x] Волна 1 — один ломающий релиз, порядок коммитов задан (20 коммитов)
- [x] Волна 1 — **консоль сверена** с изменившимся контрактом
- [x] Волна 2 — аддитивно: 11, 17, 18, 43, 44 + 56
- [x] Волна 3 — шов датаплейна: 28, 29, 30, 31, 52 (осталась 32, идёт с величинами)
- [x] Волна 4-5 — возможности и ресурс `CidrGroup` (33, 34, 40, 47)
- [x] Величины — полоса, темп, потолки, ограничение темпа запросов (46, 48, 49, 50)
- [ ] Сборка, `go vet`, гейты дерева, e2e зелёные; ни одной задачи «частично»
- [ ] Поправки внесены во все четыре документа плана
- [ ] Влито в накопительную релизную ветку одним MR

## Консоль — шаг каждой волны (требование владельца 2026-08-12)

Волна не закрыта, пока консоль не сверена с контрактом: она меняет контракт, край и консоль
одновременно, и цена расхождения **несимметрична**. Край разбирает тело строго, поэтому
снятое на сервере ломает консоль сразу и молча для автора правки, а добавленное на сервере
без консоли выглядит как невыполненное обещание. Норма, признак нарушения и чем держится —
`docs/plans/vpc-module-delta.md` §10; здесь не пересказывается.

Что дала сверка на волне 1: восемь снятых методов консоль не звала **ни одного**; форма
подсети уже была верна; **один живой дефект** — предлагалась снятая цель правила, снята
сужением типа (скрыть из списка выбора было бы недостаточно: скрытое всё ещё представимо и
приезжает из сохранённого ресурса); пять устаревших примеров в комментариях переведены на
живую координату.

**Найдено сверкой, закрывается волной 2:** снятая ветвь цели **пережила свои строки** —
домен её сериализовал в JSONB, и сохранённое правило проецируется без цели, то есть не
разрешает ничего, чего вызывающий не писал, а группу с такой строкой нельзя обновить вовсе.
Урок общего вида: снятие поля с контракта — **две** работы, и вторая (привести строки) не
видна ни в диффе контракта, ни в прогоне.

Второй предмет той же сверки — вкладка дочернего ресурса выдавала **первую страницу** за
весь список (клиентское сужение поверх курсорной страницы), тогда как страница списка в той
же консоли несёт комментарий, запрещающий ровно это. Два места об одном предмете, верно
одно. Сужение переносится на сервер там, где владелец принимает поле фильтром (это дала
волна 1), а где не принимает — продолжение показывается явно, чтобы обрезание не было
молчаливым. Остаток — задача 56 плана (сужение списка адресов по подсети на сервере).

## Число открытых задач было названо неверно — поправка 2026-08-13

Я дважды назвал остаток «12 задач», перечислив среди них 35, 36, 37, 38, 39. Это число
получено **по неверному предикату**: заголовок раздела плана «Задачи 34…39 — без изменений
по существу» перечисляет **строки исходного аудита**, а не номера задач дельты. По таблице
соответствия: строка 36 → задача 9 (сделана), 38 → задача 43 (сделана), 39 → снятие трёх
методов (сделано волной 1), причём сам план говорит о 39 «не задача, а решение — в счёте не
считать»; **строки 35 в плане не существует вовсе** (предикат: `grep -c '^| 35 |'` → 0).

Верный остаток на момент поправки — **девять**: 32, 33, 34, 40, 46, 47, 48, 49, 50.

Урок, ради которого это записано: два документа нумеруют одну работу по-разному, и таблица
соответствия между ними существует именно поэтому. Считать остаток по заголовку одного
документа, читая номера как принадлежащие другому, — способ получить число, которое
выглядит измеренным. Связано: [[a-declared-predicate-can-be-wrong-and-then-it-lies-confidently]].

## Прогон анализаторов вскрыл ложную норму про модули (2026-08-13)

`gosec`, запущенный из каталога провайдера, напечатал `Files: 0` — и это читалось как «ноль
находок», означая «ноль прочитанного». Проверка, почему, вскрыла предмет крупнее: правило
`polyrepo.md` объявляло провайдер **отдельным Go-модулем «(факт)»** и ссылалось на гейт
изоляции с шестью условиями. Ни того, ни другого в дереве нет — предикат прогнан с контролем
в обе стороны (тот же запрос находит восемь существующих гейтов рядом).

Практическая цена лжи была не в самом факте: норма предписывала провайдеру **отдельную
джобу** из пяти команд, «потому что корневой `./...` во вложенный модуль не спускается».
Спускается — корневой прогон читает 31 файл провайдера. Поверивший норме завёл бы лишнюю
джобу либо счёл бы провайдер непроверенным и добавил подавления «его всё равно никто не
смотрит». Норма перемерена и исправлена; она грузится `@import`-ом в каждую сессию.

## MR и стенд

MR — `PRO-Robotech/kacho#287` в `release/2026-08-12` (имя релизной ветки датой правилами
запрещено; в теле MR это названо, переименование — решение владельца).

Сквозные пробы гонятся на **своём** кластере `kacho-vpc`: соседний стенд поднят двое суток
и принадлежит другому сеансу, а стенд терпит одного писателя. Публикация порта Ingress из
конфигурации снята — порт занят соседом, а пути проб через него не идут (это сказано
предпусковой проверкой самого стенда).

## Состояние на конец сессии 2026-08-13/14

Ветка `agent/vpc-module-delta` — 42 коммита, MR `PRO-Robotech/kacho#287` в
`release/2026-08-12`. План исполнен целиком; сверх плана закрыты три долга (внешний адрес
шлюза, серверная половина видимости применения, страницы документации двух ресурсов).

**Зелёное и перепроверенное мной:** сборка продукта и провайдера · `golangci-lint` по
дереву — 0 · `gosec` запиненной версией вызовом конвейера — 1286 файлов, 0 уровня
вердикта · `govulncheck` — 0 и 0 · адъюдикация разрывов 25/25 · гейты дерева · миграции на
чистой базе · пробы сервисов и края · консоль.

**НЕ зелёное и названное:** сквозные пробы — 5 падений из 7292, все в новых кейсах
внешнего адреса, воспроизводятся. Предмет — `kacho#294`. Кейсы не ослаблены: толерантность
к пустой ветви сделала бы пробу зелёной ровно на дефекте.

**Долги сессии с предметом в трекере:** `kacho#285` (пин генераторов), `#290`
(conformance исполнителя), `#291` (квоты — блокирует решение владельца), `#292`
(конфигурации terraform не проверяются исполнением).

**Закрылось измерением, а не работой:** ограничение «трансляция ⇔ есть адрес» выглядело
непроверенным на старых строках. Проверка ствола показала, что значение «трансляция»
появилось только в этой ветке, значит на чистом развёртывании таких строк нет by
construction — ограничение проверяется само. Записывать это долгом было бы ложью в
обратную сторону. Связано: [[a-declared-predicate-can-be-wrong-and-then-it-lies-confidently]].

## Связанные задачи

Ветка провайдера `agent/terraform-iam-nlb` держит файл спеки, из которого волна 0 снимает два поля:
окно закрывает **не наша** работа, поэтому задача 19 идёт первой.
