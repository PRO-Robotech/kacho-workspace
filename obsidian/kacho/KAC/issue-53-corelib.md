---
title: "corelib#53: держатель выреза телеметрии по путям движка"
aliases:
  - issue-53-corelib
ticket_id: 53
category: kac
status: to-do
type: feature
repos:
  - corelib
areas: []
prs: []
issue_url: https://github.com/PRO-Robotech/corelib/issues/53
opened: 2026-09-23
tags:
  - kac
  - feature
  - kacho-corelib
verified_against: "PRO-Robotech/corelib: коммит слияния волны 227ed2b77b4 в ветку эпика 26 — работы этой задачи в нём нет (тело PR #62, раздел «вне запроса»; `git ls-remote --heads origin 53`), 2026-09-26"
---

# corelib#53: держатель выреза телеметрии по путям движка

**Состояние на момент записи**: `to-do` — 2026-09-26. Задача **открыта**, метки: `enhancement`, `size:M`, `area:iam`, `release:identity-own`, `P2`. Она sub-issue волны [[KAC/issue-32-corelib|#32]], но в запрос волны PR #62 (влит в ветку эпика `26`, координата, не живая ссылка) **не вошла**: тело запроса называет её в разделе «вне запроса». Работы задачи ни в ветке волны, ни в `main` нет.

## Что и зачем

Предмет — в задаче; сюда не пересказывается, пока работы нет. Задача заведена находкой ревью волны и вынесена запросом волны за его пределы.

## DoD

Из тела задачи, раздел «ПРЕДИКАТ.» (перечислено как есть, мной не перемерялось):

- `git ls-tree -r --name-only origin/32 -- internal/tracecut | wc -l` > 0.
- `go test -count=1 ./internal/tracecut/...` против текущего `internal/otelx` даёт код 0; `-race` в контейнере `golang:1.26.8` тоже даёт код 0.
- Инъекция: открытие спана в поддереве, закрытое не обёрткой фундамента, даёт красное с координатой, законный близнец даёт зелёное; пустой обход поддерева даёт красное.
- Разбор идентификаторов `treehygiene` по `internal/tracecut`: находок 0.

Сверено мной:

- [ ] работа задачи в ветке волны — нет: запрос волны влит без неё;
- [ ] задача переведена в другую волну — нет: на 2026-09-26 она sub-issue #32.

## Затронутые сущности vault

Поля «затронуто в vault» в возвратах исполнителей по этой задаче нет.

- [[KAC/issue-32-corelib]] — волна.

#kac #feature #kacho-corelib
