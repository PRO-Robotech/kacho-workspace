---
title: KAC tickets — trail
aliases:
  - KAC index
  - KAC tickets
category: hub
tags:
  - kac
  - index
---

# KAC — журнал работ

Записка на единицу работы: тикет, подфаза, эпик, разбор инцидента, отчёт аудит-раунда.
Требование вести журнал — `.claude/rules/vault.md`; шаблон — [[_TEMPLATE]].

> [!important] Живой перечень — в виде, а не в этом файле
> [[all-tickets|KAC/all-tickets.base]] строится **из frontmatter записок** и не может
> разойтись с ними. Вид «Active» показывает то, по чему кто-то обязан двигаться.
> Рукописный список «текущих активных тикетов», стоявший здесь прежде, разошёлся:
> он объявлял `to-do` семь позиций, из которых две к тому моменту были `done`, три —
> `test`, и назывались они через систему входа, замещённую ещё в мае. Список из двух
> источников об одном предмете снят намеренно; ведите статус в записке.

## Статусы и что они значат

| Статус | Значение | Попадает в «Active» |
|---|---|---|
| `in-progress` | работа идёт сейчас | да |
| `test` | сделано, ждёт проверки | да |
| `to-do` / `planned` | заведено, не начато | да |
| `done` | доехало — **проверено по дереву продукта**, а не по списку пунктов | нет |
| `wontfix` | отказались, с обоснованием | нет |
| `superseded` | предмет поглощён другой работой или снят с продукта решением | нет |
| `reference` | отчёт раунда / методика: единицей работы не была, закрывать нечего | нет |

Заводя новый статус — впиши его в `all-tickets.base` в **оба** места (формулу значка и
фильтр «Active»). Иначе вид молча начнёт называть закрытое открытым: неизвестное значение
проходит фильтр, потому что тот исключает статусы **поимённо**.

## Как закрывать записку честно

**Статус приводится к дереву продукта, а не к списку пунктов внутри записки.** Пункт
«PR смёржен в `kacho-<repo>`» сегодня непроверяем — таких репозиториев как единиц работы
больше нет (разработка в одном монорепо, см. [[../architecture]]). Проверяется другое:
существует ли в дереве то, что записка обещала, — контракт, таблица, ветка кода, гейт.

## Волна сверки 2026-08-05 — что измерено

Единица счёта — **отслеживаемый файл `.md`** в этом каталоге; ревизия продукта, с которой
сверялись, — `PRO-Robotech/kacho@96b2879a` (ствол `redesign/integration` — её предок).

- Отслеживается git в каталоге **204** файла: **203** записки `.md` + вид `all-tickets.base`.
  Поле `status` несут **164** записки; у остальных 39 его нет — это отдельный, не тронутый
  волной вопрос (без `status` записка в виде «Active» не появляется вовсе).
- Со статусом `in-progress` было **45**, из них один — шаблон. Оставшиеся **44** разобраны
  **по одной**, каждая со своим доказательством по дереву; результат: **27** → `done`,
  **7** → `reference`, **5** → `superseded`, **5** остались `in-progress` (подтверждено, а
  не унаследовано).
- Открытым по существу оказался ровно **один** предмет из прошлого:
  [[geo-baseline-greenfield-seed-gap]] — базовый каталог размещения на чистом стенде не
  засевается, и вдобавок комментарий, объясняющий, откуда он берётся, называет
  производителя, которого в дереве нет.
- Ещё два статуса (`landed`, `active`) вид прочитать не мог — приведены к `done` и
  `in-progress` соответственно.

> [!warning] Открытый долг с числом: 42 записки со статусом `test` не сверены
> «Ноль находок» обязано быть отличимо от «ноль прочитанного», поэтому: этих сорока двух
> волна **не касалась**. Форма долга та же — статус «сделано, ждёт проверки» держится у
> **40 из 42** дольше, чем существует монорепо (последняя правка файла — до 2026-07-15),
> то есть он пережил не только свою проверку, но и топологию, в которой заводился.
> Сверять их придётся так же — по одной, с доказательством по дереву; массовая замена
> статуса без сверки превратила бы устаревшую запись в уверенно неверную.

## Закрытые эпики, которые полезно знать

- [[KAC-2]] — NetworkInterface как самостоятельный ресурс, отдельный от инстанса.
- [[KAC-50]] — разделение слушателей края (публичный / cluster-internal).
- [[KAC-52]] — гонка привязки интерфейса, снятая атомарным CAS (канонический пример
  запрета «прочитал → проверил → записал»).
- [[KAC-55]] — ограничение мощности v4/v6 у интерфейса на уровне БД.
- [[KAC-56]] — автопривязка таблицы маршрутов к подсети триггером.
- [[KAC-71]] — разделение пула адресов на v4/v6.
- [[KAC-94]] — доведение vpc до канонического Go-стиля (скил `evgeniy`).
- [[KAC-111]] — схлопывание миграций vpc в единую базовую.
- [[KAC-124]] — снятие отдельного менеджера ресурсов; иерархия стала двухуровневой.

> [!note] Один пункт этого списка с тех пор **развернули обратно**
> KAC-15 переносил ось размещения (Region/Zone) из vpc в compute. Сегодня её владелец —
> **`geo`** (отдельный домен, leaf-owner), а рёбра «в compute ради географии» удалены как
> ложные. Записка [[KAC-15]] верна как история и неверна как описание сегодняшнего дня —
> ровно тот случай, ради которого статус и координаты сверяются с деревом.

#kac #index
