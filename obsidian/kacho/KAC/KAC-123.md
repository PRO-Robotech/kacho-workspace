# KAC-123: Group default-deny + UI AccessBindings visibility + AccountCrumb fix

**Status**: test
**Type**: fix
**Repos**: kacho-iam, kacho-api-gateway, kacho-ui
**PRs**: (TBD — branch KAC-123 local)
**YT**: https://prorobotech.youtrack.cloud/issue/KAC-123

## Что и зачем

Итерация 4 ролевой модели — закрытие 5 разрывов, найденных user'ом в админ-флоу:

1. **Group handler не имел Check** — anonymous видел Group через `/iam/v1/groups`. Default-deny через Keto: `viewer` на `account:<id>` для Get/List/ListMembers; `admin` для Create/Update/Delete/AddMember/RemoveMember. List без `account_id` → empty (если не system-admin).
2. **UI не отправлял `/iam/v1/accessBindings:listBySubject`** для текущего user'а — AccessBindingsPage по умолчанию открывалась в режиме `byResource` и `resId=""`. Добавлен top-блок **«Мои AccessBinding'и»** с auto-query через `useAuth()`.
3. **AccountCrumb / ProjectCrumb селекторы в шапке не работали** — `DropdownRow` использовал raw `<button>` внутри `DropdownMenu.Content` (Radix не закрывает меню → клик визуально «не срабатывал»). Заменено на `DropdownMenu.Item` с `onSelect`+`preventDefault`. Дополнительно: ProjectCrumb передавал `?accountId=...` (camelCase), backend ожидает `?account_id=...` — пофикшен query param.
4. **Admin (@prorobotech.ru) не видел «Регионы/Пулы/Зоны»** — `permissions=["*","admin"]` для system-admin резолвится в api-gateway через `WithAdminChecker` → `IAMSubjectClient.IsSystemAdmin` → Keto `Check(user:<id>, admin, kacho_system:root)`. UI читает `hasPermission('*')` через `useAuth()`. Backend код был готов после предыдущей итерации, требовался деплой.
5. (deferred → KAC-124) Полное удаление `kacho-resource-manager` — большой рефакторинг с acceptance-doc первым.

## Затронутые сущности vault

- [[../resources/iam-group]] — default-deny через Keto Check
- [[../resources/iam-access-binding]] — ListBySubject default-deny (user видит только свои)
- [[../packages/iam-apps-kacho-api-group]] — AuthzChecker port + WithAuthz
- [[../packages/api-gateway-middleware]] — AdminChecker.IsSystemAdmin
- [[../rpc/iam-group-service]] — auth-matrix per RPC
- [[../rpc/iam-access-binding-service]] — ListBySubject auth-rules
- [[KAC-119]] (role-model foundation — Keto Check)
- [[KAC-121]] (YC-style iteration 2)
- [[KAC-122]] (role catalog v2 — naming + IDs)

## Acceptance / Definition of Done

- [x] kacho-iam: Group handler с WithAuthz wiring в main.go
- [x] kacho-ui: AccessBindingsPage «Мои AccessBinding'и» блок с auto-query
- [x] kacho-ui: BreadcrumbSelector DropdownRow → DropdownMenu.Item + snake_case query
- [x] kacho-api-gateway: AdminChecker wired для /auth/me
- [x] Все три образа собраны и задеплоены на e2c825
- [ ] Integration tests для Group default-deny (KAC-123 follow-up)
- [ ] Newman cases для AB ListBySubject default-deny (KAC-123 follow-up)
- [ ] Playwright проверка в браузере — выбор Account/Project работает (manual smoke первого пользователя)
- [ ] PR в каждый из 3 репо

## Связанные тикеты

- [[KAC-119]] (default-deny foundation)
- [[KAC-121]] (YC-style iter 2)
- [[KAC-122]] (role catalog v2)
- KAC-124 (TBD) — kacho-resource-manager removal

#kac #fix
