---
title: "KAC-198: Phase 4 — drop JIT + simplify GDPR (keep break-glass)"
category: kac
ticket_id: KAC-198
status: superseded
type: refactor
tags:
  - kac
---

# KAC-198: Phase 4 — drop JIT + simplify GDPR (keep break-glass)

> [!note] Строка «Где ведётся» переписана 2026-08-05: это адрес РАБОТЫ, а не история
> Здесь стояли имена репозиториев полирепо-эпохи. По незакрытой записке эта строка читается
> как указание, куда идти, — и отправляла бы в репозиторий, которого нет. Домены и их
> каталоги сохранены один-в-один; прежние имена оставлены в скобках, чтобы связь со старыми
> PR и разборами не потерялась. В остальном тексте записки имена НЕ трогались — там они
> история и верны как история.

> [!important] У предстоящего удаления НЕТ ПРЕДМЕТА: удалять нечего, и часть «KEEP» тоже снята
> (перемерено 2026-08-08 на `project/kacho` @ `6b1293713`)
> Записка планирует выпилить JIT и полный конвейер GDPR-erasure и держалась в ведре «в работе»
> с невыполненными галочками DoD. В дереве этого кода нет — не «частично», а по каждому
> измерению, которое сама записка называет:
>
> | Измерение (по таблице §«Что выпиливаем») | Предикат | Итог |
> |---|---|---|
> | пакеты `api/jit_eligibility`, `api/jit_pending`, `api/jit_user` | `git ls-files 'services/iam/internal/apps/kacho/api/jit_*'` | **0** файлов |
> | `api/gdpr_erasure` | `git ls-files 'services/iam/internal/apps/kacho/api/*gdpr*'` | **0** |
> | `domain/jit_*`, `domain/gdpr_erasure*` | `git ls-files 'services/iam/internal/domain/*jit*'` | **0** |
> | proto `jit_*.proto`, `gdpr_*.proto` | `git ls-files 'proto/**/*jit*' 'proto/**/*gdpr*'` | **0** |
> | таблицы `jit_*`, `gdpr_erasure_*` в базлайне | комментарии `0001_initial.sql:1370,1722,1831` | «intentionally absent in this baseline» |
>
> Зеркальный контроль (иначе пять нулей неотличимы от негодного шаблона): по всему дереву
> `git ls-files \| grep -iE '(^\|[/_-])jit'` → **1** путь — миграция
> `services/iam/internal/migrations/0013_drop_jit_breakglass_condition_whitelist.sql`, то есть
> шаблон ищущий, а находить нечего; `git ls-files \| grep -iE 'gdpr\|erasure'` → **0**.
>
> **И «KEEP» тоже неверен, что важнее самого удаления.** Записка предписывала сохранить
> `break_glass_grant` как основной админ-механизм — его таблицы дропнуты миграцией
> `services/iam/internal/migrations/0006_drop_scim_saml_break_glass.sql`, а условие
> `break_glass_window` выведено из белого списка той же `0013`. Действовать по этому разделу
> сегодня значило бы восстанавливать снятое.
>
> Поэтому статус — `superseded`, а не `done`: предмет сняли **не этим тикетом** и шире, чем он
> предлагал (RBAC v2 + проходы pruning). Галочки DoD **не проставлены**: они относятся к трём
> PR полирепо-эпохи и к прогонам, а не к чтению дерева. Записка остаётся как намерение и его
> исход; заводить работу по ней нельзя.

**Состояние на момент записи**: in-progress
**Type**: refactor
**Где ведётся**: `PRO-Robotech/kacho` — `services/iam/`, `proto/` (прежние имена: `kacho-iam`, `kacho-proto`)
**PRs**: (не заводились)
**YT**: не заводился

## Что и зачем

Финальный pruning-проход. После phases 1-3 от 47k → 34k LOC. Цель — спустить до **~30k**:
- **DROP JIT** (Just-In-Time access) — 3 пакета (`jit_eligibility`, `jit_pending`, `jit_user`) + связанные services/domain/migration. ~−3500 LOC. **Не востребовано** в MVP: модель Account/Project + RBAC + break-glass для emergency достаточна.
- **Simplify GDPR** — оставить cascade-delete для PII; убрать full erasure pipeline (data-discovery, anonymization, retention policies, erasure jobs). ~−2000 LOC.
- **KEEP**:
  - `break_glass_grant` — emergency admin access (MFA + audit + TTL). Core admin mechanism, требуется ([[KAC-196]] partial fix).
  - `cluster_admin_grant` ([[KAC-196]]) — bootstrap-grant of admin role to a specific user.
  - Cascade-delete user → memberships → tokens (для GDPR-baseline).

## Что выпиливаем

| Пакет | Файлов | Описание |
|---|---|---|
| `api/jit_eligibility` | ~12 | JIT permission templates: «user X may request role Y for max Z hours» |
| `api/jit_pending` | ~10 | Pending JIT requests, approver workflow |
| `api/jit_user` | ~8 | User-side JIT request/list |
| `api/gdpr_erasure` | ~15 | Full GDPR erasure pipeline (data discovery + anonymization jobs) |
| `domain/jit_*` | ~6 | JIT domain entities |
| `domain/gdpr_erasure*` | ~4 | GDPR domain entities (kept: simple_anonymize helper) |
| `service/jit_*`, `service/erasure_*` | ~8 | Use-case orchestration |
| proto: `jit_*.proto`, `gdpr_*.proto` | ~5 | Proto definitions |
| migration: drop tables `jit_*`, `gdpr_erasure_*` | 1 | Squashed migration update |

Approximate net: **~−5500 LOC** (target ~28k internal/).

## Затронутые сущности vault

Список ниже — из плана; четыре из пяти имён в хранилище не резолвятся уже сейчас (их писали
по памяти, а не по дереву). Что там есть на самом деле — строкой ниже.

- `[[../resources/iam-jit-grant]]` — **DELETE** vault note
- `[[../resources/iam-gdpr-erasure]]` — **DELETE** vault note
- `[[../rpc/iam-jit-service]]` — **DELETE**
- `[[../rpc/iam-erasure-service]]` — **DELETE**
- `[[../packages/iam-apps-kacho-api-jit-eligibility]]` etc — **DELETE**

> [!note] Что из этого реально лежит в хранилище (перемерено 2026-08-08)
> `ls obsidian/kacho/resources/ | grep -iE 'jit|gdpr'` → **две** записки:
> [[resources/iam-jit-eligibility]] и [[resources/iam-gdpr-erasure-request]]. Обе описывают
> предмет, которого в дереве продукта больше нет (см. врезку в шапке), — то есть они
> **пережили свой предмет**, и это отдельная работа: у записки, снятой без разбора, теряется
> и след решения, и все ссылки на неё. Здесь это только зафиксировано; статус этих двух
> записок не менялся.

## Acceptance / Definition of Done

- [ ] kacho-proto PR merged (drop jit_*.proto, gdpr_*.proto except cascade-delete shape)
- [ ] kacho-iam PR merged (~−5500 LOC)
- [ ] All wiring cleaned: cmd/kacho-iam/main.go, route registration, hooks_mux.go
- [ ] migration: 0001_initial.sql baseline updated (drop jit_*, gdpr_erasure_* tables)
- [ ] break_glass_grant + cluster_admin_grant intact (smoke test)
- [ ] integration tests зелёные (testcontainers)
- [ ] vault: deleted notes purged, KAC trail updated

## Связанные

- `[[KAC-196]]` — cluster_admin_grant (KEEP — admin mech)
- Phase 1-3 pruning epic — predecessor

#kac #refactor #prune
