# KAC-263: test-infra — zero-manual self-provisioning + бессрочный токен

**Status**: test
**Type**: fix
**Repos**: kacho-workspace
**PRs**: PRO-Robotech/kacho-workspace#70
**YT**: https://prorobotech.youtrack.cloud/issue/KAC-263

## Что и зачем

Требование: тесты (newman/e2e) — никаких ручных манипуляций; токен бессрочный; из-под этого
токена тест-сетап сам создаёт достаточное authz-окружение. Найдено в e2e [[KAC-261]]: приходилось
вручную писать FGA-tuple `user:… editor project:…`, т.к. JWT протухали + newman гонял CRUD по stale
`existingProjectId`, где у тест-юзера PA1 нет editor → 403.

## Фиксы (`tests/authz-fixtures/`)

- **setup-jwt.py** — токены БЕССРОЧНЫЕ (`exp` опускается; `exp_seconds=None`, `--exp-hours` default 0).
  api-gateway dev-authn (`jwt.Parse`, без `WithExpirationRequired`) принимает no-exp. Expired-вариант
  (model 6) сохранён через явный отрицательный `--exp-seconds`.
- **patch-env.py** — alias `existingProjectId←projectA1Id`, `existingProjectCrossId←projectA2Id`
  (+`_suiteFolder*`). Корневой баг: `projectA1Id` патчился, а `existingProjectId` (по нему гоняет
  gen.py) оставался stale.
- **setup.sh** — PA1 получает `editor` и на `projectA2` (cross-project Move/cross-сценарии).

## Проверено zero-manual (без ручных tuple'ов)

`existingProjectId==projectA1Id` (SYNCED) · `jwt exp: отсутствует` · PA1 create/get/list на
existingProjectId → 200 · PA1 create на existingProjectCrossId → 200 · object-level cascade
(editor-на-проекте → viewer-на-ресурсе) работает.

## Затронутые сущности vault

- [[../edges/apigw-to-iam-authz]] (если есть — authz-flow на тест-стенде)

## Acceptance / DoD

- [x] токен бессрочный (verified, no exp)
- [x] self-provisioning: PA1 CRUD на A1+A2 zero-manual → 200
- [x] PR создан (#70)
- [ ] PR merged в main (ждёт ревью)

## Связанные тикеты

- [[KAC-261]] (где найдено), [[KAC-262]] (docs той же сессии)

#kac #fix
