---
title: "#<N>: <one-line summary>"
aliases:
  - issue-<N>
ticket_id: <N>
category: kac
status: in-progress
type: feature
repos:
  - kacho
areas:
  - services/vpc
prs: []
issue_url: https://github.com/PRO-Robotech/kacho/issues/<N>
opened: 2026-MM-DD
tags:
  - kac
---

# #<N>: <one-line summary>

**Type**: feature | fix | refactor | docs | epic

> [!important] Состояние живёт ТОЛЬКО в поле `status:` — второй строки про него в теле нет
> Прежняя редакция шаблона держала здесь `**Status**: in-progress | test | …`, и это
> оказалось двумя местами об одном предмете: волна 2026-08-05 нашла **62** записки,
> где тело и поле называли разное, причём тело почти всегда отставало. Такую строку
> никто не правит вслед за полем — вид-таблица читает поле, а человек читает тело.
> Если хочется сохранить, каким состояние было **на момент написания** (ветка, что было
> зелено, чего ждали), пиши это отдельной строкой и датируй:
> `**Состояние на момент записи**: test (код готов, ждёт ревью) — 2026-MM-DD`.
> Это факт о прошлом, он не устаревает.

> [!note] Имя файла — по номеру issue (`issue-<N>.md`), каталог остаётся `KAC/`
> Трекер сменён на GitHub Issues (2026-08-12, `git-issues.md`). Каталог сохраняет
> историческое имя: в нём 214 записок прежнего трекера, а проверка
> `scripts/docs-gate/check-02-kac-trail-status.py` обходит **каталог**, не префикс
> имени, — новые записки лежат там же и ничего не ломают. Старые `KAC-<N>.md` не
> переименовываются: они свидетельствуют о сделанном.

> [!note] Какой статус выбирать, чтобы вид `KAC/all-tickets.base` не солгал
> Вид «Active» показывает всё, что **не** `done`/`wontfix`/`superseded`/`reference` —
> то есть неверно выбранный статус читается как «по этому кто-то работает».
> - `done` — работа доехала (проверяется по дереву продукта, а не по списку пунктов);
> - `wontfix` — отказались, с обоснованием;
> - `superseded` — предмет поглощён другой работой или снят с продукта решением;
> - `reference` — отчёт раунда / методика: единицей работы никогда не была, закрывать нечего.
>
> Заводя новый статус — впиши его в `all-tickets.base` в **оба** места (формулу значка и
> фильтр «Active»), иначе вид перестанет отличать закрытое от открытого.
**Где ведётся**: `PRO-Robotech/kacho` — каталоги `services/<домен>` / `proto` / `pkg` /
`gateway` / `deploy` / `ui-future`; журнал и приёмка — `PRO-Robotech/kacho-workspace`.
**PRs**: PRO-Robotech/kacho#<N>, ...
**Issue**: https://github.com/PRO-Robotech/kacho/issues/<N>

> [!warning] Не выписывай сюда имена `kacho-<часть>` как адреса сегодняшней работы
> Полирепо-эпоха кончилась: разработка идёт в **одном** репозитории продукта. Имена
> `kacho-proto` / `kacho-corelib` / `kacho-api-gateway` / `kacho-deploy` / `kacho-ui`
> в старых записках верны **как история** (ссылка на PR, разбор инцидента, объяснение
> самой смены топологии) и трогать их там нельзя. Но новая записка, называющая их
> адресом работы, отправляет следующего читателя в репозиторий, которого он не найдёт.
> Перечень доменов **выводится** из `services/*` дерева продукта, а не выписывается
> по памяти: на 2026-08-05 их семь — compute, geo, iam, nlb, registry, storage, vpc.

## Что и зачем

1-2 абзаца: проблема + решение.

## Затронутые сущности vault

- `[[resources/<X>]]` — что изменилось
- `[[packages/<repo>-<pkg>]]` — что добавлено/удалено
- `[[edges/<edge>]]` — runtime изменения
- `[[rpc/<service>]]` — RPC изменения

## Acceptance / Definition of Done

> [!important] Проставляй `[x]` сразу по факту выполнения пункта.
> Перед переводом `status: done` (в frontmatter и YouTrack) — пробеги по DoD и убедись, что все выполненные пункты отмечены. `status: done` + любой `[ ]` среди фактически сделанного — регрессия, vault-stop-check hook её ловит.

> [!important] Пункт DoD обязан быть проверяем ПО ДЕРЕВУ, а не по чужому процессу
> «PR смёржен в `kacho-<repo>`», «ветка удалена», «перепрогнать CI-прогон `<id>`»,
> «YT → Done» — формулировки, которые сегодня не проверить by construction: этих
> репозиториев как единиц работы нет, а те прогоны принадлежали им. Пункт такого вида
> держит записку открытой навсегда и ничего при этом не утверждает. Пиши то, что можно
> предъявить: **какой файл, таблица, RPC, поле, гейт появится в дереве**.

- [ ] integration tests зелёные
- [ ] newman E2E зелёный
- [ ] vault записи обновлены
- [ ] предмет предъявлен в дереве продукта: `<путь/имя, которое можно найти>`

## Связанные тикеты

- `[[KAC-<N-prev>]]` (предусловие)
- `[[KAC-<N-next>]]` (follow-up)

#kac
