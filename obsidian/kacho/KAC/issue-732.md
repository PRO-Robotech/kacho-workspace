---
title: Обход цепи областей не платит за размер облака (S2 линии roles-new)
ticket_id: "732"
status: test
type: fix
repos:
  - kacho
prs: []
yt_url: ""
opened: 2026-08-19
closed: ""
category: kac
tags:
  - kac
  - fix
  - kacho
---

# S2: четыре блокера собственной константы на проверку доступа

Стадия **S2** приёмки `docs/specs/sub-phase-R7-1-constant-check-linear-materialization-acceptance.md`.
Предмет — четыре места, из-за которых стоимость одного вопроса о доступе зависела
от размера облака, а не от предмета вопроса.

## Что было неверно

| # | предмет | чем проявлялось |
|---|---|---|
| 1 | рекурсивный обход цепи областей читал таблицу рёбер **целиком на каждом уровне** | обычное соединение в рекурсивной ветви — планировщик вправе прогнать всю таблицу |
| 2 | субъект выдачи сравнивался склейкой двух колонок | склейка выводит обе колонки из-под индекса: отбор идёт по вычисленному значению, то есть по уже прочитанным строкам |
| 3 | предикат субъекта и предикат области стояли на **разных** отношениях | одного индекса, обслуживающего оба, не существовало by construction: заход со стороны области читал все выдачи области, со стороны субъекта — все выдачи субъекта в облаке |
| 4 | над союзом источников стояли три блокирующих узла, а читатель вычитывал курсор до конца | короткое замыкание было заявлено, а не наблюдаемо |

Пятое, найденное по дороге: гипотеза о дороговизне меточной ветви (И9)
**не подтвердилась** на контролируемой фикстуре.

## Что сделано

- обход цепи оставлен, но переведён на соединение вбок с пределом — форму, уже
  стоявшую на пути перечисления;
- область выдачи перенесена на строку субъекта; согласованность держит
  **составной внешний ключ с каскадом правки** плюс триггер заполнения —
  within-service инвариант на DB-уровне (ban #10), а не синхронизация в коде;
- заведён индекс, которым разрешается пара «субъект + область»;
- сняты блокирующие узлы, ветвь выдач ограничена одной строкой **на сервере**
  (закрытие курсора у клиента работы у базы не сберегает);
- усечение набора условий осталось тем же состоянием с тем же порогом и
  определяется теперь **причиной остановки**, а не длиной среза.

## Числа (предикат назван, чтобы их можно было повторить)

Прибор — `pg/planrows` на плане, захваченном у настоящего вызова; величина —
строк, отданных узлами плана, с множителем циклов.

| замер | до | после |
|---|---:|---:|
| строк из таблицы рёбер за вердикт (цепь d=3) | 2412 | 7, и не растёт с числом объектов |
| прирост строк при 10⁴ посторонних выдач **в этой области** | +9993 | −9 |
| прирост строк при 10⁴ выдачах субъекту **вне этой области** | +19991 | −9 |
| строк за разрешённый вердикт при 300 основаниях | 1222 | 25 |
| малая сетка, отношение по оси B | 6.87 | 0.33 (потолок 2.00) |
| мощность цепи областей S_набл при d=3 | 7 | 7 (не менялась) |
| объявленная граница S_гран | 11 | 11 (не менялась) |

## Что это меняет в устройстве

- **Ось R перестала быть контролем.** Разрешённый вердикт отвечает первым же
  основанием, поэтому число выдач, называющих спрашиваемого, его стоимости
  больше не меняет. Положительный контроль малой сетки переехал на ось фактов,
  где рост есть по существу.
- **Признак меточной ветви считается по прочитанному**, и неопределённость
  названа, а не спрятана: рядом заведён счётчик вердиктов, ответивших до конца
  набора, и у него есть читатель.
- **Плечо гейта Г15 остаётся действующим.** Приёмка снимала его вместе с
  переходом на одно чтение; перехода не будет.

## Чем держится

- гейт `TestVerdictSubjectIsComparedByColumnPairNotByGlue` — субъект сравнивается
  парой колонок; инъекция в обе стороны, три законных близнеца молчат;
- гейт `TestVerdictPathReadsNothingUnbounded` — на пути решения нет перечисления;
- гейт `TestVerdictScopeFormIsTheSameOnEveryEntryPoint` — все четыре точки входа
  отвечают об области **одним предикатом**; инъекция различает подменённый предел
  и одноразовое чтение, но молчит на страничном носителе;
- проба `TestScopeReachesTheRootOnTheChainProducersActuallyWrite` — цепь сеется
  **через настоящего производителя**, и выдача на аккаунт вместе с фактом
  администратора облака на кластере обязаны действовать;
- пробы `TestBindingSubjectScopeCarrier_*` — согласованность перенесённых колонок
  держит база: четыре исхода, включая названный недостижимым каскад.

## Вторая половина Б1: обход восстановил форму, но не достижимость

Возврат обхода вернул **правильный запрос**, а не работающие верхние уровни.
Рёбра появляются единственным путём — регистрацией ресурса, — и зовут её пять
сервисов-соседей для **своих** ресурсов; объектов типа `project` и `account` не
регистрирует **никто, включая сам iam**. На цепи, которую производит дерево
(одно ребро сеть → проект), обход доходит до проекта и останавливается:

| вопрос | исход сегодня |
|---|---|
| выдача на **проект** (контроль) | `allow` |
| выдача на **аккаунт** | `deny` |
| факт `system_admin` на **кластере** | `deny` |

Моя первая проба была зелена потому, что **сама досеивала** два недостающих
звена. Закреплено как есть отдельной пробой; предмет — `kacho#740`, там же
предикат снятия (проба **покраснеет** — это сигнал закрытия, а не поломка).

**Не боевая регрессия:** форма провязана только теневым сравнителем. Опасность
в том, что сравнение не краснеет на вопросах, которых корпус не задаёт, — и
переключение сняло бы верхний ярус вместе с аварийным путём администратора
облака.

## Класс, который эта задача дала корпусу

**Предпосылка о форме данных — утверждение о ПРОИЗВОДИТЕЛЯХ, а не о схеме.**
Схема допускала и замыкание, и список звеньев; приёмка и я приняли первое.
Производители пишут второе. Проверка, которая должна была это поймать, судила
**наличие** цепи, а не её длину, и сама об этом честно предупреждала.

**И вторая половина, которая опаснее первой:** переписав фикстуры под свою
предпосылку, я сделал расхождение ненаблюдаемым. Проба, чью фикстуру подогнали
под правку, различить формы больше не может — зелёное после такого означает
только то, что фикстура согласна сама с собой.

**Третье, и его я повторил уже после разбора первых двух.** Блокер был
**дизъюнкцией**: «либо правка сужает права, либо верхние уровни не работали
никогда». Я доказал первую половину замером и счёл вторую снятой. Половины
независимы, и истинны обе. Отсюда норма: доказательство одной ветви дизъюнкции
не снимает другую — каждая требует своего замера.

**Четвёртое — про гейты, которые сверяют меньше, чем объявляют.** Гейт согласия
форм брал «шаг» от `FROM`, а всё до него звал носителем. В носителе лежала
**арифметика глубины**: подмена `s.depth + 1` на `s.depth + 2` режет цепь вдвое
и проходила мимо. Граница между «носителем» и «предикатом» проводится по тому,
**что меняет исход**, а не по синтаксической позиции.

## Затронутые сущности vault

- [[iam-access-binding]] — у строки субъекта выдачи появились колонки области;
  согласованность держит составной внешний ключ с каскадом правки.

## DoD

- [x] обход цепи не платит за размер облака (одноразовое чтение отвергнуто замером)
- [x] субъект сравнивается парой колонок
- [x] индекс заведён и проверен инъекцией
- [x] блокирующие узлы сняты, замыкание на сервере
- [x] усечение осталось отказом
- [x] гипотеза И9 перемерена
- [x] перезамер полной сетки приложен отчётом
- [ ] формулы приёмки пересчитаны (в репозитории воркспейса, отдельным изменением)
