# KAC-261: kacho-vpc — чистка мёртвого кода + squash миграций + evgeniy + зелёный CI

**Status**: test
**Type**: refactor
**Repos**: kacho-vpc
**PRs**: PRO-Robotech/kacho-vpc#130
**YT**: https://prorobotech.youtrack.cloud/issue/KAC-261

## Что и зачем

Автономная сессия чистки kacho-vpc (full autonomy, 2026-06-06). Репо в середине
evgeniy-миграции: новые слои (`apps/kacho/api/*`, `dto/toproto`, `repo/kacho/pg`,
CQRS) готовы, но остался недомигрированный хвост + дубли. Baseline-тег **v0.3.3**
выпущен до работ (точка отката).

## Объём (по investigation-workflow, 6 аудитов)

1. **Dead code**: `RcNameStrict`-кластер (`domain/types.go`), 6 payload-функций
   (`repo/helpers/payloads.go`), dead `invalidArg` (privateendpoint), dead repomock
   типы (`GatewayRepo`/`PrivateEndpointRepo`), unwired `iam_listobjects_client`
   (+test, superseded by listauthz), test-only dead.
2. **Вермишель dedup**: ~40 byte-identical копий → shared: `mapRepoErr`/`stripSentinel`×8
   → `serviceerr.MapRepoErr`; `operationToProto`×9; `fgaSubjectFromCtx`×7; `payloadMap`×7;
   `invalidArg`×8.
3. **Squash миграций**: `0001..0004` → один `0001_initial.sql` (folder_id→project_id +
   principal-cols + network_id NOT NULL); удалить 0002/0003/0004 + obsolete backfill
   integration test. Verify pg_dump empty diff.
4. **evgeniy readability**: хотспот `address/create.go` (806 строк) — dead branch,
   dup subnet-reads, twin alloc-loops, file split.
5. **Internal-API dead**: убрать impl `InternalWatchService` (0 потребителей, controllers
   упразднён); `InternalResourceLifecycleService` — orphan proto (cross-repo, note).
6. **Docs staleness**: ~20 stale `internal/service/*` путей в CLAUDE.md + docs/.
7. **CI green**: ci.yaml (build/vet/test/lint/integration/authz-deny). newman-e2e —
   кросс-репо pre-existing-red (kacho-iam deploy не сходится), НЕ vpc.

## Констрейнт (главное)

**НЕ менять контракты** (proto/REST/DB-схема/`pkg/**` SDK). При конфликте «удали
internal» vs «не менять контракты» — побеждает контракт: удаляю только доказуемо-мёртвый
internal (0 потребителей). Cross-service потребители internal-API (НЕ трогать):
- `InternalAddressService` ← kacho-compute + kacho-nlb
- `InternalAddressPoolService` / `InternalCloudService` ← api-gateway → admin-UI
- `InternalNetworkService.SetDefaultSecurityGroupId` ← api-gateway/admin

## Затронутые сущности vault

- [[../rpc/vpc-internal-address-service]] (internal-API surface — обновить статусы)
- [[../resources/vpc-security-group]] (network_id NOT NULL в baseline)
- [[../packages/vpc-internal-migrations]] (squash 0001..0004)

## Прогресс (коммиты на ветке KAC-261)

- `347d0dc` squash миграций 0001..0004 → один 0001_initial.sql (pg_dump diff ПУСТОЙ, SHA256 идентичны)
- `0c47afa` dead-code (RcNameStrict, 6 payload-func, dead repomock-типы, iam_listobjects, test-dead)
- `e7e442a` docs: ~26 stale internal/service/* путей
- `6ef6955` dedup ~48 byte-identical хелперов → shared (serviceerr.MapRepoErr/InvalidArg, pbconv.OperationToProto/SubjectFromContext, helpers.DomainToMap); mapping.go удалён
- `ad1f1b4` удаление impl InternalWatchService (handler+registration+config+deploy; proto остаётся cross-repo)
- `b1ad121` gofmt/goimports — lint green (чинит pre-existing красноту main в listauthz/adapter_test.go)
- `0d35a9d` test-fix pre-existing KAC-243 SGNet тестов (rule-id "r1" → resource-id; test-only, прод не тронут)

## Затронутые сущности vault

- [[../rpc/vpc-internal-watch-service]] — impl удалён (proto остаётся)
- [[../packages/vpc-apps-kacho-shared-serviceerr]] — +InvalidArg, дедуп MapRepoErr
- [[../packages/vpc-apps-kacho-shared-pbconv]] — новый пакет (OperationToProto/SubjectFromContext)

## Pre-existing находки (не моя регрессия)

ci.yaml на main был КРАСНЫМ с 2026-06-01 (KAC-240/KAC-243): lint (gofmt в `listauthz/adapter_test.go`)
+ integration (KAC-243 SGNet тесты с невалидным rule-id `"r1"`). Оба починены в этом PR → main снова зелёный.
newman-e2e.yml — отдельный pre-existing-red (kacho-iam deployment не сходится в kind-umbrella), cross-repo, вне scope.

## Acceptance / DoD

- [x] go build/vet/test-short зелёные (на каждом коммите)
- [x] integration (testcontainers) зелёные (132s, squashed-миграция OK)
- [x] golangci-lint зелёный (0 issues)
- [x] pg_dump diff (squash) пустой
- [x] authz-deny gate (366 cases >= 200)
- [x] vault записи обновлены (rpc/packages)
- [x] PR создан (#130)
- [ ] PR merged в main (ждёт ревью)

#kac #refactor
