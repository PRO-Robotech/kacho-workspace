---
ticket_id: KAC-240
status: test
type: fix
repos: [kacho-vpc]
prs: [PRO-Robotech/kacho-vpc#118]
yt_url: https://prorobotech.youtrack.cloud/issue/KAC-240
tags: [kac, fix, kacho-vpc, rbac, authz]
---

# KAC-240 — stale-after-create: List RBAC v2 filter скрывает свежесозданный ресурс

**Status**: test (PR #118, deployed) · **Type**: fix · **Repos**: kacho-vpc

## Что и зачем

После создания VPC-ресурса он не виден в списке UI (read-your-write inconsistency).
Эмпирически подтверждено (read-only диагностика): List use-cases фильтруют строки
БД по per-object FGA allow-set (`authz.ListAllowedIDs(subject,"vpc_<res>","view")` →
`FilterByAllowedIDs`); per-object tuple пишется `fgawrite.Emit` ПОСЛЕ commit,
best-effort, а Operation done выставляется ДО записи tuple. + corelib allow-set
cache 5s. Первый refetch промахивает tuple; при silent-fail best-effort write
ресурс невидим надолго. UI корректен (refetchType:all + 3s poll + shape совпал).

## Решение (вариант 1)

List фильтровать по доступу к ПРОЕКТУ, не по per-object tuple. `repo.List` уже
folder/project-bounded → если subject может view проект, вернуть все строки. Убирает
зависимость от per-object tuple в момент List.

## Затронутые сущности vault

- 8 List use-cases kacho-vpc (address/subnet/network/route-table/security-group/gateway/network-interface/private-endpoint)
- corelib `authz.ListFilter` (возможно расширение project-scoped методом)

## DoD

- [x] RED→GREEN unit/use-case тест (create → сразу List → строка есть)
- [x] cross-project / fail-closed тесты
- [x] все 8 list.go переведены на listauthz.CanViewProject (project-level)
- [x] build+vet+test зелёные, задеплоено (под vpc-58669c8b9c-l9xn7)
- [ ] PR #118 merge + smoke на стенде

## Решение (как сделано)

8 List use-cases → общий `listauthz.CanViewProject(subjectID, projectID)` через существующий Check (relation `viewer`, object `project:<id>`), вместо per-object FGA tuple. repo.List уже project-bounded (isolation не тронута); fail-closed сохранён. PR PRO-Robotech/kacho-vpc#118.

## Связанные

- [[KAC-239]] (UI-эпик VPC redesign — где симптом всплыл)
- KAC-219 (RBAC v2 list-filter — источник механизма)

#kac #fix #kacho-vpc
