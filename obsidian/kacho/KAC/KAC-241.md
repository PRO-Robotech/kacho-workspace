---
ticket_id: KAC-241
status: in-progress
type: refactor
repos: [kacho-ui]
prs: []
yt_url: https://prorobotech.youtrack.cloud/issue/KAC-241
tags: [kac, refactor, kacho-ui, ui]
---

# KAC-241 — kacho-ui: унификация Create/Edit форм + UX-уплифт (ResourceFormBody)

**Status**: in-progress · **Type**: refactor · **Repos**: kacho-ui · **Parent**: [[KAC-239]]

## Что и зачем

Стиль Create и Edit (и modal vs full-page) расходится. Modal-формы (`Inline*Form`) следуют
§4-контракту kacho-ui CLAUDE.md; full-page (`ResourceCreatePage/EditPage`) — нет (нет `<Form>`-
обёртки, другой title/level/icon, нет фильтрации полей, иной immutable-механизм). Кастомные формы
(Subnet/SG/NIC/AddressPool) дополнительно расходятся: pending-кнопка (NIC спиннер vs DopplerButton),
info-tooltips (3 реализации + AddressPool без них), immutable-affordance (🔒 только Subnet),
CIDR-редактор (Subnet rpc vs AddressPool controlled).

## Решение (подход A, вариант B)

Единый **`ResourceFormBody`**, который рендерят И modal-, И page-шелл → `create==edit==modal==page`.
Общие примитивы `src/components/form/`: `FormShell`, `FormSection`, `FieldLabel`, `FormFooter`,
`ImmutableField`, `CidrEditor`. **Вариант B**: compute/NLB/system остаются full-page, но рендерят
тот же `ResourceFormBody` (без миграции в модалки). Удалить мёртвый `components/ui/tabs.tsx`
(button/input/dialog — used, shadcn-слой для array-items/диалогов, остаются).
Палитру/AntD-токены/Tailwind-токены НЕ трогать (синхронны, source of truth).

UX-best-practices: секции с collapse, immutable 🔒+причина, inline-help+плейсхолдеры, sticky-футер,
autofocus/Enter/Esc.

## Design / план

- Design-doc: `docs/superpowers/specs/2026-06-01-kacho-ui-form-unification-design.md` (APPROVED, вариант B)
- Implementation-plan: `docs/superpowers/specs/2026-06-01-kacho-ui-form-unification-plan.md` (writing-plans → TBD)

## Затронутые сущности vault

- `packages/` kacho-ui form-слой (новые примитивы `src/components/form/`) — обновить после merge.

## Прогресс (Plan 1 — foundation, ветка KAC-241 в worktree)

Исполнение subagent-driven (свежий implementer на группу + spec→quality ревью). Worktree
`cloud-demo/kacho-ui-KAC-241` от `main` (KAC-232 checkout не тронут). Сделано:
- ✅ Tasks 1–6: примитивы `FieldLabel/FormFooter/ImmutableField/FormShell/FormSection` + `ResourceFormBody` (TDD, 12 unit).
- ✅ Tasks 7–10: 2 modal + 2 page шелла → `ResourceFormBody` (`create==edit==modal==page`). 138 тестов зелёные.
- ✅ Task 11 (скорректирован): удалён только `components/ui/tabs.tsx`; `button/input/dialog` — used, оставлены.
- ✅ Task 12: kacho-ui CLAUDE.md §3/§15/§2 синхронизирован (вариант B).
- ⏳ Tasks 13–14: playwright-паритет + финальные гейты + PR.
- ⏭ Plan 2 (отдельный): custom-формы Subnet/SG/NIC/AddressPool + новый SG Create + `CidrEditor`.

## DoD

- [x] Примитивы §3 + unit
- [x] generic modal + page рендерят `ResourceFormBody` (паритет)
- [ ] 4 кастом-формы + новый SG Create на примитивах; дивергенции устранены — **Plan 2**
- [x] мёртвый `components/ui/tabs.tsx` удалён; kacho-ui CLAUDE.md §3/§15 синхронизирован
- [ ] tsc clean; playwright e2e (RED→GREEN); смоук на стенде
- [ ] PR merged + vault обновлён

## Связанные

- [[KAC-239]] (родительский эпик — VPC UI redesign, где всплыло)
- [[KAC-240]] (соседний fix в том же сеансе — list-filter 503)

#kac #refactor #kacho-ui
