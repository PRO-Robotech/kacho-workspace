---
title: "KAC-273: addresses-tab пула — колонки имя/идентификатор/IP-адрес/дата создания"
category: kac
ticket_id: KAC-273
status: test
verified_against: "предмет сверен с PRO-Robotech/kacho@58913d0e (2026-08-05); текст записки построчно не пересматривался"
type: fix
tags:
  - kac
---

# KAC-273: addresses-tab пула — колонки имя/идентификатор/IP-адрес/дата создания

> [!note] Строка «Где ведётся» переписана 2026-08-05: это адрес РАБОТЫ, а не история
> Здесь стояли имена репозиториев полирепо-эпохи. По незакрытой записке эта строка читается
> как указание, куда идти, — и отправляла бы в репозиторий, которого нет. Домены и их
> каталоги сохранены один-в-один; прежние имена оставлены в скобках, чтобы связь со старыми
> PR и разборами не потерялась. В остальном тексте записки имена НЕ трогались — там они
> история и верны как история.

> [!warning] Сверено по дереву 2026-08-05 (`PRO-Robotech/kacho@58913d0e`) — статус НЕ закрыт, и это названо числом
> страница пула в дереве есть (`ui-future/vpc/src/pages/AddressPoolDetailPage.tsx`), но состав колонок вкладки адресов по ней не сверялся — утверждать «сделано» не на чем. Записка остаётся `test` намеренно: закрыть её нечем.
> Правило закрытия — [[KAC/README]].

**Состояние на момент записи**: test
**Type**: fix
**Где ведётся**: `PRO-Robotech/kacho` — `ui-future/` (прежние имена: `kacho-ui`)
**PRs**: —  (ветка KAC-273, забирает заказчик)
**YT**: https://prorobotech.youtrack.cloud/issue/KAC-273

## Что и зачем

На detail-странице AddressPool вкладка «Адреса» (`/system/address-pools/<id>?tab=addresses`,
данные `GET /vpc/v1/addressPools/{id}/addresses`) имела разнородный набор колонок
(IP / Адрес+id-link / Зона / Project / Account / Reserved-Used). Привели к ровно
4 колонкам как в остальных списках проекта: **Имя / Идентификатор / IP-адрес / Дата создания**.

- Имя — `CopyableName` (fallback на id), обёрнут в Link на detail адреса.
- Идентификатор — `CopyableId` (моноширинный, copy-on-click).
- IP-адрес — строковое значение из payload `address.ipv4` (моноширинный).
- Дата создания — `formatDateTime(address.created_at)` (единый формат ru-RU).

Удалены дед-код запросы `/iam/v1/projects` и `/iam/v1/accounts` + `reverseLookup`
(питали только убранные Project/Account колонки) — две лишних IAM-list-выборки на
этой вкладке больше не делаются.

## Затронутые сущности vault

- [[../resources/vpc-addresspool]] — admin detail-tab «Адреса», без изменения ресурсной модели.

## Acceptance / Definition of Done

- [x] `npx tsc --noEmit` зелёный
- [x] `npm run build` зелёный
- [x] vault запись создана
- [ ] PR merged в main (ветку забирает заказчик)

#kac #fix
