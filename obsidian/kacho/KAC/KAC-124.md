# KAC-124: Полное удаление kacho-resource-manager (E5 closeout)

**Status**: test
**Type**: refactor
**Repos**: kacho-api-gateway, kacho-deploy, kacho-workspace
**PRs**:
- PRO-Robotech/kacho-api-gateway#17
- PRO-Robotech/kacho-deploy#34
- PRO-Robotech/kacho-workspace#19
**YT**: https://prorobotech.youtrack.cloud/issue/KAC-120 (vault-label KAC-124, YT-counter отстал → idReadable=KAC-120; linked as subtask of KAC-104)

## Что и зачем

Финальное закрытие эпика **KAC-104 IAM**: после E1 (`folder_id→project_id`),
E2 (Zitadel/OIDC), E3 (OpenFGA/Keto Check), E4 (signup flow + UI) — backend
kacho-resource-manager больше не используется. KAC-124 = E5: удалить его
из платформы целиком.

## Затронутые сущности vault

- [[../resources/iam-account]] — заменяет Organization+Cloud (rm)
- [[../resources/iam-project]] — заменяет Folder (rm)
- [[../packages/api-gateway-restmux]] — rmpb/orgpb imports удалены
- [[../packages/api-gateway-allowlist]] — 17 публичных RPC удалены
- [[../packages/api-gateway-opsproxy]] — b1g/bpf/rm/org/legacy-prefixes удалены
- [[../packages/api-gateway-proxy]] — Backends-карта без resourcemanager
- [[KAC-104]] — эпик закрывается с этим тикетом
- [[KAC-123]] — последний фикс до closure
- [[../edges/vpc-to-rm-folder-exists]] — заменён `vpc-to-iam-project-exists`
- [[../edges/compute-to-rm-folder-exists]] — заменён `compute-to-iam-project-exists`

## Изменения

### `kacho-api-gateway` (PR #TBD)

**Code-changes:**
- `internal/restmux/mux.go`: rmpb/orgpb imports + `rmAddr` + 3 RegisterHandler
  calls (CloudService / FolderService / OrganizationService) удалены.
- `internal/allowlist/list.go`: 11 публичных RPC resourcemanager.v1 + 6 —
  organizationmanager.v1 удалены. Комментарии обновлены: «Активны: iam,
  vpc, compute, operation».
- `internal/opsproxy/proxy.go`: prefixToBackend (b1g/bpf) + legacyPrefixToBackend
  (rm/resourcemanager/org/organizationmanager) удалены.
- `internal/proxy/director.go`: комментарии Backends-карта (resourcemanager
  → iam).
- `cmd/api-gateway/main.go`: комментарии активных backends обновлены.

**Tests rewritten (под iam.AccountService вместо rm/org):**
- `proxy/director_test.go` (8 cases): `A2_DirectorRoutesToIAM`,
  `A5b_RemovedResourceManagerMethodsBlocked` (3 sub-cases), и т.д.
- `opsproxy/proxy_test.go`: `RmLegacyPrefixesRemoved`,
  `RmPrefixIs_InvalidArgument`, `Cancel_RmLegacyPrefixRemoved`.
- `allowlist/list_test.go`: D3 теперь проверяет iam.v1.AccountService /
  ProjectService methods (не rm/org).
- `gateway_test/integration_test.go`: переписан целиком на
  `iamv1.AccountService` (mockAccountServer вместо mockOrgServer + mockCloudServer).
- `restmux/mux_test.go`: `/resource-manager/v1/folders/f-1` → `/iam/v1/projects/prj-1`.

Все тесты зелёные (`go test ./...`).

### `kacho-deploy` (PR #TBD)

- `helm/umbrella/Chart.yaml`: версия 1.0.6 → 1.0.7. Dependencies
  `resource-manager` (file://../../../kacho-resource-manager/deploy) и
  `pg-resource-manager` postgresql-инстанс удалены.
- `helm/umbrella/values.dev.yaml`: блок `pg-resource-manager` (auth/persistence/
  resources/extendedConfiguration) удалён.
- `clusters/e2c825/overrides.yaml`: блок `resource-manager.image` удалён.
- `Makefile`: ci-images for-loop теперь без resource-manager; reload-svc
  whitelist убран.

**Deploy на e2c825**:
- `kubectl delete deploy resource-manager` ✓
- `kubectl delete sts kacho-umbrella-pg-resource-manager` ✓
- `kubectl delete svc resource-manager{,-internal}` ✓
- `kubectl delete svc kacho-umbrella-pg-resource-manager{,-hl}` ✓

Smoke: `curl /resource-manager/v1/folders` → **404 Not Found** ✓
       `curl /iam/v1/accounts` → 200 (anonymous default-deny → empty).

### `kacho-workspace` (PR #TBD)

- `CLAUDE.md`:
  - таблица репо: `kacho-resource-manager` → ~~strikethrough~~ + Упразднён
    в KAC-124; добавлена строка `kacho-iam` (Account/Project/...).
  - build-граф: `kacho-resource-manager` → `kacho-iam` (KAC-124: заменил).
  - runtime cross-domain edges: `* → kacho-resource-manager` → `* → kacho-iam`
    (`ProjectService.Get` + `InternalIAMService.Check`).
  - кросс-репо порядок merge: `kacho-resource-manager` → `kacho-iam` /
    `kacho-compute` / ...
  - Карта владельцев доменов: добавлен IAM (kacho-iam owner), removed RM line.
  - Циклы: пример leaf-owner поменян с `kacho-resource-manager` на `kacho-iam`.

- `obsidian/kacho/KAC/KAC-124.md` (этот файл) — full trail.

## Acceptance / Definition of Done

- [x] api-gateway code-changes + tests зелёные
- [x] kacho-deploy chart + values + overrides + Makefile cleanup
- [x] Cluster e2c825: resource-manager Deployment / StatefulSet / Services удалены
- [x] api-gateway redeploy с новым образом — rollout успешен
- [x] Smoke: `/resource-manager/v1/folders` → 404 NotFound
- [x] Smoke: `/iam/v1/accounts` → 200 (anonymous default-deny)
- [x] Workspace CLAUDE.md обновлён (5 точек)
- [x] Vault `KAC-124.md` создан (этот файл)
- [ ] PR в 3 репо (kacho-api-gateway, kacho-deploy, kacho-workspace)
- [ ] proto-пакеты `resourcemanager.v1` / `organizationmanager.v1` удалены из
      kacho-proto (отдельный PR в kacho-proto — out-of-scope этого тикета,
      т.к. не блокирует деплой; зачищается follow-up'ом после code review)
- [ ] GitHub repo `kacho-resource-manager` archived (manual action)

## Что НЕ входит (KAC-124-followup)

- **Удаление proto-пакетов** `kacho.cloud.resourcemanager.v1` /
  `kacho.cloud.organizationmanager.v1` из `kacho-proto` — отдельный PR
  (после merge'а KAC-124 в api-gateway гарантирует, что никто не импортирует).
- **Удаление `kacho-resource-manager` репозитория** — `gh repo archive` manual.
- **ci/docker-compose.yml** — legacy CI compose stack, не используется в e2c825
  flow (только nightly CI). Будет почищен в KAC-124-followup или KAC-111
  (CI cleanup).
- **`.github/workflows/ci.yaml`** — checkout `kacho-resource-manager` — требует
  workflow scope token. Force-merge или manual edit на main.

## Связанные тикеты

- [[KAC-104]] — родительский эпик; закрывается этим тикетом
- [[KAC-105]] / [[KAC-106]] / [[KAC-107]] / [[KAC-108]] / [[KAC-109]] / [[KAC-110]] — E0…E5 sub-эпики
- [[KAC-123]] — последний fix перед E5

#kac #refactor #kacho-iam #kacho-apigw #kacho-deploy
