---
title: "KAC-191: kacho-iam — permission_catalog.json mirror has empty `permission` fields"
category: kac
ticket_id: KAC-191
status: done
verified_against: "предмет сверен с PRO-Robotech/kacho@58913d0e (2026-08-05); текст записки построчно не пересматривался"
type: fix
tags:
  - kac
---

# KAC-191: kacho-iam — permission_catalog.json mirror has empty `permission` fields

> [!note] Оба названных пути переехали (сверено 2026-08-05)
> Встроенная копия каталога прав в iam лежит в
> `services/iam/internal/apps/kacho/seed/embedded/permission_catalog.json`, вторая — в
> `gateway/internal/middleware/embed/permission_catalog.json`. Источника
> `kacho-proto/gen/permission_catalog.json` нет: каталог **генерируется** в дереве продукта.
> Записей 294, пустых `permission` — 0, то есть дефект записки в дереве отсутствует.

> [!note] Сверено по дереву продукта 2026-08-05 (`PRO-Robotech/kacho@58913d0e`): обе встроенные копии каталога прав в дереве, 294 записи, пустых `permission` — 0.
> Правило закрытия и что именно проверяется вместо пунктов «PR смёржен» — [[KAC/README]].

**Состояние на момент записи**: test
**Type**: fix
**Repos**: kacho-iam
**PRs**: PRO-Robotech/kacho-iam#49 (open)
**YT**: https://prorobotech.youtrack.cloud/issue/KAC-191

## Что и зачем

`internal/apps/kacho/api/internal_iam/embedded/permission_catalog.json` (embedded в iam binary через go:embed, используется `InternalIAMService.ListPermissions`) содержал 300+ entries, но во всех `"permission": ""`.

`list_permissions.go::loadCatalogPermissions` фильтрует empty (`if e.Permission == "" continue`) → runtime список пустой → `ListPermissions` возвращает `{}`.

Newman case `IAM-INT-OK-INT-LISTPERMS` асёртит non-empty array → FAIL.

## Fix

1. Copy `kacho-proto/gen/permission_catalog.json` → embedded location (300 populated entries)
2. Add Makefile target `sync-permission-catalog`
3. RED-first integration test `TestLoadCatalogPermissions_NonEmpty` — asserts ≥100 entries + format spot-check

## RED → GREEN

- OLD catalog: `FAIL: permission catalog has 0 entries, want >= 100`
- NEW catalog: `ok  ... 0.005s`

## Связанные тикеты

- [[KAC-188]] — parent epic (newman iam main → GREEN)
- KAC-178 §5 — упоминается «mirror not runtime»; KAC-191 этот mirror всё-таки runtime

#kac #fix #bug #iam #verified-by:test
