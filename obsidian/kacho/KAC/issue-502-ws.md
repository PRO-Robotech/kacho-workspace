---
title: "ws#502: норму «диагностика берётся из приёмки» не держала ни одна проверка"
aliases:
  - issue-502-ws
ticket_id: 502
category: kac
status: test
type: fix
repos:
  - kacho-workspace
issue_url: https://github.com/PRO-Robotech/kacho-workspace/issues/502
opened: 2026-09-03
tags:
  - kac
  - ci
verified_against: "круг M2, release/sdd-closeout@b5fa9696: обе посылки подтверждены (I2 красно на стволе линии; опыт приёмщика прощал придуманный код упоминанием, rc=0). Объявляющая позиция установлена замером по ВСЕМ токенам приёмки: вхождений 602 · в вердиктной тройке 598 · вне 4 (все — клаузы **Then**) · токенов 136 · объявлены тройкой 136 · только вне тройки 0. prove.py 190 из 190, код 0 (было 188 при 1 провале); инъекции «диагностика мимо приёмки» и «объявляющая позиция» доказаны, у каждой контроль зелен. Круг M1, kacho-workspace@56935d4d: посылка подтверждена дословным предикатом задачи (правила 134 · приёмка 134 · в приёмке НЕТ CG_DRIVER_BIRTH_TRIPLE_INVALID · без производителя CG_OK); prove.py 188 из 188; run-all.sh 3 из 3; inject.sh 21 из 21; prove_run_progress.py 8 из 8"
---

# ws#502: норму «диагностика берётся из приёмки» не держала ни одна проверка

Норма контура: диагностику правила объявляет **приёмка**, реализация своей не заводит —
иначе контракт пишет исполнитель, а рецензент судит то, чего не утверждал. Норма держалась
**обзором**: ни `selftest/prove.py`, ни `selftest/inject.py`, ни `tests/caselib` не сверяли
множество диагностик правил с множеством диагностик приёмки.

**Класс тихий by construction**, и это главное в нём: код, которого приёмка не объявляет,
матрица кейсов **не предъявляет** — её кейсы выводятся из приёмки, — поэтому такой код не
краснеет ничем и от законного неотличим. Единственное отступление в дереве (ws#494) нашла
честность автора полосы, а не механизм; следующее было бы найдено так же либо не найдено
вовсе.

## Посылка перемерена первым действием — ПОДТВЕРДИЛАСЬ

Предикат задачи прогнан дословно на `56935d4d`:

```
правила объявляют: 134 · приёмка объявляет: 134
в приёмке НЕТ: ['CG_DRIVER_BIRTH_TRIPLE_INVALID']
без производителя: ['CG_OK']
```

**Числа равны, а множества разошлись** — ровно тот случай, ради которого перепись обязана
печатать обе величины: одно число здесь не изменилось бы вовсе.

Второе утверждение задачи («предикат снятия сегодня отвечает „отбор не совпал ни с одной
инъекцией из 42"») тоже подтвердилось дословно, с тем же числом. Единица счёта — **запись
ведомости `INJECTIONS`**; свой замер: `len(inject.INJECTIONS)` → **42**.

## Что заведено — секция `I` в `selftest/prove.py`

Дом выбран приёмкой §6: `selftest/` — сторона **implementation**, она читает
`cglib.registry`; `tests/**` принадлежит integration-tester и в реализацию не заглядывает.
Секция попадает в дешёвую полосу вместе с `prove.py`, поэтому её зовёт `run-all.sh` через
`check-01`, а его — хук отправки. Отдельной точки входа не заведено намеренно: новая точка
потребовала бы строки в ведомости полос `lanes.py`, то есть правки за пределами полосы.

| решение | почему так |
|---|---|
| перепись печатает **пять** величин, из них обе несущие | одно число скрывает ровно тот случай, что был в дереве: 134 и 134 при разошедшихся множествах |
| `codes − spec` — находка; `spec − codes` — **строка переписи** | `CG_OK` производителя не имеет *by construction* (это исход «правило не нарушено», а не правило); смешение направлений дало бы проверку, красную ВСЕГДА, — её отключили бы первой |
| пустой обход — находка «беспредметно» | иначе недосягаемая приёмка дала бы «мимо приёмки 134», а непрочитанный реестр — ноль находок, то есть зелёное на непрочитанном |
| ведомость **самоистекает** | запись, которой нечего исключать, — находка; предикат снятия у записи стоит **командой**, а не словами |
| пустая ведомость **проходит** | пустота и есть её цель; падение на достигнутой цели подталкивало бы держать запись ради зелёного |

Ведомость несла **одну** запись — `CG_DRIVER_BIRTH_TRIPLE_INVALID`, предмет ws#494. Она
покраснеет **сама**, когда круг рецензии по приёмке код объявит: тогда записи станет нечего
исключать, и утверждение `I2` назовёт её по имени.

> [!note] Предсказание сбылось, и абзац выше оставлен как запись, а не как состояние
> Круг ws#494 код объявил, `I2` покраснело на стволе линии, запись снята кругом M2 (ниже).
> **Ведомость пуста.** Абзац переведён в прошедшее время, но не удалён: он назвал предикат
> снятия заранее, и то, что предикат сработал сам, — единственное доказательство, что
> самоистечение было не украшением.

## Пара RED → GREEN

Секция внесена с **пустой** ведомостью и прогнана по настоящему дереву:

```
FAIL I1 … CG_DRIVER_BIRTH_TRIPLE_INVALID — правило объявляет диагностику,
     которой приёмка НЕ объявляет
утверждений: 188 · прошло: 187 · провалено: 1
```

Затем добавлена запись ведомости с предикатом снятия:

```
OK   I1 …
утверждений: 188 · прошло: 188 · провалено: 0
```

Красное пришло **только** от нового утверждения: остальные 187 остались зелёными, то есть
инъекция роняла проверяемое, а не соседа.

## Падучесть доказана на ДВУХ уровнях, и это не дублирование

**Синтетикой внутри секции** — по каждой оси пара «дефект / законный близнец»: код мимо
приёмки → находка **с именем кода**; код, приёмкой объявленный → молчание; запись без
предмета → находка с именем; запись с живым предметом → молчание; пустая ведомость →
молчание; «без производителя» → перепись, а не находка; пустой обход с **обеих** сторон →
«беспредметно», непустой вход → молчание. Односторонняя проба зеленела бы на сверке,
отвергающей всё.

**Инъекцией по настоящему дереву** — `selftest/inject.py`, отбор из предиката снятия задачи:

```sh
python3 scripts/change-graph-gate/selftest/inject.py 'диагностика мимо приёмки'
# инъекций: объявлено 43, к прогону 1
# OK контроль: нетронутое дерево зелено (25 с)
# OK диагностика мимо приёмки … -> покраснело ровно 2 ожидаемых утверждений (21 с)
```

Инъекция меняет **один факт**: ведомость называет не тот код. Следствий два, и оба
принадлежат секции — живой код остаётся без прощения (`I1`), прощение остаётся без предмета
(`I2`). Это и есть две стороны самоистечения, показанные на настоящих данных дерева.

## Что нашлось по дороге — песочница не видела приёмки вовсе

`inject.py` строит песочницу копией **одного** каталога оснастки; воркспейса над ней нет, и
выписанный `../../docs/...` указывал бы в пустоту. Замер в песочнице **без** ручки:

```
путь приёмки: None
перепись: диагностик правил 134 · диагностик приёмки 0 · мимо приёмки 134 …
```

То есть контрольный прогон краснел бы на **раскладке песочницы**, а не на инъекции. Путь
поэтому **выводится** обходом вверх, а песочнице передаётся готовым через
`CG_ACCEPTANCE_DOC` (`prove.acceptance_environment`). Ручка **переживает вложение**:
`prove_run_progress.py` кладёт песочницу в песочницу, и на втором этаже выводить путь
неоткуда, — поэтому уже стоящее в окружении значение не пересчитывается.

Ветвь «беспредметно» здесь не украшение: она сработала на настоящем условии, а не только на
синтетике.

## Круг M2: ведомость самоистекла, и распознаватель судил ПО СЛОВУ

Круг рецензии дал два предмета. Оба перемерены первым действием, оба **подтвердились**.

### Дефект 1 — прощение пережило свою причину

На `release/sdd-closeout@b5fa9696` `prove.py` был красен ровно одним утверждением:

```
FAIL I2 у каждой записи ведомости есть предмет: прощение не переживает своей причины
утверждений: 188 · прошло: 187 · провалено: 1
```

Предикат снятия, записанный **у самой записи**, ответил `2` вместо нуля: круг ws#494 код
объявил, прощать стало нечего. Исход выбран не мной — его предписывала сама запись: **снять**.
Ведомость стала пустой и на пустой **проходит**, что и требовалось: превращать достигнутую
цель в поломку значило бы подталкивать держать запись ради зелёного.

### Дефект 2 — «объявлена приёмкой» означало «встречается в файле»

Замечание приёмщика: заголовок `I1` был **шире измеряемого**. Опыт воспроизведён дословно —
придуманный код, приписанный к приёмке абзацем «упоминание, а НЕ объявление», **прощался**:
находок 0, `rc=0`. Живых экземпляров в дереве не было, но дом стиля корпуса именно такой:
снятое хранится **прозой** («здесь стояло …»), и первый же такой абзац о снятом коде молча
простил бы правило, которое этот код продолжает производить. Класс — `testing.md` §«Гейт на
класс», п. 4: гейт судил **текст**, а не объявление.

**Форма объявляющей позиции установлена ЗАМЕРОМ по всем токенам, а не догадкой:**

| величина | значение |
|---|---:|
| вхождений `CG_…`/`CGA_…` в приёмке | 602 |
| из них в вердиктной тройке `<КАТЕГОРИЯ> · <КОД> · exit <N>` | 598 |
| вне тройки — все четыре суть клаузы `**Then**` | 4 |
| уникальных токенов | 136 |
| из них объявлены тройкой хотя бы раз | **136** |
| объявлены **только** вне тройки | **0** |

Позиция оказалась выразима **однозначно**: 136 из 136, и ни один код не держится на тех
четырёх упоминаниях, — поэтому взят полный исход, а не минимальный. Тот же опыт приёмщика
теперь даёт **находку**, и находка говорит, что именно неверно: «приёмка код УПОМИНАЕТ, но
НЕ ОБЪЯВЛЯЕТ».

**Два диагноза под одним утверждением, и они различены текстом**: «нет вовсе» значит, что
контракт написала реализация; «упомянут, но не объявлен» — что приёмка код сняла **либо**
что она завела форму объявления, которой проба не знает. Обе причины требуют человека, но
чинятся в разных местах.

**Граница названа словами** — в шапке секции `I` и в `selftest/README.md`: секция судит
**позицию**, а не смысл; она не читает, что́ проза о коде говорит, и не отличает объявление
от его цитаты. Свою предпосылку она проверяет с двух сторон: полный отказ («приёмка перешла
на другую форму») даёт ноль объявленных и находку «беспредметно», частичный виден шестой
величиной переписи — «из них упоминаний вне объявляющей позиции».

### Падучесть — пара в секции и ДВЕ инъекции, каждая об одном факте

В секцию добавлена пара по оси **разбора**, которой синтетика из готовых множеств дать не
могла: вход у неё — **текст**, и стороны различаются ровно позицией одного кода (`I8` /
`I8-близнец`).

```sh
python3 scripts/change-graph-gate/selftest/inject.py 'диагностика мимо приёмки'
# OK контроль: нетронутое дерево зелено (25 с)
# OK … -> покраснело ровно 1 ожидаемых утверждений (24 с)

python3 scripts/change-graph-gate/selftest/inject.py 'объявляющая позиция'
# OK контроль: нетронутое дерево зелено (20 с)
# OK … -> покраснело ровно 1 ожидаемых утверждений (20 с)
```

Прежняя инъекция роняла **два** утверждения, потому что переименование живой записи меняло
два факта разом. Ведомость пуста, лишать прощения нечего — и обе инъекции стали
односоставными: каждая роняет **названное**, а не «что-нибудь из секции» (`testing.md`
§«Гейт на класс», п. 2в).

## Чего эта задача НЕ закрывает

~~Само отступление `CG_DRIVER_BIRTH_TRIPLE_INVALID` — предмет **ws#494**~~ — **закрыто**:
круг по приёмке код объявил, ведомость самоистекла кругом M2.

Остаётся: `run_matrix.py` красен двумя кейсами (`SDD-1-AUTH-09`, `SDD-1-DRIVER-04` — фикстур
нет), и это предмет **другой полосы**, не этой. По полосе `selftest/prove.py` набор
`change-graph-gate` был провален **2 проверками из 6**, стал — **1**.

## Затронутые сущности vault

- [[KAC/issue-504-ws]]
- [[lessons/checks-with-form-but-no-substance]]

#kac
