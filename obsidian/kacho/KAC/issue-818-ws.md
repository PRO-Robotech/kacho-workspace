---
title: "ws#818: дайджест записей ревью — в форме git diff --full-index"
aliases:
  - issue-818-ws
ticket_id: 818
category: kac
status: test
type: fix
repos:
  - kacho-workspace
areas:
  - .claude/agents
  - scripts/change-graph-gate
prs:
  - https://github.com/PRO-Robotech/kacho-workspace/pull/836
  - https://github.com/PRO-Robotech/kacho-workspace/pull/841
issue_url: https://github.com/PRO-Robotech/kacho-workspace/issues/818
opened: 2026-09-23
closed: 2026-09-26
tags:
  - kac
  - fix
verified_against: "PRO-Robotech/kacho-workspace: голова задачи c376181fe35 — предок коммита слияния волны 27eb0775252 в ветку эпика 771 и не предок origin/main 6eddaa5e9c9 (`git merge-base --is-ancestor`), 2026-09-26. Пункты DoD тела задачи мной не перемерялись; 2026-09-26: трекер — закрыта 2026-09-26T09:34:23Z, меток status:* нет (`gh issue view`); коммит слияния волны 27eb0775252 — в origin/771, не в origin/main (`gh api compare`)"
---

# ws#818: дайджест записей ревью — в форме git diff --full-index

**Состояние на момент записи**: `test` — 2026-09-26. Задача **закрыта** на трекере 2026-09-26T09:34:23Z вместе с волной [[KAC/issue-805-ws|#805]]: запрос волны PR #841 влит в ветку эпика `771` (координата, не живая ссылка), коммит закрытия — слияние волны `27eb0775252`; метка `status:test` снята, метки сейчас: `bug`, `P1`, `size:S`, `area:tooling`, `release:identity-own`. В `main` работа **не** доехала, поэтому состояние записки `test`, а не `done`: `done` — посадкой эпика #771 в ствол.

## Что и зачем

Записи ревью снимали дайджест содержимого в форме, которая зависит от длины сокращённых хешей. Теперь дайджест берётся `git diff --full-index`, и check-05 не слеп к форме `git -C <копия> diff`.

## Путь в волну

| что | координата |
|---|---|
| голова задачи | `c376181fe35` → слияние в ветку сборки `835` `6dc71d50818` — «#835 merge #818: check-05 не слеп к форме git -C <копия> diff» |
| запрос сборки | PR #836 `835` → `805`, голова `2345ef6cd86`, коммит слияния `f46878626e8`; прогоны головы — 1 прогон, `success` |
| запрос волны | PR #841 `805` → `771`, голова `aa378730dcb`, коммит слияния `27eb0775252`; прогоны головы — 1 прогон, `success` |

## DoD

Из тела задачи, раздел «ПРЕДИКАТ.» (перечислено как есть, мной не перемерялось):

- Проба гейта: новая запись, чья команда дайджеста `git diff A...B | sha256sum`, даёт красное с путём записи. Близнец с `git diff --full-index A...B | sha256sum` и тем же прочим содержимым даёт зелёное. Пустой обход (ноль записей) даёт красное, а не зелёное. Гейт печатает объём осмотренного.
- `git grep -nE 'git diff[^|]*\| *sha256sum' -- '.claude/agents/'`: каждое совпадение несёт `--full-index`, без него совпадений 0.
- Ни одна запись, добавленная после границы, не несёт команду дайджеста без `--full-index`: перепись по дельте ветки против `origin/main` даёт 0.

Сверено мной:

- [x] голова задачи — предок коммита слияния волны `27eb0775252` в ветку эпика `771`;
- [ ] предмет в стволе: `main` — посадкой эпика #771.

## Затронутые сущности vault

Поля «затронуто в vault» в возвратах исполнителей по этой задаче нет.

- [[KAC/issue-805-ws]] — волна.

#kac #fix
