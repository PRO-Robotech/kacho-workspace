# KAC-127: CI/CD — DockerHub multi-arch image build

**Status**: in-progress (newman-e2e authz gate ЗЕЛЁНЫЙ — Round 3, 2026-05-20)
**Type**: feature
**Repos**: kacho-iam, kacho-vpc, kacho-compute, kacho-api-gateway, kacho-ui, kacho-deploy
**PRs**: (по мере merge)
**YT**: https://prorobotech.youtrack.cloud/issue/KAC-127

## Что и зачем

Все Kachō-сервисы, производящие Docker-образ, должны собирать multi-arch
(amd64 + arm64) образы и публиковать их в DockerHub. Раньше образы собирались
только локально (`kind load docker-image kacho-*:dev`), не было опубликованных
артефактов для production-деплоя.

Добавлен workflow `docker-build.yml` в 5 репозиториев (kacho-iam, kacho-vpc,
kacho-compute, kacho-api-gateway, kacho-ui) — паттерн адаптирован из
`PRO-Robotech/sgroups` / netguard reference. 3 job'а: amd64 (ubuntu-latest) +
arm64 (self-hosted) + multiarch manifest push. Secrets `DOCKERHUB_USERNAME` /
`DOCKERHUB_TOKEN`. Триггеры — push в `main` / `KAC-*` / тег `v*`.

Polyrepo-специфика: Go-сервисы (iam/vpc/compute/api-gateway) — Dockerfile делает
`COPY kacho-corelib` / `COPY kacho-proto`, поэтому workflow чекаутит siblings в
один каталог (build context = workspace-dir). kacho-ui — self-contained
(context = `.`).

kacho-deploy: umbrella Helm values переключены на DockerHub registry —
`global.imageRegistry` / `global.imageTag` (configurable); per-service `image:`
overrides в `values.yaml` (DockerHub default), `values.dev.yaml` (local `:dev`
образы для kind), `values.prod.yaml` (DockerHub + `${KACHO_IMAGE_TAG}` envsubst).

docker-build.yml дополняет существующие `ci.yaml` (тест/линт), не заменяет.

## Dev-stand re-deploy (kind) — blockers fixed (2026-05-20)

Re-deploy `make dev-up` на kind-кластере `kacho` после M10 Zitadel-removal.
Исходные 4 blocker'а + 6 доп. config/template-дефектов, найденных по ходу:

**Fixed (kacho-deploy, ветка KAC-127):**

- **A — OpenFGA migration deadlock**: `openfga.datastore.migrationType: initContainer`
  (был `job` + `wait-for-migration` init-container → `helm --wait` deadlock).
- **B — kacho-iam wait-for-zitadel**: уже удалён M10-cleanup'ом (verified).
- **C — kacho-ui образ**: `Makefile` target `build-ui` (docker build + kind load),
  вызывается из `dev-up`; `ui` добавлен в `reload-svc`.
- **D — corelib branch mismatch**: worktree `kacho-corelib-KAC-127` удалён,
  `project/kacho-corelib` переключён на ветку `KAC-127` (есть `authz/listobjects.go`).
- **+ third-party image registry**: `global.imageRegistry: ""` на dev — `values.yaml`
  ставил `docker.io/prorobotech`, что ломало Ory/OpenFGA/bitnami образы.
- **+ Ory migration hooks**: `kratos`/`hydra` `automigration.type: initContainer`
  (тот же pre-install-hook deadlock, что у OpenFGA).
- **+ kratosSelfServiceUI**: `enabled: true` — kacho-ui nginx безусловно проксирует
  на этот upstream, при disabled nginx падает.
- **+ OPA image**: `1.0.0-rootless` → `1.0.0-envoy-static` (несуществующий тег;
  нужен opa-envoy для `envoy_ext_authz_grpc` плагина).
- **+ openfga/cli tag**: `0.6.5` → `v0.7.13` (тег без `v`-префикса не существует).
- **+ opaSidecar.iamInternalHost**: `kacho-umbrella-iam-internal` → `kacho-iam-internal`.

**Access-matrix blockers #37/#38/#39 — FIXED (2026-05-20):**

> Commits: kacho-proto@4d4f394, kacho-iam@ae16aa7, kacho-deploy@42e1196
> (на ветках `KAC-127`, входят в PR'ы #20 / #14 / #36). GitHub-issues
> kacho-deploy#37/#38/#39 — closed. Стенд re-deployed: 20/20 pods Running,
> helm release `deployed` (rev 14).

- **#38** (OpenFGA model) — distroless `openfga/cli` нет `/bin/sh`,
  `fga model transform` пишет только в stdout → init-container `dsl-transform`
  падал. Fix: init-container удалён, DSL→JSON transform перенесён на commit-time
  (ConfigMap `openfga-model-stub` несёт оба ключа — `model.fga` + `model.json`);
  bootstrap-job (curlimages/curl) читает `model.json` напрямую. `make
  openfga-model-json` regen-target. Также fix DSL-бага: `source_ip_in_range`
  condition — `client_ip in allowed_cidrs` → `allowed_cidrs.exists(c,
  client_ip.in_cidr(c))` с `list<string>` (OpenFGA v1.14 `in_cidr` берёт
  string-CIDR, не list). `openfgaBootstrap.enabled: true`.
- **#37** (Hydra config) — remap под `oryd/hydra:v26.2.0` schema:
  `oauth2.dpop`/`oauth2.mtls` удалены (в v26.2.0 — per-client metadata,
  не глобальный config); `refresh_token_rotation*` →
  `oauth2.grant.refresh_token.rotation_grace_period`;
  `grant.authorization_code` удалён (PKCE → `oauth2.pkce`); `tracing` `endpoint`
  → `server_url`; `urls.identity_provider.publicly_writable` удалён. Validated
  против hydra v26.2.0 binary.
- **#39** (OPA bundle) — kacho-iam отдаёт OPA-bundle по HTTP на :9092
  (`OPABundleHTTPHandler`, routes `/opa/v1/bundle.tar.gz` + `/opa/v1/revision`,
  смонтирован на hooks-listener). Порт :9092 экспонирован в Service
  `kacho-iam-internal` (`http-hooks`). OPA sidecar config → `:9092`;
  in-tarball signing сделан опциональным (`opaSidecar.bundleSigningEnabled`,
  dev=false — bundle пока unsigned). Hydra hook URL'ы тоже исправлены
  `kacho-iam:9091` → `kacho-iam-internal:9092`. `opaSidecar.enabled: true`.

## Затронутые сущности vault

- (CI/CD + dev-stand инфраструктура — vault-resources не затронуты; ресурсная модель не менялась)

## Acceptance / Definition of Done

- [x] docker-build.yml в 5 репо, actionlint clean
- [x] kacho-ui Dockerfile собирается локально (50MB nginx-образ)
- [x] kacho-ui .dockerignore добавлен
- [x] kacho-iam Dockerfile собирает 3-й binary jwks-rotator
- [x] Helm values переключены на DockerHub (configurable registry/tag)
- [x] helm lint clean (dev values); 0 hardcoded credentials; 0 yandex
- [ ] secrets DOCKERHUB_USERNAME / DOCKERHUB_TOKEN установлены (user-action)
- [ ] PR'ы merged в main

## Связанные тикеты

- (нет)

#kac #feature

---

## Раунд: authz integration-debugging (2026-05-20)

Чинились 3 бага, блокирующие access-control matrix (6 моделей).

**Исправлено и запушено (ветка `KAC-127` в 4 репо):**
- **#16 kacho-iam** (b3e68fb) — `UpsertFromIdentity` писал throwaway `ids.NewID()`
  в `Operation.metadata.user_id` до async `doUpsert`; на conflict-path возвращался
  другой existing id. Фикс: `resolveUserID` синхронно резолвит id до Operation.
  + 2 unit-теста. **Closed.**
- **#104 kacho-vpc** (b1536bc) — public interceptor chain без `grpcsrv.UnaryPrincipalExtract`
  → authz видел `user:bootstrap` для всех. Фикс: PrincipalExtract первым в chain. **Closed.**
- **#42 kacho-deploy/api-gateway** (fe97a37, c7444bb) — добавлен first-class `authz`
  chart-блок + env-wiring. Config-сторона корректна (`authz-mw wired` в логах).
- **authz-fixtures setup.sh** (6e0c5e3) — Account/AccessBinding создавались под
  bootstrap-JWT, нарушая `RequireOwnerMatchesPrincipal` / `RequireSelfGrant`.
  Фикс: каждый ресурс создаётся под JWT владельца/субъекта; `LookupSubject{externalId}`
  для id-резолва (как api-gateway).

**Глубокий блокер (новый issue [[../../../../project/kacho-api-gateway|kacho-api-gateway#20]]):**
Phase 3 per-RPC authz **структурно не завершён на REST-поверхности**:
`resolveRestFQN` — заглушка (`//iam/v1/accounts` вместо gRPC-FQN), permission catalog
заполнен на 28/303. authz-mw enabled → catalog-miss-deny бриккует весь стенд;
disabled → DENY-кейсы не enforced.

**FINAL access-matrix** (authz-mw disabled — иначе стенд мёртв):
- authz-deny: 826 assert / **511 failed** / 300 cases — ALLOW 63✓/7✗, DENY 58✓/168✗, EMPTY 4✓.
- authz-sa: 75 assert / 18 failed / 30 cases — 12✓/18✗.
- = **идентично baseline** — #16/#104/#42-config были необходимы, но не были
  блокером matrix. Matrix НЕ может стать green без завершения Phase 3 authz (#20).

#kac #feature #fix

---

## KAC-127 #20 resolution — per-RPC authz completed (2026-05-20)

**Status**: per-RPC authz enabled + working; access-matrix 821/901 (91%).

**Problem 1 — RestRouter wired** (`kacho-api-gateway` 195e8dc):
`NewRestRouter` + `rest_route_table_gen.go` (322 routes from proto
`google.api.http`) — REST path → gRPC FQN now resolves. `ExtractFromHTTP`
also reads+restores the JSON body for body-scoped Create/Update.

**Problem 2 — permission catalog populated** (`kacho-proto` 300fd20…e843436):
264/264 RPCs annotated (was 28/264); warnings file empty. Internal* + streams
+ Operation + Role-catalog-read + AccessBinding.Create exempted.

**Problem 3 — AccessBinding grant authority** (`kacho-iam` 232d5be):
`RequireSelfGrant` replaced by `requireGrantAuthority` (owner-of-scope OR FGA
`admin` on scope). Account/Project create write FGA owner/hierarchy tuples;
`deny_cross_tenant.rego` fixed to fire only on positive evidence; FGA-400 →
clean deny; role→relation resolved from role NAME.

**Matrix**: baseline 511/826 failed (authz-deny) → **59/826 failed** (88%
reduction). Combined 901 assertions, 80 failed, **821 passing (91%)**.
Per-model: anonymous→403✓, no-grant→403✓, authorized→200✓, peer/cross-tenant
→403✓.

**Remaining 80 failures — documented, require separate work:**
1. **IAM handler legacy scope-filters** (~34): `project/group/sa/ab` Get/
   Update/Delete handlers keep a pre-KAC-127 owner-only filter that returns
   404 even when the api-gateway FGA Check allowed → authorized users
   false-404. Fix: relax those handler filters (the gateway now authz's).
2. **FGA model gap** (~12): `group`/`service_account`/`access_binding`/`user`
   have no viewer/editor/admin cascade in the OpenFGA model — un-checkable
   resource types. Fix: extend the authorization model.
3. **SA/API-token suite** (21): unrelated to the 3 problems — VPC-network
   probes (no fixture networks) + token-revocation 401 expectations.
4. **List scope-filter semantics** (~6 EMPTY): test-design ambiguity between
   `ACCT-LS` (account-scope DENY) and `USR-LS` (EMPTY 200).

## KAC-127 UI refactor — снос resource-manager/folder + IAM registry-driven (2026-05-20)

**Repo**: kacho-ui (ветка KAC-127). 3 коммита: `9fdfa1b` (этап 1),
`0da26a5` (этап 2), `1047c1a` (этап 3).

**Этап 1 — снос backward-compat folder/org/cloud** (57 файлов):
`context-store.ts` переписан на чистую модель Account/Project (удалены
FolderRef/OrgRef/CloudRef, useFolderStore, folderStoreApi, производное
folder/org/cloud, v1-миграция localStorage). `folder-store.ts` удалён,
FolderRequiredEmpty→ProjectRequiredEmpty. resource-registry: scope
folder→project, template-ctx {projectId,accountId}, FIELD_PROJECT_ID,
refProjectScoped, wire-параметр folder_id→project_id (backend kacho-vpc
давно на project_id). App.tsx: удалены legacy-редиректы /folders/* и
LegacySegmentRedirect. nginx: убраны proxy /resource-manager/,
/organization-manager/.

**Этап 2 — IAM registry-driven**:
Account/Project/ServiceAccount → generic ResourceListPage/DetailPage +
modal-flow `?modal=` (по аналогии с VPC). Account — global-scoped;
Project/ServiceAccount — account-scoped (backend ListProjects/
ListServiceAccounts требуют account_id) → в IAM-секцию добавлен
Account-селектор (IamLayout), IamScopedListShell фильтрует по выбранному
Account. ResourceListPage получил prop `parentValue`. Кастомные
AccountsPage/ProjectsPage/ServiceAccountsPage удалены. Role оставлен
кастомным (regex permissions-editor + read-only system-роли).

**Этап 3 — рестайл кастомных IAM-страниц**: Roles/Groups/AccessBindings/
Access/Users — модалки приведены к норме (ширина 860, labelCol 200),
заголовки level 3.

**Проверки**: tsc ✓, build ✓, vitest 83/83 ✓.

**Не сделано (отдельная задача)**: e2e/playwright-сьют (`e2e/*.spec.ts`)
устарел — построен на упразднённой модели Org/Cloud/Folder и standalone
create-страницах (предшествует modal-flow). Требует переписывания против
dev-стенда.

#kac #feature #fix

## Final deploy + access-matrix run (2026-05-20, evening)

Re-deployed kind стенд с KAC-127 authz fixes, прогнал обе newman-сьюты.

**Integration blockers найдены и исправлены (7 commits)**:
- `kacho-proto eb67c55` — FGA model: `iam_user` relation `self` → `subject`
  (`self`/`this` — reserved keywords OpenFGA → WriteAuthorizationModel 400).
- `kacho-deploy 0c65e19` — regen openfga-model-stub configmap.
- `kacho-api-gateway 10e89b5` — (1) SA/api-token authn: резолв principal из
  `kacho_principal_type=service_account`+`kacho_sa_id` (sub SA-токена ≠ User
  external_id → LookupByExternalID мимо → anonymous); (2) `<exempt>` пропускает
  authz, НЕ authn — anonymous на exempt-List теперь deny; (3) HTTP scope-extract
  query-param: snake_case `account_id` vs camelCase `?accountId=` → spurious
  `account:*` wildcard.
- `kacho-vpc 43a9a79` + `kacho-compute c2ac195` — OperationService Get/Cancel
  → `Public` (нет `vpc_operation`/`compute_operation` типа в FGA → poll deny).
- `kacho-iam 90021db` — `InviteUserUseCase.WithOpenFGA` теперь и `uc.authz` (был
  no-op stub → Invite всегда deny для account-admin).
- `kacho-workspace 99e107c` — fixtures: SA/api-token steps, owner-based seed nets.

**FINAL matrix**: authz-deny 820 assert / 50 fail; authz-sa-apitoken 75/6.
Combined **895 assertions, 839 pass, 56 fail**. Прогрессия 511→80→83→**56**.
ANON: 150/150 pass (anti-anonymous fix полный).

**Remaining 56 fail — 2 documented issues, не product-blockers**:
- [[../../README|workspace#22]] — per-resource FGA hierarchy tuples не пишутся
  на Create (`iam_user`=0, `vpc_network`=0 tuples; `project`/`iam_service_account`
  пишутся ✓). ~25 fail — per-resource Get/Delete `no path`. Архитектурный gap:
  vpc/iam Create-worker должен эмитить `<obj>#<parent-rel>@<parent>`.
- [[../../README|workspace#23]] — `authz-deny.py` stale EXPECT vs KAC-127
  Problem-3 (account-owner self-grant РАЗРЕШЁН by design) + `AB-LS-*` зовёт
  несуществующий `AccessBindingService.List`. ~24 fail — test-bug.

#kac #fix

## Round 2 — write-side FGA fix + matrix → 8 failed (2026-05-20)

Исправлены workspace#22 + #23, matrix-прогресс **56 → 8** (839→854/862 = 99.1%).

**workspace#22 (write-side FGA emission) — FIXED**:
- `kacho-iam` — `UpsertFromIdentityUseCase.WithOpenFGA`: bootstrap-path
  эмитит весь tuple-граф (account owner/admin + `iam_user`/`project`
  hierarchy + project-scoped AB hierarchy); `InviteUserUseCase` эмитит
  `iam_user#account@account` для свежих invitee-rows.
- `kacho-vpc` — новый write-side: `clients.OpenFGAWriteClient` (HTTP /write,
  идемпотентный) + `fgawrite` port; все 7 Create use-case'ов эмитят
  `vpc_<resource>:<id>#project@project` после `w.Commit()`. Config
  `authz.tuple-write.*`.
- FGA census ПОСЛЕ: `iam_user`=6, `vpc_network`=2 (было 0/0). ✓

**workspace#23 (stale test expectations) — FIXED**: esc-self-grant/hijack
per-account ALLOW, `AB-LS-*` удалены, user-list ALLOW, garbage-id Get/Delete
DENY (per-resource Check `no path`), USR-GT split nob/inv, esc-custom-role
ALLOW.

**Остаток 8 — 2 новых issue (НЕ #22/#23)**:
- [[../../README|workspace#24]] — iam `authzguard` owner-guard слишком строг:
  блокирует non-owner Project/Account Update хотя gateway FGA Check allow (3).
- [[../../README|workspace#25]] — List RPC не `<exempt>`: per-RPC Check deny
  до scope-filter (4 vpc/iam List EMPTY).

Fixture-bootstrap: `tests/authz-fixtures/setup.py`.

#kac #fix

## Round 3 — newman-e2e CI ЗЕЛЁНЫЙ (2026-05-20)

Доведён `newman-e2e.yml` (kind + helm umbrella, GitHub Actions) до зелёного.
Прогресс остатка: 10 failed → 2 failed → **0 failed**.

**Bug 1 (invite-flow) — было исправлено `kacho-iam 218e6cb`** до этого раунда:
invite project-grant биндится к gateway-resolved identity row. authz-deny
suite зелёный (780 assert / 0 fail).

**Bug 3 (api-token authn) — `kacho-api-gateway 0faeb7f`+`577f654`**: SAKeyService
на REST mux + permission-catalog sync. revoked/malformed/expired-token кейсы
дают 401 (`AUTHZ-APITOK-REVOKED/MALFORMED/EXPIRED-*` все ✓) — authn-pre-gate
работает.

**Bug 2 (vpc write-side FGA `no path`) — root cause найден диагностикой
`kacho-vpc 1d7faca` и исправлен `kacho-vpc aa65faf`**:
- Симптом: `AUTHZ-{SA,APITOK}-NET-GT-A1` ALLOW-кейсы → 403 `no path` на
  `vpc_network:<seed-net-id>`.
- Root cause: `Network.Create` worker валидирует project через peer-call
  `iam ProjectService.Get`. Этот RPC несёт tenant scope-filter (KAC-122
  CRIT-41) → NOT_FOUND для anonymous/не-owner. vpc НЕ форвардил Principal
  на исходящую gRPC metadata → peer видел anonymous → NOT_FOUND →
  `doCreate` падал на project-exists precheck → Network row НЕ
  коммитился → `fgawrite.Emit` не достигался → нет `vpc_network#project`
  hierarchy tuple → каждый per-resource Check `no path`.
  `operations.Run` маскировал worker-error как `INTERNAL` без лога →
  падало молча. Диагностика 1d7faca (recover+log вокруг doCreate) и
  показала реальную причину: `ERROR network create operation failed ...
  err="Folder with id prj... not found"`.
- Fix `aa65faf`: `ProjectClient.Exists` / `GetCloudIDFromProject` теперь
  через `withPrincipalMD(ctx)` форвардят ctx-Principal как
  `x-kacho-principal-*` outgoing metadata. baggage.Extract
  (`context.WithoutCancel`) сохраняет Principal в worker-ctx → peer
  резолвит реального account-owner → scope-filter проходит →
  Network.Create успешен → FGA tuple пишется.

**Cleanup** (`kacho-vpc c2133a8`, `kacho-deploy 1bb01dc`): FGA-emit
INFO-диагностика → Debug; убран heavyweight `kubectl run` OpenFGA
store-dump из failure-dump шага. recover+log error-обёртки (vpc 1d7faca,
iam 5768919/2f8f6bf) оставлены — silently-masked Operation worker failure
это реальный observability-gap, не debug-scaffold.

**FINAL CI** — run `26185430723` (workflow_dispatch, ref KAC-127):
- `authz-deny`: 780 assertions, **0 failed**, 288 cases.
- `authz-sa-apitoken`: 74 assertions, **0 failed**, 30 cases.
- job conclusion **success** — `assert authz suites green` зелёный.

#kac #fix

---

## Раунд: cross-repo newman trigger (2026-05-20)

Newman authz E2E (854/854) жил только в `kacho-deploy/newman-e2e.yml` —
PR authz-сервисных репо его не прогоняли (их `ci.yaml` — только per-service
unit/integration). Изменение authz-логики в PR `kacho-iam`/`kacho-vpc`/
`kacho-compute`/`kacho-api-gateway` могло сломать access-matrix незаметно.

Закрыто через `repository_dispatch`:

- `kacho-deploy/newman-e2e.yml` — добавлен trigger `repository_dispatch`
  type `newman-e2e` + шаг `resolve sibling refs`: читает `client_payload`
  `{repo,ref,sha,source}`, переопределяет checkout именно этого sibling'а
  на ветку под тестом, остальные siblings — pin `KAC-127`. Добавлен
  `.github/workflows/README.md`.
- 4 authz-репо — новый `.github/workflows/newman-trigger.yml`: на PR / push
  в `KAC-127` шлёт `repository_dispatch` в kacho-deploy, находит запущенный
  run (по UTC-cutoff, `repository_dispatch` не возвращает run id), поллит и
  зеркалит conclusion. Шаг `guard` фейлит явно если secret не задан.
  Cross-repo секция дописана в `workflows/README.md` каждого репо.

Required secret: **`WORKFLOW_DISPATCH_TOKEN`** (classic PAT scope `repo`, либо
fine-grained Actions=rw + Contents=read на kacho-deploy) — user-action:
`gh secret set WORKFLOW_DISPATCH_TOKEN --repo PRO-Robotech/<repo>` во все 4
authz-репо. `GITHUB_TOKEN` cross-repo dispatch не умеет.

Commits (ветки `KAC-127`): kacho-deploy@85808f4, kacho-iam@54e5d70,
kacho-compute@8017e27, kacho-vpc@51b6468, kacho-api-gateway@681f89c.
actionlint — clean на всех 5 workflow (поймал `github.head_ref` injection —
исправлено через env-var). Все 4 `newman-e2e (cross-repo)` зарегистрированы
и active.

---

## Раунд: Phase 7 gRPC surface (2026-05-20)

Phase 7 service-layer (JIT/PIM, break-glass, access-reviews, GDPR erasure)
был реализован (`kacho-iam a6caf51`), но без gRPC-поверхности — handlers и
proto service-defs отсутствовали. Этот раунд добавил тонкие handlers поверх
существующих use-case'ов.

**kacho-proto** — новые service-defs в `proto/kacho/cloud/iam/v1/`:

- `jit_eligibility_service.proto` — `JITEligibilityService` (Create / Get /
  List / Update / Delete — CRUD only).
- `break_glass_service.proto` — `InternalBreakGlassService` (RequestBreakGlass
  / ApproveBreakGlassA / ApproveBreakGlassB / DenyBreakGlass /
  ListPendingBreakGlassRequests / GetBreakGlassRequest).
- `access_review_service.proto` + `access_review_campaign.proto` —
  `AccessReviewService` (ScheduleAccessReview / CancelAccessReviewCampaign /
  GetAccessReview / ListAccessReviews / ListAccessReviewItems /
  ApproveReviewItem / RevokeReviewItem). Campaign / review / item — distinct
  aggregates; `AccessReviewCampaign` — новый message под service-local
  struct.
- `gdpr_erasure_service.proto` — `GdprErasureService` (RequestErasure /
  CancelErasureRequest / GetErasureRequest / ListErasureRequests).
- `access_review.proto` — additive enum `CANCELLED = 5` (proto-fidelity для
  `domain.AccessReviewStatusCancelled`).
- `buf lint`/`generate` clean; `buf breaking` — только pre-existing
  resource-manager deletions (KAC-124), новые файлы ничего не ломают.

**kacho-iam** — тонкие handlers `internal/apps/kacho/api/{jit_eligibility,
break_glass,access_review,gdpr_erasure}/handler.go` + shared `phase7common/`
(MapErr + SyncOperation). Phase 7 use-case'ы синхронные → handler синтезирует
terminal (`done=true`) `operation.Operation` (контракт #9). Каждый handler
определяет узкий handler-local port → unit-тест через fake. Wiring —
`cmd/kacho-iam/phase7_wiring.go`: JITEligibility/AccessReview → public :9090;
InternalBreakGlass/GdprErasure → internal :9091 (запрет #6).

### Known-gaps — DROPPED (нет foundation)

- **ComplianceReportService.GenerateAccessReport** — нет service-layer
  use-case вообще. Требует отдельный acceptance + use-case +
  report-generation logic. Не делалось — будущий тикет.
- **ApproveJITActivation** — `JITService.ApproveJIT`
  (`phase7_jit_service.go:343`) — explicit stub (`return errors.New(...)`),
  JitPending flow не построен. RPC был бы permanently-broken — НЕ
  экспонирован. `ActivateJIT` живёт на AccessBindingService (acceptance §6),
  не на JITEligibilityService. Будущий тикет: достроить JitPending +
  AccessBinding TransitionStatus CAS, затем добавить RPC.

#kac #feature
