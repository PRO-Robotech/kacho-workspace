---
title: "«Смёржена ли ветка» решается слиянием без рабочего дерева — и его вывод не строка"
aliases:
  - разбор веток на влитые
  - merge-tree write-tree предикат
  - is this branch merged
category: lesson
status: stable
tags:
  - kac
  - conventions
opened: 2026-08-11
---

# «Смёржена ли ветка» — предикат уровня дерева, а не графа

**Задача.** Десятки веток; надо разделить их на «работа в стволе, ветка мусор» и «работа не
влита, надо доделать или удалить осознанно». Граф на этот вопрос не отвечает: схлопнутое
слияние, черри-пик и переписанная история дают новый хеш, поэтому вершина ветки перестаёт
быть предком ствола (««0 коммитов впереди» значит смёржена», «команда мёржа, пушившая безусловно»).

**Что НЕ работает, и почему каждый способ выглядит убедительно:**

| Способ | Почему обманывает |
|---|---|
| `git branch --merged` | отвечает про граф, а не про содержимое |
| `git cherry` (patch-id) | требует ПОБАЙТОВОГО совпадения патча; ствол применил то же в другом контексте — «не найдено» |
| сравнение сообщений коммитов | сообщения переписываются по дисциплине публичного репозитория, и тогда своя же работа читается как чужая |
| `git diff base..branch` + сверка файлов | при переписанной истории общий предок уезжает далеко назад, и в «файлы ветки» попадает пол-дерева |
| `git diff main...b \| git apply --reverse --check` | ловит только точное совпадение контекста; ствол ушёл вперёд по тем же файлам — «расходится» на всём |

**Что работает.** Слияние без рабочего дерева:

```
git merge-tree --write-tree main <ветка>      # первая строка — ID дерева слияния
git diff --shortstat main <ID дерева>          # пусто = ветка НЕ привнесёт ничего
```

Пустой diff означает ровно то, что нужно: содержимое ветки уже в стволе, ветка мусор.

**Ловушка, на которой предикат врёт уверенно.** При конфликтах вывод `merge-tree`
**многострочный**: первая строка — дерево, дальше перечень конфликтующих путей. Взяв вывод
целиком за ID, получаешь неразрешимый аргумент, `git diff` тихо падает, а скрипт печатает
«ничего не привнесёт» — **для всех конфликтующих веток сразу**, то есть ровно для тех, где
ответ важнее всего. Признак подделки: «влиты все до одной». Лечится `head -1` плюс отдельным
счётчиком конфликтных строк — и он же обязан быть НАПЕЧАТАН, иначе тихий конфликт снова
станет «чисто» («объявленный предикат может быть негодным и лгать уверенно»).

**Границы.** На ветках со старой (переписанной) историей предикат отвечает «конфликт», а не
«да/нет»: слияние там бессмысленно by construction. Такие разбираются **по предмету** —
берёшь то, что ветка вводила (файл, гейт, приёмку), и спрашиваешь дерево, есть ли этот
предмет сегодня. Отсутствие файла при этом **не** доказывает потерю: подсистему могли снять
решением.

**Перед удалением.** Снять перепись «ветка → вершина → дата → исход» в файл вне репозитория:
пока объекты живы, по хешу восстанавливается всё. Работу, оказавшуюся невлитой, сначала
посадить, и только потом удалять ветку — иначе удаление становится необратимым в тот момент,
когда сборщик мусора дойдёт до объектов.

**Где наблюдалось.** Разбор 122 локальных веток монорепо 2026-08-11: 60 невлитых по графу, из
них реально несли работу три — конформанс аутентификации края, приёмка конформанса участия и
снимок незакоммиченных гейтов, спасённый при уборке рабочей копии. Первая версия предиката
объявила «влиты все» и была опровергнута тем, что один из известных мне гейтов в стволе
отсутствовал.

## Асимметрия предиката названа честно: непустая дельта — ОТСУТСТВИЕ доказательства

Пустая дельта — **доказательство** того, что снимать безопасно: содержимое уже в стволе,
каким бы путём оно туда ни попало. Непустая — **отсутствие доказательства**, а не
доказательство обратного, и путать эти два вердикта дороже всего именно здесь: снятие
необратимо.

На конфликте предикат молчит **одинаково в двух разных мирах**:

| мир | что на самом деле |
|---|---|
| ветка несёт своё | работа существует в одном экземпляре — снимать нельзя |
| ствол правил тот же файл ПОСЛЕ вливания | работа поглощена, ветка мусор |

Различить их дельта не может **by construction** — она сравнивает состояния, а не историю.
Поэтому вопрос «не потеряна ли работа ветки» ею **не решается**, и вердикт обязан
называться неустановленным, а не выбираться по правдоподобию. Разбор идёт **по предмету**
(см. §«Границы» выше) либо признаками, сравнивающими содержимое построчно и спрашивающими,
касался ли ствол этого пути вовсе.

## Корпус переписи — тоже её граница, и накопительные линии из него ВЫВЕДЕНЫ

Предикат может быть безупречен и не применяться ни разу — если предмет не попал в обход.
У переписи веток предметы берутся из **локальных** голов, а единственный путь, читающий
ветки origin, **явно пропускает накопительные линии** и к остальным применяет только
дельту слияния.

**Пропуск заведён осознанно и правильно:** линия по построению не влита, и объявлять её
висяком было бы ложной находкой — а прибор, у которого все находки ложные, перестают
читать. Но следствие оказалось **шире замысла**: из-под наблюдения ушёл сам вопрос, ради
которого перепись существует, — «не пропадёт ли работа, если это снять».

**Цена наблюдалась 2026-09-09.** На вопрос «есть ли в предыдущей накопительной линии
работа в единственном экземпляре» перепись ответить не смогла: вердикт собирали вручную
семью признаками, и он занял отдельную полосу. Результат — доказанной работы в
единственном экземпляре **ноль**, **169** файлов в третьем вердикте («поглощение не
установлено»), **577** различимых строк, не встречающихся ни в одном из восьми стволов.
**Ни одного** из этих чисел перепись не производит. Линий на origin при этом **восемь** —
предмет не единичный.

**Чинится не снятием пропуска**, а третьим разделом: линия осматривается **всеми семью**
признаками, но её вердикт формулируется не «снимать / не снимать», а **«что в ней есть
сверх стволов»**; и читается она **по SHA с origin**, а не через локальную копию, которая
может отставать на десятки коммитов — тогда осмотрен будет не тот предмет. Предмет заведён
задачей `PRO-Robotech/kacho-workspace#612`.

Родственный класс — [[absence-of-finding-versus-absence-of-inspection]]: «ноль находок» и
«ноль осмотренного» здесь дают один и тот же пустой раздел.

## Связанное

««0 коммитов впереди» значит смёржена» · [[a-declared-predicate-can-be-wrong-and-then-it-lies-confidently]] ·
«артефактные утверждения проверяются одной командой» · «слияние может молча откатить смёрженное» ·
«две копии на одной ветке готовят откат в индексе» · [[absence-of-finding-versus-absence-of-inspection]] ·
[[KAC/issue-2243]]

#kac #conventions
